var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/analysis/#Analysis-API","page":"Analysis","text":"Analysis tools for computing diagnostics, managing output, and monitoring simulations in real-time.","title":"Analysis API"},{"category":"section","location":"api/analysis/#CFL-Conditions","page":"Analysis","text":"","title":"CFL Conditions"},{"category":"section","location":"api/analysis/#CFL","page":"Analysis","text":"Computes adaptive timesteps based on the Courant-Friedrichs-Lewy (CFL) stability criterion.\n\nThe controller wraps an InitialValueSolver — build the solver first, then the CFL, then hand the CFL to run!, which calls compute_timestep for you before every step.\n\nConstructor:\n\nCFL(\n    solver::InitialValueSolver;\n    initial_dt::Float64=0.01,\n    cadence::Int=1,\n    safety::Float64=0.4,\n    threshold::Float64=0.1,\n    max_change::Float64=2.0,\n    min_change::Float64=0.5,\n    max_dt::Float64=Inf\n)\n\nArguments:\n\nsolver: the InitialValueSolver whose dt will be driven\ninitial_dt: timestep used until the first CFL computation (and whenever no velocity is registered)\ncadence: recompute the timestep every cadence calls; in between, the current dt is reused\nsafety: Safety factor (0 < safety < 1)\nthreshold: Relative-change hysteresis — a proposed dt within threshold of the current one is ignored, so the implicit LHS factorization is not needlessly rebuilt. Set 0.0 to commit every change.\nmax_change: Maximum timestep increase factor per step\nmin_change: Maximum timestep decrease factor per step (a ratio, not a floor on dt)\nmax_dt: Maximum allowed timestep\n\nExamples:\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\n# Standard CFL\ncfl = CFL(solver; initial_dt=1e-3, safety=0.4)\n\n# Conservative settings\ncfl = CFL(solver; initial_dt=1e-3, safety=0.3, max_change=1.2, min_change=0.8)\n\n# Bounded timestep, recomputed every 5 steps\ncfl = CFL(solver; initial_dt=1e-3, safety=0.4, max_dt=0.01, cadence=5)\n\nMethods:","title":"CFL"},{"category":"section","location":"api/analysis/#add_velocity!","page":"Analysis","text":"Register a velocity field for the CFL calculation. The argument must be a VectorField — its components are the advecting velocities, each divided by the grid spacing along its own axis.\n\nadd_velocity!(cfl, u)   # u is a VectorField\n\nRegistering several VectorFields is supported; the controller takes the most restrictive of them:\n\nadd_velocity!(cfl, u)        # advection velocity\nadd_velocity!(cfl, u_wave)   # a second velocity scale — also a VectorField","title":"add_velocity!"},{"category":"section","location":"api/analysis/#compute_timestep","page":"Analysis","text":"Calculate the adaptive timestep. run! calls this automatically when you pass cfl=; call it yourself only if you drive the loop by hand.\n\ndt = compute_timestep(cfl)\n\nReturns: Adaptive timestep satisfying the CFL condition, after max_dt, max_change/min_change and the threshold hysteresis have been applied.\n\nExample — the canonical form, letting run! drive the loop:\n\nrun!(solver; stop_time=t_end, cfl=cfl)   # solver.dt is updated every step\n\nand the manual equivalent. Note that proceed reads its stop criteria off the solver, and a freshly built solver has none (stop_sim_time = Inf, stop_iteration = typemax(Int)) — run! sets them from its keywords, so a hand-written loop has to set them itself or it never terminates:\n\nsolver.stop_sim_time = t_end            # otherwise proceed(solver) is always true\nwhile proceed(solver)\n    solver.dt = compute_timestep(cfl)\n    step!(solver)\nend\n\nCFL Condition:\n\nFor explicit methods:\n\nDelta t leq C left( max sum_i fracu_iDelta x_i right)^-1\n\nwhere C is the safety factor.\n\nProperties:\n\ncfl.safety          # Safety factor\ncfl.max_dt          # Maximum timestep\ncfl.max_change      # Max increase factor per step\ncfl.min_change      # Max decrease factor per step\ncfl.cadence         # Recompute every N calls\ncfl.threshold       # Relative-change hysteresis band\ncfl.current_dt      # Last committed timestep\ncfl.velocities      # Registered VectorFields\n\n","title":"compute_timestep"},{"category":"section","location":"api/analysis/#Output-Handlers","page":"Analysis","text":"","title":"Output Handlers"},{"category":"section","location":"api/analysis/#NetCDF-Output","page":"Analysis","text":"","title":"NetCDF Output"},{"category":"section","location":"api/analysis/#add*file*handler-(recommended)","page":"Analysis","text":"Create a NetCDF output handler and register it with the solver, so run! writes it automatically at its own cadence and closes it at the end — no manual process!/close! in a step loop (easy to forget, silently drops output).\n\nSyntax:\n\nadd_file_handler(\n    base_path::String,\n    solver::InitialValueSolver,\n    vars=<problem namespace>;   # optional; only for STRING-expression tasks (see below)\n    sim_dt=nothing,             # write every sim_dt time units\n    iter=nothing,               # …or every iter steps\n    wall_dt=nothing,            # …or every wall_dt seconds\n    max_writes=nothing,         # writes per file set; a new set is started when it is reached\n    mode=\"overwrite\",           # \"overwrite\" | \"append\"\n)\n\nExample:\n\n# Register handler, add tasks, run — run! handles process!/close! and GPU→CPU\nsnapshots = add_file_handler(\"output/snapshots\", solver; sim_dt=0.1)\nadd_task!(snapshots, u; name=\"u\")          # field object\nadd_task!(snapshots, lap(u); name=\"lap_u\") # operator object (derived field)\n\n# Multiple handlers at different cadences (each auto-registered on the solver)\ncheckpoints = add_file_handler(\"output/checkpoints\", solver; sim_dt=1.0)\nadd_task!(checkpoints, u; name=\"u\")\n\nrun!(solver; stop_time=20.0, cfl=cfl)      # writes both handlers + closes them\n\nA task can be a field object (u) or an operator object built from the exported operator functions (lap(u), Gradient(u, coords), curl(u), …) — see Derived Fields. For reductions use add_mean_task! / add_extrema_task!.\n\nnote: The `vars` namespace\nThe optional third positional vars is a name→field table used only by the (limited) string-expression task parser. It defaults to the problem namespace. Prefer field/operator objects over string expressions — the string parser handles only trivial cases. Pass an explicit Dict(\"name\" => field) only if you rely on string tasks that reference a non-problem field.\n\nOutput files:\n\nFiles are written under an output root, which is dirname(base_path) — or base_path itself if you pass a bare name. So \"output/snapshots\" writes into output/, while \"snapshots\" creates a directory snapshots/.\nEach write-set goes in its own subdirectory of that root: snapshots_s1/, snapshots_s2/, … A new set is started once max_writes is reached.\nSerial: one file per set — snapshots_s1/snapshots_s1.nc\nUnder MPI: one file per process per set — snapshots_s1/snapshots_s1_p0.nc, …_p1.nc, … Combine them with merge_netcdf_files (see below).\n\nReading the output: the files are NetCDF-4 and store everything in groups (vars, time, grids), so a top-level NetCDF.ncread(file, \"u\") will not find the variable — use the group readers (see Loading Data).\n\n","title":"addfilehandler (recommended)"},{"category":"section","location":"api/analysis/#NetCDFFileHandler","page":"Analysis","text":"Direct handler type, when you build it without add_file_handler (e.g. a custom step loop) and pass the Distributor explicitly.\n\nConstructor:\n\nNetCDFFileHandler(\n    base_path::String,\n    dist::Distributor,\n    vars::Dict{String,Any};     # name → field; the namespace for string-expression tasks\n    solver=nothing,             # bind the solver — see the warning below\n    sim_dt=nothing, iter=nothing, wall_dt=nothing,\n    max_writes=nothing,\n    mode=\"overwrite\",           # \"overwrite\" | \"append\"\n    parallel=\"gather\",          # only \"gather\" is implemented — see below\n)\n\nvars must be a Dict — a bare vector of fields is a MethodError (cannot convert Vector{Operand} to Dict{String,Any}). Only \"gather\" is a working parallel mode: in serial it writes one file, under MPI one file per rank. Any other value is accepted silently and merely forces the per-rank file naming (manual_s1/manual_s1_p0.nc) even in serial — there is no MPI-IO or virtual-file write path behind it.\n\nMethods:\n\nprocess!(handler)   # write if the handler's sim_dt/iter/wall_dt cadence is due\nclose!(handler)     # finalize / stamp metadata\n\nwarning: Bind the solver, or the cadence and the timestamps are meaningless\nprocess!(handler) takes the iteration and sim-time it stamps from handler.solver. An unbound handler reports iteration = 0 and sim_time = 0.0 on every call — so an iter= cadence fires every time and every record carries t = 0. Either pass solver=solver to the constructor, or call process!(handler, solver). add_file_handler(path, solver; …) does this for you.\n\nExample (manual loop — only if not using run!):\n\nhandler = NetCDFFileHandler(\"output/manual\", dist, Dict(\"u\" => u, \"T\" => T);\n                            solver=solver, iter=5)\nadd_task!(handler, T; name=\"T\")\n\nsolver.stop_iteration = 10                 # proceed() needs a stop criterion (run! sets it for you)\nprocess!(handler)                          # capture initial state (t = 0)\nwhile proceed(solver)\n    step!(solver)\n    process!(handler)                      # no-op unless cadence is due\nend\nclose!(handler)\n\nThis writes output/manual_s1/manual_s1.nc holding three records — the initial state and iterations 5 and 10.","title":"NetCDFFileHandler"},{"category":"section","location":"api/analysis/#merge*netcdf*files","page":"Analysis","text":"Reconstruct the per-process files written under MPI into one global file. Serial output is already a single file, so there is nothing to merge.\n\nThe merger resolves paths relative to the current working directory and matches on the bare handler name, so run it from the handler's output root — the directory that directly contains <name>_s<N>/. It returns true on success, false if it found no per-process files.\n\n# Handler created as add_file_handler(\"output/snapshots\", solver; …)\n#   → files at  output/snapshots_s1/snapshots_s1_p0.nc, …_p1.nc\ncd(\"output\") do\n    merge_netcdf_files(\n        \"snapshots\";\n        set_number=1,               # which write-set to merge\n        merge_mode=RECONSTRUCT,     # RECONSTRUCT | SIMPLE_CONCAT | DOMAIN_DECOMP\n        cleanup=false,              # delete the per-rank files after merge\n    )                               # → snapshots_s1/snapshots_s1.nc\n\n    # Several handlers at once\n    batch_merge_netcdf([\"snapshots\", \"checkpoints\"])\nend\n\nThe merged file keeps the same group layout as the per-rank files — read it with the group readers below, not with a bare ncread.\n\n","title":"mergenetcdffiles"},{"category":"section","location":"api/analysis/#In-Run-Diagnostics","page":"Analysis","text":"Scalar / time-series diagnostics are computed in a run! callback — an interval => function pair. The function receives the solver; an Int interval fires every N iterations, a Float64 interval every N sim-time units. Inside it, reduce a field's grid data with the MPI-aware helpers global_max / global_min / global_mean / global_sum (identical value on every rank), or use integrate for a true domain integral.\n\nrank = MPI.Initialized() ? MPI.Comm_rank(MPI.COMM_WORLD) : 0\ntimes, ke = Float64[], Float64[]\n\nfunction diagnostics(s)\n    ensure_layout!(T, :g)\n    g = get_grid_data(T)\n    push!(times, s.sim_time)\n    push!(ke, 0.5 * global_mean(dist, g .^ 2))     # ⟨½T²⟩, MPI-reduced\n    rank == 0 && println(\"t=$(s.sim_time)  KE=$(ke[end])\")\nend\n\nrun!(solver; stop_time=10.0, cfl=cfl, callbacks=[10 => diagnostics])   # every 10 steps\n\nrun! also prints a built-in progress line if you pass log_interval=100.\n\n","title":"In-Run Diagnostics"},{"category":"section","location":"api/analysis/#Derived-Fields","page":"Analysis","text":"To write a derived quantity (vorticity, Laplacian, a gradient component, …), build it as an operator object from the exported operator functions and add it as a task — run! evaluates and writes it like any field.\n\nhandler = add_file_handler(\"output/derived\", solver; sim_dt=0.1)\nadd_task!(handler, lap(T);              name=\"lap_T\")     # Laplacian (Δ also works)\nadd_task!(handler, Gradient(T, coords); name=\"grad_T\")    # gradient (vector)\nadd_task!(handler, curl(u);             name=\"vorticity\") # curl of a VectorField\nadd_task!(handler, Differentiate(T, coords[\"x\"], 1); name=\"dTdx\")   # single-axis derivative\n\nExported builders: grad/Gradient, divergence/Divergence, curl/Curl, lap/Laplacian (and the Unicode ∇, Δ), plus Differentiate(field, coord, order) for a single-axis derivative.\n\nwarning: Prefer operator objects over string expressions\nadd_task! also accepts a string (add_task!(h, \"u*u\")), but the string parser handles only the most trivial cases and fails silently — a string task typically lands in the file as a length-1 array of zeros instead of the field. Build operator objects instead.","title":"Derived Fields"},{"category":"section","location":"api/analysis/#Reduction-tasks","page":"Analysis","text":"Reduce a field to a profile or extrema series, written by the same handler:\n\nadd_mean_task!(handler, T; dims=1, name=\"T_mean_x\")   # average over axis 1 → profile\nadd_extrema_task!(handler, T; name=\"T_extrema\")       # writes T_extrema_min and T_extrema_max\n\ndims accepts axis numbers (1, (1, 2)) or coordinate names ((:x, :y)); omit it for a whole-domain mean.\n\n","title":"Reduction tasks"},{"category":"section","location":"api/analysis/#In-Memory-Analysis","page":"Analysis","text":"To keep results in memory (no file I/O), attach a DictionaryHandler to a UnifiedEvaluator and evaluate it from a callback. Results are stored by name and read with handler[name].\n\nev = UnifiedEvaluator(solver)\ndiag = add_dictionary_handler(ev; sim_dt=0.5)   # cadence: sim_dt / cadence / wall_dt\nadd_task!(diag, T; name=\"T\")\nadd_task!(diag, lap(T); name=\"lap_T\")\n\n# Evaluate every step; the handler gates its own write cadence internally.\nrun!(solver; stop_time=10.0, cfl=cfl,\n     callbacks=[1 => s -> evaluate_unified_handlers!(ev, time(), s.sim_time, s.iteration)])\n\nT_now = diag[\"T\"]      # most recent stored evaluation, a plain Array\nkeys(diag)             # task names that have been evaluated at least once\nhaskey(diag, \"lap_T\")\n\nThe handler stores only the latest evaluation of each task, so build a time series by pushing into your own vector from a callback (next section).\n\n","title":"In-Memory Analysis"},{"category":"section","location":"api/analysis/#Statistical-Analysis","page":"Analysis","text":"","title":"Statistical Analysis"},{"category":"section","location":"api/analysis/#Time-averaging","page":"Analysis","text":"There is no separate averager type — accumulate a running mean in a callback:\n\nsnaps = Vector{Array{Float64}}()\nrun!(solver; stop_time=t_end, cfl=cfl, callbacks=[0.1 => function (s)\n    ensure_layout!(T, :g)\n    push!(snaps, copy(get_grid_data(T)))\nend])\nT_time_mean = sum(snaps) ./ length(snaps)","title":"Time averaging"},{"category":"section","location":"api/analysis/#Spatial-averaging","page":"Analysis","text":"For output, use add_mean_task!(handler, field; dims=…). For an in-line value, reduce the grid array directly: global_mean(dist, get_grid_data(field)) for the whole-domain mean, or Statistics.mean(get_grid_data(field); dims=…) over chosen array dimensions.\n\n","title":"Spatial averaging"},{"category":"section","location":"api/analysis/#Spectral-Analysis","page":"Analysis","text":"power_spectrum computes the radially-binned power spectrum of a scalar field. It puts the field in coefficient layout, then bins |f̂(k)|² by wavenumber magnitude, returning a NamedTuple with k (bin centres, physical wavenumber), power, bin_counts and bin_edges.\n\nEvery axis of the field must be a Fourier basis. On a mixed Fourier×Chebyshev field it throws Unsupported field dimensions: 2 with 1 Fourier bases.\n\nps = power_spectrum(T)                  # T::ScalarField on an all-Fourier domain\nps.k                                    # bin centres\nps.power                                # binned power\nps.bin_counts                           # modes per bin\n\n# Options\nps = power_spectrum(T; max_wavenumber=8, radial_average=true)\n\nPass radial_average=false to get the unbinned spectrum as a Dict{Tuple,Float64} keyed by mode index, and binning=LogBinning(bins_per_decade=8) (or LinearBinning(), CustomBinning([…])) to change the bin layout. Plot ps.power against ps.k on log-log axes with your plotting package of choice to compare against a k^(-5/3) reference.\n\nFor a velocity VectorField, energy_spectrum(velocity) and enstrophy_spectrum(velocity) return E(k) and Z(k) = |ω̂(k)|² with the same NamedTuple shape.\n\n","title":"Spectral Analysis"},{"category":"section","location":"api/analysis/#Flow-Diagnostics","page":"Analysis","text":"Flow-property scalars are computed in a callback from the field grid data — use get_grid_data (not .data) and the MPI-aware reducers. Example: a Nusselt-like heat flux ⟨wT⟩ and an RMS-velocity Reynolds number, accumulated over time.\n\nw = u.components[end]                                                # vertical velocity\nNu, Re = Float64[], Float64[]\nfunction flow_diag(s)\n    ensure_layout!(w, :g); ensure_layout!(T, :g)\n    wT   = global_mean(dist, get_grid_data(w) .* get_grid_data(T))   # ⟨wT⟩\n    urms = sqrt(global_mean(dist, get_grid_data(w) .^ 2))            # RMS speed\n    push!(Nu, 1 + wT)\n    push!(Re, urms * L / nu)\nend\nrun!(solver; stop_time=t_end, cfl=cfl, callbacks=[10 => flow_diag])\n\nDerivative-based quantities (shear, gradient Richardson number, …) build the derivative as an operator object — Differentiate(T, coords[\"z\"], 1), Gradient, lap, … — and add it as a task or evaluate it via the in-memory handler above.\n\nWhole-field diagnostics are also available directly: total_kinetic_energy(u) and total_enstrophy(u) return a Float64, while kinetic_energy(u) and enstrophy(u) return the corresponding ScalarFields.","title":"Flow Diagnostics"},{"category":"section","location":"api/analysis/#Probe-points","page":"Analysis","text":"Sample a field at a fixed grid index inside a callback and append to a time series:\n\nidx = (8, 16)                       # grid index to sample\nt_probe, T_probe = Float64[], Float64[]\nrun!(solver; stop_time=t_end, cfl=cfl, callbacks=[10 => function (s)\n    ensure_layout!(T, :g)\n    push!(t_probe, s.sim_time)\n    push!(T_probe, get_grid_data(T)[idx...])\nend])\n\n(Under MPI get_grid_data is the rank-local slab — guard the sample by which rank owns idx, or write the field with a handler and probe the merged output.)","title":"Probe points"},{"category":"section","location":"api/analysis/#Progress-monitoring","page":"Analysis","text":"run! prints a built-in progress line every log_interval iterations; richer monitoring goes in a callback (see In-Run Diagnostics):\n\nrun!(solver; stop_time=t_end, cfl=cfl, log_interval=100)\n\n","title":"Progress monitoring"},{"category":"section","location":"api/analysis/#Data-Post-Processing","page":"Analysis","text":"","title":"Data Post-Processing"},{"category":"section","location":"api/analysis/#Merging-Parallel-Output","page":"Analysis","text":"After a simulation with MPI, reconstruct the per-rank files from the handler's output root (see mergenetcdffiles):\n\nif MPI.Comm_rank(MPI.COMM_WORLD) == 0\n    cd(\"output\") do\n        merge_netcdf_files(\"snapshots\"; cleanup=true)   # → output/snapshots_s1/snapshots_s1.nc\n    end\nend","title":"Merging Parallel Output"},{"category":"section","location":"api/analysis/#Loading-Data","page":"Analysis","text":"Tarang writes NetCDF-4 with groups: field data lives in the vars group, timestamps in time, coordinate axes in grids. A bare NetCDF.ncread(file, \"T\") therefore fails with \"does not have a variable named T\" — read through the group helpers instead.\n\nfile = \"output/snapshots_s1/snapshots_s1.nc\"\n\nTarang.group_variable_names(file, \"vars\")     # e.g. [\"T\", \"u\", \"lap_T\"]\n\nT_data = group_ncread(file, \"vars\", \"T\")      # (write, x, y)\nu_data = group_ncread(file, \"vars\", \"u\")      # (write, component, x, y)\nt      = group_ncread(file, \"time\", \"sim_time\")\nx      = group_ncread(file, \"grids\", \"x\")\n\nT_last = T_data[end, :, :]                    # last written snapshot…\nt[end]                                        # …and the time it was written at\n\nFeed T_last to your plotting package of choice (Tarang does not depend on one).\n\n","title":"Loading Data"},{"category":"section","location":"api/analysis/#Complete-Example","page":"Analysis","text":"using Tarang, MPI\n\nMPI.Init()\n\n# ... setup problem, fields, and solver ...\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\n# CFL condition — takes the solver, and run! applies it every step\ncfl = CFL(solver; initial_dt=1e-3, safety=0.4, max_dt=0.01)\nadd_velocity!(cfl, u)                       # u::VectorField\n\n# Output handler — created with the solver, so run! auto-writes + closes it.\n# Tasks (not the namespace) are what gets written, so add one per field.\nsnapshots = add_file_handler(\"output/snapshots\", solver; sim_dt=0.1)\nadd_task!(snapshots, u; name=\"u\")\nadd_task!(snapshots, p; name=\"p\")\nadd_task!(snapshots, T; name=\"T\")\n\n# Custom diagnostics live in a callback (receives the solver). global_max is\n# MPI-reduced, so the printed value is identical on every rank.\nfunction diagnostics(s)\n    ensure_layout!(T, :g)\n    max_T = global_max(dist, abs.(get_grid_data(T)))\n    MPI.Comm_rank(MPI.COMM_WORLD) == 0 &&\n        println(\"t=$(s.sim_time): dt=$(s.dt), max|T|=$max_T\")\nend\n\n# Time integration — run! drives the loop: CFL-adaptive dt, auto-writes\n# `snapshots` every sim_dt, fires `diagnostics` every 1000 steps, closes at end.\nrun!(solver; stop_time=10.0, cfl=cfl, callbacks=[1000 => diagnostics])\n\n# Reconstruct the per-rank files into one global file, from the output root\nif MPI.Comm_rank(MPI.COMM_WORLD) == 0\n    cd(\"output\") do\n        merge_netcdf_files(\"snapshots\"; cleanup=true)\n    end\nend\n\nMPI.Finalize()\n\n","title":"Complete Example"},{"category":"section","location":"api/analysis/#See-Also","page":"Analysis","text":"Solvers: Integration methods\nFields: Field operations for analysis\nTutorial: Analysis: Detailed examples","title":"See Also"},{"category":"section","location":"api/bases/#Bases-API","page":"Bases","text":"Spectral bases define how functions are represented in each coordinate direction. Tarang.jl provides Fourier, Chebyshev, and Legendre bases for different boundary conditions.","title":"Bases API"},{"category":"section","location":"api/bases/#Docstrings","page":"Bases","text":"","title":"Docstrings"},{"category":"section","location":"api/bases/#Overview","page":"Bases","text":"Each coordinate dimension requires a spectral basis that determines:\n\nRepresentation: How functions are expanded (Fourier series, polynomial series, etc.)\nBoundary conditions: Periodic, non-periodic, or special constraints\nGrid points: Where functions are evaluated (collocation points)\nDerivatives: How differentiation is computed in spectral space","title":"Overview"},{"category":"section","location":"api/bases/#Basis-Selection-Guide","page":"Bases","text":"Basis Type Use Case Boundary Conditions Grid Points\nRealFourier Periodic, real-valued data Periodic Uniform\nComplexFourier Periodic, complex data Periodic Uniform\nChebyshevT Bounded, smooth Dirichlet/Neumann Gauss-Lobatto (includes endpoints)\nChebyshevU Bounded, special BCs Custom Gauss (interior only)\nLegendre Bounded, alternative Dirichlet/Neumann Gauss-Legendre (interior only)\n\nAll examples on this page assume a coordinate system and a distributor:\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nx, z   = coords[\"x\"], coords[\"z\"]\n\n","title":"Basis Selection Guide"},{"category":"section","location":"api/bases/#Fourier-Bases","page":"Bases","text":"","title":"Fourier Bases"},{"category":"section","location":"api/bases/#RealFourier","page":"Bases","text":"Real-valued Fourier basis for periodic coordinates with real data.\n\nConstructor:\n\nRealFourier(\n    coord::Coordinate;\n    size::Int=32,\n    bounds::Tuple{Float64,Float64}=(0.0, 2π),\n    dealias::Float64=3/2,\n    dtype=Float64\n)\n\nArguments:\n\ncoord: Coordinate object for this direction\nsize: Number of grid points / modes along the axis\nbounds: Domain boundaries (min, max), as Float64. Note 2π is a Float64 but a bare π is an Irrational, so bounds=(0.0, π) raises a TypeError — write (0.0, Float64(π)).\ndealias: Dealiasing padding factor (default 3/2 = the 3/2 rule; 1.0 disables dealiasing). See Dealiasing below — this is not a cutoff fraction.\n\nExamples:\n\n# Basic real Fourier basis (dealiased by default)\nbasis = RealFourier(x; size=16, bounds=(0.0, 2π))\n\n# Dealiasing explicitly disabled\nbasis = RealFourier(x; size=16, bounds=(0.0, 2π), dealias=1.0)\n\n# Custom domain length\nbasis = RealFourier(x; size=32, bounds=(0.0, 4.0))\n\nEven size is recommended (fastest FFTs, symmetric spectrum), but odd sizes are supported and round-trip exactly.\n\nProperties: basis metadata lives in basis.meta; the basis object itself has no size, bounds or dealias field.\n\nbasis = RealFourier(x; size=32, bounds=(0.0, 4.0))\n\nbasis.meta.element_label   # \"x\"          — the coordinate this basis belongs to\nbasis.meta.size            # 32           — number of modes / grid points\nbasis.meta.bounds          # (0.0, 4.0)   — (min, max)\nbasis.meta.dealias         # 1.5          — dealiasing padding factor\nbasis.meta.dtype           # Float64\n\nGrid Points:\n\n# This rank's grid points for the axis (uniform spacing, excludes the right endpoint).\n# The third argument is the SCALE factor, not an axis index: 1 = the basis's own\n# resolution, 1.5 would return the 3/2-padded grid. (The axis is looked up from the basis.)\nxg = local_grid(basis, dist, 1)\n# 32-element Vector{Float64}: [0.0, 0.125, 0.25, ..., 3.875]  for bounds=(0.0, 4.0)\n\n# For several bases at once — this is the idiom to use, it is correct under MPI too\nz_basis = ChebyshevT(z; size=12, bounds=(0.0, 1.0))\nxg, zg  = local_grids(dist, basis, z_basis)\n\nlocal_grid/local_grids return each rank's local slice, so the same code works serially and distributed. Grid spacing for a whole domain is grid_spacing(domain).\n\nWavenumbers:\n\nk = wavenumbers(basis)\n# RealFourier native storage is cos/sin interleaved, so k repeats:\n# [0, 1, 1, 2, 2, ..., N/2] * (2π/L)   (length N)\n\nCoefficient arrays produced by the transforms use the real-to-complex (rfft) half spectrum of length N÷2+1; the matching wavenumbers are Tarang.wavenumbers_rfft(basis) ([0, 1, 2, ..., N/2] * 2π/L).\n\nMemory Savings: RealFourier uses real-to-complex FFT, saving ~50% memory compared to ComplexFourier.\n\n","title":"RealFourier"},{"category":"section","location":"api/bases/#ComplexFourier","page":"Bases","text":"Complex-valued Fourier basis for periodic coordinates.\n\nConstructor:\n\nComplexFourier(\n    coord::Coordinate;\n    size::Int=32,\n    bounds::Tuple{Float64,Float64}=(0.0, 2π),\n    dealias::Float64=3/2,\n    dtype=ComplexF64\n)\n\nArguments: Same as RealFourier, except dtype defaults to ComplexF64.\n\nExamples:\n\n# Complex Fourier basis\nbasis = ComplexFourier(x; size=16, bounds=(0.0, 2π))\n\nUse Cases:\n\nComplex-valued fields (e.g., quantum mechanics)\nIncompatible with RealFourier optimizations\nMore general but uses more memory\n\nWavenumbers:\n\nk = wavenumbers(basis)\n# FFT ordering: [0, 1, ..., N/2-1, -N/2, ..., -1] * (2π/L)   (length N)\n\n","title":"ComplexFourier"},{"category":"section","location":"api/bases/#Chebyshev-Bases","page":"Bases","text":"","title":"Chebyshev Bases"},{"category":"section","location":"api/bases/#ChebyshevT","page":"Bases","text":"Chebyshev polynomials of the first kind for bounded, non-periodic coordinates.\n\nConstructor:\n\nChebyshevT(\n    coord::Coordinate;\n    size::Int=32,\n    bounds::Tuple{Float64,Float64}=(-1.0, 1.0),\n    dealias::Float64=1.0,\n    dtype=Float64\n)\n\nArguments:\n\ncoord: Coordinate object\nsize: Number of modes (polynomial degree + 1)\nbounds: Domain boundaries (min, max)\ndealias: accepted, but ignored on non-Fourier axes (only Fourier axes are truncated)\n\nExamples:\n\n# Chebyshev basis for vertical direction\nbasis = ChebyshevT(z; size=12, bounds=(0.0, 1.0))\n\n# Canonical domain\nbasis = ChebyshevT(z; size=32, bounds=(-1.0, 1.0))\n\nProperties: as for the Fourier bases, everything lives in basis.meta:\n\nbasis.meta.element_label   # \"z\"\nbasis.meta.size            # number of modes\nbasis.meta.bounds          # (min, max)\nbasis.meta.dtype           # Float64\n\nGrid Points (Gauss-Lobatto):\n\nbasis = ChebyshevT(z; size=12, bounds=(0.0, 1.0))\nzg = local_grid(basis, dist, 1)\n# Points clustered near boundaries, ascending, endpoints INCLUDED:\n# zg[1] == 0.0 and zg[end] == 1.0\n\n# In canonical [-1, 1]:\n# ξ_k = -cos(π*k / (N-1)) for k = 0, 1, ..., N-1\n#\n# Mapped to physical domain [a, b]:\n# z_k = (a + b)/2 + (b - a)/2 * ξ_k\n\nPolynomial Expansion:\n\nFunctions are expanded as:\n\nf(x) = sum_n=0^N-1 a_n T_n(x)\n\nwhere T_n(x) is the nth Chebyshev polynomial.\n\nBoundary Conditions:\n\nValues at boundaries are grid points (includes endpoints)\nWell-suited for Dirichlet boundary conditions\nCan handle Neumann conditions with tau method\n\n","title":"ChebyshevT"},{"category":"section","location":"api/bases/#ChebyshevU","page":"Bases","text":"Chebyshev polynomials of the second kind.\n\nConstructor:\n\nChebyshevU(\n    coord::Coordinate;\n    size::Int=32,\n    bounds::Tuple{Float64,Float64}=(-1.0, 1.0)\n)\n\nUse Cases:\n\nSpecific boundary condition types\nAlternative to ChebyshevT for some problems\nLess commonly used than ChebyshevT\n\nGrid Points (Gauss):\n\nbasis = ChebyshevU(z; size=12, bounds=(0.0, 1.0))\nzg = local_grid(basis, dist, 1)\n# Chebyshev-Gauss points, endpoints EXCLUDED (zg[1] ≈ 0.0043, zg[end] ≈ 0.9957)\n# ξ_k = -cos(π*(k + 1/2) / N) for k = 0, 1, ..., N-1\n\n","title":"ChebyshevU"},{"category":"section","location":"api/bases/#Legendre-Basis","page":"Bases","text":"","title":"Legendre Basis"},{"category":"section","location":"api/bases/#Legendre","page":"Bases","text":"Legendre polynomials for bounded coordinates.\n\nConstructor:\n\nLegendre(\n    coord::Coordinate;\n    size::Int=32,\n    bounds::Tuple{Float64,Float64}=(-1.0, 1.0)\n)\n\nArguments: Same as ChebyshevT\n\nExamples:\n\n# Legendre basis\nbasis = Legendre(z; size=12, bounds=(0.0, 1.0))\n\nGrid Points (Gauss-Legendre):\n\nzg = local_grid(basis, dist, 1)\n# Roots of P_N: interior nodes, endpoints EXCLUDED\n# (zg[1] ≈ 0.0092, zg[end] ≈ 0.9908 for bounds=(0.0, 1.0), size=12)\n\nComparison with Chebyshev:\n\nSimilar accuracy and convergence properties\nDifferent grid point distribution: ChebyshevT includes the endpoints, Legendre does not\nChoice often based on problem-specific considerations\nLegendre may be preferred for some weight functions\nLegendre is serial only: MPI runs raise an error (MPI parallelization is not yet supported for Legendre bases)\n\n","title":"Legendre"},{"category":"section","location":"api/bases/#Compound-Bases","page":"Bases","text":"For multi-dimensional problems, combine bases for each direction.\n\nExample: 2D Rayleigh-Bénard (using the coords / dist from the top of this page):\n\n# Periodic horizontal direction\nx_basis = RealFourier(coords[\"x\"]; size=32, bounds=(0.0, 4.0))\n\n# Bounded vertical direction with walls\nz_basis = ChebyshevT(coords[\"z\"]; size=16, bounds=(0.0, 1.0))\n\n# Create domain with both bases\ndomain = Domain(dist, (x_basis, z_basis))\n\nExample: 3D Channel Flow:\n\ncoords3 = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist3   = Distributor(coords3; dtype=Float64, device=CPU())\n\n# Streamwise (periodic)\nxb3 = RealFourier(coords3[\"x\"]; size=16, bounds=(0.0, 2π))\n\n# Spanwise (periodic)\nyb3 = RealFourier(coords3[\"y\"]; size=16, bounds=(0.0, Float64(π)))\n\n# Wall-normal (bounded)\nzb3 = ChebyshevT(coords3[\"z\"]; size=12, bounds=(0.0, 1.0))\n\ndomain3 = Domain(dist3, (xb3, yb3, zb3))\n\nnote: Basis order under MPI\nSerially the Chebyshev axis may sit anywhere. Under MPI it must come first: the trailing axes are the decomposed ones, and a Chebyshev axis cannot be split (its DCT needs the whole axis on each rank). Putting it last raises an error telling you to reorder. So the distributed form of the example above is CartesianCoordinates(\"z\", \"x\") with Domain(dist, (z_basis, x_basis)).\n\n","title":"Compound Bases"},{"category":"section","location":"api/bases/#Grid-and-Spectral-Space","page":"Bases","text":"","title":"Grid and Spectral Space"},{"category":"section","location":"api/bases/#Transforms","page":"Bases","text":"A field carries a layout flag; ensure_layout! moves it between grid (:g) and coefficient (:c) space, transforming only when needed.\n\ndomain = Domain(dist, (x_basis, z_basis))\nT = ScalarField(domain, \"T\")\n\n# Move to grid space and edit in physical space\nensure_layout!(T, :g)\ngrid_data = get_grid_data(T)          # (32, 16) Array{Float64}\ngrid_data[10, 8] = 1.0\n\n# Move back to coefficient space\nensure_layout!(T, :c)\ncoeff_data = get_coeff_data(T)        # (17, 16) Array{ComplexF64} — rfft half spectrum\n\nThe same field can also be built from the distributor and a basis tuple directly: ScalarField(dist, \"T\", (x_basis, z_basis)).\n\nwarning: One basis configuration per Distributor\nThe transform plan lives on the Distributor and is rebuilt for the domain most recently constructed on it. If a script needs two domains with different bases, give each its own Distributor — otherwise fields belonging to the earlier domain are transformed with the later domain's plan.","title":"Transforms"},{"category":"section","location":"api/bases/#Grid-Point-Access","page":"Bases","text":"# Per-axis local grid vectors\nxg, zg = local_grids(dist, x_basis, z_basis)\n\n# Initialize a field by broadcasting (xg is a column, zg' a row)\nensure_layout!(T, :g)\nget_grid_data(T) .= sin.(2π/4 .* xg) .* sin.(π .* zg')   # L_x = 4, so 2π/4 is the fundamental\nensure_layout!(T, :c)\n\nBroadcasting against local_grids is the idiom to use: it is correct serially and under MPI, where each rank owns only a slab of the grid. (set!(field, ::Function) builds the global meshgrid and therefore only works serially.)\n\n","title":"Grid Point Access"},{"category":"section","location":"api/bases/#Differentiation","page":"Bases","text":"Spectral differentiation is computed by applying operators in spectral space.","title":"Differentiation"},{"category":"section","location":"api/bases/#Fourier-Differentiation","page":"Bases","text":"For Fourier bases, differentiation is multiplication by ik:\n\nfracddx left(sum a_k e^ikxright) = sum (ik) a_k e^ikx\n\nIn equations you write the derivative as ∂x(u). Standalone, the same derivative is the Differentiate operator (T here is the field from the section above, holding sin(2πx/4)·sin(πz)):\n\ndTdx = evaluate(Differentiate(T, coords[\"x\"], 1), :g)   # returns a ScalarField\n\nexact = (2π/4) .* cos.(2π/4 .* xg) .* sin.(π .* zg')\nmaximum(abs.(get_grid_data(dTdx) .- exact))             # 6.3e-15 — spectrally accurate","title":"Fourier Differentiation"},{"category":"section","location":"api/bases/#Chebyshev-Differentiation","page":"Bases","text":"For Chebyshev bases, differentiation uses recurrence relations:\n\nfracdT_ndx = n U_n-1(x)\n\nImplemented as sparse matrix multiplication in spectral space.\n\nExample — the same operator, along the Chebyshev axis:\n\ndTdz = evaluate(Differentiate(T, coords[\"z\"], 1), :g)\n\nexact = π .* sin.(2π/4 .* xg) .* cos.(π .* zg')\nmaximum(abs.(get_grid_data(dTdz) .- exact))             # 2.5e-13\n\nIn a problem the Chebyshev derivative normally enters through lap/div/grad on the implicit side, written as \"∂t(T) - kappa*lap(T) = 0\", with the wall conditions supplied by add_bc!. Because each boundary condition consumes an equation slot, such a problem also needs tau variables — see Tau Method.\n\nwarning: Keep non-Fourier derivatives off the explicit side under MPI\nA Chebyshev/Legendre derivative in the explicit (RHS) part of an equation cannot be evaluated distributed — each rank owns only part of that axis. Serially it is fine; under MPI, move the term to the implicit (L) side.\n\n","title":"Chebyshev Differentiation"},{"category":"section","location":"api/bases/#Dealiasing","page":"Bases","text":"Dealiasing prevents aliasing errors in nonlinear terms.","title":"Dealiasing"},{"category":"section","location":"api/bases/#dealias-is-a-padding-factor","page":"Bases","text":"dealias is the padding factor, not a cutoff fraction: dealias=3/2 gives the 3/2 rule (equivalently, 2/3-rule truncation), and any value ≤ 1 switches dealiasing off. The retained band is |k| ≤ min(floor(N / (2*dealias)), (N-1)÷3).\n\n# The 3/2 rule — this is already the RealFourier/ComplexFourier default\nbasis = RealFourier(x; size=32, bounds=(0.0, 2π), dealias=3/2)\n\nCutoffs actually applied for RealFourier(size=32):\n\ndealias= modes retained effect\n3/2 (default) wavenumbers up to 10 the 3/2 / 2-3 rule\n2.0 wavenumbers up to 8 stronger truncation\n1.0 all dealiasing off\n2/3 all dealiasing off (it is ≤ 1, so not what you want)\n\nThe factor is ignored on non-Fourier axes: passing dealias=3/2 to a Chebyshev or Legendre basis is harmless but truncates nothing.","title":"dealias is a padding factor"},{"category":"section","location":"api/bases/#When-to-Dealias","page":"Bases","text":"Always: For products of fields (e.g., u*∂x(u))\nOptional: For linear problems\nRecommend: 3/2 rule for most problems (the default)\nHigher: dealias=2.0 for very nonlinear problems\n\n","title":"When to Dealias"},{"category":"section","location":"api/bases/#Resolution-Guidelines","page":"Bases","text":"","title":"Resolution Guidelines"},{"category":"section","location":"api/bases/#Fourier-Bases-2","page":"Bases","text":"Rule of thumb: resolve the smallest scales in the flow. With η = (ν³/ε)^(1/4) the Kolmogorov scale and L the domain length, you need roughly N ≈ L/η modes per direction.\n\nLow Re flow: 64-128 modes\nModerate Re: 256-512 modes\nHigh Re/turbulent: 1024+ modes","title":"Fourier Bases"},{"category":"section","location":"api/bases/#Chebyshev/Legendre-Bases","page":"Bases","text":"Exponential convergence for smooth functions:\n\nSmooth flows: 32-64 modes\nBoundary layers: 64-128 modes\nSteep gradients: 128-256 modes\n\n# Example: Rayleigh-Bénard at Ra=10^6\nz_basis_hires = ChebyshevT(z; size=64, bounds=(0.0, 1.0))\n\n","title":"Chebyshev/Legendre Bases"},{"category":"section","location":"api/bases/#Advanced-Topics","page":"Bases","text":"","title":"Advanced Topics"},{"category":"section","location":"api/bases/#Grid-sizes-and-padding","page":"Bases","text":"A basis has one size, fixed at construction (basis.meta.size); there is no separate grid size to set on it. The padding used for nonlinear products comes from the dealias factor. A field can be evaluated on a padded grid with set_scales!:\n\nTpad = ScalarField(domain, \"Tpad\")\nensure_layout!(Tpad, :g)\nget_grid_data(Tpad) .= sin.(2π/4 .* xg) .* sin.(π .* zg')\n\nsize(get_grid_data(Tpad))     # (32, 16)\nset_scales!(Tpad, 1.5)\nsize(get_grid_data(Tpad))     # (48, 24) — 3/2-padded grid\n\n# The values are the same function, spectrally resampled onto the finer nodes:\nxg15, zg15 = local_grids(dist, x_basis, z_basis; scales=1.5)\nmaximum(abs.(get_grid_data(Tpad) .- sin.(2π/4 .* xg15) .* sin.(π .* zg15')))   # 3.9e-15\n\nwarning: On a mixed Fourier-Chebyshev field, `set_scales!` does not reverse\nPadding works, but scaling such a field back down — set_scales!(Tpad, 1.0) — throws TypeError: in setfield!, expected Matrix{Float64}, got a value of type Matrix{ComplexF64}. The forward transform of a padded grid stores a padded half-spectrum (here 25×16 rather than the base 17×16), and the backward transform cannot map that back onto the base real grid. Give the padded output its own field instead of rescaling one you still need. Pure-Fourier fields rescale in both directions.","title":"Grid sizes and padding"},{"category":"section","location":"api/bases/#Domain-shapes","page":"Bases","text":"global_shape(domain, :g)   # (32, 16) — grid shape\nglobal_shape(domain, :c)   # (17, 16) — coefficient shape (rfft halves the first axis)\nlocal_shape(domain, :g)    # this rank's slab\ngrid_spacing(domain)       # per-axis spacing","title":"Domain shapes"},{"category":"section","location":"api/bases/#Spectral-Accuracy","page":"Bases","text":"Inspect the coefficient magnitudes to check that the resolution is sufficient — they should decay to the noise floor well before the last mode. (Plot decay on a log y-axis with your plotting package of choice.)\n\nensure_layout!(T, :c)\ndecay = vec(maximum(abs.(get_coeff_data(T)); dims=2))   # per x-mode, 17 entries\n# T = sin(2πx/4)·sin(πz) -> decay[1:5] = [0.0, 7.99, 0.0, 0.0, 0.0]\n# and decay[end] = 8.8e-17: all the energy sits in one mode, resolution is ample\n\n","title":"Spectral Accuracy"},{"category":"section","location":"api/bases/#Performance-Considerations","page":"Bases","text":"Power-of-2 sizes: Use N = 2^m for fastest FFTs\nMemory alignment: FFTW performs best with aligned arrays\nFFTW planning: Use FFTWMEASURE or FFTWPATIENT for optimal plans\nGrid size: Larger grids = more memory, slower transforms\nDealiasing: Increases computational cost by ~50%\n\n","title":"Performance Considerations"},{"category":"section","location":"api/bases/#See-Also","page":"Bases","text":"Coordinates: Coordinate systems for bases\nDomains: Combining bases into domains\nFields: Creating fields on bases\nOperators: Differential operators","title":"See Also"},{"category":"type","location":"api/bases/#Tarang.RealFourier","page":"Bases","text":"RealFourier <: FourierBasis\n\nReal Fourier sine/cosine basis (msin = -sin convention). Modes: [cos(0x), cos(1x), -sin(1x), cos(2x), -sin(2*x), ...]\n\n\n\n\n\n","title":"Tarang.RealFourier"},{"category":"type","location":"api/bases/#Tarang.ComplexFourier","page":"Bases","text":"ComplexFourier <: FourierBasis\n\nComplex Fourier exponential basis. Modes: [exp(i0x), exp(i1x), exp(-i1x), exp(i2x), exp(-i2x), ...]\n\n\n\n\n\n","title":"Tarang.ComplexFourier"},{"category":"type","location":"api/bases/#Tarang.ChebyshevT","page":"Bases","text":"ChebyshevT <: JacobiBasis\n\nChebyshev polynomials of the first kind T_n(x). Equivalent to Ultraspherical(alpha=0) or Jacobi(a=-1/2, b=-1/2).\n\n\n\n\n\n","title":"Tarang.ChebyshevT"},{"category":"type","location":"api/bases/#Tarang.ChebyshevU","page":"Bases","text":"ChebyshevU <: JacobiBasis\n\nChebyshev polynomials of the second kind U_n(x). Equivalent to Ultraspherical(alpha=1) or Jacobi(a=1/2, b=1/2).\n\n\n\n\n\n","title":"Tarang.ChebyshevU"},{"category":"type","location":"api/bases/#Tarang.Legendre","page":"Bases","text":"Legendre <: JacobiBasis\n\nLegendre polynomial basis P_n(x). Equivalent to Jacobi(a=0, b=0).\n\n\n\n\n\n","title":"Tarang.Legendre"},{"category":"section","location":"api/fields/#Fields-API","page":"Fields","text":"Fields represent variables in your PDE system. Tarang.jl provides scalar, vector, and tensor fields with CPU, GPU, and MPI support.","title":"Fields API"},{"category":"section","location":"api/fields/#Quick-Field-Creation","page":"Fields","text":"The simplest way to create fields is from a domain:\n\ndomain = PeriodicDomain(128, 128)\nT = ScalarField(domain, \"temperature\")\nu = VectorField(domain, \"velocity\")","title":"Quick Field Creation"},{"category":"section","location":"api/fields/#ScalarField","page":"Fields","text":"","title":"ScalarField"},{"category":"section","location":"api/fields/#Constructors","page":"Fields","text":"# From domain (recommended)\nScalarField(domain::Domain, name::String; dtype=domain.dist.dtype)\n\n# Full form\nScalarField(distributor::Distributor, name::String, bases::Tuple, dtype::Type=Float64)\n\nExample:\n\n# Simple (using convenience API)\ndomain = ChannelDomain(256, 64; Lx=4.0, Lz=1.0)\nT = ScalarField(domain, \"T\")\n\n# Explicit (when you need full control)\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords)\nxb = RealFourier(coords[\"x\"]; size=256, bounds=(0.0, 4.0))\nzb = ChebyshevT(coords[\"z\"]; size=64, bounds=(0.0, 1.0))\nT = ScalarField(dist, \"T\", (xb, zb))","title":"Constructors"},{"category":"section","location":"api/fields/#Data-Access","page":"Fields","text":"# Grid-space data (physical values)\nensure_layout!(field, :g)\ndata = get_grid_data(field)         # AbstractArray\n\n# Coefficient-space data (spectral coefficients)\nensure_layout!(field, :c)\ndata = get_coeff_data(field)        # AbstractArray\n\n# Shorthand (auto-transforms if needed)\ndata_g = field[\"g\"]                  # Grid data\ndata_c = field[\"c\"]                  # Coeff data\n\n# Set data\nset_grid_data!(field, array)\nset_coeff_data!(field, array)","title":"Data Access"},{"category":"section","location":"api/fields/#Layout-Management","page":"Fields","text":"Fields live in either grid space (:g) or coefficient space (:c):\n\nensure_layout!(field, :g)           # Transform to grid space if needed\nensure_layout!(field, :c)           # Transform to coefficient space if needed\nforward_transform!(field)           # Grid -> coefficients\nbackward_transform!(field)          # Coefficients -> grid","title":"Layout Management"},{"category":"section","location":"api/fields/#Initial-Conditions","page":"Fields","text":"# Fill with random data\nfill_random!(field, \"g\"; seed=42, distribution=\"normal\", scale=1.0)\n\n# Manual initialization\nensure_layout!(field, :g)\ndata = get_grid_data(field)\ndata .= sin.(x_grid) .* cos.(y_grid)","title":"Initial Conditions"},{"category":"section","location":"api/fields/#VectorField","page":"Fields","text":"","title":"VectorField"},{"category":"section","location":"api/fields/#Constructors-2","page":"Fields","text":"# From domain (recommended)\nVectorField(domain::Domain, name::String; dtype=domain.dist.dtype)\n\n# Full form\nVectorField(dist::Distributor, coordsys::CoordinateSystem, name::String, bases::Tuple)\n\nExample:\n\ndomain = PeriodicDomain(128, 128, 128)\nu = VectorField(domain, \"velocity\")\n\n# Access components\nux = u.components[1]    # x-component (ScalarField)\nuy = u.components[2]    # y-component\nuz = u.components[3]    # z-component\n\n# Or use indexing\nux = u[1]","title":"Constructors"},{"category":"section","location":"api/fields/#Vector-Operations","page":"Fields","text":"# Dot product: u . v\nresult = evaluate(dot(u, v))    # or: u . v\n\n# Cross product (3D): u x v\nresult = evaluate(cross(u, v))  # or: u x v\n\n# Gradient of scalar\ngrad_T = evaluate(grad(T))      # Returns VectorField\n\n# Divergence of vector\ndiv_u = evaluate(div(u))        # Returns ScalarField","title":"Vector Operations"},{"category":"section","location":"api/fields/#TensorField","page":"Fields","text":"# Create rank-2 tensor\ntau = TensorField(domain, \"stress\")\n\n# Access components\ntau_xx = tau.components[1, 1]\ntau_xy = tau.components[1, 2]","title":"TensorField"},{"category":"section","location":"api/fields/#GPU-Fields","page":"Fields","text":"All field operations work on GPU arrays when the domain uses device=GPU():\n\nusing CUDA\ndomain = PeriodicDomain(256, 256; device=GPU())\nu = ScalarField(domain, \"u\")    # Data lives on GPU\nforward_transform!(u)            # Uses cuFFT","title":"GPU Fields"},{"category":"section","location":"api/fields/#Type-Parameters","page":"Fields","text":"Fields are parametric: ScalarField{T, S} where:\n\nT: element type (Float64, Float32, etc.)\nS: storage backend (SerialFieldStorage, TransposableFieldStorage)\n\nThis enables the compiler to specialize operations based on element type and storage strategy.","title":"Type Parameters"},{"category":"section","location":"pages/temporal_filters/#Temporal-Filters-for-Lagrangian-Averaging","page":"Temporal Filters for Lagrangian Averaging","text":"Efficient computation of Lagrangian means using exponential time filters.\n\nReference: Minz, Baker, Kafiabad & Vanneste (2025). \"Efficient Lagrangian averaging with exponential filters\". Phys. Rev. Fluids 10, 074902.\n\n","title":"Temporal Filters for Lagrangian Averaging"},{"category":"section","location":"pages/temporal_filters/#Quick-Summary","page":"Temporal Filters for Lagrangian Averaging","text":"What: Temporal filters that separate fast waves from slow mean flowsWhy: Traditional averaging requires storing entire time history; these filters need only the current stateWhich filter to use:ExponentialMean - Simple, low memory (1 array), moderate filtering\nButterworthFilter - Sharp cutoff, 2x memory (2 arrays), excellent filtering\nLagrangianFilter - Full Lagrangian averaging with particle displacement tracking\n\n","title":"Quick Summary"},{"category":"section","location":"pages/temporal_filters/#Quick-Start","page":"Temporal Filters for Lagrangian Averaging","text":"","title":"Quick Start"},{"category":"section","location":"pages/temporal_filters/#Basic-Usage-(3-lines-of-code)","page":"Temporal Filters for Lagrangian Averaging","text":"using Tarang\n\n# 1. Create a filter for your field size\nfilter = ButterworthFilter((64, 64); α=0.5)  # α = 1/averaging_time\n\n# 2. In your time loop, update the filter\nupdate!(filter, field_data, dt)\n\n# 3. Get the filtered mean anytime\nmean_field = get_mean(filter)","title":"Basic Usage (3 lines of code)"},{"category":"section","location":"pages/temporal_filters/#Complete-Working-Example","page":"Temporal Filters for Lagrangian Averaging","text":"using Tarang\n\n# Setup: 2D field with slow trend + fast oscillations\nNx, Ny = 64, 64\nα = 0.5      # Averaging time = 1/α = 2 time units\ndt = 0.01\nnsteps = 500\n\n# Create Butterworth filter (better than exponential for sharp separation)\nfilter = ButterworthFilter((Nx, Ny); α=α)\n\n# Simulate: field = slow_trend + fast_oscillation\nfor step in 1:nsteps\n    t = step * dt\n\n    # Your field: slow part (want to keep) + fast part (want to remove)\n    slow_part = 1.0 + 0.1 * t\n    fast_part = 0.5 * sin(20.0 * t)  # ω=20 >> α=0.5, will be filtered out\n    field = fill(slow_part + fast_part, Nx, Ny)\n\n    # Update filter\n    update!(filter, field, dt)\nend\n\n# Result: filtered mean ≈ slow_part only\nh_mean = get_mean(filter)\nprintln(\"Filtered mean: \", h_mean[1,1])  # ≈ 1.0 + 0.1*5.0 = 1.5\n\n","title":"Complete Working Example"},{"category":"section","location":"pages/temporal_filters/#Choosing-a-Filter","page":"Temporal Filters for Lagrangian Averaging","text":"                    ┌─────────────────────────────────────┐\n                    │  Do you need Lagrangian averaging?  │\n                    │  (particle tracking, Stokes drift)  │\n                    └─────────────────────────────────────┘\n                                    │\n                    ┌───────────────┴───────────────┐\n                    ▼                               ▼\n                   YES                              NO\n                    │                               │\n                    ▼                               ▼\n           ┌────────────────┐              ┌─────────────────────┐\n           │LagrangianFilter│              │Need sharp wave-mean │\n           └────────────────┘              │   separation?       │\n                                           └─────────────────────┘\n                                                    │\n                                    ┌───────────────┴───────────────┐\n                                    ▼                               ▼\n                                   YES                              NO\n                                    │                               │\n                                    ▼                               ▼\n                          ┌──────────────────┐            ┌─────────────────┐\n                          │ButterworthFilter │            │ ExponentialMean │\n                          │  (recommended)   │            │   (simplest)    │\n                          └──────────────────┘            └─────────────────┘","title":"Choosing a Filter"},{"category":"section","location":"pages/temporal_filters/#Filter-Comparison","page":"Temporal Filters for Lagrangian Averaging","text":"Feature ExponentialMean ButterworthFilter LagrangianFilter\nUse case Simple running mean Wave-mean separation Full Lagrangian averaging\nMemory 1 array 2 arrays 2-4 arrays\nHigh-freq rolloff -20 dB/decade -40 dB/decade Depends on type\nAttenuation at ω=10α 1% passes 0.01% passes -\nComplexity Simplest Moderate Most complex\n\n","title":"Filter Comparison"},{"category":"section","location":"pages/temporal_filters/#The-Key-Parameter:-α","page":"Temporal Filters for Lagrangian Averaging","text":"The parameter α (alpha) is the inverse averaging timescale:\n\nalpha = frac1T_textavg","title":"The Key Parameter: α"},{"category":"section","location":"pages/temporal_filters/#How-to-Choose-α","page":"Temporal Filters for Lagrangian Averaging","text":"Rule of thumb: Set α much smaller than the frequency of oscillations you want to remove.\n\nα << ω_wave    (α should be much less than wave frequency)\n\nPractical guidance:\n\nWave period Suggested α Averaging time\n1 hour 0.05/hour 20 hours\n10 minutes 0.005/min 200 min\n1 second 0.05/s 20 s\n\nExample: If you have internal waves with period T=1 hour (ω = 2π/hour ≈ 6.28/hour):\n\nα = 0.1  # per hour → averages over 10 hours → filters waves with ω >> 0.1\n\ntip: Quick rule\nFor Butterworth: waves with ω > 20α are effectively filtered out. For Exponential: waves with ω > 50α are effectively filtered out.\n\n","title":"How to Choose α"},{"category":"section","location":"pages/temporal_filters/#API-Reference","page":"Temporal Filters for Lagrangian Averaging","text":"","title":"API Reference"},{"category":"section","location":"pages/temporal_filters/#ExponentialMean","page":"Temporal Filters for Lagrangian Averaging","text":"First-order filter. Simple and memory-efficient.\n\n# Constructor\nfilter = ExponentialMean((Nx, Ny); α=0.5, dtype=Float64)\n\n# Key ODE: dh̄/dt = α(h - h̄)\n# Transfer function: H(s) = α/(s + α)\n\nFunctions:\n\nupdate!(filter, h, dt)           # Update with new data (forward Euler)\nupdate!(filter, h, dt, Val(:RK2)) # Update with RK2 (more accurate)\nget_mean(filter)                 # Get filtered mean h̄\nreset!(filter)                   # Reset to zero\nset_α!(filter, new_α)            # Change α dynamically\neffective_averaging_time(filter) # Returns 1/α\nfilter_response(filter, ω)       # Returns |H(iω)|² at frequency ω","title":"ExponentialMean"},{"category":"section","location":"pages/temporal_filters/#ButterworthFilter","page":"Temporal Filters for Lagrangian Averaging","text":"Second-order filter. Sharper cutoff, recommended for wave-mean separation.\n\n# Constructor\nfilter = ButterworthFilter((Nx, Ny); α=0.5, dtype=Float64)\n\n# Key ODEs (coupled system):\n#   dh̃/dt = α[h - (√2-1)h̃ - (2-√2)h̄]\n#   dh̄/dt = α(h̃ - h̄)\n# Transfer function: K(s) = α²/(s² + √2αs + α²)\n\nFunctions:\n\nupdate!(filter, h, dt)           # Update with new data\nupdate!(filter, h, dt, Val(:RK2)) # Update with RK2\nget_mean(filter)                 # Get filtered mean h̄\nget_auxiliary(filter)            # Get auxiliary field h̃\nreset!(filter)                   # Reset to zero\nset_α!(filter, new_α)            # Change α\neffective_averaging_time(filter) # Returns 1/α\nfilter_response(filter, ω)       # Returns |K(iω)|²","title":"ButterworthFilter"},{"category":"section","location":"pages/temporal_filters/#LagrangianFilter","page":"Temporal Filters for Lagrangian Averaging","text":"Full Lagrangian averaging with displacement field tracking.\n\n# Constructor\nfilter = LagrangianFilter((Nx, Ny);\n    α=0.5,\n    filter_type=:butterworth,  # or :exponential\n    dtype=Float64\n)\n\n# Key relations:\n#   Mean velocity: ū = α·ξ (exponential) or ū = α·ξ̃ (Butterworth)\n#   Displacement PDE: ∂ξ/∂t + ū·∇ξ = u∘(id+ξ) - ū\n\nFunctions:\n\nupdate_displacement!(filter, velocity, dt)  # Update displacement ξ\nget_mean_velocity(filter)                   # Get ū\nget_displacement(filter)                    # Get ξ\nlagrangian_mean!(filter, gᴸ, g, dt)         # Compute Lagrangian mean of scalar g\nreset!(filter)                              # Reset all fields\nset_α!(filter, new_α)                       # Change α\n\n","title":"LagrangianFilter"},{"category":"section","location":"pages/temporal_filters/#Detailed-Examples","page":"Temporal Filters for Lagrangian Averaging","text":"","title":"Detailed Examples"},{"category":"section","location":"pages/temporal_filters/#Example-1:-Filtering-Oscillations-from-a-Time-Series","page":"Temporal Filters for Lagrangian Averaging","text":"using Tarang\n\n# Problem: Extract slowly-varying part from noisy oscillating signal\nNx = 100\nα = 1.0      # Filter frequencies >> 1\ndt = 0.01\nT_final = 10.0\n\n# Compare exponential vs Butterworth\nexp_filter = ExponentialMean((Nx,); α=α)\nbut_filter = ButterworthFilter((Nx,); α=α)\n\nt = 0.0\nwhile t < T_final\n    # Signal: mean=2.0, slow variation, fast noise\n    signal = fill(2.0 + 0.5*sin(0.1*t) + 0.3*sin(50.0*t), Nx)\n    #              ↑         ↑                ↑\n    #           constant   slow (keep)    fast (remove)\n\n    update!(exp_filter, signal, dt)\n    update!(but_filter, signal, dt)\n    t += dt\nend\n\nexp_mean = get_mean(exp_filter)[1]\nbut_mean = get_mean(but_filter)[1]\n\nprintln(\"Exponential mean: \", round(exp_mean, digits=3))  # Some fast oscillation leaks through\nprintln(\"Butterworth mean: \", round(but_mean, digits=3))  # Cleaner result","title":"Example 1: Filtering Oscillations from a Time Series"},{"category":"section","location":"pages/temporal_filters/#Example-2:-Wave-Mean-Flow-Decomposition","page":"Temporal Filters for Lagrangian Averaging","text":"using Tarang\n\n# Setup domain (simplified - not using full Tarang fields)\nNx, Ny = 64, 64\nα = 0.2      # Average over 5 time units\ndt = 0.02\n\n# Filter for each velocity component\nu_filter = ButterworthFilter((Nx, Ny); α=α)\nv_filter = ButterworthFilter((Nx, Ny); α=α)\n\n# Simulation loop\nfor step in 1:1000\n    t = step * dt\n\n    # Synthetic velocity: mean jet + inertia-gravity waves\n    u = zeros(Nx, Ny)\n    v = zeros(Nx, Ny)\n    for i in 1:Nx, j in 1:Ny\n        x, y = (i-1)/Nx * 2π, (j-1)/Ny * 2π\n\n        # Mean zonal jet (slow)\n        u[i,j] = sin(y)\n\n        # Wave perturbations (fast)\n        ω = 10.0  # wave frequency >> α\n        u[i,j] += 0.1 * cos(2x + 3y - ω*t)\n        v[i,j] = 0.1 * sin(2x + 3y - ω*t)\n    end\n\n    # Update filters\n    update!(u_filter, u, dt)\n    update!(v_filter, v, dt)\nend\n\n# Get mean flow\nū = get_mean(u_filter)\nv̄ = get_mean(v_filter)\n\n# Wave fluctuations\nu_wave = u - ū\nv_wave = v - v̄\n\nprintln(\"Mean u max: \", maximum(abs.(ū)))     # ≈ 1.0 (the jet)\nprintln(\"Mean v max: \", maximum(abs.(v̄)))     # ≈ 0.0 (waves filtered)","title":"Example 2: Wave-Mean Flow Decomposition"},{"category":"section","location":"pages/temporal_filters/#Example-3:-Comparing-Filter-Frequency-Response","page":"Temporal Filters for Lagrangian Averaging","text":"using Tarang\n\nα = 1.0\n\nexp_filter = ExponentialMean((10,); α=α)\nbut_filter = ButterworthFilter((10,); α=α)\n\nprintln(\"Frequency Response |H(iω)|²\")\nprintln(\"=\"^50)\nprintln(\"ω/α      Exponential    Butterworth\")\nprintln(\"-\"^50)\n\nfor ω_ratio in [0.1, 0.5, 1.0, 2.0, 5.0, 10.0, 20.0]\n    ω = ω_ratio * α\n    exp_resp = filter_response(exp_filter, ω)\n    but_resp = filter_response(but_filter, ω)\n\n    @printf(\"%4.1f     %10.6f     %10.6f\\n\", ω_ratio, exp_resp, but_resp)\nend\n\nOutput:\n\nω/α      Exponential    Butterworth\n--------------------------------------------------\n 0.1       0.990099       0.999800\n 0.5       0.800000       0.941176\n 1.0       0.500000       0.500000  ← half-power point\n 2.0       0.200000       0.058824\n 5.0       0.038462       0.001575\n10.0       0.009901       0.000100\n20.0       0.002494       0.000006\n\nnote: Key observation\nAt ω = 10α, Butterworth passes only 0.01% vs Exponential's 1% — that's 100x better filtering!\n\n","title":"Example 3: Comparing Filter Frequency Response"},{"category":"section","location":"pages/temporal_filters/#Background-Theory","page":"Temporal Filters for Lagrangian Averaging","text":"This section explains the physics behind temporal filters. Skip if you just want to use the filters.","title":"Background Theory"},{"category":"section","location":"pages/temporal_filters/#The-Wave-Mean-Flow-Problem","page":"Temporal Filters for Lagrangian Averaging","text":"Geophysical flows contain both:\n\nSlow mean flows: jets, eddies, large-scale circulation\nFast waves: gravity waves, inertial oscillations, acoustic waves\n\nu(x,t) = ū(x,t) + u'(x,t)\n         ↑          ↑\n      mean flow   wave fluctuation\n\nThe challenge: How do we define and compute the \"mean\" efficiently?","title":"The Wave-Mean Flow Problem"},{"category":"section","location":"pages/temporal_filters/#Eulerian-vs-Lagrangian-Averaging","page":"Temporal Filters for Lagrangian Averaging","text":"Eulerian (fixed point): baru^E(mathbfx) = frac1T int_0^T u(mathbfx t)  dt\n\nProblem: Misses Stokes drift! A wave with zero Eulerian mean can still transport particles.\n\nLagrangian (following particles): baru^L = frac1T int_0^T u(mathbfX(t) t)  dt\n\nAdvantage: Captures actual material transport.","title":"Eulerian vs Lagrangian Averaging"},{"category":"section","location":"pages/temporal_filters/#Why-Exponential-Filters?","page":"Temporal Filters for Lagrangian Averaging","text":"Traditional averaging requires storing entire time history. Exponential filters use a weighted average:\n\nbarh(t) = alpha int_0^infty e^-alphatau h(t-tau)  dtau\n\nThis satisfies a simple ODE:\n\nfracdbarhdt = alpha(h - barh)\n\nNo storage needed — just update the current mean at each timestep!","title":"Why Exponential Filters?"},{"category":"section","location":"pages/temporal_filters/#The-Butterworth-Advantage","page":"Temporal Filters for Lagrangian Averaging","text":"The exponential filter has slow rolloff (-20 dB/decade). The second-order Butterworth filter has:\n\nK(s) = fracalpha^2s^2 + sqrt2alpha s + alpha^2\n\nThis gives -40 dB/decade rolloff — much sharper separation between waves and mean flow.","title":"The Butterworth Advantage"},{"category":"section","location":"pages/temporal_filters/#The-Lifting-Map-(Advanced)","page":"Temporal Filters for Lagrangian Averaging","text":"For true Lagrangian averaging, we need the lifting map boldsymbolXi(mathbfx t):\n\nboldsymbolXi(mathbfx t)= position at time t of the parcel whose mean position is mathbfx\n\nThe displacement is: boldsymbolxi = boldsymbolXi - mathbfx\n\nKey result from Minz et al.:\n\nFor exponential mean: barmathbfu = alpha boldsymbolxi\nFor Butterworth: barmathbfu = alpha tildeboldsymbolxi\n\nThis is implemented in LagrangianFilter.\n\n","title":"The Lifting Map (Advanced)"},{"category":"section","location":"pages/temporal_filters/#Mathematical-Details","page":"Temporal Filters for Lagrangian Averaging","text":"","title":"Mathematical Details"},{"category":"section","location":"pages/temporal_filters/#Exponential-Filter","page":"Temporal Filters for Lagrangian Averaging","text":"Kernel: k(tau) = alpha e^-alphatau Theta(tau)\n\nODE: fracdbarhdt = alpha(h - barh)\n\nTransfer function: H(s) = fracalphas + alpha\n\nFrequency response: H(iomega)^2 = fracalpha^2alpha^2 + omega^2","title":"Exponential Filter"},{"category":"section","location":"pages/temporal_filters/#Butterworth-Filter","page":"Temporal Filters for Lagrangian Averaging","text":"Kernel: k(tau) = sqrt2alpha e^-alphatausqrt2 sin(alphatausqrt2) Theta(tau)\n\nCoupled ODEs: fracdtildehdt = alphalefth - (sqrt2-1)tildeh - (2-sqrt2)barhright fracdbarhdt = alpha(tildeh - barh)\n\nTransfer function: K(s) = fracalpha^2s^2 + sqrt2alpha s + alpha^2\n\nFrequency response: K(iomega)^2 = fracalpha^4(alpha^2 - omega^2)^2 + 2alpha^2omega^2","title":"Butterworth Filter"},{"category":"section","location":"pages/temporal_filters/#Filter-Matrix","page":"Temporal Filters for Lagrangian Averaging","text":"The Butterworth system can be written as: fracddtbeginpmatrixtildehbarhendpmatrix = -alphamathbfAbeginpmatrixtildehbarhendpmatrix + alphabeginpmatrixh0endpmatrix\n\nwhere: mathbfA = beginpmatrixsqrt2-1  2-sqrt2-1  1endpmatrix approx beginpmatrix0414  0586-1  1endpmatrix","title":"Filter Matrix"},{"category":"section","location":"pages/temporal_filters/#Lagrangian-Mean-Equations","page":"Temporal Filters for Lagrangian Averaging","text":"Displacement evolution: fracpartialboldsymbolxipartial t + barmathbfucdotnablaboldsymbolxi = mathbfucirc(mathbfid+boldsymbolxi) - barmathbfu\n\nScalar Lagrangian mean (exponential): fracpartial g^Lpartial t + barmathbfucdotnabla g^L = alpha(gcircboldsymbolXi - g^L)\n\nScalar Lagrangian mean (Butterworth): fracpartialtildegpartial t + barmathbfucdotnablatildeg = alphaleftgcircboldsymbolXi - (sqrt2-1)tildeg - (2-sqrt2)g^Lright fracpartial g^Lpartial t + barmathbfucdotnabla g^L = alpha(tildeg - g^L)\n\n","title":"Lagrangian Mean Equations"},{"category":"section","location":"pages/temporal_filters/#Advanced-Time-Integration-Methods","page":"Temporal Filters for Lagrangian Averaging","text":"The basic update! function uses Forward Euler, which has a stability limit. For larger timesteps or coupling with implicit PDE solvers, use these advanced methods.","title":"Advanced Time Integration Methods"},{"category":"section","location":"pages/temporal_filters/#Stability-Limits-(Explicit-Methods)","page":"Temporal Filters for Lagrangian Averaging","text":"Filter Forward Euler RK2\nExponentialMean Delta t leq 2alpha Delta t leq 4alpha\nButterworthFilter Delta t leq sqrt2alpha Delta t leq 2sqrt2alpha\n\n# Check maximum stable timestep\ndt_max = max_stable_timestep(filter)           # Forward Euler\ndt_max_rk2 = max_stable_timestep(filter; method=:RK2)  # RK2","title":"Stability Limits (Explicit Methods)"},{"category":"section","location":"pages/temporal_filters/#ETD-(Exponential-Time-Differencing)-Recommended","page":"Temporal Filters for Lagrangian Averaging","text":"Unconditionally stable for any timestep. Treats the linear decay term exactly.\n\n# Precompute coefficients once (per α, dt pair)\ncoeffs = precompute_etd_coefficients(filter, dt)\n\n# In time loop - no stability limit!\nfor step in 1:nsteps\n    # ... your PDE timestepping ...\n    update_etd!(filter, field_data, coeffs)\nend\n\nHow it works:\n\nFor the filter ODE dbarhdt = -alphabarh + alpha h, ETD computes:\n\nbarh^n+1 = e^-alphaDelta tbarh^n + (1 - e^-alphaDelta t)h^n\n\nThis is exact when h is constant over the timestep, and unconditionally stable otherwise.","title":"ETD (Exponential Time Differencing) - Recommended"},{"category":"section","location":"pages/temporal_filters/#IMEX/SBDF-Integration","page":"Temporal Filters for Lagrangian Averaging","text":"For coupling with implicit PDE solvers using SBDF2 or SBDF3 timestepping.\n\n# Precompute IMEX coefficients\ncoeffs = precompute_imex_coefficients(filter, dt; scheme=:SBDF2)\n\n# Store field history for multistep method\nh_prev = copy(h)\nh_curr = copy(h)\n\n# In time loop - unconditionally stable!\nfor step in 1:nsteps\n    # ... your SBDF2 PDE timestepping ...\n    h_new = # ... compute new field value ...\n\n    # Update filter with field history\n    update_imex!(filter, (h_curr, h_prev), coeffs)\n\n    h_prev .= h_curr\n    h_curr .= h_new\nend\n\nAvailable schemes:\n\n:SBDF1 (Backward Euler): First-order, most stable\n:SBDF2: Second-order, recommended\n:SBDF3: Third-order, highest accuracy","title":"IMEX/SBDF Integration"},{"category":"section","location":"pages/temporal_filters/#Accessing-Linear-Operator-for-External-Solvers","page":"Temporal Filters for Lagrangian Averaging","text":"If you're using your own IMEX framework:\n\n# Get the linear operator coefficient\nL = linear_operator_coefficients(filter)\n# Returns -α (scalar) for ExponentialMean\n# Returns -α·A (2×2 matrix) for ButterworthFilter\n\n","title":"Accessing Linear Operator for External Solvers"},{"category":"section","location":"pages/temporal_filters/#Troubleshooting","page":"Temporal Filters for Lagrangian Averaging","text":"","title":"Troubleshooting"},{"category":"section","location":"pages/temporal_filters/#Common-Issues","page":"Temporal Filters for Lagrangian Averaging","text":"1. Filter not converging to expected mean\n\nCause: Not enough spinup time.\n\nSolution: Run for at least 5alpha time units before trusting results.\n\nT_spinup = 5 / α  # Minimum spinup time\n\n2. Oscillations in filtered output\n\nCause: Timestep too large for stability.\n\nSolution: Use ETD integration (unconditionally stable):\n\ncoeffs = precompute_etd_coefficients(filter, dt)\nupdate_etd!(filter, h, coeffs)  # Always stable!\n\nOr use RK2 for explicit methods:\n\nupdate!(filter, h, dt, Val(:RK2))  # 2× larger stability region\n\n3. High frequencies leaking through\n\nCause: Using ExponentialMean when ButterworthFilter is needed.\n\nSolution: Switch to Butterworth for sharper cutoff.\n\n4. Memory issues with large 3D fields\n\nCause: ButterworthFilter uses 2x memory.\n\nSolution: Use ExponentialMean if memory is critical:\n\n# ButterworthFilter: 2 arrays\n# ExponentialMean: 1 array (half the memory)\nfilter = ExponentialMean((Nx, Ny, Nz); α=α)\n\n5. Need to couple with implicit timestepper\n\nSolution: Use IMEX integration:\n\ncoeffs = precompute_imex_coefficients(filter, dt; scheme=:SBDF2)\nupdate_imex!(filter, (h_n, h_nm1), coeffs)\n\n","title":"Common Issues"},{"category":"section","location":"pages/temporal_filters/#Tutorials","page":"Temporal Filters for Lagrangian Averaging","text":"Rotating Shallow Water with Lagrangian Averaging - Complete example of wave-mean separation in a rotating flow\n\n","title":"Tutorials"},{"category":"section","location":"pages/temporal_filters/#References","page":"Temporal Filters for Lagrangian Averaging","text":"Minz, C., Baker, L. E., Kafiabad, H. A., & Vanneste, J. (2025). Efficient Lagrangian averaging with exponential filters. Physical Review Fluids, 10, 074902. DOI\nAndrews, D. G., & McIntyre, M. E. (1978). An exact theory of nonlinear waves on a Lagrangian-mean flow. J. Fluid Mech., 89(4), 609-646.\nBühler, O. (2014). Waves and Mean Flows (2nd ed.). Cambridge University Press.\nGilbert, A. D., & Vanneste, J. (2018). Geometric generalised Lagrangian-mean theories. J. Fluid Mech., 839, 95-134.\n\n\n\nDocument version 2.0 | December 2024 | Tarang.jl","title":"References"},{"category":"section","location":"pages/tau_method/#The-Tau-Method-for-Boundary-Conditions","page":"Tau Method","text":"Tarang enforces boundary conditions using the tau method, a spectral technique that lets you solve PDEs with non-periodic boundary conditions without modifying the spectral basis. This page explains what the tau method is, how Tarang's implementation works, and how to write code that uses it correctly.\n\nIf you have used Dedalus, the approach here is almost identical: tau fields are added to the state vector, and lift() operators inject them into the equations as extra degrees of freedom to match boundary conditions.","title":"The Tau Method for Boundary Conditions"},{"category":"section","location":"pages/tau_method/#Why-Do-We-Need-the-Tau-Method?","page":"Tau Method","text":"Consider the steady diffusion problem:\n\nfracd^2 udz^2 = f(z) quad z in -1 1 quad u(-1) = u(+1) = 0\n\nIn a Chebyshev spectral method, u is expanded as\n\nu(z) = sum_n=0^N-1 a_n T_n(z)\n\nand the PDE is projected onto the basis to give an N × N linear system in the coefficients a_n. The problem: this system has no leftover degrees of freedom to enforce the boundary conditions. All N unknowns are already determined by the interior equations, but we also need two boundary constraints.\n\nThe tau method solves this by adding extra unknowns (the tau fields) that act as corrections designed to make the boundary conditions hold. The classical formulation, due to Lanczos (1938), replaces the highest-order rows of the spectral system with BC rows — but that makes the interior equation different at those rows, which is awkward for nonlinear and time-dependent problems.\n\nModern spectral codes (Dedalus, Tarang) use a cleaner variant: instead of replacing rows, they add a tau term to the equation itself:\n\nmathcalLu + tau_1phi_1(z) + tau_2phi_2(z) = f(z)\n\nwhere phi_1 phi_2 are chosen basis functions that live in a \"lift basis\". The tau scalars tau_1 tau_2 become new unknowns in the system, and the boundary conditions become new equation rows that link them to the state. The interior of the PDE is untouched; the tau corrections only perturb the equation at a small number of high spectral modes, and for smooth solutions the required tau_k are spectrally small.\n\nThat is exactly how Tarang implements BCs.","title":"Why Do We Need the Tau Method?"},{"category":"section","location":"pages/tau_method/#Quick-Start-Example","page":"Tau Method","text":"Here's the smallest complete verified example — a 2D Poisson problem Δu = -2 with homogeneous Dirichlet BCs on the two z walls, whose exact solution is u = z(Lz - z). The domain has one separable (Fourier) x direction and one bounded (Chebyshev) z direction; the Chebyshev direction is the coupled one that needs tau corrections:\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\n\nxb = RealFourier(coords[\"x\"]; size=4,  bounds=(0.0, 2π))    # periodic (separable)\nzb = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))   # bounded  (coupled)\ndom = Domain(dist, (xb, zb))\n\n# State field\nu = ScalarField(dom, \"u\")\n\n# Tau fields — one per boundary condition. Each lives on the COMPLEMENT of the\n# state's bases: the coupled (Chebyshev) z axis is dropped, leaving (xb,).\ntau1 = ScalarField(dist, \"tau1\", (xb,), Float64)\ntau2 = ScalarField(dist, \"tau2\", (xb,), Float64)\n\n# The lift basis. This equation is written as a DIRECT second-order operator\n# (`Δ(u)`), so the second-derivative basis is the matching one. The first-order\n# `grad_u`/`grad_T` formulation further down uses `derivative_basis(zb, 1)`.\nlb2 = derivative_basis(zb, 2)\n\nproblem = LBVP([u, tau1, tau2])\n\n# Register the lifts as named parameters, then reference them in the equation.\n# lift(tau, lb2, -k) places tau at the k-th-from-last Chebyshev coefficient\n# slot (-1 = last, -2 = second-to-last).\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"Δ(u) + l1 + l2 = -2\")\n\nadd_bc!(problem, \"u(z=0)   = 0\")\nadd_bc!(problem, \"u(z=1.0) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nensure_layout!(u, :g)            # scatter writes coefficients; switch to grid\n\nThis reproduces the exact solution u = z(1 - z) to 1.4e-16, with both wall residuals at 0.0.\n\nNotice that:\n\nTau fields (tau1, tau2) are added to the state vector, not computed from the state afterwards.\nThe lift terms are registered as named parameters via add_parameters! and then referenced by name (l1, l2) in the equation. Each lift uses the 3-arg form lift(tau, lift_basis, -k).\nThe BCs are declared with add_bc! (u(z=0) = 0) — Tarang converts each into an algebraic row that constrains the tau fields.\n\nThe solver builds one square tau subproblem per separable Fourier mode and determines u, tau1, and tau2 simultaneously in a single linear solve.\n\nnote: 1D pure-Chebyshev BVP\nA pure single-axis Chebyshev BVP (no Fourier direction) works the same way — drop the x axis and define the tau variables on (). The solver builds a single coupled tau subproblem over the Chebyshev spectrum.","title":"Quick-Start Example"},{"category":"section","location":"pages/tau_method/#The-lift-Operator","page":"Tau Method","text":"lift is how a tau field enters an equation. Always use the explicit 3-argument form, with the lift basis built from derivative_basis, choosing the derivative order that matches the space the lifted term is added to:\n\nlift(tau, derivative_basis(basis, order), -k)   # explicit lift basis\n\ntau is the tau field (a ScalarField or VectorField whose bases are a strict subset of the state field's bases — it's missing the coupled direction).\nderivative_basis(basis, order) is the lift basis. A direct second-order equation (Δ(u)) uses order = 2; the first-order grad_u/grad_T formulation used by the flow examples uses order = 1. Bind the result once and reuse it. See Why still pass the derivative basis? below for what this argument does — and does not — control today.\n-k is an integer mode index with wraparound semantics: -1 is the last coefficient slot of the coupled direction (lift_mode = N-1 → Julia index N), -2 is the second-to-last, 0 is the first, and so on. For a ChebyshevT state of size N, -1 places the tau value at coefficient N-1. A second-order problem uses two taus with lift orders -1 and -2.","title":"The lift Operator"},{"category":"section","location":"pages/tau_method/#The-2-argument-short-form-does-not-work","page":"Tau Method","text":"lift(tau, n) — the basis-less short form that appears in the lift docstring — is broken in the current release. It tries to auto-detect the lift basis by scanning tau.dist.layouts, but that dictionary is keyed by (bases, dtype) pairs rather than by bases alone, so the scan hands a tuple to is_fourier_basis and dies:\n\nMethodError: no method matching is_fourier_basis(::Tuple{RealFourier, ChebyshevT})\n\nIt fails this way regardless of the order in which you create your fields — creating the full state field first, so the distributor has already cached the complete layout, does not rescue it. Pass the lift basis explicitly instead — bind it once and reuse it:\n\nlb1    = derivative_basis(zbasis, 1)\ntau_u1 = ScalarField(dist, \"tau_u1\", (xbasis,), Float64)\ngrad_u = grad(u) + ez * lift(tau_u1, lb1, -1)\n\nAll of Tarang's shipped examples use this explicit form.\n\nwarning: The short form inside an equation *string* fails silently\nWriting lift(tau1, -1) inside an add_equation! string does not raise: the parser catches the auto-detection failure and falls back to the bare operand, dropping the Lift wrapper. On the quick-start problem the solution u still comes out bit-identical, but the tau fields scatter back as 0.0 instead of their true values, so you silently lose the tau diagnostic — and you are leaning on an untracked fallback path. Build the lift explicitly, name it with add_parameters!, and reference it by name.","title":"The 2-argument short form does not work"},{"category":"section","location":"pages/tau_method/#What-lift(tau,-basis,-n)-actually-computes-(solver-view)","page":"Tau Method","text":"At the solver level, lift(tau, basis, n) resolves to a single-column sparse matrix with a 1 at row lift_mode (where lift_mode = n is wraparound-resolved: -1 → last, -2 → second-to-last, etc.). This column is a discrete delta in the coupled direction's coefficient space.\n\ntextlift(tau cdot n) longrightarrow tau cdot e_textlift_mode\n\nwhere e_textlift_mode is the unit vector with a 1 at the lift_mode-th Chebyshev-coefficient slot. When this is added to an equation's LHS, it contributes the unknown tau to exactly one coefficient row, leaving every other interior row untouched. The linear solver then chooses tau so that the BC rows hold — and because the perturbation is confined to one high-order coefficient, the interior PDE residual stays spectrally small for smooth solutions.\n\nImplementation detail worth knowing: In Tarang's current solver, subproblem_matrix(op::Lift, sp) reads the dimension N from _subproblem_cheb_basis(sp) — the problem's own Chebyshev basis — and does not use op.basis to compute the matrix. That means lift(tau, zb, -1), lift(tau, derivative_basis(zb, 1), -1) and lift(tau, derivative_basis(zb, 2), -1) produce identical delta columns at row N-1. (Measured: all three spellings give bit-identical solutions and bit-identical tau values on the quick-start problem.) The basis argument is a semantic hint inherited from Dedalus's type system; it's retained so that future refinements — e.g. explicit basis tracking through expression trees — can hook in without breaking user code.","title":"What lift(tau, basis, n) actually computes (solver view)"},{"category":"section","location":"pages/tau_method/#Why-still-pass-the-derivative-basis?","page":"Tau Method","text":"Even though the explicit basis choices produce identical lift columns in the current solver, passing the intended derivative basis is worth doing:\n\nForward-compatibility: if Tarang later tracks the lift output basis in the matrix representation, your expression already identifies the intended space. Code that passed the state basis zb would silently start producing different matrices.\nSemantic clarity: in first-order formulations like grad_u = grad(u) + ez * lift(tau_u1, lb1, -1), the lift lives alongside grad(u), which maps a ChebyshevT field into the first-derivative (ChebyshevU) space. Passing the derivative basis tells the reader what the lift is conceptually contributing, even if the linear algebra doesn't currently care.\nConsistency with examples and tutorials: all of Tarang's shipped examples build the lift basis with derivative_basis, so sticking with it makes your code pattern-match familiar code.\n\nRule of thumb: bind the lift basis once at the top of your setup block and reuse it — derivative_basis(zb, 1) for a lift added to a first-order gradient substitution (which is what examples/ivp/rayleigh_benard_2d.jl does), and derivative_basis(zb, 2) for lifts added directly to a second-order equation (Δ(u), two lifts at -1 and -2). Don't invent your own lift basis.","title":"Why still pass the derivative basis?"},{"category":"section","location":"pages/tau_method/#Boundary-Conditions-as-Algebraic-Constraint-Rows","page":"Tau Method","text":"When you write\n\nadd_bc!(problem, \"u(z=-1) = 0\")\n\nTarang produces a new equation row that looks like\n\nsum_n=0^N-1 a_n T_n(-1) = 0\n\ni.e. a linear combination of u's coefficients that equals the boundary value. This row is added to the system, not substituted in. It's an algebraic equation — there is no time derivative, so it contributes nothing to the M matrix in the M·dX/dt + L·X = F formulation. In the linear-algebra picture:\n\nu contributes Nz rows (PDE interior) + BC rows\ntau_u1, tau_u2 contribute columns (one each, at the lift modes)\nBC rows are zero in M (no time derivative) → they're pure algebraic constraints\nThe combined LHS matrix is square because (PDE rows + BC rows) = (state cols + tau cols)\n\nThis square-system condition is enforced automatically when you declare the right number of tau fields to match the number of BCs. If you miss a BC you'll get a singular / non-square system at solver-build time.","title":"Boundary Conditions as Algebraic Constraint Rows"},{"category":"section","location":"pages/tau_method/#DAE-style-handling-in-IVP-steppers","page":"Tau Method","text":"For initial-value problems, BC rows have M_row = 0, which makes the full system a differential-algebraic equation (DAE) rather than a pure ODE. Tarang's subproblem stepper handles this correctly via a per-stage row override on what it classifies as \"BC rows\".\n\nA note on how this classification works: sp.bc_rows is not built by scanning M for zero rows directly. Instead, build_matrices! labels rows by equation size — any equation whose per-subproblem row count is smaller than the coupled-direction basis size Nz is classified as a BC row (pressure gauge integ(p) = 0 → 1 row; wall BC T(z=0) = 1 → 1 row; vector wall BC → 2 rows; etc.). Rows from equations with eq_size ≥ Nz — the PDE interior and any Nz-sized algebraic equation like the continuity equation — are classified as \"bulk\".\n\nAt each IMEX-RK stage (or multistep update), the solver assembles the stage RHS from the accumulated dt·Σ(AᴱF − Aⁱ·L·X) formula for every row, then calls apply_bc_override! to replace the sp.bc_rows entries with dt·a_ii·F_BC. After the LHS solve, those rows yield exactly L_row·X = F_BC, which is the correct BC enforcement.\n\nWhy a size-based classifier is good enough: the only algebraic rows that don't get overridden are Nz-sized, F-zero equations like continuity. For them, the accumulated-RHS formula naturally produces zero on the right-hand side (since their F is zero and L*X = 0 is the target), so no override is needed. The override only matters for small algebraic rows whose F can be nonzero — i.e., BCs and gauge constraints — and those are exactly what sp.bc_rows captures.\n\nWhy the override is necessary: without it, the raw accumulated-RHS formula produces a wrong 1/γ scaling factor for inhomogeneous algebraic rows. For RK222 (γ = 1 - 1/√2, 1/γ = 2 + √2 ≈ 3.414), a BC like T(z=0) = 1 would be enforced as T(z=0) = 2+√2 instead of 1. The override is built into step_subproblem_rk! and step_subproblem_multistep!, so you don't have to do anything to enable it — but it's worth knowing the mechanism exists if you're debugging a \"BC value is off by a constant factor\" symptom.","title":"DAE-style handling in IVP steppers"},{"category":"section","location":"pages/tau_method/#First-Order-Formulation-(Recommended)","page":"Tau Method","text":"For time-dependent problems and anything involving the Navier–Stokes equations, the first-order formulation is the right default. Instead of writing a 2nd-order operator like Δ(u) directly, introduce an auxiliary gradient field with a tau correction. The lift basis here is the first-derivative basis, because the lift is added to grad(u), which lives in the first-derivative space:\n\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\n\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\n\nNow grad_u is an \"augmented gradient\" that lives in the derivative (ChebyshevU) space and carries its own tau. Second derivatives become div(grad_u) rather than Δ(u). Register grad_u and τ_lift as parameters so the equation string can name them:\n\nadd_parameters!(problem, nu=nu, grad_u=grad_u, τ_lift=τ_lift)\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) + τ_lift(tau_u2) = -u⋅∇(u)\")\n\nHow the two formulations compare:\n\nAspect 2nd-order (Δ(u)) First-order (div(grad_u))\nTau DOFs for two scalar wall constraints 2 lift terms in the second-order equation (modes -1, -2) 2 tau fields: one in the gradient substitution and one in the evolution equation\nOperator representation Second derivative assembled directly Composition of first-order gradient and divergence operators\nBoundary enforcement Two high-mode lift columns in the bulk equation Split between the gradient substitution and the evolution equation\nRecommended use Compact scalar BVPs and small prototypes Coupled IVPs and production flow problems\n\nThe convention we use throughout Tarang's examples is:\n\ntau_u1 — correction added inside grad_u, one-dimensional (xbasis only)\ntau_u2 — correction added to the evolution equation directly, one-dimensional (xbasis only)\n\nFor a vector field u, both tau_u1 and tau_u2 are VectorFields too (one component per velocity component).","title":"First-Order Formulation (Recommended)"},{"category":"section","location":"pages/tau_method/#Worked-Example:-2D-Rayleigh–Bénard-Convection","page":"Tau Method","text":"Here is the full first-order RBC setup, which is the examples/ivp/rayleigh_benard_2d.jl example in the repository. This is the canonical pattern to copy for any 2D channel-flow problem with non-periodic BCs.\n\nusing Tarang\n\n# Domain and physical parameters\nLx, Lz = 4.0, 1.0\nNx, Nz = 256, 64\nRayleigh, Prandtl = 2e6, 1.0\nnu   = Prandtl                 # viscous coefficient (diffusive-time scaling)\nbuoy = Rayleigh * Prandtl      # buoyancy forcing Ra·Pr\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nzbasis = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(0.0, Lz), dealias=3/2)\n\ndomain = Domain(dist, (xbasis, zbasis))\n\n# State variables: pressure, temperature, velocity\np = ScalarField(domain, \"p\")\nT = ScalarField(domain, \"T\")\nu = VectorField(domain, \"u\")\n\n# Tau fields. Each lives on `(xbasis,)` only — the coupled direction (z)\n# is dropped because the tau correction lives at a single z mode.\n#\n# tau_p:  scalar, no bases — gauge for the pressure constraint\n# tau_T1: gradient-substitution correction for T\n# tau_T2: evolution-equation correction for T\n# tau_u1, tau_u2: ditto for each velocity component (so they are VectorFields)\ntau_p  = ScalarField(dist, \"tau_p\",  (),         Float64)\ntau_T1 = ScalarField(dist, \"tau_T1\", (xbasis,),  Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (xbasis,),  Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis,), Float64)\n\n# First-order substitutions — FIRST-derivative lift basis\nex, ez = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A) = lift(A, lift_basis, -1)\n\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_T = grad(T) + ez * τ_lift(tau_T1)\n\n# Problem declaration includes ALL tau fields as state\nproblem = IVP([p, T, u, tau_p, tau_T1, tau_T2, tau_u1, tau_u2])\n\nadd_parameters!(problem,\n    nu=nu, buoy=buoy, ez=ez,\n    grad_u=grad_u, grad_T=grad_T, τ_lift=τ_lift)\n\n# Equations\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\nadd_equation!(problem,\n    \"∂t(u) - nu*div(grad_u) + ∇(p) - buoy*T*ez + τ_lift(tau_u2) = -u⋅∇(u)\")\n\n# Boundary conditions.\n# NOTE the `$Lz` interpolation: a BC string is parsed by Tarang, not by Julia,\n# so it cannot see the Julia binding `Lz`. Writing \"T(z=Lz) = 0\" drops the BC\n# and the first solve fails with a DimensionMismatch. Interpolate, or write the\n# number literally.\nadd_bc!(problem, \"T(z=0) = 1\")        # hot bottom wall\nadd_bc!(problem, \"T(z=$Lz) = 0\")      # cold top wall\nadd_bc!(problem, \"u(z=0) = 0\")        # no-slip\nadd_bc!(problem, \"u(z=$Lz) = 0\")      # no-slip\nadd_bc!(problem, \"integ(p) = 0\")      # pressure gauge\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\nPattern summary:\n\nThe five add_bc! calls expand to 7 scalar constraint rows: two temperature rows, four velocity rows (two vector components at each of two walls), and one pressure-gauge row. The tau variables supply the matching seven scalar tau DOFs: one each from tau_T1 and tau_T2, four from the two-component tau_u1/tau_u2, and one from tau_p.\nEach tau field drops the coupled direction (xbasis only, not zbasis).\ntau_p is a 0-D scalar (no bases). It contributes a one-DOF candidate column during each subproblem build, but valid-mode filtering removes it at non-DC modes, leaving the actual gauge correction only at DC.\nThe pressure gauge integ(p) = 0 is an algebraic constraint on the mean; it lives alongside the other BCs.","title":"Worked Example: 2D Rayleigh–Bénard Convection"},{"category":"section","location":"pages/tau_method/#Pressure-Gauge-and-Valid-Mode-Filtering","page":"Tau Method","text":"In incompressible flow the pressure is defined only up to one global constant, so the gauge ambiguity belongs to the all-zero (DC) Fourier mode. At non-DC modes the pressure fluctuation is determined, while the domain-integral gauge equation is identically 0 = 0. Tarang handles the DC gauge and those trivial non-DC rows with two mechanisms that are sometimes confused:\n\n1. Pressure gauge (a user-visible tau). You add tau_p to continuity and provide a gauge-fixing BC like integ(p) = 0:\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_bc!(problem, \"integ(p) = 0\")\n\ntau_p is typically declared as a 0-D scalar (ScalarField(dist, \"tau_p\", (), Float64)). In the raw, pre-filtered matrix this gives it 1 DOF per subproblem (because a 0-D field has subproblem_field_size == 1 at every Fourier mode). After valid-mode filtering (see next section) tau_p is dropped from every non-DC subproblem — so in practice it's a single gauge-fixing scalar at the DC Fourier mode only. The non-DC subproblems don't carry it, and scatter_inputs on those modes is a no-op because the filter removed the column.\n\nThat's the picture you should hold in your head: tau_p fixes the pressure gauge at the DC mode; at every other mode, pressure is well-defined by the trace(grad_u) = 0 constraint together with the wall BCs, with no gauge ambiguity.\n\n2. Valid-mode filtering (invisible, done by the solver). At the non-DC Fourier modes (and in some situations at the DC mode too), the integral constraint integ(p) = 0 produces a zero row in the raw LHS matrix — the integral of a non-DC Fourier mode over the domain is identically zero, so the row just says 0 = 0, which is trivially true and carries no information.\n\nThe solver detects these zero rows during matrix assembly (build_matrices!, in src/core/subsystems/subproblem_matrix_build.jl) and pairs each with a 1-DOF tau column — any variable whose per-subproblem size equals 1. That includes:\n\ntau_p (a 0-D gauge scalar, always vsz=1)\ntau_T1, tau_T2 (declared with (xbasis,) — at each Fourier mode, the xbasis contribution reduces to a single coefficient, so vsz=1 per subproblem)\nComponents of tau_u1, tau_u2 (same story, per component)\n\nThe pairing uses a smallest-column-norm heuristic: for each zero row, the filter picks the unused 1-DOF tau column with the smallest total |L| + |M| norm — i.e., the tau that appears in the fewest or smallest-magnitude entries elsewhere. That's typically tau_p at non-DC subproblems because tau_p only contributes to continuity (one nonzero entry from +tau_p), while tau_T1/tau_T2 contribute through the full gradient substitution and so carry more weight. After pairing, both the row and the column are dropped from the filtered system, yielding a smaller square matrix that the sparse LU factorizes cleanly.\n\nYou do not have to do anything to enable this. Just declare tau_p and the integ(p) = 0 BC, and the valid-mode filter takes care of the rest. The take-away is: if you ever see a \"singular pencils\" or \"Non-square filtered system\" warning, it usually means a BC is missing, a tau field is missing, or the number of small-eq-size algebraic rows doesn't match the number of 1-DOF tau columns.","title":"Pressure Gauge and Valid-Mode Filtering"},{"category":"section","location":"pages/tau_method/#Number-of-Tau-Terms","page":"Tau Method","text":"The number of scalar tau DOFs must match the number of scalar constraint rows (including gauge conditions). A vector-valued add_bc! call contributes one scalar row per component, and a vector tau field contributes the corresponding component-wise tau DOFs:\n\nPDE order in coupled direction BCs needed Tau terms per equation\n1st (∂u/∂z) 1 1\n2nd (∂²u/∂z², via first-order form) 2 2 (one in grad, one in evolution)\n4th (biharmonic) 4 4\n\nIn a 2D problem (x-Fourier, z-coupled) each equation carries tau corrections only in the z direction — not \"2 per direction\". The Fourier x direction is periodic and needs no tau terms. Only the coupled z direction contributes.\n\nVector equations need vector tau fields. In RBC, u is a 2-component velocity, so tau_u1 and tau_u2 are VectorFields (one scalar per component per Fourier mode).","title":"Number of Tau Terms"},{"category":"section","location":"pages/tau_method/#Time-and-Space-Dependent-BCs","page":"Tau Method","text":"Tarang supports boundary conditions whose value varies in time, space, or both. They are refreshed by the stepper:\n\nTime-dependent BCs (e.g. T(z=0) = sin(t)): the RK stepper re-evaluates the BC value at each stage time t + c[i]*dt, so multi-stage methods retain their full formal order of accuracy for rapidly-varying BCs.\nSpace-dependent BCs (e.g. T(z=0) = sin(2*pi*x/4.0)): at solver-build time the BC expression is evaluated on the global coordinate grid, and the resulting array is projected onto the Fourier modes via an unnormalized FFTW.rfft. Each subproblem picks its own mode from the cached coefficient array.\nSpace+time BCs (e.g. T(z=0) = sin(2*pi*x/4.0) * cos(2*pi*t)): combined — re-projected on every stage.\n\nYou don't need to register coordinate fields manually. The solver auto-registers global grid arrays for every Fourier/Chebyshev axis under its element label (\"x\", \"y\", \"z\", ...), so BC string expressions can reference those coordinates directly:\n\nadd_bc!(problem, \"T(z=0) = 1 + 0.1*sin(2*pi*x/4.0)\")   # Lx = 4.0, written literally\nadd_bc!(problem, \"T(z=0) = sin(t)\")                     # time-dependent\n\nBoth are enforced to machine precision — measured max|T(z=0) − target| of 2.2e-16 (space) and 2.3e-16 (time) after 5 RK222 steps of a diffusion problem.\n\nwarning: BC strings cannot see Julia variables\nA BC string is parsed by Tarang's own expression parser, not by Julia, so it resolves only coordinate names (x, y, z), the time variable t, and names registered with add_parameters!. A bare Julia binding — \"T(z=0) = 1 + 0.1*sin(2*pi*x/Lx)\" with Lx = 4.0 defined in your script — logs Warning: Unknown variable: Lx and is then enforced as zero, silently satisfying the wrong condition (measured error against the intended profile: 1.1). Interpolate the value (\"…/$Lx)\"), write the literal, or register it with add_parameters!. The same applies to the location: use \"T(z=$Lz) = 0\", never \"T(z=Lz) = 0\".\n\nnote: Space- and time-dependent BC strings are an IVP-path feature\nThe coordinate-array projection and the per-stage BC refresh are wired into the InitialValueSolver build. A BoundaryValueSolver (LBVP/NLBVP) does not register the coordinate fields, so a BC string like \"T(z=0) = 1 + 0.1*sin(2*pi*x/4.0)\" is enforced as zero there — with a loud Warning: Boundary condition right-hand side of type … is not supported and is being enforced as ZERO. In a BVP, keep each BC value a constant (or a compound constant like h*T_amb).\n\nUnder MPI, every rank evaluates the BC expression on the full (global) grid and computes a local FFT — no inter-rank communication is needed because all ranks produce identical coefficient arrays.","title":"Time- and Space-Dependent BCs"},{"category":"section","location":"pages/tau_method/#Inspecting-BC-Satisfaction","page":"Tau Method","text":"After a solve, you can verify that the BCs are actually being enforced:\n\n# After solve!()\nensure_layout!(u, :g)\nu_data = get_grid_data(u)\n\n# For Chebyshev-T, the Gauss-Lobatto grid includes the endpoints, and the grid\n# runs from z_min to z_max.\n# u_data[:, 1]    is the solution at z = z_min (first Cheb grid point)\n# u_data[:, end]  is the solution at z = z_max (last Cheb grid point)\n@assert maximum(abs.(u_data[:, 1]))   < 1e-10  \"Bottom BC not satisfied\"\n@assert maximum(abs.(u_data[:, end])) < 1e-10  \"Top BC not satisfied\"\n\nFor a vector field, index a component: get_grid_data(u.components[2])[:, 1].\n\nAfter a BVP solve!, the tau fields are scattered back with the corrections the solver chose, so you can read them to see how hard the tau method had to work. For smooth, well-resolved solutions they are spectrally small:\n\ntau_data = get_coeff_data(tau2)          # ScalarField tau; for a VectorField use\n@info \"tau2 magnitude: $(maximum(abs.(tau_data)))\"   # tau2.components[i]\n\nIn the quick-start problem the exact solution z(1 - z) is a quadratic that the Chebyshev basis represents exactly, so both taus come back as 0.0 — the tau correction has nothing to do. Swap the constant forcing for a non-polynomial one (a grid field holding sin(7z), passed in with add_parameters!) at Nz=16 and they become nonzero but tiny: max|tau1| = 2.1e-8, max|tau2| = 6.5e-8.\n\nnote: Tau values are not exposed after an IVP step\nThe IVP steppers solve for the stage taus internally but leave the tau state fields at exactly zero, so get_coeff_data(tau_T2) after run! reads 0.0 no matter how large the corrections were. BC enforcement itself is unaffected — measured max|T(z=0) − 1| = 0.0 and max|u_x(z=0)| < 1e-16 after 20 RK222 steps of the RBC problem. To inspect tau magnitudes, use a BVP solve.\n\nFor time-dependent problems, check BC satisfaction inside a callback:\n\nrun!(solver;\n     stop_iteration=30,\n     callbacks=[on_interval(10) do s\n         ensure_layout!(T, :g)\n         T_data = get_grid_data(T)\n         @info \"T(z=0) residual: $(maximum(abs.(T_data[:, 1] .- 1.0)))\"\n     end])","title":"Inspecting BC Satisfaction"},{"category":"section","location":"pages/tau_method/#Common-Pitfalls","page":"Tau Method","text":"","title":"Common Pitfalls"},{"category":"section","location":"pages/tau_method/#1.-Wrong-number-of-tau-fields","page":"Tau Method","text":"If you have two wall BCs on u but only declare one tau field in the equation, the matrix system is under-determined. You'll see Warning: Matrix is not square: rows=70, cols=65 followed by Warning: Non-square filtered system: group=(3, nothing), valid_eqn=14, valid_var=13 at solver-build time (the exact counts depend on your resolution), and the solve then dies with a DimensionMismatch.\n\nFix: count the scalar constraint rows — including gauge conditions like integ(p) = 0, and counting one row per component for a vector BC — and declare one tau DOF per row.","title":"1. Wrong number of tau fields"},{"category":"section","location":"pages/tau_method/#2.-Missing-lift()-term-in-the-equation","page":"Tau Method","text":"A tau field declared in problem.variables but never referenced in any equation contributes a zero column to the LHS matrix, making the system rank-deficient.\n\nFix: every tau field must appear inside exactly one lift() (or τ_lift() substitution) on the implicit (left-hand) side of some equation.","title":"2. Missing lift() term in the equation"},{"category":"section","location":"pages/tau_method/#3.-Wrong-lift-basis-—-and-what-it-does-*not*-cause","page":"Tau Method","text":"Writing lift(tau, zb, -1) (the state basis), or using the first-derivative basis where the second-derivative basis is meant, is a style problem, not a numerical one: as explained above, the current solver ignores op.basis and sizes the delta column from the problem's own Chebyshev basis, so these spellings build identical matrices and produce the same solution to the last bit. Nothing diverges at high Nz because of this choice — the lift basis is not a conditioning control.\n\nFix: still pass the basis that names the space you are lifting into — derivative_basis(zb, 1) for a first-order gradient substitution, derivative_basis(zb, 2) for a direct second-order equation. It says what you mean, matches every shipped example, and stays correct if Tarang later makes the lift basis load-bearing.","title":"3. Wrong lift basis — and what it does not cause"},{"category":"section","location":"pages/tau_method/#4.-Tau-field-bases-don't-match","page":"Tau Method","text":"A tau field must live on the complement of the state field's bases — the state's bases minus the coupled direction. For a 2D state on (xbasis, zbasis), the tau lives on (xbasis,); for a 0-D gauge like tau_p it's ().\n\nIf you accidentally declare tau_u1 = ScalarField(dist, \"tau_u1\", (xbasis, zbasis)) (same bases as u), the lift call itself still constructs — the mistake is not caught there. It surfaces at solver build as Warning: Matrix is not square: rows=70, cols=180 (the tau contributes a whole Chebyshev spectrum of columns instead of one), and the solve then throws a DimensionMismatch.\n\nFix: drop the Chebyshev axis when declaring tau fields.","title":"4. Tau field bases don't match"},{"category":"section","location":"pages/tau_method/#5.-Non-square-system-at-DC-mode","page":"Tau Method","text":"Most non-square warnings at the DC Fourier mode come from a missing gauge BC like integ(p) = 0. The valid-mode filter can only drop a zero row if there's a paired 0-D tau column (like tau_p) to drop along with it.\n\nFix: make sure every PDE with a gauge ambiguity has both a tau_* field and a corresponding integ() BC.","title":"5. Non-square system at DC mode"},{"category":"section","location":"pages/tau_method/#6.-BC-F-value-not-reaching-the-stepper","page":"Tau Method","text":"For inhomogeneous BCs like T(z=0) = 1, the stepper has to carry the value 1 through the stage RHS assembly. This happens automatically through gather_alg_F! + the apply_bc_override! path in step_subproblem_rk!, but only if the BC equation has a non-zero F expression in equation_data[eq_idx][\"F\"].\n\nSymptom of the bug: max|T| decays to zero over time even though T(z=0) = 1 is declared, OR max|T| sticks at 1/γ = 2+√2 ≈ 3.414 (the classical 1/γ scaling factor from RK222's implicit coefficient).\n\nIf you see this: confirm you're on a recent version — this was a regression fixed after the subproblem-architecture rewrite. The apply_bc_override! override is what enforces L_row·X = F_BC at every stage regardless of accumulation history.","title":"6. BC F value not reaching the stepper"},{"category":"section","location":"pages/tau_method/#Historical-Note","page":"Tau Method","text":"The tau method was introduced by Cornelius Lanczos in 1938 as an approximation technique: rather than solving a PDE exactly, he sought polynomial approximations that satisfied the PDE with a small residual (the \"tau error\"). It was refined for spectral methods by Steven Orszag, David Gottlieb, and others in the 1970s–80s, and adapted to modern lift-based formulations by Keaton Burns et al. in the Dedalus project in the 2010s.\n\nThe name \"tau\" (τ) comes from Lanczos's notation for the residual/correction terms introduced when truncating the polynomial expansion and enforcing boundary conditions.","title":"Historical Note"},{"category":"section","location":"pages/tau_method/#References","page":"Tau Method","text":"","title":"References"},{"category":"section","location":"pages/tau_method/#Textbooks","page":"Tau Method","text":"Canuto, C., Hussaini, M. Y., Quarteroni, A., & Zang, T. A. (2006). Spectral Methods: Fundamentals in Single Domains. Springer. — Rigorous treatment of tau and Galerkin methods.\nBoyd, J. P. (2001). Chebyshev and Fourier Spectral Methods (2nd ed.). Dover. — Very readable; freely available online.\nTrefethen, L. N. (2000). Spectral Methods in MATLAB. SIAM. — Practical, code-oriented.\nPeyret, R. (2002). Spectral Methods for Incompressible Viscous Flow. Springer. — Detailed treatment of Navier–Stokes with spectral methods.","title":"Textbooks"},{"category":"section","location":"pages/tau_method/#Key-papers","page":"Tau Method","text":"Lanczos, C. (1938). \"Trigonometric interpolation of empirical and analytical functions.\" Journal of Mathematics and Physics, 17(1–4), 123–199. — Original tau method paper.\nBurns, K. J., Vasil, G. M., Oishi, J. S., Lecoanet, D., & Brown, B. P. (2020). \"Dedalus: A flexible framework for numerical simulations with spectral methods.\" Physical Review Research, 2, 023068. — Modern lift-based tau method as implemented in Tarang and Dedalus.\nOrszag, S. A. (1971). \"Accurate solution of the Orr-Sommerfeld stability equation.\" Journal of Fluid Mechanics, 50(4), 689–703. — Classic application to hydrodynamic stability.","title":"Key papers"},{"category":"section","location":"pages/tau_method/#See-Also","page":"Tau Method","text":"Boundary Conditions Tutorial: step-by-step BC examples\nBases: spectral bases (Chebyshev, Fourier, Legendre, Jacobi)\nSolvers: using IVP / LBVP / NLBVP solvers\nAPI: Problems: programmatic API for adding equations and BCs\n2D RBC Tutorial: complete Rayleigh–Bénard convection walkthrough","title":"See Also"},{"category":"section","location":"pages/timesteppers/#Time-Steppers","page":"Time Steppers","text":"Time steppers integrate PDEs forward in time.","title":"Time Steppers"},{"category":"section","location":"pages/timesteppers/#Overview","page":"Time Steppers","text":"Tarang.jl provides several families of time integration schemes:\n\nFamily Type Best For\nRK IMEX Runge-Kutta General problems\nCNAB Multistep IMEX Moderately stiff\nSBDF Multistep IMEX Stiff problems\nETD Exponential Time Differencing Very stiff linear terms","title":"Overview"},{"category":"section","location":"pages/timesteppers/#IMEX-Runge-Kutta","page":"Time Steppers","text":"All RK methods use Additive Runge-Kutta (ARK) schemes:\n\nLinear terms (LHS) are treated implicitly\nNonlinear terms (RHS) are treated explicitly\n\nThis allows stable integration of stiff problems with larger timesteps.","title":"IMEX Runge-Kutta"},{"category":"section","location":"pages/timesteppers/#RK111","page":"Time Steppers","text":"First-order IMEX method (Backward Euler / Forward Euler).\n\ntimestepper = RK111()\n\nImplicit part: Backward Euler (linear terms)\nExplicit part: Forward Euler (nonlinear terms)\nAccuracy: O(Δt)\nUse case: Testing, simple problems","title":"RK111"},{"category":"section","location":"pages/timesteppers/#RK222","page":"Time Steppers","text":"Second-order IMEX Runge-Kutta using a three-stage matched ARS tableau (an explicit first stage followed by two diagonally implicit stages).\n\ntimestepper = RK222()\n\nImplicit part: ESDIRK with γ = 1 - 1/√2 on stages 2 and 3\nExplicit part: matched three-row explicit tableau with c = [0, γ, 1]\nAccuracy: O(Δt²)\nStability: L-stable\nUse case: General purpose (recommended)","title":"RK222"},{"category":"section","location":"pages/timesteppers/#RK443","page":"Time Steppers","text":"Third-order, 4-stage IMEX Runge-Kutta (Kennedy & Carpenter ARK3(2)4L[2]SA).\n\ntimestepper = RK443()\n\nImplicit part: 4-stage ESDIRK (γ ≈ 0.4359)\nExplicit part: 4-stage explicit RK\nAccuracy: O(Δt³)\nStability: L-stable\nUse case: High accuracy requirements, stiff problems","title":"RK443"},{"category":"section","location":"pages/timesteppers/#RKSMR","page":"Time Steppers","text":"The Spalart–Moser–Rogers semi-implicit scheme, stored as an equivalent four-stage additive Runge-Kutta tableau.\n\ntimestepper = RKSMR()\n\nExplicit part: third-order treatment of nonlinear/advection terms\nImplicit part: second-order treatment of the linear term\nRuntime path: the same generic IMEX driver used by RK222 and RK443\nUse case: incompressible spectral DNS where the classic SMR accuracy profile is desired","title":"RKSMR"},{"category":"section","location":"pages/timesteppers/#Multistep-IMEX-Methods","page":"Time Steppers","text":"Implicit-Explicit methods treat stiff (linear) terms implicitly and non-stiff (nonlinear) terms explicitly.","title":"Multistep IMEX Methods"},{"category":"section","location":"pages/timesteppers/#CNAB-(Crank-Nicolson-Adams-Bashforth)","page":"Time Steppers","text":"CNAB1()  # 1st order\nCNAB2()  # 2nd order (recommended)\n\nLinear terms: Crank-Nicolson (implicit)\nNonlinear terms: Adams-Bashforth (explicit)\nUse case: Diffusion-dominated flows","title":"CNAB (Crank-Nicolson Adams-Bashforth)"},{"category":"section","location":"pages/timesteppers/#SBDF-(Semi-implicit-BDF)","page":"Time Steppers","text":"SBDF1()  # 1st order\nSBDF2()  # 2nd order (recommended)\nSBDF3()  # 3rd order\nSBDF4()  # 4th order\n\nLinear terms: BDF (implicit)\nNonlinear terms: Extrapolation (explicit)\nUse case: Stiff problems, high Re flows","title":"SBDF (Semi-implicit BDF)"},{"category":"section","location":"pages/timesteppers/#How-Boundary-Conditions-Are-Enforced-During-Time-Stepping","page":"Time Steppers","text":"Tarang's stepper is aware of the fact that boundary-condition rows are algebraic (have no time derivative), which makes the full system a differential-algebraic equation (DAE) rather than a pure ODE. If you write a BC like T(z=0) = 1, the corresponding row of the combined M·dX/dt + L·X = F system has M_row = 0, and the raw accumulated IMEX-RK stage RHS formula produces the wrong scaling factor for that row.\n\nConcretely, for RK222 (γ = 1 − 1/√2 ≈ 0.293), the naive formula enforces L_row·X = (1/γ)·F_BC = (2+√2)·F_BC ≈ 3.414·F_BC instead of F_BC. A T(z=0) = 1 BC would produce T(z=0) ≈ 3.414 in grid space.","title":"How Boundary Conditions Are Enforced During Time Stepping"},{"category":"section","location":"pages/timesteppers/#The-apply_bc_override!-fix","page":"Time Steppers","text":"step_subproblem_rk! handles this by overriding the BC-row entries of the stage RHS at each stage:\n\n# (pseudo-code — internal stepper logic, not user API)\n\n# After the normal accumulation:\nrhs = M*X_n + dt * Σ (A^E[i,j]*F_j − A^I[i,j]*L*X_j)\n\n# Override BC rows with the correct direct-enforcement value:\nfor r in sp.bc_rows\n    rhs[r] = dt * a_ii * F_BC[r]\nend\n\nAfter this override, the stage solve (M + dt·a_ii·L)·X = rhs gives dt·a_ii·L_row·X = dt·a_ii·F_BC → L_row·X = F_BC directly, at every stage. Since the singular-M final-update path keeps the last stage's value, this guarantees the BC is correctly enforced in the final solution.\n\nsp.bc_rows comes from the subproblem builder's equation-size classification (rows with eq_size < Nz — small algebraic rows like wall BCs and gauge constraints). The override only touches those rows; larger Nz-sized algebraic rows like trace(grad_u) + tau_p = 0 (continuity) have F = 0 and ride the normal accumulation path without issue.\n\nstep_subproblem_multistep! does the same thing for CNAB / SBDF — the override coefficient there is b[0] (the implicit-diagonal coefficient of the multistep formula) instead of dt·a_ii, but the mechanism is identical.","title":"The apply_bc_override! fix"},{"category":"section","location":"pages/timesteppers/#Time-and-space-dependent-BCs","page":"Time Steppers","text":"For BCs that vary with time (T(z=0) = sin(t)) or space (T(z=0) = sin(2πx/Lx)) or both, the stepper refreshes the BC F value before each stage solve:\n\nupdate_time_dependent_bcs!(bc_manager, t + c[i]·dt) — evaluates the BC expression string at the stage time.\n_apply_bc_values_to_equations!(solver, stage_time) — writes the fresh value into equation_data[eq_idx][\"F\"] (as ConstantOperator for scalar values or ArrayOperator for spatially-varying ones).\ngather_alg_F! picks up the new F on the next read.\n\nThis per-stage refresh (gated on has_time_dependent_bcs, so it's free when BCs are constant) preserves the stepper's formal order of accuracy for rapidly-varying BCs. Without it, a multi-stage RK method would freeze the BC at the t+dt value and see O(dt) error at intermediate stages, silently dropping the order from 2 to 1.","title":"Time- and space-dependent BCs"},{"category":"section","location":"pages/timesteppers/#Multistep-steppers-fall-through-to-the-same-code-path","page":"Time Steppers","text":"RK dispatch via step_rk_imex! → step_subproblem_rk! is the obvious path, but CNAB1/CNAB2/SBDF1..4 also dispatch through the subproblem path whenever subproblems are available (problem.parameters[\"subproblems\"] !== nothing). The dispatch happens inside each step_<scheme>! function: it computes the scheme's (a, b, c) coefficient tuples and calls the generic step_subproblem_multistep!(state, solver, sps, a, b, c).\n\nThis means every IMEX stepper that supports the subproblem path gets DAE-correct BC handling automatically. The global-matrix path is still used when subproblem decomposition is unavailable, including pure-periodic/global systems. Inhomogeneous tau BCs should use the subproblem path; the global multistep path does not carry those algebraic BC values through the same override machinery.","title":"Multistep steppers fall through to the same code path"},{"category":"section","location":"pages/timesteppers/#Why-not-just-always-use-the-override","page":"Time Steppers","text":"The override path is only correct for algebraic rows with F ≠ 0 that we want to enforce directly. Applying it to PDE rows (which have M ≠ 0) would break the time integration. The row classification is done once per subproblem at matrix-build time — sp.bc_rows lists exactly the rows where the override is needed, and nothing else.","title":"Why not just always use the override"},{"category":"section","location":"pages/timesteppers/#Exponential-Time-Differencing-(ETD)","page":"Time Steppers","text":"Exponential integrators solve the linear part of the ODE exactly using matrix exponentials, while treating nonlinear terms explicitly. This is fundamentally different from IMEX methods.","title":"Exponential Time Differencing (ETD)"},{"category":"section","location":"pages/timesteppers/#How-ETD-Works","page":"Time Steppers","text":"For the semi-linear ODE:\n\nfracdudt = Lu + N(u)\n\nwhere L is the linear operator and N(u) is the nonlinear term, ETD methods use the variation of constants formula:\n\nu(t_n+1) = e^h L u(t_n) + int_0^h e^(h-tau)L N(u(t_n + tau)) dtau\n\nThe key insight is that exp(hL) propagates the linear part exactly, eliminating stability constraints from stiff linear terms entirely.","title":"How ETD Works"},{"category":"section","location":"pages/timesteppers/#ETD-vs-IMEX","page":"Time Steppers","text":"Aspect IMEX ETD\nLinear terms Implicit (approximate) Exponential (exact)\nLinear solve Yes, every step No (but needs exp(hL))\nStiffness Limited by approximation No limit from L\nBest for General stiff problems Diagonal L (Fourier)","title":"ETD vs IMEX"},{"category":"section","location":"pages/timesteppers/#ETD_RK222","page":"Time Steppers","text":"Second-order exponential Runge-Kutta method (Cox-Matthews 2002).\n\ntimestepper = ETD_RK222()\n\nAlgorithm (Cox-Matthews 2002, eq. 22):\n\nStage 1 (predictor): a_n = exp(hL) * u_n\n                     c   = a_n + h * phi_1(hL) * N(u_n)\nStage 2 (corrector): u_{n+1} = c + h * phi_2(hL) * (N(c) - N(u_n))\n\nwhere phi1(z) = (exp(z) - 1) / z and phi2(z) = (exp(z) - 1 - z) / z².\n\nThe corrector needs both N(u_n) and N(c), and the difference must be weighted by phi_2 — a corrector that applies phi_1 to N(c) alone is only first order.\n\nLinear part: Exact via exp(hL) and phi functions\nNonlinear part: Explicit 2-stage Runge-Kutta\nAccuracy: O(dt^2)\nStability: Unconditionally stable for linear terms\nUse case: Very stiff diffusion, reaction-diffusion","title":"ETD_RK222"},{"category":"section","location":"pages/timesteppers/#ETD_CNAB2","page":"Time Steppers","text":"Second-order exponential Adams-Bashforth method.\n\ntimestepper = ETD_CNAB2()\n\nAlgorithm:\n\nw = h_n / h_{n-1}          (timestep ratio; w = 1 for constant dt)\nu_{n+1} = exp(hL) * u_n\n          + h * phi_1(hL) * N(u_n)\n          + h * w * phi_2(hL) * (N(u_n) - N(u_{n-1}))\n\nDespite the name, the linear term is not treated with Crank-Nicolson — \"CNAB\" refers only to the two-step structure. Note that the history term carries phi_2, not phi_1: applying a single phi_1 to a plain AB2 extrapolation (3/2)N_n − (1/2)N_{n−1} is second order only in the hL → 0 limit, and loses accuracy exactly in the stiff regime ETD exists for.\n\nLinear part: Exact via exponential propagator\nNonlinear part: Adams-Bashforth extrapolation\nAccuracy: O(dt^2)\nStartup: Uses ETD_RK222 for first step\nUse case: Long time integrations with stiff diffusion","title":"ETD_CNAB2"},{"category":"section","location":"pages/timesteppers/#ETD_SBDF2","page":"Time Steppers","text":"Second-order exponential multistep method.\n\ntimestepper = ETD_SBDF2()\n\nAlgorithm: Uses the same implemented variable-step two-step exponential update as ETD_CNAB2:\n\nw = h_n / h_{n-1}\nu_{n+1} = exp(hL) * u_n + h * phi_1(hL) * N_n\n          + h * w * phi_2(hL) * (N_n - N_{n-1})\n\nThis is the same second-order exponential-multistep update as ETD_CNAB2 — both are the ETD analogue of AB2, obtained by interpolating N linearly between t_{n-1} and t_n and integrating the variation-of-constants formula exactly. The two schemes differ only in bookkeeping; pick either.\n\nLinear part: Exact via exponential + phi functions\nNonlinear part: BDF-style extrapolation\nAccuracy: O(dt^2)\nStartup: Uses ETD_RK222 for first step\nUse case: Stiff problems requiring multistep efficiency","title":"ETD_SBDF2"},{"category":"section","location":"pages/timesteppers/#Phi-Functions","page":"Time Steppers","text":"ETD methods use phi functions defined as:\n\nphi_0(z) = e^z\n\nphi_1(z) = frace^z - 1z\n\nphi_2(z) = frace^z - 1 - zz^2\n\nScalar arguments use a Taylor expansion near zero (|z| < 1e-8), where the e^z − 1 − z numerator cancels catastrophically against the z^2 denominator.\n\nFor the matrix case, phi_functions_matrix(L, dt) computes all three at once from the assembled L_matrix, choosing its method by the operator norm ‖dt·L‖:\n\n‖dt·L‖ method\n< 1e-8 Taylor series\n< 50 dense matrix exponential plus stable φ solves, with an eigendecomposition fallback when z is singular (the k=0 mode of a pure diffusion operator makes it so)\n≥ 50 Krylov approximation, column by column, via ExponentialUtilities.phiv\n\nMatrix ETD is rejected above size 4096 because it requires dense O(n²) storage — see the serial size limit below.\n\nThe Krylov branch announces itself with Warning: Matrix is large or stiff (norm=...), using Krylov approximation. It is not an error — a stiff L is the whole point of ETD, so any useful ETD run will trip it (the 1-D heat example below has ‖dt·L‖ ≈ 269 and still reproduces the analytic solution to 8 digits).\n\nThe result is cached and only recomputed when dt changes.","title":"Phi Functions"},{"category":"section","location":"pages/timesteppers/#When-to-Use-ETD","page":"Time Steppers","text":"Ideal for:\n\nFourier-based spectral methods (L is diagonal)\nVery stiff diffusion (high viscosity/diffusivity)\nReaction-diffusion equations\nProblems where IMEX requires very small dt\n\nLess suitable for:\n\nNon-diagonal L (Chebyshev with BCs) - use IMEX instead\nLarge serial problems (see the size limit below)\nProblems with complex boundary conditions","title":"When to Use ETD"},{"category":"section","location":"pages/timesteppers/#Setup-(none-required)","page":"Time Steppers","text":"You do not build or install a linear operator. InitialValueSolver runs build_solver_matrices!, which assembles L_matrix, M_matrix and F_vector from the equation strings and stores them in problem.parameters; the ETD steppers read L_matrix from there (and use M_matrix when present). Anything you write on the left-hand side of the equation is the linear operator L that gets propagated exactly; anything on the right-hand side is the nonlinear term N. So writing add_equation!(problem, \"∂t(T) - nu*lap(T) = 0\") is all it takes for ETD_RK222() to propagate nu·∇² with exp(hL) — see the complete example below.\n\nBecause the solver assembles these matrices itself, application code should not invent or manually insert a separate \"L_matrix\": a hand-built matrix will not share the solver's state ordering, and the stepper will silently propagate the wrong operator.","title":"Setup (none required)"},{"category":"section","location":"pages/timesteppers/#Size-limit-(serial)","page":"Time Steppers","text":"In serial the exponential is taken of the dense global matrix, so the cost is O(n³) in the total number of coefficients. Above n = 4096 degrees of freedom the phi-function builder refuses, and the stepper emits\n\nWarning: ETD-RK222 failed: ArgumentError(\"ETD matrix exponential requires dense O(n²) storage but n=8193 is too large ...\"), falling back to RK222\n\nand carries on with RK222. If you asked for ETD and see that warning, you are not getting ETD — reduce the resolution or switch to SBDF2.","title":"Size limit (serial)"},{"category":"section","location":"pages/timesteppers/#Under-MPI-(pure-Fourier)","page":"Time Steppers","text":"The serial size limit does not apply to a distributed pure-Fourier problem: a global dense matrix exponential cannot run on distributed field vectors, so the distributed path steps each Fourier mode with its own scalar exponential and never forms a global matrix (verified: a 128×128 Fourier heat problem, 8320 coefficients, steps at np=2 with no fallback).\n\nThe trade-off is that all three exported ETD types (ETD_RK222, ETD_CNAB2, ETD_SBDF2) dispatch to the same distributed per-mode ETD-RK2 implementation on that path — the distinction between the exponential-RK and the exponential-multistep variants is lost. If that distinction matters to you, use the serial global-matrix path.","title":"Under MPI (pure-Fourier)"},{"category":"section","location":"pages/timesteppers/#Example:-1D-Heat-Equation-with-ETD","page":"Time Steppers","text":"The linear part is propagated exactly, so for a purely linear problem ETD is exact at any timestep — dt = 0.1 here is far past the explicit diffusive stability limit and still lands on the analytic answer.\n\nusing Tarang\n\n# Fourier domain (L is diagonal)\ncoords  = CartesianCoordinates(\"x\")\ndist    = Distributor(coords; dtype=Float64, device=CPU())\nx_basis = RealFourier(coords[\"x\"]; size=64, bounds=(0.0, 2*pi))\ndomain  = Domain(dist, (x_basis,))\n\nT = ScalarField(domain, \"T\")\n\nproblem = IVP([T])\nadd_parameters!(problem, nu=1.0)\nadd_equation!(problem, \"∂t(T) - nu*lap(T) = 0\")     # L is built from this line\n\nx, = local_grids(dist, x_basis)\nensure_layout!(T, :g)\nget_grid_data(T) .= sin.(x)\nensure_layout!(T, :c)\n\n# ETD solver - no stability limit from diffusion!\nsolver = InitialValueSolver(problem, ETD_RK222(); dt=0.1)\n\nfor _ in 1:10          # t = 1.0\n    step!(solver)\nend\n\nensure_layout!(T, :g)\nmaximum(abs, get_grid_data(T))      # 0.36787944 == exp(-nu*1.0), to 8 digits","title":"Example: 1D Heat Equation with ETD"},{"category":"section","location":"pages/timesteppers/#References","page":"Time Steppers","text":"Cox, S. M., & Matthews, P. C. (2002). \"Exponential Time Differencing for Stiff Systems\". J. Comput. Phys. 176, 430-455.\nHochbruck, M., & Ostermann, A. (2010). \"Exponential integrators\". Acta Numerica 19, 209-286.\nKassam, A.-K., & Trefethen, L. N. (2005). \"Fourth-Order Time Stepping for Stiff PDEs\". SIAM J. Sci. Comput. 26(4), 1214-1233.","title":"References"},{"category":"section","location":"pages/timesteppers/#Choosing-a-Timestepper","page":"Time Steppers","text":"","title":"Choosing a Timestepper"},{"category":"section","location":"pages/timesteppers/#By-Problem-Stiffness","page":"Time Steppers","text":"Stiffness Indicator Recommended\nMild Explicit CFL dominates RK222\nModerate linear stiffness Implicit linear solve permits a larger step RK443, CNAB2, SBDF2, RKSMR\nSmooth solution needing higher temporal order Fixed/slowly varying step and adequate startup history RK443, SBDF3, SBDF4\nVery stiff, manageable global matrix Dense exponential is affordable ETDRK222, ETDCNAB2, ETD_SBDF2\nPure-Fourier diagonal linear operator Per-mode implicit division is available DiagonalIMEX_RK222, DiagonalIMEX_RK443, DiagonalIMEX_SBDF2","title":"By Problem Stiffness"},{"category":"section","location":"pages/timesteppers/#By-Physics","page":"Time Steppers","text":"Problem Type Recommended\nGeneral purpose RK222, RK443\nDiffusion-dominated CNAB2, SBDF2\nHigh Rayleigh number RK222, RK443, or SBDF2 with CFL control\nTurbulence RK443, RKSMR, or SBDF2 with CFL control\nReaction-diffusion (Fourier) ETD_RK222\nVery stiff diffusion (Fourier) ETD_SBDF2","title":"By Physics"},{"category":"section","location":"pages/timesteppers/#Stability-Analysis","page":"Time Steppers","text":"","title":"Stability Analysis"},{"category":"section","location":"pages/timesteppers/#CFL-Condition-(Explicit)","page":"Time Steppers","text":"For explicit methods, the timestep is limited by:\n\nDelta t  fracC cdot Delta xu_max\n\nwhere C is the CFL number (typically 0.5-1.0).","title":"CFL Condition (Explicit)"},{"category":"section","location":"pages/timesteppers/#Diffusive-Stability-(IMEX)","page":"Time Steppers","text":"IMEX methods allow larger timesteps by treating diffusion implicitly:\n\nDelta t  fracC cdot Delta xu_max\n\nonly constrained by advection, not diffusion.\n\nwarning: Explicitly-treated diffusion still limits dt\nThat holds only for diffusion the timestepper actually treats implicitly. A spatially varying coefficient — most importantly an LES eddy viscosity νₑ — cannot go down the implicit path, so it is stepped explicitly and imposesDelta t le frac12 nu_max sum_i Delta x_i^-2(equivalently Δx²/(2dν) on an isotropic d-dimensional grid). The CFL controller does not enforce this unless you register the coefficient with add_diffusivity!(cfl, get_eddy_viscosity(model)).","title":"Diffusive Stability (IMEX)"},{"category":"section","location":"pages/timesteppers/#Adaptive-Time-Stepping","page":"Time Steppers","text":"","title":"Adaptive Time Stepping"},{"category":"section","location":"pages/timesteppers/#With-CFL","page":"Time Steppers","text":"CFL is constructed from the solver (not the problem), and velocities are registered as VectorFields. The canonical use is to hand the controller to run!, which recomputes dt every cadence iterations and updates solver.dt for you:\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\ncfl = CFL(solver; initial_dt=1e-3, cadence=5, safety=0.4, max_dt=0.01)\nadd_velocity!(cfl, u)                      # u must be a VectorField\n\nrun!(solver; stop_iteration=10, cfl=cfl, progress=false)\nsolver.dt                                  # updated by the controller\n\nIf you drive the loop yourself, call compute_timestep and pass the result to step!:\n\nwhile solver.sim_time < t_end\n    dt = compute_timestep(cfl)\n    step!(solver, dt)\nend\n\nThe remaining knobs are threshold, max_change and min_change (all ratios limiting how fast dt may change between recomputations). There is no min_dt field; the current step is cfl.current_dt.\n\nRegistering velocities alone gives an advection-only step. Add add_diffusivity!(cfl, ν) for anything diffusive you integrate explicitly — see Diffusive Limit.","title":"With CFL"},{"category":"section","location":"pages/timesteppers/#Manual-Adjustment","page":"Time Steppers","text":"# Reduce timestep if solver becomes unstable\nif any(isnan, get_grid_data(field))\n    solver.dt *= 0.5\n    error(\"NaN detected, reduce timestep\")\nend","title":"Manual Adjustment"},{"category":"section","location":"pages/timesteppers/#Multi-Step-Methods","page":"Time Steppers","text":"SBDF and CNAB methods require previous time level data:\n\n# First step uses lower order\n# SBDF2 uses SBDF1 for first step\n# Automatic in Tarang\n\nsolver = InitialValueSolver(problem, SBDF2(); dt=0.001)\n# First step: SBDF1\n# Subsequent steps: SBDF2","title":"Multi-Step Methods"},{"category":"section","location":"pages/timesteppers/#Performance-Comparison","page":"Time Steppers","text":"The RK counts below are the number of stages actually driven per step. Note that RK222 is a three-stage ESDIRK tableau (explicit first stage), so it costs three RHS evaluations, not two.\n\nMethod RHS evaluations/step Implicit/exponential work Memory\nRK111 1 1 implicit stage solve Medium\nRK222 3 2 implicit stage solves (the first stage is explicit) Medium\nRK443 4 3 implicit stage solves Higher\nRKSMR 4 3 implicit stage solves Higher\nCNAB2 1 1 implicit solve after startup Medium\nSBDF2 1 1 implicit solve after startup Medium\nETD_RK222 2 cached exp(hL), phi_1, and phi_2 actions Dense n×n (serial)\nETDCNAB2 / ETDSBDF2 1 cached exponential/φ actions after ETD-RK2 startup Dense n×n (serial)\n\nThe implicit factorization is cached and reused while dt and the operator are unchanged, so the factorization is paid once, not per step. Two consequences:\n\nAdaptive steps can cost more than fixed steps, because changing dt invalidates the cached factorization. (This is why CFL has a threshold ratio — it refuses to commit a new dt for a change smaller than that, rather than refactorizing every recomputation.)\nFor ETD, the O(n³) exponential is likewise paid once per distinct dt, but the dense n×n storage is permanent — which is why serial ETD is capped at n = 4096 coefficients.","title":"Performance Comparison"},{"category":"section","location":"pages/timesteppers/#Example-Usage","page":"Time Steppers","text":"The timestepper is the only thing that changes between the three families — the problem, the fields and the loop are identical. This 2-D advection-diffusion problem runs with any of them:\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb     = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2pi), dealias=3/2)\nyb     = RealFourier(coords[\"y\"]; size=16, bounds=(0.0, 2pi), dealias=3/2)\ndomain = Domain(dist, (xb, yb))\n\ns = ScalarField(domain, \"s\")\nu = VectorField(domain, \"u\")\n\nproblem = IVP([s, u])\nadd_parameters!(problem, nu=0.05)\nadd_equation!(problem, \"∂t(s) - nu*lap(s) = -u⋅∇(s)\")   # LHS implicit, RHS explicit\nadd_equation!(problem, \"∂t(u) - nu*lap(u) = 0\")\n\nset!(s, (x, y) -> sin(x) * cos(y))\nset!(u.components[1], (x, y) -> 0.5)\n\n# Pick one:\n#   RK443()      -- IMEX Runge-Kutta, stiff diffusion handled implicitly\n#   SBDF2()      -- multistep IMEX, one RHS evaluation per step\n#   ETD_RK222()  -- exponential, no stability limit from the linear term\nsolver = InitialValueSolver(problem, RK443(); dt=1e-3)\n\nt_end = 0.005\nwhile solver.sim_time < t_end\n    step!(solver)\nend\n\nSwapping RK443() for SBDF2() or ETD_RK222() changes nothing else and, at this timestep, agrees to 6 digits (max|s| = 0.999497 after 5 steps). In particular, the ETD stepper needs no extra setup: the problem equations already define the assembled linear operator it propagates.","title":"Example Usage"},{"category":"section","location":"pages/timesteppers/#Stochastic-Forcing","page":"Time Steppers","text":"For turbulence simulations requiring external energy injection, see Stochastic Forcing.\n\nKey points:\n\nForcing is generated once per timestep, constant across substeps\nRequired for proper Stratonovich calculus treatment\nSupports ring, isotropic, and custom forcing spectra\n\nRegister the forcing on the problem, against the variable it drives, before building the solver — add_stochastic_forcing! is the supported entry point, and the stepper picks the forcing up from there.\n\ns = ScalarField(domain, \"s\")                      # starts at zero\n\nproblem = IVP([s])\nadd_parameters!(problem, nu=0.01)\nadd_equation!(problem, \"∂t(s) - nu*lap(s) = 0\")   # no explicit source term\n\nforcing = StochasticForcing(\n    field_size   = (16, 16),        # grid shape of the forced field\n    domain_size  = (2pi, 2pi),\n    forcing_rate = 0.1,\n    k_forcing    = 4.0,\n    dk_forcing   = 1.0,\n    dt           = 1e-3,\n)\nadd_stochastic_forcing!(problem, :s, forcing)     # :s is the variable's name\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\nrun!(solver; stop_iteration=20, progress=false)   # forcing drives s off zero (max|s| ~ 4e-3)\n\nThe magnitude varies from run to run — the forcing is white in time. Pass an explicit rng= to StochasticForcing if you need a reproducible realization.","title":"Stochastic Forcing"},{"category":"section","location":"pages/timesteppers/#See-Also","page":"Time Steppers","text":"Solvers: Using time steppers with solvers\nCFL and Analysis: Adaptive time stepping\nStochastic Forcing: External forcing for turbulence\nAPI: Timesteppers: Complete reference","title":"See Also"},{"category":"section","location":"pages/domains/#Domains","page":"Domains","text":"Domains combine spectral bases with the MPI distributor to define the computational space.","title":"Domains"},{"category":"section","location":"pages/domains/#Creating-a-Domain","page":"Domains","text":"using Tarang\n\n# Setup coordinates and distributor\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# Define spectral bases\nx_basis = RealFourier(coords[\"x\"]; size=32, bounds=(0.0, 2π))\nz_basis = ChebyshevT(coords[\"z\"]; size=16, bounds=(0.0, 1.0))\n\n# Create domain\ndomain = Domain(dist, (x_basis, z_basis))\n\nLeave mesh= off the Distributor unless you have a reason to set it: the mesh is chosen automatically from the number of MPI ranks, and the automatic choice is the one that works (see Mesh Selection below). Under MPI a Chebyshev axis must come first in the basis tuple; see Domain Decomposition.","title":"Creating a Domain"},{"category":"section","location":"pages/domains/#Domain-Properties","page":"Domains","text":"domain.dist                 # MPI distributor\ndomain.bases                # Tuple of spectral bases\ndomain.dim                  # Number of dimensions -> 2\n\nglobal_shape(domain, :g)    # Global grid shape        -> (32, 16)\nglobal_shape(domain, :c)    # Global coefficient shape -> (17, 16)  (rfft halves axis 1)\nlocal_shape(domain, :g)     # This process's grid slab -> (32, 16) serially\n\ngrid_spacing(domain)        # Per-axis spacing -> [0.19634954, 0.01092620]\nvolume(domain)              # Domain volume    -> 6.283185307179586\n\nShapes are functions, not fields: Domain itself only stores dist, bases and dim.","title":"Domain Properties"},{"category":"section","location":"pages/domains/#Quick-Domain-Creation","page":"Domains","text":"Tarang provides convenience constructors for common domain types. Each returns a single Domain; the distributor is available as domain.dist.","title":"Quick Domain Creation"},{"category":"section","location":"pages/domains/#Periodic-Box","page":"Domains","text":"# 2D doubly-periodic domain on [0, 2π)²\ndomain = PeriodicDomain(128, 128)\n\n# 3D triply-periodic domain with a custom box size\ndomain = PeriodicDomain(64, 64, 64; L=(1.0, 1.0, 1.0))\n\ndist = domain.dist   # the distributor it built for you","title":"Periodic Box"},{"category":"section","location":"pages/domains/#Channel-Domain","page":"Domains","text":"# Periodic in x (Fourier), bounded in z (Chebyshev on [0, Lz])\ndomain = ChannelDomain(256, 64; Lx=4.0, Lz=1.0)\n\n# 3D: periodic in x and y, bounded in z (Chebyshev, always on [-1, 1])\ndomain = ChannelDomain3D(64, 64, 32; Lx=4π, Ly=2π)\n\nChannelDomain3D fixes its Chebyshev axis to [-1, 1]; it accepts an Lz keyword but ignores it. If you need a different wall separation, build the domain explicitly.\n\nBoth channel constructors put the Chebyshev axis last, so they are serial-only: under MPI they fail at construction with the \"reorder your bases\" error described in Domain Decomposition. For a distributed channel, assemble the domain by hand with the Chebyshev axis first.","title":"Channel Domain"},{"category":"section","location":"pages/domains/#Chebyshev-Box","page":"Domains","text":"# Bounded in all directions (Chebyshev on every axis)\ndomain = ChebyshevDomain(64, 64; bounds=((0.0, 1.0), (0.0, 2.0)))\n\nA pure-Chebyshev domain is serial-only as well; on more than one process it errors with \"MPI parallelization is not supported for pure Chebyshev domains\". Distributed runs need at least one Fourier axis to decompose.","title":"Chebyshev Box"},{"category":"section","location":"pages/domains/#Working-with-Domains","page":"Domains","text":"","title":"Working with Domains"},{"category":"section","location":"pages/domains/#Creating-Fields","page":"Domains","text":"# Directly from the domain (recommended)\nT = ScalarField(domain, \"T\")\nu = VectorField(domain, \"u\")\n\n# Explicit form, when you want a different dtype or a subset of the bases\nT = ScalarField(dist, \"T\", domain.bases, Float64)\nu = VectorField(dist, coords, \"u\", domain.bases, Float64)","title":"Creating Fields"},{"category":"section","location":"pages/domains/#Grid-Information","page":"Domains","text":"# Per-axis local grid points\nfor (i, basis) in enumerate(domain.bases)\n    grid = local_grid(basis, dist, 1)   # third argument is the scale factor\n    println(\"Axis $i ($(basis.meta.element_label)): $(length(grid)) local points\")\nend\n\n# All axes at once — this is the distributed-safe way to get coordinates\nx, z = local_grids(dist, x_basis, z_basis)   # sizes (32,) and (16,)\n\n# Fourier wavenumbers of a basis\nk = wavenumbers(x_basis)   # 32 entries: [0.0, 1.0, 1.0, 2.0, 2.0, …]\n\nlocal_grids returns each process's local slice, so the same code is correct in serial and under MPI.\n\nwavenumbers returns physical wavenumbers k = 2πn/L, one per grid point (length N, in conjugate pairs) — not the half-spectrum that a RealFourier coefficient array stores.","title":"Grid Information"},{"category":"section","location":"pages/domains/#Domain-Dimensions","page":"Domains","text":"# Total number of grid points\ntotal_points = prod(global_shape(domain, :g))\n\n# Number of spectral modes per axis\ntotal_modes = prod(basis.meta.size for basis in domain.bases)\n\n# Domain volume\nvol = volume(domain)\n\nBasis metadata lives under basis.meta: basis.meta.size, basis.meta.bounds, basis.meta.dealias, basis.meta.element_label, basis.meta.dtype.","title":"Domain Dimensions"},{"category":"section","location":"pages/domains/#Multi-Dimensional-Layouts","page":"Domains","text":"","title":"Multi-Dimensional Layouts"},{"category":"section","location":"pages/domains/#Grid-Space-(:g)","page":"Domains","text":"Data stored as real values at collocation points:\n\nensure_layout!(field, :g)\n# get_grid_data(field) contains real grid values\n# Shape: (32, 16) for the domain above","title":"Grid Space (:g)"},{"category":"section","location":"pages/domains/#Coefficient-Space-(:c)","page":"Domains","text":"Data stored as spectral coefficients:\n\nensure_layout!(field, :c)\n# get_coeff_data(field) contains spectral coefficients\n# Shape: (17, 16) — a RealFourier axis stores only the half-spectrum","title":"Coefficient Space (:c)"},{"category":"section","location":"pages/domains/#Layout-Transforms","page":"Domains","text":"# Grid to spectral\nensure_layout!(field, :c)\n\n# Spectral to grid\nensure_layout!(field, :g)\n\n# Check current layout\ncurrent_layout = field.current_layout  # :g or :c\n\nThe round trip is exact to roundoff (measured 4.4e-16 on a 32×16 Fourier–Chebyshev field).","title":"Layout Transforms"},{"category":"section","location":"pages/domains/#Domain-Decomposition","page":"Domains","text":"PencilArrays decomposes the trailing dimensions of the domain, so a 2D domain gets a 1D mesh and a 3D domain gets a 2D mesh:\n\n2D Domain (32 × 16), pure Fourier, on 2 processes (auto mesh = (2,)):\n\nProcess 0: x ∈ 1:32, y ∈ 1:8      # local shape (32, 8)\nProcess 1: x ∈ 1:32, y ∈ 9:16     # local shape (32, 8)\n\nThe same domain on 4 processes (auto mesh = (4,)):\n\nProcess 0: x ∈ 1:32, y ∈  1:4     # local shape (32, 4)\nProcess 1: x ∈ 1:32, y ∈  5:8\nProcess 2: x ∈ 1:32, y ∈  9:12\nProcess 3: x ∈ 1:32, y ∈ 13:16\n\nBecause the decomposed axes are the trailing ones, and a Chebyshev axis cannot be decomposed (its DCT needs the whole axis on each rank), a Chebyshev axis must be the first axis under MPI:\n\n# Under MPI: Chebyshev first, Fourier after\ncoords = CartesianCoordinates(\"z\", \"x\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nz_basis = ChebyshevT(coords[\"z\"]; size=12, bounds=(0.0, 1.0))\nx_basis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\ndomain  = Domain(dist, (z_basis, x_basis))\n\nA Chebyshev axis placed last raises a clear error at Domain construction rather than producing a wrong answer:\n\nMPI mixed Fourier-Chebyshev: the decomposed (trailing) axis/axes [2] are non-Fourier … Reorder your bases so the Chebyshev axis comes BEFORE the Fourier axes\n\nIn serial the order does not matter, and Fourier-first is the conventional choice.","title":"Domain Decomposition"},{"category":"section","location":"pages/domains/#Accessing-Local-Data","page":"Domains","text":"# Local data array (a PencilArray when running on more than one process)\nlocal_data = get_grid_data(field)\n\n# Local array size\nlocal_size = size(local_data)\n\n# Global index range owned by this process along an axis (internal function)\nrng = Tarang.local_indices(dist, axis, global_size)   # a UnitRange, e.g. 9:16\nstart_idx, end_idx = first(rng), last(rng)\n\nlocal_indices returns a range. Do not write start_idx, end_idx = Tarang.local_indices(…): Julia would destructure the range element-wise and silently bind (1, 2).\n\nThe same information is available straight from the array under MPI:\n\nusing PencilArrays\nPencilArrays.range_local(get_grid_data(field))   # e.g. (1:32, 9:16) on rank 1 of 2\nparent(get_grid_data(field))                     # the raw local storage","title":"Accessing Local Data"},{"category":"section","location":"pages/domains/#Coordinate-Systems","page":"Domains","text":"","title":"Coordinate Systems"},{"category":"section","location":"pages/domains/#Cartesian","page":"Domains","text":"coords = CartesianCoordinates(\"x\", \"y\", \"z\")\n# ∂x, ∂y, ∂z derivatives (equivalently d(u, x), d(u, y), d(u, z))\n# grad/∇, div, curl, lap/Δ in standard form\n\ndx, dy, dz are not derivative operators. The parser does not know them, and it warns Unknown variable: dx / Unknown function in expression: dx at parse time. What happens next depends on which side of the equation the term sits on:\n\non the explicit right-hand side, the first timestep aborts with UnrecognizedRHSExpression: dx(u) … Aborting rather than silently dropping the term;\non the implicit left-hand side, the term is silently dropped — the solver runs and returns exactly the answer it would have given had you never written the term.\n\nTake the parse-time warning seriously; write ∂x.","title":"Cartesian"},{"category":"section","location":"pages/domains/#Future-Support","page":"Domains","text":"Spherical and cylindrical coordinates are not implemented — SphericalCoordinates and CylindricalCoordinates do not exist. CartesianCoordinates is currently the only coordinate system.","title":"Future Support"},{"category":"section","location":"pages/domains/#Performance-Considerations","page":"Domains","text":"","title":"Performance Considerations"},{"category":"section","location":"pages/domains/#Mesh-Selection","page":"Domains","text":"Do not hand-pick a mesh; omit mesh= and let the distributor choose. The rule it follows is forced by the decomposition:\n\nDomain Valid mesh Auto choice on 4 ranks\n2D 1D, e.g. (4,) (4,)\n3D 2D, e.g. (2, 2) (2, 2)\n\nA mesh whose rank equals the domain dimension (a 2D domain with a (2, 2) mesh) decomposes every axis, leaves no axis local for the FFT, and fails hard with PencilFFT plan creation failed with 4 MPI processes.","title":"Mesh Selection"},{"category":"section","location":"pages/domains/#Resolution-Balance","page":"Domains","text":"More modes in directions with:\nSmaller scales\nSharper gradients\nMore active dynamics","title":"Resolution Balance"},{"category":"section","location":"pages/domains/#Memory-Usage","page":"Domains","text":"# Estimate memory per field\nbytes_per_element = sizeof(dist.dtype)          # 8 for Float64\ntotal_elements = prod(global_shape(domain, :g))\nmemory_per_field = total_elements * bytes_per_element\n\n# Per process\nmemory_per_process = memory_per_field / dist.size","title":"Memory Usage"},{"category":"section","location":"pages/domains/#See-Also","page":"Domains","text":"Coordinates: Coordinate systems\nBases: Spectral basis types\nFields: Creating fields on domains\nAPI: Domains: Complete reference","title":"See Also"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Tutorial:-3D-Turbulence-Simulation","page":"3D Turbulence","text":"This tutorial demonstrates setting up and running a 3D turbulent flow simulation using Tarang.jl.","title":"Tutorial: 3D Turbulence Simulation"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Physical-Problem","page":"3D Turbulence","text":"We simulate decaying turbulence in a triply-periodic box, governed by the incompressible Navier-Stokes equations:\n\nbeginaligned\nfracpartial mathbfupartial t + mathbfu cdot nabla mathbfu = -nabla p + nu nabla^2 mathbfu \nnabla cdot mathbfu = 0\nendaligned\n\nwhere mathbfu = (u v w) is the velocity, p the pressure (divided by density), and nu the kinematic viscosity.","title":"Physical Problem"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Formulation:-vorticity-and-vector-potential","page":"3D Turbulence","text":"Tarang's pure-Fourier time-stepping path integrates each field with an implicit linear operator that is diagonal in Fourier space (a Laplacian, a hyper/fractional Laplacian, a constant damping, or a derivative of the field itself). The pressure-velocity saddle-point system is not of that form: an implicit nabla p couples a different field into the momentum equation, and the solver rejects it (under MPI it raises an error; in serial the coupled system is singular and the implicit solve is silently dropped, leaving p = 0 and a velocity that is no longer divergence-free).\n\nIn a triply-periodic box the pressure can be removed analytically, which is what we do here. Taking the curl of the momentum equation gives the vorticity equation, and the velocity is recovered from a vector potential mathbfA in the Coulomb gauge:\n\nbeginaligned\nfracpartial boldsymbolomegapartial t - nu nabla^2 boldsymbolomega\n   = -(mathbfucdotnabla)boldsymbolomega + (boldsymbolomegacdotnabla)mathbfu \nnabla^2 mathbfA = -boldsymbolomega qquad mathbfu = nabla times mathbfA\nendaligned\n\nEvery implicit term is now a Laplacian (diagonal), every nonlinear term is explicit and differentiated along Fourier axes only, and the two constraint equations are exactly the Poisson / algebraic-substitution pattern the solver refreshes each step.\n\nA velocity built as mathbfu = nablatimesmathbfA is divergence-free whatever mathbfA is, so maxnablacdotmathbfu approx 8times10^-15 (measured on the 32^3 run below) confirms nothing on its own. The check that actually tests the Poisson solve is the reconstructed velocity itself: after one step max|u1| = 0.999940, matching the analytic viscous decay of the Taylor-Green amplitude, e^-3nuDelta t = 0999940, to seven digits.","title":"Formulation: vorticity and vector potential"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Domain-Setup","page":"3D Turbulence","text":"","title":"Domain Setup"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Coordinates-and-Distribution","page":"3D Turbulence","text":"using Tarang\nusing MPI\nusing Printf\n\nMPI.Init()\n\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\n\nLeave mesh unset unless you have a reason: the distributor picks a valid process mesh for you ((1, 2) on 2 ranks, (2, 2) on 4 ranks).\n\nwarning: An N-dimensional domain takes an (N−1)-dimensional process mesh\nPencilFFTs must keep at least one axis process-local to transform it. A 3-element mesh over a 3D domain decomposes all three axes and the run dies at plan creation with PencilFFT plan creation failed with 8 MPI processes. For a 3D domain the mesh is a 2D pencil grid: mesh=(2, 4) on 8 ranks, mesh=(8, 8) on 64, mesh=(16, 32) on 512.","title":"Coordinates and Distribution"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Spectral-Bases","page":"3D Turbulence","text":"All directions are periodic, so we use Fourier bases:\n\nL  = 2π       # domain size\nN  = 32       # N^3 grid points\nRe = 100.0\nnu = 1.0 / Re\n\nx_basis = RealFourier(coords[\"x\"]; size=N, bounds=(0.0, L), dealias=3/2)\ny_basis = RealFourier(coords[\"y\"]; size=N, bounds=(0.0, L), dealias=3/2)\nz_basis = RealFourier(coords[\"z\"]; size=N, bounds=(0.0, L), dealias=3/2)\n\ndomain = Domain(dist, (x_basis, y_basis, z_basis))\n\nnote: Dealiasing\ndealias is a padding factor, not a cutoff fraction. dealias=3/2 pads the transform by 3/2 and truncates the nonlinear product back to k le N3 — the 3/2 rule (equivalently, the 2/3 rule). Any value ≤ 1 disables dealiasing entirely, so dealias=2/3 leaves the product fully aliased. 3/2 is also the default for RealFourier and ComplexFourier.\n\ninfo: No Tau Fields Needed for Boundaries\nSince all directions use periodic Fourier bases, no boundary conditions are required. The tau variables below are gauge constants for the Poisson equations (they fix the k=0 mode of the vector potential), not boundary-condition unknowns. Tau fields with the lift() operator are needed for non-periodic directions — see the Boundary Conditions Tutorial.","title":"Spectral Bases"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Fields-and-Problem-Definition","page":"3D Turbulence","text":"The unknowns are the three vorticity components (time-stepped), the three vector-potential components and the three velocity components (both solved from constraints each step), plus one gauge constant per Poisson equation:\n\nw1 = ScalarField(domain, \"w1\"); w2 = ScalarField(domain, \"w2\"); w3 = ScalarField(domain, \"w3\")\nA1 = ScalarField(domain, \"A1\"); A2 = ScalarField(domain, \"A2\"); A3 = ScalarField(domain, \"A3\")\nu1 = ScalarField(domain, \"u1\"); u2 = ScalarField(domain, \"u2\"); u3 = ScalarField(domain, \"u3\")\n\ntau_A1 = ScalarField(dist, \"tau_A1\", (), Float64)\ntau_A2 = ScalarField(dist, \"tau_A2\", (), Float64)\ntau_A3 = ScalarField(dist, \"tau_A3\", (), Float64)\n\nproblem = IVP([w1, w2, w3, A1, A2, A3, u1, u2, u3, tau_A1, tau_A2, tau_A3])\nadd_parameters!(problem, nu=nu)\n\n# Vorticity transport: advection + vortex stretching on the explicit side,\n# viscous diffusion on the implicit side.\nfor (w, u) in ((\"w1\", \"u1\"), (\"w2\", \"u2\"), (\"w3\", \"u3\"))\n    add_equation!(problem,\n        \"∂t($w) - nu*Δ($w) = -(u1*∂x($w) + u2*∂y($w) + u3*∂z($w))\" *\n        \" + (w1*∂x($u) + w2*∂y($u) + w3*∂z($u))\")\nend\n\n# Vector potential: Δ(A) = -ω, one Poisson equation per component.\nadd_equation!(problem, \"Δ(A1) + tau_A1 + w1 = 0\")\nadd_equation!(problem, \"Δ(A2) + tau_A2 + w2 = 0\")\nadd_equation!(problem, \"Δ(A3) + tau_A3 + w3 = 0\")\n\n# Velocity: u = ∇ × A.\nadd_equation!(problem, \"u1 - (∂y(A3) - ∂z(A2)) = 0\")\nadd_equation!(problem, \"u2 - (∂z(A1) - ∂x(A3)) = 0\")\nadd_equation!(problem, \"u3 - (∂x(A2) - ∂y(A1)) = 0\")\n\n# Gauge: fix the mean of each vector-potential component.\nadd_bc!(problem, \"integ(A1) = 0\")\nadd_bc!(problem, \"integ(A2) = 0\")\nadd_bc!(problem, \"integ(A3) = 0\")\n\nnote: Equation Syntax\n∂t(w1) — time derivative\nΔ(w1) — Laplacian\n∂x(w1), ∂y(w1), ∂z(w1) — partial derivatives (dx(...) is not valid syntax)\ninteg(A1) — domain integral, used here as the gauge condition\nThe problem is validated with one equation per variable (counting add_bc! conditions), which is why each of the three tau constants carries its own gauge condition.","title":"Fields and Problem Definition"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Solver-Setup","page":"3D Turbulence","text":"solver = InitialValueSolver(problem, RK443(); dt=2e-3)\n\n# The RHS compiles to a type-specialized plan (no interpreted fallback):\n@assert solver.rhs_plan.is_compiled\n\nRK443 is an IMEX Runge-Kutta scheme; RK222, SBDF2 and SBDF3 are also exported and work with this problem. Under MPI the pure-Fourier diagonal-IMEX path takes over automatically — the viscous term stays implicit, the advection and vortex-stretching terms explicit.\n\nnote: Three `Laplacian ... approximated as IDENTITY-on-operand` warnings are expected\nBuilding the solver prints, once per Poisson equation:┌ Warning: Implicit global matrix: operator Laplacian{...} has no spectral matrix builder and is\n│ approximated as IDENTITY-on-operand; the implicit solve will be wrong for this term.They come from the global matrix assembly, which this problem does not use — the actual stepping goes through the per-subproblem solve path, and the result is correct (the Δ(A) = -ω solve reproduces the exact Taylor-Green velocity, as above). The warnings are noise here; they are not a sign that the run is wrong.\n\nnote: Adaptive timestep\nThe CFL controller takes a VectorField velocity (add_velocity!(cfl, u)). This formulation carries the velocity as three constraint-solved ScalarFields, so it uses a fixed dt chosen from the initial velocity: dt ≲ safety · Δx / max|u| (here Δx = 2π/32 ≈ 0.2 and max|u| = 1).","title":"Solver Setup"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Initial-Conditions","page":"3D Turbulence","text":"","title":"Initial Conditions"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Taylor-Green-Vortex","page":"3D Turbulence","text":"The classic test case. The velocity\n\nmathbfu = (sin x cos y cos z -cos x sin y cos z 0)\n\nhas the vorticity boldsymbolomega = nablatimesmathbfu we initialize below (the solver reconstructs mathbfA and mathbfu from it on the first step):\n\nx, y, z = local_grids(dist, x_basis, y_basis, z_basis)\nZ = reshape(z, 1, 1, :)          # broadcast the third axis\n\nfor w in (w1, w2, w3)\n    ensure_layout!(w, :g)\nend\nget_grid_data(w1) .= -cos.(x) .* sin.(y') .* sin.(Z)\nget_grid_data(w2) .= -sin.(x) .* cos.(y') .* sin.(Z)\nget_grid_data(w3) .=  2 .* sin.(x) .* sin.(y') .* cos.(Z)\nfor w in (w1, w2, w3)\n    ensure_layout!(w, :c)\nend\n\nlocal_grids returns each rank's local slice of the coordinate axes, so the same code is correct in serial and under MPI. (set!(field, ::Function) builds the global meshgrid and therefore throws a DimensionMismatch when the field is distributed.)","title":"Taylor-Green Vortex"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Random-Initial-Conditions","page":"3D Turbulence","text":"For developed turbulence, seed the vorticity with noise instead — fill_random! is distributed-safe and reproducible — and band-limit it:\n\nfor w in (w1, w2, w3)\n    fill_random!(w, \"g\"; seed=42, distribution=\"normal\", scale=1e-1)\n    low_pass_filter!(w; scales=(0.25, 0.25, 0.25))     # keep |k| ≤ N/8\nend\n\nThe low-pass filter matters. Grid noise puts energy right up to the Nyquist mode, where the spectral derivative is not invertible, and the reconstructed velocity comes out only approximately divergence-free (max|∇·u| ≈ 5e-2 after 20 steps at 16^3). With the noise band-limited, the same run gives max|∇·u| ≈ 3e-13.","title":"Random Initial Conditions"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Analysis-and-Output","page":"3D Turbulence","text":"","title":"Analysis and Output"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Diagnostics","page":"3D Turbulence","text":"Kinetic energy, enstrophy and dissipation are box averages. global_mean reduces the local slab and performs the MPI reduction itself — never wrap it in another MPI.Allreduce:\n\nfunction kinetic_energy()\n    for f in (u1, u2, u3)\n        ensure_layout!(f, :g)\n    end\n    0.5 * global_mean(dist, get_grid_data(u1).^2 .+ get_grid_data(u2).^2 .+ get_grid_data(u3).^2)\nend\n\nfunction enstrophy()\n    for f in (w1, w2, w3)\n        ensure_layout!(f, :g)\n    end\n    0.5 * global_mean(dist, get_grid_data(w1).^2 .+ get_grid_data(w2).^2 .+ get_grid_data(w3).^2)\nend\n\ndissipation() = 2 * nu * enstrophy()     # ε = 2νΩ\n\nFor the Taylor-Green initial condition these give Ω = 0.375 and ε = 7.5e-3 at Re = 100 as soon as the vorticity is set, and KE = 0.125 once the solver has taken a step.\n\nnote: `kinetic_energy()` reads 0.0 before the first step\nOnly the vorticity is initialized; u1, u2, u3 are constraint variables that the solver fills in during the first step. Call kinetic_energy() straight after the initial condition and you get exactly 0.0, not 0.125. After one step it is 0.124985.\n\nwarning: Reading the velocity back under MPI\nThe velocity is a substitution constraint (u = ∇ × A). Serial CPU and single-GPU output is observational: a handler stages the constraint state produced by the completed timestep without solving it again, so writing a snapshot no longer changes u1, u2, u3.Under MPI, on every step, handler or no handler, the reconstructed velocity remains corrupted (measured identically at np = 2, 4 and 8: max|u1| = 0.6658 against a true 0.9987).What is not affected is the physics. The time-stepped vorticity is bit-identical at np = 1, 2 and 4 (max|w1| = 0.998933308710438 at all three), and the enstrophy after 20 steps agrees to ~15 digits (0.374138356478886; the MPI reduction can differ in the last ulp because it sums in a different order) whether a handler writes every iteration or not — the solver's internal RHS uses a correct velocity. So enstrophy() and dissipation() are trustworthy everywhere. Take kinetic_energy() and the velocity spectrum below from a serial run; under MPI, diagnose with the vorticity or reconstruct the velocity from saved vorticity snapshots in post-processing.","title":"Diagnostics"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Energy-Spectrum","page":"3D Turbulence","text":"power_spectrum requires every axis to be Fourier (true here) and returns a NamedTuple (k, power, bin_counts, bin_edges) with shell-averaged power at the bin centres k:\n\nfunction velocity_spectrum()\n    s1 = power_spectrum(u1)\n    s2 = power_spectrum(u2)\n    s3 = power_spectrum(u3)\n    return (k = s1.k, E = 0.5 .* (s1.power .+ s2.power .+ s3.power))\nend\n\nenstrophy_spectrum and energy_spectrum are also available, but they take a VectorField.","title":"Energy Spectrum"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Time-Integration","page":"3D Turbulence","text":"run! drives the loop: it steps the solver, writes any handler created with the solver, fires callbacks at their intervals, and closes the handlers at the end. An Int callback interval fires every N iterations; a Float64 interval fires every Δt of simulation time.\n\nfunction report(s)\n    @root_only @printf(\"t = %.3f  KE = %.6e  Ω = %.6e  ε = %.6e\\n\",\n                       s.sim_time, kinetic_energy(), enstrophy(), dissipation())\nend\n\n@root_only @printf(\"3D Taylor-Green: Re = %.0f, N = %d^3\\n\", Re, N)\n\nrun!(solver; stop_iteration=100, callbacks=[20 => report], progress=false)\n\nMPI.Finalize()\n\nOutput of the run above (serial, 32^3, RK443, dt = 2e-3):\n\n3D Taylor-Green: Re = 100, N = 32^3\nt = 0.040  KE = 1.247024e-01  Ω = 3.741384e-01  ε = 7.482767e-03\nt = 0.080  KE = 1.244096e-01  Ω = 3.733526e-01  ε = 7.467051e-03\nt = 0.120  KE = 1.241216e-01  Ω = 3.726414e-01  ε = 7.452828e-03\nt = 0.160  KE = 1.238381e-01  Ω = 3.720037e-01  ε = 7.440074e-03\nt = 0.200  KE = 1.235593e-01  Ω = 3.714385e-01  ε = 7.428770e-03\n\nThe energy decays at the viscous rate while the vortex tubes are still smooth; run longer (and at higher N) to see the enstrophy rise as the Taylor-Green vortex breaks down.","title":"Time Integration"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Saving-Snapshots","page":"3D Turbulence","text":"add_file_handler(path, solver; ...) registers the handler with the solver, so run! writes it automatically — declare it before run!, in place of the bare run! above.\n\nwarning: Pass field objects to `add_task!`, not strings\nadd_task!(handler, w1; name=\"w1\") — a field object — is the only form that reliably works. Strings behave three different ways: a bare field name (\"w1\") is resolved and written correctly; a compound expression (\"w1*w1\", \"w1 + w1\") is silently written as a zero scalar — not even the right shape, you get a (n_writes,) array of 0.0 instead of a field; and a derivative expression (\"∂x(w1)\") throws a MethodError outright.\n\nsnapshots = add_file_handler(\"turb3d/turb3d\", solver; sim_dt=0.1, max_writes=10)\nadd_task!(snapshots, w1; name=\"w1\")\nadd_task!(snapshots, w2; name=\"w2\")\nadd_task!(snapshots, w3; name=\"w3\")\n\nrun!(solver; stop_iteration=100, callbacks=[20 => report], progress=false)\n\nThe output is NetCDF-4 with the data in groups (vars, time, grids), one file per rank under MPI:\n\nf = \"turb3d/turb3d_s1/turb3d_s1.nc\"\nTarang.group_variable_names(f, \"vars\")       # [\"w1\", \"w2\", \"w3\"]\nTarang.group_ncread(f, \"vars\", \"w3\")         # (write, x, y, z)\nTarang.group_ncread(f, \"time\", \"sim_time\")   # (write,)\n\nwarning: Snapshot vorticity under MPI\nIn serial CPU and single-GPU runs, add_task!(snapshots, u1) writes the constraint state without mutating it. Under MPI the velocity is already affected by the reconstruction issue described under Diagnostics, independent of output. For distributed runs, snapshot w1, w2, w3 and reconstruct velocity in post-processing.","title":"Saving Snapshots"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Performance-Considerations","page":"3D Turbulence","text":"","title":"Performance Considerations"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Resolution-Requirements","page":"3D Turbulence","text":"Reynolds Number Minimum Grid Recommended Grid\nRe ~ 100 32³ 64³\nRe ~ 1000 64³ 128³\nRe ~ 10000 128³ 256³\nRe ~ 100000 256³ 512³+","title":"Resolution Requirements"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#MPI-Scaling","page":"3D Turbulence","text":"A 3D domain is decomposed with a 2D process mesh (see the warning above):\n\nProcesses Mesh Notes\n8 (2, 4) Basic\n64 (8, 8) Good scaling\n512 (16, 32) Large scale\n\nOmitting mesh= lets the distributor choose one of these shapes for you.","title":"MPI Scaling"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Memory-Usage","page":"3D Turbulence","text":"Estimate: ~100 bytes per grid point (double precision, velocity + vorticity + vector potential + work arrays)\n\nN_est = 256\nbytes_per_point = 100\ntotal_memory = N_est^3 * bytes_per_point / 1e9  # GB\nprintln(\"Estimated memory: $(total_memory) GB\")   # Estimated memory: 1.6777216 GB","title":"Memory Usage"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Running-the-Simulation","page":"3D Turbulence","text":"# Set environment\nexport OMP_NUM_THREADS=1\n\n# Run with MPI (8 ranks: a 2x4 pencil mesh)\nmpiexec -n 8 julia --project turbulence_3d.jl\n\nThe script above runs unchanged in serial and under MPI. The time-stepped vorticity — and therefore the enstrophy and dissipation — agrees to roundoff across rank counts: after 20 steps max|w1| is 0.998933308710438 at 1, 2 and 4 ranks and 0.998933308710437 at 8, and the enstrophy agrees to ~15 digits. Do not expect bit-identical results at every rank count — the distributed transforms and reductions reassociate the arithmetic, so the last ulp can move. (Reading the velocity back distributed hits the corruption noted under Diagnostics.)","title":"Running the Simulation"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#Visualization","page":"3D Turbulence","text":"There is no VTK/ParaView writer in Tarang. Read a slice back out of the NetCDF snapshots and hand it to the plotting package of your choice (Plots.jl, Makie.jl — neither is a dependency of Tarang):\n\nf  = \"turb3d/turb3d_s1/turb3d_s1.nc\"\nw3 = Tarang.group_ncread(f, \"vars\", \"w3\")    # (write, x, y, z)\nxg = Tarang.group_ncread(f, \"grids\", \"x\")\nyg = Tarang.group_ncread(f, \"grids\", \"y\")\n\nslice = w3[end, :, :, 1]                     # last write, z = z[1] plane\n# heatmap(xg, yg, slice')\n\nUnder MPI each rank writes its own file (turb3d_s1_p0.nc, turb3d_s1_p1.nc, …) containing its slab.","title":"Visualization"},{"category":"section","location":"tutorials/ivp_3d_turbulence/#References","page":"3D Turbulence","text":"Taylor, G. I., & Green, A. E. (1937). Mechanism of the production of small eddies from large ones.\nPope, S. B. (2000). Turbulent Flows. Cambridge University Press.\nCanuto, C., et al. (2007). Spectral Methods: Fundamentals in Single Domains.","title":"References"},{"category":"section","location":"examples/gpu_solvers_preconditioning/#GPU-Solvers:-Preconditioning","page":"GPU Solvers: Preconditioning","text":"CuIterativeCG and CuIterativeGMRES support several preconditioners to accelerate convergence on GPU.","title":"GPU Solvers: Preconditioning"},{"category":"section","location":"examples/gpu_solvers_preconditioning/#Available-Preconditioners","page":"GPU Solvers: Preconditioning","text":"Preconditioner Value Best For\nJacobi (diagonal) :jacobi Diagonally dominant matrices (default)\nILU(0) :ilu0 Ill-conditioned matrices, strong off-diagonal coupling\nIC(0) :ic0 Symmetric positive definite (SPD) matrices\nNone :none Well-conditioned problems\n\nThe preconditioner keyword is declared ::Symbol, so these four values are the only ones accepted. Passing a vector of diagonal values, a custom function, or nothing is rejected at the call boundary, before the constructor body runs:\n\nCuIterativeCG(A; preconditioner=d_inv)   # TypeError: in keyword argument preconditioner,\n                                         # expected Symbol, got a value of type Vector{Float64}","title":"Available Preconditioners"},{"category":"section","location":"examples/gpu_solvers_preconditioning/#Requirements","page":"GPU Solvers: Preconditioning","text":"These solvers live behind the CUDA package extension and are gated on Tarang.CUDA_AVAILABLE[]. When the flag is false, every constructor below raises\n\nERROR: CUDA not available. Use CPU solver instead.\n\nwarning: Known issue: the CUDA gate never opens\nCUDA_AVAILABLE[] is set only from _init_gpu_solvers!(), which runs from Tarang.__init__() — and that runs before Julia loads package extensions. Base.get_extension(Tarang, :TarangCUDAExt) therefore returns nothing there and the flag stays false, in either load order (using CUDA, Tarang or using Tarang, CUDA). The extension never sets the flag itself. Until it does, the GPU matrix solvers on this page error out at construction even on a machine with a working GPU, and the code below is a description of the API rather than something you can run today.\n\nUntil then, use the CPU solvers, which take the same (solver, rhs) call shape:\n\nusing SparseArrays, LinearAlgebra, Tarang\n\nA = sprand(10_000, 10_000, 1e-3)\nA = A + A' + 10I               # symmetric positive definite\nb = rand(size(A, 1))\n\nsolver = Tarang.MatSolvers.SparseLUSolver(A)\nx = Tarang.MatSolvers.solve(solver, b)\n\nnorm(A * x - b) / norm(b)      # ≈ 2.4e-15\n\nsolve is not exported by Tarang; call it as Tarang.solve (or Tarang.MatSolvers.solve). The same goes for SparseLUSolver.","title":"Requirements"},{"category":"section","location":"examples/gpu_solvers_preconditioning/#Jacobi-Preconditioner-(Default)","page":"GPU Solvers: Preconditioning","text":"Simple diagonal scaling using the inverse of the matrix diagonal:\n\nusing CUDA, SparseArrays, LinearAlgebra, Tarang\n\nA = sprand(10_000, 10_000, 1e-3)\nA = A + A' + 10I               # symmetric positive definite\nb = rand(size(A, 1))\n\n# Jacobi preconditioner (default)\nsolver = CuIterativeCG(A; preconditioner=:jacobi, tol=1e-10)\nx = Tarang.solve(solver, b)\n\nGood for diagonally dominant matrices where the diagonal captures most of the matrix structure.","title":"Jacobi Preconditioner (Default)"},{"category":"section","location":"examples/gpu_solvers_preconditioning/#ILU(0)-Preconditioner-(Recommended-for-Tough-Problems)","page":"GPU Solvers: Preconditioning","text":"Incomplete LU factorization with zero fill-in. Uses cuSPARSE for GPU-accelerated factorization and triangular solves.\n\nusing CUDA, SparseArrays, LinearAlgebra, Tarang\n\n# Create a challenging problem (2D Laplacian)\nfunction laplacian_2d(n)\n    I_n = sparse(I, n, n)\n    D = spdiagm(-1 => -ones(n-1), 0 => 2*ones(n), 1 => -ones(n-1))\n    return kron(I_n, D) + kron(D, I_n)\nend\n\nn = 100\nA = laplacian_2d(n)            # 10_000 x 10_000, SPD\nb = rand(size(A, 1))\n\n# ILU(0) preconditioner - much better for ill-conditioned matrices\nsolver = CuIterativeCG(A; preconditioner=:ilu0, tol=1e-10)\nx = Tarang.solve(solver, b)\n\nBenefits of ILU(0):\n\nCaptures matrix structure beyond just the diagonal, so it typically converges in far fewer iterations than Jacobi on ill-conditioned problems\nEfficient GPU implementation via cuSPARSE\nZero fill-in: the factors reuse the sparsity pattern of A\n\nWhen to use ILU(0):\n\nIll-conditioned matrices\nProblems with strong off-diagonal coupling\nWhen Jacobi converges slowly or not at all\nLarge sparse systems from PDE discretizations","title":"ILU(0) Preconditioner (Recommended for Tough Problems)"},{"category":"section","location":"examples/gpu_solvers_preconditioning/#IC(0)-Preconditioner-(Optimal-for-SPD-Matrices)","page":"GPU Solvers: Preconditioning","text":"Incomplete Cholesky factorization with zero fill-in. The symmetric variant of ILU(0), specifically optimized for symmetric positive definite (SPD) matrices.\n\nusing CUDA, SparseArrays, LinearAlgebra, Tarang\n\nfunction laplacian_2d(n)\n    I_n = sparse(I, n, n)\n    D = spdiagm(-1 => -ones(n-1), 0 => 2*ones(n), 1 => -ones(n-1))\n    return kron(I_n, D) + kron(D, I_n)\nend\n\nn = 100\nA = laplacian_2d(n)            # this is SPD\nb = rand(size(A, 1))\n\n# IC(0) preconditioner - optimal for SPD matrices\nsolver = CuIterativeCG(A; preconditioner=:ic0, tol=1e-10)\nx = Tarang.solve(solver, b)\n\nBenefits of IC(0) over ILU(0) for SPD matrices:\n\nLower memory: IC(0) factorizes tril(A) and stores only L, where ILU(0) stores L and U together. For the 2D Laplacian above that is 29,800 stored entries against 49,600.\nBetter numerical stability for symmetric systems\nPreserves symmetry properties\n\nWhen to use IC(0):\n\nMatrix is known to be symmetric positive definite\nUsing Conjugate Gradient solver (CG requires SPD matrix anyway)\nMemory is a concern\nPoisson/Laplacian problems, diffusion equations, mass matrices\n\nImportant: IC(0) requires the matrix to be SPD. Using it on a non-SPD matrix may produce incorrect results or fail. If unsure about SPD property, use ILU(0) instead.","title":"IC(0) Preconditioner (Optimal for SPD Matrices)"},{"category":"section","location":"examples/gpu_solvers_preconditioning/#GMRES-with-ILU(0)","page":"GPU Solvers: Preconditioning","text":"For non-symmetric systems, use CuIterativeGMRES:\n\nusing CUDA, SparseArrays, LinearAlgebra, Tarang\n\n# Non-symmetric convection-diffusion problem\nfunction convection_diffusion_1d(n; peclet=10.0)\n    h = 1.0 / (n + 1)\n    diffusion  = spdiagm(-1 => -ones(n-1), 0 => 2*ones(n), 1 => -ones(n-1)) ./ h^2\n    convection = spdiagm(-1 => -ones(n-1), 1 => ones(n-1)) .* (peclet / (2h))\n    return diffusion + convection\nend\n\nA = convection_diffusion_1d(10_000)   # non-symmetric\nb = rand(size(A, 1))\n\n# GMRES with ILU(0) preconditioning\nsolver = CuIterativeGMRES(A;\n    preconditioner=:ilu0,\n    tol=1e-10,\n    restart=50,    # Restart parameter\n    maxiter=500\n)\nx = Tarang.solve(solver, b)","title":"GMRES with ILU(0)"},{"category":"section","location":"examples/gpu_solvers_preconditioning/#Preconditioner-Summary","page":"GPU Solvers: Preconditioning","text":"# Allowed preconditioner values (Symbol only):\nsolver = CuIterativeCG(A; preconditioner=...)\n\n# :none      - No preconditioning\n# :jacobi    - Diagonal inverse from diag(A) [default]\n# :ilu0      - ILU(0) via cuSPARSE (recommended for tough non-SPD problems)\n# :ic0       - IC(0) via cuSPARSE (optimal for SPD matrices, lower memory than ILU(0))\n\nQuick selection guide:\n\nSPD matrix + CG solver: Use :ic0 (best performance and memory)\nNon-symmetric matrix + GMRES: Use :ilu0\nUnsure if SPD: Use :ilu0 (works for all matrices)\nSimple/well-conditioned: Use :jacobi (fastest setup)","title":"Preconditioner Summary"},{"category":"section","location":"examples/gpu_solvers_preconditioning/#See-Also","page":"GPU Solvers: Preconditioning","text":"GPU Solvers Overview: Solver types and selection\nOptimization: Performance tuning","title":"See Also"},{"category":"section","location":"pages/architecture/#Architecture-and-Codebase-Structure","page":"Architecture & Codebase","text":"Overview of Tarang.jl's internal architecture. This page describes how the pieces fit together — from coordinate/basis setup through problem assembly, subproblem decomposition, RHS evaluation, and time stepping. It's aimed at contributors and power users who want to understand (or extend) what happens under the hood.","title":"Architecture and Codebase Structure"},{"category":"section","location":"pages/architecture/#Package-Structure","page":"Architecture & Codebase","text":"Tarang.jl/\n├── src/\n│   ├── Tarang.jl                     # Root module and five-stage bootstrap\n│   ├── dependencies.jl               # External package imports\n│   ├── load_order.jl                 # Ordered implementation loaders\n│   ├── public_api.jl                 # Root-module public exports\n│   ├── runtime_init.jl               # MPI, FFTW, logging, and GPU initialization\n│   ├── core/                         # Numerical model and solver implementation\n│   │   ├── load_*.jl                 # Stable domain-level load manifests\n│   │   ├── stochastic_forcing.jl     # Backend-neutral forcing state and updates\n│   │   ├── basis/                    # Basis contracts, matrices, and wavenumbers\n│   │   ├── distributor/              # MPI layouts and transposes\n│   │   ├── field/                    # Field types, storage, and layouts\n│   │   ├── operators/                # Symbolic, evaluated, and matrix operators\n│   │   ├── problems/                 # Problem parsing and matrix assembly\n│   │   ├── solvers/                  # Solver construction and RHS execution\n│   │   ├── subsystems/               # Per-mode subproblem build and runtime\n│   │   ├── timesteppers/             # RK, multistep, IMEX, and ETD schemes\n│   │   ├── transforms/               # Serial, MPI, and backend transforms\n│   │   ├── transpose/                # Distributed transpose implementation\n│   │   └── nonlinear/                # Dealiasing and nonlinear evaluation\n│   ├── api/\n│   │   ├── public/                   # Public symbols grouped by capability\n│   │   └── namespaces.jl             # Fields/Problems/Solvers facade modules\n│   ├── tools/                        # Runtime, output, solver, and utility support\n│   │   ├── load_*.jl                 # Tool-layer load manifests\n│   │   ├── gpu_matsolvers.jl         # GPU matrix-solver types and registration\n│   │   ├── netcdf_output.jl          # Scheduled output and GPU host staging\n│   │   └── temporal_filters/         # Temporal-filter implementations\n│   └── extras/                       # Convenience domains and analysis helpers\n│       ├── load_extras.jl            # Extras load manifest\n│       └── flow_tools/               # CFL, spectra, and flow diagnostics\n├── ext/\n│   ├── TarangCUDAExt.jl              # CUDA weak-dependency extension\n│   └── cuda/                         # GPU kernels and transform backends\n│       ├── transforms.jl             # Field-level CUDA transform dispatch\n│       ├── mixed_transforms.jl       # Cached Fourier--Chebyshev plans/scratch\n│       └── cheb_deriv.jl             # Device-native DCT-I differentiation\n├── examples/                         # Runnable simulations\n├── test/\n│   └── file_lists.jl                 # Serial, optional, MPI, and GPU CI registry\n└── docs/\n    └── make.jl                       # Documenter build and website navigation\n\nThis map intentionally stops at stable subsystem boundaries. For an implementation detail, open the subsystem's entry file first. Files such as basis.jl, field.jl, problems.jl, solvers.jl, and transforms.jl act as manifests for focused files below the same-named directory.","title":"Package Structure"},{"category":"section","location":"pages/architecture/#Package-Bootstrap","page":"Architecture & Codebase","text":"src/Tarang.jl keeps package startup declarative. It includes five files in order:\n\nsrc/dependencies.jl imports external dependencies.\nsrc/load_order.jl loads the internal implementation in dependency order.\nsrc/public_api.jl defines the root-module public surface from files under src/api/public/.\nsrc/api/namespaces.jl creates domain facades such as Tarang.Fields, Tarang.Problems, and Tarang.Solvers.\nsrc/runtime_init.jl defines __init__ for MPI, FFTW threads, configuration, logging, and GPU solver discovery.\n\nThe implementation order in src/load_order.jl is grouped by responsibility rather than by individual file:\n\nLoader Responsibility\nsrc/core/load_contracts.jl Architecture and module contracts needed by every later layer\nsrc/tools/load_bootstrap.jl General utilities, exceptions, caches, dispatch, and parsing\nsrc/core/load_fields.jl Coordinates, bases, distributors, domains, fields, futures, and arithmetic\nsrc/core/load_problem_stack.jl Operators, transforms, BCs, problems, subproblems, and system assembly\nsrc/tools/load_matsolvers.jl CPU and GPU matrix-solver backends\nsrc/core/load_solver_stack.jl Solvers, forcing, timesteppers, and distributed field runtime\nsrc/tools/load_output.jl Output handlers required by evaluators\nsrc/core/load_evaluation.jl Evaluator and nonlinear execution\nsrc/tools/load_runtime.jl Configuration, parallel utilities, logging, progress, I/O, and filters\nsrc/core/load_models.jl Physics/model layers built on the solver stack\nsrc/extras/load_extras.jl Optional flow tools, plotting, quick domains, and analysis tasks\nsrc/tools/load_pretty_printing.jl Display methods loaded after public types exist\n\nWhen load-order bugs occur, modify the owning loader rather than adding another direct include to src/Tarang.jl.","title":"Package Bootstrap"},{"category":"section","location":"pages/architecture/#Runtime-Map","page":"Architecture & Codebase","text":"If you are reading the codebase for the first time, trace one IVP step in this order:\n\nsrc/core/solvers/solver_stepping.jl Entry point for step!(solver, dt). Refreshes dynamic BCs, owns TimestepperState, and hands off to the timestepper layer.\nsrc/core/timesteppers/dispatch.jl Thin dispatch table from timestepper type to implementation file.\nsrc/core/timesteppers/step_rk.jl or step_multistep.jl Top-level scheme logic. For IMEX RK, this is where the code chooses between subproblem stepping, explicit fallback, and legacy global-matrix solves.\nsrc/core/timesteppers/step_subproblem_rk.jl or step_subproblem_multistep.jl Performance-critical per-subproblem path for mixed Fourier / Chebyshev problems.\nsrc/core/subsystems/subproblem_runtime.jl Manifest for the per-mode gather/scatter and BC/RHS plumbing. The concrete helpers live in subproblem_io.jl, subproblem_rhs.jl, and subproblem_modes.jl.\nsrc/core/solvers/lazy_rhs.jl Optional RHS acceleration layer used by evaluate_rhs.\nsrc/core/boundary_conditions.jl BC manager, dynamic BC evaluation, and projection of refreshed BC values back into equation data.","title":"Runtime Map"},{"category":"section","location":"pages/architecture/#Contributor-File-Map","page":"Architecture & Codebase","text":"Start from the layer that owns the behavior, then follow its manifest into a focused directory:\n\nsrc/api/public/: root public exports grouped by user capability; implementations remain in core/, tools/, or extras/\nsrc/core/basis/: basis types, wavenumbers, NCC product matrices, and derivative/conversion operators\nsrc/core/distributor/: MPI layout setup, collectives, transpose helpers, and communication-buffer management\nsrc/core/field/field_data/: raw field storage, copying/allocation, local/global sizing, and scaling\nsrc/core/field/field_layout/: layout transitions, arithmetic, filtering, and vectorized field helpers\nsrc/core/operators/derivatives/, operations/, matrices/, tensor/: evaluation operators separated from sparse matrix assembly\nsrc/core/problems/problem_matrices/: solver-facing matrix construction, expression analysis, spectral block builders, and legacy forcing-vector helpers\nsrc/core/subsystems/: subsystem grouping, per-mode subproblem construction, runtime I/O/BC helpers, and NCC assembly\nsrc/core/solvers/: solver state, stepping entry points, interpreted/compiled RHS paths, and lazy RHS evaluation\nsrc/core/timesteppers/: timestepper types, dispatch, state management, and scheme implementations\nsrc/core/transforms/ and src/core/transpose/: spectral transforms and distributed data movement, respectively\nsrc/core/nonlinear/: padding, transform setup, dealiasing, pencil compatibility helpers, and nonlinear evaluation\nsrc/tools/: support code used across core layers; read the appropriate load_*.jl file to find its load boundary\nsrc/extras/flow_tools/: CFL control, diagnostics, spectra, streamfunction/SQG helpers, QG tools, and boundary-advection helpers\nsrc/tools/temporal_filters/: temporal-filter core types, IMEX/ETD updates, wave-mean helpers, and GQL utilities\next/cuda/: CUDA-only implementations activated through ext/TarangCUDAExt.jl; core code must remain loadable without CUDA\n\nTests mirror these ownership boundaries where practical. Use test/file_lists.jl to see the serial, MPI, optional, and GPU inventories used by CI.","title":"Contributor File Map"},{"category":"section","location":"pages/architecture/#GPU-IVP,-Forcing,-and-Output-Ownership","page":"Architecture & Codebase","text":"GPU support is an extension of the same solver stack, not a second solver tree. The core package owns backend-neutral state and dispatch hooks, while ext/TarangCUDAExt.jl installs CUDA implementations when CUDA.jl is loaded. This split keeps using Tarang valid on systems without CUDA.\n\nConcern Core owner CUDA owner and allocation boundary Primary tests\nStochastic forcing src/core/stochastic_forcing.jl constructs, normalizes, caches, and applies StochasticForcing and SeparableStochasticForcing Forcing arrays follow the field architecture. Pure Fourier forcing uses the full coefficient shape; Fourier–Chebyshev forcing draws only Fourier modes and combines them with a fixed Chebyshev profile. test/test_stochastic_forcing.jl, test/test_separable_stochastic_forcing.jl\nPure Fourier transforms src/core/transforms/transform_gpu.jl defines the backend hook and field-layout contract ext/cuda/transforms.jl dispatches 2D/3D Fourier fields to multidimensional cuFFT and reuses transform plans. test/test_gpu_transform_correctness.jl\nMixed transforms and derivatives Core transform and operator code selects the required basis operation ext/cuda/mixed_transforms.jl owns cached Fourier–Chebyshev plans and persistent scratch; ext/cuda/cheb_deriv.jl performs Chebyshev DCT-I differentiation without a CPU round trip. test/test_gpu_transform_correctness.jl, test/test_evaluator.jl\nCoupled IVP solves The subproblem timesteppers gather, solve, and scatter each Fourier-mode block src/tools/gpu_matsolvers.jl provides CuSparseLU and reusable device RHS/solution buffers. The default matsolver=:auto selects it for a single-GPU mixed IVP. test/test_gpu_field_rk_allocations.jl, test/test_subproblem_rk.jl\nNetCDF output src/tools/netcdf_output.jl owns scheduling, diagnostic refresh, metadata, and writes A per-write NetCDFStagingCache performs bulk GPU-to-host staging and reuses a staged scalar field within that write. Output after a completed step rebinds current state without re-solving constraints, so writing is observational. test/test_compatibility.jl, test/test_gpu_transform_correctness.jl\n\nThe supported single-GPU IVP paths are:\n\nFourier–Fourier in 2D and Fourier–Fourier–Fourier in 3D: field-native RK, multidimensional cuFFT, and StochasticForcing remain device-resident.\nFourier–Chebyshev in 2D and Fourier–Fourier–Chebyshev in 3D: per-mode CuSparseLU, cached mixed-transform scratch, and SeparableStochasticForcing keep the timestep path device-resident.\n\nAfter plans and workspaces are warm, these timestep paths reuse their device buffers rather than allocating on each step!. NetCDF is necessarily a host I/O boundary: it stages each requested GPU field to CPU memory, then writes the staged arrays. Distributed multi-GPU transforms and output have separate MPI layout constraints; do not infer their support from the single-GPU paths above.","title":"GPU IVP, Forcing, and Output Ownership"},{"category":"section","location":"pages/architecture/#How-the-Solver-Actually-Works","page":"Architecture & Codebase","text":"This section walks through the life of a step! call end-to-end, so you can see where each file fits.","title":"How the Solver Actually Works"},{"category":"section","location":"pages/architecture/#1.-Setup-phase","page":"Architecture & Codebase","text":"CartesianCoordinates ─► Distributor ─► Bases ─► Domain ─► Fields ─► Problem\n\nCartesianCoordinates(\"x\", \"z\") names the axes.\nDistributor(coords; dtype, device) owns the MPI communicator, architecture (CPU / GPU), and cached layout plans for different basis tuples.\nRealFourier(...), ChebyshevT(...) define the spectral bases.\nDomain(dist, (xbasis, zbasis)) ties a distributor to an ordered tuple of bases and allocates per-rank grids.\nScalarField, VectorField, TensorField hold data in either grid space (real values on Gauss-Lobatto / uniform grids) or coefficient space (Fourier + Chebyshev coefficients). The active layout is tracked by current_layout::Symbol and switched via ensure_layout!(field, :c) / (:g).\nIVP([vars...]), LBVP, NLBVP, EVP group state variables into a Problem and build a parser namespace from the variables' names.","title":"1. Setup phase"},{"category":"section","location":"pages/architecture/#2.-Equation-and-BC-assembly","page":"Architecture & Codebase","text":"Users add equations via string syntax:\n\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\nadd_bc!(problem, \"T(z=0) = 1\")\n\nadd_equation! pushes the string to problem.equations. add_bc!(problem, bc::String) pushes the string to problem.boundary_conditions and auto-parses it into a DirichletBC / NeumannBC object registered in problem.bc_manager.conditions, with is_time_dependent / is_space_dependent flags auto-detected from the value expression. This is what enables the per-step refresh path for T(z=0) = sin(t) or T(z=0) = sin(2·π·x/Lx).\n\nAt solver build time, _merge_boundary_conditions! pushes each BC string into problem.equations (so the equation parser sees it) and links the parsed DirichletBC back to its equation index via bc_manager.bc_equation_indices.","title":"2. Equation and BC assembly"},{"category":"section","location":"pages/architecture/#3.-Global-matrix-build","page":"Architecture & Codebase","text":"build_solver_matrices! → build_matrices(problem) → build_matrix_expressions!(problem).\n\nThis logic now lives under src/core/problems/problem_matrices/. The entry file problem_matrices.jl is only a manifest; the actual work is split across:\n\nproblem_matrices_build.jl for top-level assembly\nproblem_matrices_expr_analysis.jl for equation-variable analysis and operator splitting\nproblem_matrices_spectral.jl for spectral block construction\nproblem_matrices_support.jl and problem_matrices_legacy.jl for helpers and compatibility shims\n\nFor each equation (including merged BCs), the equation-string parser splits LHS and RHS:\n\nLHS is parsed into an operator tree with M (time-derivative) and L (spatial) parts\nRHS is parsed into an F expression (handled specially per equation)\n\nThe result is a Vector{Dict} — problem.equation_data — where each entry holds \"M\", \"L\", \"F\", \"lhs\", and \"equation_size\" keys.\n\nFor the global-matrix path (used by legacy steppers and BVPs), build_matrices walks equation_data and assembles sparse L_matrix, M_matrix, and a dense F_vector. The result is stored in problem.parameters[\"L_matrix\"], [\"M_matrix\"], [\"F_vector\"].","title":"3. Global matrix build"},{"category":"section","location":"pages/architecture/#4.-Subproblem-decomposition","page":"Architecture & Codebase","text":"This is the core of the modern solver path. _try_build_subproblems!(solver) calls build_subproblems in subsystems.jl. The implementation is now split by responsibility:\n\nsubproblem_build_orchestration.jl constructs subproblems and orchestrates per-group assembly\nsubproblem_expr_helpers.jl handles expression helpers, DOF sizing, and valid-mode checks used during build\nsubproblem_matrix_build.jl builds the small sparse L / M blocks\nsubproblem_runtime.jl delegates per-step I/O, BC projection, and mode checks to subproblem_io.jl, subproblem_rhs.jl, and subproblem_modes.jl\n\nAt runtime, the build path does the following:\n\nEnumerates Fourier-mode groups. For each separable (Fourier) axis, each mode is a \"subproblem group\". In a 2D problem with Nx = 256 RealFourier modes and one Chebyshev direction, there are Nx/2 + 1 = 129 subproblems.\nBuilds per-subproblem matrices. For each subproblem, build_matrices!(sp, ...) walks the problem's equation list and calls expression_matrices(expr, sp, vars) to construct small sparse L and M matrices — typically of shape (n_eqs_rows, n_var_cols) where the sizes are the per-Fourier-mode DOF counts (262 × 262 for the 2D RBC skeleton at Nz = 64).\nApplies left / right permutations (left_permutation, right_permutation) to group equations and variables by their domain dimension, matching the Dedalus subsystem ordering convention.\nApplies valid-mode filtering. Rows that are identically zero in both L and M — typically trivially-satisfied gauge constraints like integ(p) = 0 at non-DC Fourier modes — are paired with the \"least-used\" 1-DOF tau column (by smallest |L| + |M| norm) and both are dropped. The filtered system (L_min, M_min) is square and factors cleanly under a sparse LU; where a mode has no such zero rows it is simply the raw block.\nClassifies rows by equation size. sp.bulk_rows holds row indices where eq_size ≥ Nz (PDE rows and Nz-sized algebraic rows like continuity); sp.bc_rows holds row indices where eq_size < Nz (BC rows and gauge rows). The IMEX stepper uses sp.bc_rows to target algebraic-constraint rows for the per-stage apply_bc_override! path.\n\nThe output — a Tuple{Vararg{Subproblem}} stored in problem.parameters[\"subproblems\"] — is what the stepper consumes.","title":"4. Subproblem decomposition"},{"category":"section","location":"pages/architecture/#5.-Lazy-RHS-plan","page":"Architecture & Codebase","text":"build_lazy_rhs_plan!(solver) walks each equation's F expression and translates it into a type-parameterized LazyFuture tree (see src/core/solvers/lazy_rhs.jl). Each node — LazyStateField, LazyParamField, LazyConst, LazyAdd, LazySub, LazyMul, LazyNegate, LazyScale, LazyDiv, LazyPow, LazyUnaryFunc, LazyDiff, LazyMultiDiff — has a specialized evaluate_lazy! method. At first call, Julia's JIT specializes the entire evaluate_lazy! chain — eliminating dynamic dispatch and enabling broadcast fusion across arithmetic combinators.\n\nLazyMultiDiff is the fused Laplacian: lap(u) is a SUM of derivatives of one operand, so it evaluates the operand once, does a single forward transform, applies every axis's multiplier in one coefficient-space visit, and does a single backward transform — two spectral transforms regardless of dimension, instead of a full grid→coefficient→grid round-trip per axis.\n\nIf translation fails for any equation, the plan's is_compiled stays false and evaluate_rhs falls back to the interpreted expression path — for the whole solver, not just the offending term.\n\nFailure is not always a defect. _lazy_diff_axis_supported (lazy_rhs.jl:329) deliberately declines to translate a derivative in two situations where the lazy path would be wrong rather than merely absent, and the interpreted evaluator — correct, but roughly 100× slower — takes over.\n\nwarning: The interpreted fallback is slow, and under MPI it is not always correct\nThe fallback is roughly 100× slower per RHS evaluation. Worse, on a distributed all-Fourier field the interpreted derivative has been observed to evaluate to zero, leaving the field frozen with no error raised. Do not treat the fallback as a safe default: treat it as a bug to fix in your equation.Check whether your solver compiled:solver.rhs_plan.is_compiled   # false ⇒ you are on the interpreted pathdiagnose(solver) reports the same thing. To turn a silent fallback into a hard error:Tarang.require_lazy_rhs!()    # subsequent solver construction errors instead of warningA fallback is expected and correct in two cases, both of them declines by _lazy_diff_axis_supported:Situation Why the lazy path is declined\nDerivative along a distributed non-Fourier (Chebyshev) axis in an explicit RHS term The compiled path works in coefficient space, where that axis is the decomposed one, so a rank's slab does not conform to the global differentiation matrix. The interpreted path works in grid space, where the axis is local, and is accurate.\nDerivative on a Legendre / general (non-Chebyshev) Jacobi basis — distributed or not differentiation_matrix returns the recurrence for unnormalized Pₙ, while the transform stores orthonormal coefficients. The interpreted path applies that normalization; the lazy one does not, so it would be silently wrong. ChebyshevT / ChebyshevU carry no such normalization and are compiled.The @warn spells the first case out (\"This is EXPECTED and CORRECT for a distributed non-Fourier (Chebyshev/Jacobi) axis…\"). It does not yet do so for the second: a Legendre decline prints only the generic \"Usual cause: an unsupported or mistyped operator in this term\", so if you are on a Legendre / general-Jacobi basis, do not go hunting for a typo that isn't there.For the first case, move the term to the implicit (L) side to keep the compiled RHS; for the second, there is nothing to fix — the interpreted path is the correct path on that basis.\n\nThe plan is stored as solver.rhs_plan::LazyRHSPlan and used by evaluate_rhs(solver, state, time) during every stage of every step.","title":"5. Lazy RHS plan"},{"category":"section","location":"pages/architecture/#6.-Time-step","page":"Architecture & Codebase","text":"A single step!(solver, dt) call does:\n\nRefresh dynamic BCs (_refresh_step_boundary_conditions!, solver_stepping.jl). If has_time_dependent_bcs(bcm) is true, call update_time_dependent_bcs!(bcm, t+dt) and _apply_bc_values_to_equations!(solver, t+dt), which rewrites equation_data[eq_idx][\"F\"] with a fresh ConstantOperator or ArrayOperator for every time/space-dependent BC.\nDispatch by timestepper type (timesteppers/dispatch.jl). RK222() → step_rk_imex! → if problem.parameters[\"subproblems\"] exists, call step_subproblem_rk!(state, solver, sps). For CNAB2/SBDF2 the dispatch is similar via step_subproblem_multistep!.\nPer-stage subproblem solve (step_subproblem_rk.jl). For each RK stage:\nRefresh ALG_F per stage (re-run gather_alg_F! after re-calling update_time_dependent_bcs! at t + c[i]·dt). This recovers full stage-order accuracy for rapidly-varying BCs.\nPer subproblem: gather inputs, build stage RHS via dt·Σ(Aᴱ·F − Aⁱ·L·X), then apply_bc_override! to overwrite the sp.bc_rows entries with dt·a_ii · F_BC so the BC row enforces L_row·X = F_BC instead of the 1/γ-scaled value that the raw accumulated formula would give.\nSolve (M_min + dt·a_ii·L_min) · X_stage = rhs using the cached LHS solver (sparse LU for CPU, CuSparseLU for GPU).\nscatter_inputs writes X_stage back to the state fields.\nEvaluate F_stage and L·X_stage for use in the next stage.\nFinal update. For singular M (DAE system), the last stage value is kept. For non-singular M, the standard weighted-sum update solves M · X_{n+1} = M · X_n + dt·Σ(bᴱ·F − bⁱ·L·X) via the cached mass-matrix LU.\n\nThe same pattern applies to the multistep stepper (step_subproblem_multistep.jl), except the per-stage loop is replaced by per-subproblem history accumulation and a single (a[0]·M + b[0]·L) · X = rhs solve per step.","title":"6. Time step"},{"category":"section","location":"pages/architecture/#Core-Types","page":"Architecture & Codebase","text":"","title":"Core Types"},{"category":"section","location":"pages/architecture/#Coordinates","page":"Architecture & Codebase","text":"abstract type CoordinateSystem end\n\nstruct CartesianCoordinates <: CoordinateSystem\n    names::Vector{String}\n    dim::Int\n    coords::Vector{Coordinate}\n    curvilinear::Bool\n    right_handed::Union{Nothing, Bool}\n    default_nonconst_groups::Tuple{Vararg{Int}}\nend","title":"Coordinates"},{"category":"section","location":"pages/architecture/#Bases","page":"Architecture & Codebase","text":"abstract type Basis end\nabstract type IntervalBasis <: Basis end\nabstract type JacobiBasis  <: IntervalBasis end\nabstract type FourierBasis <: IntervalBasis end\n\nstruct RealFourier    <: FourierBasis end\nstruct ComplexFourier <: FourierBasis end\nstruct ChebyshevT     <: JacobiBasis  end\nstruct ChebyshevU     <: JacobiBasis  end\nstruct Legendre       <: JacobiBasis  end\nstruct Jacobi         <: JacobiBasis  end\n\nEvery basis carries a meta::BasisMeta — coordsys, element_label, dim, size, bounds, dealias, dtype — and, alongside it, its own memo dictionaries for product, conversion, and differentiation matrices (_conversion_matrix_cache and friends live on the basis struct, not on meta).","title":"Bases"},{"category":"section","location":"pages/architecture/#Fields","page":"Architecture & Codebase","text":"mutable struct ScalarField{T, S<:AbstractFieldStorage} <: Operand\n    dist::Distributor\n    name::String\n    bases::Tuple{Vararg{Basis}}\n    domain::Union{Nothing, Domain}\n    dtype::Type{T}\n    storage::S\n    layout::Union{Nothing, Layout}\n    current_layout::Symbol           # :g (grid) or :c (coefficient)\n    scales::Union{Nothing, Tuple}\n    # ...\nend\n\nVectorField and TensorField hold components::Vector{ScalarField} / Matrix{ScalarField}.","title":"Fields"},{"category":"section","location":"pages/architecture/#Subproblem","page":"Architecture & Codebase","text":"mutable struct Subproblem\n    solver::Any\n    problem::Problem\n    subsystems::Tuple{Vararg{Subsystem}}\n    group::Tuple\n    dist::Any\n    domain::Any\n    dtype::DataType\n    group_dict::Dict{String, Any}\n\n    variable_range::UnitRange{Int}\n    equation_range::UnitRange{Int}\n\n    matrices::Dict{String, Any}  # build-time matrices only\n\n    pre_left::Union{Nothing, SparseMatrixCSC}\n    pre_left_pinv::Union{Nothing, SparseMatrixCSC}\n    pre_right::Union{Nothing, SparseMatrixCSC}\n    pre_right_pinv::Union{Nothing, SparseMatrixCSC}\n\n    L_min::Union{Nothing, SparseMatrixCSC}\n    M_min::Union{Nothing, SparseMatrixCSC}\n    L_exp::Union{Nothing, SparseMatrixCSC}\n    M_exp::Union{Nothing, SparseMatrixCSC}\n    LHS::Union{Nothing, SparseMatrixCSC}\n\n    update_rank::Int\n    _input_buffer::Union{Nothing, Matrix{ComplexF64}}\n    _output_buffer::Union{Nothing, Matrix{ComplexF64}}\n\n    runtime::SubproblemRuntimeCache\n    LHS_solvers::Dict{Float64, Any}\n\n    bulk_rows::Vector{Int}\n    bc_rows::Vector{Int}\n    bulk_cols::Vector{Int}\n    bc_cols::Vector{Int}\nend\n\nSubproblemRuntimeCache holds the per-mode scratch vectors, backend-adapted matrices, BC gather buffers, and RK stage buffers that used to be mixed into generic dictionaries.","title":"Subproblem"},{"category":"section","location":"pages/architecture/#Timestepper","page":"Architecture & Codebase","text":"abstract type TimeStepper end\n\nstruct RK222 <: TimeStepper\n    stages::Int\n    A_explicit::Matrix{Float64}\n    b_explicit::Vector{Float64}\n    c_explicit::Vector{Float64}\n    A_implicit::Matrix{Float64}\n    b_implicit::Vector{Float64}\n    c_implicit::Vector{Float64}\nend\n\nAll RK variants carry their full Butcher tableau. Multistep variants (CNAB1/2, SBDF1..4) expose coefficient builders (_cnab1_coefs(dt), _sbdf2_coefs(dt), etc.) in step_subproblem_multistep.jl.","title":"Timestepper"},{"category":"section","location":"pages/architecture/#Solver","page":"Architecture & Codebase","text":"mutable struct InitialValueSolver <: Solver\n    base::SolverBaseData\n    problem::IVP\n    timestepper::TimeStepper\n    sim_time::Float64\n    iteration::Int\n    stop_sim_time::Float64\n    stop_wall_time::Float64\n    stop_iteration::Int\n    state::Vector{<:ScalarField}\n    dt::Float64\n    timestepper_state::Union{Nothing, AbstractTimestepperState}\n    evaluator::Union{Nothing, AbstractEvaluator}\n    wall_time_start::Float64\n    performance_stats::SolverPerformanceStats\n    rhs_plan::Any                # Lazy RHS plan or nothing\nend","title":"Solver"},{"category":"section","location":"pages/architecture/#Data-Flow-Diagrams","page":"Architecture & Codebase","text":"","title":"Data Flow Diagrams"},{"category":"section","location":"pages/architecture/#Simulation-lifecycle","page":"Architecture & Codebase","text":"Setup       : Coordinates → Distributor → Bases → Domain → Fields\nProblem     : Fields → Problem → add_equation!, add_bc!, add_parameters!\nBuild       : build_solver_matrices! → _try_build_subproblems! → build_lazy_rhs_plan!\nTime step   : step! → refresh BCs → dispatch → step_subproblem_rk! / multistep\nOutput      : evaluator → add_task! → NetCDF / callback","title":"Simulation lifecycle"},{"category":"section","location":"pages/architecture/#Layout-transitions","page":"Architecture & Codebase","text":"Grid space (physical)\n    ↓ forward_transform! (FFTW.rfft + DCT-I, or CUFFT on GPU)\nCoefficient space (spectral)\n    ↓ apply operators (matrix-free in subproblem path)\nCoefficient space\n    ↓ backward_transform!\nGrid space","title":"Layout transitions"},{"category":"section","location":"pages/architecture/#MPI-communication","page":"Architecture & Codebase","text":"All-to-all — layout transposes between pencils: inside the FFTs (PencilFFTPlan), and — on a distributed mixed Fourier–Chebyshev domain — the solve-layout transposes described below (PencilArrays.transpose!).\nAllreduce — global reductions for energy, CFL, diagnostics.\nAllgatherv — the Z→Y transpose step on a true 2D process mesh, and the EVP eigenvalue gather (_allgather_complex in solver_stepping.jl), where every rank's local Fourier modes must contribute to one global which/nev selection.\n\nUnder the subproblem architecture, each MPI rank owns a subset of Fourier modes, and the per-subproblem solve itself is rank-local: every rank iterates its own subproblems with no rank-to-rank traffic between them, and none of it is per-mode. What surrounds that loop depends on the bases:\n\nPure Fourier. The only traffic is the forward/backward FFTs in the RHS evaluation, plus whatever global reductions user callbacks ask for. The stepper loop itself communicates nothing.\nMixed Fourier–Chebyshev, distributed. The stepper additionally pays collective pencil transposes inside the step. In coefficient space a mixed field is decomposed along the Chebyshev axis (the pencil_fft_output layout), but the per-mode tau solve needs the opposite: Fourier modes split across ranks and full Chebyshev columns rank-local. That is the dist.pencil_solve layout, and to_solve_layout! / from_solve_layout! (subsystems/subproblem_io.jl) move the state and F fields between the two. step_subproblem_rk! brackets every RK stage with them; step_subproblem_multistep! does the same once per step.\nEach call is one collective over the whole comm — so they are hoisted outside the for sp in subproblems loop, and every rank must issue the same transpose count in the same order. The cost is therefore per stage, not per mode; but \"no communication inside the stepper loop\" is true only for the pure-Fourier case.\n\nBoth helpers short-circuit to a no-op when dist === nothing, dist.size <= 1, or dist.pencil_solve === nothing — i.e. serial and pure-Fourier runs pay nothing for them.","title":"MPI communication"},{"category":"section","location":"pages/architecture/#Extension-Points","page":"Architecture & Codebase","text":"","title":"Extension Points"},{"category":"section","location":"pages/architecture/#Custom-basis","page":"Architecture & Codebase","text":"Subtype Basis (typically JacobiBasis for bounded-interval bases). Implement:\n\nbasis.meta::BasisMeta with size, bounds, element_label, dealias\n_native_grid(basis, scale) — Gauss-Lobatto or uniform collocation nodes\nderivative_basis(basis) — the basis the derivative lives in\ndifferentiation_matrix(basis), conversion_matrix(a, b), _apply_forward / _apply_backward\n\nRegister with register_operator_alias! / register_operator_parseable! if it introduces new operators.","title":"Custom basis"},{"category":"section","location":"pages/architecture/#Custom-timestepper","page":"Architecture & Codebase","text":"Subtype TimeStepper. Implement:\n\nConstructor that populates Butcher tableau fields (for RK variants) or coefficient builders (for multistep)\nA new dispatch entry in src/core/timesteppers/dispatch.jl mapping the type to a stepping function\nA stepping function — either reuse step_subproblem_rk! with a new tableau, or write a dedicated step_<name>! that takes a TimestepperState and InitialValueSolver","title":"Custom timestepper"},{"category":"section","location":"pages/architecture/#Custom-operator","page":"Architecture & Codebase","text":"Subtype Operator. Implement:\n\nsubproblem_matrix(op::NewOp, sp; kwargs...) — returns a sparse matrix column/block for per-subproblem assembly\nevaluate_lazy! methods if the operator should participate in the lazy RHS plan\nexpression_matrices(op::NewOp, sp, vars; kwargs...) for arithmetic combinators (Add/Subtract/Multiply wrappers)","title":"Custom operator"},{"category":"section","location":"pages/architecture/#Design-Principles","page":"Architecture & Codebase","text":"Per-subproblem decomposition: the solver works one Fourier mode at a time, producing small sparse systems that an LU can crack efficiently. Gauge and BC rows are handled by valid-mode filtering and the apply_bc_override! DAE override.\nLazy / JIT-specialized RHS: the RHS plan is a type-parametric tree; the JIT specializes it to avoid dynamic dispatch.\nMPI transparency: users never touch the communicator — Distributor does.\nDevice transparency: CPU() vs GPU() is a constructor argument. The same code path runs on both.\nDedalus-style tau method: BCs are enforced via explicit tau fields and lift() operators added to equations, not by row replacement.","title":"Design Principles"},{"category":"section","location":"pages/architecture/#See-Also","page":"Architecture & Codebase","text":"Tau Method: full explanation of how BCs are enforced\nTime Steppers: available schemes and when to use them\nSolvers: the InitialValueSolver / BVP / EVP API\nContributing: development guidelines\nTesting: test architecture","title":"See Also"},{"category":"section","location":"api/problems/#Problems-API","page":"Problems","text":"Problems define the PDE systems to be solved, including equations, parameters, and boundary conditions. Tarang.jl supports Initial Value Problems (IVP), Boundary Value Problems (LBVP/NLBVP), and Eigenvalue Problems (EVP).","title":"Problems API"},{"category":"section","location":"api/problems/#Problem-Types","page":"Problems","text":"","title":"Problem Types"},{"category":"section","location":"api/problems/#Initial-Value-Problems-(IVP)","page":"Problems","text":"Time-evolution problems where PDEs are integrated forward in time from initial conditions.\n\nConstructor:\n\nIVP(variables::Vector{<:Operand})\n\nArguments:\n\nvariables: Vector of the unknowns — ScalarFields, VectorFields, and any tau variables required by the boundary conditions\n\nExample (2D advection-diffusion on a doubly-periodic domain):\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\nzb = RealFourier(coords[\"z\"]; size=16, bounds=(0.0, 2π))\ndom = Domain(dist, (xb, zb))\n\nT = ScalarField(dom, \"T\")\nu = VectorField(dom, \"u\")\n\nproblem = IVP([T, u])\nadd_parameters!(problem, kappa=0.05, nu=0.05)\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = 0\")\n\nset!(T, (x, z) -> sin(x) * cos(z))\nset!(u.components[1], (x, z) -> 0.5)\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\nrun!(solver; stop_iteration=20, progress=false)\n\nUse cases:\n\nFluid dynamics (Navier-Stokes)\nHeat/mass diffusion\nReaction-diffusion systems\nTurbulence simulations\n\n","title":"Initial Value Problems (IVP)"},{"category":"section","location":"api/problems/#Linear-Boundary-Value-Problems-(LBVP)","page":"Problems","text":"Steady-state linear problems with boundary conditions.\n\nConstructor:\n\nLBVP(variables::Vector{<:Operand})\n\nArguments:\n\nvariables: Vector of unknowns (fields plus the tau variables)\n\nBoundary conditions (tau method): a bounded (Chebyshev/Jacobi) direction needs explicit tau variables, one per boundary condition, lifted into the bulk equation via lift(tau, derivative_basis(basis, 2), -k). Boundary conditions are declared with add_bc! (not add_equation!). The solver builds one square tau subproblem per separable Fourier mode.\n\nExample (2D Poisson Δu = -2, u = 0 on both z walls; solution u = z(Lz - z), verified to machine precision):\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb = RealFourier(coords[\"x\"]; size=4,  bounds=(0.0, 2π))   # periodic (separable)\nzb = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))  # bounded  (coupled)\ndom = Domain(dist, (xb, zb))\n\nu    = ScalarField(dom, \"u\")\ntau1 = ScalarField(dist, \"tau1\", (xb,), Float64)           # one tau per BC\ntau2 = ScalarField(dist, \"tau2\", (xb,), Float64)\nlb2  = derivative_basis(zb, 2)\n\nproblem = LBVP([u, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"Δ(u) + l1 + l2 = -2\")\nadd_bc!(problem, \"u(z=0)   = 0\")\nadd_bc!(problem, \"u(z=1.0) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nensure_layout!(u, :g)            # scatter writes coefficients; switch to grid\n\nnote: 1D pure-Chebyshev BVP\nA pure single-axis Chebyshev BVP (no Fourier direction) works too — just drop the x axis and put the tau variables on (). The solver builds a single coupled tau subproblem over the Chebyshev spectrum.\n\nUse cases:\n\nSteady heat conduction\nPoisson/Laplace equations\nStokes flow\nElectrostatics\n\n","title":"Linear Boundary Value Problems (LBVP)"},{"category":"section","location":"api/problems/#Nonlinear-Boundary-Value-Problems-(NLBVP)","page":"Problems","text":"Steady-state nonlinear problems with boundary conditions.\n\nConstructor:\n\nNLBVP(variables::Vector{<:Operand})\n\nArguments:\n\nvariables: Vector of unknowns (fields plus the tau variables)\n\nSame tau-method boundary handling as the LBVP (tau variables + lift + add_bc!). The nonlinear terms go on the right-hand side; the solver linearizes them with a symbolic Frechet derivative and runs a per-Fourier-mode Newton iteration (the Jacobian dF = ∂(LHS − RHS)/∂u is rebuilt each iteration with the current state).\n\nExample (manufactured quadratic nonlinearity Δu = u² + g, with g = -2 - u_exact² so u_exact = z(Lz - z); verified to ~1e-12):\n\n# domain / fields / taus as in the LBVP example above (u, tau1, tau2, lb2)\ng = ScalarField(dom, \"g\"); ensure_layout!(g, :g)\nzg = create_meshgrid(dom; on_device=false)[\"z\"]\nget_grid_data(g) .= -2 .- (zg .* (1.0 .- zg)).^2\n\nproblem = NLBVP([u, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2), g=g)\nadd_equation!(problem, \"Δ(u) + l1 + l2 = u*u + g\")   # nonlinearity on the RHS\nadd_bc!(problem, \"u(z=0)   = 0\")\nadd_bc!(problem, \"u(z=1.0) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolver.tolerance = 1e-10\nensure_layout!(u, :g); get_grid_data(u) .= 0.0       # initial guess\nsolve!(solver)\nensure_layout!(u, :g)\n\nUse cases:\n\nSteady nonlinear flows\nBifurcation analysis\nMultiple steady states\n\nSolution method: per-Fourier-mode Newton iteration with a symbolic (Frechet) Jacobian.\n\n","title":"Nonlinear Boundary Value Problems (NLBVP)"},{"category":"section","location":"api/problems/#Eigenvalue-Problems-(EVP)","page":"Problems","text":"Linear eigenvalue problems for stability analysis and normal modes.\n\nConstructor:\n\nEVP(variables::Vector{<:Operand}; eigenvalue::Symbol)\n\nArguments:\n\nvariables: Vector of unknowns (plus the tau variables for BCs)\neigenvalue: Symbol naming the eigenvalue (declarative; e.g. :σ, :λ)\n\nEigenvalue convention: the generalized problem is L x = λ M x, where the mass matrix M is assembled from the time-derivative terms dt(·). The eigenvalue replaces the time derivative (dt(u) → λ u), so a normal-mode ansatz u ~ e^{λ t} is written by keeping the dt(u) term in the equation. Do not multiply the eigenvalue symbol into the equation (σ*u = … builds an empty M and returns no eigenvalues). Boundary conditions use the same tau method as the BVP (tau vars + lift + add_bc!).\n\nExample (1D diffusion eigenproblem λu = Δu, Dirichlet; eigenvalues are the Dirichlet-Laplacian values λ_n = -(nπ/Lz)², verified to ~1e-13):\n\ncoords = CartesianCoordinates(\"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nzb     = ChebyshevT(coords[\"z\"]; size=32, bounds=(0.0, 1.0))\ndom    = Domain(dist, (zb,))\n\nu    = ScalarField(dom, \"u\")\ntau1 = ScalarField(dist, \"tau1\", (), Float64)\ntau2 = ScalarField(dist, \"tau2\", (), Float64)\nlb2  = derivative_basis(zb, 2)\n\nproblem = EVP([u, tau1, tau2]; eigenvalue=:σ)\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"dt(u) - Δ(u) - l1 - l2 = 0\")   # dt(u) → λu marks M\nadd_bc!(problem, \"u(z=0)   = 0\")\nadd_bc!(problem, \"u(z=1.0) = 0\")\n\nsolver = EigenvalueSolver(problem; nev=5, which=:SM)   # 5 smallest-magnitude\neigenvalues, eigenvectors = solve!(solver)\n# |eigenvalues| ≈ (nπ)² = 9.8696, 39.4784, 88.8264, ...\n\nThe solver solves eigen(L, M) per Fourier mode on the square tau matrices and filters spurious eigenvalues from the singular mass matrix (the BC/tau rows). which accepts :LM :SM :LR :SR :LI :SI, or pass target=… to order by proximity to a shift.\n\nUse cases:\n\nLinear stability analysis\nGrowth rates and frequencies\nNormal mode decomposition\nCritical parameter values\n\n","title":"Eigenvalue Problems (EVP)"},{"category":"section","location":"api/problems/#Adding-Equations","page":"Problems","text":"","title":"Adding Equations"},{"category":"section","location":"api/problems/#add_equation!","page":"Problems","text":"Add a PDE to the problem.\n\nSyntax:\n\nadd_equation!(problem, equation_string::String)\n\nArguments:\n\nproblem: Problem object (IVP, LBVP, NLBVP, or EVP)\nequation_string: String equation using symbolic syntax\n\nThe parser reads the string against the problem namespace: the problem's own variables, everything registered with add_parameters!, the coordinate names taken from the bases, the time variable t, and the operator registry (Δ/lap, ∇/grad, div, curl, ∂x/∂z/…, ∂t/dt, lift, trace, integ, …). A name that is none of these is not an error: the parser warns Unknown variable: … and substitutes an opaque placeholder, so watch for that warning.\n\nSplit convention: linear terms that should be treated implicitly go on the left; the explicit (nonlinear, forcing) terms go on the right. Putting a linear operator on the RHS is legal but triggers a warning, because IMEX timesteppers only apply the implicit solve to the LHS.\n\nExamples:","title":"add_equation!"},{"category":"section","location":"api/problems/#Simple-equations","page":"Problems","text":"# Diffusion (IVP)\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = 0\")\n\n# Poisson (LBVP; `l1`, `l2` are the lifted tau terms)\nadd_equation!(problem, \"Δ(phi) + l1 + l2 = rho\")\n\nwarning: Only first-order time derivatives\n∂t(u) is the only time derivative the mass matrix understands. A second-order form such as \"∂t(∂t(u)) - c^2*Δ(u) = 0\" is not rejected — it is silently integrated as if it were first order (measured: it decays like the heat equation instead of oscillating). Reduce the equation to a first-order system instead:problem = IVP([u, v])\nadd_parameters!(problem, c2=1.0)\nadd_equation!(problem, \"∂t(u) - v = 0\")\nadd_equation!(problem, \"∂t(v) - c2*Δ(u) = 0\")(RUN: 1D, 32 modes, RK222, dt=1e-3 to t=1 — matches cos(x)cos(t) to 3e-8.)","title":"Simple equations"},{"category":"section","location":"api/problems/#Coupled-equations","page":"Problems","text":"coords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb  = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\nzb  = RealFourier(coords[\"z\"]; size=16, bounds=(0.0, 2π))\ndom = Domain(dist, (xb, zb))\n\nu = ScalarField(dom, \"u\")   # x-velocity\nw = ScalarField(dom, \"w\")   # z-velocity\nT = ScalarField(dom, \"T\")\n\nproblem = IVP([u, w, T])\nadd_parameters!(problem, nu=0.01, kappa=0.01, Ra=100.0, Pr=1.0)\n\n# Momentum: buoyancy is linear in T, so it belongs on the LHS with the diffusion\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = -u*∂x(u) - w*∂z(u)\")\nadd_equation!(problem, \"∂t(w) - nu*Δ(w) - Ra*Pr*T = -u*∂x(w) - w*∂z(w)\")\n# Energy\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u*∂x(T) - w*∂z(T)\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)\n\nVector notation works too, and is preferred when the unknown is a VectorField — one equation then contributes one row block per component:\n\nT = ScalarField(dom, \"T\")\nu = VectorField(dom, \"u\")\n\nproblem = IVP([T, u])\nadd_parameters!(problem, nu=0.01, kappa=0.01)\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = -u⋅∇(u)\")\n\nThe incompressible form (∇(p) in the momentum equation plus div(u) = 0) needs the tau machinery and a pressure gauge — see the Rayleigh-Bénard example at the end of this page.\n\n","title":"Coupled equations"},{"category":"section","location":"api/problems/#Parameters","page":"Problems","text":"","title":"Parameters"},{"category":"section","location":"api/problems/#Setting-parameters","page":"Problems","text":"Use add_parameters!. Every name registered this way becomes visible to the equation parser, and to constant boundary-condition values — but not to space- or time-dependent BC values (see Boundary Conditions below).\n\nproblem = IVP([u, w, T])\n\n# Dimensionless numbers\nadd_parameters!(problem, Re=1000.0, Pr=0.7, Ra=1e6)\n\n# Physical parameters\nadd_parameters!(problem, nu=1e-3, kappa=1e-3, g=9.81)\n\nValues may be numbers, fields, operator expressions, or plain Julia functions — anything the parser can splice into the expression tree. (They are stored in problem.namespace, which is a Dict{String, Any}; problem.namespace[\"nu\"] = 1e-3 is equivalent to add_parameters!(problem, nu=1e-3), but add_parameters! is the supported spelling.)","title":"Setting parameters"},{"category":"section","location":"api/problems/#Using-parameters-in-equations","page":"Problems","text":"add_equation!(problem, \"∂t(u) - nu*Δ(u) = -u*∂x(u)\")\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u*∂x(T)\")\n\n# Dimensionless formulation\nadd_equation!(problem, \"∂t(u) - (1/Re)*Δ(u) = -u*∂x(u)\")\nadd_equation!(problem, \"∂t(T) - (1/(Re*Pr))*Δ(T) - Ra*Pr*w = -u*∂x(T)\")","title":"Using parameters in equations"},{"category":"section","location":"api/problems/#Accessing-and-modifying-parameters","page":"Problems","text":"Parameters are read back from the namespace, and re-registering a name overwrites it. Values are baked into the matrices when the solver is built, so change them before constructing the solver.\n\nRa = problem.namespace[\"Ra\"]          # read\nadd_parameters!(problem, Ra=1e7)      # overwrite (before InitialValueSolver)\n\nnote: Note\nproblem.namespace is not a list of your parameters: it is pre-populated with the operator registry, the coordinate names, and the time variable (~60 entries before you add anything). Iterating it prints all of that. The problem.parameters field is a separate, unused dict — it stays empty.\n\n","title":"Accessing and modifying parameters"},{"category":"section","location":"api/problems/#Boundary-Conditions","page":"Problems","text":"Tarang has a dedicated add_bc! function for boundary conditions. It's the preferred API — use it instead of add_equation! for anything that's field(coord=value) = ....","title":"Boundary Conditions"},{"category":"section","location":"api/problems/#add_bc!","page":"Problems","text":"add_bc!(problem::Problem, bc::String)\n\nAdds a boundary condition to a problem. The BC string must match one of these forms:\n\nDirichlet: \"field(coord=position) = value\" (e.g. \"T(z=0) = 1\")\nNeumann: \"∂coord(field)(coord=position) = value\" (e.g. \"∂z(T)(z=0) = 0\")\nIntegral constraint: \"integ(field) = value\" (e.g. \"integ(p) = 0\" as a pressure gauge)\n\nadd_bc! does two things:\n\nPushes the raw string into problem.boundary_conditions (so that _merge_boundary_conditions! adds it as an equation at solver-build time for the parser to see).\nParses the string via parse_bc_string / parse_neumann_bc_string and registers a concrete DirichletBC or NeumannBC object in problem.bc_manager.conditions, with is_time_dependent / is_space_dependent flags auto-detected from the value expression. This second step is what enables time- and space-dependent BC refresh — without it, the BC is treated as a plain algebraic constraint with no runtime value updates.\n\nIntegral constraints like \"integ(p) = 0\" don't match the field(coord=pos) pattern; they flow through as raw strings to the equation parser and are handled by valid-mode filtering in subsystems.jl.","title":"add_bc!"},{"category":"section","location":"api/problems/#Dirichlet-(fixed-value)","page":"Problems","text":"# Constant value\nadd_bc!(problem, \"T(z=0) = 1\")\nadd_bc!(problem, \"T(z=1) = 0\")\n\n# No-slip velocity (vector BC — applies to all components)\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\n\nA constant BC value may be a literal, or a name registered with add_parameters!. A bare Julia global is not visible to the parser — it warns Unknown variable: Tbot and silently applies 0:\n\nadd_parameters!(problem, Tbot=3.0)\nadd_bc!(problem, \"T(z=0) = Tbot\")     # 3.0, exactly","title":"Dirichlet (fixed value)"},{"category":"section","location":"api/problems/#Time-dependent-BC","page":"Problems","text":"add_bc!(problem, \"T(z=0) = 1.0 + 0.1*cos(2*pi*t)\")\n\nThe BC value is re-evaluated at every RK stage time t + c[i]·dt, so multi-stage methods retain full formal order of accuracy. No extra configuration required — t is the default time variable and is recognized automatically.","title":"Time-dependent BC"},{"category":"section","location":"api/problems/#Space-dependent-BC","page":"Problems","text":"add_bc!(problem, \"T(z=0) = 1.0 + 0.1*sin(2*pi*x/4)\")\n\nThe BC expression is evaluated against the global x coordinate grid at solver-build time, producing a 1D array. At each stepper call, the array is projected onto each subproblem's Fourier mode via an unnormalized RFFT. Coordinate names (x, y, z, ...) are auto-registered from the problem's bases — no add_coordinate_field! call needed.","title":"Space-dependent BC"},{"category":"section","location":"api/problems/#Space-time-dependent-BC","page":"Problems","text":"add_bc!(problem, \"T(z=0) = 1.0 + 0.1*sin(2*pi*x/4)*cos(2*pi*t)\")\n\nThe spatial pattern is projected onto Fourier modes and re-evaluated at each stage time, combining both refresh paths.\n\nwarning: Non-constant BC values take literals only — not parameter names\nThe refresh evaluator for space- and time-dependent BC values has its own scope: t, pi, e, and the coordinate grids. Names you registered with add_parameters! are not in it. add_parameters!(problem, Lx=4.0) + \"T(z=0) = 1.0 + 0.1*sin(2*pi*x/Lx)\" warns \"BC expression evaluation failed … Unknown variable: Lx\" and the boundary value comes out wrong (measured error 1.1 on an O(1) BC). Bake the number in with a literal or with Julia string interpolation:Lx = 4.0\nadd_bc!(problem, \"T(z=0) = 1.0 + 0.1*sin(2*pi*x/$Lx)*cos(2*pi*t)\")   # exact(RUN: literal and $-interpolated forms enforce the BC to 1e-16; the parameter-name form is off by 1.1.)","title":"Space + time dependent BC"},{"category":"section","location":"api/problems/#Neumann-(fixed-derivative)","page":"Problems","text":"# Insulating boundary\nadd_bc!(problem, \"∂z(T)(z=0) = 0\")\n\n# Fixed flux\nadd_bc!(problem, \"∂z(T)(z=1) = -1\")\n\n# Time-dependent flux\nadd_bc!(problem, \"∂z(T)(z=1) = sin(2*pi*t)\")\n\nNeumann BCs detect space/time dependency in exactly the same way as Dirichlet. The ∂coord(field) prefix is what tells _register_string_bc! to route through parse_neumann_bc_string.","title":"Neumann (fixed derivative)"},{"category":"section","location":"api/problems/#Robin-(mixed)","page":"Problems","text":"Robin conditions α·f + β·∂f/∂n = value are written as a single BC string mixing the interpolated field and its interpolated derivative:\n\nadd_bc!(problem, \"u(z=1.0) + 2*∂z(u)(z=1.0) = -2\")\n\n(RUN: Δu = -2 on z ∈ [0,1] with u(z=0) = 0 and the Robin condition above recovers u = z(1-z) to 8e-17.)\n\nA Robin string does not match the Dirichlet/Neumann patterns, so it is not registered in bc_manager.conditions — it goes through the raw-string equation path. That is fine for a constant right-hand side, but a time- or space-dependent Robin value will not be refreshed during the run.","title":"Robin (mixed)"},{"category":"section","location":"api/problems/#Stress-free-/-free-slip","page":"Problems","text":"add_bc!(problem, \"w(z=0) = 0\")\nadd_bc!(problem, \"∂z(u)(z=0) = 0\")\nadd_bc!(problem, \"w(z=1) = 0\")\nadd_bc!(problem, \"∂z(u)(z=1) = 0\")","title":"Stress-free / free-slip"},{"category":"section","location":"api/problems/#No-slip","page":"Problems","text":"All velocity components vanish at the wall. On a VectorField a single BC covers every component:\n\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\n\nComponent-by-component (scalar velocity fields) it is one BC per component:\n\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"w(z=0) = 0\")\n\nThere are also named helpers for the common cases, which build the BC object directly: no_slip!(problem, \"u\", \"z\", 0.0), fixed_value!(problem, \"T\", \"z\", 0.0, 1.0), free_slip!(problem, \"u\", \"z\", 0.0), insulating!(problem, \"T\", \"z\", 1.0).","title":"No-slip"},{"category":"section","location":"api/problems/#Pressure-gauge","page":"Problems","text":"For incompressible flow, pressure is defined only up to a constant. Fix the gauge with an integral constraint:\n\ntau_p = ScalarField(dist, \"tau_p\", (), Float64)   # add tau_p to the IVP variables\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_bc!(problem, \"integ(p) = 0\")\n\nThe integ(p) = 0 constraint is trivially zero at non-DC Fourier modes and is filtered by valid-mode filtering in the subproblem builder — it's only active at the DC mode, where it fixes the pressure gauge.","title":"Pressure gauge"},{"category":"section","location":"api/problems/#Custom-boundary-conditions","page":"Problems","text":"Any linear combination of interpolated fields and interpolated derivatives works, including combinations that couple two fields at a wall — provided both fields are problem variables and every field term is on the left-hand side. Each interpolated field term becomes a column of the BC row, so it has to be an unknown:\n\n# T and S are both unknowns; S is pinned to 1, and T is tied to it at the wall\nproblem = LBVP([T, S, tau_T1, tau_T2, tau_S1, tau_S2])\nadd_parameters!(problem; lT1=lift(tau_T1, lb2, -1), lT2=lift(tau_T2, lb2, -2),\n                         lS1=lift(tau_S1, lb2, -1), lS2=lift(tau_S2, lb2, -2))\nadd_equation!(problem, \"Δ(T) + lT1 + lT2 = 0\")\nadd_equation!(problem, \"Δ(S) + lS1 + lS2 = 0\")\nadd_bc!(problem, \"S(z=0) = 1\")\nadd_bc!(problem, \"S(z=1) = 1\")               # ⇒ S ≡ 1\nadd_bc!(problem, \"T(z=0) - 2*S(z=0) = 0\")    # coupled BC: T(z=0) = 2·S(z=0)\nadd_bc!(problem, \"T(z=1) = 0\")\n\n(RUN: recovers S ≡ 1 exactly and T = 2(1-z) to 2e-16.)\n\nwarning: A coupled BC only sees the problem's variables\nIf S is a known field handed to the problem through add_parameters! rather than declared as a variable, the S(z=0) term has no column to land in: it is dropped without a warning and the condition silently degrades to T(z=0) = 0 (measured T ≡ 0 instead of 2(1-z)). For a known field, evaluate it yourself and write the resulting number on the right — add_bc!(problem, \"T(z=0) = 2\").\n\nwarning: Field terms on the RHS of a BC are dropped\nThe right-hand side of a BC must be a constant, a compound constant, or a coordinate/time expression — never a field. Writing the same coupling as \"T(z=0) = 2*S(z=0)\" still builds and solves, but the solver warns \"Boundary condition right-hand side … is not supported and is being enforced as ZERO\" and discards the term — measured T ≡ 0 instead of 2(1-z). Keep every field reference on the left of the =.","title":"Custom boundary conditions"},{"category":"section","location":"api/problems/#Legacy:-add_equation!-for-BCs","page":"Problems","text":"add_equation!(problem, \"T(z=0) = 1\") still works — the equation parser detects the field(coord=value) syntax and converts it internally. However, this path does not register the BC in bc_manager.conditions, so time- and space-dependent BC refresh is disabled. For constant BCs it's equivalent to add_bc!; for anything else, prefer add_bc!.\n\n","title":"Legacy: add_equation! for BCs"},{"category":"section","location":"api/problems/#Problem-Properties","page":"Problems","text":"","title":"Problem Properties"},{"category":"section","location":"api/problems/#Accessing-variables","page":"Problems","text":"# The unknowns, as declared\nvars = problem.variables\n\nfor v in problem.variables\n    println(v.name)\nend\n\n# Scalar names, flattening VectorField components (\"u_x\", \"u_z\", ...)\nget_variable_names(problem)\nget_variable_count(problem)","title":"Accessing variables"},{"category":"section","location":"api/problems/#Accessing-equations","page":"Problems","text":"# Get equations\nequations = problem.equations\n\n# Number of equations\nn_eqs = length(problem.equations)\n\n# Print equations\nfor (i, eq) in enumerate(problem.equations)\n    println(\"Equation $i: $eq\")\nend","title":"Accessing equations"},{"category":"section","location":"api/problems/#Accessing-boundary-conditions","page":"Problems","text":"problem.boundary_conditions is a Vector{String} — the raw strings you passed to add_bc!. The structured objects (with field / coordinate / position / value) live in problem.bc_manager.conditions:\n\n# Raw strings\nfor bc in problem.boundary_conditions\n    println(bc)\nend\n\n# Parsed BC objects (DirichletBC / NeumannBC)\nfor bc in problem.bc_manager.conditions\n    println(\"$(bc.field) at $(bc.coordinate)=$(bc.position): $(bc.value)\")\nend\n\nOnly BCs matching the Dirichlet/Neumann patterns appear in bc_manager.conditions; integral constraints and Robin combinations exist only as raw strings.\n\n","title":"Accessing boundary conditions"},{"category":"section","location":"api/problems/#Problem-Validation","page":"Problems","text":"","title":"Problem Validation"},{"category":"section","location":"api/problems/#validate_problem","page":"Problems","text":"A coarse pre-flight check on the problem's bookkeeping.\n\nSyntax:\n\nvalidate_problem(problem)   # returns true, or throws ArgumentError\n\nChecks:\n\nThere is at least one variable and at least one equation\nIVP/EVP: length(problem.equations) == length(problem.variables)\nLBVP/NLBVP: length(problem.equations) >= length(problem.variables), and at least one boundary condition exists\nAny BC objects registered in bc_manager are self-consistent\n\nExample:\n\nT = ScalarField(dom, \"T\")\nu = VectorField(dom, \"u\")\n\nproblem = IVP([T, u])                                  # 2 declared variables\nadd_parameters!(problem, kappa=0.05, nu=0.05)\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\") # 2 equations\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = 0\")\n\nvalidate_problem(problem)   # true\n\nwarning: It counts declared operands, not degrees of freedom\nA VectorField counts as one variable, and boundary conditions added with add_bc! are not counted as equations. A well-posed tau-method problem therefore trips the count check — e.g. LBVP([u, tau1, tau2]) with one bulk equation and two add_bc! calls throws \"Number of equations (1) is less than number of variables (3)\". The BCs are merged into problem.equations when the solver is built, so the very same call passes afterwards. Build the solver instead: BoundaryValueSolver / InitialValueSolver do the real structural checks.\n\n","title":"validate_problem"},{"category":"section","location":"api/problems/#Problem-Substitutions","page":"Problems","text":"Give a name to an operator expression and reuse it in equations. Substitutions are operator objects, not strings:\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb  = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\nzb  = RealFourier(coords[\"z\"]; size=16, bounds=(0.0, 2π))\ndom = Domain(dist, (xb, zb))\n\nu = ScalarField(dom, \"u\")\nw = ScalarField(dom, \"w\")\n\n# Vorticity ω = ∂x(w) - ∂z(u), built once as an operator\nomega = d(w, coords[\"x\"], 1) - d(u, coords[\"z\"], 1)\n\nproblem = IVP([u, w])\nadd_parameters!(problem, nu=0.01, omega=omega)\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = 0\")\nadd_equation!(problem, \"∂t(w) - nu*Δ(w) = omega\")\n\nThe same mechanism carries the tau-lift and first-order-reduction expressions of the tau method (grad_u = grad(u) + ez*lift(tau_u1, lift_basis, -1)), and a plain Julia function may be registered and called from an equation string (add_parameters!(problem, τ_lift=A -> lift(A, lift_basis, -1)), then \"… + τ_lift(tau_T2) = …\").\n\nwarning: A substitution given as a String evaluates to zero\nadd_parameters!(problem, omega=\"∂x(w) - ∂z(u)\") is accepted, but the string is never parsed: the lazy RHS refuses to compile the equation, the solver falls back to the interpreted evaluator, warns \"Unsupported expression type in evaluatesolverexpression: String\" on every stage, and the term contributes zero (measured from the same initial condition after 10 steps: max|w| = 0.0 for the string, 1.0e-2 for the operator object). Always pass the operator object.\n\n","title":"Problem Substitutions"},{"category":"section","location":"api/problems/#Advanced-Features","page":"Problems","text":"","title":"Advanced Features"},{"category":"section","location":"api/problems/#Tau-method-for-boundary-conditions","page":"Problems","text":"Tau variables are not created automatically. In a bounded (Chebyshev/Jacobi) direction you declare one tau variable per boundary condition, include it in the problem's variable list, and lift it into the bulk equation:\n\ntau1 = ScalarField(dist, \"tau1\", (xb,), Float64)   # per-Fourier-mode tau (serial)\ntau2 = ScalarField(dist, \"tau2\", (xb,), Float64)\nlb2  = derivative_basis(zb, 2)\n\nproblem = LBVP([u, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"Δ(u) + l1 + l2 = -2\")\n\nThe lifted taus add the extra degrees of freedom that make each per-mode subproblem square, so the boundary conditions can be enforced exactly. See Tau Method for the full treatment, and the LBVP example above for a complete run.","title":"Tau method for boundary conditions"},{"category":"section","location":"api/problems/#Constraints","page":"Problems","text":"There is no add_constraint!. Integral constraints are boundary conditions: use add_bc!(problem, \"integ(p) = 0\") (see Pressure gauge above).\n\n","title":"Constraints"},{"category":"section","location":"api/problems/#Complete-Examples","page":"Problems","text":"","title":"Complete Examples"},{"category":"section","location":"api/problems/#2D-Rayleigh-Bénard-convection","page":"Problems","text":"The full tau-method skeleton: first-order reduction, per-mode tau variables, add_bc! boundary conditions, and a pressure gauge. (RUN: 16×12, 20 steps of RK222; BCs satisfied to 2e-16.)\n\nusing Tarang\n\nLx, Lz = 4.0, 1.0\nNx, Nz = 16, 12\nRayleigh, Prandtl = 2e4, 1.0\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nzbasis = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))       # serial: Fourier first, Chebyshev last\n\np = ScalarField(domain, \"p\")\nT = ScalarField(domain, \"T\")\nu = VectorField(domain, \"u\")\n\n# tau variables: rank-reduced fields carrying the Fourier basis (one tau per x-mode)\ntau_p  = ScalarField(dist, \"tau_p\",  (), Float64)          # scalar gauge unknown\ntau_T1 = ScalarField(dist, \"tau_T1\", (xbasis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (xbasis,), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis,), Float64)\n\n# first-order reduction: grad_X = ∇X + ẑ·lift(τ)\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_T = grad(T) + ez * τ_lift(tau_T1)\n\nproblem = IVP([p, T, u, tau_p, tau_T1, tau_T2, tau_u1, tau_u2])\nadd_parameters!(problem, nu=Prandtl, buoy=Rayleigh*Prandtl, ez=ez,\n                grad_u=grad_u, grad_T=grad_T, τ_lift=τ_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) - buoy*T*ez + τ_lift(tau_u2) = -u⋅∇(u)\")\n\nadd_bc!(problem, \"T(z=0) = 1\")\nadd_bc!(problem, \"T(z=1) = 0\")\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")      # pressure gauge\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)\n\n# initial condition: conduction profile + damped noise\nx, z = local_grids(dist, xbasis, zbasis)\nfill_random!(T, \"g\"; seed=42, distribution=\"normal\", scale=1e-3)\nget_grid_data(T) .*= z' .* (1.0 .- z')\nget_grid_data(T) .+= 1.0 .- z'\nensure_layout!(T, :c)\n\nrun!(solver; stop_iteration=20, progress=false)\n\nwarning: This example is serial-only\nThe -u⋅∇(T) / -u⋅∇(u) advection expands to a Chebyshev derivative on the explicit side, which the distributed RHS cannot evaluate (each rank owns only part of the Chebyshev spectrum). Under MPI the first step errors out. See Running with MPI.\n\n","title":"2D Rayleigh-Bénard convection"},{"category":"section","location":"api/problems/#See-Also","page":"Problems","text":"Solvers: Solving problems with different algorithms\nOperators: Mathematical operators for equations\nFields: Field types used in problems\nTimesteppers: Time integration schemes","title":"See Also"},{"category":"section","location":"pages/optimization/#Performance-Optimization","page":"Performance Optimization","text":"Guide to optimizing Tarang.jl simulations.","title":"Performance Optimization"},{"category":"section","location":"pages/optimization/#Profiling","page":"Performance Optimization","text":"","title":"Profiling"},{"category":"section","location":"pages/optimization/#Julia-Profiler","page":"Performance Optimization","text":"using Profile\n\nstep!(solver)      # warm up: the first step pays compilation\n\nProfile.clear()\n@profile begin\n    for i in 1:50\n        step!(solver)\n    end\nend\n\nProfile.print(maxdepth=6)","title":"Julia Profiler"},{"category":"section","location":"pages/optimization/#Timing","page":"Performance Optimization","text":"step!(solver)                                    # warm up\nelapsed = @elapsed for i in 1:50\n    step!(solver)\nend\nprintln(\"time per step: $(elapsed/50 * 1000) ms\")\n\n@btime from BenchmarkTools works too, but BenchmarkTools is not a dependency of Tarang — add it to your own environment first.","title":"Timing"},{"category":"section","location":"pages/optimization/#Counting-Spectral-Transforms","page":"Performance Optimization","text":"Spectral transforms dominate the cost of a step, and a redundant one is invisible to an allocation profiler: mul!/ldiv! are in-place, so a wasted FFT allocates nothing. Tarang ships an opt-in counter for exactly this. The three functions are not exported, so they need the Tarang. prefix:\n\nTarang.enable_transform_counts!(true)   # off by default (one Ref load when off)\nTarang.reset_transform_counts!()\nstep!(solver)\nTarang.transform_counts()               # (forward = 18, backward = 27)\nTarang.enable_transform_counts!(false)\n\nThe numbers above are for one RK222 step of a 2D scalar + vector system. What matters is that the count is stable: if it grows after a change to your equations or to Tarang, you have added a transform.","title":"Counting Spectral Transforms"},{"category":"section","location":"pages/optimization/#Memory-Optimization","page":"Performance Optimization","text":"","title":"Memory Optimization"},{"category":"section","location":"pages/optimization/#Minimize-Allocations","page":"Performance Optimization","text":"Broadcast assignment (.= with a dotted right-hand side) is fused and allocates nothing. The trap is an undotted operator, which materializes a temporary array:\n\n# Bad: `+` builds a whole temporary array\nget_grid_data(field) .= get_grid_data(field) + get_grid_data(other)\n\n# Good: fully fused, no temporary\nget_grid_data(field) .+= get_grid_data(other)\n\nMeasured inside a function on a 16×16 field: the undotted form allocates 2128 bytes per call, the fused form 0. a .= a .+ b is also fused and also costs 0 — it is the missing dot, not the .=, that allocates. Measure allocations inside a function, not at global scope: untyped globals box on their own and swamp the number you are looking for.","title":"Minimize Allocations"},{"category":"section","location":"pages/optimization/#Pre-allocate-Work-Arrays","page":"Performance Optimization","text":"work = similar(get_grid_data(field))     # once, at setup\n\nfunction loop!(field, other, work, dt, nsteps)\n    ensure_layout!(field, :g)\n    ensure_layout!(other, :g)\n    fd = get_grid_data(field)\n    od = get_grid_data(other)\n    for step in 1:nsteps\n        work .= od\n        fd .+= dt .* work\n    end\nend\n\nHoisting get_grid_data out of the loop and reusing work makes the loop body allocation free (measured: 0 bytes for 100 iterations).","title":"Pre-allocate Work Arrays"},{"category":"section","location":"pages/optimization/#Minimize-Transforms","page":"Performance Optimization","text":"Every ensure_layout! that actually changes layout is an FFT. Batch all grid-space work between one pair of transforms:\n\n# Bad: a transform for every operation\nensure_layout!(field, :g)\noperation1(field)\nensure_layout!(field, :c)\nensure_layout!(field, :g)\noperation2(field)\nensure_layout!(field, :c)\n\n# Good: batch operations\nensure_layout!(field, :g)\noperation1(field)\noperation2(field)\nensure_layout!(field, :c)\n\nCounted with Tarang.transform_counts(), both starting from grid layout: the first form costs (forward = 2, backward = 1) — three transforms — the second (forward = 1, backward = 0).","title":"Minimize Transforms"},{"category":"section","location":"pages/optimization/#FFT-Optimization","page":"Performance Optimization","text":"","title":"FFT Optimization"},{"category":"section","location":"pages/optimization/#Grid-Sizes","page":"Performance Optimization","text":"FFT cost is very sensitive to the factorization of N. Measured here for a 2D real FFT (FFTW.plan_rfft, FFTW.MEASURE, single thread):\n\nN time per 2D rfft\n256 (2⁸) 0.11 ms\n250 (2·5³) 0.14 ms\n251 (prime) 1.09 ms\n\nPrefer powers of two; small-prime factorizations (2, 3, 5) cost a little more but are perfectly acceptable; a large prime factor is a factor-of-ten cliff.","title":"Grid Sizes"},{"category":"section","location":"pages/optimization/#FFTW-Planning","page":"Performance Optimization","text":"Planning rigor is not user-tunable at present: every FFTW plan in Tarang is created with flags=FFTW.MEASURE (src/core/transforms/transform_planning.jl). The TARANG_FFTW_RIGOR environment variable sets a config value that nothing reads, and there is no FFTW_PLANNING_RIGOR knob at all. Do not expect to change planning rigor from outside the code.\n\nWhat is tunable is the FFTW thread count — see Threading below.","title":"FFTW Planning"},{"category":"section","location":"pages/optimization/#FFTW-Wisdom","page":"Performance Optimization","text":"FFTW wisdom is global to the FFTW library, so plans Tarang builds later reuse it. Import it before you create your first Domain (that is when the plans are built):\n\nusing FFTW\n\nFFTW.import_wisdom(\"fftw_wisdom.txt\")    # at startup, if the file exists\n# ... build the domain, run the simulation ...\nFFTW.export_wisdom(\"fftw_wisdom.txt\")    # after the run\n\nTarang does not load or save wisdom for you; the WISDOM_FILE config key is currently inert.","title":"FFTW Wisdom"},{"category":"section","location":"pages/optimization/#MPI-Optimization","page":"Performance Optimization","text":"","title":"MPI Optimization"},{"category":"section","location":"pages/optimization/#Process-Mesh","page":"Performance Optimization","text":"The mesh rank must be at most one less than the domain dimension: PencilFFTs needs at least one axis to stay local for the FFT. A 2D domain therefore takes a 1D mesh; a 3D domain takes a 2D (pencil) mesh, or a 1D (slab) mesh if you prefer.\n\n# 2D domain: 1D mesh                       (np=4: local grid 32×8 of a 32×32 domain)\ndist = Distributor(coords; mesh=(4,))\n\n# 3D domain: 2D mesh — square              (np=4: local grid 16×8×8 of 16³)\ndist = Distributor(coords; mesh=(2, 2))\n\n# 3D domain: 2D mesh — anisotropic         (np=4: local grid 16×4×16)\ndist = Distributor(coords; mesh=(4, 1))\n\n# 3D domain: 1D slab mesh — also valid     (np=4: local grid 16×16×4)\ndist = Distributor(coords; mesh=(4,))\n\nA mesh whose rank equals the domain dimension decomposes every axis and leaves nothing local to transform. On a 2D domain, mesh=(2, 2) at np=4 is a hard error:\n\nPencilFFT plan creation failed with 4 MPI processes. Local FFTW fallback would produce incorrect results.\n\nUnless you have a specific reason, omit mesh=: the auto mesh is (nprocs,) for a 2D domain and a balanced 2D mesh for a 3D domain (at np=4: (2, 2)).","title":"Process Mesh"},{"category":"section","location":"pages/optimization/#Load-Balance","page":"Performance Optimization","text":"PencilArrays spreads the remainder across ranks, so a non-divisible N is only mildly uneven — it is not a correctness problem:\n\nN = 256   # np=4: per-rank extents 64, 64, 64, 64\nN = 250   # np=4: per-rank extents 62, 63, 62, 63\n\nN = 250 also costs a little more in the FFT than N = 256 (see Grid Sizes above), but neither the one-plane imbalance nor the factorization is worth worrying about. What actually hurts is a large prime factor, such as N = 251.","title":"Load Balance"},{"category":"section","location":"pages/optimization/#Communication","page":"Performance Optimization","text":"Transposes are the dominant communication cost. Keep synchronization points out of the inner loop: reduce over parent(get_grid_data(field)) and issue a single explicit MPI.Allreduce (or use global_sum / global_max / integrate, which already do that), and gate output and progress reporting on a cadence rather than doing it every step.","title":"Communication"},{"category":"section","location":"pages/optimization/#Threading","page":"Performance Optimization","text":"","title":"Threading"},{"category":"section","location":"pages/optimization/#FFTW-Threads","page":"Performance Optimization","text":"Tarang sets the FFTW thread count at startup: all Julia threads in a serial run, and one thread per rank under MPI (ranks already provide the parallelism). Override with an environment variable:\n\nexport TARANG_FFTW_THREADS=4\n\nBLAS threads are separate and are worth pinning to 1 under MPI:\n\nusing LinearAlgebra\nBLAS.set_num_threads(1)","title":"FFTW Threads"},{"category":"section","location":"pages/optimization/#Julia-Threads","page":"Performance Optimization","text":"Threads.nthreads()          # threads available (set with `julia -t N`)\n\nThreads.@threads for i in 1:N\n    # ...\nend","title":"Julia Threads"},{"category":"section","location":"pages/optimization/#Resolution-Guidelines","page":"Performance Optimization","text":"","title":"Resolution Guidelines"},{"category":"section","location":"pages/optimization/#Minimum-Resolution","page":"Performance Optimization","text":"Feature Modes Needed\nSmooth profile ~10\nModerate gradient ~30\nSharp gradient ~100\nBoundary layer ~50-100","title":"Minimum Resolution"},{"category":"section","location":"pages/optimization/#Check-Convergence","page":"Performance Optimization","text":"Run the same problem at two resolutions and compare. Spectral convergence is exponential: once the solution is resolved, refining changes nothing.\n\nfunction heat_max(N, nsteps; nu=0.1, dt=1e-3)\n    coords = CartesianCoordinates(\"x\", \"y\")\n    dist   = Distributor(coords; dtype=Float64, device=CPU())\n    xb     = RealFourier(coords[\"x\"]; size=N, bounds=(0.0, 2pi))\n    yb     = RealFourier(coords[\"y\"]; size=N, bounds=(0.0, 2pi))\n    domain = Domain(dist, (xb, yb))\n\n    v = ScalarField(domain, \"v\")\n    problem = IVP([v])\n    add_parameters!(problem, nu=nu)\n    add_equation!(problem, \"∂t(v) - nu*lap(v) = 0\")\n    set!(v, (x, y) -> sin(x) * cos(y))\n\n    solver = InitialValueSolver(problem, RK222(); dt=dt)\n    run!(solver; stop_iteration=nsteps, progress=false)\n    ensure_layout!(v, :g)\n    return maximum(abs, get_grid_data(v))\nend\n\nheat_max(16, 100)    # 0.9801986733\nheat_max(32, 100)    # 0.9801986733  (difference 1.6e-15 — already resolved at N=16)","title":"Check Convergence"},{"category":"section","location":"pages/optimization/#Timestepping","page":"Performance Optimization","text":"","title":"Timestepping"},{"category":"section","location":"pages/optimization/#CFL-Optimization","page":"Performance Optimization","text":"CFL takes the solver, not the problem, and velocities are added as VectorFields. Pass the controller to run!:\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\ncfl = CFL(solver; safety=0.4, max_change=1.2, cadence=5, max_dt=0.01)\nadd_velocity!(cfl, u)\n\nrun!(solver; stop_iteration=1000, cfl=cfl)\n\nDefaults: initial_dt=0.01, cadence=1, safety=0.4, threshold=0.1, max_change=2.0, min_change=0.5, max_dt=Inf.\n\nsafety — lower is conservative, higher is aggressive (0.4 is the default).\nmax_change / min_change — bound the ratio between successive timesteps. Tightening max_change to ~1.2 keeps dt smooth.\nthreshold — sticky-dt hysteresis: a new dt is only committed when it differs from the current one by more than this relative amount. This matters for implicit solvers, whose LHS factorization is rebuilt whenever dt changes.\ncadence — recompute the timestep every cadence calls rather than every step.\n\nCFL is a mutable struct, so the knobs can also be changed mid-run (cfl.max_change = 1.2). The current step is cfl.current_dt (there is no min_dt field).","title":"CFL Optimization"},{"category":"section","location":"pages/optimization/#Implicit-Methods","page":"Performance Optimization","text":"For stiff problems, IMEX methods allow larger timesteps:\n\n# Explicit-ish (small dt for diffusion)\nsolver = InitialValueSolver(problem, RK222(); dt=1e-5)\n\n# IMEX multistep (diffusion implicit, larger dt)\nsolver = InitialValueSolver(problem, SBDF2(); dt=1e-3)","title":"Implicit Methods"},{"category":"section","location":"pages/optimization/#Benchmarking","page":"Performance Optimization","text":"","title":"Benchmarking"},{"category":"section","location":"pages/optimization/#Standard-Benchmark","page":"Performance Optimization","text":"function benchmark_solver(solver, nsteps)\n    step!(solver)                    # warm up (compilation)\n    start = time()\n    for i in 1:nsteps\n        step!(solver)\n    end\n    elapsed = time() - start\n\n    ngrid = prod(b.meta.size for b in solver.problem.domain.bases)\n    println(\"Time per step: $(elapsed/nsteps * 1000) ms\")\n    println(\"Grid points/sec: $(ngrid * nsteps / elapsed)\")\nend\n\nngrid is the global grid size (b.meta.size is the global size of each basis). For the per-rank count use length(get_grid_data(solver.state[1])).","title":"Standard Benchmark"},{"category":"section","location":"pages/optimization/#Scaling-Test","page":"Performance Optimization","text":"Strong scaling holds the problem fixed and varies the process count; weak scaling holds the work per process fixed. Both are driven from the shell, one mpiexec per point:\n\nfor np in 1 2 4 8 16; do\n    mpiexec -n $np julia --project=. run_benchmark.jl\ndone","title":"Scaling Test"},{"category":"section","location":"pages/optimization/#Common-Bottlenecks","page":"Performance Optimization","text":"FFT transforms: use FFT-friendly sizes, batch layout changes, watch Tarang.transform_counts()\nMPI communication: valid mesh, minimize syncs\nMemory allocation: pre-allocate, use in-place ops\nI/O: write less frequently, use parallel I/O","title":"Common Bottlenecks"},{"category":"section","location":"pages/optimization/#See-Also","page":"Performance Optimization","text":"Parallelism: MPI optimization\nTimesteppers: Choosing methods\nDomains: Resolution guidelines","title":"See Also"},{"category":"section","location":"getting_started/first_steps/#First-Steps-with-Tarang.jl","page":"First Steps","text":"This tutorial introduces the basic workflow for setting up and solving PDEs with Tarang.jl. We'll build a simple 2D diffusion problem step by step.","title":"First Steps with Tarang.jl"},{"category":"section","location":"getting_started/first_steps/#The-Tarang.jl-Workflow","page":"First Steps","text":"Every Tarang.jl simulation follows these steps:\n\nInitialize MPI for parallel computing\nDefine coordinates and create a distributor for MPI processes\nChoose spectral bases for each coordinate direction\nCreate a domain combining the bases\nDefine fields (scalar, vector, or tensor)\nSet up a problem (IVP, BVP, or EVP)\nAdd equations using symbolic syntax\nSpecify boundary conditions\nCreate a solver with a timestepper\nRun the simulation with time stepping\nAnalyze and output results\n\nLet's walk through each step with a concrete example.","title":"The Tarang.jl Workflow"},{"category":"section","location":"getting_started/first_steps/#Example:-2D-Heat-Diffusion","page":"First Steps","text":"We'll solve the 2D heat equation:\n\nfracpartial Tpartial t = kappa nabla^2 T\n\non a rectangular domain with Dirichlet boundary conditions.","title":"Example: 2D Heat Diffusion"},{"category":"section","location":"getting_started/first_steps/#Step-1:-Initialize-MPI","page":"First Steps","text":"Every Tarang.jl script starts by initializing MPI:\n\nusing Tarang\nusing MPI\n\nMPI.Init()","title":"Step 1: Initialize MPI"},{"category":"section","location":"getting_started/first_steps/#Step-2:-Define-Coordinates-and-Distributor","page":"First Steps","text":"Coordinates define the dimension names and the MPI process distribution:\n\n# Create 2D Cartesian coordinates\ncoords = CartesianCoordinates(\"x\", \"z\")\n\n# Create distributor with 2×2 process mesh (4 MPI processes total)\ndist = Distributor(coords, mesh=(2, 2))\n\nThe mesh=(2, 2) means we'll use 4 MPI processes arranged in a 2×2 grid. This enables parallelization in both horizontal (x) and vertical (z) directions.\n\ntip: Choosing Process Mesh\nThe product of mesh dimensions should equal your MPI process count:mesh=(2, 2) → 4 processes\nmesh=(4, 2) → 8 processes\nmesh=(4, 4) → 16 processes","title":"Step 2: Define Coordinates and Distributor"},{"category":"section","location":"getting_started/first_steps/#Step-3:-Choose-Spectral-Bases","page":"First Steps","text":"Bases define the spectral representation in each direction:\n\n# Periodic direction (x) - use Fourier basis\nx_basis = RealFourier(coords[\"x\"], size=128, bounds=(0.0, 2π))\n\n# Bounded direction (z) - use Chebyshev basis\nz_basis = ChebyshevT(coords[\"z\"], size=64, bounds=(0.0, 1.0))\n\nBasis selection guide:\n\nFourier: Periodic boundaries (e.g., horizontal directions in atmospheric flows)\nChebyshev: Non-periodic with high accuracy (e.g., vertical direction with walls)\nLegendre: Non-periodic, alternative to Chebyshev\n\nThe size parameter determines the spectral resolution.","title":"Step 3: Choose Spectral Bases"},{"category":"section","location":"getting_started/first_steps/#Step-4:-Create-Domain","page":"First Steps","text":"Combine the bases into a domain:\n\ndomain = Domain(dist, (x_basis, z_basis))\n\nThe domain handles the spatial discretization and MPI distribution.","title":"Step 4: Create Domain"},{"category":"section","location":"getting_started/first_steps/#Step-5:-Define-Fields","page":"First Steps","text":"Create a scalar field for temperature:\n\nT = ScalarField(dist, \"T\", (x_basis, z_basis))\n\nFor vector fields (like velocity):\n\nu = VectorField(dist, coords, \"u\", (x_basis, z_basis))","title":"Step 5: Define Fields"},{"category":"section","location":"getting_started/first_steps/#Step-6:-Set-Up-Problem","page":"First Steps","text":"Create an Initial Value Problem (IVP):\n\nproblem = IVP([T])\n\n# Add the heat equation\nadd_equation!(problem, \"∂t(T) - kappa*lap(T) = 0\")\n\n# Set diffusion coefficient\nproblem.namespace[\"kappa\"] = 1.0\n\nThe equation uses symbolic notation:\n\n∂t(T): time derivative ∂T/∂t\nlap(T): Laplacian ∇²T\nkappa: a parameter we can easily modify","title":"Step 6: Set Up Problem"},{"category":"section","location":"getting_started/first_steps/#Step-7:-Add-Boundary-Conditions","page":"First Steps","text":"Specify boundary conditions at the domain edges:\n\n# Bottom wall (z=0): hot, T=1\nadd_equation!(problem, \"T(z=0) = 1\")\n\n# Top wall (z=1): cold, T=0\nadd_equation!(problem, \"T(z=1) = 0\")\n\nThe x-direction is periodic (RealFourier basis), so no boundary conditions are needed there.","title":"Step 7: Add Boundary Conditions"},{"category":"section","location":"getting_started/first_steps/#Step-8:-Create-Solver","page":"First Steps","text":"Choose a timestepper and create the solver:\n\n# RK222 is a good general-purpose IMEX timestepper\ntimestepper = RK222()\nsolver = InitialValueSolver(problem, timestepper, dt=0.001)\n\nPopular timesteppers:\n\nRK222, RK443: IMEX Runge-Kutta (good for most problems)\nCNAB2, SBDF2: IMEX multistep methods (for stiff problems)","title":"Step 8: Create Solver"},{"category":"section","location":"getting_started/first_steps/#Step-9:-Set-Initial-Conditions","page":"First Steps","text":"Initialize the temperature field:\n\n# Get the grid space representation\nT_grid = get_grid_data(T)\n\n# Set a Gaussian perturbation\nfor i in 1:size(T_grid, 1), j in 1:size(T_grid, 2)\n    x = (i-1) * 2π / 128\n    z = (j-1) / 64\n    T_grid[i, j] = 0.5 + 0.1 * exp(-((x-π)^2 + (z-0.5)^2) / 0.1)\nend\n\n# Transform to spectral space\nto_spectral!(T)","title":"Step 9: Set Initial Conditions"},{"category":"section","location":"getting_started/first_steps/#Step-10:-Run-Simulation","page":"First Steps","text":"Time-step the solver:\n\nt_end = 1.0\niteration = 0\n\nwhile solver.sim_time < t_end\n    step!(solver)\n    iteration += 1\n\n    # Print progress every 100 steps\n    if iteration % 100 == 0 && MPI.Comm_rank(MPI.COMM_WORLD) == 0\n        println(\"Iteration: $iteration, Time: $(solver.sim_time)\")\n    end\nend","title":"Step 10: Run Simulation"},{"category":"section","location":"getting_started/first_steps/#Step-11:-Finalize-MPI","page":"First Steps","text":"Always finalize MPI at the end:\n\nMPI.Finalize()","title":"Step 11: Finalize MPI"},{"category":"section","location":"getting_started/first_steps/#Complete-Example","page":"First Steps","text":"Here's the full script:\n\nusing Tarang, MPI\n\nMPI.Init()\n\n# Setup\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords, mesh=(2, 2))\n\nx_basis = RealFourier(coords[\"x\"], size=128, bounds=(0.0, 2π))\nz_basis = ChebyshevT(coords[\"z\"], size=64, bounds=(0.0, 1.0))\n\ndomain = Domain(dist, (x_basis, z_basis))\nT = ScalarField(dist, \"T\", (x_basis, z_basis))\n\n# Problem\nproblem = IVP([T])\nadd_equation!(problem, \"∂t(T) - kappa*lap(T) = 0\")\nproblem.namespace[\"kappa\"] = 0.01\n\n# Boundary conditions (syntax auto-detected)\nadd_equation!(problem, \"T(z=0) = 1\")\nadd_equation!(problem, \"T(z=1) = 0\")\n\n# Solver\nsolver = InitialValueSolver(problem, RK222(), dt=0.001)\n\n# Initial conditions\nT_grid = get_grid_data(T)\nfor i in 1:size(T_grid, 1), j in 1:size(T_grid, 2)\n    x = (i-1) * 2π / 128\n    z = (j-1) / 64\n    T_grid[i, j] = 0.5 + 0.1 * exp(-((x-π)^2 + (z-0.5)^2) / 0.1)\nend\nto_spectral!(T)\n\n# Time stepping\nwhile solver.sim_time < 1.0\n    step!(solver)\nend\n\nMPI.Finalize()\n\nSave this as heat_diffusion.jl and run:\n\nmpiexec -n 4 julia heat_diffusion.jl","title":"Complete Example"},{"category":"section","location":"getting_started/first_steps/#What's-Next?","page":"First Steps","text":"Now that you understand the basic workflow, explore:\n\nRunning with MPI: Details on parallel execution\n2D Rayleigh-Bénard Tutorial: More complex fluid dynamics example\nBoundary Conditions: Advanced boundary condition types\nAnalysis and Output: Saving data and computing diagnostics","title":"What's Next?"},{"category":"section","location":"getting_started/first_steps/#Common-Patterns","page":"First Steps","text":"","title":"Common Patterns"},{"category":"section","location":"getting_started/first_steps/#Multiple-Fields","page":"First Steps","text":"For coupled PDEs with multiple fields:\n\nu = VectorField(dist, coords, \"u\", (x_basis, z_basis))\np = ScalarField(dist, \"p\", (x_basis, z_basis))\nT = ScalarField(dist, \"T\", (x_basis, z_basis))\n\nproblem = IVP([u.components[1], u.components[2], p, T])","title":"Multiple Fields"},{"category":"section","location":"getting_started/first_steps/#Parameters","page":"First Steps","text":"Add and modify parameters easily:\n\nproblem.namespace[\"Ra\"] = 1e6  # Rayleigh number\nproblem.namespace[\"Pr\"] = 0.7  # Prandtl number\nproblem.namespace[\"kappa\"] = 0.01  # Thermal diffusivity","title":"Parameters"},{"category":"section","location":"getting_started/first_steps/#Adaptive-Time-Stepping","page":"First Steps","text":"Use CFL condition for adaptive time steps:\n\ncfl = CFL(problem, safety=0.5)\nadd_velocity!(cfl, u)\n\nwhile solver.sim_time < t_end\n    dt = compute_timestep(cfl)\n    step!(solver, dt)\nend","title":"Adaptive Time Stepping"},{"category":"section","location":"getting_started/first_steps/#Key-Takeaways","page":"First Steps","text":"MPI must be initialized before creating any Tarang objects\nChoose bases appropriate for your boundary conditions (Fourier for periodic, Chebyshev/Legendre for bounded)\nProcess mesh should match your MPI process count\nSymbolic equations use natural mathematical notation\nBoundary conditions are specified separately from equations\nAlways finalize MPI at the end of your script","title":"Key Takeaways"},{"category":"section","location":"pages/stochastic_forcing/#Stochastic-Forcing","page":"Stochastic Forcing","text":"This page demonstrates how to set up a forced-dissipative 2D turbulence simulation, following the approach from GeophysicalFlows.jl.\n\n","title":"Stochastic Forcing"},{"category":"section","location":"pages/stochastic_forcing/#TL;DR-Automatic-Forcing-(Recommended)","page":"Stochastic Forcing","text":"The simplest way to add stochastic forcing is using add_stochastic_forcing!:\n\n# 1. Create forcing\nforcing = StochasticForcing(\n    field_size = (256, 256),\n    energy_injection_rate = 0.1,\n    injection_metric = :vorticity_kinetic,\n    k_forcing = 10.0,\n    dt = dt\n)\n\n# 2. Register with problem by variable symbol - forcing is handled automatically!\nadd_stochastic_forcing!(problem, :ω, forcing)\n\n# 3. Just call step! - no manual forcing management needed\nfor step in 1:nsteps\n    step!(solver)  # Forcing generated & applied internally\nend\n\nThe timestepper automatically:\n\nGenerates forcing ONCE at the start of each timestep\nUses the SAME forcing value across all RK substeps\nAdds forcing to the RHS of the specified equation\n\nThis keeps one white-noise draw fixed across the stages of a supported one-step method.\n\n","title":"TL;DR - Automatic Forcing (Recommended)"},{"category":"section","location":"pages/stochastic_forcing/#Why-Stochastic-Forcing-Needs-Special-Handling","page":"Stochastic Forcing","text":"White-in-time noise requires careful handling for correct statistics:\n\nStratonovich vs Itô: Choose the diagnostic interpretation appropriate to the model\nConstant within timestep: For Stratonovich correctness, forcing must be constant across all RK substeps\n√dt scaling: Discrete forcing needs F = √(Q̂/dt) · noise to give correct variance\n\nIf forcing were evaluated independently at every RK stage, its discrete covariance would be wrong.\n\n","title":"Why Stochastic Forcing Needs Special Handling"},{"category":"section","location":"pages/stochastic_forcing/#Maintained-Example:-Forced-2D-Turbulence","page":"Stochastic Forcing","text":"The runnable CPU/GPU example is examples/ivp/forced_2d_turbulence.jl. It evolves vorticity, solves for streamfunction, derives velocity, and registers ring forcing on vorticity. Its essential forcing setup is:\n\nforcing = StochasticForcing(\n    field_size = (Nx, Ny),\n    domain_size = (Lx, Ly),\n    energy_injection_rate = ε,\n    injection_metric = :vorticity_kinetic,\n    k_forcing = k_f,\n    dk_forcing = dk_f,\n    dt = max_dt,\n    spectrum_type = :ring,\n    architecture = device,\n)\n\nproblem = IVP([ζ, ψ, u, tau_ψ])\nadd_equation!(problem, \"∂t(ζ) = -u⋅∇(ζ) - drag*ζ - nu*Δ⁴(ζ)\")\nadd_equation!(problem, \"Δ(ψ) + tau_ψ - ζ = 0\")\nadd_equation!(problem, \"u - skew(grad(ψ)) = 0\")\nadd_bc!(problem, \"integ(ψ) = 0\")\nadd_stochastic_forcing!(problem, :ζ, forcing)\n\nsolver = InitialValueSolver(problem, RK222(); dt=max_dt)\nstep!(solver)\n\nThe full file includes initialization, CFL control, output, and CPU/GPU selection. Run it on CUDA with:\n\nTARANG_FORCED_2D_DEVICE=gpu julia --project=. examples/ivp/forced_2d_turbulence.jl\n\nThe prognostic terms remain on the explicit RHS because the pure-Fourier GPU runtime uses a device-native explicit RK path while refreshing the Poisson and velocity constraints spectrally at every stage.\n\n","title":"Maintained Example: Forced 2D Turbulence"},{"category":"section","location":"pages/stochastic_forcing/#Fourier–Chebyshev-domains","page":"Stochastic Forcing","text":"StochasticForcing is the right choice when every spatial direction is Fourier. A bounded Chebyshev direction is not a wavenumber axis, so applying a Fourier ring formula to its coefficient index is not physically meaningful. Use SeparableStochasticForcing instead:\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=GPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx))\nzbasis = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(0.0, Lz))\ndomain = Domain(dist, (xbasis, zbasis))\n\nb = ScalarField(domain, \"b\")\nforcing = SeparableStochasticForcing(\n    fourier_size=(Nx,),\n    chebyshev_basis=zbasis,\n    chebyshev_profile=z -> z * (Lz - z),\n    domain_size=(Lx,),\n    energy_injection_rate=0.05,\n    injection_metric=:direct,\n    k_forcing=4 * 2pi / Lx,\n    dk_forcing=2pi / Lx,\n    dt=dt,\n    architecture=GPU(),\n    rng=MersenneTwister(1234),\n)\nadd_stochastic_forcing!(problem, :b, forcing)\nsolver = InitialValueSolver(problem, RK222(); dt)\n\nThe function profile is evaluated on the physical Chebyshev grid, transformed once, and normalized to unit quadrature mean square. You may instead pass an Nz-element vector of Chebyshev coefficients. The forcing then draws only the Fourier modes and forms their outer product with that fixed profile. It stays constant across RK stages and changes at the next timestep.\n\nMixed forcing supports injection_metric=:direct. The :vorticity_kinetic metric is intentionally rejected because its Fourier 1/|k|² definition does not extend to a bounded direction without specifying the model's inverse elliptic operator.\n\nOn CUDA, a coupled Fourier–Chebyshev IVP left at the default matsolver=:auto selects CuSparseLU. The field, forcing, RK workspace, subproblem vectors, sparse matrix, and solve buffers remain device-resident, and warmed solves reuse their RHS, solution, and scratch allocations. Passing an explicit CPU-only matsolver=:sparse or :dense raises an error with a suggestion to use :auto or :cuda_sparse.\n\nThe same single-GPU paths support three-dimensional IVPs. For a triply periodic Fourier–Fourier–Fourier domain, use StochasticForcing; the field-native RK path and multidimensional cuFFT stay on the GPU. For a Fourier–Fourier–Chebyshev domain, use SeparableStochasticForcing with fourier_size=(Nx, Ny) and the Chebyshev basis/profile. That coupled path uses CuSparseLU and cached mixed-transform buffers. Once the plans and workspaces are warm, neither path allocates device memory during step!. These guarantees currently apply to one GPU; distributed multi-GPU IVPs have separate transform constraints.\n\n","title":"Fourier–Chebyshev domains"},{"category":"section","location":"pages/stochastic_forcing/#Understanding-the-Example","page":"Stochastic Forcing","text":"","title":"Understanding the Example"},{"category":"section","location":"pages/stochastic_forcing/#Forcing-Spectrum","page":"Stochastic Forcing","text":"The forcing concentrates energy input around a specific wavenumber:\n\n          │\n    Q̂(k)  │      ╱╲\n          │    ╱    ╲\n          │  ╱        ╲\n          │╱            ╲\n          └──────────────────► |k|\n                k_f\n               ←─────→\n              2*δ_f (width)\n\nModes with |k| ≈ k_f receive most of the energy. This creates a dual cascade:\n\nEnergy cascades to large scales (inverse cascade)\nEnstrophy cascades to small scales (forward cascade)","title":"Forcing Spectrum"},{"category":"section","location":"pages/stochastic_forcing/#Energy-Balance","page":"Stochastic Forcing","text":"In steady state:\n\nvarepsilon_textforcing = varepsilon_textdrag + varepsilon_textviscous\n\nThe linear drag μ removes energy at large scales, preventing condensation.\n\n","title":"Energy Balance"},{"category":"section","location":"pages/stochastic_forcing/#Step-by-Step-Breakdown","page":"Stochastic Forcing","text":"","title":"Step-by-Step Breakdown"},{"category":"section","location":"pages/stochastic_forcing/#Step-1:-Choose-Forcing-Parameters","page":"Stochastic Forcing","text":"# Where to inject energy (wavenumber)\nforcing_wavenumber = 14.0 * 2π/L  # Mode k ≈ 14\n\n# How spread out the forcing is\nforcing_bandwidth = 1.5 * 2π/L    # A few modes wide\n\n# How much energy to inject per unit time\nε = 0.1\n\nGuidelines:\n\nk_forcing: Set to intermediate scales (not too large, not too small)\ndk_forcing: Usually 1-3 in mode units; too narrow causes intermittency\nε: Balance with dissipation; if energy grows unbounded, increase μ or ν","title":"Step 1: Choose Forcing Parameters"},{"category":"section","location":"pages/stochastic_forcing/#Step-2:-Create-the-Forcing-Object","page":"Stochastic Forcing","text":"forcing = StochasticForcing(\n    field_size = (n, n),              # Match your grid\n    domain_size = (L, L),             # Match your domain\n    energy_injection_rate = ε,        # Normalized automatically\n    injection_metric = :vorticity_kinetic, # forcing is applied to vorticity\n    k_forcing = forcing_wavenumber,\n    dk_forcing = forcing_bandwidth,\n    dt = dt,                          # For √dt scaling\n    spectrum_type = :ring,            # Isotropic ring\n    rng = MersenneTwister(1234)       # Reproducibility\n)","title":"Step 2: Create the Forcing Object"},{"category":"section","location":"pages/stochastic_forcing/#Step-3:-Apply-Forcing-in-Time-Loop","page":"Stochastic Forcing","text":"for step in 1:nsteps\n    # 1. Store qⁿ for the optional Stratonovich work diagnostic\n    store_prevsol!(forcing, get_coeff_data(ω))\n\n    # 2. Advance; registered forcing is generated and applied automatically\n    step!(solver)\n\n    # 3. Compute work done\n    W = work_stratonovich(forcing, get_coeff_data(ω))\nend","title":"Step 3: Apply Forcing in Time Loop"},{"category":"section","location":"pages/stochastic_forcing/#Step-4:-Verify-Energy-Budget","page":"Stochastic Forcing","text":"# Average over many timesteps in steady state\nε_measured = mean(work_array) / dt\n\n# Should match target\n@assert abs(ε_measured - ε) / ε < 0.1  \"Energy budget not balanced!\"\n\n","title":"Step 4: Verify Energy Budget"},{"category":"section","location":"pages/stochastic_forcing/#Spectrum-Types","page":"Stochastic Forcing","text":"","title":"Spectrum Types"},{"category":"section","location":"pages/stochastic_forcing/#Ring-Forcing-(:ring)-Default","page":"Stochastic Forcing","text":"Forces modes in a Gaussian ring around |k| = k_f:\n\nforcing = StochasticForcing(\n    field_size = (256, 256),\n    k_forcing = 10.0,\n    dk_forcing = 2.0,\n    spectrum_type = :ring\n)\n\nBest for: Standard 2D/3D turbulence, dual cascade studies","title":"Ring Forcing (:ring) - Default"},{"category":"section","location":"pages/stochastic_forcing/#Band-Forcing-(:band)","page":"Stochastic Forcing","text":"Sharp cutoff band |k| ∈ [k_f - δ_f, k_f + δ_f]:\n\nforcing = StochasticForcing(\n    field_size = (256, 256),\n    k_forcing = 10.0,\n    dk_forcing = 2.0,\n    spectrum_type = :band\n)\n\nBest for: Controlled experiments with exact spectral support","title":"Band Forcing (:band)"},{"category":"section","location":"pages/stochastic_forcing/#Low-k-Forcing-(:lowk)","page":"Stochastic Forcing","text":"Force all modes with |k| < k_f:\n\nforcing = StochasticForcing(\n    field_size = (256, 256),\n    k_forcing = 5.0,\n    spectrum_type = :lowk\n)\n\nBest for: Large-scale forcing, simple turbulence setups","title":"Low-k Forcing (:lowk)"},{"category":"section","location":"pages/stochastic_forcing/#Kolmogorov-Forcing-(:kolmogorov)","page":"Stochastic Forcing","text":"Smooth large-scale forcing for Kolmogorov cascade:\n\nforcing = StochasticForcing(\n    field_size = (256, 256),\n    k_forcing = 4.0,\n    spectrum_type = :kolmogorov\n)\n\nBest for: Kolmogorov-Obukhov theory verification\n\n","title":"Kolmogorov Forcing (:kolmogorov)"},{"category":"section","location":"pages/stochastic_forcing/#Mathematical-Background","page":"Stochastic Forcing","text":"","title":"Mathematical Background"},{"category":"section","location":"pages/stochastic_forcing/#Forcing-Statistics","page":"Stochastic Forcing","text":"The forcing ξ(x,t) is a random field with:\n\nProperty Formula\nZero mean ⟨ξ(x,t)⟩ = 0\nWhite in time ⟨ξ(x,t) ξ(x',t')⟩ = Q(x-x') δ(t-t')\nPower spectrum ⟨ξ̂(k) ξ̂*(k')⟩ = Q̂(k) δ(k-k')","title":"Forcing Statistics"},{"category":"section","location":"pages/stochastic_forcing/#Numerical-Implementation","page":"Stochastic Forcing","text":"For discrete time with step dt:\n\nhatF(k) = sqrtfrachatQ(k)dt cdot e^2pi i cdot textrand()\n\nThe √dt scaling gives correct variance: ⟨|F̂|²⟩ · dt = Q̂(k)","title":"Numerical Implementation"},{"category":"section","location":"pages/stochastic_forcing/#Injection-metric-and-normalization","page":"Stochastic Forcing","text":"varepsilon = frac12M^2sum_k hatQ(k)w_k\nqquad\nw_k = begincases\n1  textttdirect\nk^-2  textttvorticity_kinetic\nendcases\n\nHere M = prod(field_size) because Tarang uses unnormalized FFT coefficients. The default injection_metric = :direct normalizes the quadratic invariant of the forced variable itself. When forcing is applied to 2-D vorticity and ε should mean kinetic-energy injection, set injection_metric = :vorticity_kinetic explicitly. The zero mode has zero weight for that metric.\n\nThe constructor stores the full Fourier spectrum. Diagnostics also accept real-FFT half-spectra: omitted conjugate modes then have multiplicity 2, except the DC mode and an even-length Nyquist endpoint, which have multiplicity 1. A positive ε with no representable nonzero forced mode raises ArgumentError instead of silently creating zero forcing.","title":"Injection metric and normalization"},{"category":"section","location":"pages/stochastic_forcing/#Stratonovich-vs-Itô","page":"Stochastic Forcing","text":"Why Stratonovich?\n\nChain rule works normally: d(f(X)) = f'(X) dX\nPhysical limit of colored noise with τ→0\nSame formulas for stochastic and deterministic forcing\n\nWork calculation:\n\nCalculus Formula\nStratonovich W = dt Re⟨(qⁿ + qⁿ⁺¹)/2, F̂⟩_w\nItô W = dt Re⟨qⁿ, F̂⟩_w + ε dt\n\nThe weighted pairing ⟨⋅,⋅⟩_w includes M⁻², the selected injection metric, and real-FFT multiplicities. For vorticity-kinetic forcing, pass vorticity coefficients; the pairing supplies 1/|k|².\n\n","title":"Stratonovich vs Itô"},{"category":"section","location":"pages/stochastic_forcing/#Supported-timesteppers","page":"Stochastic Forcing","text":"For Runge–Kutta and other supported one-step methods, forcing stays constant within a timestep:\n\nTimestep n:     [stage 1] → [stage 2] → [stage 3] → [stage 4]\nForcing:           F_n         F_n         F_n         F_n\n\nTimestep n+1:   [stage 1] → [stage 2] → [stage 3] → [stage 4]\nForcing:         F_{n+1}     F_{n+1}     F_{n+1}     F_{n+1}\n\nTarang handles this via the substep argument:\n\n# Substep 1: generates NEW forcing\ngenerate_forcing!(forcing, t, 1)\n\n# Substeps 2-4: returns CACHED forcing\ngenerate_forcing!(forcing, t, 2)  # Same as substep 1\ngenerate_forcing!(forcing, t, 3)  # Same as substep 1\ngenerate_forcing!(forcing, t, 4)  # Same as substep 1\n\nFirst-order CNAB1 and SBDF1 are also supported. Methods that reuse or combine stochastic RHS/state values across time levels would color white noise or produce the wrong variance, so Tarang fails before drawing forcing for CNAB2, MCNAB2, CNLF2, SBDF2, SBDF3, SBDF4, DiagonalIMEX_SBDF2, ETD_CNAB2, and ETD_SBDF2. Use a supported one-step Runge–Kutta/ETD method or a first-order IMEX method instead.\n\nWith enforce_hermitian=true, self-conjugate Fourier modes are projected to sqrt(2) * real(z). The factor preserves their variance while making the inverse transform real.\n\n","title":"Supported timesteppers"},{"category":"section","location":"pages/stochastic_forcing/#Adaptive-Timestepping","page":"Stochastic Forcing","text":"When dt changes, update the forcing:\n\ndt_new = compute_timestep(cfl)\nset_dt!(forcing, dt_new)\ngenerate_forcing!(forcing, t, 1)\n\n","title":"Adaptive Timestepping"},{"category":"section","location":"pages/stochastic_forcing/#Troubleshooting","page":"Stochastic Forcing","text":"Problem Cause Solution\nEnergy grows unbounded ε too high or dissipation too low Increase μ or ν, or decrease ε\nNo turbulence develops ε too low Increase energy_injection_rate\nForcing at wrong scales k_f mismatched Check units: k_f should be in radians, not mode number\nResults not reproducible RNG not seeded Pass rng = MersenneTwister(seed)\nJerky dynamics dt too large Reduce timestep or forcing bandwidth\n\n","title":"Troubleshooting"},{"category":"section","location":"pages/stochastic_forcing/#API-Reference-Summary","page":"Stochastic Forcing","text":"","title":"API Reference Summary"},{"category":"section","location":"pages/stochastic_forcing/#Constructor","page":"Stochastic Forcing","text":"StochasticForcing(;\n    field_size,                          # (Nx, Ny) or (Nx, Ny, Nz)\n    domain_size = (2π, 2π, ...),        # (Lx, Ly, ...)\n    energy_injection_rate = 1.0,         # ε\n    injection_metric = :direct,          # or :vorticity_kinetic\n    k_forcing = 4.0,                     # k_f\n    dk_forcing = 1.0,                    # δ_f\n    dt = 0.01,\n    spectrum_type = :ring,               # :ring, :band, :lowk, :kolmogorov\n    rng = Random.MersenneTwister(),       # fresh RNG per forcing instance\n    dtype = Float64\n)","title":"Constructor"},{"category":"section","location":"pages/stochastic_forcing/#Key-Functions","page":"Stochastic Forcing","text":"Function Purpose\ngenerate_forcing!(f, t, substep) Generate/cache forcing\napply_forcing!(rhs, f, t, substep) Add forcing to RHS\nstore_prevsol!(f, sol) Store ψⁿ for work calculation\nwork_stratonovich(f, sol) Compute Stratonovich work\nset_dt!(f, dt) Update timestep\nmean_energy_injection_rate(f) Get target ε\ninstantaneous_power(f, sol) Get current P(t)\n\n","title":"Key Functions"},{"category":"section","location":"pages/stochastic_forcing/#See-Also","page":"Stochastic Forcing","text":"Timesteppers - Time integration methods\nSolvers - Using forcing with IVP solvers\nAPI: Stochastic Forcing - Complete API reference","title":"See Also"},{"category":"section","location":"pages/stochastic_forcing/#References","page":"Stochastic Forcing","text":"GeophysicalFlows.jl Stochastic Forcing\nGeophysicalFlows.jl 2D NS Example\nConstantinou, N. C., & Hogg, A. M. (2021). \"Intrinsic oceanic decadal variability\"","title":"References"},{"category":"section","location":"examples/gallery/#Example-Gallery","page":"Examples Gallery","text":"This gallery showcases various problems that can be solved with Tarang.jl. Each example includes complete, runnable code and visualization.","title":"Example Gallery"},{"category":"section","location":"examples/gallery/#Quick-Navigation","page":"Examples Gallery","text":"","title":"Quick Navigation"},{"category":"section","location":"examples/gallery/#By-Physics","page":"Examples Gallery","text":"Fluid Dynamics\nHeat Transfer\nStability Analysis\nAtmospheric Flows","title":"By Physics"},{"category":"section","location":"examples/gallery/#By-Complexity","page":"Examples Gallery","text":"Beginner (⭐)\nIntermediate (⭐⭐)\nAdvanced (⭐⭐⭐)","title":"By Complexity"},{"category":"section","location":"examples/gallery/#By-Features","page":"Examples Gallery","text":"2D Problems\n3D Problems\nTime-Dependent\nEigenvalue Problems\n\n","title":"By Features"},{"category":"section","location":"examples/gallery/#Fluid-Dynamics","page":"Examples Gallery","text":"","title":"Fluid Dynamics"},{"category":"section","location":"examples/gallery/#2D-Rayleigh-Bénard-Convection-(Intermediate)","page":"Examples Gallery","text":"Thermal convection in a horizontal layer heated from below.\n\nPhysics: Buoyancy-driven flow, convection cells, heat transfer\n\nFeatures:\n\nNavier-Stokes equations with buoyancy\nNo-slip boundary conditions\nAdaptive time stepping\nNusselt number analysis\n\nCode: examples/ivp/rayleigh_benard_2d.jl\n\nTutorial: 2D Rayleigh-Bénard\n\nUses the first-order formulation: divergence and Laplacian operators are expressed via trace(grad_f) and div(grad_f), with lift closures on the derivative basis to handle tau terms at both walls.\n\nusing Tarang\nusing Printf\n\n# Parameters\nLx, Lz   = 4.0, 1.0\nNx, Nz   = 256, 64\nRayleigh = 2e6\nPrandtl  = 1.0\n\nkappa = (Rayleigh * Prandtl)^(-1/2)\nnu    = (Rayleigh / Prandtl)^(-1/2)\n\n# Bases\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nzbasis = ChebyshevT(coords[\"z\"];  size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\n# Fields and tau variables\np = ScalarField(domain, \"p\");  b = ScalarField(domain, \"b\")\nu = VectorField(domain, \"u\")\ntau_p  = ScalarField(dist, \"tau_p\",  (), Float64)\ntau_b1 = ScalarField(dist, \"tau_b1\", (xbasis,), Float64)\ntau_b2 = ScalarField(dist, \"tau_b2\", (xbasis,), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis,), Float64)\n\n# First-order reduction with lift closures\nex, ez    = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_b = grad(b) + ez * τ_lift(tau_b1)\n\n# Problem\nproblem = IVP([p, b, u, tau_p, tau_b1, tau_b2, tau_u1, tau_u2])\nadd_parameters!(problem, kappa=kappa, nu=nu, Lz=Lz, ez=ez,\n                grad_u=grad_u, grad_b=grad_b, τ_lift=τ_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(b) - kappa*div(grad_b) + τ_lift(tau_b2) = -u⋅∇(b)\")\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + grad(p) - b*ez + τ_lift(tau_u2) = -u⋅∇(u)\")\n\nadd_bc!(problem, \"b(z=0) = Lz\");  add_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"b(z=Lz) = 0\"); add_bc!(problem, \"u(z=Lz) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-5)\n\nParameters to Try:\n\nRa = 10⁴ to 10⁷ (different convection regimes)\nPr = 0.7 (air), 1.0 (water), 7.0 (oils)\nAspect ratio = 1, 2, 4, 8\n\n","title":"2D Rayleigh-Bénard Convection (Intermediate)"},{"category":"section","location":"examples/gallery/#Forced-2D-Turbulence-(Advanced)","page":"Examples Gallery","text":"Stochastically forced 2D Navier-Stokes on a doubly-periodic domain, driving both an inverse energy cascade (energy → large scales) and a forward enstrophy cascade (enstrophy → small scales).\n\nPhysics: Inverse energy cascade, k⁻⁵/³ and k⁻³ spectra, 2D turbulence\n\nFeatures:\n\nVorticity-streamfunction formulation\nWhite-in-time ring forcing at wavenumber k_f with prescribed energy injection rate ε\nHyperviscosity (8th-order) for small-scale dissipation\nStochastic forcing with Hermitian symmetry for real-valued fields\n\nCode: examples/ivp/forced_2d_turbulence.jl\n\nThe governing equation is:\n\n∂t(q) + u·∇(q) = -ν Δ⁴(q) + F\n\nwhere q = Δ(ψ), u = skew(∇ψ), and F is a ring forcing injecting energy at |k| ∈ [kf − dkf, kf + dkf].\n\nusing Tarang\nusing Printf\n\n# Parameters\nNx, Ny  = 512, 512\nLx, Ly  = 2π, 2π\nnu      = 1e-20          # Hyperviscosity coefficient (8th-order)\nk_f     = 8.0            # Central forcing wavenumber\ndk_f    = 2.0            # Forcing bandwidth\nε       = 0.1            # Energy injection rate\n\ndomain = PeriodicDomain(Nx, Ny; L=(Lx, Ly))\ndist   = domain.dist\n\nq     = ScalarField(domain, \"q\")          # Vorticity\nψ     = ScalarField(domain, \"ψ\")          # Streamfunction\nu     = VectorField(domain, \"u\")\ntau_ψ = ScalarField(dist, \"tau_ψ\", (), Float64)\n\n# Ring forcing in wavenumber space\nforcing = StochasticForcing(\n    field_size            = (Nx, Ny),\n    domain_size           = (Lx, Ly),\n    energy_injection_rate = ε,\n    k_forcing             = k_f,\n    dk_forcing            = dk_f,\n    dt                    = 5e-3,\n    spectrum_type         = :ring,\n    enforce_hermitian     = true,\n)\n\nproblem = IVP([q, ψ, u, tau_ψ])\nadd_parameters!(problem, nu=nu)\n\nadd_equation!(problem, \"∂t(q) + nu*Δ⁴(q)  = -u⋅∇(q)\")  # PV evolution\nadd_equation!(problem, \"Δ(ψ) + tau_ψ - q  = 0\")          # Poisson equation\nadd_equation!(problem, \"u - skew(grad(ψ)) = 0\")           # Velocity from ψ\nadd_bc!(problem, \"integ(ψ) = 0\")\n\nadd_stochastic_forcing!(problem, :q, forcing)\n\nsolver = InitialValueSolver(problem, RK222(); dt=5e-3)\n\nTo run:\n\njulia --project=. examples/ivp/forced_2d_turbulence.jl\n\n# The domain is all-Fourier, so this one does decompose across ranks\nmpiexec -n 4 julia --project=. examples/ivp/forced_2d_turbulence.jl\n\n","title":"Forced 2D Turbulence (Advanced)"},{"category":"section","location":"examples/gallery/#Forced-SQG-Turbulence-(Advanced)","page":"Examples Gallery","text":"Surface quasi-geostrophic (SQG) turbulence: the streamfunction-vorticity relation uses the fractional Laplacian (−Δ)^{−1/2} instead of Δ^{−1}, producing sharp fronts and filamentary structures in the surface buoyancy field.\n\nPhysics: Forward energy cascade, k⁻⁵/³ spectrum, surface buoyancy fronts, filaments\n\nFeatures:\n\nFractional Laplacian SQG inversion: fraclap(ψ, 0.5) for (−Δ)^{1/2}\nHyperdiffusion with tunable exponent alpha\nStochastic ring forcing on the buoyancy field\nDoubly-periodic domain\n\nCode: examples/ivp/forced_sqg_turbulence.jl\n\nThe governing equation is:\n\n∂t(θ) + u·∇(θ) = -ν (−Δ)^α θ + F\n\nwhere ψ = (−Δ)^{−1/2} θ (SQG inversion) and u = skew(∇ψ).\n\nusing Tarang\nusing Printf\n\n# Parameters\nNx, Ny  = 512, 512\nLx, Ly  = 2π, 2π\nnu      = 1e-16          # Hyperdiffusion coefficient\nalpha   = 4.0            # Dissipation exponent: (-Δ)^α\nk_f     = 10.0           # Central forcing wavenumber\ndk_f    = 2.0\nε       = 0.1            # Energy injection rate\n\ndomain = PeriodicDomain(Nx, Ny; L=(Lx, Ly))\ndist   = domain.dist\n\nθ     = ScalarField(domain, \"θ\")          # Surface buoyancy\nψ     = ScalarField(domain, \"ψ\")          # Streamfunction\nu     = VectorField(domain, \"u\")\ntau_ψ = ScalarField(dist, \"tau_ψ\", (), Float64)\n\nforcing = StochasticForcing(\n    field_size            = (Nx, Ny),\n    domain_size           = (Lx, Ly),\n    energy_injection_rate = ε,\n    k_forcing             = k_f,\n    dk_forcing            = dk_f,\n    dt                    = 2e-3,\n    spectrum_type         = :ring,\n    enforce_hermitian     = true,\n)\n\nproblem = IVP([θ, ψ, u, tau_ψ])\nadd_parameters!(problem, nu=nu, alpha=alpha)\n\n# SQG inversion: fraclap(ψ, 0.5) implements (-Δ)^{1/2}\nadd_equation!(problem, \"∂t(θ) + nu*fraclap(θ, alpha) = -u⋅∇(θ)\")\nadd_equation!(problem, \"fraclap(ψ, 0.5) + tau_ψ - θ = 0\")\nadd_equation!(problem, \"u - skew(grad(ψ)) = 0\")\nadd_bc!(problem, \"integ(ψ) = 0\")\n\nadd_stochastic_forcing!(problem, :θ, forcing)\n\nsolver = InitialValueSolver(problem, RK222(); dt=2e-3)\n\nTo run:\n\njulia --project=. examples/ivp/forced_sqg_turbulence.jl\n\n","title":"Forced SQG Turbulence (Advanced)"},{"category":"section","location":"examples/gallery/#3D-Rotating-Rayleigh-Bénard-Convection-(Advanced)","page":"Examples Gallery","text":"Standard rotating Rayleigh-Bénard convection (rRBC) non-dimensionalized using box height H and thermal diffusion time τ_κ = H²/κ, with the E/Pr scaling on the momentum equation so that the Coriolis term ez×u is O(1).\n\nPhysics: Geophysical convection, Taylor columns, Coriolis effects, rotating turbulence\n\nFeatures:\n\nFull 3D (x periodic, y periodic, z Chebyshev)\nE/Pr prefactor on ∂t(u); Coriolis term curl(u) = ez×u\nFirst-order formulation with derivative-basis lift closures (tauu1/tauu2, tauθ1/tauθ2)\nRK222 IMEX timestepper\nEkman number, Prandtl number, Rayleigh number as control parameters\n\nCode: examples/ivp/rotating_rayleigh_benard_3d.jl\n\nThe governing equations are:\n\n∂t(θ) - Δ(θ)                         = -u⋅∇(θ)\nE/Pr * ∂t(u) - E*Δ(u) + ∇(p) + ez×u = Ra*θ*ez - u⋅∇(u)\n∇⋅u = 0\n\nwith no-slip, fixed-temperature walls: θ(z=0)=1, θ(z=1)=0, u(z=0,1)=0.\n\nusing Tarang\nusing Printf\n\n# Parameters\nLx, Ly, Lz = 2.0, 2.0, 1.0\nNx, Ny, Nz  = 64, 64, 32\nRa  = 1e6;  Pr = 1.0;  Ek = 1e-3\nEPr = Ek / Pr          # E/Pr prefactor on ∂t(u)\n\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nybasis = RealFourier(coords[\"y\"]; size=Ny, bounds=(0.0, Ly), dealias=3/2)\nzbasis = ChebyshevT(coords[\"z\"];  size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndomain = Domain(dist, (xbasis, ybasis, zbasis))\n\np = ScalarField(domain, \"p\");  θ = ScalarField(domain, \"θ\")\nu = VectorField(domain, \"u\")\ntau_p  = ScalarField(dist, \"tau_p\",  (), Float64)\ntau_θ1 = ScalarField(dist, \"tau_θ1\", (xbasis, ybasis), Float64)\ntau_θ2 = ScalarField(dist, \"tau_θ2\", (xbasis, ybasis), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis, ybasis), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis, ybasis), Float64)\n\nex, ey, ez = unit_vector_fields(coords, dist)\nlift_basis  = derivative_basis(zbasis, 1)\nτ_lift(A)   = lift(A, lift_basis, -1)\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_θ = grad(θ) + ez * τ_lift(tau_θ1)\n\nproblem = IVP([p, θ, u, tau_p, tau_θ1, tau_θ2, tau_u1, tau_u2])\nadd_parameters!(problem,\n    Ra=Ra, Ek=Ek, EPr=EPr, Lz=Lz, ez=ez,\n    grad_u=grad_u, grad_θ=grad_θ, τ_lift=τ_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(θ) - div(grad_θ) + τ_lift(tau_θ2) = -u⋅∇(θ)\")\nadd_equation!(problem, \"EPr*∂t(u) - Ek*div(grad_u) + ∇(p) + curl(u) + τ_lift(tau_u2) = Ra*θ*ez - u⋅∇(u)\")\n\nadd_bc!(problem, \"θ(z=0) = Lz\");  add_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"θ(z=Lz) = 0\"); add_bc!(problem, \"u(z=Lz) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-6)\n\nThe timestep is set by the buoyancy, not by the CFL condition. Ra*θ*ez sits on the explicit side, and with Ra = 10⁶ against the E/Pr = 10⁻³ prefactor on ∂t(u) its effective forcing is Ra/(E/Pr) = 10⁹. dt = 10⁻³ — the value the shipped script uses — goes to NaN within twenty steps, at every resolution and with every timestepper tried (8³ and 16³; RK222() and ETD_RK222()). dt = 10⁻⁶ integrates stably. Tarang also warns that θ appears linearly on the RHS; moving - Ra*θ*ez to the left-hand side gives the same answer but does not buy a larger step. Rescale the buoyancy (or shrink dt) before running this at the parameters above.\n\nThe exponential timesteppers are not usable here either: the tau-augmented linear operator of this first-order formulation is singular, so ETD_RK222() cannot build its matrix exponential and warns (ETD-RK222 failed: SingularException(…), falling back to RK222) on every step. Use RK222() directly.\n\nTo run — serial only. Under MPI the decomposed (trailing) axes must all be Fourier, so a (x, y, z_cheb) domain errors out at construction; and even with the Chebyshev axis moved to the front, the advective -u⋅∇(u) term needs a Chebyshev derivative on the explicit side, which is not supported distributed (see Running with MPI).\n\njulia --project=. examples/ivp/rotating_rayleigh_benard_3d.jl\n\nParameters to Try:\n\nEk = 10⁻³ to 10⁻⁵ (rotation rate)\nRa = 10⁵ to 10⁸ (supercriticality)\nPr = 0.7 (air), 1.0, 7.0 (water)\n\n","title":"3D Rotating Rayleigh-Bénard Convection (Advanced)"},{"category":"section","location":"examples/gallery/#3D-Taylor-Green-Vortex-(Advanced)","page":"Examples Gallery","text":"Canonical test case for 3D turbulence and vortex dynamics.\n\nPhysics: Vortex stretching, energy cascade, turbulence\n\nFeatures:\n\n3D Navier-Stokes\nPeriodic boundaries all directions\nEnergy spectrum analysis\nEnstrophy tracking\n\nCode: See 3D Turbulence Tutorial for a complete implementation — there is no standalone script for it under examples/.\n\nVisualization: Vorticity isosurfaces, energy spectra\n\n","title":"3D Taylor-Green Vortex (Advanced)"},{"category":"section","location":"examples/gallery/#3D-Channel-Flow-(Advanced)","page":"Examples Gallery","text":"Turbulent flow between parallel plates.\n\nPhysics: Wall-bounded turbulence, Reynolds stress, mean profiles\n\nFeatures:\n\n3D Navier-Stokes with walls\nPeriodic in x,y; no-slip at z walls\nMean flow forcing\nTurbulence statistics\n\nCode: Example coming soon\n\nAnalysis: Mean velocity profile, Reynolds stresses, energy spectra\n\n","title":"3D Channel Flow (Advanced)"},{"category":"section","location":"examples/gallery/#2D-Kelvin-Helmholtz-Instability-(Intermediate)","page":"Examples Gallery","text":"Shear flow instability and vortex formation.\n\nPhysics: Shear instability, vortex roll-up, mixing\n\nFeatures:\n\n2D Euler or Navier-Stokes\nShear layer with perturbation\nPeriodic boundaries\nVorticity evolution\n\nCode: Example coming soon\n\n","title":"2D Kelvin-Helmholtz Instability (Intermediate)"},{"category":"section","location":"examples/gallery/#Heat-Transfer","page":"Examples Gallery","text":"","title":"Heat Transfer"},{"category":"section","location":"examples/gallery/#1D-Heat-Diffusion-(Beginner)","page":"Examples Gallery","text":"Simple diffusion equation in one dimension.\n\nPhysics: Heat conduction, diffusion\n\nFeatures:\n\nSingle PDE\nDirichlet boundary conditions via the tau method\nExact solution comparison\nGood first example\n\nCode: See example below\n\nA bounded (Chebyshev) direction needs one tau variable per boundary condition: the BCs count as equations, so IVP must be given as many variables as there are equations, and the tau variables must be lifted back into the bulk equation. Two Dirichlet conditions ⇒ two tau variables ⇒ IVP([T, tau_1, tau_2]).\n\nusing Tarang, Printf\n\ncoords = CartesianCoordinates(\"x\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\n\nxbasis = ChebyshevT(coords[\"x\"]; size=64, bounds=(0.0, 1.0))\ndomain = Domain(dist, (xbasis,))\n\nT = ScalarField(domain, \"T\")\n\n# One tau variable per boundary condition, lifted onto the derivative basis\ntau_1 = ScalarField(dist, \"tau_1\", (), Float64)\ntau_2 = ScalarField(dist, \"tau_2\", (), Float64)\nlift_basis = derivative_basis(xbasis, 1)\nl1 = lift(tau_1, lift_basis, -1)\nl2 = lift(tau_2, lift_basis, -2)\n\nproblem = IVP([T, tau_1, tau_2])\nadd_parameters!(problem, kappa=0.01, l1=l1, l2=l2)\nadd_equation!(problem, \"∂t(T) - kappa*lap(T) + l1 + l2 = 0\")\n\n# Boundary conditions go through add_bc!, never add_equation!\nadd_bc!(problem, \"T(x=0) = 1\")\nadd_bc!(problem, \"T(x=1) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\n# Initial condition: the steady profile 1 - x plus one decaying sine mode\nx, = local_grids(dist, xbasis)\nensure_layout!(T, :g)\nget_grid_data(T) .= 1.0 .- x .+ 0.5 .* sin.(π .* x)\nensure_layout!(T, :c)\n\n# run! drives the loop to stop_time using the solver's fixed dt (no CFL here).\nrun!(solver; stop_time=0.5)\n\n# Exact solution: T = 1 - x + 0.5 sin(πx) exp(-κπ²t)\nensure_layout!(T, :g)\nexact = 1.0 .- x .+ 0.5 .* sin.(π .* x) .* exp(-0.01 * π^2 * 0.5)\n@printf(\"max error = %.2e\\n\", maximum(abs, Array(get_grid_data(T)) .- exact))\n\nPrints max error = 9.27e-12, and the boundary values are enforced to machine precision (T(x=0) = 1, T(x=1) = -5.2e-15).\n\n","title":"1D Heat Diffusion (Beginner)"},{"category":"section","location":"examples/gallery/#2D-Steady-State-Heat-Conduction-(Beginner)","page":"Examples Gallery","text":"Solve Laplace equation for steady heat distribution.\n\nPhysics: Steady-state diffusion, thermal equilibrium\n\nFeatures:\n\nBoundary value problem (LBVP)\nMixed boundary conditions\nSparse linear solve\n\nCode: Example coming soon\n\n","title":"2D Steady-State Heat Conduction (Beginner)"},{"category":"section","location":"examples/gallery/#3D-Thermal-Convection-with-Rotation-(Advanced)","page":"Examples Gallery","text":"Rotating Rayleigh-Bénard convection.\n\nPhysics: Geophysical flows, rotation effects, Taylor-Proudman\n\nFeatures:\n\nCoriolis force\n3D buoyancy-driven flow\nPattern formation\n\nCode: See 3D Rotating Rayleigh-Bénard above\n\n","title":"3D Thermal Convection with Rotation (Advanced)"},{"category":"section","location":"examples/gallery/#Stability-Analysis","page":"Examples Gallery","text":"","title":"Stability Analysis"},{"category":"section","location":"examples/gallery/#Rayleigh-Bénard-Linear-Stability-(Intermediate)","page":"Examples Gallery","text":"Compute critical Rayleigh number and eigenmodes.\n\nPhysics: Hydrodynamic stability, critical conditions\n\nFeatures:\n\nEigenvalue problem (EVP)\nGrowth rates and frequencies\nCritical modes visualization\n\nCode: See Eigenvalue Problems Tutorial for complete implementation\n\nTutorial: Eigenvalue Problems\n\n","title":"Rayleigh-Bénard Linear Stability (Intermediate)"},{"category":"section","location":"examples/gallery/#Plane-Poiseuille-Stability-(Advanced)","page":"Examples Gallery","text":"Stability of parallel shear flow.\n\nPhysics: Orr-Sommerfeld equation, Tollmien-Schlichting waves\n\nFeatures:\n\nOrr-Sommerfeld eigenvalue problem\nNeutral stability curves\nMost unstable modes\n\nCode: Example coming soon\n\n","title":"Plane Poiseuille Stability (Advanced)"},{"category":"section","location":"examples/gallery/#Wave-Propagation","page":"Examples Gallery","text":"","title":"Wave Propagation"},{"category":"section","location":"examples/gallery/#1D-Linear-Wave-Equation-(Beginner)","page":"Examples Gallery","text":"Simple wave propagation.\n\nPhysics: Hyperbolic PDE, wave dynamics\n\nFeatures:\n\nSecond-order time derivative\nExact solutions\nWave reflections\n\nCode: Example coming soon\n\n","title":"1D Linear Wave Equation (Beginner)"},{"category":"section","location":"examples/gallery/#2D-Acoustic-Waves-(Intermediate)","page":"Examples Gallery","text":"Sound wave propagation in 2D.\n\nPhysics: Acoustics, wave scattering\n\nFeatures:\n\nLinear acoustics\nPoint source\nRadiation patterns\n\nCode: Example coming soon\n\n","title":"2D Acoustic Waves (Intermediate)"},{"category":"section","location":"examples/gallery/#Atmospheric-Flows","page":"Examples Gallery","text":"","title":"Atmospheric Flows"},{"category":"section","location":"examples/gallery/#2D-Gravity-Waves-(Intermediate)","page":"Examples Gallery","text":"Internal gravity waves in stratified fluid.\n\nPhysics: Stratified flows, buoyancy oscillations\n\nFeatures:\n\nBoussinesq equations\nStable stratification\nWave propagation\n\nCode: Example coming soon\n\n","title":"2D Gravity Waves (Intermediate)"},{"category":"section","location":"examples/gallery/#2D-Quasi-Geostrophic-Flow-(Advanced)","page":"Examples Gallery","text":"Large-scale atmospheric/oceanic flows.\n\nPhysics: Geostrophic balance, Rossby waves\n\nFeatures:\n\nPotential vorticity equation\nBeta-plane approximation\nJet formation\n\nCode: Example coming soon\n\n","title":"2D Quasi-Geostrophic Flow (Advanced)"},{"category":"section","location":"examples/gallery/#Advanced-Examples","page":"Examples Gallery","text":"","title":"Advanced Examples"},{"category":"section","location":"examples/gallery/#MHD-(Magnetohydrodynamics)-(Advanced)","page":"Examples Gallery","text":"Fluid dynamics with magnetic fields.\n\nPhysics: Lorentz force, magnetic induction\n\nFeatures:\n\nCoupled fluid and magnetic fields\nAlfvén waves\nMagnetic energy\n\nCode: Example coming soon\n\n","title":"MHD (Magnetohydrodynamics) (Advanced)"},{"category":"section","location":"examples/gallery/#Double-Diffusive-Convection-(Advanced)","page":"Examples Gallery","text":"Convection driven by two diffusing species.\n\nPhysics: Salt-fingering, thermohaline circulation\n\nFeatures:\n\nTwo scalar fields (T and S)\nDifferent diffusivities\nLayering phenomena\n\nCode: Example coming soon\n\n","title":"Double-Diffusive Convection (Advanced)"},{"category":"section","location":"examples/gallery/#Stratified-Shear-Flow-(Advanced)","page":"Examples Gallery","text":"Combined shear and stratification.\n\nPhysics: Richardson number, mixing, turbulence\n\nFeatures:\n\nBackground shear and stratification\nKelvin-Helmholtz + gravity waves\nMixing efficiency\n\nCode: Example coming soon\n\n","title":"Stratified Shear Flow (Advanced)"},{"category":"section","location":"examples/gallery/#Example-Structure","page":"Examples Gallery","text":"Each example follows this structure:\n\n# 1. Load packages\nusing Tarang\n\n# 2. Parameters\nRa = 1e6\nPr = 1.0\n# ...\n\n# 3. Setup domain\ncoords = CartesianCoordinates(...)\ndist = Distributor(...)\nbases = (...)\ndomain = Domain(...)\n\n# 4. Create fields\nu = VectorField(...)\np = ScalarField(...)\n# ...\n\n# 5. Define problem\nproblem = IVP([...])\nadd_equation!(problem, \"...\")\nadd_parameters!(problem, ...)\n\n# 6. Boundary conditions\nadd_bc!(problem, \"...\")\n\n# 7. Initial conditions\n# ... initialize fields ...\n\n# 8. Solver\nsolver = InitialValueSolver(...)\n\n# 9. Analysis setup\ncfl = CFL(...)\nsnapshots = add_file_handler(...)\n\n# 10. Time loop\nrun!(solver;\n     stop_time=t_end,\n     log_interval=100,\n     callbacks=[...])\n\n","title":"Example Structure"},{"category":"section","location":"examples/gallery/#Running-Examples","page":"Examples Gallery","text":"","title":"Running Examples"},{"category":"section","location":"examples/gallery/#From-Repository","page":"Examples Gallery","text":"# Clone repository\ngit clone https://github.com/subhk/Tarang.jl.git\ncd Tarang.jl\n\n# Install dependencies\njulia --project -e 'using Pkg; Pkg.instantiate()'\n\n# Run an example (serial)\njulia --project=. examples/ivp/rayleigh_benard_2d.jl\n\n# Run with MPI — only the all-Fourier examples are distributable; a Chebyshev\n# axis must stay local, which rules out the Rayleigh-Bénard / channel examples.\nmpiexec -n 4 julia --project=. examples/ivp/forced_2d_turbulence.jl","title":"From Repository"},{"category":"section","location":"examples/gallery/#Modifying-Examples","page":"Examples Gallery","text":"Copy an example and modify:\n\ncp examples/ivp/rayleigh_benard_2d.jl my_simulation.jl\n# Edit my_simulation.jl\njulia --project=. my_simulation.jl","title":"Modifying Examples"},{"category":"section","location":"examples/gallery/#Creating-New-Examples","page":"Examples Gallery","text":"Start from the structure above and adapt the basis, fields, and equations for your problem.\n\n","title":"Creating New Examples"},{"category":"section","location":"examples/gallery/#Visualization","page":"Examples Gallery","text":"","title":"Visualization"},{"category":"section","location":"examples/gallery/#During-Simulation","page":"Examples Gallery","text":"Pass a callback to run! to plot periodically — an Int interval fires every N iterations:\n\nusing Plots\n\nrun!(solver; stop_time=t_end, cfl=cfl,\n     callbacks=[\n         on_interval(100) do s\n             ensure_layout!(T, :g)\n             T_grid = Array(get_grid_data(T))\n             heatmap(T_grid')\n             savefig(\"output/T_$(s.iteration).png\")\n         end\n     ])","title":"During Simulation"},{"category":"section","location":"examples/gallery/#Post-Processing","page":"Examples Gallery","text":"Snapshot files are NetCDF-4 files whose variables live in groups (vars, time, grids), so a plain NetCDF.ncread(file, \"T\") cannot find them — read them with group_ncread. The leading axis of a task variable is the write index:\n\nusing Plots, Tarang\n\nfile   = \"snapshots/snapshots_s1/snapshots_s1.nc\"\nT_data = group_ncread(file, \"vars\", \"T\")        # (write, x, z)\nt      = group_ncread(file, \"time\", \"sim_time\") # (write,)\n\n# Animate\nanim = @animate for n in 1:size(T_data, 1)\n    heatmap(T_data[n, :, :]', clims=(0, 1), title=\"t = $(t[n])\")\nend\ngif(anim, \"animation.gif\", fps=15)\n\nTarang.group_variable_names(file, \"vars\") lists the task names available in a file.\n\n","title":"Post-Processing"},{"category":"section","location":"examples/gallery/#Contributing-Examples","page":"Examples Gallery","text":"We welcome new examples! Guidelines:\n\nComplete and runnable: Include all necessary code\nWell-commented: Explain physics and numerics\nDocumented: Add to this gallery with description\nTested: Verify runs correctly with typical parameters\nRealistic: Use physically meaningful parameters\n\nSee Contributing Guide for details.\n\n","title":"Contributing Examples"},{"category":"section","location":"examples/gallery/#Next-Steps","page":"Examples Gallery","text":"Learn the basics: Getting Started\nDetailed tutorials: Tutorials\nUnderstand the API: API Reference\nAsk questions: GitHub Discussions","title":"Next Steps"},{"category":"section","location":"examples/gpu_fft_heuristics/#GPU-FFT-Heuristics","page":"GPU FFT Heuristics","text":"Tarang lets you control when a field uses GPU FFTs via set_gpu_fft_min_elements! and per-field set_gpu_fft_mode!.\n\nusing Tarang\n\nfield = ScalarField(dist, \"θ\", bases)\nset_gpu_fft_mode!(field, :cpu)   # force CPU\nset_gpu_fft_mode!(field, :gpu)   # force GPU\nset_gpu_fft_mode!(field, :auto)  # default heuristic\n\nset_gpu_fft_min_elements!(64_000)  # global cutoff for :auto fields","title":"GPU FFT Heuristics"},{"category":"section","location":"notebooks/taylor_green/#Notebook:-Taylor-Green-Vortex","page":"Notebook: Taylor-Green Vortex","text":"This notebook demonstrates the Taylor-Green vortex, a classic benchmark for spectral codes.","title":"Notebook: Taylor-Green Vortex"},{"category":"section","location":"notebooks/taylor_green/#Overview","page":"Notebook: Taylor-Green Vortex","text":"The two-dimensional Taylor-Green vortex is an exact solution of the incompressible Navier-Stokes equations: the nonlinear advection term is balanced identically by the pressure gradient, so the initial vortex array simply decays under viscosity without changing shape. That makes it the cleanest available check on a spectral code — the numerical answer can be compared against a closed-form solution at every instant.\n\nWe solve it in vorticity-streamfunction form, which is how Tarang handles incompressible flow on a periodic (all-Fourier) domain:\n\nbeginaligned\npartial_t zeta - nu Delta zeta = -mathbfu cdot nabla zeta \nDelta psi = zeta \nmathbfu = (-partial_y psi partial_x psi)\nendaligned\n\nThe streamfunction formulation enforces incompressibility by construction, so no pressure variable is needed.","title":"Overview"},{"category":"section","location":"notebooks/taylor_green/#Setup","page":"Notebook: Taylor-Green Vortex","text":"using Tarang\nusing Statistics\nusing Printf\nusing Plots\n\nTarang initializes MPI itself when it loads, so there is no MPI.Init()/MPI.Finalize() to call. The script below is unchanged whether you run it serially or under mpiexec.","title":"Setup"},{"category":"section","location":"notebooks/taylor_green/#Parameters","page":"Notebook: Taylor-Green Vortex","text":"Re = 100.0       # Reynolds number\nnu = 1.0 / Re    # Kinematic viscosity\nL = 2π           # Domain size\nN = 64           # Resolution","title":"Parameters"},{"category":"section","location":"notebooks/taylor_green/#Domain-(Doubly-Periodic)","page":"Notebook: Taylor-Green Vortex","text":"coords = CartesianCoordinates(\"x\", \"y\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# Fourier bases for periodic boundaries\nxbasis = RealFourier(coords[\"x\"]; size=N, bounds=(0.0, L), dealias=3/2)\nybasis = RealFourier(coords[\"y\"]; size=N, bounds=(0.0, L), dealias=3/2)\n\ndomain = Domain(dist, (xbasis, ybasis))\n\ndealias=3/2 is the padding factor for the 3/2 rule, and it is already the default for Fourier bases. (Note that dealias=2/3 would disable dealiasing — the factor is a padding ratio, not a truncation fraction.)","title":"Domain (Doubly Periodic)"},{"category":"section","location":"notebooks/taylor_green/#Fields","page":"Notebook: Taylor-Green Vortex","text":"ζ     = ScalarField(domain, \"ζ\")      # Vorticity\nψ     = ScalarField(domain, \"ψ\")      # Streamfunction\nu     = VectorField(domain, \"u\")      # Velocity\ntau_ψ = ScalarField(dist, \"tau_ψ\", (), Float64)   # Gauge for the Poisson solve\n\ntau_ψ pins the constant mode of ψ, which the Poisson equation leaves undetermined.","title":"Fields"},{"category":"section","location":"notebooks/taylor_green/#Problem-Definition","page":"Notebook: Taylor-Green Vortex","text":"problem = IVP([ζ, ψ, u, tau_ψ])\nadd_parameters!(problem, nu=nu)\n\nadd_equation!(problem, \"∂t(ζ) - nu*lap(ζ) = -u⋅∇(ζ)\")   # Vorticity transport\nadd_equation!(problem, \"lap(ψ) + tau_ψ - ζ = 0\")        # Poisson: Δψ = ζ\nadd_equation!(problem, \"u - skew(grad(ψ)) = 0\")         # u = (-∂y ψ, ∂x ψ)\n\nadd_bc!(problem, \"integ(ψ) = 0\")                        # Fix the constant in ψ\n\nParameters referenced from an equation string (nu here) must be registered with add_parameters! — a plain Julia global is not visible to the parser.","title":"Problem Definition"},{"category":"section","location":"notebooks/taylor_green/#Analytical-Solution","page":"Notebook: Taylor-Green Vortex","text":"The Taylor-Green initial condition and its exact time evolution:\n\nbeginaligned\npsi(xyt)   = phantom-cos(x)cos(y) e^-2nu t \nzeta(xyt)  = -2cos(x)cos(y) e^-2nu t \nu(xyt)      = phantom-cos(x)sin(y) e^-2nu t \nv(xyt)      = -sin(x)cos(y) e^-2nu t\nendaligned\n\nBecause zeta = -2psi, the advection term mathbfucdotnablazeta vanishes identically and the vortex decays at the pure viscous rate e^-2nu t. Energy and enstrophy, being quadratic, decay twice as fast:\n\nE(t) = tfrac14 L^2 e^-4nu t qquad\nZ(t) = tfrac12 L^2 e^-4nu t\n\n(These are domain integrals, matching what total_kinetic_energy and total_enstrophy return.)","title":"Analytical Solution"},{"category":"section","location":"notebooks/taylor_green/#Initial-Conditions","page":"Notebook: Taylor-Green Vortex","text":"local_grids returns each rank's slice of the grid, so this works serially and distributed. (set!(field, ::Function) builds a global mesh and is serial-only — do not use it here.)\n\nx, y = local_grids(dist, xbasis, ybasis)\n\nensure_layout!(ζ, :g)\nget_grid_data(ζ) .= -2 .* cos.(x) .* cos.(y')\nensure_layout!(ζ, :c)\n\nOnly ζ needs initializing: ψ and u are diagnosed from it by the constraint equations.","title":"Initial Conditions"},{"category":"section","location":"notebooks/taylor_green/#Diagnostic-Functions","page":"Notebook: Taylor-Green Vortex","text":"Tarang ships the diagnostics, so define only the analytical reference:\n\narea = L^2\nKE_exact(t)  = 0.25 * area * exp(-4 * nu * t)\nENS_exact(t) = 0.5  * area * exp(-4 * nu * t)\n\ntotal_kinetic_energy(u) and total_enstrophy(u) take a VectorField and return a Float64 (the domain integral). The un-prefixed kinetic_energy(u) and enstrophy(u) return the corresponding ScalarFields if you want the spatial distribution.","title":"Diagnostic Functions"},{"category":"section","location":"notebooks/taylor_green/#Solver","page":"Notebook: Taylor-Green Vortex","text":"solver = InitialValueSolver(problem, RK443(); dt=0.01)\n@show solver.rhs_plan.is_compiled   # true — the fast compiled RHS is in use\n\ncfl = CFL(solver; initial_dt=0.01, cadence=10, safety=0.5, max_dt=0.05)\nadd_velocity!(cfl, u)\n\nCFL takes the solver, not the problem, and add_velocity! takes the whole VectorField — there is no per-component form.","title":"Solver"},{"category":"section","location":"notebooks/taylor_green/#Simulation","page":"Notebook: Taylor-Green Vortex","text":"run! drives the loop: it applies the CFL controller, fires callbacks, and stops at stop_time.\n\nt_end = 10.0\n\ntimes        = Float64[]\nKE_numerical = Float64[]\nKE_reference = Float64[]\n\nfunction record!(s)\n    t = s.sim_time\n    push!(times, t)\n    push!(KE_numerical, total_kinetic_energy(u))\n    push!(KE_reference, KE_exact(t))\n    @printf(\"t = %6.3f   KE = %.8f   exact = %.8f\\n\", t, KE_numerical[end], KE_reference[end])\nend\n\nrun!(solver; stop_time=t_end, cfl=cfl, callbacks=[10 => record!], progress=false)\n\nOutput (abridged):\n\nt =  0.139   KE = 9.81481264   exact = 9.81481264\nt =  5.057   KE = 8.06222401   exact = 8.06222401\nt =  9.974   KE = 6.62258755   exact = 6.62258755","title":"Simulation"},{"category":"section","location":"notebooks/taylor_green/#Results","page":"Notebook: Taylor-Green Vortex","text":"","title":"Results"},{"category":"section","location":"notebooks/taylor_green/#Energy-Decay","page":"Notebook: Taylor-Green Vortex","text":"plot(times, KE_numerical, label=\"Numerical\", marker=:circle)\nplot!(times, KE_reference, label=\"Analytical\", linestyle=:dash)\nxlabel!(\"Time\")\nylabel!(\"Kinetic Energy\")\ntitle!(\"Taylor-Green Vortex Energy Decay (Re=$Re)\")","title":"Energy Decay"},{"category":"section","location":"notebooks/taylor_green/#Error","page":"Notebook: Taylor-Green Vortex","text":"relative_error = abs.(KE_numerical .- KE_reference) ./ KE_reference\n@printf(\"max relative KE error = %.3e\\n\", maximum(relative_error))   # 9.734e-12\n\nplot(times, relative_error,\n    xlabel=\"Time\",\n    ylabel=\"Relative Error\",\n    title=\"Energy Error\",\n    yscale=:log10,\n    legend=false\n)\n\nThe remaining error is time-integration error from RK443, not spatial error — the Fourier representation of the initial condition is exact. Checking the velocity field directly at t = 10:\n\nensure_layout!(u, :g)\nux, uy = u.components\nuxe =   cos.(x) .* sin.(y') .* exp(-2 * nu * solver.sim_time)\nuye = .-sin.(x) .* cos.(y') .* exp(-2 * nu * solver.sim_time)\n\n@printf(\"max|ux - exact| = %.3e\\n\", maximum(abs, get_grid_data(ux) .- uxe))   # 4.003e-12\n@printf(\"max|uy - exact| = %.3e\\n\", maximum(abs, get_grid_data(uy) .- uye))   # 4.003e-12\n@printf(\"enstrophy = %.6f  exact = %.6f\\n\",\n        total_enstrophy(u), ENS_exact(solver.sim_time))   # 13.219147  13.219147","title":"Error"},{"category":"section","location":"notebooks/taylor_green/#Vorticity-Visualization","page":"Notebook: Taylor-Green Vortex","text":"ensure_layout!(ζ, :g)\nheatmap(x, y, get_grid_data(ζ)',\n    xlabel=\"x\", ylabel=\"y\",\n    title=\"Vorticity at t = $(round(solver.sim_time, digits=2))\",\n    aspect_ratio=:equal, color=:balance\n)","title":"Vorticity Visualization"},{"category":"section","location":"notebooks/taylor_green/#Energy-Spectrum","page":"Notebook: Taylor-Green Vortex","text":"All axes are Fourier, so power_spectrum applies:\n\nps = power_spectrum(ζ)\n@printf(\"peak bin: k = %.2f\\n\", ps.k[argmax(ps.power)])   # 1.50 — the |k| = √2 mode","title":"Energy Spectrum"},{"category":"section","location":"notebooks/taylor_green/#Running-in-Parallel","page":"Notebook: Taylor-Green Vortex","text":"The script needs no changes to run distributed — the initial condition already uses local_grids, and total_kinetic_energy reduces across ranks:\n\nmpiexec -n 4 julia --project=. taylor_green.jl\n\nVerified at 1, 2 and 4 ranks: the kinetic energy agrees with the analytical value to roundoff (relative error ~5e-15 after 100 steps).","title":"Running in Parallel"},{"category":"section","location":"notebooks/taylor_green/#The-Three-Dimensional-Taylor-Green-Vortex","page":"Notebook: Taylor-Green Vortex","text":"The Re = 1600 three-dimensional Taylor-Green vortex is the classic transition-to-turbulence benchmark, and it is not what this notebook computes. Two things are worth being clear about:\n\nIt has no closed-form solution. The exact decay above is a two-dimensional result. In 3D the initial vortex is not a solution of Navier-Stokes; it stretches, breaks down, and the enstrophy peaks around t approx 9 before decaying. There is nothing to validate against except reference data.\nTarang cannot currently run it. A 3D incompressible flow needs a velocity-pressure formulation (div(u) = 0 plus ∇(p) in the momentum equation), and on a triply periodic all-Fourier domain Tarang builds no per-mode implicit solve for that constraint. The assembled global matrix is singular, the timestepper warns once (IMEX RK: system matrix is singular, falling back to explicit) and then drops every implicit term — pressure and viscosity alike — so the run proceeds and silently produces a wrong answer (the energy does not decay at all).\nPer-mode tau subproblems are only built when a coupled (Chebyshev/Jacobi) direction is present. So a 3D incompressible run needs at least one wall-bounded direction; see examples/ivp/rotating_rayleigh_benard_3d.jl for a working velocity-pressure setup.","title":"The Three-Dimensional Taylor-Green Vortex"},{"category":"section","location":"notebooks/taylor_green/#Exercises","page":"Notebook: Taylor-Green Vortex","text":"Convergence study: N = 16, 32, 64 — the spatial error is already at roundoff, so what you are really measuring is the RK443 temporal error. Vary max_dt instead.\nReynolds number scan: Re = 100, 400, 1600. The decay rate should track e^-4nu t exactly at every Re, since the solution is exact for all nu.\nPerturbed vortex: add noise with fill_random!(ζ, \"g\"; seed=42, scale=1e-2) on top of the Taylor-Green field and watch the exact-solution agreement break down.\nEnstrophy budget: compare total_enstrophy(u) against tfrac12L^2 e^-4nu t.","title":"Exercises"},{"category":"section","location":"notebooks/taylor_green/#References","page":"Notebook: Taylor-Green Vortex","text":"Taylor, G.I. & Green, A.E. (1937). Mechanism of the production of small eddies from large ones\nBrachet, M.E. et al. (1983). Small-scale structure of the Taylor-Green vortex","title":"References"},{"category":"section","location":"examples/fluid_dynamics/#Fluid-Dynamics-Examples","page":"Fluid Dynamics Examples","text":"Collection of fluid dynamics simulations with Tarang.jl. Each example is a complete, self-contained script that runs as it stands; the resolutions and step counts are kept small so they finish in seconds to a couple of minutes, and the numbers quoted after each one are what it prints. Raise N and the stop criterion for production runs.","title":"Fluid Dynamics Examples"},{"category":"section","location":"examples/fluid_dynamics/#Incompressible-Navier-Stokes","page":"Fluid Dynamics Examples","text":"Which formulation you write is decided by the geometry, because the incompressibility constraint is solved through the tau method on a coupled (non-periodic) axis:\n\ngeometry formulation\nwalls in one direction, periodic in the others primitive variables u, p + Chebyshev tau method\nfully periodic vorticity–streamfunction (2D)\n\nTwo configurations that look natural but do not work:\n\nTwo bounded axes (Chebyshev × Chebyshev — the classic lid-driven cavity) cannot be built. Tarang couples exactly one non-periodic axis; the remaining axes must be separable (Fourier). Even with correct tau variables and lifts, the solve fails with DimensionMismatch: second dimension of A, 12, does not match the first dimension of B, 144.\nPrimitive variables in a fully periodic box. div(u) + tau_p = 0 is accepted, but with no coupled axis the constraint is never enforced: starting from a random (non-solenoidal) field, max|div u| was 2.918 before and 2.920 after 20 RK222() steps, and SBDF2() throws SingularException. Use the vorticity–streamfunction form in a periodic box, where u = skew(∇ψ) is divergence-free by construction.","title":"Incompressible Navier-Stokes"},{"category":"section","location":"examples/fluid_dynamics/#2D-Channel-Flow-(Poiseuille)","page":"Fluid Dynamics Examples","text":"Wall-bounded flow driven by a constant body force: Fourier in the streamwise direction, Chebyshev between the walls, no-slip at both walls.\n\nusing Tarang, Printf\n\nLx, Lz = 4.0, 1.0\nNx, Nz = 16, 16\nnu     = 1.0          # kinematic viscosity\nfx     = 1.0          # constant streamwise body force (the pressure gradient)\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\n\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nzbasis = ChebyshevT(coords[\"z\"];  size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\np = ScalarField(domain, \"p\")\nu = VectorField(domain, \"u\")\n\n# tau variables carry the Fourier basis: one tau per streamwise mode\ntau_p  = ScalarField(dist, \"tau_p\", (), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis,), Float64)\n\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u     = grad(u) + ez * τ_lift(tau_u1)      # first-order reduction\n\nproblem = IVP([p, u, tau_p, tau_u1, tau_u2])\nadd_parameters!(problem, nu=nu, fx=fx, ex=ex, grad_u=grad_u, τ_lift=τ_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) + τ_lift(tau_u2) = -u⋅∇(u) + fx*ex\")\n\nadd_bc!(problem, \"u(z=0) = 0\")        # no-slip walls\nadd_bc!(problem, \"u(z=$Lz) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")      # pressure gauge\n\nsolver = InitialValueSolver(problem, RK222(); dt=2e-2)\n\ncfl = CFL(solver; initial_dt=2e-2, cadence=10, safety=0.5, max_dt=2e-2)\nadd_velocity!(cfl, u)\n\nrun!(solver; stop_iteration=250, cfl=cfl, progress=false)   # t = 5 viscous times\n\n# Steady state is the analytic Poiseuille parabola ux(z) = fx z (Lz - z) / (2ν)\nux = u.components[1]\nensure_layout!(ux, :g)\nx, z    = local_grids(dist, xbasis, zbasis)\nprofile = get_grid_data(ux)[1, :]                 # ux is x-independent\nexact   = @. fx * z * (Lz - z) / (2 * nu)\n@printf(\"t = %.2f   max|ux| = %.6f (exact %.6f)   rel. error = %.2e\\n\",\n        solver.sim_time, maximum(profile), fx * Lz^2 / (8 * nu),\n        maximum(abs, profile .- exact) / maximum(exact))\n\nAfter 250 steps (t = 5, five viscous times) the flow has converged to the analytic parabola: the profile matches fx z (Lz - z) / 2ν to a relative error of 6.3e-15. The printed max|ux| = 0.123634 is the largest value on the grid — the Chebyshev nodes do not include the centreline where the exact peak fx Lz²/8ν = 0.125 sits.\n\nNote that the boundary-condition strings are parsed by Tarang, not Julia: they cannot see Julia globals, so Lz is interpolated into the string by Julia before add_bc! sees it. Boundary conditions must go through add_bc! — declaring them with add_equation! silently fails to enforce anything but a constant.","title":"2D Channel Flow (Poiseuille)"},{"category":"section","location":"examples/fluid_dynamics/#Kelvin-Helmholtz-Instability","page":"Fluid Dynamics Examples","text":"Shear-layer instability in a doubly periodic domain, in vorticity–streamfunction form. A double shear layer keeps the base state periodic.\n\nusing Tarang, Printf\n\nN      = 64\nδ, A   = 0.05, 1e-2      # shear-layer thickness, perturbation amplitude\nnu     = 1e-4\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=N, bounds=(0.0, 1.0), dealias=3/2)\nzbasis = RealFourier(coords[\"z\"]; size=N, bounds=(0.0, 1.0), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\nζ     = ScalarField(domain, \"zeta\")     # vorticity\nψ     = ScalarField(domain, \"psi\")      # streamfunction\nu     = VectorField(domain, \"u\")        # velocity, divergence-free by construction\ntau_ψ = ScalarField(dist, \"tau_psi\", (), Float64)\n\nproblem = IVP([ζ, ψ, u, tau_ψ])\nadd_parameters!(problem, nu=nu)\nadd_equation!(problem, \"∂t(zeta) - nu*Δ(zeta) = -u⋅∇(zeta)\")   # vorticity transport\nadd_equation!(problem, \"Δ(psi) + tau_psi - zeta = 0\")          # Δψ = ζ\nadd_equation!(problem, \"u - skew(grad(psi)) = 0\")              # u = ∇⊥ψ\nadd_bc!(problem, \"integ(psi) = 0\")                             # gauge\n\nsolver = InitialValueSolver(problem, RK222(); dt=2e-3)\n\n# Double shear layer (periodic): ux = tanh((z-1/4)/δ) below, tanh((3/4-z)/δ) above,\n# so ζ = -∂z(ux); seeded with a k=1 perturbation.\nx, z = local_grids(dist, xbasis, zbasis)\nzz   = reshape(z, 1, :)\nensure_layout!(ζ, :g)\nget_grid_data(ζ) .= @. ifelse(zz < 0.5,\n                              -(1 - tanh((zz - 0.25) / δ)^2) / δ,\n                               (1 - tanh((0.75 - zz) / δ)^2) / δ) +\n                       A * sin(2π * x) * cos(2π * zz)\nensure_layout!(ζ, :c)\n\nuz = u.components[2]\nuz_max() = (ensure_layout!(uz, :g); maximum(abs, get_grid_data(uz)))\n\nrun!(solver; stop_iteration=1, progress=false)      # one step populates u from ψ\nuz0 = uz_max()\nrun!(solver; stop_iteration=500, progress=false)\nensure_layout!(ζ, :g)\n@printf(\"t = %.2f   max|uz|: %.2e -> %.2e (×%.0f)   max|zeta| = %.2f   E = %.4f\\n\",\n        solver.sim_time, uz0, uz_max(), uz_max() / uz0,\n        maximum(abs, get_grid_data(ζ)), total_kinetic_energy(u))\n\nψ and u are algebraic (no ∂t), so they are only filled once the solver takes a step — hence the one-step warm-up before reading the initial max|uz|. Over t = 1 the perturbation grows from 7.96e-4 to 6.64e-3 (a factor of 8): the shear layer is unstable, as expected.","title":"Kelvin-Helmholtz Instability"},{"category":"section","location":"examples/fluid_dynamics/#Thermal-Convection","page":"Fluid Dynamics Examples","text":"Both examples below are Rayleigh-Bénard-type: Fourier in x, Chebyshev in z, tau variables on the Fourier basis, first-order reduction (grad_X = ∇X + ẑ·lift(τ)) for the Chebyshev tau method.","title":"Thermal Convection"},{"category":"section","location":"examples/fluid_dynamics/#Double-Diffusive-Convection","page":"Fluid Dynamics Examples","text":"Convection driven by two scalars with very different diffusivities (temperature and salinity).\n\nusing Tarang, Printf\n\nLx, Lz = 2.0, 1.0\nNx, Nz = 32, 24\nPr     = 7.0      # Prandtl number\ntau_d  = 0.01     # diffusivity ratio κ_S/κ_T\nRa_T   = 1e5      # thermal Rayleigh number\nRa_S   = 1e4      # solutal Rayleigh number\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nzbasis = ChebyshevT(coords[\"z\"];  size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\np = ScalarField(domain, \"p\")\nT = ScalarField(domain, \"T\")      # temperature\nS = ScalarField(domain, \"S\")      # salinity\nu = VectorField(domain, \"u\")\n\ntau_p  = ScalarField(dist, \"tau_p\", (), Float64)\ntau_T1 = ScalarField(dist, \"tau_T1\", (xbasis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (xbasis,), Float64)\ntau_S1 = ScalarField(dist, \"tau_S1\", (xbasis,), Float64)\ntau_S2 = ScalarField(dist, \"tau_S2\", (xbasis,), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis,), Float64)\n\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_T = grad(T) + ez * τ_lift(tau_T1)\ngrad_S = grad(S) + ez * τ_lift(tau_S1)\n\nproblem = IVP([p, T, S, u, tau_p, tau_T1, tau_T2, tau_S1, tau_S2, tau_u1, tau_u2])\nadd_parameters!(problem, Pr=Pr, taud=tau_d, buoyT=Ra_T*Pr, buoyS=Ra_S*Pr, ez=ez,\n                grad_u=grad_u, grad_T=grad_T, grad_S=grad_S, τ_lift=τ_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(S) - taud*div(grad_S) + τ_lift(tau_S2) = -u⋅∇(S)\")\nadd_equation!(problem,\n    \"∂t(u) - Pr*div(grad_u) + ∇(p) - buoyT*T*ez + buoyS*S*ez + τ_lift(tau_u2) = -u⋅∇(u)\")\n\nadd_bc!(problem, \"T(z=0) = 1\"); add_bc!(problem, \"T(z=$Lz) = 0\")\nadd_bc!(problem, \"S(z=0) = 1\"); add_bc!(problem, \"S(z=$Lz) = 0\")\nadd_bc!(problem, \"u(z=0) = 0\"); add_bc!(problem, \"u(z=$Lz) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)\n\n# Linear conduction profiles + damped noise on both scalars\nx, z = local_grids(dist, xbasis, zbasis)\nfor (field, seed) in ((T, 42), (S, 43))\n    fill_random!(field, \"g\"; seed=seed, distribution=\"normal\", scale=1e-3)\n    get_grid_data(field) .*= z' .* (Lz .- z')\n    get_grid_data(field) .+= 1.0 .- z' ./ Lz\n    ensure_layout!(field, :c)\nend\n\nrun!(solver; stop_iteration=100, progress=false)\n\nuz = u.components[2]\nensure_layout!(T, :g); ensure_layout!(S, :g); ensure_layout!(uz, :g)\nTg, Sg = get_grid_data(T), get_grid_data(S)\n@printf(\"t = %.4f   max|T| = %.4f   max|S| = %.4f   max|uz| = %.3e\\n\",\n        solver.sim_time, maximum(abs, Tg), maximum(abs, Sg), maximum(abs, get_grid_data(uz)))\n@printf(\"BC residuals: |T(z=0)-1| = %.1e   |S(z=Lz)| = %.1e\\n\",\n        maximum(abs, Tg[:, 1] .- 1), maximum(abs, Sg[:, end]))\n\nAfter 100 steps the scalars stay in [0, 1], convection is starting (max|uz| = 2.4), and the boundary conditions hold to machine precision (|T(z=0) − 1| ≈ 1e-15, |S(z=Lz)| = 1.2e-16). The buoyancy signs are what set the regime: -buoyT*T*ez + buoyS*S*ez makes temperature destabilizing and salinity stabilizing.","title":"Double-Diffusive Convection"},{"category":"section","location":"examples/fluid_dynamics/#Rotating-Convection","page":"Fluid Dynamics Examples","text":"Rotation about the vertical axis couples the in-plane flow to an out-of-plane velocity, so the 2D plane needs a third velocity component: a scalar field v. The Coriolis terms +f v x̂ and −f ux are the two halves of −2Ω × u.\n\nusing Tarang, Printf\n\nLx, Lz = 2.0, 1.0\nNx, Nz = 32, 24\nPr     = 1.0\nRa     = 2e4\nTa     = 1e4                 # Taylor number\nfcor   = sqrt(Ta) * Pr       # Coriolis coefficient in these units\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nzbasis = ChebyshevT(coords[\"z\"];  size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\np = ScalarField(domain, \"p\")\nT = ScalarField(domain, \"T\")\nv = ScalarField(domain, \"v\")      # out-of-plane (spanwise) velocity\nu = VectorField(domain, \"u\")      # in-plane velocity (ux, uz)\n\ntau_p  = ScalarField(dist, \"tau_p\", (), Float64)\ntau_T1 = ScalarField(dist, \"tau_T1\", (xbasis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (xbasis,), Float64)\ntau_v1 = ScalarField(dist, \"tau_v1\", (xbasis,), Float64)\ntau_v2 = ScalarField(dist, \"tau_v2\", (xbasis,), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis,), Float64)\n\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_T = grad(T) + ez * τ_lift(tau_T1)\ngrad_v = grad(v) + ez * τ_lift(tau_v1)\nux     = u.components[1]\n\nproblem = IVP([p, T, v, u, tau_p, tau_T1, tau_T2, tau_v1, tau_v2, tau_u1, tau_u2])\nadd_parameters!(problem, Pr=Pr, buoy=Ra*Pr, fcor=fcor, ex=ex, ez=ez, ux=ux,\n                grad_u=grad_u, grad_T=grad_T, grad_v=grad_v, τ_lift=τ_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\n# Spanwise momentum: Coriolis couples v to ux\nadd_equation!(problem, \"∂t(v) - Pr*div(grad_v) + τ_lift(tau_v2) = -u⋅∇(v) - fcor*ux\")\n# In-plane momentum: buoyancy along ẑ, Coriolis along x̂\nadd_equation!(problem,\n    \"∂t(u) - Pr*div(grad_u) + ∇(p) - buoy*T*ez + τ_lift(tau_u2) = -u⋅∇(u) + fcor*v*ex\")\n\nadd_bc!(problem, \"T(z=0) = 1\"); add_bc!(problem, \"T(z=$Lz) = 0\")\nadd_bc!(problem, \"u(z=0) = 0\"); add_bc!(problem, \"u(z=$Lz) = 0\")\nadd_bc!(problem, \"v(z=0) = 0\"); add_bc!(problem, \"v(z=$Lz) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)\n\nx, z = local_grids(dist, xbasis, zbasis)\nfill_random!(T, \"g\"; seed=42, distribution=\"normal\", scale=1e-3)\nget_grid_data(T) .*= z' .* (Lz .- z')\nget_grid_data(T) .+= 1.0 .- z' ./ Lz\nensure_layout!(T, :c)\n\nrun!(solver; stop_iteration=100, progress=false)\n\nuz = u.components[2]\nensure_layout!(v, :g); ensure_layout!(uz, :g)\n@printf(\"t = %.4f   max|uz| = %.3e   max|v| = %.3e\\n\",\n        solver.sim_time, maximum(abs, get_grid_data(uz)), maximum(abs, get_grid_data(v)))\n\nAfter 100 steps: max|uz| = 9.8e-3 and max|v| = 2.5e-3 — the spanwise velocity is generated entirely by rotation (it is zero at Ta = 0).\n\nThere is no parseable cross-product in equation strings, so write the Coriolis force with unit vectors and components, as above. A vector times a scalar field (fcor*v*ex) is fine on either side of the equation.","title":"Rotating Convection"},{"category":"section","location":"examples/fluid_dynamics/#Stratified-Flows","page":"Fluid Dynamics Examples","text":"","title":"Stratified Flows"},{"category":"section","location":"examples/fluid_dynamics/#Internal-Gravity-Waves","page":"Fluid Dynamics Examples","text":"Boussinesq flow with a stable background stratification, doubly periodic, in vorticity–streamfunction form. With u = ∇⊥ψ the vertical velocity is uz = ∂x(ψ), which is how the stratification term is written.\n\nusing Tarang, Printf\n\nN     = 32\nN2    = 1.0        # Brunt-Väisälä frequency squared\nnu    = 1e-6\nkappa = 1e-6\nA     = 1e-3       # wave amplitude (linear regime)\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=N, bounds=(0.0, 2π), dealias=3/2)\nzbasis = RealFourier(coords[\"z\"]; size=N, bounds=(0.0, 2π), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\nζ     = ScalarField(domain, \"zeta\")\nψ     = ScalarField(domain, \"psi\")\nb     = ScalarField(domain, \"b\")        # buoyancy\nu     = VectorField(domain, \"u\")\ntau_ψ = ScalarField(dist, \"tau_psi\", (), Float64)\n\nproblem = IVP([ζ, ψ, b, u, tau_ψ])\nadd_parameters!(problem, nu=nu, kappa=kappa, N2=N2)\n\nadd_equation!(problem, \"∂t(zeta) - nu*Δ(zeta) - ∂x(b) = -u⋅∇(zeta)\")\nadd_equation!(problem, \"∂t(b) - kappa*Δ(b) + N2*∂x(psi) = -u⋅∇(b)\")\nadd_equation!(problem, \"Δ(psi) + tau_psi - zeta = 0\")\nadd_equation!(problem, \"u - skew(grad(psi)) = 0\")\nadd_bc!(problem, \"integ(psi) = 0\")\n\n# Plane wave with kx = kz = 1: theory gives ω = N*kx/|k| = 1/√2\nkx, kz = 1.0, 1.0\nω      = sqrt(N2) * kx / hypot(kx, kz)\nperiod = 2π / ω\n\nsolver = InitialValueSolver(problem, RK222(); dt=period / 2000)\n\nx, z = local_grids(dist, xbasis, zbasis)\nensure_layout!(b, :g)\nget_grid_data(b) .= A .* cos.(kx .* x .+ kz .* reshape(z, 1, :))\nensure_layout!(b, :c)\nb0 = copy(get_grid_data(b))\n\n# Starting from rest, b(t) = b(0)*cos(ωt): the correlation with b(0) traces the wave\ncorr() = (ensure_layout!(b, :g); sum(get_grid_data(b) .* b0) / sum(b0 .^ 2))\n\nfor (frac, steps) in ((0.5, 1000), (1.0, 2000))\n    run!(solver; stop_iteration=steps, progress=false)\n    @printf(\"t = %.3f (%.2f period)  <b(t),b(0)>/<b(0),b(0)> = %+.4f  (cos(ωt) = %+.4f)\\n\",\n            solver.sim_time, frac, corr(), cos(ω * solver.sim_time))\nend\n\nThis reproduces the internal-wave dispersion relation ω = N kx/|k| exactly: the buoyancy field anti-correlates with its initial state after half a period (−1.0000) and returns to it after a full period (+1.0000, theoretical period 8.8858).","title":"Internal Gravity Waves"},{"category":"section","location":"examples/fluid_dynamics/#Turbulence","page":"Fluid Dynamics Examples","text":"","title":"Turbulence"},{"category":"section","location":"examples/fluid_dynamics/#Decaying-2D-Turbulence","page":"Fluid Dynamics Examples","text":"Band-limited random vorticity in a doubly periodic box, then let it evolve: the enstrophy cascades to small scales and is dissipated, while the energy stays at large scales.\n\nusing Tarang, Printf, Random\nusing Statistics: mean\n\nN  = 64\nν  = 1e-3\ndt = 2e-3\n\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=N, bounds=(0.0, 2π), dealias=3/2)\nybasis = RealFourier(coords[\"y\"]; size=N, bounds=(0.0, 2π), dealias=3/2)\ndomain = Domain(dist, (xbasis, ybasis))\n\nζ     = ScalarField(domain, \"zeta\")\nψ     = ScalarField(domain, \"psi\")\nu     = VectorField(domain, \"u\")\ntau_ψ = ScalarField(dist, \"tau_psi\", (), Float64)\n\nproblem = IVP([ζ, ψ, u, tau_ψ])\nadd_parameters!(problem, nu=ν)\nadd_equation!(problem, \"∂t(zeta) - nu*Δ(zeta) = -u⋅∇(zeta)\")\nadd_equation!(problem, \"Δ(psi) + tau_psi - zeta = 0\")\nadd_equation!(problem, \"u - skew(grad(psi)) = 0\")\nadd_bc!(problem, \"integ(psi) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=dt)\n\n# Band-limited random vorticity: energy initially in 4 ≤ |k| ≤ 8\nRandom.seed!(42)\nx, y = local_grids(dist, xbasis, ybasis)\nζ0 = zeros(length(x), length(y))\nfor kx in 1:10, ky in 1:10\n    4 <= hypot(kx, ky) <= 8 || continue\n    a, φ = randn(), 2π * rand()\n    @. ζ0 += a * sin(kx * x + φ) * cos(ky * y' + φ)\nend\nζ0 .*= 10.0 / maximum(abs, ζ0)\nensure_layout!(ζ, :g)\nget_grid_data(ζ) .= ζ0\nensure_layout!(ζ, :c)\n\nenstrophy_of(ζ) = (ensure_layout!(ζ, :g); 0.5 * mean(get_grid_data(ζ) .^ 2))\n\n# ψ and u are algebraic: one step of the solver populates them from ζ\nrun!(solver; stop_iteration=1, progress=false)\nE0, Z0 = total_kinetic_energy(u), enstrophy_of(ζ)\n\nrun!(solver; stop_iteration=500, progress=false)\n\nE1, Z1 = total_kinetic_energy(u), enstrophy_of(ζ)\n@printf(\"t = %.2f   E: %.3f -> %.3f (%.0f%%)   Z: %.2f -> %.2f (%.0f%%)\\n\",\n        solver.sim_time, E0, E1, 100 * E1 / E0, Z0, Z1, 100 * Z1 / Z0)\n\nspec = energy_spectrum(u)                       # NamedTuple (k, power, bin_counts, bin_edges)\nkpeak = spec.k[argmax(spec.power)]\n@printf(\"energy spectrum peaks at k = %.1f\\n\", kpeak)\n\nOver t = 1 at ν = 1e-3: E falls from 5.225 to 4.902 and Z from 4.26 to 3.95, and the energy spectrum still peaks at k = 5.5, inside the band it was seeded in. Push N, lower ν and integrate for longer to see the inverse cascade move that peak to smaller k.\n\nTwo things are deliberately not shown here. 3D homogeneous isotropic turbulence has no supported formulation today: a triply periodic box has no pressure projection (see above), and the vorticity–streamfunction trick is 2D-only. Stochastically forced turbulence via StochasticForcing builds and steps, but the energy it actually injects is many orders of magnitude below its energy_injection_rate setting (at N = 64, ε = 1, k_f = 8, the kinetic energy after t = 1 was 1.4e-6, not ≈ 1), so the knob does not currently mean what it says.","title":"Decaying 2D Turbulence"},{"category":"section","location":"examples/fluid_dynamics/#Tips","page":"Fluid Dynamics Examples","text":"","title":"Tips"},{"category":"section","location":"examples/fluid_dynamics/#Resolution-Requirements","page":"Fluid Dynamics Examples","text":"Problem Typical Resolution\nLaminar 64-128\nTransitional 128-256\nTurbulent (moderate Re) 256-512\nTurbulent (high Re) 512-2048+","title":"Resolution Requirements"},{"category":"section","location":"examples/fluid_dynamics/#Timestepping","page":"Fluid Dynamics Examples","text":"Use RK222 or RK443 for advection-dominated flows.\nUse SBDF2/SBDF3 for diffusion-dominated or stiff problems.\nDrive the loop with a CFL controller (as in the channel example above): construct it from the solver, register a VectorField velocity, and hand it to run!.\n\ncfl = CFL(solver; initial_dt=1e-3, cadence=10, safety=0.5, max_dt=1e-2)\nadd_velocity!(cfl, u)                 # VectorField only\nrun!(solver; stop_iteration=1000, cfl=cfl)","title":"Timestepping"},{"category":"section","location":"examples/fluid_dynamics/#Diagnostics","page":"Fluid Dynamics Examples","text":"total_kinetic_energy and total_enstrophy return domain integrals (Float64); kinetic_energy and enstrophy return the corresponding ScalarFields. energy_spectrum needs every axis to be Fourier. global_max is MPI-reduced, so it returns the same value on every rank.\n\nE    = total_kinetic_energy(u)      # Float64: domain integral of ½|u|²\nZ    = total_enstrophy(u)           # Float64: domain integral of ½ω²  (2D only)\nspec = energy_spectrum(u)           # NamedTuple: (k, power, bin_counts, bin_edges)\n\nux = u.components[1]\nensure_layout!(ux, :g)\numax = global_max(dist, abs.(get_grid_data(ux)))   # MPI-reduced: same on every rank\n\n@printf(\"E = %.4e   Z = %.4e   max|ux| = %.4f   spectrum peak at k = %.1f\\n\",\n        E, Z, umax, spec.k[argmax(spec.power)])","title":"Diagnostics"},{"category":"section","location":"examples/fluid_dynamics/#Running-in-parallel","page":"Fluid Dynamics Examples","text":"The fully periodic examples (Kelvin-Helmholtz, internal waves, decaying turbulence) run under MPI unchanged — they use local_grids for initial conditions, which returns each rank's own slice:\n\nmpiexec -n 2 julia --project=. kelvin_helmholtz.jl\n\nThe Kelvin-Helmholtz script above gives bit-identical output at 1 and 2 ranks (max|zeta| = 18.62, E = 0.3948).\n\nThe wall-bounded examples (channel, double-diffusive, rotating convection) are serial: their right-hand sides contain -u⋅∇(u), a Chebyshev derivative in the explicit RHS, which a distributed run rejects (each rank owns only part of the Chebyshev axis). MPI also requires the Chebyshev axis to come first in the domain. See Running with MPI.","title":"Running in parallel"},{"category":"section","location":"examples/fluid_dynamics/#See-Also","page":"Fluid Dynamics Examples","text":"Rayleigh-Bénard Tutorial\nHeat Transfer Examples\nExample Gallery","title":"See Also"},{"category":"section","location":"pages/fields/#Fields","page":"Fields","text":"Fields represent physical quantities distributed across the computational domain.\n\nEvery snippet on this page runs against the small 2D domain set up here:\n\nusing Tarang\nusing MPI          # for MPI.MAX / MPI.SUM in the reduction snippets\n\ncoords  = CartesianCoordinates(\"x\", \"z\")\ndist    = Distributor(coords; dtype=Float64, device=CPU())\nx_basis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\nz_basis = RealFourier(coords[\"z\"]; size=8,  bounds=(0.0, 2π))\nbases   = (x_basis, z_basis)\ndomain  = Domain(dist, bases)","title":"Fields"},{"category":"section","location":"pages/fields/#Field-Types","page":"Fields","text":"","title":"Field Types"},{"category":"section","location":"pages/fields/#ScalarField","page":"Fields","text":"Single-component fields like temperature or pressure.\n\n# Create scalar field\nT = ScalarField(dist, \"temperature\", (x_basis, z_basis), Float64)\np = ScalarField(dist, \"pressure\", (x_basis, z_basis), Float64)","title":"ScalarField"},{"category":"section","location":"pages/fields/#VectorField","page":"Fields","text":"Multi-component fields like velocity. One component per coordinate of the coordinate system.\n\n# Create velocity field\nu = VectorField(dist, coords, \"u\", (x_basis, z_basis), Float64)\n\n# Access components\nux = u.components[1]  # x-component, named \"u_x\"\nuz = u.components[2]  # z-component, named \"u_z\"","title":"VectorField"},{"category":"section","location":"pages/fields/#TensorField","page":"Fields","text":"Rank-2 tensor fields like stress tensors. The components are a dense dim × dim matrix of ScalarFields named after the coordinate pairs (tau_xx, tau_xz, …); there is no symmetric-storage variant.\n\ntau = TensorField(dist, coords, \"tau\", bases, Float64)\n\n# Convenience form: uses the distributor's coordinate system\ngrad_u = TensorField(dist, \"grad_u\", bases)\n\ntau.components[1, 2]        # the xz component (a ScalarField named \"tau_xz\")","title":"TensorField"},{"category":"section","location":"pages/fields/#Field-Properties","page":"Fields","text":"T.name           # String: field name\nT.dist           # Distributor\nT.bases          # Tuple of spectral bases\nT.dtype          # Data type (Float64, etc.)\nT.current_layout # :g (grid) or :c (coefficient)\nget_grid_data(T)    # Grid space data (when in :g layout)\nget_coeff_data(T)   # Coefficient data (when in :c layout)\n\nBasis metadata lives under basis.meta (x_basis.meta.size, x_basis.meta.bounds, x_basis.meta.dealias), and the domain knows both shapes:\n\nglobal_shape(domain, :g)   # (16, 8)  — full grid\nglobal_shape(domain, :c)   # (9, 8)   — coefficients (rfft halves the first axis)\nlocal_shape(domain, :g)    # this rank's slab","title":"Field Properties"},{"category":"section","location":"pages/fields/#Data-Access","page":"Fields","text":"","title":"Data Access"},{"category":"section","location":"pages/fields/#Grid-Space","page":"Fields","text":"# Ensure field is in grid space\nensure_layout!(T, :g)\n\n# Access data\ndata = get_grid_data(T)\n\n# Modify values (indices are LOCAL to this rank under MPI)\ndata[3, 2] = 1.0\n\n# Broadcast over the local grid coordinates\nx, z = local_grids(dist, x_basis, z_basis)\ndata .= sin.(x) .* cos.(z')","title":"Grid Space"},{"category":"section","location":"pages/fields/#Coefficient-Space","page":"Fields","text":"# Ensure field is in coefficient space\nensure_layout!(T, :c)\n\n# Access spectral coefficients\ncoeffs = get_coeff_data(T)\n\n# Set specific modes\ncoeffs[1, 1] = 0.0  # Zero mean","title":"Coefficient Space"},{"category":"section","location":"pages/fields/#Initialization","page":"Fields","text":"","title":"Initialization"},{"category":"section","location":"pages/fields/#Constant-Value","page":"Fields","text":"ensure_layout!(p, :g)\nget_grid_data(p) .= 1.0\n\nset!(field, ::Number) does the same thing and works distributed.","title":"Constant Value"},{"category":"section","location":"pages/fields/#From-Function","page":"Fields","text":"local_grids(dist, bases...) returns each rank's local slice of every axis, so broadcasting over it is correct in serial and under MPI alike:\n\nfunction initialize_field!(field, f)\n    ensure_layout!(field, :g)\n    x, z = local_grids(field.dist, field.bases...)\n    get_grid_data(field) .= f.(x, z')\n    return field\nend\n\n# Usage\ninitialize_field!(T, (x, z) -> sin(x) * cos(z))\n\nset!(field, ::Function) is a shorthand for the same thing, but it is serial only — it builds the global meshgrid and throws a DimensionMismatch when the field is distributed. Use the local_grids form above in code that must run under MPI.","title":"From Function"},{"category":"section","location":"pages/fields/#Random-Perturbations","page":"Fields","text":"fill_random! fills the field in place. With reproducible=true (the default) the same seed produces the same global field regardless of how many ranks are used.\n\n# Small-amplitude noise on top of an existing profile\nnoise = copy(T)\nfill_random!(noise, \"g\"; seed=42, distribution=\"normal\", scale=0.01)\n\nensure_layout!(T, :g)\nensure_layout!(noise, :g)\nget_grid_data(T) .+= get_grid_data(noise)\n\ndistribution may be \"normal\", \"standard_normal\", or \"uniform\"; fill_random! also accepts a VectorField, filling each component with a different seed.","title":"Random Perturbations"},{"category":"section","location":"pages/fields/#Field-Operations","page":"Fields","text":"","title":"Field Operations"},{"category":"section","location":"pages/fields/#Copy","page":"Fields","text":"# Create copy\nT2 = copy(T)\n\n# Deep copy\nT3 = deepcopy(T)","title":"Copy"},{"category":"section","location":"pages/fields/#Arithmetic","page":"Fields","text":"# In-place operations (preferred); both fields must be in the same layout\nensure_layout!(T, :g)\nensure_layout!(p, :g)\n\nget_grid_data(T) .+= get_grid_data(p)\nget_grid_data(T) .*= 2.0\n\n# Scaling by a global norm — compute the norm with a reduction (see below),\n# never with a bare maximum() over distributed data\nget_grid_data(T) ./= global_max(T)","title":"Arithmetic"},{"category":"section","location":"pages/fields/#Reductions","page":"Fields","text":"Under MPI get_grid_data(field) is a PencilArray, and sum/maximum/minimum on a PencilArray are already collective — they return the global value. Wrapping one in another reduction double-reduces (the \"global sum\" comes out nprocs × too large). Reduce parent(...) — the rank-local storage — and then reduce once:\n\n# Local operations: parent() keeps the reduction local\nlocal_max = maximum(abs, parent(get_grid_data(T)))\nlocal_sum = sum(parent(get_grid_data(T)))\n\n# Global MPI reductions\nreducer = GlobalArrayReducer(dist.comm)\nglobal_maximum = reduce_scalar(reducer, local_max, MPI.MAX)\nglobal_total   = reduce_scalar(reducer, local_sum, MPI.SUM)\n\nThe ready-made field helpers do exactly this and are usually what you want:\n\nglobal_max(T)    # Float64, same value on every rank\nglobal_min(T)\nglobal_sum(T)\nglobal_mean(T)\nintegrate(T)     # quadrature-weighted domain integral","title":"Reductions"},{"category":"section","location":"pages/fields/#Layout-Management","page":"Fields","text":"","title":"Layout Management"},{"category":"section","location":"pages/fields/#Transforms","page":"Fields","text":"# Transform to grid space\nensure_layout!(T, :g)\n\n# Transform to coefficient space\nensure_layout!(T, :c)\n\n# Check current layout\nif T.current_layout == :g\n    println(\"Field is in grid space\")\nend","title":"Transforms"},{"category":"section","location":"pages/fields/#Efficient-Transform-Usage","page":"Fields","text":"# Bad: Multiple unnecessary transforms\nfor i in 1:100\n    ensure_layout!(T, :g)\n    # ... work ...\n    ensure_layout!(T, :c)\nend\n\n# Good: Batch operations in same space\nensure_layout!(T, :g)\nfor i in 1:100\n    # ... all grid-space work ...\nend\nensure_layout!(T, :c)","title":"Efficient Transform Usage"},{"category":"section","location":"pages/fields/#VectorField-Operations","page":"Fields","text":"","title":"VectorField Operations"},{"category":"section","location":"pages/fields/#Component-Access","page":"Fields","text":"# Individual components (one per coordinate; this domain is 2D)\nux = u.components[1]\nuz = u.components[2]\n\n# Iterate\nfor (i, component) in enumerate(u.components)\n    println(\"Component $i: $(component.name)\")\nend","title":"Component Access"},{"category":"section","location":"pages/fields/#Vector-Magnitude","page":"Fields","text":"function magnitude(u)\n    mag = nothing\n    for c in u.components\n        ensure_layout!(c, :g)\n        d = parent(get_grid_data(c))   # rank-local storage\n        mag = mag === nothing ? d .^ 2 : mag .+ d .^ 2\n    end\n    return sqrt.(mag)\nend\n\ninitialize_field!(u.components[1], (x, z) -> sin(x))\ninitialize_field!(u.components[2], (x, z) -> cos(z))\n\nmaximum(magnitude(u))   # 1.4142… (magnitude returns a plain Array of this rank's |u|)","title":"Vector Magnitude"},{"category":"section","location":"pages/fields/#Divergence-Check","page":"Fields","text":"divergence(u) builds an operator; evaluate turns it into a ScalarField. For an incompressible flow the global maximum should be at roundoff level.\n\n# A divergence-free velocity: u = (sin(x)cos(z), -cos(x)sin(z))\ninitialize_field!(u.components[1], (x, z) ->  sin(x) * cos(z))\ninitialize_field!(u.components[2], (x, z) -> -cos(x) * sin(z))\n\ndivu = evaluate(divergence(u))\nmax_div = global_max(divu)   # ~1e-15 — collective, same answer on every rank\n\nIn equation strings the same operator is written div(...), but in Julia code you must call divergence — a bare div(u) resolves to Base.div (integer division) and raises a MethodError.","title":"Divergence Check"},{"category":"section","location":"pages/fields/#Memory-Management","page":"Fields","text":"","title":"Memory Management"},{"category":"section","location":"pages/fields/#Pre-allocation","page":"Fields","text":"# Create work array once\nensure_layout!(T, :g)\nwork = similar(get_grid_data(T))\n\n# Reuse it: explicit forward Euler diffusion step\nν, dt = 0.1, 1e-3\nfor step in 1:10\n    lapT = evaluate(lap(T))\n    ensure_layout!(lapT, :g)\n    ensure_layout!(T, :g)\n    work .= ν .* get_grid_data(lapT)\n    get_grid_data(T) .+= dt .* work\nend\n\n(For real runs use a timestepper; this is only to show the buffer reuse pattern.)","title":"Pre-allocation"},{"category":"section","location":"pages/fields/#In-place-Operations","page":"Fields","text":"# Bad: allocates a temporary array, then copies it back\ntmp = get_grid_data(T) + get_grid_data(p)\nset_grid_data!(T, tmp)\n\n# Good: no allocation\nget_grid_data(T) .+= get_grid_data(p)","title":"In-place Operations"},{"category":"section","location":"pages/fields/#Parallel-Considerations","page":"Fields","text":"","title":"Parallel Considerations"},{"category":"section","location":"pages/fields/#Local-Data","page":"Fields","text":"Each process holds only its portion of the field:\n\n# Local array size (this rank's slab)\nlocal_size = size(get_grid_data(T))\n\n# Global size\nglobal_size = (x_basis.meta.size, z_basis.meta.size)   # or global_shape(domain, :g)\n\nUnder MPI the field data is a PencilArray, which knows which slice of the global grid this rank owns (in serial it is a plain Array and this call does not apply):\n\nusing PencilArrays\nPencilArrays.range_local(get_grid_data(T))   # (1:16, 1:4) on rank 0 at np=2","title":"Local Data"},{"category":"section","location":"pages/fields/#MPI-Communication","page":"Fields","text":"Communication happens automatically during:\n\nLayout transforms\nGlobal reductions\nOutput operations","title":"MPI Communication"},{"category":"section","location":"pages/fields/#See-Also","page":"Fields","text":"Domains: Where fields live\nOperators: Operations on fields\nAPI: Fields: Complete reference","title":"See Also"},{"category":"section","location":"api/domains/#Domains-API","page":"Domains","text":"A domain represents the spatial discretization of your problem, combining coordinate systems, spectral bases, and MPI distribution.","title":"Domains API"},{"category":"section","location":"api/domains/#Domain","page":"Domains","text":"The domain combines multiple spectral bases and manages the spatial discretization.\n\nConstructor:\n\nDomain(\n    distributor::Distributor,\n    bases::Tuple{Vararg{Basis}}\n)\n\nArguments:\n\ndistributor: MPI distributor managing process distribution\nbases: Tuple of spectral bases, one per coordinate dimension\n\nThe process mesh belongs to the Distributor, not to the Domain. Omit mesh= and it is chosen automatically from the number of MPI ranks (in serial it is (1,)). If you do pass mesh=, prod(mesh) must equal the number of ranks — Distributor(coords; mesh=(4,)) on one rank throws ArgumentError: Mesh size 4 does not match number of processes 1. See Domains under MPI below.\n\nExamples:","title":"Domain"},{"category":"section","location":"api/domains/#1D-Domain","page":"Domains","text":"using Tarang\n\n# Setup coordinates and distributor\ncoords = CartesianCoordinates(\"x\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# Create basis\nx_basis = RealFourier(coords[\"x\"]; size=128, bounds=(0.0, 2π))\n\n# Create domain\ndomain = Domain(dist, (x_basis,))","title":"1D Domain"},{"category":"section","location":"api/domains/#2D-Domain","page":"Domains","text":"coords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# Periodic horizontal, bounded vertical\nx_basis = RealFourier(coords[\"x\"]; size=64, bounds=(0.0, 4.0))\nz_basis = ChebyshevT(coords[\"z\"]; size=32, bounds=(0.0, 1.0))\n\ndomain = Domain(dist, (x_basis, z_basis))","title":"2D Domain"},{"category":"section","location":"api/domains/#3D-Domain","page":"Domains","text":"coords = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# All periodic (e.g., turbulence)\nx_basis = RealFourier(coords[\"x\"]; size=32, bounds=(0.0, 2π))\ny_basis = RealFourier(coords[\"y\"]; size=32, bounds=(0.0, 2π))\nz_basis = RealFourier(coords[\"z\"]; size=32, bounds=(0.0, 2π))\n\ndomain = Domain(dist, (x_basis, y_basis, z_basis))\n\n","title":"3D Domain"},{"category":"section","location":"api/domains/#Properties","page":"Domains","text":"","title":"Properties"},{"category":"section","location":"api/domains/#Basic-Properties","page":"Domains","text":"A Domain has exactly three user-facing fields:\n\ndomain.dist             # Distributor: MPI distribution\ndomain.bases            # Tuple: Spectral bases, sorted by axis\ndomain.dim              # Int: Number of dimensions\n\nThe coordinate system is reached through the distributor (domain.dist.coordsys) or a single coordinate by name with get_coord(domain, \"x\").","title":"Basic Properties"},{"category":"section","location":"api/domains/#Size-Information","page":"Domains","text":"Sizes are functions, not fields:\n\n# Global grid shape (grid points)\nglobal_shape(domain, :g)    # (64, 32)\n\n# Global coefficient shape — the first Fourier axis is halved by the rfft\nglobal_shape(domain, :c)    # (33, 32)\n\n# Local shape on this MPI rank (equals the global shape in serial)\nlocal_shape(domain, :g)     # (64, 32)\nlocal_shape(domain, :c)     # (33, 32)\n\n# Grid spacing per axis (minimum spacing for Chebyshev)\ngrid_spacing(domain)        # [0.0625, 0.00256...]\n\n# Domain volume (product of interval lengths)\nvolume(domain)              # 4.0\n\n# Coordinate names, in axis order\nbasis_names(domain)         # [\"x\", \"z\"]\n\nBounds and lengths live on the bases, in basis.meta:\n\nbounds  = map(b -> b.meta.bounds, domain.bases)                       # ((0.0, 4.0), (0.0, 1.0))\nlengths = map(b -> b.meta.bounds[2] - b.meta.bounds[1], domain.bases) # (4.0, 1.0)\nsizes   = map(b -> b.meta.size, domain.bases)                         # (64, 32)\n\n","title":"Size Information"},{"category":"section","location":"api/domains/#Methods","page":"Domains","text":"","title":"Methods"},{"category":"section","location":"api/domains/#Grid-Access","page":"Domains","text":"Grids come from the bases plus the distributor, so the same call is correct in serial and distributed — it always returns this rank's slice.\n\n# All axes at once: a tuple of local 1D grid vectors\nx, z = local_grids(dist, x_basis, z_basis)\n\n# A single axis (third argument is the scale factor, 1 = no upsampling)\nx = local_grid(domain.bases[1], domain.dist, 1)","title":"Grid Access"},{"category":"section","location":"api/domains/#Meshgrid-Creation","page":"Domains","text":"create_meshgrid returns a Dict keyed by coordinate name, with one full N-dimensional array per coordinate:\n\ng = create_meshgrid(domain; on_device=false)\nX, Z = g[\"x\"], g[\"z\"]        # each of size (64, 32)\n\nwarning: create_meshgrid is global\ncreate_meshgrid builds the global grid arrays on every rank. Under MPI they do not match the local slab of a field, so broadcasting them into field data throws DimensionMismatch. Initialize fields from local_grids instead — that is correct in serial and distributed.\n\nThat idiom looks like this — correct in serial and at any rank count:\n\nfunction init_temperature!(T, domain)\n    x, z = local_grids(domain.dist, domain.bases...)\n    Lx = domain.bases[1].meta.bounds[2] - domain.bases[1].meta.bounds[1]\n    Lz = domain.bases[2].meta.bounds[2] - domain.bases[2].meta.bounds[1]\n\n    ensure_layout!(T, :g)\n    get_grid_data(T) .= sin.(2π .* x ./ Lx) .* cos.(π .* z' ./ Lz)\n    ensure_layout!(T, :c)\n\n    return T\nend","title":"Meshgrid Creation"},{"category":"section","location":"api/domains/#Wavenumber-Arrays","page":"Domains","text":"Wavenumbers come from a Fourier basis, not from the domain:\n\n# Physical wavenumbers k = 2πn/L, one entry per grid point\nkx = wavenumbers(x_basis)   # length 64 for the size-64 basis above: [0.0, 1.5708, 1.5708, ...]\n\n# Wavenumber magnitude on a 3D Fourier domain\nfunction k_magnitude(domain)\n    kx, ky, kz = map(wavenumbers, domain.bases)\n\n    KX = reshape(kx, :, 1, 1)\n    KY = reshape(ky, 1, :, 1)\n    KZ = reshape(kz, 1, 1, :)\n\n    return @. sqrt(KX^2 + KY^2 + KZ^2)\nend\n\nwavenumbers is only defined for RealFourier and ComplexFourier bases.\n\n","title":"Wavenumber Arrays"},{"category":"section","location":"api/domains/#Domain-Types-and-Examples","page":"Domains","text":"","title":"Domain Types and Examples"},{"category":"section","location":"api/domains/#Periodic-Domains","page":"Domains","text":"All directions periodic (e.g., homogeneous turbulence):\n\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\nbases = (\n    RealFourier(coords[\"x\"]; size=32, bounds=(0.0, 2π)),\n    RealFourier(coords[\"y\"]; size=32, bounds=(0.0, 2π)),\n    RealFourier(coords[\"z\"]; size=32, bounds=(0.0, 2π))\n)\n\ndomain = Domain(dist, bases)\n\nUse cases: Turbulence, Taylor-Green vortex, forced isotropic flow\n\n","title":"Periodic Domains"},{"category":"section","location":"api/domains/#Channel-Domains","page":"Domains","text":"Periodic in streamwise/spanwise, bounded in wall-normal:\n\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\nbases = (\n    RealFourier(coords[\"x\"]; size=32, bounds=(0.0, 2π)),          # Streamwise\n    RealFourier(coords[\"y\"]; size=16, bounds=(0.0, Float64(π))),  # Spanwise\n    ChebyshevT(coords[\"z\"]; size=16, bounds=(0.0, 1.0))           # Wall-normal\n)\n\ndomain = Domain(dist, bases)\n\nUse cases: Channel flow, pipe flow, boundary layers\n\nbounds must be a Tuple{Float64, Float64}: bounds=(0.0, π) throws a TypeError because π is an Irrational. Write (0.0, 2π) (already a Float64) or (0.0, Float64(π)).\n\nThis axis order (bounded axis last) is the serial one. Under MPI the Chebyshev axis must come first — see Domains under MPI.\n\n","title":"Channel Domains"},{"category":"section","location":"api/domains/#Convection-Domains","page":"Domains","text":"Periodic horizontal, bounded vertical:\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\nbases = (\n    RealFourier(coords[\"x\"]; size=64, bounds=(0.0, 4.0)),  # Horizontal\n    ChebyshevT(coords[\"z\"]; size=32, bounds=(0.0, 1.0))    # Vertical\n)\n\ndomain = Domain(dist, bases)\n\nUse cases: Rayleigh-Bénard convection, atmospheric convection\n\n","title":"Convection Domains"},{"category":"section","location":"api/domains/#Non-Cartesian-Domains","page":"Domains","text":"note: Note\nCartesianCoordinates is the only coordinate system implemented. SphericalCoordinates and other curvilinear systems are planned but do not exist yet, so there is no spherical-shell or polar domain, and no metric/Jacobian machinery behind grad, div, curl.\n\nYou can still name Cartesian coordinates anything you like and mix bases freely — but the operators remain Cartesian; no curvature terms are added:\n\n# Chebyshev × Fourier, with coordinates named \"r\" and \"theta\".\n# The metric is still Cartesian: ∇ is (∂r, ∂theta), NOT (∂r, (1/r)∂theta).\ncoords = CartesianCoordinates(\"r\", \"theta\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\nbases = (\n    ChebyshevT(coords[\"r\"]; size=32, bounds=(0.0, 1.0)),\n    RealFourier(coords[\"theta\"]; size=64, bounds=(0.0, 2π))\n)\n\ndomain = Domain(dist, bases)\n\n","title":"Non-Cartesian Domains"},{"category":"section","location":"api/domains/#Multi-Domain-Problems","page":"Domains","text":"Some problems require multiple domains (e.g., multi-scale, coarse/fine comparison). Domains are cheap to build and can share a distributor:\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# Coarse domain for large scales\ndomain_coarse = Domain(dist, (\n    RealFourier(coords[\"x\"]; size=32, bounds=(0.0, 4.0)),\n    ChebyshevT(coords[\"z\"]; size=16, bounds=(0.0, 1.0))\n))\n\n# Fine domain for small scales\ndomain_fine = Domain(dist, (\n    RealFourier(coords[\"x\"]; size=128, bounds=(0.0, 4.0)),\n    ChebyshevT(coords[\"z\"]; size=64, bounds=(0.0, 1.0))\n))\n\n","title":"Multi-Domain Problems"},{"category":"section","location":"api/domains/#Domain-Utilities","page":"Domains","text":"","title":"Domain Utilities"},{"category":"section","location":"api/domains/#Integration","page":"Domains","text":"Quadrature weights are per-axis vectors from integration_weights; the domain integral of a field is integrate:\n\nw = integration_weights(domain; on_device=false)   # one weight vector per axis\nV = volume(domain)                                 # product of interval lengths\n\n# Domain integral of a field (correct in serial and under MPI)\ntotal = integrate(T)","title":"Integration"},{"category":"section","location":"api/domains/#Domain-Information","page":"Domains","text":"function print_domain_info(domain)\n    println(\"Domain Information:\")\n    println(\"  Dimensions: $(domain.dim)\")\n    println(\"  Grid shape: $(global_shape(domain, :g))\")\n    println(\"  Coeff shape: $(global_shape(domain, :c))\")\n    println(\"  Local grid shape: $(local_shape(domain, :g))\")\n    println(\"  Volume: $(volume(domain))\")\n\n    for (i, basis) in enumerate(domain.bases)\n        println(\"  Basis $i: $(typeof(basis)) on '$(basis.meta.element_label)'\")\n        println(\"    Size: $(basis.meta.size)\")\n        println(\"    Bounds: $(basis.meta.bounds)\")\n        println(\"    Dealias: $(basis.meta.dealias)\")\n    end\nend","title":"Domain Information"},{"category":"section","location":"api/domains/#Domain-Validation","page":"Domains","text":"function validate_domain(domain)\n    dist = domain.dist\n\n    # One basis per axis\n    @assert length(domain.bases) == domain.dim\n\n    # The process mesh must cover exactly the ranks in the communicator\n    @assert prod(dist.mesh) == dist.size\n\n    # MPI decomposes the TRAILING axes: those must be Fourier\n    if dist.size > 1\n        ndecomp = length(dist.mesh)\n        for basis in domain.bases[(end - ndecomp + 1):end]\n            @assert basis isa FourierBasis \"decomposed axis '$(basis.meta.element_label)' must be Fourier\"\n        end\n    end\n\n    println(\"Domain validation passed!\")\nend","title":"Domain Validation"},{"category":"section","location":"api/domains/#Grid-Refinement","page":"Domains","text":"function refine_domain(domain; factor=2)\n    bases_fine = map(domain.bases) do basis\n        coord = get_coord(domain, basis.meta.element_label)\n        if basis isa RealFourier\n            RealFourier(coord; size=factor * basis.meta.size,\n                        bounds=basis.meta.bounds, dealias=basis.meta.dealias)\n        elseif basis isa ChebyshevT\n            ChebyshevT(coord; size=factor * basis.meta.size,\n                       bounds=basis.meta.bounds, dealias=basis.meta.dealias)\n        else\n            error(\"refine_domain: unsupported basis $(typeof(basis))\")\n        end\n    end\n\n    return Domain(domain.dist, Tuple(bases_fine))\nend\n\n","title":"Grid Refinement"},{"category":"section","location":"api/domains/#Domains-under-MPI","page":"Domains","text":"Nothing in the domain changes under MPI — the decomposition is entirely the distributor's job — but two rules are hard constraints.","title":"Domains under MPI"},{"category":"section","location":"api/domains/#1.-The-process-mesh-has-one-dimension-fewer-than-the-domain","page":"Domains","text":"PencilArrays decomposes the trailing axes and keeps the leading axis local for the FFT.\n\ndomain mesh= result\n2D omitted (2,) at 2 ranks, (4,) at 4 ranks — a 1D mesh\n2D (nprocs,) works — same decomposition as the automatic mesh\n2D (2, 2) on 4 ranks hard error: PencilFFT plan creation failed with 4 MPI processes\n3D omitted (1, 2) at 2 ranks, (2, 2) at 4 ranks — a 2D mesh\n\nA mesh whose rank equals the domain dimension decomposes every axis and leaves nothing local for the FFT. Just omit mesh= unless you have a specific reason to pin it.\n\nA 16×32 (Chebyshev × Fourier) domain therefore has local_shape(domain, :g) == (16, 16) at 2 ranks and (16, 8) at 4 ranks: only the trailing Fourier axis is split.","title":"1. The process mesh has one dimension fewer than the domain"},{"category":"section","location":"api/domains/#2.-A-Chebyshev-axis-must-come-first","page":"Domains","text":"A Chebyshev transform needs the whole axis on one rank, and the trailing axes are the decomposed ones — so the Chebyshev axis has to be the leading one. This reverses the serial convention:\n\n# Serial:  Domain(dist, (x_fourier, z_chebyshev))\n# MPI:     Domain(dist, (z_chebyshev, x_fourier))\n\ncoords = CartesianCoordinates(\"z\", \"x\")          # note the coordinate order too\ndist = Distributor(coords; dtype=Float64, device=CPU())\nz_basis = ChebyshevT(coords[\"z\"]; size=16, bounds=(0.0, 1.0))\nx_basis = RealFourier(coords[\"x\"]; size=32, bounds=(0.0, 2π))\n\ndomain = Domain(dist, (z_basis, x_basis))\n\nGetting this wrong is a loud error, not a silent wrong answer — building a domain with the Chebyshev axis last on more than one rank reports that the decomposed trailing axes are non-Fourier and tells you to reorder the bases.\n\n","title":"2. A Chebyshev axis must come first"},{"category":"section","location":"api/domains/#Performance-Considerations","page":"Domains","text":"","title":"Performance Considerations"},{"category":"section","location":"api/domains/#Memory-Usage","page":"Domains","text":"function estimate_memory(domain)\n    bytes_per_point = sizeof(domain.dist.dtype)\n    points = prod(global_shape(domain, :g))\n    total_gb = bytes_per_point * points / 1e9\n\n    per_rank_gb = total_gb / domain.dist.size\n\n    println(\"Total memory per field: $(total_gb) GB\")\n    println(\"Per rank: $(per_rank_gb) GB\")\nend\n\nThis counts one grid-space array per field; a solver also holds coefficient-space arrays (global_shape(domain, :c)) and per-stage timestepper workspace, so budget a few times this.","title":"Memory Usage"},{"category":"section","location":"api/domains/#Choosing-a-Decomposition","page":"Domains","text":"The mesh rank is fixed by the domain (see above), so the only free choice is how the ranks are split across the trailing axes. The automatic mesh (mesh= omitted) balances that for you, and in practice it is the right answer. Whatever you choose, check what each rank actually got with local_shape(domain, :g) — a slab that is only one or two points thick along a decomposed axis spends most of its time in communication.\n\n","title":"Choosing a Decomposition"},{"category":"section","location":"api/domains/#See-Also","page":"Domains","text":"Coordinates: Coordinate system setup\nBases: Spectral basis selection\nFields: Creating fields on domains\nOperators: Differential operators\nParallelism Guide: MPI configuration","title":"See Also"},{"category":"section","location":"pages/contributing/#Contributing","page":"Contributing","text":"Fork, clone, and submit PRs to main. Run tests with julia --project=. -e 'using Pkg; Pkg.test()'.\n\nStyle: 4-space indent, 92-char lines, snake_case functions, CamelCase types.\n\nHelp: Issues | Discussions","title":"Contributing"},{"category":"section","location":"tutorials/rotating_shallow_water/#Rotating-Shallow-Water-with-Lagrangian-Averaging","page":"Rotating Shallow Water","text":"This tutorial demonstrates how to use temporal filters to separate fast inertia-gravity waves from slow geostrophic flow in a rotating shallow water model.\n\n","title":"Rotating Shallow Water with Lagrangian Averaging"},{"category":"section","location":"tutorials/rotating_shallow_water/#Physical-Problem","page":"Rotating Shallow Water","text":"The rotating shallow water equations on an f-plane:\n\n\nfracpartial upartial t - fv = -gfracpartial etapartial x\n\n\n\nfracpartial vpartial t + fu = -gfracpartial etapartial y\n\n\n\nfracpartial etapartial t + Hleft(fracpartial upartial x + fracpartial vpartial yright) = 0\n\n\nwhere\n- u v = velocity components\n- eta = surface elevation\n- f = Coriolis parameter\n- g = gravity\n- H = mean depth\n\nThis system supports two types of motion\n1 **Slow geostrophic flow** - balanced flow satisfying fv = gpartialetapartial x\n2 **Fast inertia-gravity waves** - oscillations with frequency omega^2 = f^2 + c^2(k_x^2 + k_y^2)\n\nThe challenge **How do we separate these efficiently**\n\n---\n\n Solution Temporal Filtering\n\nInstead of explicit trajectory tracking we use **exponential temporal filters** that run alongside the simulation The Butterworth filter provides sharp frequency separation with only two state arrays (the mean barh and the auxiliary tildeh) per filtered field\n\n Wave-Mean Decomposition\n\nAfter filtering\n\nu = ū + u     (mean + wave fluctuation)\n\n\nThe mean baru captures the slow geostrophic motion while u contains the fast wave oscillations\n\n---\n\n Complete Example Code\n\nThe filters below are the only Tarang machinery this tutorial needs the shallow water\ndynamics are a plain FFTW pseudo-spectral RK4 solver so the example is self-contained\nThe main loop is 10000 RK4 steps at 64² which takes roughly half a minute  shorten\nT_final if you only want to see the filter machinery work\n\njulia\nusing Tarang\nusing Statistics mean\nusing Printf\n\n =============================================================================\n ROTATING SHALLOW WATER MODEL WITH WAVE-MEAN DECOMPOSITION\n =============================================================================\n\n -----------------------------------------------------------------------------\n 1 Physical Parameters\n -----------------------------------------------------------------------------\n\n Domain\nLx Ly = 2π 2π         Domain size\nNx Ny = 64 64         Resolution\n\n Physical constants\nf = 10                 Coriolis parameter\ng = 100                Gravity\nH = 10                 Mean depth\nν = 0001               Viscosity (for numerical stability)\n\n Derived quantities\nc = sqrt(g * H)         Gravity wave phase speed\nT_inertial = 2π  f     Inertial period\n\nprintln(Physical parameters)\nprintln(  Gravity wave speed c =  c)\nprintln(  Inertial period T =  round(T_inertial digits=3))\n\n -----------------------------------------------------------------------------\n 2 Grid and Operators\n -----------------------------------------------------------------------------\n\ndx dy = LxNx LyNy\nx = (i-1)*dx for i in 1Nx\ny = (j-1)*dy for j in 1Ny\n\n Spectral wavenumbers\nkx = i = Nx2+1  i-1  i-Nx-1 for i in 1Nx * (2πLx)\nky = j = Ny2+1  j-1  j-Ny-1 for j in 1Ny * (2πLy)\n\n Preallocate FFT arrays\nusing FFTW\nFFTWset_num_threads(4)\nplan_fft = plan_fft(zeros(ComplexF64 Nx Ny))\nplan_ifft = plan_ifft(zeros(ComplexF64 Nx Ny))\n\n Derivative operators in spectral space\nfunction spectral_gradient(dfdx dfdy f_hat kx ky)\n    inbounds for j in 1size(f_hat 2) i in 1size(f_hat 1)\n        dfdxij = im * kxi * f_hatij\n        dfdyij = im * kyj * f_hatij\n    end\nend\n\n -----------------------------------------------------------------------------\n 3 Initialize Fields\n -----------------------------------------------------------------------------\n\n Velocity and elevation\nu = zeros(Nx Ny)\nv = zeros(Nx Ny)\nη = zeros(Nx Ny)\n\n Work arrays\nu_hat = zeros(ComplexF64 Nx Ny)\nv_hat = zeros(ComplexF64 Nx Ny)\nη_hat = zeros(ComplexF64 Nx Ny)\n\n Temporary arrays for derivatives\ndudx = zeros(ComplexF64 Nx Ny)\ndudy = zeros(ComplexF64 Nx Ny)\ndvdx = zeros(ComplexF64 Nx Ny)\ndvdy = zeros(ComplexF64 Nx Ny)\ndηdx = zeros(ComplexF64 Nx Ny)\ndηdy = zeros(ComplexF64 Nx Ny)\n\n Wave perturbation (k=2 l=2 mode)\nk_wave l_wave = 20 20\nω_wave = sqrt(f^2 + c^2 * (k_wave^2 + l_wave^2))\namplitude = 01\n\n Initial condition geostrophic jet + wave perturbation\nfor i in 1Nx j in 1Ny\n     Background geostrophic jet\n     Balance is f*u = -g ηy so u = 05 sin(y)  η = +(fg) * 05 * cos(y)\n    uij = 05 * sin(yj)\n    vij = 00\n    ηij = (fg) * 05 * cos(yj)\n\n    ηij += amplitude * cos(k_wave * xi + l_wave * yj)\nend\n\nprintln(nInitial condition)\nprintln(  Geostrophic jet U = 05 sin(y))\nprintln(  Wave perturbation amplitude =  amplitude)\nprintln(  Wave frequency ω =  round(ω_wave digits=3))\n\n -----------------------------------------------------------------------------\n 4 Setup Temporal Filters\n -----------------------------------------------------------------------------\n\nT_wave = 2π  ω_wave     Wave period for k = l = 2\nα = 10  (20 * T_wave)  Average over 20 wave periods\n\nprintln(nFilter setup)\nprintln(  Wave period T_wave =  round(T_wave digits=3))\nprintln(  Filter timescale 1α =  round(1α digits=1))\n\n Create filters for each prognostic variable\nu_filter = ButterworthFilter((Nx Ny) α=α)\nv_filter = ButterworthFilter((Nx Ny) α=α)\nη_filter = ButterworthFilter((Nx Ny) α=α)\n\n Precompute ETD coefficients for unconditional stability\ndt = 001   Time step\netd_coeffs_u = precompute_etd_coefficients(u_filter dt)\netd_coeffs_v = precompute_etd_coefficients(v_filter dt)\netd_coeffs_η = precompute_etd_coefficients(η_filter dt)\n\nprintln(  Using ETD integration (unconditionally stable))\nprintln(  αdt =  round(α * dt digits=4))\n\n -----------------------------------------------------------------------------\n 5 Time Integration (RK4 for dynamics ETD for filters)\n -----------------------------------------------------------------------------\n\nfunction rhs(du dv dη u v η u_hat v_hat η_hat\n              dudx dudy dvdx dvdy dηdx dηdy\n              plan_fft plan_ifft kx ky f g H ν)\n\n     Transform to spectral space\n    u_hat = u\n    v_hat = v\n    η_hat = η\n    plan_fft * u_hat\n    plan_fft * v_hat\n    plan_fft * η_hat\n\n     Compute gradients\n    spectral_gradient(dudx dudy u_hat kx ky)\n    spectral_gradient(dvdx dvdy v_hat kx ky)\n    spectral_gradient(dηdx dηdy η_hat kx ky)\n\n     Transform derivatives back\n    plan_ifft * dudx\n    plan_ifft * dudy\n    plan_ifft * dvdx\n    plan_ifft * dvdy\n    plan_ifft * dηdx\n    plan_ifft * dηdy\n\n     RHS of shallow water equations\n    inbounds for j in 1size(u 2) i in 1size(u 1)\n         Nonlinear advection\n        adv_u = uij * real(dudxij) + vij * real(dudyij)\n        adv_v = uij * real(dvdxij) + vij * real(dvdyij)\n\n         Momentum equations\n        duij = f * vij - g * real(dηdxij) - adv_u\n        dvij = -f * uij - g * real(dηdyij) - adv_v\n\n         Continuity equation\n        dηij = -H * (real(dudxij) + real(dvdyij))\n    end\n\n     Add viscous damping in spectral space for stability\n    if ν  0\n        u_hat = u\n        v_hat = v\n        plan_fft * u_hat\n        plan_fft * v_hat\n        inbounds for j in 1size(u_hat 2) i in 1size(u_hat 1)\n            k2 = kxi^2 + kyj^2\n            visc = -ν * k2\n            u_hatij *= visc\n            v_hatij *= visc\n        end\n        plan_ifft * u_hat\n        plan_ifft * v_hat\n        du += real(u_hat)\n        dv += real(v_hat)\n    end\nend\n\n RK4 time stepper for dynamics\nfunction step_rk4(u v η dt args)\n     Allocate RK stages\n    k1_u k1_v k1_η = similar(u) similar(v) similar(η)\n    k2_u k2_v k2_η = similar(u) similar(v) similar(η)\n    k3_u k3_v k3_η = similar(u) similar(v) similar(η)\n    k4_u k4_v k4_η = similar(u) similar(v) similar(η)\n    u_tmp v_tmp η_tmp = similar(u) similar(v) similar(η)\n\n     k1\n    rhs(k1_u k1_v k1_η u v η args)\n\n     k2\n     u_tmp = u + 05 * dt * k1_u\n     v_tmp = v + 05 * dt * k1_v\n     η_tmp = η + 05 * dt * k1_η\n    rhs(k2_u k2_v k2_η u_tmp v_tmp η_tmp args)\n\n     k3\n     u_tmp = u + 05 * dt * k2_u\n     v_tmp = v + 05 * dt * k2_v\n     η_tmp = η + 05 * dt * k2_η\n    rhs(k3_u k3_v k3_η u_tmp v_tmp η_tmp args)\n\n     k4\n     u_tmp = u + dt * k3_u\n     v_tmp = v + dt * k3_v\n     η_tmp = η + dt * k3_η\n    rhs(k4_u k4_v k4_η u_tmp v_tmp η_tmp args)\n\n     Update\n     u += dt6 * (k1_u + 2*k2_u + 2*k3_u + k4_u)\n     v += dt6 * (k1_v + 2*k2_v + 2*k3_v + k4_v)\n     η += dt6 * (k1_η + 2*k2_η + 2*k3_η + k4_η)\nend\n\n -----------------------------------------------------------------------------\n 6 Main Time Loop\n -----------------------------------------------------------------------------\n\nT_final = 1000                    Total simulation time (7 filter timescales)\nnsteps = Int(T_final  dt)\noutput_interval = Int(100  dt)   Output every 10 time units\n\n Diagnostics storage\ntimes = Float64\nKE_total = Float64\nKE_mean = Float64\nKE_wave = Float64\nPE_total = Float64\n\nprintln(n * =^60)\nprintln(Starting simulation)\nprintln(=^60)\n\nfor step in 1nsteps\n    t = step * dt\n\n     Step dynamics (RK4)\n    step_rk4(u v η dt\n              u_hat v_hat η_hat\n              dudx dudy dvdx dvdy dηdx dηdy\n              plan_fft plan_ifft kx ky f g H ν)\n\n     Update filters (ETD - unconditionally stable)\n    update_etd(u_filter u etd_coeffs_u)\n    update_etd(v_filter v etd_coeffs_v)\n    update_etd(η_filter η etd_coeffs_η)\n\n     Output diagnostics\n    if step  output_interval == 0\n         Get filtered means\n        ū = get_mean(u_filter)\n        v = get_mean(v_filter)\n        η = get_mean(η_filter)\n\n         Wave components\n        u_wave = u - ū\n        v_wave = v - v\n        η_wave = η - η\n\n         Energy diagnostics\n        ke_total = 05 * mean(u^2 + v^2)\n        ke_mean = 05 * mean(ū^2 + v^2)\n        ke_wave = 05 * mean(u_wave^2 + v_wave^2)\n        pe_total = 05 * gH * mean(η^2)\n\n        push(times t)\n        push(KE_total ke_total)\n        push(KE_mean ke_mean)\n        push(KE_wave ke_wave)\n        push(PE_total pe_total)\n\n        printf(t = 62f  KE_total = 4e  KE_mean = 4e  KE_wave = 4en\n                t ke_total ke_mean ke_wave)\n    end\nend\n\n -----------------------------------------------------------------------------\n 7 Final Analysis\n -----------------------------------------------------------------------------\n\nprintln(n * =^60)\nprintln(Simulation complete)\nprintln(=^60)\n\n Final wave-mean decomposition\nū = get_mean(u_filter)\nv = get_mean(v_filter)\nη = get_mean(η_filter)\n\nu_wave = u - ū\nv_wave = v - v\nη_wave = η - η\n\nprintln(nFinal state statistics)\nprintln(  Mean flow)\nprintln(    maxū =  round(maximum(abs(ū)) digits=4))\nprintln(    maxv =  round(maximum(abs(v)) digits=4))\nprintln(    maxη =  round(maximum(abs(η)) digits=4))\n\nprintln(  Wave fluctuations)\nprintln(    maxu =  round(maximum(abs(u_wave)) digits=4))\nprintln(    maxv =  round(maximum(abs(v_wave)) digits=4))\nprintln(    maxη =  round(maximum(abs(η_wave)) digits=4))\n\nprintln(nEnergy partition)\nke_final_total = 05 * mean(u^2 + v^2)\nke_final_mean = 05 * mean(ū^2 + v^2)\nke_final_wave = 05 * mean(u_wave^2 + v_wave^2)\nprintln(  Total KE      round(ke_final_total digits=6))\nprintln(  Mean flow KE  round(ke_final_mean digits=6)\n         ( round(100*ke_final_meanke_final_total digits=1) ))\nprintln(  Wave KE       round(ke_final_wave digits=6)\n         ( round(100*ke_final_waveke_final_total digits=1) ))\n\n\n---\n\n Understanding the Output\n\n Filter spin-up\n\nThe filters start from zero so the mean grows in over roughly one filter timescale\n1alpha approx 14 What you see in the diagnostic stream is the wave energy being\nhanded over to the mean\n\n\nt =  1000  KE_total = 68060e-02  KE_mean = 20311e-03  KE_wave = 47654e-02\nt =  2000  KE_total = 64902e-02  KE_mean = 15411e-02  KE_wave = 19322e-02\nt =  3000  KE_total = 60933e-02  KE_mean = 36276e-02  KE_wave = 43692e-03\nt =  4000  KE_total = 58691e-02  KE_mean = 53346e-02  KE_wave = 42893e-04\nt =  5000  KE_total = 58350e-02  KE_mean = 61947e-02  KE_wave = 11300e-03\nt =  6000  KE_total = 57332e-02  KE_mean = 63704e-02  KE_wave = 13469e-03\nt =  7000  KE_total = 55658e-02  KE_mean = 61958e-02  KE_wave = 60873e-04\nt =  8000  KE_total = 54461e-02  KE_mean = 59197e-02  KE_wave = 26110e-04\nt =  9000  KE_total = 54004e-02  KE_mean = 56644e-02  KE_wave = 78581e-04\nt = 10000  KE_total = 52820e-02  KE_mean = 54660e-02  KE_wave = 58412e-04\n\n\nEarly on KE_wave is large simply because baru is still near zero so\nu = u - baru is the *whole* flow Only after t gtrsim 3alpha approx 40 is the\ndecomposition meaningful\n\n Energy partition\n\nThe final block prints\n\n\nEnergy partition\n  Total KE     005282\n  Mean flow KE 005466 (1035)\n  Wave KE      0000584 (11)\n\n\nTwo things are worth reading carefully\n\n- The mean flow holds essentially all the kinetic energy and the residual wave energy is\n  1 of it  the filter has done its job\n- The percentages do **not** add to 100 and the mean flow can even exceed the\n  instantaneous total That is expected not a bug the filtered mean is a weighted average\n  over the *past* and viscosity is slowly draining the jet so baru lags a slightly\n  stronger flow than the one at time t There is also no orthogonality that would force\n  overlineu^2 = baru^2 + u^2 the cross term barucdot u is not zero\n\n Wave-mean decomposition\n\nThe filtered mean recovers the initial geostrophic jet\n\n\nFinal state statistics\n  Mean flow\n    maxū = 04691       the jet is 05 sin(y) viscously damped and filter-lagged\n    maxv = 00001       no mean meridional flow as expected\n    maxη = 00468\n  Wave fluctuations\n    maxu = 00592\n    maxv = 00458\n    maxη = 00109\n\n\nComparing baru with the analytic jet gives maxū  05 sin(y)  0031 ie the mean\nprofile is the jet to 6 the deficit being the viscous decay accumulated over 100 time\nunits\n\n---\n\n Time Integration Methods\n\n Method 1 Explicit Euler (not recommended)\n\njulia\nupdate(filter field dt)   Forward Euler\n\n**Stability limit** Delta t leq sqrt2alpha update *throws* an ArgumentError\nabove it rather than silently producing garbage\n\n Method 2 RK2 (moderate stability)\n\njulia\nupdate(filter field dt Val(RK2))\n\n**Stability limit** Delta t approx 2183alpha for the Butterworth filter Query it\nwith max_stable_timestep(filter method=RK2) (and method=euler for Method 1)\n\n Method 3 ETD (recommended - unconditionally stable)\n\njulia\ncoeffs = precompute_etd_coefficients(filter dt)\nupdate_etd(filter field coeffs)\n\n**No stability limit** Can use any timestep\n\n Method 4 IMEXSBDF (for implicit PDE solvers)\n\njulia\ncoeffs = precompute_imex_coefficients(filter dt scheme=SBDF2)\nupdate_imex(filter (h_n h_nm1) coeffs)\n\n**No stability limit** Integrates naturally with SBDF timestepping Pass a 1-tuple\n(h_n) for SBDF1 and a 2-tuple (h_n h_nm1) for SBDF2\n\n---\n\n Choosing Filter Parameters\n\n The key parameter α\n\nalpha = frac1T_textavg\n\nRule of thumb: T_textavg approx 10-100 times T_textwave\n\nWaves to filter Wave period Recommended α\nInertia-gravity waves 2pisqrtf^2 + c^2 k^2 alpha = 005-01 times wave frequency\nNear-inertial waves 2pif alpha approx f50\nInternal gravity waves 2piN alpha approx N100\n\nRemember that the mean needs sim 3alpha of simulated time to spin up from zero, so a long averaging window costs you a long transient.","title":"Physical Problem"},{"category":"section","location":"tutorials/rotating_shallow_water/#Filter-comparison","page":"Rotating Shallow Water","text":"Filter High-freq rolloff State Use case\nExponentialMean -20 dB/decade 1 array (h̄) Simple averaging\nButterworthFilter -40 dB/decade 2 arrays (h̄, h̃) Sharp wave-mean separation\n\n","title":"Filter comparison"},{"category":"section","location":"tutorials/rotating_shallow_water/#Extensions","page":"Rotating Shallow Water","text":"","title":"Extensions"},{"category":"section","location":"tutorials/rotating_shallow_water/#Adding-Lagrangian-Averaging","page":"Rotating Shallow Water","text":"For a true Lagrangian mean (following particle motion), LagrangianFilter carries a displacement field xi alongside the flow. Two API details matter:\n\nupdate_displacement! takes the velocity as one array of shape (field_size..., ndim), not a tuple of components. Pack u, v into a (Nx, Ny, 2) array.\nWith filter_type=:butterworth, lagrangian_mean! needs five positional arguments — the auxiliary state g̃ is part of the two-stage Butterworth scheme: lagrangian_mean!(filter, gᴸ, g̃, g, dt). (Only the :exponential filter takes the four-argument form.)\n\nContinuing from the state reached above, run the dynamics for another nsteps while advancing the Lagrangian filter:\n\nlag_filter = LagrangianFilter((Nx, Ny); α=α, filter_type=:butterworth)\n\nuv  = zeros(Nx, Ny, 2)   # velocity packed as (field_size..., ndim)\nη_L = zeros(Nx, Ny)      # Lagrangian mean of η (output, modified in place)\nη̃   = zeros(Nx, Ny)      # auxiliary Butterworth state for the tracer\n\nfor step in 1:nsteps\n    step_rk4!(u, v, η, dt,\n              u_hat, v_hat, η_hat,\n              dudx, dudy, dvdx, dvdy, dηdx, dηdy,\n              plan_fft, plan_ifft, kx, ky, f, g, H, ν)\n\n    uv[:, :, 1] .= u\n    uv[:, :, 2] .= v\n    update_displacement!(lag_filter, uv, dt)\n    lagrangian_mean!(lag_filter, η_L, η̃, η, dt)\n\n    # keep the Eulerian means in step, for the Stokes drift below\n    update_etd!(u_filter, u, etd_coeffs_u)\n    update_etd!(v_filter, v, etd_coeffs_v)\nend\n\n# Lagrangian mean velocity, shape (Nx, Ny, 2)\nū_L = get_mean_velocity(lag_filter)\nprintln(\"max|ū_L| = \", round(maximum(abs.(ū_L[:, :, 1])), digits=4))   # 0.4274\nprintln(\"max|η_L| = \", round(maximum(abs.(η_L)), digits=4))            # 0.0427\n\nget_displacement(lag_filter) returns xi itself, also shaped (Nx, Ny, 2).\n\nNote that get_mean, get_mean_velocity and get_displacement all hand back the filter's live state array rather than a copy. The loop above keeps stepping u_filter, so the ū bound back in the final-analysis block changes underneath you as it runs. copy the array if you want a snapshot to keep.","title":"Adding Lagrangian Averaging"},{"category":"section","location":"tutorials/rotating_shallow_water/#Computing-Stokes-Drift","page":"Rotating Shallow Water","text":"The Stokes drift is the difference between the Lagrangian and Eulerian means, taken component by component:\n\nu_stokes = ū_L[:, :, 1] .- get_mean(u_filter)\nv_stokes = ū_L[:, :, 2] .- get_mean(v_filter)\n\nprintln(\"max|u_stokes| = \", round(maximum(abs.(u_stokes)), digits=5))  # 0.00166\nprintln(\"max|v_stokes| = \", round(maximum(abs.(v_stokes)), digits=5))  # 0.0\n\nHere the drift is small (~0.3% of the jet) because the wave amplitude is small; it is the wave-induced transport that matters for:\n\nPollutant dispersion\nLarval transport in oceans\nSea ice drift\n\nNote that update_displacement! and lagrangian_mean! use the small-displacement approximation unless you supply an interpolate_fn keyword that evaluates a field at the displaced positions x + xi.\n\n","title":"Computing Stokes Drift"},{"category":"section","location":"tutorials/rotating_shallow_water/#References","page":"Rotating Shallow Water","text":"Minz, C., Baker, L. E., Kafiabad, H. A., & Vanneste, J. (2025). Efficient Lagrangian averaging with exponential filters. Phys. Rev. Fluids, 10, 074902. DOI\nVallis, G. K. (2017). Atmospheric and Oceanic Fluid Dynamics (2nd ed.). Cambridge University Press. Chapter 3: Shallow water systems.\nBühler, O. (2014). Waves and Mean Flows (2nd ed.). Cambridge University Press.\n\n","title":"References"},{"category":"section","location":"tutorials/rotating_shallow_water/#Next-Steps","page":"Rotating Shallow Water","text":"Temporal Filters Reference - Complete API documentation\nLES Models - Subgrid-scale modeling for turbulence\nSurface Dynamics - SQG and other surface-confined flows\n\n\n\nTutorial version 1.0 | December 2024 | Tarang.jl","title":"Next Steps"},{"category":"section","location":"notebooks/rayleigh_benard/#Notebook:-Rayleigh-Bénard-Convection","page":"Notebook: Rayleigh-Bénard Convection","text":"This notebook demonstrates 2D Rayleigh-Bénard convection simulation.","title":"Notebook: Rayleigh-Bénard Convection"},{"category":"section","location":"notebooks/rayleigh_benard/#Setup","page":"Notebook: Rayleigh-Bénard Convection","text":"using Tarang\nusing Printf\nusing Plots","title":"Setup"},{"category":"section","location":"notebooks/rayleigh_benard/#Problem-Definition","page":"Notebook: Rayleigh-Bénard Convection","text":"Rayleigh-Bénard convection simulates fluid heated from below in a gravity field. Non-dimensionalizing with the box height and the thermal diffusion time, the equations are\n\npartial_t u + u cdot nabla u = -nabla p + mathrmPr nabla^2 u + mathrmRamathrmPr T hatz\nqquad\npartial_t T + u cdot nabla T = nabla^2 T\nqquad\nnabla cdot u = 0 ","title":"Problem Definition"},{"category":"section","location":"notebooks/rayleigh_benard/#Parameters","page":"Notebook: Rayleigh-Bénard Convection","text":"Ra = 2e4    # Rayleigh number\nPr = 1.0    # Prandtl number\nLx = 4.0    # Domain width\nLz = 1.0    # Domain height\n\nNx, Nz = 64, 16\n\nThe resolution is deliberately small so the notebook runs in under a minute. Production runs use Nx, Nz = 256, 64 and Ra = 2e6 (see examples/ivp/rayleigh_benard_2d.jl).","title":"Parameters"},{"category":"section","location":"notebooks/rayleigh_benard/#Domain","page":"Notebook: Rayleigh-Bénard Convection","text":"The wall-bounded vertical direction is Chebyshev; the periodic horizontal direction is Fourier. In serial the Fourier axis comes first.\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\n\nx_basis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nz_basis = ChebyshevT(coords[\"z\"];  size=Nz, bounds=(0.0, Lz), dealias=3/2)\n\ndomain = Domain(dist, (x_basis, z_basis))\n\ndealias=3/2 is a padding factor: it applies the 3/2 (a.k.a. 2/3) rule. Passing dealias=2/3 would switch dealiasing off.","title":"Domain"},{"category":"section","location":"notebooks/rayleigh_benard/#Fields","page":"Notebook: Rayleigh-Bénard Convection","text":"Velocity is a single VectorField, not two scalars — the parser's ∇, div and u⋅∇(u) all act on vectors, and CFL only accepts vector velocities.\n\np = ScalarField(domain, \"p\")\nT = ScalarField(domain, \"T\")\nu = VectorField(domain, \"u\")","title":"Fields"},{"category":"section","location":"notebooks/rayleigh_benard/#Tau-fields-and-the-first-order-reduction","page":"Notebook: Rayleigh-Bénard Convection","text":"A Chebyshev problem needs tau terms to carry the boundary conditions. Each tau variable is a rank-reduced field living on the Fourier basis only — one tau per horizontal mode — except the pressure gauge tau_p, which is a single scalar.\n\ntau_p  = ScalarField(dist, \"tau_p\",  (), Float64)\ntau_T1 = ScalarField(dist, \"tau_T1\", (x_basis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (x_basis,), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (x_basis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (x_basis,), Float64)\n\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(z_basis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\n\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_T = grad(T) + ez * τ_lift(tau_T1)","title":"Tau fields and the first-order reduction"},{"category":"section","location":"notebooks/rayleigh_benard/#Equations","page":"Notebook: Rayleigh-Bénard Convection","text":"Parameters and substitutions are registered with add_parameters!; equation strings are parsed by Tarang and cannot see Julia globals otherwise.\n\nproblem = IVP([p, T, u, tau_p, tau_T1, tau_T2, tau_u1, tau_u2])\n\nadd_parameters!(problem, nu=Pr, buoy=Ra*Pr, ez=ez,\n                grad_u=grad_u, grad_T=grad_T, τ_lift=τ_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) - buoy*T*ez + τ_lift(tau_u2) = -u⋅∇(u)\")","title":"Equations"},{"category":"section","location":"notebooks/rayleigh_benard/#Boundary-Conditions","page":"Notebook: Rayleigh-Bénard Convection","text":"Boundary conditions go through add_bc!, never add_equation!. A BC declared with add_equation! is not registered with the boundary-condition manager and is silently not enforced (a constant BC may still come out right by accident, which is what makes the mistake so easy to miss).\n\n# No-slip walls\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=$Lz) = 0\")\n\n# Fixed temperatures\nadd_bc!(problem, \"T(z=0) = 1\")     # Hot\nadd_bc!(problem, \"T(z=$Lz) = 0\")   # Cold\n\n# Pressure gauge (the incompressible pressure is defined only up to a constant)\nadd_bc!(problem, \"integ(p) = 0\")\n\nBC strings can only refer to literals and names registered with add_parameters! — a bare Julia global such as Lz inside the string would warn Unknown variable: Lz and be applied as 0. Interpolating the value with $Lz sidesteps that.","title":"Boundary Conditions"},{"category":"section","location":"notebooks/rayleigh_benard/#Initial-Conditions","page":"Notebook: Rayleigh-Bénard Convection","text":"The conduction profile T = 1 - z plus a small random perturbation, damped at the walls so it does not fight the boundary conditions.\n\nx, z = local_grids(dist, x_basis, z_basis)\n\nfill_random!(T, \"g\"; seed=42, distribution=\"normal\", scale=1e-3)\nget_grid_data(T) .*= z' .* (1.0 .- z')   # damp noise at the walls\nget_grid_data(T) .+= 1.0 .- z'           # add the conduction profile\nensure_layout!(T, :c)","title":"Initial Conditions"},{"category":"section","location":"notebooks/rayleigh_benard/#Solver","page":"Notebook: Rayleigh-Bénard Convection","text":"CFL is built from the solver, and velocities are added as VectorFields.\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)\n\ncfl = CFL(solver; initial_dt=1e-4, cadence=10, safety=0.5, max_dt=1e-3)\nadd_velocity!(cfl, u)\n\nThe full keyword list is initial_dt, cadence, safety, threshold, max_change, min_change, max_dt. The current step is cfl.current_dt; min_change/max_change are the per-update ratio limits.","title":"Solver"},{"category":"section","location":"notebooks/rayleigh_benard/#Simulation","page":"Notebook: Rayleigh-Bénard Convection","text":"stop_time = 0.02\n\ntimes    = Float64[]\nenergies = Float64[]\n\nwhile solver.sim_time < stop_time\n    dt = compute_timestep(cfl)\n    step!(solver, dt)\n\n    push!(times, solver.sim_time)\n    push!(energies, total_kinetic_energy(u))\n\n    if solver.iteration % 10 == 0\n        @printf(\"iter=%d  t=%.4f  dt=%.2e  KE=%.4e\\n\",\n                solver.iteration, solver.sim_time, dt, energies[end])\n    end\nend\n\ntotal_kinetic_energy(u) returns the domain integral as a Float64. (kinetic_energy(u) returns the field ½ρ|u|², which is what you want for a snapshot, not for a time series.) Over the 29 steps above the kinetic energy grows from 1.5e-7 to 8.6e-5 as the buoyancy instability takes off.\n\nPassing the controller to run! is equivalent and shorter:\n\nrun!(solver; stop_time=stop_time, cfl=cfl, progress=false)","title":"Simulation"},{"category":"section","location":"notebooks/rayleigh_benard/#Visualization","page":"Notebook: Rayleigh-Bénard Convection","text":"ensure_layout!(T, :g)\nTg = Array(get_grid_data(T))\n\nheatmap(x, z, Tg',\n    xlabel=\"x\", ylabel=\"z\",\n    title=\"Temperature at t=$(round(solver.sim_time, digits=3))\",\n    aspect_ratio=:equal, color=:inferno\n)\n\nget_grid_data returns the coefficient array unless the field is in grid layout, so ensure_layout!(T, :g) first. The array is (Nx, Nz), hence the transpose for heatmap.","title":"Visualization"},{"category":"section","location":"notebooks/rayleigh_benard/#Analysis","page":"Notebook: Rayleigh-Bénard Convection","text":"plot(times, energies,\n    xlabel=\"Time\",\n    ylabel=\"Kinetic Energy\",\n    title=\"Energy Evolution\"\n)\n\nA quick check that the tau method did its job — the boundary values are exact to roundoff (max|T(z=0) - 1| ≈ 2e-15):\n\n@printf(\"max|T(z=0) - 1| = %.2e\\n\", maximum(abs, Tg[:, 1]   .- 1.0))\n@printf(\"max|T(z=Lz)|    = %.2e\\n\", maximum(abs, Tg[:, end]))","title":"Analysis"},{"category":"section","location":"notebooks/rayleigh_benard/#Running-under-MPI","page":"Notebook: Rayleigh-Bénard Convection","text":"This problem is serial-only. The advection terms -u⋅∇(T) and -u⋅∇(u) put a Chebyshev derivative on the explicit right-hand side, and a distributed run cannot take it: each rank owns only part of the Chebyshev axis, so the first step errors with\n\nLazy RHS: cannot differentiate along the non-Fourier axis 1 (ChebyshevT) of a DISTRIBUTED field …\n\nDistributed Chebyshev-Fourier runs are supported when every explicit derivative is along a Fourier axis (and then the Chebyshev axis must come first in the domain). See Running with MPI.","title":"Running under MPI"},{"category":"section","location":"notebooks/rayleigh_benard/#Exercises","page":"Notebook: Rayleigh-Bénard Convection","text":"Vary Rayleigh number: Try Ra = 10^4, 10^5, 10^7\nChange aspect ratio: Lx = 2, 8, 16\nCompute Nusselt number: Track heat transfer efficiency, Nu = 1 + integrate(w T) / (Lx * Lz)\nResolution study: How do results change with Nx, Nz?","title":"Exercises"},{"category":"section","location":"notebooks/rayleigh_benard/#References","page":"Notebook: Rayleigh-Bénard Convection","text":"Chandrasekhar, S. (1961). Hydrodynamic and Hydromagnetic Stability\nSpectral methods tutorials","title":"References"},{"category":"section","location":"examples/","page":"-","text":"GPU FFT Heuristics","title":"-"},{"category":"section","location":"pages/configuration/#Configuration","page":"Configuration","text":"Tarang.jl provides multiple ways to configure its behavior, from environment variables to configuration files. This guide covers all configuration options.","title":"Configuration"},{"category":"section","location":"pages/configuration/#Configuration-Files","page":"Configuration","text":"","title":"Configuration Files"},{"category":"section","location":"pages/configuration/#Location","page":"Configuration","text":"Tarang.jl searches for configuration files in this order:\n\nCurrent working directory: ./tarang.toml\nUser configuration: ~/.tarang/config.toml\nPackage default: Built-in defaults\n\nThe first file found takes precedence.","title":"Location"},{"category":"section","location":"pages/configuration/#File-Format","page":"Configuration","text":"Configuration uses TOML format:\n\n# tarang.toml\n\n[parallelism]\nTRANSPOSE_LIBRARY = \"PENCIL\"\nGROUP_TRANSPOSES = true\nSYNC_TRANSPOSES = true\n\n[transforms]\nGROUP_TRANSFORMS = true\nDEALIAS_BEFORE_CONVERTING = true\n\n[transforms-fftw]\nPLANNING_RIGOR = \"FFTW_MEASURE\"\nUSE_FFTW_WISDOM = true\nWISDOM_FILE = \"fftw_wisdom.dat\"\n\n[logging]\nLEVEL = \"INFO\"\nFILE = \"tarang.log\"\nMPI_AWARE = true\n\n[performance]\nPROFILE_DIR = \"profiles\"\nENABLE_PROFILING = false\nMEMORY_POOL_SIZE = \"1GB\"\n\n[output]\nDEFAULT_FORMAT = \"netcdf\"\nCOMPRESSION_LEVEL = 4\nCHUNK_SIZE = \"auto\"","title":"File Format"},{"category":"section","location":"pages/configuration/#Configuration-Sections","page":"Configuration","text":"","title":"Configuration Sections"},{"category":"section","location":"pages/configuration/#Parallelism","page":"Configuration","text":"Controls MPI and parallel execution:\n\n[parallelism]\n# Transpose library: \"PENCIL\" or \"CUSTOM\"\nTRANSPOSE_LIBRARY = \"PENCIL\"\n\n# Group multiple transposes together\nGROUP_TRANSPOSES = true\n\n# Synchronize after transposes (for debugging)\nSYNC_TRANSPOSES = false\n\n# Process mesh optimization\nAUTO_OPTIMIZE_MESH = true\nPREFERRED_PENCIL_AXIS = \"auto\"  # \"auto\", \"x\", \"y\", \"z\"\n\nOptions:\n\nTRANSPOSE_LIBRARY: Backend for array transposes\n\"PENCIL\": Use PencilArrays (recommended)\n\"CUSTOM\": Custom implementation\nGROUP_TRANSPOSES: Batch transposes for efficiency\nSYNC_TRANSPOSES: Add MPI barriers (reduces performance, useful for debugging)","title":"Parallelism"},{"category":"section","location":"pages/configuration/#Transforms","page":"Configuration","text":"FFT and spectral transform settings:\n\n[transforms]\n# Group multiple transforms\nGROUP_TRANSFORMS = true\n\n# Dealias before converting between spaces\nDEALIAS_BEFORE_CONVERTING = true\n\n# Transform cache size (number of temporary arrays)\nCACHE_SIZE = 10\n\n# Forward transform in-place when possible\nINPLACE_FORWARD = true\n\n# Backward transform in-place when possible\nINPLACE_BACKWARD = true","title":"Transforms"},{"category":"section","location":"pages/configuration/#FFTW-Settings","page":"Configuration","text":"FFTW-specific configuration:\n\n[transforms-fftw]\n# Planning rigor: determines FFTW plan optimization level\nPLANNING_RIGOR = \"FFTW_MEASURE\"\n\n# Save and load FFTW wisdom\nUSE_FFTW_WISDOM = true\nWISDOM_FILE = \"fftw_wisdom.dat\"\n\n# Number of threads for FFTW (usually leave at 1 with MPI)\nNUM_THREADS = 1\n\n# FFTW flags\nFLAGS = [\"FFTW_MEASURE\", \"FFTW_DESTROY_INPUT\"]\n\nPLANNING_RIGOR options:\n\n\"FFTW_ESTIMATE\": Fast planning, slower execution (good for testing)\n\"FFTW_MEASURE\": Medium planning time, good performance (recommended)\n\"FFTW_PATIENT\": Slow planning, best performance (for production runs)\n\"FFTW_EXHAUSTIVE\": Very slow planning, marginally better than PATIENT\n\ntip: FFTW Wisdom\nSave FFTW wisdom on first run, then reuse it to skip planning:using FFTW\n# After first run with FFTW_MEASURE\nFFTW.export_wisdom(\"fftw_wisdom.dat\")\n\n# Subsequent runs\nFFTW.import_wisdom(\"fftw_wisdom.dat\")","title":"FFTW Settings"},{"category":"section","location":"pages/configuration/#Logging","page":"Configuration","text":"Configure logging output:\n\n[logging]\n# Log level: \"TRACE\", \"DEBUG\", \"INFO\", \"NOTICE\", \"WARN\", \"ERROR\"\nLEVEL = \"INFO\"\n\n# Log file (empty string for stdout only)\nFILE = \"tarang.log\"\n\n# Include MPI rank in log messages\nMPI_AWARE = true\n\n# Log to stdout as well as file\nALSO_STDOUT = false\n\n# Timestamp format\nTIMESTAMP_FORMAT = \"yyyy-mm-dd HH:MM:SS\"\n\nProgrammatic logging setup:\n\nusing Tarang.Logging\n\nsetup_tarang_logging(\n    level=\"INFO\",\n    filename=\"simulation.log\",\n    mpi_aware=true,\n    console=true,\n    format_timestamp=true\n)","title":"Logging"},{"category":"section","location":"pages/configuration/#Performance","page":"Configuration","text":"Performance tuning and profiling:\n\n[performance]\n# Enable performance profiling\nENABLE_PROFILING = false\n\n# Directory for profile output\nPROFILE_DIR = \"profiles\"\n\n# Memory pool size for reusable arrays\nMEMORY_POOL_SIZE = \"1GB\"\n\n# Pre-allocate work arrays\nPREALLOCATE_WORK = true\n\n# Number of work arrays per field\nWORK_ARRAY_COUNT = 5","title":"Performance"},{"category":"section","location":"pages/configuration/#Output","page":"Configuration","text":"Default output settings:\n\n[output]\n# Output format: \"netcdf\" (the only supported format)\nDEFAULT_FORMAT = \"netcdf\"\n\n# Compression level (0-9, 0=none, 9=maximum)\nCOMPRESSION_LEVEL = 4\n\n# Chunk size: \"auto\" or explicit dimensions\nCHUNK_SIZE = \"auto\"\n\n# Output precision: \"single\", \"double\"\nPRECISION = \"double\"\n\n# Include metadata\nINCLUDE_METADATA = true","title":"Output"},{"category":"section","location":"pages/configuration/#Environment-Variables","page":"Configuration","text":"Environment variables override configuration file settings:","title":"Environment Variables"},{"category":"section","location":"pages/configuration/#Core-Settings","page":"Configuration","text":"# Log level\nexport TARANG_LOG_LEVEL=DEBUG\n\n# Log file\nexport TARANG_LOG_FILE=my_simulation.log\n\n# Profile directory\nexport TARANG_PROFILE_DIR=my_profiles\n\n# Configuration file location\nexport TARANG_CONFIG=/path/to/tarang.toml","title":"Core Settings"},{"category":"section","location":"pages/configuration/#FFTW-Settings-2","page":"Configuration","text":"# FFTW planning rigor\nexport FFTW_PLANNING_RIGOR=FFTW_MEASURE\n\n# FFTW wisdom file\nexport FFTW_WISDOM_FILE=fftw_wisdom.dat\n\n# FFTW threads (use 1 with MPI)\nexport OMP_NUM_THREADS=1","title":"FFTW Settings"},{"category":"section","location":"pages/configuration/#MPI-Settings","page":"Configuration","text":"# OpenMPI settings\nexport OMPI_MCA_mpi_show_mca_params=1\nexport OMPI_MCA_btl=^openib  # Disable InfiniBand\n\n# Process binding\nexport OMPI_MCA_hwloc_base_binding_policy=core","title":"MPI Settings"},{"category":"section","location":"pages/configuration/#Julia-Settings","page":"Configuration","text":"# Julia threads\nexport JULIA_NUM_THREADS=4\n\n# Optimization level\nexport JULIA_OPT_LEVEL=3\n\n# Disable precompilation\nexport JULIA_PKG_PRECOMPILE_AUTO=0","title":"Julia Settings"},{"category":"section","location":"pages/configuration/#Runtime-Configuration","page":"Configuration","text":"Some settings can be changed at runtime:\n\nusing Tarang.Config\n\n# Get current configuration\nconfig = get_config()\n\n# Modify settings\nconfig.logging.level = \"DEBUG\"\nconfig.transforms.group_transforms = false\n\n# Apply changes\napply_config!(config)","title":"Runtime Configuration"},{"category":"section","location":"pages/configuration/#Example-Configurations","page":"Configuration","text":"","title":"Example Configurations"},{"category":"section","location":"pages/configuration/#Development-(Fast-Startup)","page":"Configuration","text":"[transforms-fftw]\nPLANNING_RIGOR = \"FFTW_ESTIMATE\"  # Fast planning\n\n[logging]\nLEVEL = \"DEBUG\"\nFILE = \"debug.log\"\n\n[performance]\nENABLE_PROFILING = true","title":"Development (Fast Startup)"},{"category":"section","location":"pages/configuration/#Production-(Maximum-Performance)","page":"Configuration","text":"[transforms-fftw]\nPLANNING_RIGOR = \"FFTW_PATIENT\"\nUSE_FFTW_WISDOM = true\n\n[transforms]\nGROUP_TRANSFORMS = true\nDEALIAS_BEFORE_CONVERTING = true\n\n[logging]\nLEVEL = \"INFO\"\n\n[performance]\nMEMORY_POOL_SIZE = \"2GB\"\nPREALLOCATE_WORK = true","title":"Production (Maximum Performance)"},{"category":"section","location":"pages/configuration/#Debugging-(Verbose-Output)","page":"Configuration","text":"[parallelism]\nSYNC_TRANSPOSES = true  # Add barriers for debugging\n\n[logging]\nLEVEL = \"DEBUG\"\nMPI_AWARE = true\nALSO_STDOUT = true\n\n[performance]\nENABLE_PROFILING = true","title":"Debugging (Verbose Output)"},{"category":"section","location":"pages/configuration/#HPC-Cluster","page":"Configuration","text":"[parallelism]\nTRANSPOSE_LIBRARY = \"PENCIL\"\nAUTO_OPTIMIZE_MESH = true\n\n[transforms-fftw]\nPLANNING_RIGOR = \"FFTW_MEASURE\"\nUSE_FFTW_WISDOM = true\nWISDOM_FILE = \"/scratch/user/fftw_wisdom.dat\"\n\n[logging]\nLEVEL = \"INFO\"\nFILE = \"/scratch/user/simulation.log\"\n\n[output]\nDEFAULT_FORMAT = \"netcdf\"\nCOMPRESSION_LEVEL = 6","title":"HPC Cluster"},{"category":"section","location":"pages/configuration/#Configuration-Priority","page":"Configuration","text":"Settings are applied in this order (later overrides earlier):\n\nBuilt-in defaults\nConfiguration file (tarang.toml)\nEnvironment variables\nRuntime API calls\n\nExample:\n\n# File: tarang.toml has LEVEL = \"INFO\"\nexport TARANG_LOG_LEVEL=DEBUG  # Overrides file setting","title":"Configuration Priority"},{"category":"section","location":"pages/configuration/#Best-Practices","page":"Configuration","text":"","title":"Best Practices"},{"category":"section","location":"pages/configuration/#For-Development","page":"Configuration","text":"export OMP_NUM_THREADS=1\nexport FFTW_PLANNING_RIGOR=FFTW_ESTIMATE\nexport TARANG_LOG_LEVEL=DEBUG","title":"For Development"},{"category":"section","location":"pages/configuration/#For-Production","page":"Configuration","text":"[transforms-fftw]\nPLANNING_RIGOR = \"FFTW_MEASURE\"\nUSE_FFTW_WISDOM = true\n\n[performance]\nMEMORY_POOL_SIZE = \"2GB\"\nPREALLOCATE_WORK = true","title":"For Production"},{"category":"section","location":"pages/configuration/#For-Benchmarking","page":"Configuration","text":"[performance]\nENABLE_PROFILING = true\n\n[logging]\nLEVEL = \"INFO\"\nFILE = \"benchmark.log\"","title":"For Benchmarking"},{"category":"section","location":"pages/configuration/#Validation","page":"Configuration","text":"Check your configuration:\n\nusing Tarang.Config\n\n# Load and validate configuration\nconfig = load_config(\"tarang.toml\")\nvalidate_config(config)\n\n# Print current configuration\nprint_config(config)","title":"Validation"},{"category":"section","location":"pages/configuration/#Next-Steps","page":"Configuration","text":"Parallelism Guide: Detailed MPI configuration\nOptimization: Performance tuning\nAnalysis and Output: Output configuration","title":"Next Steps"},{"category":"section","location":"pages/gpu_computing/#GPU-Computing","page":"GPU Computing","text":"Tarang.jl provides comprehensive GPU acceleration through CUDA.jl, enabling significant speedups for spectral simulations on NVIDIA GPUs.","title":"GPU Computing"},{"category":"section","location":"pages/gpu_computing/#Overview","page":"GPU Computing","text":"<div class=\"admonition is-info\">\n<p class=\"admonition-title\">Requirements</p>\n<p>GPU support requires an NVIDIA GPU with CUDA capability 5.0+ and the CUDA.jl package.</p>\n</div>","title":"Overview"},{"category":"section","location":"pages/gpu_computing/#Key-Features","page":"GPU Computing","text":"Feature Description\nAutomatic Dispatch Arrays automatically use GPU kernels when on GPU memory\nCUFFT Integration Optimized FFT plans via NVIDIA's cuFFT library\nCustom Kernels KernelAbstractions.jl for portable CPU/GPU code\nMemory Pools Efficient GPU memory management with pooling\nMulti-GPU MPI + CUDA for distributed GPU computing\nMixed Precision Float32 support for memory-bound problems","title":"Key Features"},{"category":"section","location":"pages/gpu_computing/#Quick-Start","page":"GPU Computing","text":"","title":"Quick Start"},{"category":"section","location":"pages/gpu_computing/#Basic-GPU-Setup","page":"GPU Computing","text":"using Tarang\nusing CUDA\n\n# Check GPU availability\n@assert CUDA.functional() \"CUDA not available\"\n\n# Create distributor with GPU architecture\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist = Distributor(coords; mesh=(1,), dtype=Float64, device=GPU())\n\n# Create bases and fields (automatically on GPU)\nxbasis = Fourier(coords, \"x\", 256)\nybasis = Fourier(coords, \"y\", 256)\nfield = ScalarField(dist, \"u\", (xbasis, ybasis))\n\n# Field data is a CuArray\n@assert field[\"g\"] isa CuArray","title":"Basic GPU Setup"},{"category":"section","location":"pages/gpu_computing/#CPU-vs-GPU-Architecture","page":"GPU Computing","text":"# CPU execution (default)\ndist_cpu = Distributor(coords; device=CPU())\n\n# GPU execution\ndist_gpu = Distributor(coords; device=GPU())\n\n# Check architecture\narch = dist_gpu.architecture  # GPU()","title":"CPU vs GPU Architecture"},{"category":"section","location":"pages/gpu_computing/#GPU-Transforms","page":"GPU Computing","text":"","title":"GPU Transforms"},{"category":"section","location":"pages/gpu_computing/#Automatic-FFT-Acceleration","page":"GPU Computing","text":"When fields are on GPU, transforms automatically use CUFFT:\n\nusing Tarang, CUDA\n\ndist = Distributor(coords; device=GPU())\nfield = ScalarField(dist, \"u\", (xbasis, ybasis))\n\n# Initialize with GPU data\nfield[\"g\"] .= CUDA.rand(Float64, size(field[\"g\"])...)\n\n# Forward transform (uses CUFFT automatically)\nforward_transform!(field)\n\n# Backward transform\nbackward_transform!(field)","title":"Automatic FFT Acceleration"},{"category":"section","location":"pages/gpu_computing/#FFT-Mode-Control","page":"GPU Computing","text":"Control when GPU FFTs are used:\n\n# Per-field control\nset_gpu_fft_mode!(field, :gpu)   # Always use GPU FFT\nset_gpu_fft_mode!(field, :cpu)   # Always use CPU FFT\nset_gpu_fft_mode!(field, :auto)  # Heuristic-based (default)\n\n# Global threshold for :auto mode\n# Use GPU FFT only if array has >= N elements\nset_gpu_fft_min_elements!(64_000)","title":"FFT Mode Control"},{"category":"section","location":"pages/gpu_computing/#Mixed-Fourier-Chebyshev-Transforms","page":"GPU Computing","text":"GPU DCT for Chebyshev bases:\n\n# Mixed basis domain\nxbasis = Fourier(coords, \"x\", 256)      # FFT\nzbasis = ChebyshevT(coords, \"z\", 64)    # DCT\n\ndist = Distributor(coords; device=GPU())\nfield = ScalarField(dist, \"T\", (xbasis, zbasis))\n\n# Transforms automatically select FFT or DCT per dimension\nforward_transform!(field)   # FFT in x, DCT in z\nbackward_transform!(field)  # IFFT in x, IDCT in z","title":"Mixed Fourier-Chebyshev Transforms"},{"category":"section","location":"pages/gpu_computing/#GPU-Memory-Management","page":"GPU Computing","text":"Tarang relies on CUDA.jl's built-in memory pool (following the Oceananigans.jl approach). No custom pooling is needed — CUDA.jl handles allocation efficiently.\n\nConfigure the pool via environment variable:\n\nexport JULIA_CUDA_MEMORY_POOL=binned  # default, efficient for repeated allocations","title":"GPU Memory Management"},{"category":"section","location":"pages/gpu_computing/#Data-Transfers","page":"GPU Computing","text":"# CPU → GPU\nasync_copy_to_gpu!(gpu_array, cpu_array)\n\n# GPU → CPU\nasync_copy_to_cpu!(cpu_array, gpu_array)\n\nFor pinned memory (faster MPI transfers), use CUDA.jl directly:\n\nCUDA.Mem.pin(cpu_array)  # page-lock for faster DMA transfers","title":"Data Transfers"},{"category":"section","location":"pages/gpu_computing/#Custom-GPU-Kernels","page":"GPU Computing","text":"","title":"Custom GPU Kernels"},{"category":"section","location":"pages/gpu_computing/#KernelAbstractions-Integration","page":"GPU Computing","text":"Write portable kernels that run on both CPU and GPU:\n\nusing Tarang, KernelAbstractions\n\n# Define a kernel\n@kernel function add_kernel!(c, @Const(a), @Const(b))\n    i = @index(Global)\n    @inbounds c[i] = a[i] + b[i]\nend\n\n# Wrap as KernelOperation\nadd_op = KernelOperation(add_kernel!) do c, a, b\n    length(c)  # ndrange\nend\n\n# Use on any architecture\narch = GPU()\na = ones(arch, Float64, 1024)\nb = ones(arch, Float64, 1024)\nc = zeros(arch, Float64, 1024)\n\nadd_op(arch, c, a, b)  # Runs on GPU","title":"KernelAbstractions Integration"},{"category":"section","location":"pages/gpu_computing/#Built-in-GPU-Kernels","page":"GPU Computing","text":"Tarang provides optimized kernels for common operations:\n\nusing TarangCUDAExt\n\n# Element-wise operations\ngpu_add!(c, a, b)           # c = a + b\ngpu_sub!(c, a, b)           # c = a - b\ngpu_mul!(c, a, b)           # c = a * b\ngpu_scale!(y, α, x)         # y = α * x\ngpu_axpy!(y, α, x)          # y = y + α * x\ngpu_linear_combination!(y, α, a, β, b)  # y = α*a + β*b\n\n# Fused operations for timestepping\ngpu_rk_stage!(u_new, u, k, dt, coeff)\ngpu_axpby!(y, α, x, β)      # y = α*x + β*y\n\n# Physics kernels\ngpu_kinetic_energy_2d!(ke, ux, uy)\ngpu_kinetic_energy_3d!(ke, ux, uy, uz)\ngpu_viscous_damping!(field, ν, k2)","title":"Built-in GPU Kernels"},{"category":"section","location":"pages/gpu_computing/#Distributed-GPU-Computing","page":"GPU Computing","text":"","title":"Distributed GPU Computing"},{"category":"section","location":"pages/gpu_computing/#MPI-CUDA","page":"GPU Computing","text":"For multi-GPU simulations:\n\nusing Tarang, MPI, CUDA\n\nMPI.Init()\n\ncomm = MPI.COMM_WORLD\nrank = MPI.Comm_rank(comm)\n\n# Assign GPU to MPI rank\nif CUDA.ndevices() >= MPI.Comm_size(comm)\n    CUDA.device!(rank)\nend\n\n# Create distributed GPU setup\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist = Distributor(coords; mesh=(2, 2), device=GPU())\n\n# Each rank has its own GPU memory\nfield = ScalarField(dist, \"u\", bases)","title":"MPI + CUDA"},{"category":"section","location":"pages/gpu_computing/#CUDA-Aware-MPI","page":"GPU Computing","text":"For direct GPU-to-GPU communication:\n\nusing TarangCUDAExt: check_cuda_aware_mpi\n\nif check_cuda_aware_mpi()\n    println(\"CUDA-aware MPI available - using direct GPU transfers\")\nelse\n    println(\"Staging through CPU for MPI transfers\")\nend","title":"CUDA-Aware MPI"},{"category":"section","location":"pages/gpu_computing/#TransposableField-for-GPUMPI","page":"GPU Computing","text":"Efficient distributed FFTs with GPU:\n\n# Create transposable field for distributed transforms\nfield = ScalarField(dist, \"u\", bases)\ntf = TransposableField(field)\n\n# Distributed forward transform (handles GPU transposes)\ndistributed_forward_transform!(tf)\n\n# Distributed backward transform\ndistributed_backward_transform!(tf)","title":"TransposableField for GPU+MPI"},{"category":"section","location":"pages/gpu_computing/#Performance-Optimization","page":"GPU Computing","text":"","title":"Performance Optimization"},{"category":"section","location":"pages/gpu_computing/#Best-Practices","page":"GPU Computing","text":"Use Float32 when possible - 2x memory bandwidth, often sufficient accuracy\ndist = Distributor(coords; dtype=Float32, device=GPU())\nBatch operations - Minimize kernel launches\n# Bad: many small operations\nfor i in 1:n\n    gpu_scale!(field, α)\nend\n\n# Good: fused operations\ngpu_rk_stage!(u_new, u, k, dt, coeff)\nPreallocate buffers - Avoid allocation in hot loops\n# Preallocate work arrays\nwork = zeros(GPU(), Float64, size(field[\"g\"]))\n\nfor step in 1:nsteps\n    # Reuse work array\n    compute!(work, field)\nend\nSynchronize when needed - Ensure GPU operations complete before CPU access\nCUDA.synchronize()  # Wait for all GPU operations to finish","title":"Best Practices"},{"category":"section","location":"pages/gpu_computing/#Profiling","page":"GPU Computing","text":"using CUDA\n\n# Profile a section\nCUDA.@profile begin\n    forward_transform!(field)\n    backward_transform!(field)\nend\n\n# Time with synchronization\nCUDA.@elapsed begin\n    forward_transform!(field)\n    CUDA.synchronize()\nend","title":"Profiling"},{"category":"section","location":"pages/gpu_computing/#Memory-Bandwidth-Optimization","page":"GPU Computing","text":"# Check if transform is memory-bound\nn = prod(size(field[\"g\"]))\nbytes_transferred = n * sizeof(eltype(field[\"g\"])) * 4  # rough estimate\n\n# Theoretical bandwidth (e.g., A100 = 2 TB/s)\ntheoretical_time = bytes_transferred / 2e12\n\n# Compare to actual time\nactual_time = CUDA.@elapsed forward_transform!(field)\nefficiency = theoretical_time / actual_time\nprintln(\"Memory bandwidth efficiency: $(efficiency * 100)%\")","title":"Memory Bandwidth Optimization"},{"category":"section","location":"pages/gpu_computing/#Tensor-Core-Support","page":"GPU Computing","text":"For supported operations on Ampere+ GPUs:\n\nusing TarangCUDAExt: enable_tensor_cores!, disable_tensor_cores!\n\n# Enable tensor cores (requires compatible data types)\nenable_tensor_cores!()\n\n# Disable if numerical precision is critical\ndisable_tensor_cores!()","title":"Tensor Core Support"},{"category":"section","location":"pages/gpu_computing/#Troubleshooting","page":"GPU Computing","text":"","title":"Troubleshooting"},{"category":"section","location":"pages/gpu_computing/#Common-Issues","page":"GPU Computing","text":"Out of Memory\n\n# Check available memory before large allocations\ninfo = gpu_memory_info()\nif info.free_bytes < required_bytes\n    clear_memory_pool!()  # Free cached allocations\n    GC.gc()               # Trigger garbage collection\n    CUDA.reclaim()        # Reclaim CUDA memory\nend\n\nSlow Performance\n\n# Ensure synchronization isn't killing performance\nCUDA.allowscalar(false)  # Disable slow scalar indexing\n\n# Check for CPU fallbacks\n@assert field[\"g\"] isa CuArray \"Data not on GPU!\"\n\nMPI + CUDA Issues\n\n# Ensure correct GPU assignment\nprintln(\"Rank $rank using GPU $(CUDA.device())\")\n\n# Force synchronization before MPI calls\nCUDA.synchronize()\nMPI.Barrier(comm)","title":"Common Issues"},{"category":"section","location":"pages/gpu_computing/#API-Reference","page":"GPU Computing","text":"See the GPU API documentation for complete function references.","title":"API Reference"},{"category":"section","location":"pages/gpu_computing/#Key-Functions","page":"GPU Computing","text":"Function Description\nGPU() GPU architecture singleton\non_architecture(GPU(), array) Move array to GPU\nforward_transform!(field) GPU-accelerated forward FFT\nbackward_transform!(field) GPU-accelerated inverse FFT\nset_gpu_fft_mode!(field, mode) Control FFT backend\nTransposableField(field) Distributed GPU transforms","title":"Key Functions"},{"category":"section","location":"tutorials/surface_dynamics/#Surface-and-Boundary-Dynamics","page":"Surface Dynamics","text":"This tutorial covers solving advection-diffusion equations on domain boundaries, including Surface Quasi-Geostrophic (SQG) dynamics and general boundary-coupled systems.","title":"Surface and Boundary Dynamics"},{"category":"section","location":"tutorials/surface_dynamics/#Overview","page":"Surface Dynamics","text":"Many physical systems involve dynamics confined to surfaces or boundaries:\n\nSurface Quasi-Geostrophic (SQG): Ocean/atmosphere surface temperature dynamics\nPassive tracers: Concentration advected by prescribed flow\nReactive surfaces: Chemical species on catalytic surfaces\n\nTarang provides a flexible BoundaryAdvectionDiffusion framework for these cases.\n\nwarning: Interior coupling is not functional\nThe framework also declares an interior-coupled mode (InteriorDerivedVelocity + interior_coupling=), intended for full QG with a 3D streamfunction inversion, along with a dedicated QGSystem. Neither currently runs; the sections below give the exact failures. Everything else on this page is verified working, in serial and under MPI.","title":"Overview"},{"category":"section","location":"tutorials/surface_dynamics/#The-Advection-Diffusion-Equation","page":"Surface Dynamics","text":"The general equation solved on each boundary is:\n\n∂c/∂t + u·∇c = D(c) + S\n\nwhere:\n\nc is the scalar field (buoyancy, concentration, temperature)\nu is the advection velocity\nD(c) is the diffusion operator\nS is an optional source term\n\nThe whole system is stepped with explicit timesteppers (bad_step!); there is no implicit solve in this module.","title":"The Advection-Diffusion Equation"},{"category":"section","location":"tutorials/surface_dynamics/#Velocity-Sources","page":"Surface Dynamics","text":"The key difference between problems is how the velocity u is obtained.","title":"Velocity Sources"},{"category":"section","location":"tutorials/surface_dynamics/#Self-Derived-Velocity-(SQG-like)","page":"Surface Dynamics","text":"Velocity computed from the boundary field itself through an inversion:\n\nψ = (-Δ)^α c      (fractional Laplacian inversion)\nu = ∇⊥ψ           (perpendicular gradient for incompressibility)\n\nFor SQG, α = -1/2, giving ψ = (-Δ)^(-1/2) θ.\n\nusing Tarang\n\n# SQG setup: velocity from buoyancy inversion\nbad = boundary_advection_diffusion_setup(\n    Lx=2π, Ly=2π,\n    Nx=64, Ny=64,\n    boundaries=[BoundarySpec(\"theta\", :z, 0.0)],\n    velocity_source=SelfDerivedVelocity(\n        inversion_exponent=-0.5,  # SQG: ψ = (-Δ)^(-1/2) θ\n        use_perp_grad=true        # u = ∇⊥ψ (incompressible)\n    ),\n    diffusion=DiffusionSpec(\n        type=:fractional,\n        coefficient=1e-4,\n        exponent=0.5              # Physical SQG dissipation\n    )\n)\n\nThis is the path exercised by the complete SQG example below.","title":"Self-Derived Velocity (SQG-like)"},{"category":"section","location":"tutorials/surface_dynamics/#Interior-Derived-Velocity","page":"Surface Dynamics","text":"The intent of InteriorDerivedVelocity(:perp_grad) together with interior_coupling= is a full QG system: the boundary fields provide Neumann boundary conditions for a 3D elliptic problem, and the velocity is ∇⊥ψ evaluated at each surface.\n\nThis mode does not currently work. boundary_advection_diffusion_setup builds the interior LBVP without installing any boundary conditions on it, so the first bad_step! — which solves that LBVP — fails validation:\n\nArgumentError: Problem validation failed:\nBoundary value problem requires boundary conditions\n\nDo not use interior_coupling= until that is fixed. For surface-only QG dynamics, use SelfDerivedVelocity (the SQG limit), which is fully functional.","title":"Interior-Derived Velocity"},{"category":"section","location":"tutorials/surface_dynamics/#Prescribed-Velocity-(Passive-Tracer)","page":"Surface Dynamics","text":"Velocity set externally by the user each timestep:\n\n# Passive tracer in prescribed flow\ntracer = boundary_advection_diffusion_setup(\n    Lx=2π, Ly=2π,\n    Nx=64, Ny=64,\n    boundaries=[BoundarySpec(\"concentration\", :z, 0.0)],\n    velocity_source=PrescribedVelocity(),\n    diffusion=DiffusionSpec(type=:laplacian, coefficient=0.01)\n)\n\n# Set the velocity before each step. `local_grids` returns this rank's slice of each\n# axis, so the same code is correct in serial and under MPI.\nfunction set_velocity!(bad, t)\n    x, y = local_grids(bad.dist, bad.bases...)\n    u = bad.velocities[\"concentration\"]\n    ensure_layout!(u.components[1], :g)\n    ensure_layout!(u.components[2], :g)\n    get_grid_data(u.components[1]) .= -sin.(y') .* ones(length(x))\n    get_grid_data(u.components[2]) .=  sin.(x)  .* ones(length(y))'\nend\n\nc = tracer.fields[\"concentration\"]\nensure_layout!(c, :g)\nx, y = local_grids(tracer.dist, tracer.bases...)\nget_grid_data(c) .= exp.(-((x .- π).^2 .+ (y' .- π).^2))\n\nfor step in 1:10\n    set_velocity!(tracer, tracer.time)\n    bad_step!(tracer, 0.005)\nend\n\nAfter 10 steps this gives bad_energy(tracer) = 0.0397, bad_max_velocity(tracer) = √2 (the rotating flow above has |u| ≤ √2).","title":"Prescribed Velocity (Passive Tracer)"},{"category":"section","location":"tutorials/surface_dynamics/#Field-access","page":"Surface Dynamics","text":"A BoundaryAdvectionDiffusion exposes its state through plain dictionaries:\n\naccessor contents\nbad.fields[name] the ScalarField c on boundary name\nbad.velocities[name] the VectorField u on boundary name\nbad.dist, bad.bases, bad.coords distributor, (x_basis, y_basis), coordinates\nbad.params \"Lx\", \"Ly\", \"κ\", \"α\"\nbad.time, bad.iteration simulation time and step count\n\nGrid coordinates come from local_grids(bad.dist, bad.bases...), which returns each rank's local x and y vectors. Broadcast them (x against y') rather than building a global meshgrid — that keeps the code correct under MPI.","title":"Field access"},{"category":"section","location":"tutorials/surface_dynamics/#Diffusion-Types","page":"Surface Dynamics","text":"","title":"Diffusion Types"},{"category":"section","location":"tutorials/surface_dynamics/#Standard-Laplacian","page":"Surface Dynamics","text":"DiffusionSpec(type=:laplacian, coefficient=0.01)\n# Gives: κΔc","title":"Standard Laplacian"},{"category":"section","location":"tutorials/surface_dynamics/#Fractional-Laplacian","page":"Surface Dynamics","text":"DiffusionSpec(type=:fractional, coefficient=1e-4, exponent=0.5)\n# Gives: -κ(-Δ)^α c\n\nCommon exponents:\n\nα = 0.5: Physical SQG dissipation\nα = 1.0: Standard Laplacian (equivalent to :laplacian)\nα = 2.0: Biharmonic (hyperviscosity)","title":"Fractional Laplacian"},{"category":"section","location":"tutorials/surface_dynamics/#Hyperdiffusion","page":"Surface Dynamics","text":"DiffusionSpec(type=:hyperdiffusion, coefficient=1e-8, exponent=2.0)\n# Gives: -κ(-Δ)^n c (typically n=2 for biharmonic)\n\n:hyperdiffusion and :fractional share the same implementation; the two names exist only to document intent.","title":"Hyperdiffusion"},{"category":"section","location":"tutorials/surface_dynamics/#No-Diffusion","page":"Surface Dynamics","text":"DiffusionSpec(type=:none)\n# Inviscid dynamics\n\nnote: `implicit=true` is applied explicitly\nDiffusionSpec accepts an implicit flag, but this module has only explicit timesteppers. Setting implicit=true emits a warning and integrates the diffusion term explicitly anyway. Choose dt accordingly, or use the main solver framework (IVP + InitialValueSolver) if you need a true implicit diffusion solve.","title":"No Diffusion"},{"category":"section","location":"tutorials/surface_dynamics/#Complete-SQG-Example","page":"Surface Dynamics","text":"using Tarang\n\n# Domain and resolution\nLx, Ly = 2π, 2π\nNx, Ny = 64, 64\n\n# Create SQG system\nsqg = boundary_advection_diffusion_setup(\n    Lx=Lx, Ly=Ly,\n    Nx=Nx, Ny=Ny,\n    boundaries=[BoundarySpec(\"theta\", :z, 0.0)],\n    velocity_source=SelfDerivedVelocity(inversion_exponent=-0.5),\n    diffusion=DiffusionSpec(type=:fractional, coefficient=1e-4, exponent=0.5)\n)\n\n# Initial condition: an elliptical vortex\nθ = sqg.fields[\"theta\"]\nensure_layout!(θ, :g)\nx, y = local_grids(sqg.dist, sqg.bases...)\nget_grid_data(θ) .= exp.(-((x .- π).^2 ./ 0.5 .+ (y' .- π).^2 ./ 0.3))\n\n# Time stepping\ndt = 0.001\nnsteps = 200\noutput_interval = 100\n\nfor step in 1:nsteps\n    bad_step!(sqg, dt; timestepper=:RK4)\n\n    if step % output_interval == 0\n        energy = bad_energy(sqg)\n        max_vel = bad_max_velocity(sqg)\n        println(\"Step $step: E = $energy, max|u| = $max_vel\")\n    end\nend\n\nOutput:\n\nStep 100: E = 0.015409487302486865, max|u| = 0.47549001329457397\nStep 200: E = 0.015408863536076695, max|u| = 0.4755762567027247\n\nRaise Nx, Ny and nsteps for production runs; the numbers above are just a quick, reproducible check that the vortex is being advected and slowly dissipated.","title":"Complete SQG Example"},{"category":"section","location":"tutorials/surface_dynamics/#Multiple-Boundaries","page":"Surface Dynamics","text":"A setup may carry several boundaries. Each gets its own field, its own velocity, and its own advection-diffusion equation, and bad_step! advances them all together:\n\ntwo = boundary_advection_diffusion_setup(\n    Lx=2π, Ly=2π,\n    Nx=32, Ny=32,\n    boundaries=[\n        BoundarySpec(\"theta_bot\", :z, 0.0),\n        BoundarySpec(\"theta_top\", :z, 1.0)\n    ],\n    velocity_source=SelfDerivedVelocity(inversion_exponent=-0.5),\n    diffusion=DiffusionSpec(type=:fractional, coefficient=1e-4, exponent=0.5)\n)\n\n# Different initial condition on each surface\nx, y = local_grids(two.dist, two.bases...)\nfor (name, amp) in ((\"theta_bot\", 1.0), (\"theta_top\", 0.5))\n    f = two.fields[name]\n    ensure_layout!(f, :g)\n    get_grid_data(f) .= amp .* exp.(-((x .- π).^2 .+ (y' .- π).^2) ./ 0.3)\nend\n\nfor step in 1:50\n    bad_step!(two, 0.001)\nend\n\nbad_energy(two)                      # 0.00746 — pooled over both surfaces\nbad_enstrophy(two, \"theta_bot\")      # 0.0796 — one named surface at a time\nbad_enstrophy(two, \"theta_top\")      # 0.0199\n\nWith SelfDerivedVelocity each surface inverts its own field, so the two surfaces evolve independently — they are two SQG problems sharing a timestepper. Coupling them through a shared interior is what interior_coupling= is for, and that path is not functional (see above).\n\nBoundarySpec also takes a field_name keyword, which renames the underlying field without changing the dictionary key: BoundarySpec(\"theta_bot\", :z, 0.0; field_name=\"θ_bottom\") is still reached as bad.fields[\"theta_bot\"].","title":"Multiple Boundaries"},{"category":"section","location":"tutorials/surface_dynamics/#Adding-Source-Terms","page":"Surface Dynamics","text":"A source function receives the system and the boundary name, and returns an array shaped like that rank's grid slab:\n\n# Gaussian forcing centered at (π, π)\nfunction my_forcing(bad, boundary_name)\n    x, y = local_grids(bad.dist, bad.bases...)\n    return 0.1 .* exp.(-((x .- π).^2 .+ (y' .- π).^2) ./ 0.5)\nend\n\nbad_add_source!(sqg, \"theta\", my_forcing)\n\nThe array is added to the RHS of that boundary's equation on every stage of every step. The function is re-evaluated each time, so it may depend on bad.time for time-dependent forcing — but note that bad.time is only advanced after the step completes, so all stages within one step see the same t. The forcing is first-order accurate in time.","title":"Adding Source Terms"},{"category":"section","location":"tutorials/surface_dynamics/#Available-Timesteppers","page":"Surface Dynamics","text":"Method Order Description\n:Euler 1 Forward Euler\n:RK2 2 Midpoint method\n:RK4 4 Classical Runge-Kutta\n:SSPRK3 3 Strong Stability Preserving RK3\n\nbad_step!(sqg, dt; timestepper=:SSPRK3)\n\n:RK4 is the default. Anything else throws ArgumentError: Unknown timestepper.","title":"Available Timesteppers"},{"category":"section","location":"tutorials/surface_dynamics/#Diagnostics","page":"Surface Dynamics","text":"All four reduce correctly under MPI (they perform their own Allreduce) — do not wrap them in a collective yourself. The corollary is that they are collective calls: every rank must reach them, so never hide one behind a rank guard.\n\nrank = MPI.Comm_rank(MPI.COMM_WORLD)\n\n# WRONG — rank 0 enters the Allreduce alone. The ranks desynchronize: the job either\n# hangs forever, or aborts once another rank reaches a different collective\n#   (Fatal error in internal_Allreduce: Message truncated).\nif rank == 0\n    println(\"E = \", bad_energy(sqg))\nend\n\n# RIGHT — all ranks compute, one rank prints\nE = bad_energy(sqg)\nrank == 0 && println(\"E = \", E)","title":"Diagnostics"},{"category":"section","location":"tutorials/surface_dynamics/#Energy","page":"Surface Dynamics","text":"E = bad_energy(sqg)  # mean square of the boundary fields, averaged over all boundaries","title":"Energy"},{"category":"section","location":"tutorials/surface_dynamics/#Enstrophy","page":"Surface Dynamics","text":"Z = bad_enstrophy(sqg, \"theta\")  # mean squared gradient of one named field","title":"Enstrophy"},{"category":"section","location":"tutorials/surface_dynamics/#Maximum-Velocity","page":"Surface Dynamics","text":"umax = bad_max_velocity(sqg)","title":"Maximum Velocity"},{"category":"section","location":"tutorials/surface_dynamics/#CFL-Timestep","page":"Surface Dynamics","text":"dt_cfl = bad_cfl_dt(sqg; safety=0.5)   # safety * min(dx, dy) / max|u|\n\nbad_cfl_dt returns Inf when the velocity is identically zero, so cap it before use.","title":"CFL Timestep"},{"category":"section","location":"tutorials/surface_dynamics/#Adaptive-Timestepping","page":"Surface Dynamics","text":"dt_max = 0.01\n\nfor step in 1:nsteps\n    # CFL-limited timestep, capped from above\n    dt_next = min(bad_cfl_dt(sqg; safety=0.4), dt_max)\n\n    bad_step!(sqg, dt_next)\nend\n\nsqg.time        # current simulation time\nsqg.iteration   # number of completed steps","title":"Adaptive Timestepping"},{"category":"section","location":"tutorials/surface_dynamics/#MPI-Parallelism","page":"Surface Dynamics","text":"The SQG / prescribed-velocity paths run under MPI unchanged. The boundary domain is 2-dimensional, so it takes a slab decomposition: pass a mesh with a unit factor, such as (nprocs, 1). A genuinely 2-D mesh like (2, 2) decomposes both axes, leaves no local axis for the FFT, and fails hard with PencilFFT plan creation failed with 4 MPI processes.\n\nusing Tarang\nusing MPI\nMPI.Initialized() || MPI.Init()\nnp = MPI.Comm_size(MPI.COMM_WORLD)\n\nsqg = boundary_advection_diffusion_setup(\n    Lx=2π, Ly=2π,\n    Nx=64, Ny=64,\n    boundaries=[BoundarySpec(\"theta\", :z, 0.0)],\n    velocity_source=SelfDerivedVelocity(inversion_exponent=-0.5),\n    diffusion=DiffusionSpec(type=:fractional, coefficient=1e-4, exponent=0.5),\n    mesh=(np, 1)          # slab decomposition over `np` processes\n)\n\nθ = sqg.fields[\"theta\"]\nensure_layout!(θ, :g)\nx, y = local_grids(sqg.dist, sqg.bases...)   # this rank's slice of each axis\nget_grid_data(θ) .= exp.(-((x .- π).^2 ./ 0.5 .+ (y' .- π).^2 ./ 0.3))\n\nfor step in 1:100\n    bad_step!(sqg, 0.001; timestepper=:RK4)\nend\n\n# bad_energy / bad_max_velocity do the MPI reduction internally\nprintln(\"E = \", bad_energy(sqg), \"  max|u| = \", bad_max_velocity(sqg))\n\nRun with:\n\nmpiexec -n 4 julia --project=. my_sqg_simulation.jl\n\nThe energy is bit-identical at 1, 2 and 4 processes (E = 0.015409487302486865), matching the serial run above.\n\nTwo rules to keep the distributed version correct:\n\nBuild initial conditions from local_grids(...), never from a global range/meshgrid.\nNever wrap bad_energy / bad_enstrophy / bad_max_velocity / bad_cfl_dt in an MPI.Allreduce; they already reduce globally.","title":"MPI Parallelism"},{"category":"section","location":"tutorials/surface_dynamics/#The-dedicated-QGSystem","page":"Surface Dynamics","text":"qg_system_setup, qg_step!, qg_invert! and qg_energy are exported and are intended to provide full QG with interior PV inversion. They do not currently work. qg_system_setup builds the interior inversion problem with parameters written directly into problem.parameters (invisible to the equation parser, which warns Unknown variable: S / N / f0) and declares the surface Neumann conditions with add_equation! instead of add_bc!. The result is a non-square operator (1440×1152 at Nx=Ny=16, Nz=8) and qg_step! throws DimensionMismatch.\n\nUse the BoundaryAdvectionDiffusion framework with SelfDerivedVelocity — the SQG limit of QG — until this is repaired.","title":"The dedicated QGSystem"},{"category":"section","location":"tutorials/surface_dynamics/#Summary","page":"Surface Dynamics","text":"Use Case Velocity Source Status\nSQG (surface buoyancy) SelfDerivedVelocity works, serial and MPI\nPassive tracer in a known flow PrescribedVelocity works, serial and MPI\nQG (coupled surface + interior) InteriorDerivedVelocity not functional","title":"Summary"},{"category":"section","location":"tutorials/surface_dynamics/#See-Also","page":"Surface Dynamics","text":"Operators: Fractional Laplacian details\nProblems: IVP and LBVP setup\nSolvers: Timestepping and BVP solvers","title":"See Also"},{"category":"section","location":"#Tarang.jl","page":"Home","text":"<div style=\"background: linear-gradient(135deg, #2563eb 0%, #7c3aed 100%); color: white; padding: 2.5rem; border-radius: 16px; margin-bottom: 2rem; text-align: center;\">\n    <h1 style=\"font-size: 2.5rem; margin: 0 0 0.5rem 0; color: white; border: none;\">Tarang.jl</h1>\n    <p style=\"font-size: 1.25rem; margin: 0; opacity: 0.95;\">A High-Performance Spectral PDE Solver for Julia</p>\n    <p style=\"margin-top: 1rem; font-size: 0.9rem; opacity: 0.8;\">CPU | GPU | MPI Distributed | Symbolic Equations</p>\n</div>\n\n(Image: Build Status) (Image: Documentation) (Image: License: MIT)\n\n","title":"Tarang.jl"},{"category":"section","location":"#Features","page":"Home","text":"<div style=\"display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.5rem; margin: 2rem 0;\">\n\n<div style=\"background: white; border: 1px solid #e2e8f0; border-radius: 12px; padding: 1.5rem; transition: transform 0.2s, box-shadow 0.2s;\">\n    <h3 style=\"color: #2563eb; margin-top: 0;\">Spectral Methods</h3>\n    <ul style=\"margin: 0; padding-left: 1.25rem; color: #475569;\">\n        <li>Fourier, Chebyshev, and Legendre bases</li>\n        <li>Spectral accuracy for smooth solutions</li>\n        <li>Automatic differentiation operators</li>\n    </ul>\n</div>\n\n<div style=\"background: white; border: 1px solid #e2e8f0; border-radius: 12px; padding: 1.5rem;\">\n    <h3 style=\"color: #7c3aed; margin-top: 0;\">GPU Acceleration</h3>\n    <ul style=\"margin: 0; padding-left: 1.25rem; color: #475569;\">\n        <li>Native CUDA support with cuFFT</li>\n        <li>KernelAbstractions.jl backend</li>\n        <li>Automatic CPU/GPU dispatch</li>\n    </ul>\n</div>\n\n<div style=\"background: white; border: 1px solid #e2e8f0; border-radius: 12px; padding: 1.5rem;\">\n    <h3 style=\"color: #059669; margin-top: 0;\">MPI Parallelization</h3>\n    <ul style=\"margin: 0; padding-left: 1.25rem; color: #475569;\">\n        <li>PencilArrays pencil decomposition</li>\n        <li>Scalable to thousands of cores</li>\n        <li>Efficient distributed FFTs</li>\n    </ul>\n</div>\n\n<div style=\"background: white; border: 1px solid #e2e8f0; border-radius: 12px; padding: 1.5rem;\">\n    <h3 style=\"color: #dc2626; margin-top: 0;\">Symbolic Equations</h3>\n    <ul style=\"margin: 0; padding-left: 1.25rem; color: #475569;\">\n        <li>Natural mathematical syntax for PDEs</li>\n        <li>Automatic operator construction</li>\n        <li>Flexible boundary conditions</li>\n    </ul>\n</div>\n\n</div>\n\n","title":"Features"},{"category":"section","location":"#Quick-Start","page":"Home","text":"","title":"Quick Start"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url=\"https://github.com/subhk/Tarang.jl\")\n\nThat is the whole installation. MPI, PencilArrays, PencilFFTs and KernelAbstractions are hard dependencies — they are installed with Tarang, and distributed runs work out of the box.\n\nGPU support is the one opt-in: CUDA is a weak dependency loaded through a package extension, so add it only if you want it.\n\nPkg.add(\"CUDA\")     # enables the GPU backend (TarangCUDAExt)","title":"Installation"},{"category":"section","location":"#1D-Diffusion","page":"Home","text":"using Tarang\n\ndomain = PeriodicDomain(64)                     # 64-point periodic [0, 2π]\nT = ScalarField(domain, \"T\")                    # Temperature field\n\nproblem = IVP([T])\nadd_parameters!(problem, kappa=0.01)\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = 0\")\n\nset!(T, x -> sin(x))                            # Initial condition\nsolver = InitialValueSolver(problem, RK222(); dt=0.01)\nrun!(solver; stop_time=1.0)                     # That's it!\n\nThe single mode sin(x) decays as exp(-κt); after t = 1 the solver gives max|T| = 0.99004983 against the exact 0.99004983 — a relative error of 4e-12.","title":"1D Diffusion"},{"category":"section","location":"#2D-Rayleigh-Benard-Convection","page":"Home","text":"A bounded (Chebyshev) direction needs the tau method: one tau variable per boundary condition, lifted into the equations. See The Tau Method for Boundary Conditions for the why.\n\nusing Tarang\n\nLx, Lz = 4.0, 1.0\nNx, Nz = 64, 32\nRayleigh, Prandtl = 2e4, 1.0\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nzbasis = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\np = ScalarField(domain, \"p\")                   # Pressure\nT = ScalarField(domain, \"T\")                   # Temperature\nu = VectorField(domain, \"u\")                   # Velocity\n\n# One tau per boundary condition, carrying the Fourier bases (one per x-mode).\ntau_p  = ScalarField(dist, \"tau_p\",  (), Float64)          # pressure gauge\ntau_T1 = ScalarField(dist, \"tau_T1\", (xbasis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (xbasis,), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis,), Float64)\n\n# First-order reduction: grad_X = ∇X + ẑ·lift(τ)\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_T = grad(T) + ez * τ_lift(tau_T1)\n\nproblem = IVP([p, T, u, tau_p, tau_T1, tau_T2, tau_u1, tau_u2])\nadd_parameters!(problem, nu=Prandtl, buoy=Rayleigh*Prandtl, ez=ez,\n                grad_u=grad_u, grad_T=grad_T, τ_lift=τ_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")                                  # continuity\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) - buoy*T*ez + τ_lift(tau_u2) = -u⋅∇(u)\")\n\n# Boundary conditions go through add_bc!, never add_equation!.\nadd_bc!(problem, \"T(z=0) = 1\")                 # hot bottom\nadd_bc!(problem, \"T(z=$Lz) = 0\")               # cold top\nadd_bc!(problem, \"u(z=0) = 0\")                 # no-slip\nadd_bc!(problem, \"u(z=$Lz) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")               # pressure gauge\n\n# Conduction profile 1 - z, plus wall-damped noise to seed convection.\nx, z = local_grids(dist, xbasis, zbasis)\nfill_random!(T, \"g\"; seed=42, distribution=\"normal\", scale=1e-3)\nget_grid_data(T) .*= z' .* (1.0 .- z')\nget_grid_data(T) .+= 1.0 .- z'\nensure_layout!(T, :c)\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)\ndiagnose(solver)                               # Print solver summary\nrun!(solver; stop_time=0.1, log_interval=100)\n\nTime is measured in thermal diffusion times, so buoy = Ra·Pr and nu = Pr. Over those first 1000 steps the seeded noise grows into rolls (max|u_z| reaches 3.76) while the walls stay pinned to machine precision: max|T(z=0) − 1| = 3.6e-15, max|T(z=Lz)| = 8.1e-15.\n\nwarning: A fixed `dt` will not survive a stiff Rayleigh number\nThis is a Quick Start, not a production run. Push Rayleigh to 2e6 with the same fixed dt=1e-4 and the velocities outrun the CFL limit — the run goes to NaN within ~100 steps, at 64×32 and at 128×64. Real runs let a CFL controller choose dt (run!(solver; cfl=cfl, ...)); see examples/ivp/rayleigh_benard_2d.jl for the full 256×64, Ra = 2e6 version.\n\nwarning: Boundary-condition strings are parsed by Tarang, not by Julia\nadd_bc!(problem, \"T(z=Lz) = 0\") does not work: the parser resolves names against the problem's variables and add_parameters! entries, never your script's globals. A bare global warns (Unknown variable: Lz) and then fails the matrix build outright. Use a literal (\"T(z=1) = 0\") or interpolate the value in (\"T(z=$Lz) = 0\") — both enforce the boundary exactly.\n\nnote: This example is serial\nTwo things here are serial-only. Under MPI the Chebyshev axis must come first (Domain(dist, (zbasis, xbasis))) because a decomposed axis cannot hold a Chebyshev transform — and per-mode tau fields ((xbasis,)) need the Fourier axis first, so distributed runs use bare () taus. -u⋅∇(T) also puts a Chebyshev derivative on the explicit side, which the distributed solver rejects. Pure-Fourier problems parallelize with no changes at all; see Running with MPI.\n\ntip: Pro Tip\nUse diagnose(solver) at any time to inspect field layout, compiled RHS status, dealiasing mode, and memory usage.\n\n","title":"2D Rayleigh-Benard Convection"},{"category":"section","location":"#GPU-Example","page":"Home","text":"using Tarang, CUDA\n\n# Just add device=GPU() — everything else stays the same\ndomain = PeriodicDomain(512, 512; device=GPU(), dtype=Float32)\nfield = ScalarField(domain, \"u\")\nforward_transform!(field)   # Uses cuFFT automatically\n\ndevice is the only line that changes: the same script with device=CPU() runs on the CPU, and dtype=Float32 gives ComplexF32 coefficients either way. GPU() needs CUDA.jl loaded — without it the constructor throws rather than silently falling back.\n\n","title":"GPU Example"},{"category":"section","location":"#Scientific-Applications","page":"Home","text":"<div style=\"display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1rem; margin: 1.5rem 0;\">\n\n<div style=\"background: #eff6ff; border-radius: 8px; padding: 1rem; border-left: 4px solid #2563eb;\">\n    <strong style=\"color: #1e40af;\">Fluid Dynamics</strong>\n    <p style=\"margin: 0.5rem 0 0 0; font-size: 0.9rem; color: #475569;\">\n        Navier-Stokes, Rayleigh-Benard convection, channel flow, jets\n    </p>\n</div>\n\n<div style=\"background: #fef3c7; border-radius: 8px; padding: 1rem; border-left: 4px solid #f59e0b;\">\n    <strong style=\"color: #92400e;\">Turbulence</strong>\n    <p style=\"margin: 0.5rem 0 0 0; font-size: 0.9rem; color: #475569;\">\n        LES models (Smagorinsky, AMD), stochastic forcing, GQL approximation\n    </p>\n</div>\n\n<div style=\"background: #f3e8ff; border-radius: 8px; padding: 1rem; border-left: 4px solid #7c3aed;\">\n    <strong style=\"color: #5b21b6;\">Magnetohydrodynamics</strong>\n    <p style=\"margin: 0.5rem 0 0 0; font-size: 0.9rem; color: #475569;\">\n        MHD with magnetic fields, dynamo problems, magnetic dissipation\n    </p>\n</div>\n\n<div style=\"background: #ecfdf5; border-radius: 8px; padding: 1rem; border-left: 4px solid #10b981;\">\n    <strong style=\"color: #065f46;\">Geophysical Flows</strong>\n    <p style=\"margin: 0.5rem 0 0 0; font-size: 0.9rem; color: #475569;\">\n        Rotating shallow water, stratified turbulence, surface dynamics\n    </p>\n</div>\n\n</div>\n\n","title":"Scientific Applications"},{"category":"section","location":"#Problem-Types","page":"Home","text":"Type Description Example\nIVP Initial Value Problems Time-dependent Navier-Stokes\nLBVP Linear Boundary Value Problems Poisson equation\nNLBVP Nonlinear Boundary Value Problems Steady nonlinear systems\nEVP Eigenvalue Problems Linear stability analysis","title":"Problem Types"},{"category":"section","location":"#Spectral-Bases","page":"Home","text":"Basis Domain Usage\nRealFourier Periodic Horizontal directions, real-valued fields\nComplexFourier Periodic Complex-valued fields\nChebyshevT Bounded Wall-bounded domains, boundary conditions\nLegendre Bounded Alternative to Chebyshev\n\n","title":"Spectral Bases"},{"category":"section","location":"#Documentation","page":"Home","text":"<div style=\"display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1rem; margin: 1.5rem 0;\">\n\n<a href=\"getting_started/installation/\" style=\"display: flex; align-items: center; gap: 0.75rem; padding: 1rem; background: white; border: 1px solid #e2e8f0; border-radius: 8px; text-decoration: none; color: #1e293b; transition: all 0.2s;\">\n    <div>\n        <strong>Installation</strong>\n        <p style=\"margin: 0; font-size: 0.85rem; color: #64748b;\">Setup and configuration</p>\n    </div>\n</a>\n\n<a href=\"tutorials/overview/\" style=\"display: flex; align-items: center; gap: 0.75rem; padding: 1rem; background: white; border: 1px solid #e2e8f0; border-radius: 8px; text-decoration: none; color: #1e293b; transition: all 0.2s;\">\n    <div>\n        <strong>Tutorials</strong>\n        <p style=\"margin: 0; font-size: 0.85rem; color: #64748b;\">Step-by-step guides</p>\n    </div>\n</a>\n\n<a href=\"pages/gpu_computing/\" style=\"display: flex; align-items: center; gap: 0.75rem; padding: 1rem; background: white; border: 1px solid #e2e8f0; border-radius: 8px; text-decoration: none; color: #1e293b; transition: all 0.2s;\">\n    <div>\n        <strong>GPU Guide</strong>\n        <p style=\"margin: 0; font-size: 0.85rem; color: #64748b;\">CUDA acceleration</p>\n    </div>\n</a>\n\n<a href=\"api/coordinates/\" style=\"display: flex; align-items: center; gap: 0.75rem; padding: 1rem; background: white; border: 1px solid #e2e8f0; border-radius: 8px; text-decoration: none; color: #1e293b; transition: all 0.2s;\">\n    <div>\n        <strong>API Reference</strong>\n        <p style=\"margin: 0; font-size: 0.85rem; color: #64748b;\">Complete documentation</p>\n    </div>\n</a>\n\n</div>\n\nPages = [\n    \"getting_started/installation.md\",\n    \"tutorials/overview.md\",\n    \"pages/gpu_computing.md\",\n    \"pages/parallelism.md\",\n    \"api/coordinates.md\",\n    \"examples/gallery.md\"\n]\nDepth = 1\n\n","title":"Documentation"},{"category":"section","location":"#Installation-Options","page":"Home","text":"Setup Command Use Case\nDefault Pkg.add(url=\"...\") Single CPU and MPI — nothing else to install\nGPU Pkg.add(\"CUDA\") NVIDIA GPU acceleration (loads TarangCUDAExt)\nCluster MPI MPIPreferences.use_system_binary() Bind MPI.jl to the cluster's own MPI\n\nMPI, MPIPreferences, PencilArrays, PencilFFTs and KernelAbstractions are [deps] of Tarang, so a plain install is already MPI-capable: mpiexec -n 4 julia --project=. run.jl works with the MPI binary that MPI.jl ships. Only CUDA is a [weakdeps] package extension.\n\nnote: Requirements\nJulia 1.10 or later\nFor GPU: NVIDIA GPU with CUDA support\nFor MPI on a cluster: call MPIPreferences.use_system_binary() once so MPI.jl uses the site's MPI (and its launcher) instead of the bundled one — see Running with MPI.\n\n","title":"Installation Options"},{"category":"section","location":"#Contributing","page":"Home","text":"We welcome contributions! See our GitHub repository for:\n\nBug reports and feature requests\nDocumentation improvements\nPull requests\n\n","title":"Contributing"},{"category":"section","location":"#Citation","page":"Home","text":"If you use Tarang.jl in your research, please cite:\n\n@software{tarang_jl,\n  author = {Kar, Subhajit},\n  title  = {Tarang.jl: A Spectral PDE Solver for Julia},\n  url    = {https://github.com/subhk/Tarang.jl},\n  year   = {2024}\n}\n\n","title":"Citation"},{"category":"section","location":"#License","page":"Home","text":"Tarang.jl is released under the MIT License.\n\n<div style=\"text-align: center; margin-top: 2rem; padding: 1.5rem; background: #f8fafc; border-radius: 8px;\">\n    <p style=\"margin: 0; color: #64748b;\">\n        Made for the scientific computing community\n    </p>\n</div>","title":"License"},{"category":"section","location":"api/timesteppers/#Timesteppers-API","page":"Time Steppers","text":"Time integration schemes for evolving PDEs in time.\n\nTarang's timesteppers are IMEX schemes: the linear operator L (everything on the left-hand side of an equation) is advanced implicitly, and the nonlinear right-hand side is advanced explicitly.","title":"Timesteppers API"},{"category":"section","location":"api/timesteppers/#TimeStepper-Types","page":"Time Steppers","text":"","title":"TimeStepper Types"},{"category":"section","location":"api/timesteppers/#Abstract-Type","page":"Time Steppers","text":"All timesteppers inherit from the abstract type Tarang.TimeStepper. It is not exported — write it qualified if you need to refer to it (e.g. in a type annotation):\n\nusing Tarang\n\nsupertype(RK222)                  # Tarang.TimeStepper\nRK222() isa Tarang.TimeStepper    # true\n\nThe concrete schemes below are exported, unless the section says otherwise.","title":"Abstract Type"},{"category":"section","location":"api/timesteppers/#IMEX-Runge-Kutta","page":"Time Steppers","text":"","title":"IMEX Runge-Kutta"},{"category":"section","location":"api/timesteppers/#RK111","page":"Time Steppers","text":"1st-order IMEX Runge-Kutta.\n\nRK111()\n\nProperties:\n\nOrder: 1\nStages: 1 (all implicit)\nImplicit part: Backward Euler for linear terms\nExplicit part: Forward Euler for nonlinear terms\nWorkspace field sets: 3 (stage state, explicit RHS, implicit RHS)","title":"RK111"},{"category":"section","location":"api/timesteppers/#RK222","page":"Time Steppers","text":"2nd-order IMEX Runge-Kutta (Ascher, Ruuth & Spiteri 1997, ARS(2,2,2)) with three stored stages: an explicit first stage and two diagonally implicit stages.\n\nRK222()\n\nProperties:\n\nOrder: 2\nStages: 3 — an ESDIRK form whose first stage is explicit, so only 2 of the 3 stages require an implicit solve. (RK222().stages == 3; the explicit and implicit tableaux share the abscissae c = [0, γ, 1], which is what makes the IMEX pair 2nd order.)\nImplicit part: L-stable ESDIRK with γ = 1 - 1/sqrt(2) on stages 2 and 3\nExplicit part: matched three-row explicit tableau\nWorkspace field sets: 6 (stage state, explicit RHS, and implicit RHS storage)\n\nRecommended for: General purpose problems.","title":"RK222"},{"category":"section","location":"api/timesteppers/#RK443","page":"Time Steppers","text":"3rd-order, 4-stage IMEX Runge-Kutta (Kennedy & Carpenter ARK3(2)4L[2]SA).\n\nRK443()\n\nProperties:\n\nOrder: 3\nStages: 4 — ESDIRK, first stage explicit, so 3 implicit solves per step\nImplicit part: L-stable ESDIRK for linear terms\nExplicit part: 4-stage explicit RK for nonlinear terms\nWorkspace field sets: 12 (three field sets per stage)\n\nRecommended for: High accuracy requirements.","title":"RK443"},{"category":"section","location":"api/timesteppers/#IMEX-Multistep-Methods","page":"Time Steppers","text":"","title":"IMEX Multistep Methods"},{"category":"section","location":"api/timesteppers/#CNAB-(Crank-Nicolson-Adams-Bashforth)","page":"Time Steppers","text":"CNAB1()  # 1st order\nCNAB2()  # 2nd order\n\nTreatment:\n\nLinear terms: Crank-Nicolson (implicit)\nNonlinear terms: Adams-Bashforth (explicit)\n\nProperties:\n\nCNAB1: Order 1, 1 history level\nCNAB2: Order 2, 2 history levels","title":"CNAB (Crank-Nicolson Adams-Bashforth)"},{"category":"section","location":"api/timesteppers/#SBDF-(Semi-implicit-BDF)","page":"Time Steppers","text":"SBDF1()  # 1st order\nSBDF2()  # 2nd order\nSBDF3()  # 3rd order\nSBDF4()  # 4th order\n\nTreatment:\n\nLinear terms: BDF (implicit)\nNonlinear terms: Extrapolation (explicit)\n\nProperties: | Method | Order | History Levels | Stability | |––––|–––-|––––––––|–––––-| | SBDF1 | 1 | 1 | A-stable | | SBDF2 | 2 | 2 | A-stable | | SBDF3 | 3 | 3 | A(α)-stable | | SBDF4 | 4 | 4 | A(α)-stable |\n\nMultistep schemes start from a single state, so the history has to be seeded. CNAB2 and SBDF2 bootstrap with their own 1st-order member (CNAB1 / SBDF1), so their first step is reduced order; SBDF3 / SBDF4 self-start with an order-3 RK443 step on the global-matrix path, which keeps the one-time startup error from capping the global order (on the per-subproblem path they still bootstrap from SBDF2/SBDF1 and are order-capped at 2). Only SBDF1/SBDF2 are A-stable: the higher-order members trade stability angle for formal order, so they suit smooth, accuracy-limited integration rather than the stiffest linear terms.","title":"SBDF (Semi-implicit BDF)"},{"category":"section","location":"api/timesteppers/#MCNAB2-/-CNLF2","page":"Time Steppers","text":"Additional two-step multistep schemes. Neither is exported — they must be qualified:\n\nTarang.MCNAB2()   # θ-weighted Crank-Nicolson + Adams-Bashforth 2\nTarang.CNLF2()    # Crank-Nicolson Leapfrog (CN implicit + centered leapfrog explicit)\n\nMCNAB2 accepts an optional CN weight, Tarang.MCNAB2(theta) (default 0.5). This is a two-level θ-method, not the three-level Ascher-Ruuth-Wetton \"modified CNAB\": at θ = 0.5 it is Crank-Nicolson and 2nd order, but any θ > 0.5 adds damping at the cost of dropping to 1st order. Use the default unless you specifically want that trade.\n\nCNLF2 is 2nd order and 2-step.","title":"MCNAB2 / CNLF2"},{"category":"section","location":"api/timesteppers/#Exponential-Time-Differencing-(ETD)","page":"Time Steppers","text":"Exponential integrators that treat the linear term exactly via the matrix exponential, which is well suited to very stiff linear operators.\n\nETD_RK222()  # 2nd-order exponential Runge-Kutta\nETD_CNAB2()  # 2nd-order exponential Crank-Nicolson Adams-Bashforth\nETD_SBDF2()  # 2nd-order exponential semi-implicit BDF\n\nProperties:\n\nOrder: 2\nLinear term: exact exponential propagation (via φ-functions)\nNonlinear term: explicit (RK / Adams-Bashforth / BDF extrapolation)\nBest for problems where the linear part dominates the stiffness.","title":"Exponential Time Differencing (ETD)"},{"category":"section","location":"api/timesteppers/#Specialized-IMEX-RK","page":"Time Steppers","text":"Additive Runge-Kutta (ARK) schemes. RKSMR is exported; RKGFY and RK443_IMEX are not, and must be qualified.","title":"Specialized IMEX-RK"},{"category":"section","location":"api/timesteppers/#RKSMR","page":"Time Steppers","text":"RKSMR()\n\nThe Spalart–Moser–Rogers scheme, the workhorse IMEX integrator of incompressible spectral DNS. Its nonlinear/explicit part is third order and its linear/implicit part (Crank-Nicolson on the viscous term) is second order — that asymmetric accuracy profile is the standard SMR one, not an implementation limitation.\n\nIt is stored in a four-stage additive-RK (ESDIRK) tableau — a trivial explicit first stage plus the three SMR substeps — so it shares the same generic IMEX runtime paths as RK222 and RK443.","title":"RKSMR"},{"category":"section","location":"api/timesteppers/#RKGFY-/-RK443_IMEX","page":"Time Steppers","text":"Tarang.RKGFY()       # 3-stage 2nd-order L-stable ARK (Ascher-Ruuth-Spiteri 1997)\nTarang.RK443_IMEX()  # Same Kennedy-Carpenter coefficients as RK443; the suffix clarifies IMEX intent","title":"RKGFY / RK443_IMEX"},{"category":"section","location":"api/timesteppers/#Diagonal-IMEX","page":"Time Steppers","text":"DiagonalIMEX_RK222()  # 2nd order\nDiagonalIMEX_RK443()  # 3rd order\nDiagonalIMEX_SBDF2()  # 2nd order multistep\n\nThe implicit solve is diagonal in spectral space — the linear operator is applied per Fourier mode ((I + γ·dt·L̂)⁻¹ per wavenumber) rather than through a global matrix solve. Much cheaper for pure-Fourier / diagonalizable linear terms, and because the solve is an element-wise division there is no sparse factorization to move off-device on GPU. It requires the implicit operator to be diagonal in the chosen basis.\n\nThese schemes read the implicit operator from your equation's L and diagonalize it automatically, so ∂t(u) - nu*lap(u) = 0 is treated implicitly with no extra setup. You can still attach an explicit SpectralLinearOperator with set_spectral_linear_operator! to override that — an attached operator takes precedence and is applied to every field:\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb     = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\n\nu = ScalarField(dist, \"u\", (xb,), Float64)\nx = local_grid(xb, dist, 1)\nensure_layout!(u, :g)\nget_grid_data(u) .= cos.(2 .* x)          # a single mode, k = 2\n\nproblem = IVP([u])\nadd_equation!(problem, \"∂t(u) = 0\")       # the linear term lives in L, NOT in the equation\n\nsolver = InitialValueSolver(problem, DiagonalIMEX_RK222(); dt=0.005)\nL = SpectralLinearOperator(dist, (xb,), :laplacian; ν=0.5)   # L̂(k) = ν k²\nset_spectral_linear_operator!(solver, L)\n\nrun!(solver; stop_iteration=200, progress=false)   # t = 1.0\n\nensure_layout!(u, :g)\nmaximum(abs, get_grid_data(u))   # 0.13533 ≈ exp(-ν k² t) = exp(-2) = 0.13534\n\nSpectralLinearOperator(dist, bases, kind; ν, order) supports :laplacian (L̂ = ν k²), :hyperviscosity (L̂ = ν (k²)^order), :biharmonic (L̂ = ν k⁴), and :custom (pass coefficients yourself).\n\nnote: A non-diagonalizable implicit term is an error, not a silent drop\nThese schemes require the implicit operator to be diagonal in the chosen basis. A constant-coefficient Fourier term such as nu*lap(u) qualifies; a term coupling different fields, a Chebyshev/Jacobi basis, or a field-valued (spatially varying) coefficient does not. In those cases the scheme raises an ArgumentError naming the field and the offending equation — use RK222 / SBDF2 / RK443, which solve the implicit part through a global matrix. A fully explicit RK step is used only when the equation genuinely has no implicit term, and that is announced once at @info.Earlier versions silently fell back to a fully explicit step whenever no SpectralLinearOperator was attached, so ∂t(u) - nu*lap(u) = 0 ran undiffused with no diagnostic. That no longer happens.","title":"Diagonal IMEX"},{"category":"section","location":"api/timesteppers/#Usage-with-Solver","page":"Time Steppers","text":"using Tarang\n\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb     = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\nyb     = RealFourier(coords[\"y\"]; size=16, bounds=(0.0, 2π))\ndomain = Domain(dist, (xb, yb))\n\nu = ScalarField(domain, \"u\")\nproblem = IVP([u])\nadd_parameters!(problem, nu=0.1)\nadd_equation!(problem, \"∂t(u) - nu*lap(u) = 0\")\nset!(u, (x, y) -> sin(x) * cos(y))\n\n# Create solver with a timestepper — note the `()`\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\n# Drive it with run!\nrun!(solver; stop_iteration=50, progress=false)\n\nensure_layout!(u, :g)\nmaximum(abs, get_grid_data(u))   # 0.99004983, matching exp(-2νt) to 1.6e-11\n\nOr drive the loop yourself:\n\nsolver = InitialValueSolver(problem, SBDF2(); dt=1e-3)\nwhile solver.sim_time < 0.05\n    step!(solver)\nend","title":"Usage with Solver"},{"category":"section","location":"api/timesteppers/#Timestepper-State","page":"Time Steppers","text":"","title":"Timestepper State"},{"category":"section","location":"api/timesteppers/#TimestepperState","page":"Time Steppers","text":"Tarang.TimestepperState holds everything a scheme needs between steps: the state history, the timestep history (for variable-dt multistep schemes), pre-allocated workspace fields, and any forcing. It is not exported and is managed automatically by the solver — you reach it as solver.timestepper_state and normally never touch it.\n\nfieldnames(Tarang.TimestepperState)\n# (:timestepper, :dt, :history, :dt_history, :stage, :timestepper_data,\n#  :workspace_fields, :workspace_allocated, :forcing, :current_substep, :forcing_generated)","title":"TimestepperState"},{"category":"section","location":"api/timesteppers/#Stability","page":"Time Steppers","text":"","title":"Stability"},{"category":"section","location":"api/timesteppers/#Explicit-(advective)-restriction","page":"Time Steppers","text":"The explicit half of every IMEX scheme is limited by the advective CFL condition:\n\nDelta t  C fracDelta xu_max\n\nThe admissible factor C depends on the PDE, the spatial discretization and the scheme; Tarang does not define universal CFL constants for individual steppers. Rather than hard-coding C, use the CFL controller, which recomputes dt from the grid spacing and the current velocity field with a safety factor. It is constructed from the solver (never from a problem), and its velocities must be VectorFields:\n\nv = VectorField(domain, \"v\")\nset!(v.components[1], (x, y) -> 0.5)\n\nprob = IVP([u, v])\nadd_parameters!(prob, nu=0.1)\nadd_equation!(prob, \"∂t(u) - nu*lap(u) = -v⋅∇(u)\")\nadd_equation!(prob, \"∂t(v) - nu*lap(v) = 0\")\nset!(u, (x, y) -> sin(x) * cos(y))\n\nsolver = InitialValueSolver(prob, RK222(); dt=1e-3)\n\ncfl = CFL(solver; initial_dt=1e-3, cadence=5, safety=0.4, max_dt=0.01)\nadd_velocity!(cfl, v)\n\nrun!(solver; stop_iteration=10, cfl=cfl, progress=false)\nsolver.dt   # updated by the controller","title":"Explicit (advective) restriction"},{"category":"section","location":"api/timesteppers/#Implicit-(diffusive)-restriction","page":"Time Steppers","text":"Because the linear operator is advanced implicitly, the diffusive stability restriction (Δt ≲ Δx²/ν) is removed. Implicit linear treatment removes the explicit diffusive limit, but it does not remove the advective CFL limit above: only advective CFL constrains dt. That is the whole point of the IMEX splitting, and it is why all of Tarang's schemes are IMEX.","title":"Implicit (diffusive) restriction"},{"category":"section","location":"api/timesteppers/#Method-Selection-Guide","page":"Time Steppers","text":"Problem Type Method Reason\nGeneral purpose RK222 Balance of cost/accuracy\nHigh accuracy RK443 More stages, higher order\nDiffusion-dominated SBDF2 A-stable implicit diffusion, one solve per step\nSmooth high-order integration RK443 or SBDF3/SBDF4 Higher formal order; the multistep members need startup history and are only A(α)-stable\nVery stiff, linear-dominated ETD_RK222 Exact exponential propagation of L\nClassic incompressible DNS IMEX RKSMR SMR explicit-third / implicit-second accuracy profile\nDiagonal Fourier linear operator / GPU DiagonalIMEX family Per-mode implicit division avoids a global sparse solve (needs an attached SpectralLinearOperator)","title":"Method Selection Guide"},{"category":"section","location":"api/timesteppers/#Performance","page":"Time Steppers","text":"","title":"Performance"},{"category":"section","location":"api/timesteppers/#Computational-Cost","page":"Time Steppers","text":"The RHS is evaluated once per stage; an implicit solve is needed on each stage whose implicit diagonal coefficient A_implicit[s,s] is nonzero (the ESDIRK first stage is free). Each IMEX-RK step then finishes with one mass-matrix solve for the update; the factorizations of (M + a·dt·L) and M are cached across steps and reused as long as dt is unchanged.\n\nMethod Stages RHS Evaluations Implicit Solves\nRK111 1 1 1\nRK222 3 (1st explicit) 3 2\nRK443 4 (1st explicit) 4 3\nRKSMR 4 (1st explicit) 4 3\nCNAB2 — 1 1\nSBDF2 — 1 1\n\nThe multistep rows are the per-step cost once the history ring is full; the startup steps cost the same but run at reduced order.\n\nFor the DiagonalIMEX_* schemes the stage counts are the same, but each \"implicit solve\" is an element-wise division by (1 + a·dt·L̂(k)) instead of a sparse factorization.","title":"Computational Cost"},{"category":"section","location":"api/timesteppers/#Memory-Requirements","page":"Time Steppers","text":"Workspace storage is preallocated per timestepper type and reused: these are scratch buffers, not fresh state copies allocated on every step. Workspace field sets are allocated per state field (each set is one full field):\n\nMethod Workspace Sets\nRK111 3\nRK222 6\nRK443 12\nCNAB1 / CNAB2 2\nSBDF1 – SBDF4 2\nETDRK222 / ETDCNAB2 / ETD_SBDF2 3\nDiagonalIMEX_* 4\neverything else (RKSMR, MCNAB2, CNLF2, RKGFY, RK443_IMEX) 2\n\nThe multistep schemes additionally retain previous state and RHS levels in a history ring (2–5 levels, depending on order), which the RK schemes do not.","title":"Memory Requirements"},{"category":"section","location":"api/timesteppers/#See-Also","page":"Time Steppers","text":"Solvers: Using timesteppers\nUser Guide: Timesteppers: Conceptual guide","title":"See Also"},{"category":"section","location":"api/gpu/#GPU-API-Reference","page":"GPU","text":"This page documents the GPU-related functions and types that Tarang.jl exports.\n\nGPU support ships as a package extension (TarangCUDAExt), which Julia loads automatically once CUDA.jl is available. You therefore need both packages in scope:\n\nusing CUDA     # loads the extension\nusing Tarang\n\nWithout CUDA loaded, GPU() raises an actionable error (\"GPU architecture requires CUDA.jl to be loaded\"), so everything runs on the CPU.\n\nThe API below is architecture-generic: the same functions take CPU() or GPU(), and you select the device once, on the Distributor:\n\ndist = Distributor(coords; dtype=Float64, device=CPU())   # or device=GPU()\n\nFields, transforms, and timesteppers all inherit the architecture from the distributor. The examples on this page are written with CPU() so that they run anywhere; substituting device=GPU() is the only change needed to run them on a GPU.","title":"GPU API Reference"},{"category":"section","location":"api/gpu/#Architecture-Types","page":"GPU","text":"","title":"Architecture Types"},{"category":"section","location":"api/gpu/#Array-Operations","page":"GPU","text":"","title":"Array Operations"},{"category":"section","location":"api/gpu/#Data-Movement","page":"GPU","text":"a = zeros(CPU(), Float64, 4, 4)\n\non_architecture(CPU(), a)   # move/keep an array on the CPU (a no-op for a plain Array)\n\nis_gpu_array(a)             # false\narchitecture(a)             # CPU()  — architecture inferred from the array type\nis_gpu(CPU())               # false\nhas_cuda()                  # false when CUDA.jl is not loaded\n\nWith the extension loaded, on_architecture(GPU(), a) returns a CuArray and on_architecture(CPU(), cu_a) copies it back to a host Array. on_architecture also accepts tuples, named tuples, numbers, and nothing, so it can be mapped over heterogeneous state without special-casing.","title":"Data Movement"},{"category":"section","location":"api/gpu/#Allocation","page":"GPU","text":"Base.zeros, Base.ones, and Base.similar take an architecture as their first argument; with GPU() they allocate CuArrays instead of Arrays.\n\nzeros(CPU(), Float64, 128, 128)     # Matrix{Float64}\nones(CPU(), Float32, 256)           # Vector{Float32}\nsimilar(CPU(), a)                   # same shape/eltype, same architecture\ncreate_array(CPU(), ComplexF64, 2, 2)\n\nallocate_like(a, dims...), similar_zeros(a), and copy_to_device(a, target) allocate from an existing array instead of an architecture, which keeps the device implicit.","title":"Allocation"},{"category":"section","location":"api/gpu/#Device-Context-and-Cleanup","page":"GPU","text":"Function Description\nensure_device!(arch) Activate the correct CUDA device. No-op on CPU(). Called by launch! before every kernel.\nsynchronize(arch) Wait for outstanding device work. No-op on CPU().\nunsafe_free!(arch, a) Eagerly release a device buffer. No-op on CPU().\ndevice(arch) The KernelAbstractions backend (KernelAbstractions.CPU(false) for CPU()).\narray_type(arch) Array for CPU(), CuArray for GPU().\n\nFor multi-GPU runs (one GPU per MPI rank) build the architecture with an explicit device id — GPU(device_id = MPI.Comm_rank(comm) % CUDA.ndevices()) — and let ensure_device! handle the context switching; launch! calls it on every launch.","title":"Device Context and Cleanup"},{"category":"section","location":"api/gpu/#Transform-Functions","page":"GPU","text":"","title":"Transform Functions"},{"category":"section","location":"api/gpu/#FFT-Backend-Control","page":"GPU","text":"Transforms choose between CPU FFTW and GPU CUFFT per field. The choice is a per-field mode plus a global size threshold.\n\nFunction Description\ngpu_fft_mode(field) Current mode: :auto (default), :cpu, or :gpu\nset_gpu_fft_mode!(field, mode) Set the mode. Any other symbol throws ArgumentError\nset_gpu_fft_min_elements!(n) Element count below which :auto stays on the CPU (default 32_768)\ngpu_fft_min_elements() Read that threshold\nshould_use_gpu_fft(field) Whether the GPU FFT path would be taken for this field\n\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nbx = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2pi))\nby = RealFourier(coords[\"y\"]; size=16, bounds=(0.0, 2pi))\ns  = ScalarField(Domain(dist, (bx, by)), \"s\")\n\ngpu_fft_mode(s)                    # :auto\nshould_use_gpu_fft(s)              # false — 256 elements < the 32_768 threshold\n\nset_gpu_fft_mode!(s, :gpu)         # force GPU regardless of size\nshould_use_gpu_fft(s)              # true\n\nset_gpu_fft_mode!(s, :auto)\nset_gpu_fft_min_elements!(1)       # lower the threshold instead\nshould_use_gpu_fft(s)              # true\n\nNote that should_use_gpu_fft reports the preference: on a CPU() distributor the GPU path is never actually taken, whatever the mode says.","title":"FFT Backend Control"},{"category":"section","location":"api/gpu/#Transform-Execution","page":"GPU","text":"Function Description\nforward_transform!(field) Grid → coefficient transform; dispatches to GPU when the field is on a GPU\nbackward_transform!(field) Coefficient → grid transform\n\nforward_transform!(s)      # s.current_layout == :c\nbackward_transform!(s)     # s.current_layout == :g, round-trip error ~3e-16\n\nTarang.gpu_forward_transform! / Tarang.gpu_backward_transform! are the internal dispatch hooks these call. They are not exported, and they are not a way to \"force\" a GPU transform: each returns a Bool saying whether it did run on the GPU (always false on a CPU architecture). Use set_gpu_fft_mode! to steer the backend.","title":"Transform Execution"},{"category":"section","location":"api/gpu/#Writing-Kernels:-KernelOperation","page":"GPU","text":"KernelOperation wraps a KernelAbstractions @kernel together with a default ndrange, and launches it through the architecture abstraction so the same code runs on CPU and GPU.\n\nusing KernelAbstractions: @kernel, @index, @Const\n\n@kernel function scale_kernel!(y, @Const(x), alpha)\n    i = @index(Global)\n    @inbounds y[i] = alpha * x[i]\nend\n\n# ndrange from a do-block over the call arguments\nmy_op = KernelOperation(scale_kernel!) do y, x, alpha\n    length(y)\nend\n\nx = ones(CPU(), Float64, 8)\ny = zeros(CPU(), Float64, 8)\nmy_op(CPU(), y, x, 2.5)            # y == fill(2.5, 8)\n\nImport KernelAbstractions selectively, as above. A bare using KernelAbstractions alongside using Tarang makes CPU ambiguous — both packages export that name — and every later CPU() fails with UndefVarError CPU not defined in Main.\n\nWithout a do-block the default ndrange is length(args[1]), and launch! is the underlying one-shot form:\n\nop = KernelOperation(scale_kernel!)   # ndrange = length(y)\nop(CPU(), y, x, 3.0)\n\nlaunch!(CPU(), scale_kernel!, y, x, 4.0; ndrange=length(y))\nworkgroup_size(CPU(), 8)              # 8\n\nlaunch! accepts an architecture or an array (whose architecture it infers), and calls ensure_device! before launching.","title":"Writing Kernels: KernelOperation"},{"category":"section","location":"api/gpu/#Distributed-Transforms:-TransposableField","page":"GPU","text":"TransposableField wraps a ScalarField with the buffers and sub-communicators needed for pencil transposes, and drives the transpose-FFT-transpose sequence. It is the custom transpose path, written for multi-GPU runs: create_distributed_gpu_transform builds one internally, and its MPI validation errors name the GPU+MPI configuration.\n\nIt is not the CPU+MPI path. On a CPU() distributor with more than one rank, fields are allocated by PencilArrays as z-decomposed slabs, and TransposableField — which expects its own ZLocal decomposition (y decomposed, z local) — rejects them:\n\nTransposableField layout mismatch: field storage shape (8, 8, 4) does not match expected ZLocal shape (8, 4, 8) for topology (Rx=1, Ry=2)\n\nFor CPU + MPI, just call forward_transform! / backward_transform!; PencilFFTs does the transposes for you. TransposableField on a CPU distributor is useful at nprocs == 1, which is what the example below runs.\n\nTwo constraints come from the implementation:\n\nthe field's bases must all be ComplexFourier under MPI — RealFourier's half-spectrum layout is incompatible with the custom transposes. This one is enforced: the constructor errors at nprocs > 1.\nthe distributor's dtype must be complex, since the transpose buffers are complex. This one is not checked. With a real dtype the TransposableField constructs (the buffers are promoted to Complex{dtype}), and the failure only surfaces inside the transform, as MethodError: no method matching mul!(::Array{ComplexF64, 3}, ::FFTW.cFFTWPlan{…}, ::Array{Float64, 3}, …). Pass dtype=ComplexF64.\n\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist   = Distributor(coords; dtype=ComplexF64, device=CPU())\nbx = ComplexFourier(coords[\"x\"]; size=8, bounds=(0.0, 2pi))\nby = ComplexFourier(coords[\"y\"]; size=8, bounds=(0.0, 2pi))\nbz = ComplexFourier(coords[\"z\"]; size=8, bounds=(0.0, 2pi))\nf  = ScalarField(Domain(dist, (bx, by, bz)), \"f\")\n\nxg, yg, zg = local_grids(dist, bx, by, bz)\nensure_layout!(f, :g)\nget_grid_data(f) .= sin.(xg) .* cos.(yg') .* ones(1, 1, 8)\n\ntf = TransposableField(f)          # or make_transposable(f)\n\nactive_layout(tf)                  # ZLocal  (also XLocal, YLocal)\ncurrent_data(tf)                   # the buffer for the active layout\nlocal_shape(tf, XLocal)            # (8, 8, 8)\n\ndistributed_forward_transform!(tf)   # writes the result back into f[\"c\"]\ndistributed_backward_transform!(tf)  # round-trip error ~2e-16\n\nBoth transforms accept overlap=true to use async transposes that overlap communication with computation (not supported for a 2D domain on a true 2D mesh, which warns and falls back).","title":"Distributed Transforms: TransposableField"},{"category":"section","location":"api/gpu/#CUDA-Aware-MPI","page":"GPU","text":"check_cuda_aware_mpi()      # false unless the MPI build supports device pointers\n\nWhen it returns true, distributed transforms pass CuArrays straight to MPI; otherwise the buffers are staged through the host.","title":"CUDA-Aware MPI"},{"category":"section","location":"api/gpu/#Memory-Management","page":"GPU","text":"Tarang uses CUDA.jl's built-in memory pool — there is no custom pooling layer. Use CUDA.jl's own tools for inspection and reclamation (CUDA.memory_status(), CUDA.reclaim()), and unsafe_free!(arch, a) when you want to drop a large buffer without waiting for the GC.","title":"Memory Management"},{"category":"section","location":"api/gpu/#The-CUDA-Extension-is-Not-Public-API","page":"GPU","text":"The GPU kernels, FFT/DCT plans, and transpose helpers live in the extension module TarangCUDAExt (ext/cuda/*.jl). A Julia package extension cannot add names to its parent module, so these are not reachable as Tarang.x and are not in scope after using Tarang, CUDA — isdefined(Tarang, :gpu_add!) is false. They are implementation details that Tarang's own transform and timestepping code calls; they are reachable only via Base.get_extension(Tarang, :TarangCUDAExt) and may change without notice.\n\nWhat the extension implements, for orientation when reading the source:\n\nArea Source Contents\nElement-wise kernels ext/cuda/kernels.jl gpu_add!, gpu_sub!, gpu_mul!, gpu_scale!, gpu_axpy!, gpu_linear_combination! and their GPU_*_OP KernelOperation wrappers\nFused kernels ext/cuda/kernels.jl gpu_rk_stage!, gpu_axpby!, gpu_fma!, gpu_dealias_multiply!, gpu_triple_product!, gpu_conj_multiply!, gpu_squared_magnitude!, gpu_kinetic_energy_2d!/3d!, gpu_grad_mag_sq_2d!, gpu_viscous_damping!\nFFT plans ext/cuda/transforms.jl, ext/cuda/batched_fft.jl plan_gpu_fft, gpu_forward_fft!, gpu_backward_fft!, batched plans, plan caches\nChebyshev DCT ext/cuda/dct.jl, ext/cuda/dct_distributed.jl DCT plans, multi-GPU distributed DCT\nMixed transforms ext/cuda/mixed_transforms.jl Fourier-Chebyshev plans\nTransposes ext/cuda/transpose_kernels.jl, ext/cuda/nccl_transpose.jl pack/unpack kernels, NCCL all-to-all\nConfig / memory ext/cuda/config.jl, ext/cuda/memory.jl tensor-core toggles, async host↔device copies\n\nTo write your own device code, use the public KernelOperation / launch! path above rather than reaching into the extension.","title":"The CUDA Extension is Not Public API"},{"category":"type","location":"api/gpu/#Tarang.GPU","page":"GPU","text":"GPU <: AbstractSerialArchitecture\n\nGPU architecture using CUDA.jl for NVIDIA GPUs. Requires CUDA.jl to be loaded for full functionality.\n\nExample\n\nusing CUDA  # Must load CUDA first\ndist = Distributor(coordsys; device=GPU())  # uses CUDA/cuFFT, TransposableField for MPI\n\nNotes\n\nGPU support is optional and loaded via package extension\nIf CUDA.jl is not available, GPU() will throw an error\nFor multi-GPU, use with MPI (one GPU per MPI rank)\n\n\n\n\n\n","title":"Tarang.GPU"},{"category":"type","location":"api/gpu/#Tarang.CPU","page":"GPU","text":"CPU <: AbstractSerialArchitecture\n\nCPU architecture using Julia's native arrays and threading. This is the default architecture.\n\nExample\n\ndist = Distributor(coordsys; device=CPU())  # default, uses PencilArrays/PencilFFTs for MPI\n\n\n\n\n\n","title":"Tarang.CPU"},{"category":"type","location":"api/gpu/#Tarang.AbstractArchitecture","page":"GPU","text":"AbstractArchitecture\n\nAbstract supertype for all compute architectures.\n\n\n\n\n\n","title":"Tarang.AbstractArchitecture"},{"category":"section","location":"pages/coordinates/#Coordinates","page":"Coordinates","text":"Coordinates define the spatial dimensions and geometry of your simulation domain.","title":"Coordinates"},{"category":"section","location":"pages/coordinates/#Cartesian-Coordinates","page":"Coordinates","text":"For rectangular domains with uniform grid spacing in grid space.\n\nusing Tarang\n\n# 1D\ncoords = CartesianCoordinates(\"x\")\n\n# 2D\ncoords = CartesianCoordinates(\"x\", \"z\")\n\n# 3D\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")","title":"Cartesian Coordinates"},{"category":"section","location":"pages/coordinates/#Accessing-Coordinates","page":"Coordinates","text":"coords = CartesianCoordinates(\"x\", \"z\")\n\n# Get a specific coordinate, by name or by position\nx = coords[\"x\"]\nz = coords[2]\n\n# Number of dimensions\nndim = coords.dim          # 2\n\n# The coordinate names, in axis order\ncoords.names               # [\"x\", \"z\"]\n\n# Iterate over all coordinates\nfor coord in get_coords(coords)\n    println(coord.name)\nend\n\nA CartesianCoordinates is not itself iterable — for coord in coords and length(coords) throw a MethodError. Use get_coords(coords) to get the Vector{Coordinate}, and coords.dim for the dimension count.","title":"Accessing Coordinates"},{"category":"section","location":"pages/coordinates/#Coordinate-Properties","page":"Coordinates","text":"coord = coords[\"x\"]\n\ncoord.name         # String: \"x\"\ncoord.coordsys     # The parent CartesianCoordinates object, {x,z}\ncoord.dim          # Int: 1 — a Coordinate is always one-dimensional\ncoord.curvilinear  # Bool: false for Cartesian","title":"Coordinate Properties"},{"category":"section","location":"pages/coordinates/#Distributor","page":"Coordinates","text":"The Distributor manages MPI process distribution across the domain.\n\n# Create distributor — the mesh is chosen for you\ndist = Distributor(coords; dtype=Float64, device=CPU())","title":"Distributor"},{"category":"section","location":"pages/coordinates/#Parameters","page":"Coordinates","text":"coords: Coordinate system\nmesh: Tuple specifying the MPI process grid. Optional; omit it and Tarang picks a valid mesh for the number of ranks it is launched with.\ndtype: Data type (Float64, Float32)\ndevice: CPU() or GPU()","title":"Parameters"},{"category":"section","location":"pages/coordinates/#MPI-Process-Mesh","page":"Coordinates","text":"The product of the mesh dimensions must equal the number of MPI processes; anything else is an ArgumentError (\"Mesh size 4 does not match number of processes 1\").\n\nThe mesh must have one dimension fewer than the domain: a 2D domain takes a 1D mesh, a 3D domain takes a 2D mesh. The FFT needs at least one axis held whole on each rank, so a mesh that decomposes every axis has nothing left to transform.\n\ncoords_2d = CartesianCoordinates(\"x\", \"y\")\ncoords_3d = CartesianCoordinates(\"x\", \"y\", \"z\")\n\n# 2D domain on 4 processes: a 1D mesh\ndist = Distributor(coords_2d; mesh=(4,))\n\n# 3D domain on 8 processes: a 2D mesh\ndist = Distributor(coords_3d; mesh=(2, 4))\n\n# Serial (single process)\ndist = Distributor(coords_2d; mesh=(1,))\n\n(Each of those lines is only valid when the job is launched with that many ranks.)\n\nPassing a 2D mesh for a 2D domain — mesh=(2, 2) on 4 ranks — is a hard error:\n\nPencilFFT plan creation failed with 4 MPI processes. Local FFTW fallback would produce incorrect results.\n\nThe defaults are the safe choice. Omitting mesh gives (2,), (4,), (8,) at 2, 4 and 8 ranks for a 2D domain, and (1, 2), (2, 2), (2, 4) for a 3D domain.","title":"MPI Process Mesh"},{"category":"section","location":"pages/coordinates/#Distributor-Properties","page":"Coordinates","text":"dist.coordsys   # Coordinate system, {x,z}\ndist.coords     # Tuple of Coordinate objects, (x, z)\ndist.dim        # Total number of dimensions\ndist.comm       # MPI communicator\ndist.rank       # This process's rank\ndist.size       # Total number of processes\ndist.mesh       # Process mesh tuple — (1,) in serial\ndist.dtype      # Float64","title":"Distributor Properties"},{"category":"section","location":"pages/coordinates/#Domain-Decomposition","page":"Coordinates","text":"","title":"Domain Decomposition"},{"category":"section","location":"pages/coordinates/#Local-vs-Global-Indices","page":"Coordinates","text":"Tarang decomposes the trailing axes and keeps the leading axis whole on every rank. On a 2D domain the first axis is therefore always local, and only the second is split:\n\n# 2D domain, 32×32 grid, 4 processes (mesh (4,))\nTarang.local_indices(dist, 1, 32)   # 1:32 on every rank — axis 1 is not decomposed\nTarang.local_indices(dist, 2, 32)   # rank 0: 1:8, rank 1: 9:16, … rank 3: 25:32\n\nlocal_indices is internal. In user code, get the local extent from the field itself:\n\nusing PencilArrays\n\nu  = ScalarField(domain, \"u\")\ngd = get_grid_data(u)               # a PencilArray when nprocs > 1\n\nPencilArrays.range_local(gd)        # rank 0 of 4: (1:32, 1:8)\nlocal_shape(domain, :g)             # (32, 8)\nglobal_shape(domain, :g)            # (32, 32)","title":"Local vs Global Indices"},{"category":"section","location":"pages/coordinates/#Pencil-Decomposition","page":"Coordinates","text":"Tarang uses pencil (slab) decomposition for efficient parallel FFTs. A 3D 16³ domain on 4 processes gets the auto mesh (2, 2), which splits the two trailing axes and leaves the first whole, so each rank holds a 16×8×8 pencil:\n\n3D domain distributed across 4 processes (2×2 mesh):\n\n    Process 0     Process 1\n    ┌─────────┐  ┌─────────┐\n    │ ▓▓▓▓▓▓▓ │  │ ░░░░░░░ │\n    │ ▓▓▓▓▓▓▓ │  │ ░░░░░░░ │\n    │ ▓▓▓▓▓▓▓ │  │ ░░░░░░░ │\n    └─────────┘  └─────────┘\n    Process 2     Process 3\n    ┌─────────┐  ┌─────────┐\n    │ ▒▒▒▒▒▒▒ │  │ ████████ │\n    │ ▒▒▒▒▒▒▒ │  │ ████████ │\n    │ ▒▒▒▒▒▒▒ │  │ ████████ │\n    └─────────┘  └─────────┘","title":"Pencil Decomposition"},{"category":"section","location":"pages/coordinates/#Coordinate-Aware-Operations","page":"Coordinates","text":"","title":"Coordinate-Aware Operations"},{"category":"section","location":"pages/coordinates/#Derivatives","page":"Coordinates","text":"Coordinate names determine derivative operators. A coordinate named \"x\" gives you ∂x, one named \"z\" gives you ∂z, and so on:\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2pi), dealias=3/2)\nybasis = RealFourier(coords[\"y\"]; size=16, bounds=(0.0, 2pi), dealias=3/2)\ndomain = Domain(dist, (xbasis, ybasis))\n\nu = ScalarField(domain, \"u\")\nT = ScalarField(domain, \"T\")\n\nproblem = IVP([u, T])\nadd_parameters!(problem, nu=0.01)\nadd_equation!(problem, \"∂t(u) - nu*lap(u) = ∂x(T)\")   # ∂x — named after coords[\"x\"]\nadd_equation!(problem, \"∂t(T) - nu*lap(T) = 0\")\n\nx, y = local_grids(dist, xbasis, ybasis)\nensure_layout!(T, :g)\nget_grid_data(T) .= sin.(x) .* cos.(y')\nensure_layout!(T, :c)\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\nrun!(solver; stop_iteration=5, progress=false)","title":"Derivatives"},{"category":"section","location":"pages/coordinates/#Grid-Access","page":"Coordinates","text":"Grids come from the basis plus the distributor, never from the basis alone — that is what makes the same code correct in serial and under MPI, since each rank gets only its own slice.\n\n# All local grid vectors at once, in axis order\nx, y = local_grids(dist, xbasis, ybasis)      # ((16,), (16,)) in serial\n\n# One axis at a time\nx = local_grid(xbasis, dist, 1)\n\n# Spectral wavenumbers for a Fourier basis\nk = wavenumbers(xbasis)                       # physical k = 2πn/L\n\nBroadcast against these to set a field, orienting each vector along its own axis:\n\n# 2D\nensure_layout!(u, :g)\nget_grid_data(u) .= sin.(x) .* cos.(y')\nensure_layout!(u, :c)\n\n# 3D\nx, y, z = local_grids(dist, xbasis, ybasis, zbasis)\nget_grid_data(v) .= sin.(x) .* cos.(y') .* sin.(reshape(z, 1, 1, :))\n\nThis is the distributed-safe way to initialize a field, because each rank writes only its own slab. set!(field, ::Function) builds the global meshgrid and so is serial-only — under MPI it throws a DimensionMismatch. (set!(field, ::Number) and fill_random! are fine distributed.)","title":"Grid Access"},{"category":"section","location":"pages/coordinates/#Best-Practices","page":"Coordinates","text":"","title":"Best Practices"},{"category":"section","location":"pages/coordinates/#Choosing-Mesh-Dimensions","page":"Coordinates","text":"Omit mesh unless you have a specific reason to set it. When you do set it, the mesh rank is fixed by the domain — one less than the domain dimension — and only the split is yours to choose:\n\nDomain Processes Valid mesh Notes\n2D 4 (4,) The only shape. (2, 2) is an error.\n3D 4 (2, 2) The auto choice; (1, 4) and (4, 1) also work.\n3D 8 (2, 4) Split the two trailing axes.","title":"Choosing Mesh Dimensions"},{"category":"section","location":"pages/coordinates/#Memory-Considerations","page":"Coordinates","text":"Each process holds global_size / num_processes data\nCommunication overhead scales with surface area between processes\nBalance process count with per-process work","title":"Memory Considerations"},{"category":"section","location":"pages/coordinates/#See-Also","page":"Coordinates","text":"Bases: Spectral bases for each coordinate\nDomains: Combining coordinates and bases\nParallelism: MPI details","title":"See Also"},{"category":"section","location":"api/coordinates/#Coordinates-API","page":"Coordinates","text":"Coordinates define the dimensional structure of your problem domain. Tarang.jl currently implements Cartesian coordinates only.","title":"Coordinates API"},{"category":"section","location":"api/coordinates/#Docstrings","page":"Coordinates","text":"","title":"Docstrings"},{"category":"section","location":"api/coordinates/#Coordinate-Systems","page":"Coordinates","text":"","title":"Coordinate Systems"},{"category":"section","location":"api/coordinates/#CartesianCoordinates","page":"Coordinates","text":"Cartesian coordinate system for rectangular domains.\n\nConstructor:\n\nCartesianCoordinates(names::String...)\n\nArguments:\n\nnames: Coordinate dimension names (e.g., \"x\", \"y\", \"z\")\n\nExamples:\n\n# 1D Cartesian\ncoords = CartesianCoordinates(\"x\")\n\n# 2D Cartesian\ncoords = CartesianCoordinates(\"x\", \"z\")\n\n# 3D Cartesian\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\n\nProperties:\n\ncoords.names        # Vector{String}: coordinate names, in order\ncoords.dim          # Int: number of dimensions\ncoords.coords       # Vector{Coordinate}: the Coordinate objects, in order\n\nThe exported coords(coordsys) function returns the same Coordinate objects as a Tuple. (Naming the coordinate system coords, as the examples below do, shadows that function; call it as Tarang.coords(coords) if you need both.)\n\nMethods:","title":"CartesianCoordinates"},{"category":"section","location":"api/coordinates/#Accessing-Coordinates","page":"Coordinates","text":"coords = CartesianCoordinates(\"x\", \"y\", \"z\")\n\n# Get coordinate by name\nx_coord = coords[\"x\"]\n\n# Get coordinate by index\nx_coord = coords[1]\n\n# Iterate over coordinates (the system itself is not iterable)\nfor coord in coords.coords\n    println(coord.name)\nend","title":"Accessing Coordinates"},{"category":"section","location":"api/coordinates/#Unit-Vectors","page":"Coordinates","text":"unit_vector_fields(coords, dist) returns one VectorField per coordinate direction, in coordinate order. Each is a constant field with a single non-zero component.\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\n\nex, ez = unit_vector_fields(coords, dist)\n\ntypeof(ez)                                            # VectorField{Float64, …}\n[get_grid_data(comp)[1] for comp in ez.components]    # [0.0, 1.0]\n\nTo use a unit vector inside an equation string, register it with add_parameters! first. It is used for directional forces (buoyancy) and for the tau lift in a first-order reduction — both on the implicit (left-hand) side of the equation:\n\nτ_lift(A) = lift(A, derivative_basis(zbasis, 1), -1)\n\nadd_parameters!(problem, nu=Prandtl, buoy=Rayleigh*Prandtl, ez=ez, τ_lift=τ_lift,\n                grad_u=grad(u) + ez * τ_lift(tau_u1))\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) - buoy*T*ez + τ_lift(tau_u2) = -u⋅∇(u)\")\n\nSee the Rayleigh–Bénard tutorial for the surrounding problem.\n\n","title":"Unit Vectors"},{"category":"section","location":"api/coordinates/#Spherical-and-polar-coordinates","page":"Coordinates","text":"Not implemented. CartesianCoordinates is the only coordinate system Tarang.jl provides; SphericalCoordinates and PolarCoordinates do not exist. Curvilinear geometries are planned for a future release.\n\n","title":"Spherical and polar coordinates"},{"category":"section","location":"api/coordinates/#Coordinate-Object","page":"Coordinates","text":"Individual coordinate dimension, produced by a coordinate system.\n\nProperties:\n\ncoord.name          # String: coordinate name\ncoord.dim           # Int: dimension of the coordinate itself (always 1)\ncoord.coordsys      # CoordinateSystem: parent system\ncoord.curvilinear   # Bool: false for Cartesian\n\nExample:\n\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\nx = coords[\"x\"]\n\nprintln(x.name)                 # \"x\"\nprintln(x.coordsys === coords)  # true\n\nA Coordinate does not know its own position in the parent system; use the system's order (coords.names) if you need the axis index.\n\n","title":"Coordinate Object"},{"category":"section","location":"api/coordinates/#Distributor","page":"Coordinates","text":"The distributor manages MPI process distribution across coordinate dimensions.\n\nConstructor:\n\nDistributor(\n    coords::CoordinateSystem;\n    comm::MPI.Comm=MPI.COMM_WORLD,\n    mesh::Union{Nothing,Tuple{Vararg{Int}}}=nothing,   # nothing = auto\n    dtype::Type=Float64,\n    device::AbstractArchitecture=CPU()                 # CPU() or GPU()\n)\n\nArguments:\n\ncoords: Coordinate system\nmesh: MPI process mesh. Omit it and Tarang picks a valid mesh for the domain dimension (see Mesh Configuration below). prod(mesh) must equal the number of MPI processes.\n\nExamples:\n\nusing Tarang, MPI\n\n# Serial (or MPI with an auto-chosen mesh)\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# Explicit mesh: a 2D domain takes a 1D mesh\ndist = Distributor(coords; mesh=(MPI.Comm_size(MPI.COMM_WORLD),), device=CPU())\n\nMPI is not re-exported by Tarang — using MPI yourself whenever a snippet touches MPI.Comm_size, MPI.Barrier, or a communicator.\n\nProperties:\n\ndist.coordsys       # CoordinateSystem   (dist.coords is the Tuple of Coordinate objects)\ndist.mesh           # Tuple: Process mesh dimensions\ndist.comm           # MPI.Comm: MPI communicator\ndist.rank           # Int: MPI rank of this process\ndist.size           # Int: Total number of processes\ndist.dtype          # Type: default element type\n\nMethods:","title":"Distributor"},{"category":"section","location":"api/coordinates/#Process-Information","page":"Coordinates","text":"There are no get_rank/get_size accessors — read the fields directly.\n\nrank = dist.rank        # process rank\nnprocs = dist.size      # total number of processes\nis_root = dist.rank == 0","title":"Process Information"},{"category":"section","location":"api/coordinates/#Domain-Decomposition","page":"Coordinates","text":"Sizes come from the domain, not from the distributor:\n\nglobal_shape(domain, :g)          # global grid shape, e.g. (32, 32)\nlocal_shape(domain, :g)           # this rank's slab, e.g. (32, 16) at np=2\nget_global_size(dist, basis)      # global size of one basis, e.g. 32\n\nThe grid coordinates this rank owns come from local_grids, which is the same call in serial and under MPI:\n\nxg, zg = local_grids(dist, xbasis, zbasis)\n\nUnder MPI (dist.size > 1) a field's grid data is a PencilArray, and it can tell you the global index range this rank owns. In serial the data is a plain Array and these calls do not apply (pencil_local_range returns nothing).\n\nusing PencilArrays\ngd = get_grid_data(u)\nPencilArrays.range_local(gd)                       # rank 0 of 2: (1:32, 1:16)\n\nTarang.pencil_local_range(dist, 1, dist.size, 32)  # (mesh dim, #procs, global size) -> 1:16\n\n","title":"Domain Decomposition"},{"category":"section","location":"api/coordinates/#Mesh-Configuration","page":"Coordinates","text":"","title":"Mesh Configuration"},{"category":"section","location":"api/coordinates/#Choosing-Process-Mesh","page":"Coordinates","text":"The process mesh determines how data is distributed across MPI processes. PencilFFTs requires at least one axis to stay local for the FFT, so the mesh must have one fewer dimension than the domain:\n\ndomain valid mesh auto mesh at np=4\n2D 1D, (nprocs,) (4,)\n3D 2D, (Px, Py) with Px*Py == nprocs (2, 2)\n\nA mesh whose rank equals the domain dimension decomposes every axis and fails hard: a 2D domain with mesh=(2, 2) at np=4 aborts with \"PencilFFT plan creation failed with 4 MPI processes\" when the domain is built. A mesh with a unit factor ((1, 4)) is normalized to the equivalent slab mesh (4,).\n\nGeneral rules:\n\nOmit mesh unless you have a reason — the auto mesh is valid by construction\nProduct of mesh dimensions must equal the number of MPI processes\nFor 3D, match the mesh aspect ratio to the domain aspect ratio\nUse powers of 2 when possible for optimal FFT performance\n\nThe mesh entries decompose the trailing axes, in order: for a 3D domain, mesh=(Px, Py) splits axis 2 into Px pieces and axis 3 into Py pieces; axis 1 stays local on every rank (which is why a Chebyshev axis must come first in a distributed mixed Fourier–Chebyshev domain — see Parallelism).","title":"Choosing Process Mesh"},{"category":"section","location":"api/coordinates/#Process-Mesh-Strategies","page":"Coordinates","text":"","title":"Process Mesh Strategies"},{"category":"section","location":"api/coordinates/#2D-Problems","page":"Coordinates","text":"Only slab decomposition is possible; the mesh is (nprocs,) and it splits the last axis.\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords; mesh=(4,), device=CPU())     # 4 processes\n# 32×32 grid -> local grid (32, 8) on every rank","title":"2D Problems"},{"category":"section","location":"api/coordinates/#3D-Problems","page":"Coordinates","text":"coords = CartesianCoordinates(\"x\", \"y\", \"z\")\n\n# Square pencil (isotropic domains)\ndist = Distributor(coords; mesh=(2, 2), device=CPU())   # 4 processes\n# 16³ grid -> local grid (16, 8, 8)\n\n# Anisotropic pencil (stratified flows)\ndist = Distributor(coords; mesh=(4, 2), device=CPU())   # 8 processes\n# 16³ grid -> local grid (16, 4, 8)\n\n","title":"3D Problems"},{"category":"section","location":"api/coordinates/#Pencil-Decomposition","page":"Coordinates","text":"Tarang.jl uses pencil decomposition for 3D problems, where data is distributed in two dimensions while remaining contiguous in one dimension.","title":"Pencil Decomposition"},{"category":"section","location":"api/coordinates/#Pencil-Orientations","page":"Coordinates","text":"For 3D domains, data can be organized in different pencil configurations:\n\nX-pencils: Contiguous in x, distributed in y and z\nY-pencils: Contiguous in y, distributed in x and z\nZ-pencils: Contiguous in z, distributed in x and y\n\nExample:\n\n# 3D domain, 2D process mesh (auto-selected: (2, 2) at np=4)\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# PencilArrays automatically handles pencil rotations\n# during FFT operations","title":"Pencil Orientations"},{"category":"section","location":"api/coordinates/#Transpose-Operations","page":"Coordinates","text":"Pencil decomposition requires transpose operations to switch between orientations. PencilFFTs performs them inside the distributed FFT; there is no user-facing transpose call and nothing to configure. (tarang.toml accepts parallelism.TRANSPOSE_LIBRARY and parallelism.GROUP_TRANSPOSES keys, but no code path reads them — the CPU+MPI path always uses PencilArrays.)\n\n","title":"Transpose Operations"},{"category":"section","location":"api/coordinates/#Usage-Examples","page":"Coordinates","text":"","title":"Usage Examples"},{"category":"section","location":"api/coordinates/#Basic-Setup","page":"Coordinates","text":"using Tarang, MPI\n\nMPI.Init()\n\n# Define coordinate system\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\n\n# Create distributor (mesh auto-selected: 2D mesh for a 3D domain)\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# Verify setup\nif dist.rank == 0\n    println(\"Running on $(dist.size) processes\")\n    println(\"Process mesh: $(dist.mesh)\")\nend","title":"Basic Setup"},{"category":"section","location":"api/coordinates/#Multi-Resolution-Studies","page":"Coordinates","text":"Resolution is set by the basis sizes, not by the process mesh — the same distributor serves every resolution.\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\nfor N in (32, 64)\n    xbasis = RealFourier(coords[\"x\"]; size=N, bounds=(0.0, 2pi), dealias=3/2)\n    zbasis = RealFourier(coords[\"z\"]; size=N, bounds=(0.0, 2pi), dealias=3/2)\n    domain = Domain(dist, (xbasis, zbasis))\n    println(\"N=$N  global grid $(global_shape(domain, :g))  local grid $(local_shape(domain, :g))\")\n    # ... build and run the problem at this resolution\nend","title":"Multi-Resolution Studies"},{"category":"section","location":"api/coordinates/#Custom-Coordinate-Names","page":"Coordinates","text":"# Use physics-appropriate names\ncoords = CartesianCoordinates(\"streamwise\", \"spanwise\", \"wall_normal\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# Access by custom names\nx = coords[\"streamwise\"]\ny = coords[\"spanwise\"]\nz = coords[\"wall_normal\"]\n\n","title":"Custom Coordinate Names"},{"category":"section","location":"api/coordinates/#Advanced-Topics","page":"Coordinates","text":"","title":"Advanced Topics"},{"category":"section","location":"api/coordinates/#Custom-MPI-Communicators","page":"Coordinates","text":"Pass comm= to run independent simulations on disjoint groups of ranks. The mesh is derived from the size of the communicator you pass, not from COMM_WORLD.\n\nusing Tarang, MPI\nMPI.Init()\n\nrank  = MPI.Comm_rank(MPI.COMM_WORLD)\ncolor = rank % 2                                       # two independent groups\nsub   = MPI.Comm_split(MPI.COMM_WORLD, color, rank)\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords; comm=sub, dtype=Float64, device=CPU())\n\nprintln(\"world rank $rank -> group $color, rank $(dist.rank) of $(dist.size), mesh $(dist.mesh)\")","title":"Custom MPI Communicators"},{"category":"section","location":"api/coordinates/#Load-Balancing","page":"Coordinates","text":"Each rank prints the slab it owns (run under MPI; in serial the data is a plain Array). PencilArrays splits the decomposed axis as evenly as it can.\n\nusing PencilArrays, MPI\n\nu  = ScalarField(domain, \"u\")\ngd = get_grid_data(u)\n\nfor r in 0:(dist.size - 1)\n    if dist.rank == r\n        println(\"rank $r: local shape $(local_shape(domain, :g)), owns $(PencilArrays.range_local(gd))\")\n    end\n    MPI.Barrier(dist.comm)\nend\n\nAt np=2 on a 32×32 grid this prints rank 0: local shape (32, 16), owns (1:32, 1:16) and rank 1: local shape (32, 16), owns (1:32, 17:32).\n\n","title":"Load Balancing"},{"category":"section","location":"api/coordinates/#Performance-Tips","page":"Coordinates","text":"Let the mesh default unless profiling says otherwise — it is always dimensionally valid\nUse power-of-2 mesh dimensions for optimal FFT performance\nMatch mesh to domain aspect ratio (3D) for balanced load\nMonitor communication overhead with profiling tools\nConsider memory constraints when choosing mesh\n\n","title":"Performance Tips"},{"category":"section","location":"api/coordinates/#See-Also","page":"Coordinates","text":"Bases: Spectral basis functions for each coordinate\nDomains: Combining coordinates and bases into domains\nParallelism Guide: Detailed MPI configuration\nConfiguration: Process mesh tuning","title":"See Also"},{"category":"type","location":"api/coordinates/#Tarang.CartesianCoordinates","page":"Coordinates","text":"CartesianCoordinates\n\nCartesian coordinate system with named coordinates. Following implementation in coords:159-189.\n\nConstructor\n\nCartesianCoordinates(names...; right_handed=true)\n\nArguments\n\nnames: Names for each coordinate (e.g., \"x\", \"y\", \"z\")\nright_handed: Whether the coordinate system is right-handed (only used for 3D)\n\nExamples\n\n# 1D\ncoords = CartesianCoordinates(\"x\")\n\n# 2D\ncoords = CartesianCoordinates(\"x\", \"z\")\n\n# 3D\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\ncoords[\"x\"]  # Get x coordinate\ncoords[1]    # Get first coordinate\n\n\n\n\n\n","title":"Tarang.CartesianCoordinates"},{"category":"method","location":"api/coordinates/#Tarang.coords-Tuple{CartesianCoordinates}","page":"Coordinates","text":"coords(coordsys::CoordinateSystem)\n\nReturn tuple of Coordinate objects for this coordinate system. Following pattern.\n\n\n\n\n\n","title":"Tarang.coords"},{"category":"section","location":"examples/eigenvalue_analysis/#Eigenvalue-Analysis-Examples","page":"Eigenvalue Analysis Examples","text":"Collection of linear stability and eigenvalue problem examples with Tarang.jl.","title":"Eigenvalue Analysis Examples"},{"category":"section","location":"examples/eigenvalue_analysis/#Linear-Stability-Overview","page":"Eigenvalue Analysis Examples","text":"Eigenvalue problems determine the stability of base states by solving:\n\nA mathbfx = sigma B mathbfx\n\nwhere σ is the eigenvalue (growth rate + frequency).\n\nIn Tarang the eigenvalue replaces the time derivative: you write the linearised equation exactly as you would for an IVP, keeping the dt(·) term, and the solver assembles σ M x + L x = 0. You never multiply the eigenvalue symbol into the equation yourself. Every boundary condition is declared with add_bc! and paired with a tau variable lifted into the bulk equation.\n\nwarning: Fold constant coefficients into a single parameter\nA coefficient built from several constants multiplied together inside the equation string — Pr*Ra*k2*T, or 2*k2*∂z(∂z(w)) — loses its value when the implicit matrix is assembled: the term enters L with coefficient 1. Compute the product in Julia and register it as one parameter (buoy = Pr*Ra*k^2, twok2 = 2k^2), then write buoy*T and twok2*∂z(∂z(w)). A single scalar multiplying an expression — Pr*(…), invRe*(…) — is fine.","title":"Linear Stability Overview"},{"category":"section","location":"examples/eigenvalue_analysis/#Rayleigh-Bénard-Stability","page":"Eigenvalue Analysis Examples","text":"Critical Rayleigh number for convection onset. Eliminating the pressure and the horizontal velocity leaves a fourth-order system for the vertical velocity w and the temperature perturbation T at a single horizontal wavenumber k:\n\nsigma (partial_z^2 - k^2) w = Prleft(partial_z^2 - k^2)^2 w - Ra k^2 Tright\nqquad sigma T = w + (partial_z^2 - k^2) T\n\nw is fourth order, so it needs four boundary conditions and four tau variables; T is second order and needs two. With stress-free walls (w = ∂z²w = 0) the onset is known analytically — Ra_c(k) = (π² + k²)³ / k², minimised at Ra_c = 657.51 for k = 2.2214 — which makes it a good check that the problem is assembled correctly.\n\nusing Tarang\nusing Printf\n\nfunction rbc_evp(Ra, k; Pr=1.0, Nz=32, slip=:free)\n    coords = CartesianCoordinates(\"z\")\n    dist   = Distributor(coords; dtype=Float64, device=CPU())\n    zbasis = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(0.0, 1.0))\n    domain = Domain(dist, (zbasis,))\n    lift_basis = derivative_basis(zbasis, 2)\n\n    w = ScalarField(domain, \"w\")\n    T = ScalarField(domain, \"T\")\n    # tau variables: four for the fourth-order w equation, two for T\n    tw = [ScalarField(dist, \"tw$i\", (), Float64) for i in 1:4]\n    tT = [ScalarField(dist, \"tT$i\", (), Float64) for i in 1:2]\n\n    evp = EVP([w, T, tw..., tT...]; eigenvalue=:σ)\n    add_parameters!(evp; Pr=Pr, k2=k^2, k4=k^4, twok2=2k^2, buoy=Pr*Ra*k^2,\n                    lw1=lift(tw[1], lift_basis, -1), lw2=lift(tw[2], lift_basis, -2),\n                    lw3=lift(tw[3], lift_basis, -3), lw4=lift(tw[4], lift_basis, -4),\n                    lT1=lift(tT[1], lift_basis, -1), lT2=lift(tT[2], lift_basis, -2))\n\n    # KEEP dt(·) — it builds the mass matrix M. Never multiply σ into the equation.\n    add_equation!(evp, \"dt(∂z(∂z(w)) - k2*w) \" *\n                       \"- Pr*(∂z(∂z(∂z(∂z(w)))) - twok2*∂z(∂z(w)) + k4*w) \" *\n                       \"+ buoy*T - lw1 - lw2 - lw3 - lw4 = 0\")\n    add_equation!(evp, \"dt(T) - (∂z(∂z(T)) - k2*T) - w - lT1 - lT2 = 0\")\n\n    # boundary conditions — always add_bc!, never add_equation!\n    add_bc!(evp, \"w(z=0) = 0\")\n    add_bc!(evp, \"w(z=1) = 0\")\n    if slip == :free                       # stress-free\n        add_bc!(evp, \"∂z(∂z(w))(z=0) = 0\")\n        add_bc!(evp, \"∂z(∂z(w))(z=1) = 0\")\n    else                                   # no-slip\n        add_bc!(evp, \"∂z(w)(z=0) = 0\")\n        add_bc!(evp, \"∂z(w)(z=1) = 0\")\n    end\n    add_bc!(evp, \"T(z=0) = 0\")\n    add_bc!(evp, \"T(z=1) = 0\")\n    return evp\nend\n\nfunction growth_rate(Ra, k; Pr=1.0, Nz=32)\n    solver = EigenvalueSolver(rbc_evp(Ra, k; Pr=Pr, Nz=Nz); nev=4, which=:LR)\n    λ, _ = solve!(solver)\n    return maximum(real, λ)\nend\n\nfor Ra in (500.0, 800.0)\n    @printf(\"Ra = %5.0f   σ_max = %+.6f\\n\", Ra, growth_rate(Ra, 2.2214))\nend\n\nRa =   500   σ_max = -1.894439\nRa =   800   σ_max = +1.525506\n\nThe sign of σ_max brackets the onset, so a bisection gives the critical Rayleigh number:\n\nfunction critical_Ra(k; Pr=1.0, Nz=32, Ra_lo=100.0, Ra_hi=5000.0, tol=1e-4)\n    while (Ra_hi - Ra_lo) / Ra_hi > tol\n        Ra_mid = (Ra_lo + Ra_hi) / 2\n        if growth_rate(Ra_mid, k; Pr=Pr, Nz=Nz) > 0\n            Ra_hi = Ra_mid\n        else\n            Ra_lo = Ra_mid\n        end\n    end\n    return (Ra_lo + Ra_hi) / 2\nend\n\n@printf(\"Ra_c = %.2f   (exact: %.2f)\\n\", critical_Ra(2.2214), (π^2 + 2.2214^2)^3 / 2.2214^2)\n\nRa_c = 657.53   (exact: 657.51)\n\nThe no-slip case (slip=:noslip) has no closed form, and its tau system throws off large spurious eigenvalues that which=:LR picks up first — see Spurious Modes for the filter that makes it usable.","title":"Rayleigh-Bénard Stability"},{"category":"section","location":"examples/eigenvalue_analysis/#Orr-Sommerfeld-(Plane-Poiseuille)","page":"Eigenvalue Analysis Examples","text":"Stability of channel flow with base profile U(z) = 1 - z² on z ∈ [-1, 1]. Written for the growth rate σ (the complex wave speed is then c = iσ/k):\n\nsigma (partial_z^2 - k^2)psi = -ikU(partial_z^2 - k^2)psi + ikUpsi\n    + tfrac1Re(partial_z^2 - k^2)^2psi\n\nwarning: The base-flow field must be real\nU(z) enters as a spatially varying (non-constant) coefficient, and only a real coefficient field is representable in the implicit operator. Build the fields with dtype=Float64 and keep the complex factor -ik outside, as a plain scalar parameter (mik = -1im*k). If you fold -ik into the field instead — making it complex, or declaring the fields ComplexF64 — the coefficient is silently replaced by 1 and the solver returns a converged-looking but wrong spectrum. The assembled matrices are complex regardless of the field dtype, so nothing is lost.\n\nusing Tarang\nusing Printf\n\n# A fourth-order tau system produces spurious eigenvalues. A spurious eigenvalue moves\n# when the resolution changes; a physical one does not. See Tips → Spurious Modes.\nfunction converged_eigenvalues(build, N1, N2; nev=25, which=:LR, rtol=1e-5)\n    λ1, _ = solve!(EigenvalueSolver(build(N1); nev=nev, which=which))\n    λ2, _ = solve!(EigenvalueSolver(build(N2); nev=nev, which=which))\n    return [λ for λ in λ1 if minimum(abs.(λ .- λ2)) <= rtol * max(1.0, abs(λ))]\nend\n\nfunction os_evp(Re, k, Nz)\n    coords = CartesianCoordinates(\"z\")\n    dist   = Distributor(coords; dtype=Float64, device=CPU())\n    zbasis = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(-1.0, 1.0))\n    domain = Domain(dist, (zbasis,))\n    lift_basis = derivative_basis(zbasis, 2)\n\n    psi = ScalarField(domain, \"psi\")\n    tp  = [ScalarField(dist, \"tp$i\", (), Float64) for i in 1:4]   # fourth order → 4 taus\n\n    U = ScalarField(domain, \"U\")            # REAL base flow, U(z) = 1 - z²\n    z = local_grid(zbasis, dist, 1)\n    ensure_layout!(U, :g)\n    get_grid_data(U) .= 1 .- z .^ 2\n    ensure_layout!(U, :c)\n\n    evp = EVP([psi, tp...]; eigenvalue=:σ)\n    add_parameters!(evp; U=U, k2=k^2, k4=k^4, twok2=2k^2,\n                    mik=-1im*k,           # -ik\n                    Upp=1im*k*(-2.0),     #  ik U''   (U'' = -2)\n                    invRe=1.0/Re,\n                    l1=lift(tp[1], lift_basis, -1), l2=lift(tp[2], lift_basis, -2),\n                    l3=lift(tp[3], lift_basis, -3), l4=lift(tp[4], lift_basis, -4))\n\n    add_equation!(evp, \"dt(∂z(∂z(psi)) - k2*psi) \" *\n                       \"- mik*(U*(∂z(∂z(psi)) - k2*psi)) - Upp*psi \" *\n                       \"- invRe*(∂z(∂z(∂z(∂z(psi)))) - twok2*∂z(∂z(psi)) + k4*psi) \" *\n                       \"- l1 - l2 - l3 - l4 = 0\")\n\n    # No-slip: ψ = ∂ψ/∂z = 0 at both walls\n    add_bc!(evp, \"psi(z=-1) = 0\")\n    add_bc!(evp, \"psi(z=1) = 0\")\n    add_bc!(evp, \"∂z(psi)(z=-1) = 0\")\n    add_bc!(evp, \"∂z(psi)(z=1) = 0\")\n    return evp\nend\n\nRe, k = 10000.0, 1.0\nλ = converged_eigenvalues(N -> os_evp(Re, k, N), 64, 80; nev=30)\nσ = λ[argmax(real.(λ))]                 # most unstable growth rate\n@printf(\"c = %s\\n\", string(round(1im * σ / k, digits=8)))\n\nc = 0.23752649 + 0.00373967im\n\nThis is Orszag's (1971) benchmark value for Re = 10000, k = 1, to all eight digits; Im(c) > 0, so the mode grows.","title":"Orr-Sommerfeld (Plane Poiseuille)"},{"category":"section","location":"examples/eigenvalue_analysis/#Neutral-Curves","page":"Eigenvalue Analysis Examples","text":"The stability boundary in parameter space is one bisection per wavenumber, reusing critical_Ra from the Rayleigh-Bénard example:\n\nks   = [1.5, 2.0, 2.2214, 2.5, 3.0]\nRa_n = [critical_Ra(k) for k in ks]\n\nfor (k, Ra) in zip(ks, Ra_n)\n    @printf(\"k = %.4f   Ra_c = %8.2f   (exact %8.2f)\\n\", k, Ra, (π^2 + k^2)^3 / k^2)\nend\n\ni = argmin(Ra_n)\n@printf(\"critical point: k = %.4f, Ra_c = %.2f\\n\", ks[i], Ra_n[i])\n\nk = 1.5000   Ra_c =   791.19   (exact   791.19)\nk = 2.0000   Ra_c =   667.02   (exact   667.01)\nk = 2.2214   Ra_c =   657.53   (exact   657.51)\nk = 2.5000   Ra_c =   670.16   (exact   670.17)\nk = 3.0000   Ra_c =   746.54   (exact   746.53)\ncritical point: k = 2.2214, Ra_c = 657.53\n\nRefine the k grid around the minimum to locate the critical point more precisely.","title":"Neutral Curves"},{"category":"section","location":"examples/eigenvalue_analysis/#Eigenmode-Structure","page":"Eigenvalue Analysis Examples","text":"solve! returns (eigenvalues, eigenvectors). The eigenvector columns live in the subproblem's coefficient space — every variable and tau concatenated, not a per-field dictionary. Write a column back onto the problem's fields with scatter_inputs, then transform to the grid:\n\nNz  = 32\nevp = rbc_evp(2000.0, 2.2214; Nz=Nz)\nsolver = EigenvalueSolver(evp; nev=4, which=:LR)\nλ, v = solve!(solver)\n\n# most unstable mode → scatter it back onto the problem's variables\nmode = v[:, argmax(real.(λ))]\nTarang.scatter_inputs(solver.subproblems[1], mode, evp.variables)\n\nw = evp.variables[1]\nensure_layout!(w, :g)\nw_profile = real.(copy(get_grid_data(w)))\nw_profile ./= maximum(abs, w_profile)          # normalise\n\ncoords = CartesianCoordinates(\"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nz      = local_grid(ChebyshevT(coords[\"z\"]; size=Nz, bounds=(0.0, 1.0)), dist, 1)\n\n@printf(\"σ = %.6f\\n\", maximum(real, λ))\n@printf(\"max | |w| - |sin(πz)| | = %.2e\\n\",\n        maximum(abs, abs.(w_profile) .- abs.(sin.(π .* z))))\n\nσ = 11.015345\nmax | |w| - |sin(πz)| | = 3.16e-03\n\nThe stress-free eigenfunction is sin(πz), recovered here to three digits. z and w_profile are plain vectors — plot them with the package of your choice.","title":"Eigenmode Structure"},{"category":"section","location":"examples/eigenvalue_analysis/#Solver-Options","page":"Eigenvalue Analysis Examples","text":"","title":"Solver Options"},{"category":"section","location":"examples/eigenvalue_analysis/#Finding-Specific-Eigenvalues","page":"Eigenvalue Analysis Examples","text":"Building a solver merges the boundary conditions into the equation set, which mutates the problem: a given EVP can be handed to EigenvalueSolver only once. Reusing it raises Number of equations (14) does not match number of variables (8). Build a fresh problem for each solver — which is why every example here wraps the problem in a function.\n\n# Most unstable (largest real part)\nsolver = EigenvalueSolver(rbc_evp(2000.0, 2.2214); nev=10, which=:LR)\n\n# Largest magnitude\nsolver = EigenvalueSolver(rbc_evp(2000.0, 2.2214); nev=10, which=:LM)\n\n# Near a target value\nsolver = EigenvalueSolver(rbc_evp(2000.0, 2.2214); nev=5, target=0.1+1.5im)\n\n# Most oscillatory (largest imaginary)\nsolver = EigenvalueSolver(rbc_evp(2000.0, 2.2214); nev=10, which=:LI)\n\nwhich accepts the symbols :LM :SM :LR :SR :LI :SI, or pass target=… to order by proximity to a shift. The EigenvalueSolver constructor takes only nev, which, target, and matsolver keywords.","title":"Finding Specific Eigenvalues"},{"category":"section","location":"examples/eigenvalue_analysis/#Selecting-eigenvalues-by-magnitude","page":"Eigenvalue Analysis Examples","text":"# Smallest-magnitude eigenvalues (e.g. least-damped modes)\nsolver = EigenvalueSolver(rbc_evp(2000.0, 2.2214); nev=20, which=:SM)\nλ, _ = solve!(solver)\n@printf(\"least-damped: σ = %+.6f\\n\", λ[1])\n\nleast-damped: σ = +11.015345","title":"Selecting eigenvalues by magnitude"},{"category":"section","location":"examples/eigenvalue_analysis/#Tips","page":"Eigenvalue Analysis Examples","text":"","title":"Tips"},{"category":"section","location":"examples/eigenvalue_analysis/#Resolution","page":"Eigenvalue Analysis Examples","text":"Start coarse (N=32), refine until eigenvalues converge\nChebyshev provides exponential convergence\nBoundary layer modes need more resolution","title":"Resolution"},{"category":"section","location":"examples/eigenvalue_analysis/#Spurious-Modes","page":"Eigenvalue Analysis Examples","text":"The tau rows carry zero mass, so a tau system throws off spurious eigenvalues — and they are not small. The no-slip Rayleigh-Bénard problem above returns modes near +8e5 at Nz = 24, +3e6 at Nz = 32 and +1.7e7 at Nz = 48: large enough to clear the solver's internal |λ| < 1e10 cut, and picked first by which=:LR, which would report a comfortably stable state as violently unstable.\n\nThe tell is in those numbers — a spurious eigenvalue moves with the resolution, a physical one does not. Solve at two resolutions and keep the modes that agree (this is the converged_eigenvalues used in the Orr-Sommerfeld example):\n\nfunction critical_Ra_filtered(k; N1=24, N2=32, Ra_lo=500.0, Ra_hi=4000.0, tol=1e-4)\n    while (Ra_hi - Ra_lo) / Ra_hi > tol\n        Ra_mid = (Ra_lo + Ra_hi) / 2\n        λ = converged_eigenvalues(N -> rbc_evp(Ra_mid, k; Nz=N, slip=:noslip),\n                                  N1, N2; nev=10)\n        maximum(real, λ) > 0 ? (Ra_hi = Ra_mid) : (Ra_lo = Ra_mid)\n    end\n    return (Ra_lo + Ra_hi) / 2\nend\n\n@printf(\"no-slip Ra_c = %.2f   (Chandrasekhar: 1707.762)\\n\", critical_Ra_filtered(3.117))\n\nno-slip Ra_c = 1707.77   (Chandrasekhar: 1707.762)\n\nAlso check the mode structure — spurious eigenfunctions oscillate wildly at the grid scale — and confirm that the answer is insensitive to nev.","title":"Spurious Modes"},{"category":"section","location":"examples/eigenvalue_analysis/#Physical-Validation","page":"Eigenvalue Analysis Examples","text":"Compare with known analytical results: stress-free Rayleigh-Bénard has Ra_c(k) = (π² + k²)³/k²; no-slip has Ra_c = 1707.762 at k = 3.117; the Orr-Sommerfeld benchmark is c = 0.23752649 + 0.00373967i at Re = 10⁴, k = 1\nCheck limiting cases\nVerify neutral curve shape","title":"Physical Validation"},{"category":"section","location":"examples/eigenvalue_analysis/#See-Also","page":"Eigenvalue Analysis Examples","text":"Eigenvalue Problems Tutorial\nAPI: Solvers\nExample Gallery","title":"See Also"},{"category":"section","location":"pages/testing/#Testing","page":"Testing","text":"Guide to running and writing tests for Tarang.jl.","title":"Testing"},{"category":"section","location":"pages/testing/#Running-Tests","page":"Testing","text":"","title":"Running Tests"},{"category":"section","location":"pages/testing/#Full-Test-Suite","page":"Testing","text":"julia --project=. -e 'using Pkg; Pkg.test()'","title":"Full Test Suite"},{"category":"section","location":"pages/testing/#Specific-Test-File","page":"Testing","text":"julia --project=. test/test_specific.jl","title":"Specific Test File"},{"category":"section","location":"pages/testing/#With-MPI","page":"Testing","text":"The multi-rank MPI tests each run in their own mpiexec world, via a driver (CI exercises 1, 2, and 4 ranks):\n\njulia --project=. test/run_mpi_ci.jl 4      # all MPI tests at 4 ranks\n./test/run_mpi_tests.sh 4                    # convenience wrapper","title":"With MPI"},{"category":"section","location":"pages/testing/#GPU","page":"Testing","text":"GPU tests need an NVIDIA GPU and run on JuliaGPU Buildkite CI (see Continuous Integration). To run them locally on a CUDA host:\n\njulia --project=. -e 'using Pkg; Pkg.add(\"CUDA\")'   # CUDA is a weak dependency\njulia --project=. test/run_gpu_ci.jl                # single-process GPU tests\n# distributed (NCCL) tests across, e.g., 2 GPUs:\nTARANG_MPI_FILESET=distributed_gpu julia --project=. test/run_mpi_ci.jl 2","title":"GPU"},{"category":"section","location":"pages/testing/#Test-Structure","page":"Testing","text":"test/\n├── runtests.jl              # Main test runner\n├── test_cfl.jl              # CFL condition tests\n├── test_domain_metadata.jl  # Domain tests\n├── test_solvers.jl          # Solver tests\n├── test_flow_tools.jl       # Analysis tools\n├── test_quick_domains.jl    # Domain helpers\n├── test_plot_tools.jl       # Visualization\n└── test_compatibility.jl    # Compatibility tests","title":"Test Structure"},{"category":"section","location":"pages/testing/#Writing-Tests","page":"Testing","text":"","title":"Writing Tests"},{"category":"section","location":"pages/testing/#Basic-Test","page":"Testing","text":"using Test\nusing Tarang\n\n@testset \"My Feature\" begin\n    # Setup\n    coords = CartesianCoordinates(\"x\")\n    dist = Distributor(coords; mesh=(1,), dtype=Float64)\n\n    # Test\n    @test dist.size == 1\n    @test dist.rank == 0\nend","title":"Basic Test"},{"category":"section","location":"pages/testing/#Testing-Fields","page":"Testing","text":"@testset \"ScalarField\" begin\n    coords = CartesianCoordinates(\"x\")\n    dist = Distributor(coords; mesh=(1,), dtype=Float64)\n    basis = RealFourier(coords[\"x\"]; size=8, bounds=(0.0, 2π))\n\n    field = ScalarField(dist, \"T\", (basis,), Float64)\n\n    # Test creation\n    @test field.name == \"T\"\n    @test field.dtype == Float64\n\n    # Test data\n    Tarang.ensure_layout!(field, :g)\n    get_grid_data(field) .= 1.0\n    @test all(get_grid_data(field) .== 1.0)\nend","title":"Testing Fields"},{"category":"section","location":"pages/testing/#Testing-Transforms","page":"Testing","text":"@testset \"Transforms\" begin\n    # Setup\n    field = ScalarField(dist, \"f\", (basis,), Float64)\n\n    # Initialize in grid space\n    Tarang.ensure_layout!(field, :g)\n    get_grid_data(field) .= sin.(x_grid)\n\n    # Transform to spectral\n    Tarang.ensure_layout!(field, :c)\n\n    # Transform back\n    Tarang.ensure_layout!(field, :g)\n\n    # Check roundtrip\n    @test get_grid_data(field) ≈ sin.(x_grid) atol=1e-10\nend","title":"Testing Transforms"},{"category":"section","location":"pages/testing/#Testing-Solvers","page":"Testing","text":"@testset \"IVP Solver\" begin\n    # Setup problem\n    problem = IVP([field])\n    Tarang.add_equation!(problem, \"∂t(f) = -f\")\n\n    # Create solver\n    solver = InitialValueSolver(problem, RK222(); dt=0.01)\n\n    # Run\n    step!(solver)\n\n    # Check\n    @test solver.sim_time ≈ 0.01\n    @test solver.iteration == 1\nend","title":"Testing Solvers"},{"category":"section","location":"pages/testing/#Test-Patterns","page":"Testing","text":"","title":"Test Patterns"},{"category":"section","location":"pages/testing/#Analytical-Comparison","page":"Testing","text":"@testset \"Analytical Solution\" begin\n    # Solve diffusion equation\n    # Compare with exact solution\n    exact = exp.(-kappa * k^2 * t) .* initial\n\n    @test maximum(abs.(numerical .- exact)) < 1e-6\nend","title":"Analytical Comparison"},{"category":"section","location":"pages/testing/#Convergence-Test","page":"Testing","text":"@testset \"Convergence\" begin\n    errors = Float64[]\n\n    for N in [16, 32, 64, 128]\n        # Solve at resolution N\n        error = compute_error(N)\n        push!(errors, error)\n    end\n\n    # Check spectral convergence\n    for i in 2:length(errors)\n        @test errors[i] < errors[i-1] / 2\n    end\nend","title":"Convergence Test"},{"category":"section","location":"pages/testing/#MPI-Test","page":"Testing","text":"@testset \"MPI Parallelism\" begin\n    MPI.Init()\n\n    rank = MPI.Comm_rank(MPI.COMM_WORLD)\n    size = MPI.Comm_size(MPI.COMM_WORLD)\n\n    # Test distributed computation\n    local_sum = compute_local()\n    global_sum = MPI.Allreduce(local_sum, MPI.SUM, MPI.COMM_WORLD)\n\n    @test global_sum ≈ expected_total\n\n    MPI.Finalize()\nend","title":"MPI Test"},{"category":"section","location":"pages/testing/#Test-Coverage","page":"Testing","text":"","title":"Test Coverage"},{"category":"section","location":"pages/testing/#Generate-Coverage-Report","page":"Testing","text":"using Coverage\n\n# Run tests with coverage\ncoverage = process_folder()\n\n# Print summary\nprintln(coverage)","title":"Generate Coverage Report"},{"category":"section","location":"pages/testing/#Continuous-Integration","page":"Testing","text":"CPU tests run on GitHub Actions for every push and pull request:\n\nthe default suite on Julia 1.10/1.11/1.12 across Linux, macOS, and Windows;\nthe optional CPU feature tests (TARANG_ONLY_OPTIONAL_TESTS=true);\nthe MPI suite via test/run_mpi_ci.jl at 1, 2, and 4 ranks.\n\nGPU tests cannot run on GitHub-hosted runners (no NVIDIA GPU), so they run on the JuliaGPU Buildkite CI, defined in .buildkite/pipeline.yml:\n\na single-GPU job (test/run_gpu_ci.jl) on Julia 1.10/1.11/1.12;\na multi-GPU NCCL job (test/run_mpi_ci.jl with the distributed_gpu fileset, 2 ranks).\n\nBecause CUDA is a weak dependency and NCCL is loaded dynamically — which keeps CPU installs lean — these jobs Pkg.add CUDA/NCCL before running instead of using the standard package test target.\n\nnote: Enabling GPU CI\nThe Buildkite pipeline is inert until the repository is added to the JuliaGPU Buildkite organization (free for open-source packages): ask on the Julia Slack #gpu channel for a Buildkite admin to create the pipeline, with the Buildkite GitHub App installed on the hosting account. The pipeline file is then run automatically on each push/PR.","title":"Continuous Integration"},{"category":"section","location":"pages/testing/#See-Also","page":"Testing","text":"Contributing: Development guidelines\nArchitecture: Code structure","title":"See Also"},{"category":"section","location":"pages/solvers/#Solvers","page":"Solvers","text":"Solvers integrate PDEs in time or solve for steady states.","title":"Solvers"},{"category":"section","location":"pages/solvers/#InitialValueSolver","page":"Solvers","text":"For time-dependent problems (IVP).\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\nybasis = RealFourier(coords[\"y\"]; size=16, bounds=(0.0, 2π))\ndomain = Domain(dist, (xbasis, ybasis))\n\nu = ScalarField(domain, \"u\")\nproblem = IVP([u])\nadd_parameters!(problem, nu=0.1)\nadd_equation!(problem, \"∂t(u) - nu*lap(u) = 0\")\nset!(u, (x, y) -> sin(x) * cos(y))\n\n# Create solver\nsolver = InitialValueSolver(problem, RK222(); dt=0.001)\n\n# Time stepping\nt_end = 0.01\nwhile solver.sim_time < t_end\n    step!(solver)\nend","title":"InitialValueSolver"},{"category":"section","location":"pages/solvers/#Properties","page":"Solvers","text":"solver.problem       # The IVP problem\nsolver.timestepper   # Time integration scheme\nsolver.dt            # Current timestep\nsolver.sim_time      # Current simulation time\nsolver.iteration     # Iteration count\nsolver.state         # Vector of the problem's ScalarFields\nsolver.rhs_plan      # Compiled RHS plan; `solver.rhs_plan.is_compiled` is the fast-path flag","title":"Properties"},{"category":"section","location":"pages/solvers/#Methods","page":"Solvers","text":"# Advance one step\nstep!(solver)           # Use solver.dt\nstep!(solver, 5e-4)     # Step with the given dt, and adopt it as solver.dt\n\n# Check stopping conditions\nproceed(solver)         # Returns true if should continue\n\n# Run the whole loop (handles stopping, CFL, outputs, callbacks)\nrun!(solver; stop_iteration=100, progress=false)\n\n# Print a tree-style summary of the solver configuration\ndiagnose(solver)","title":"Methods"},{"category":"section","location":"pages/solvers/#Internal-solver-build-path","page":"Solvers","text":"At solver construction, InitialValueSolver(problem, timestepper; dt=...) runs several build stages in order:\n\nbuild_solver_matrices!(solver) — parses the equation strings and assembles the global L_matrix, M_matrix, and F_vector for legacy fall-back paths. These are stored in problem.parameters.\n_try_build_subproblems!(solver) — this is the fast path. It decomposes the problem into per-Fourier-mode subproblems via build_subproblems in src/core/subsystems/, building small sparse L_min / M_min matrices per mode, applying left/right permutations, and running valid-mode filtering to drop trivially-satisfied constraint rows (like integ(p) = 0 at non-DC modes). The resulting Tuple{Vararg{Subproblem}} is stored in problem.parameters[\"subproblems\"] and drives the modern IMEX stepper.\nbuild_lazy_rhs_plan!(solver) — walks each equation's F expression and translates it into a type-parametric LazyFuture tree (in src/core/solvers/lazy_rhs.jl). Each node (LazyAdd, LazySub, LazyMul, LazyDiv, LazyPow, LazyUnaryFunc, LazyDiff, LazyMultiDiff, LazyStateField, LazyParamField, LazyConst, …) has a specialized evaluate_lazy! method. Julia's JIT then specializes the whole evaluate_lazy! call chain on first use — eliminating dynamic dispatch and fusing broadcast operations. If translation fails for any equation, the plan's is_compiled flag stays false and evaluate_rhs falls back to the interpreted expression path for the entire solver. See the RHS compilation section — the fallback is ~100× slower, so it is worth checking (solver.rhs_plan.is_compiled, or the diagnose(solver) output).\n_apply_bc_values_to_equations!(solver, 0.0) — only runs if there are time- or space-dependent BCs. Populates the initial equation_data[eq_idx][\"F\"] slots with the BC values evaluated at t=0, and auto-registers global coordinate arrays in problem.bc_manager.coordinate_fields so user BC expressions referencing x, y, z, t can be resolved at runtime.\n\nAll four steps are transparent to the user. You only interact with the resulting solver object.","title":"Internal solver build path"},{"category":"section","location":"pages/solvers/#BoundaryValueSolver","page":"Solvers","text":"BoundaryValueSolver solves both steady problem types: it dispatches on the problem, solving an LBVP with a single linear solve and an NLBVP with Newton iteration. There is no separate nonlinear solver type.","title":"BoundaryValueSolver"},{"category":"section","location":"pages/solvers/#Linear-(LBVP)","page":"Solvers","text":"Manufactured Poisson problem Δu = -2 on z ∈ [0, 1] with u(0) = u(1) = 0, whose exact solution is u(z) = z(1-z):\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=4,  bounds=(0.0, 2π))\nzbasis = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))\ndomain = Domain(dist, (xbasis, zbasis))\n\nu    = ScalarField(domain, \"u\")\ntau1 = ScalarField(dist, \"tau1\", (xbasis,), Float64)\ntau2 = ScalarField(dist, \"tau2\", (xbasis,), Float64)\nlb   = derivative_basis(zbasis, 2)\n\nproblem = LBVP([u, tau1, tau2])\nadd_parameters!(problem; Lz=1.0, l1=lift(tau1, lb, -1), l2=lift(tau2, lb, -2))\nadd_equation!(problem, \"Δ(u) + l1 + l2 = -2\")\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=Lz) = 0\")\n\n# Create solver and solve\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\n\n# Solution is in the field variables\nensure_layout!(u, :g)\nget_grid_data(u)          # matches z(1-z) to 1.4e-16","title":"Linear (LBVP)"},{"category":"section","location":"pages/solvers/#Nonlinear-(NLBVP)","page":"Solvers","text":"Declare the problem as an NLBVP and put the nonlinear terms on the right-hand side. The same BoundaryValueSolver then runs a per-Fourier-mode Newton iteration, rebuilding the Frechet Jacobian at the current state each iteration.\n\n# Δu + lift(τ) = u² + g   with   g = -2 - (z(1-z))²\n# chosen so the equation reduces to Δu = -2 and the exact solution is again z(1-z).\ng = ScalarField(domain, \"g\")\nensure_layout!(g, :g)\nzg = Tarang.create_meshgrid(domain; on_device=false)[\"z\"]\nget_grid_data(g) .= -2 .- (zg .* (1 .- zg)) .^ 2\n\nproblem = NLBVP([u, tau1, tau2])\nadd_parameters!(problem; Lz=1.0, l1=lift(tau1, lb, -1), l2=lift(tau2, lb, -2), g=g)\nadd_equation!(problem, \"Δ(u) + l1 + l2 = u*u + g\")\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=Lz) = 0\")\n\nensure_layout!(u, :g)\nget_grid_data(u) .= 0.0   # Newton initial guess\n\nsolver = BoundaryValueSolver(problem; tolerance=1e-10, max_iterations=50)\nsolve!(solver)            # Newton; returns the solver, warns if it does not converge\n\nOnly tolerance and max_iterations are Newton knobs (defaults 1e-10 and 100); they can also be set after construction (solver.tolerance = 1e-8). solve! always returns the solver and leaves the solution in the field variables — it does not return a convergence flag. Non-convergence is reported as a warning naming the final residual:\n\n┌ Warning: NLBVP per-mode Newton did not reach tolerance 1.0e-10 in 100 iters (final |F|=…)","title":"Nonlinear (NLBVP)"},{"category":"section","location":"pages/solvers/#EigenvalueSolver","page":"Solvers","text":"For eigenvalue problems (EVP). The eigenvalue symbol declared with eigenvalue= replaces dt(...) in the equations, so dt(u) - Δ(u) = 0 becomes the generalized problem σ M u + L u = 0 and the returned values are the growth rates σ.\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nzbasis = ChebyshevT(coords[\"z\"]; size=24, bounds=(0.0, 1.0))\ndomain = Domain(dist, (zbasis,))\n\nu    = ScalarField(domain, \"u\")\ntau1 = ScalarField(dist, \"tau1\", (), Float64)\ntau2 = ScalarField(dist, \"tau2\", (), Float64)\nlb   = derivative_basis(zbasis, 2)\n\nproblem = EVP([u, tau1, tau2]; eigenvalue=:σ)\nadd_parameters!(problem; Lz=1.0, l1=lift(tau1, lb, -1), l2=lift(tau2, lb, -2))\nadd_equation!(problem, \"dt(u) - Δ(u) - l1 - l2 = 0\")\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=Lz) = 0\")\n\n# Create solver\nsolver = EigenvalueSolver(problem;\n    nev=4,        # Number of eigenvalues\n    which=\"SM\"    # Smallest magnitude\n)\n\n# Solve\neigenvalues, eigenvectors = solve!(solver)\n# eigenvalues ≈ [-9.8696, -39.4784, -88.8264, -157.9137] = -(nπ)², the Dirichlet Laplacian\n\nnev, which and target may be given to the constructor or overridden at solve time (solve!(solver; nev=8, which=\"LR\")); the values used are stored back on the solver. Passing target=0.0+0.0im selects the nev eigenvalues closest to the target instead of using which.\n\nEigenvectors come back only when the problem has exactly one active subproblem — as above, where there is no separable Fourier axis. A Fourier axis makes every mode its own subproblem, and solve! then returns the eigenvalues pooled over all modes together with an empty 0×0 eigenvector matrix.","title":"EigenvalueSolver"},{"category":"section","location":"pages/solvers/#Which-Eigenvalues","page":"Solvers","text":"\"LM\": Largest magnitude (default)\n\"SM\": Smallest magnitude\n\"LR\": Largest real part (most unstable)\n\"SR\": Smallest real part\n\"LI\": Largest imaginary part\n\"SI\": Smallest imaginary part","title":"Which Eigenvalues"},{"category":"section","location":"pages/solvers/#Time-Steppers","page":"Solvers","text":"","title":"Time Steppers"},{"category":"section","location":"pages/solvers/#IMEX-Runge-Kutta","page":"Solvers","text":"RK111()  # 1st order, 1 stage\nRK222()  # 2nd order, 3 stages: explicit first stage + 2 implicit stages (recommended)\nRK443()  # 3rd order, 4 stages (higher accuracy)\nRKSMR()  # SMR: explicit 3rd order, implicit 2nd order","title":"IMEX Runge-Kutta"},{"category":"section","location":"pages/solvers/#IMEX-Multistep-Methods","page":"Solvers","text":"For problems with stiff linear terms:\n\nCNAB1()  # Crank-Nicolson Adams-Bashforth, 1st order\nCNAB2()  # Crank-Nicolson Adams-Bashforth, 2nd order\n\nSBDF1()  # Semi-implicit BDF, 1st order\nSBDF2()  # Semi-implicit BDF, 2nd order\nSBDF3()  # Semi-implicit BDF, 3rd order\nSBDF4()  # Semi-implicit BDF, 4th order\n\nExponential and diagonal-spectral families are also public:\n\nETD_RK222()\nETD_CNAB2()\nETD_SBDF2()\n\nDiagonalIMEX_RK222()\nDiagonalIMEX_RK443()\nDiagonalIMEX_SBDF2()\n\nThe ETD types use global matrix φ-functions in serial; under MPI pure-Fourier execution they currently share the distributed ETD-RK2 path. Diagonal IMEX requires a SpectralLinearOperator registered with set_spectral_linear_operator!.","title":"IMEX Multistep Methods"},{"category":"section","location":"pages/solvers/#Choosing-a-Timestepper","page":"Solvers","text":"Problem Type Recommended\nGeneral purpose RK222, RK443\nClassic incompressible DNS RKSMR\nStiff linear term CNAB2, SBDF2\nSmooth high-order integration RK443, SBDF3, SBDF4\nAffordable global matrix exponential ETDRK222, ETDCNAB2, ETD_SBDF2\nPure-Fourier diagonal linear term Diagonal IMEX family","title":"Choosing a Timestepper"},{"category":"section","location":"pages/solvers/#Adaptive-Time-Stepping","page":"Solvers","text":"","title":"Adaptive Time Stepping"},{"category":"section","location":"pages/solvers/#CFL-Condition","page":"Solvers","text":"CFL is built from an InitialValueSolver (not from the problem), and velocities are registered as VectorFields. The simplest way to use it is to hand it to run!, which recomputes solver.dt before each step:\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π), dealias=3/2)\nybasis = RealFourier(coords[\"y\"]; size=16, bounds=(0.0, 2π), dealias=3/2)\ndomain = Domain(dist, (xbasis, ybasis))\n\ns = ScalarField(domain, \"s\")\nu = VectorField(domain, \"u\")\n\nproblem = IVP([s, u])\nadd_parameters!(problem, nu=0.05)\nadd_equation!(problem, \"∂t(s) - nu*lap(s) = -u⋅∇(s)\")\nadd_equation!(problem, \"∂t(u) - nu*lap(u) = 0\")\nset!(s, (x, y) -> sin(x) * cos(y))\nset!(u.components[1], (x, y) -> 0.5)\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\n# Create CFL controller from the SOLVER\ncfl = CFL(solver;\n    initial_dt=1e-3,  # Starting timestep\n    cadence=5,        # Recompute dt every 5 iterations\n    safety=0.4,       # Safety factor\n    threshold=0.1,    # Only commit a dt change larger than 10% (avoids LU rebuilds)\n    max_change=1.5,   # Max dt increase per update\n    min_change=0.5,   # Max dt decrease per update\n    max_dt=0.01       # Upper limit on dt\n)\n\n# Register velocity field (VectorField only)\nadd_velocity!(cfl, u)\n\n# run! drives the controller\nrun!(solver; stop_iteration=10, cfl=cfl, progress=false)\nsolver.dt          # 0.01 — updated by the controller\n\nIf you write the loop yourself, ask the controller for the new step and assign it:\n\nsolver.stop_iteration = 20\nwhile proceed(solver)\n    solver.dt = compute_timestep(cfl)\n    step!(solver)\nend","title":"CFL Condition"},{"category":"section","location":"pages/solvers/#CFL-Parameters","page":"Solvers","text":"All are keyword arguments to CFL(solver; …) and are also live fields of the returned object (cfl.max_dt = 0.02 after construction is fine).\n\nParameter Default Typical Value Description\ninitial_dt 0.01 your starting dt dt used before the first recomputation\ncadence 1 1-10 Recompute dt every N iterations\nsafety 0.4 0.3-0.5 Lower = more stable\nthreshold 0.1 0.0-0.2 Relative change below which dt is kept (0 = commit every change)\nmax_change 2.0 1.2-2.0 Smooth dt increases\nmin_change 0.5 0.5 Prevent sudden drops\nmax_dt Inf your output cadence Hard ceiling on dt\n\nThere is no min_dt: the floor on the timestep is expressed as the ratio min_change, not as an absolute value. The current step is cfl.current_dt.","title":"CFL Parameters"},{"category":"section","location":"pages/solvers/#Diffusive-Limit-for-Explicit-Diffusion","page":"Solvers","text":"warning: Warning\nWith only add_velocity! registered, compute_timestep returns an advection-only step, dt = safety / max(Σᵢ |uᵢ|/Δxᵢ). Diffusion that the timestepper integrates explicitly is not accounted for at all.\n\nThe usual offender is an LES eddy viscosity νₑ. A spatially varying coefficient cannot go down the implicit path, so it is stepped explicitly and carries its own parabolic limit. Register it with add_diffusivity!:\n\ncfl = CFL(solver; safety=0.4)\nadd_velocity!(cfl, u)\n\nadd_diffusivity!(cfl, nu)                             # constant, explicitly-treated ν\nadd_diffusivity!(cfl, get_eddy_viscosity(les_model))  # LES νₑ, refreshed in place\nadd_diffusivity!(cfl, nu_field)                       # a ScalarField\nadd_diffusivity!(cfl, nu_array; domain=other.domain)  # explicit grid for a bare array\n\nArgument Meaning\nReal Constant diffusivity\nAbstractArray Per-point diffusivity; under MPI this rank's slab, reduced for you\nScalarField Transformed to grid space on each evaluation\ndomain= Grid supplying the spacings (defaults to the field's, else the first velocity's, else the problem's)\n\nEach entry contributes the frequency\n\nf_diff = 2 ν_max Σᵢ Δxᵢ⁻²        ⟹        dt ≤ 1 / f_diff\n\nwhich is the forward-Euler limit of the second-order central Laplacian (extreme eigenvalue -4ν Σᵢ Δxᵢ⁻², so |1 + λ dt| ≤ 1 gives dt ≤ 1/(2ν Σᵢ Δxᵢ⁻²)). The sum over axes is the anisotropic form; on an isotropic d-dimensional grid it collapses to the familiar dt ≤ Δx²/(2dν). The advective and diffusive limits are combined by taking the smaller step (the larger frequency), and safety applies to both.\n\nNotes:\n\nOpt-in. A CFL with no registered diffusivity behaves exactly as before.\nRegister once. Arrays and fields are held by reference, so a model that overwrites νₑ in place every step is picked up automatically.\nOne collective. Diffusivities share the single batched Allreduce(MAX) with the velocities, so ν_max is the global maximum without adding communication.\nImplicit diffusion needs no registration — only what the timestepper treats explicitly.\nChebyshev axes matter most. grid_spacing reports the minimum near-wall spacing, Δz = L(1 − cos(π/(N−1)))/2, which is far below L/N; the Δz⁻² there routinely dominates the step.","title":"Diffusive Limit for Explicit Diffusion"},{"category":"section","location":"pages/solvers/#Stopping-Conditions","page":"Solvers","text":"# Set stop conditions\nsolver.stop_sim_time = 10.0      # Stop at t=10\nsolver.stop_wall_time = 3600.0   # Stop after 1 hour\nsolver.stop_iteration = 10000    # Stop after 10000 steps\n\n# Use proceed() to check\nwhile proceed(solver)\n    step!(solver)\nend\n\nrun! sets the same three fields from its stop_time, stop_wall_time and stop_iteration keywords, so run!(solver; stop_time=10.0) is equivalent to the loop above.","title":"Stopping Conditions"},{"category":"section","location":"pages/solvers/#Complete-Example","page":"Solvers","text":"using Tarang\n\n# Domain\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=32, bounds=(0.0, 2π), dealias=3/2)\nybasis = RealFourier(coords[\"y\"]; size=32, bounds=(0.0, 2π), dealias=3/2)\ndomain = Domain(dist, (xbasis, ybasis))\n\n# Fields and problem: a scalar advected by a fixed velocity, with diffusion\ns = ScalarField(domain, \"s\")\nu = VectorField(domain, \"u\")\n\nproblem = IVP([s, u])\nadd_parameters!(problem, nu=0.01)\nadd_equation!(problem, \"∂t(s) - nu*lap(s) = -u⋅∇(s)\")\nadd_equation!(problem, \"∂t(u) - nu*lap(u) = 0\")\n\nset!(s, (x, y) -> sin(x) * cos(y))\nset!(u.components[1], (x, y) -> 1.0)\n\n# Solver\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\n# CFL\ncfl = CFL(solver; initial_dt=1e-3, cadence=10, safety=0.5, max_dt=0.01)\nadd_velocity!(cfl, u)\n\n# Run: stopping, adaptive dt and logging in one call\nrun!(solver; stop_time=0.2, cfl=cfl,\n     callbacks=[(20, sol -> println(\"t = $(sol.sim_time), dt = $(sol.dt)\"))],\n     progress=false)\n\nThe same script runs under MPI without modification — launch it with mpiexec and see Parallelism.","title":"Complete Example"},{"category":"section","location":"pages/solvers/#See-Also","page":"Solvers","text":"Problems: Problem definition\nTimesteppers: Time integration details\nAPI: Solvers: Complete reference","title":"See Also"},{"category":"section","location":"api/operators/#Operators-API","page":"Operators","text":"Operators compute derivatives and other mathematical operations on fields. Tarang.jl provides a symbolic syntax for natural mathematical notation in equations.","title":"Operators API"},{"category":"section","location":"api/operators/#Overview","page":"Operators","text":"Tarang.jl supports:\n\nDifferential operators: grad (∇), divergence (div), curl, lap (Δ, ∇²)\nCoordinate derivatives: ∂x, ∂y, ∂z, … in equation strings; d(field, coord, order) in Julia code\nTime derivatives: ∂t\nField operations: dot (⋅), cross (×)\nCustom operators: helper functions that compose the built-in operators\n\nOperators are lazy: ∇(T) builds a Gradient object, it does not compute anything. Call evaluate to get a field back:\n\n∇T = ∇(T)            # Gradient{ScalarField{...}}  — an unevaluated operator\n∇T = evaluate(∇(T))  # VectorField                 — the computed gradient\n\nInside an equation string (add_equation!) you never call evaluate — the solver does it.","title":"Overview"},{"category":"section","location":"api/operators/#Unicode-Operators","page":"Operators","text":"Tarang.jl uses Unicode mathematical symbols for readable, publication-quality code. Some names are callable Julia functions, others exist only inside equation strings — the last column says which:\n\nSyntax Description Typing Available as\n∇(f) Gradient \\nabla Tab Julia function + equation string (grad)\nΔ(f) or ∇²(f) Laplacian \\Delta Tab Julia function + equation string (lap, Δ)\n∂t(f) Time derivative \\partial Tab t Julia function + equation string\n∂x(f) x-derivative \\partial Tab x equation string only\n∂y(f) y-derivative \\partial Tab y equation string only\n∂z(f) z-derivative \\partial Tab z equation string only\nu ⋅ v Dot product \\cdot Tab Julia function + equation string\nu × v Cross product \\times Tab Julia function + equation string\n\n∂x, ∂y, ∂z (and ∂<name> for any coordinate in your coordinate system) are tokens the equation parser recognizes — they are not bound in the Tarang module, so dudx = ∂x(u) in Julia code is an UndefVarError. The callable derivative is d(field, coord, order) (see Coordinate Derivatives below).\n\nExample - advection-diffusion:\n\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\")\n\nTyping Unicode in Julia:\n\n∇ : Type \\nabla then press Tab\nΔ : Type \\Delta then press Tab\n∂t : Type \\partial Tab \\_t Tab\n∂x : Type \\partial Tab x\n⋅ : Type \\cdot then press Tab\n× : Type \\times then press Tab\n\n","title":"Unicode Operators"},{"category":"section","location":"api/operators/#Setup-used-by-the-examples-below","page":"Operators","text":"Every programmatic example on this page runs against this 2-D periodic domain:\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nbx = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2pi))\nby = RealFourier(coords[\"y\"]; size=16, bounds=(0.0, 2pi))\ndomain = Domain(dist, (bx, by))\n\nT = ScalarField(domain, \"T\")\nu = VectorField(domain, \"u\")\nset!(T, (x, y) -> sin(x) * cos(y))\nset!(u.components[1], (x, y) -> sin(y))\nset!(u.components[2], (x, y) -> cos(x))\n\nx, y = local_grids(dist, bx, by)   # grid vectors, for checking results\n\n","title":"Setup used by the examples below"},{"category":"section","location":"api/operators/#Differential-Operators","page":"Operators","text":"","title":"Differential Operators"},{"category":"section","location":"api/operators/#Gradient-(grad-/-)","page":"Operators","text":"Computes the gradient of a scalar field, returning a vector field.\n\nSyntax:\n\n# In equations\nadd_equation!(problem, \"∂t(T) = -u⋅∇(T)\")   # or \"grad(T)\"\n\n# Programmatic\n∇T = evaluate(∇(T))    # or evaluate(grad(T))  -> VectorField\n\nDefinitions:\n\nCartesian:\n\nnabla p = fracpartial ppartial xhatx + fracpartial ppartial yhaty + fracpartial ppartial zhatz\n\nSpherical:\n\nnabla p = fracpartial ppartial rhatr + frac1rfracpartial ppartial thetahattheta + frac1rsinthetafracpartial ppartial phihatphi\n\nExample:\n\n∇T = evaluate(∇(T))                       # VectorField, 2 components\nensure_layout!(∇T.components[1], :g)\nget_grid_data(∇T.components[1])           # ∂T/∂x = cos(x)cos(y), error 2.1e-15\n\nThe components live on the evaluated field: ∇(T).components does not exist, because ∇(T) is still an operator.\n\nApplied to a VectorField, grad returns a TensorField with (∇u)[i,j] = ∂u_j/∂x_i (the first index is the derivative direction).\n\nReturn type: Gradient operator; evaluates to VectorField (from a scalar) or TensorField (from a vector).\n\n","title":"Gradient (grad / ∇)"},{"category":"section","location":"api/operators/#Divergence-(div)","page":"Operators","text":"Computes the divergence of a vector field, returning a scalar field.\n\nSyntax:\n\n# In equations\nadd_equation!(problem, \"div(grad_phi) + τ_lift(tau2) = rho\")\n\n# Programmatic — the callable is `divergence` (alias `div_op`).\n# `div` is Julia's integer division; `div(u)` is a MethodError.\ndiv_u = evaluate(divergence(u))\n\nDefinitions:\n\nCartesian:\n\nnabla cdot mathbfu = fracpartial u_xpartial x + fracpartial u_ypartial y + fracpartial u_zpartial z\n\nSpherical:\n\nnabla cdot mathbfu = frac1r^2fracpartial (r^2 u_r)partial r + frac1rsinthetafracpartial (sintheta u_theta)partial theta + frac1rsinthetafracpartial u_phipartial phi\n\nExample:\n\ndiv_u = evaluate(divergence(u))       # ScalarField\nensure_layout!(div_u, :g)\nmaximum(abs, get_grid_data(div_u))    # u = (sin y, cos x) is divergence-free: 0.0\n\nevaluate does not compose Divergence with an unevaluated Gradient (evaluate(divergence(grad(T))) throws). Use Δ(T) for the Laplacian, or evaluate the inner operator first. Inside an equation string, div(...) of a gradient expression is supported on the implicit side — that is the tau-method form used below and in Tau method.\n\nReturn type: Divergence operator; evaluates to ScalarField.\n\n","title":"Divergence (div)"},{"category":"section","location":"api/operators/#Curl-(curl)","page":"Operators","text":"Computes the curl of a vector field.\n\nSyntax:\n\nω = evaluate(curl(u))\n\nDefinitions:\n\nCartesian (3D):\n\nnabla times mathbfu = left(fracpartial u_zpartial y - fracpartial u_ypartial zright)hatx + left(fracpartial u_xpartial z - fracpartial u_zpartial xright)haty + left(fracpartial u_ypartial x - fracpartial u_xpartial yright)hatz\n\n2D (returns scalar vorticity):\n\nomega = fracpartial vpartial x - fracpartial upartial y\n\nExample — 2-D vorticity diagnostic:\n\nω = evaluate(curl(u))                 # 2D -> ScalarField\nensure_layout!(ω, :g)\nget_grid_data(ω)                      # -sin(x) - cos(y), error 3.0e-15\n\nVorticity evolution. The rotational form ∇×(u×ω) is not supported: a CrossProduct cannot be an operand of curl (curl(u × ω) throws FieldError: type CrossProduct has no field dist). Write the vorticity equation in advective form instead — it compiles and steps:\n\nomega = ScalarField(domain, \"omega\")     # in 2D the vorticity is a SCALAR\nset!(omega, (x, y) -> -sin(x) - cos(y))  # = curl(u) for the u above\n\nproblem = IVP([omega, u])\nadd_parameters!(problem, nu=0.01)\nadd_equation!(problem, \"∂t(omega) - nu*Δ(omega) = -u⋅∇(omega)\")\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = -u⋅∇(u)\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\nsolver.rhs_plan.is_compiled              # true\nrun!(solver; stop_iteration=5, progress=false)   # max|omega| = 1.9999\n\n# `run!` evolves `u` as well, so restore the setup's velocity for the examples below:\nset!(u.components[1], (x, y) -> sin(y))\nset!(u.components[2], (x, y) -> cos(x))\n\nThere is no vortex-stretching term in 2D, and you cannot write one: with a scalar omega, omega⋅∇(u) is a scalar dotted with a tensor, and the solver rejects it on the first step with UnrecognizedRHSExpression. Stretching only makes sense on a 3-D domain, where omega is a VectorField; there the same equation with + omega⋅∇(u) compiles and steps.\n\nReturn type: Curl operator; evaluates to ScalarField (2D) or VectorField (3D).\n\n","title":"Curl (curl)"},{"category":"section","location":"api/operators/#Laplacian-(lap)","page":"Operators","text":"Computes the Laplacian (second derivative) of a field.\n\nSyntax:\n\n# In equations\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = 0\")\n\n# Programmatic\nΔT = evaluate(Δ(T))    # or lap(T), or ∇²(T)\n\nDefinitions:\n\nCartesian:\n\nnabla^2 f = fracpartial^2 fpartial x^2 + fracpartial^2 fpartial y^2 + fracpartial^2 fpartial z^2\n\nSpherical:\n\nnabla^2 f = frac1r^2fracpartialpartial rleft(r^2fracpartial fpartial rright) + frac1r^2sinthetafracpartialpartial thetaleft(sinthetafracpartial fpartial thetaright) + frac1r^2sin^2thetafracpartial^2 fpartial phi^2\n\nExamples:\n\n# Programmatic: Δ(sin(x)cos(y)) = -2 sin(x)cos(y), error 1.1e-14\nΔT = evaluate(Δ(T))\n\n# Diffusion / advection-diffusion equation\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = 0\")\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\")\n\n# Poisson equation (LBVP) — Δφ = ρ with Dirichlet BCs on a Fourier × Chebyshev domain.\n# A BVP needs boundary conditions, and the Chebyshev axis needs tau terms.\n# NOTE: this block builds its OWN domain, so it uses fresh names (`pcoords`, `pdist`, …)\n# and leaves the 2-D setup above intact for the later sections.\npcoords = CartesianCoordinates(\"x\", \"z\")\npdist   = Distributor(pcoords; dtype=Float64, device=CPU())\nxb = RealFourier(pcoords[\"x\"]; size=16, bounds=(0.0, 2pi))\nzb = ChebyshevT(pcoords[\"z\"];  size=24, bounds=(0.0, 1.0))\npdomain = Domain(pdist, (xb, zb))\n\nphi  = ScalarField(pdomain, \"phi\")\nrho  = ScalarField(pdomain, \"rho\")\ntau1 = ScalarField(pdist, \"tau1\", (xb,), Float64)\ntau2 = ScalarField(pdist, \"tau2\", (xb,), Float64)\n\nxg, zg = local_grids(pdist, xb, zb)\nensure_layout!(rho, :g)\nget_grid_data(rho) .= -(1 + pi^2) .* cos.(xg) .* sin.(pi .* zg')  # exact: φ = cos(x) sin(πz)\nensure_layout!(rho, :c)\n\nex, ez     = unit_vector_fields(pcoords, pdist)\nlift_basis = derivative_basis(zb, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_phi   = grad(phi) + ez * τ_lift(tau1)\n\nproblem = LBVP([phi, tau1, tau2])\nadd_parameters!(problem, rho=rho, grad_phi=grad_phi, τ_lift=τ_lift)\nadd_equation!(problem, \"div(grad_phi) + τ_lift(tau2) = rho\")\nadd_bc!(problem, \"phi(z=0) = 0\")\nadd_bc!(problem, \"phi(z=1) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)                       # max error vs cos(x)sin(πz): 4.4e-16\n\nWorks on: ScalarField, VectorField (applies componentwise)\n\n","title":"Laplacian (lap)"},{"category":"section","location":"api/operators/#Coordinate-Derivatives","page":"Operators","text":"","title":"Coordinate Derivatives"},{"category":"section","location":"api/operators/#First-Derivatives","page":"Operators","text":"Partial derivatives with respect to coordinate directions.\n\nIn equation strings — ∂<coord> for any coordinate name in your coordinate system:\n\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -0.5*∂x(T) - 0.2*∂y(T)\")\n\nIn Julia code — ∂x is not a function. Use d(field, coord, order) (constructor alias Differentiate), and evaluate it:\n\ndTdx = evaluate(d(T, coords[\"x\"], 1))     # ∂T/∂x = cos(x)cos(y), error 2.1e-15\nd2T  = evaluate(d(T, coords[\"x\"], 2))     # ∂²T/∂x² = -sin(x)cos(y), error 8.9e-15\n\nImplementation:\n\nFourier: Multiplication by ik in spectral space\nChebyshev/Legendre: Sparse matrix multiplication using recurrence relations\n\n","title":"First Derivatives"},{"category":"section","location":"api/operators/#Higher-Derivatives","page":"Operators","text":"In equation strings derivatives compose directly:\n\n# Second and mixed derivatives\nadd_equation!(problem, \"∂t(T) = ∂x(∂y(T))\")            # compiles; RHS = ∂²T/∂x∂y\n\n# Biharmonic / hyperdiffusion\nadd_equation!(problem, \"∂t(psi) + nu4*Δ(Δ(psi)) = 0\")\n\nIn Julia code an unevaluated Differentiate cannot be differentiated again. Either raise the order, or evaluate the inner derivative first:\n\nd2T   = evaluate(d(T, coords[\"x\"], 2))                       # ∂²T/∂x²\ndxT   = evaluate(d(T, coords[\"x\"], 1))\ndxdyT = evaluate(d(dxT, coords[\"y\"], 1))                     # ∂²T/∂x∂y, error 3.1e-15\n# evaluate(d(d(T, coords[\"x\"], 1), coords[\"y\"], 1))          # ArgumentError\n\n","title":"Higher Derivatives"},{"category":"section","location":"api/operators/#Time-Derivatives","page":"Operators","text":"","title":"Time Derivatives"},{"category":"section","location":"api/operators/#dt-/-t-Operator","page":"Operators","text":"Time derivative for initial value problems.\n\nSyntax:\n\ndt(field)   # ASCII\n∂t(field)   # Unicode (type \\partial Tab \\_t Tab)\n\nExamples:\n\n# Evolution equations\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = -u⋅∇(u)\")\n\nNote: Only use in IVP (Initial Value Problems). Not valid for BVP or EVP.\n\n","title":"dt / ∂t Operator"},{"category":"section","location":"api/operators/#Spectral-Operators","page":"Operators","text":"","title":"Spectral Operators"},{"category":"section","location":"api/operators/#Hilbert-Transform-(hilbert)","page":"Operators","text":"Applies the Hilbert transform in spectral space: each Fourier mode k is multiplied by -i·sign(k) (the k = 0 mode maps to 0). Useful for analytic signals, envelope/phase diagnostics, and 90° phase shifts along a Fourier axis.\n\nSyntax:\n\nHf = evaluate(hilbert(f))                      # ScalarField -> ScalarField\n\nadd_equation!(problem, \"∂t(T) = hilbert(T)\")   # parseable in equation strings\n\nProperties (all verified on a 32-point RealFourier axis):\n\nH[sin x] = -cos x (error 5.6e-16), and H[H[f]] = -f for zero-mean f (error 3.3e-16).\nIt is applied to every Fourier axis of the field, not just the first: on the 2-D domain above, hilbert(sin(x)cos(y)) returns -cos(x)sin(y) (error 8.9e-16).\nScalar fields only (a VectorField operand throws).\n\nLimitations:\n\nhilbert in an equation RHS is not handled by the compiled (\"lazy\") RHS: the solver logs a warning and the whole problem falls back to the slower interpreted evaluator (solver.rhs_plan.is_compiled == false).\nUnder MPI it throws if a Fourier axis is the decomposed one — the -i·sign(k) multiplier needs global wavenumbers, which a decomposed axis does not expose.\n\n","title":"Hilbert Transform (hilbert)"},{"category":"section","location":"api/operators/#Vector-Operations","page":"Operators","text":"","title":"Vector Operations"},{"category":"section","location":"api/operators/#Dot-Product-/-Advection","page":"Operators","text":"In equations — use vector notation directly:\n\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\")    # scalar advection\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = -u⋅∇(u)\")       # vector advection\n\nProgrammatic:\n\nadv = evaluate(u ⋅ ∇(T))     # ScalarField, u·∇T — error ~1e-15 against the exact field\n\n","title":"Dot Product / Advection"},{"category":"section","location":"api/operators/#Cross-Product","page":"Operators","text":"The cross product is a 3-D operation, so it needs a 3-D domain. As with the Poisson block, this one uses fresh names so it does not disturb the 2-D setup used by the sections below:\n\ncoords3 = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist3   = Distributor(coords3; dtype=Float64, device=CPU())\ndomain3 = Domain(dist3, (RealFourier(coords3[\"x\"]; size=8, bounds=(0.0, 2pi)),\n                         RealFourier(coords3[\"y\"]; size=8, bounds=(0.0, 2pi)),\n                         RealFourier(coords3[\"z\"]; size=8, bounds=(0.0, 2pi))))\nu3 = VectorField(domain3, \"u\")\nω3 = VectorField(domain3, \"omega\")\nset!(u3.components[1], (x, y, z) -> sin(y))     # u = (sin y, 0, 0)\nset!(ω3.components[3], (x, y, z) -> cos(x))     # ω = (0, 0, cos x)\n\nuxω = evaluate(cross(u3, ω3))                   # or: evaluate(u3 × ω3)  -> VectorField\nensure_layout!(uxω.components[2], :g)\nget_grid_data(uxω.components[2])                # -sin(y)cos(x), error 7.8e-16\n\nu × ω is also parseable inside an equation string, but the term is not compiled by the lazy RHS (the solver falls back to the interpreted evaluator). It cannot be nested inside curl — see Curl above for the supported vorticity form.\n\n","title":"Cross Product"},{"category":"section","location":"api/operators/#Composite-Operators","page":"Operators","text":"Combine operators for complex expressions.","title":"Composite Operators"},{"category":"section","location":"api/operators/#Vector-Laplacian","page":"Operators","text":"# For vector field u\n# ∇²u = (∇²u_x, ∇²u_y, ∇²u_z)\n\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = -u⋅∇(u)\")\n# Δ automatically applies componentwise","title":"Vector Laplacian"},{"category":"section","location":"api/operators/#Advection-Operator","page":"Operators","text":"# Scalar advection:\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\")\n\n# Vector advection (Navier-Stokes nonlinear term):\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = -u⋅∇(u)\")\n\nThe incompressible momentum equation with a pressure gradient needs the tau/BC machinery — see the Rayleigh-Bénard tutorial for the full form.","title":"Advection Operator"},{"category":"section","location":"api/operators/#Strain-Rate-Tensor","page":"Operators","text":"grad of a VectorField evaluates to a TensorField with (∇u)[i,j] = ∂u_j/∂x_i. Build the symmetric part from its components — arithmetic on ScalarFields is eager, so each expression below is a real field:\n\n∇u  = evaluate(grad(u))                                  # TensorField\nS11 = ∇u.components[1, 1]\nS12 = 0.5 * (∇u.components[1, 2] + ∇u.components[2, 1])  # ScalarField\nS22 = ∇u.components[2, 2]\n\nFor u = (sin y, cos x): S11 = S22 = 0 exactly, and S12 = 0.5(cos y - sin x) to 1.3e-15.\n\nS[i, j] is shorthand for S.components[i, j], for reading and for assignment, so you can also collect the components into a TensorField of your own:\n\nS = TensorField(dist, \"S\", (bx, by), Float64)\nS[1, 2] = S12\nS[2, 1] = S12                    # S[1,2] === S.components[1,2]\n\nThere is no symmetric= keyword on the TensorField constructor (MethodError).\n\n","title":"Strain Rate Tensor"},{"category":"section","location":"api/operators/#Operator-Properties","page":"Operators","text":"The identities below were checked numerically on the 2-D setup, with a second scalar field\n\ng = ScalarField(domain, \"g\")\nset!(g, (x, y) -> cos(2x))\n\nThe quoted figure is the measured maximum difference.","title":"Operator Properties"},{"category":"section","location":"api/operators/#Linearity","page":"Operators","text":"# ∇(αf + βg) = α∇f + β∇g          — max difference 3.6e-15\nlhs = evaluate(∇(2.0 * T + 3.0 * g))\ngT, gg = evaluate(∇(T)), evaluate(∇(g))\n# lhs.components[i] ≈ 2.0 * gT.components[i] + 3.0 * gg.components[i]","title":"Linearity"},{"category":"section","location":"api/operators/#Commutativity","page":"Operators","text":"# ∂²f/∂x∂y = ∂²f/∂y∂x             — max difference 3.4e-15\na = evaluate(d(evaluate(d(T, coords[\"x\"], 1)), coords[\"y\"], 1))\nb = evaluate(d(evaluate(d(T, coords[\"y\"], 1)), coords[\"x\"], 1))","title":"Commutativity"},{"category":"section","location":"api/operators/#Product-Rule","page":"Operators","text":"# ∇(fg) = f∇g + g∇f               — max difference ~2e-15\nlhs = evaluate(∇(T * g))\n# lhs.components[i] ≈ T * gg.components[i] + g * gT.components[i]\n\n","title":"Product Rule"},{"category":"section","location":"api/operators/#Custom-Operators","page":"Operators","text":"","title":"Custom Operators"},{"category":"section","location":"api/operators/#Defining-Helper-Functions","page":"Operators","text":"Helper functions that compose built-in operators return operator objects, which you can register as a parameter and then name inside an equation string:\n\nhyperdiffusion(field, k4) = k4 * Δ(Δ(field))\n\nproblem = IVP([T])\nadd_parameters!(problem, hyper_T = hyperdiffusion(T, 1e-4))\nadd_equation!(problem, \"∂t(T) + hyper_T = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\nrun!(solver; stop_iteration=10, progress=false)\nsolver.rhs_plan.is_compiled      # true\n\nThis is the supported way to inject a hand-built term: an equation string can only see fields of the problem and names registered with add_parameters! — it cannot see plain Julia globals, and there is no field.rhs / field.data to write into.","title":"Defining Helper Functions"},{"category":"section","location":"api/operators/#Using-Built-in-Operators-in-Equations","page":"Operators","text":"The equation parser recognizes all registered operators. Use them directly in equation strings:\n\n# Available operators in equations:\n# grad, div, curl, lap (or Δ), dt (or ∂t), d, ∂<coord>\n# integrate, average, interpolate, convert, lift, trace, hilbert\n# sin, cos, tan, exp, log, sqrt, abs, tanh\n\n# Example: diffusion equation\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = 0\")\n\n# Example: advection-diffusion\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\")\n\n","title":"Using Built-in Operators in Equations"},{"category":"section","location":"api/operators/#Equation-Parsing","page":"Operators","text":"","title":"Equation Parsing"},{"category":"section","location":"api/operators/#Symbolic-Syntax","page":"Operators","text":"Tarang.jl parses equation strings into operator applications:\n\n# String equation\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -0.5*∂x(T) - 0.2*∂y(T)\")\n\n# Parsed as:\n# LHS: ∂t(T) - kappa*Δ(T)\n# RHS: -0.5*∂x(T) - 0.2*∂y(T)\n\nSupported operations:\n\nAddition: +\nSubtraction: -\nMultiplication: *\nDivision: /\nParentheses: (, )\nFunctions: sin, cos, exp, etc.","title":"Symbolic Syntax"},{"category":"section","location":"api/operators/#Operator-Evaluation","page":"Operators","text":"Operators are evaluated in spectral space when possible:\n\n# ∂x(u): Multiply by ik in Fourier space\n# Δ(u): Multiply by -k² in Fourier space\n# Nonlinear terms: Transform to grid space, evaluate, transform back\n\n","title":"Operator Evaluation"},{"category":"section","location":"api/operators/#Performance-Tips","page":"Operators","text":"","title":"Performance Tips"},{"category":"section","location":"api/operators/#Check-that-the-RHS-compiled","page":"Operators","text":"The explicit side of every equation is compiled into a type-specialized plan. If any term cannot be compiled, the whole solver silently drops to the ~100×-slower interpreted evaluator (after a loud @warn). Check it:\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\nsolver.rhs_plan.is_compiled      # true for every equation on this page except `hilbert`\n                                 # (a bare `u × ω` term does not compile either)","title":"Check that the RHS compiled"},{"category":"section","location":"api/operators/#Keep-non-Fourier-derivatives-off-the-explicit-side","page":"Operators","text":"A Chebyshev/Legendre derivative in an RHS is either declined by the compiler (Legendre, and any non-Fourier axis under MPI) or — distributed — errors at the first step, because a rank owns only part of that axis. Express those terms with grad/div on the implicit (L) side of the equation. Fourier derivatives are fine anywhere: they are a diagonal ik multiply.","title":"Keep non-Fourier derivatives off the explicit side"},{"category":"section","location":"api/operators/#Use-sparse-differentiation","page":"Operators","text":"Chebyshev/Legendre derivatives are sparse matrix operations - very efficient.\n\n","title":"Use sparse differentiation"},{"category":"section","location":"api/operators/#See-Also","page":"Operators","text":"Fields: Field types that operators act on\nProblems: Using operators in equations\nDomains: Spatial discretization for operators\nBases: Spectral bases for differentiation","title":"See Also"},{"category":"section","location":"api/les_models/#LES-Models-API","page":"LES Models","text":"The SGS models are array-level utilities: they consume grid-space velocity-gradient arrays and produce an eddy-viscosity array. They are not automatically coupled into an IVP — you evaluate the gradients, call the model, and apply the resulting stress yourself.","title":"LES Models API"},{"category":"section","location":"api/les_models/#Usage","page":"LES Models","text":"using Tarang\n\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\nybasis = RealFourier(coords[\"y\"]; size=16, bounds=(0.0, 2π))\ndomain = Domain(dist, (xbasis, ybasis))\n\nu = VectorField(domain, \"u\")\nx, y = local_grids(dist, xbasis, ybasis)\nensure_layout!(u, :g)\nget_grid_data(u.components[1]) .=  sin.(x) .* cos.(y')\nget_grid_data(u.components[2]) .= .-cos.(x) .* sin.(y')\nensure_layout!(u, :c)\n\n# grad(u) is a TensorField; its components are the velocity-gradient tensor in\n# component-major order: (∂u∂x, ∂u∂y, ∂v∂x, ∂v∂y) — exactly the order the models want.\nG = evaluate(grad(u))\n∂u∂x, ∂u∂y, ∂v∂x, ∂v∂y = (get_grid_data(g) for g in G.components)\n\nΔx, Δy = grid_spacing(domain)\nmodel  = SmagorinskyModel(C_s=0.17, filter_width=(Δx, Δy), field_size=(16, 16))\n\ncompute_eddy_viscosity!(model, ∂u∂x, ∂u∂y, ∂v∂x, ∂v∂y)\n\nνₑ = get_eddy_viscosity(model)          # 16×16 Matrix{Float64}\nmean_eddy_viscosity(model)              # 0.00352…\nmax_eddy_viscosity(model)               # 0.00891…\n\n# deviatoric SGS stress τᵢⱼ = -2 νₑ S̄ᵢⱼ\nS11 = ∂u∂x\nS12 = 0.5 .* (∂u∂y .+ ∂v∂x)\nS22 = ∂v∂y\nτ11, τ12, τ22 = compute_sgs_stress(model, S11, S12, S22)\n\nmean_sgs_dissipation(model, model.strain_magnitude)   # 0.00643…","title":"Usage"},{"category":"section","location":"api/les_models/#Usage-under-MPI","page":"LES Models","text":"The models hold plain per-rank arrays, so size the model to this rank's slab, not to the global grid. Under MPI get_grid_data returns a PencilArray whose size is already the local shape; parent gives the raw local storage:\n\nG = evaluate(grad(u))\n∂u∂x, ∂u∂y, ∂v∂x, ∂v∂y = (parent(get_grid_data(g)) for g in G.components)\n\nΔx, Δy = grid_spacing(domain)\nmodel  = SmagorinskyModel(C_s=0.17, filter_width=(Δx, Δy),\n                          field_size=size(∂u∂x))   # LOCAL slab: (16, 8) at np=2\n\ncompute_eddy_viscosity!(model, ∂u∂x, ∂u∂y, ∂v∂x, ∂v∂y)\n\nmean_eddy_viscosity(model)      # 0.00352… — global, Allreduced across ranks\nmax_eddy_viscosity(model)       # 0.00891… — global\n\nmean_eddy_viscosity, max_eddy_viscosity and mean_sgs_dissipation each perform their own reduction over MPI.COMM_WORLD, so they return domain-global values: for the 16×16 case above they give the same numbers at np = 1, 2 and 4. Being collectives, they must be called on every rank — reaching for one inside an if rank == 0 block deadlocks. Pass global_reduce=false to skip the reduction and get this rank's slab value, which is safe to call from a subset of ranks:\n\n# Deadlocks at np > 1 — only rank 0 enters the collective.\nrank == 0 && @info \"mean νₑ\" mean_eddy_viscosity(model)\n\n# Correct: every rank reduces, one rank prints.\nν̄ = mean_eddy_viscosity(model)\nrank == 0 && @info \"mean νₑ\" ν̄\n\n# Or opt out of the reduction entirely.\nrank == 0 && @info \"local νₑ\" mean_eddy_viscosity(model; global_reduce=false)\n\nget_eddy_viscosity(model) is, by contrast, this rank's slab — broadcast it against the other parent(...) arrays to build the stress locally.\n\nSizing the model to the global grid is a loud error rather than a silent wrong answer:\n\nDimensionMismatch: Gradient array 1 has size (16, 8), expected (16, 16)","title":"Usage under MPI"},{"category":"section","location":"api/les_models/#Types","page":"LES Models","text":"","title":"Types"},{"category":"section","location":"api/les_models/#Abstract-Types","page":"LES Models","text":"abstract type SGSModel end\nabstract type EddyViscosityModel <: SGSModel end","title":"Abstract Types"},{"category":"section","location":"api/les_models/#SmagorinskyModel","page":"LES Models","text":"Classic Smagorinsky (1963) subgrid-scale model.\n\nType signature:\n\nmutable struct SmagorinskyModel{T<:AbstractFloat, N,\n                                A<:AbstractArray{T, N},\n                                Arch<:AbstractArchitecture} <: EddyViscosityModel\n\nThe array parameter A is the type the model's internal buffers are stored in — an Array{T,N} on CPU(), a CuArray on GPU().\n\nFields:\n\nField Type Description\nC_s T Smagorinsky constant\nfilter_width NTuple{N, T} Filter width (Δx, Δy, Δz)\neffective_delta T Effective Δ = (Δx Δy Δz)^(1/N)\neddy_viscosity A νₑ field\nstrain_magnitude A |S̄| field\nfield_size NTuple{N, Int} Grid dimensions\narchitecture Arch CPU() or GPU()\n\nConstructor:\n\nSmagorinskyModel(;\n    C_s = 0.17,\n    filter_width::NTuple{N, Real},\n    field_size::NTuple{N, Int},\n    dtype = Float64,\n    architecture = CPU()\n)","title":"SmagorinskyModel"},{"category":"section","location":"api/les_models/#AMDModel","page":"LES Models","text":"Anisotropic Minimum Dissipation model (Rozema et al., 2015).\n\nType signature:\n\nmutable struct AMDModel{T<:AbstractFloat, N,\n                        A<:AbstractArray{T, N},\n                        Arch<:AbstractArchitecture} <: EddyViscosityModel\n\nFields:\n\nField Type Description\nC T Poincaré constant\nfilter_width NTuple{N, T} Anisotropic filter widths\nfilter_width_sq NTuple{N, T} Δₖ² for each direction\neddy_viscosity A νₑ field\neddy_diffusivity A κₑ field (for scalars)\nfield_size NTuple{N, Int} Grid dimensions\nclip_negative Bool Whether to clip νₑ < 0\narchitecture Arch CPU() or GPU()\n\nConstructor:\n\nAMDModel(;\n    C = 1/12,\n    filter_width::NTuple{N, Real},\n    field_size::NTuple{N, Int},\n    clip_negative = true,\n    dtype = Float64,\n    architecture = CPU()\n)\n\n","title":"AMDModel"},{"category":"section","location":"api/les_models/#Eddy-Viscosity-Computation","page":"LES Models","text":"","title":"Eddy Viscosity Computation"},{"category":"section","location":"api/les_models/#compute*eddy*viscosity!","page":"LES Models","text":"Compute eddy viscosity from velocity gradient components. The gradients are plain grid-space arrays of size model.field_size, passed in component-major order (all derivatives of u, then all of v, then all of w).\n\n2D Signature:\n\ncompute_eddy_viscosity!(model, ∂u∂x, ∂u∂y, ∂v∂x, ∂v∂y)\n\n3D Signature:\n\ncompute_eddy_viscosity!(model,\n    ∂u∂x, ∂u∂y, ∂u∂z,\n    ∂v∂x, ∂v∂y, ∂v∂z,\n    ∂w∂x, ∂w∂y, ∂w∂z\n)\n\nReturns: The eddy viscosity array model.eddy_viscosity","title":"computeeddyviscosity!"},{"category":"section","location":"api/les_models/#compute*eddy*diffusivity!","page":"LES Models","text":"Compute eddy diffusivity for scalar transport (AMD model only). The AMD diffusivity (Abkar, Bae & Moin 2016, eq. 2.7) is a full double contraction over the scaled-gradient direction k and every velocity component i:\n\nκₑ† = -C · [ Σₖ Δₖ² (∂ₖuᵢ)(∂ₖb)(∂ᵢb) ] / [ (∂ₗb)(∂ₗb) ]\n\nso the method needs the complete velocity-gradient tensor (2D: 4 components, 3D: 9), not just the gradient of one velocity component. The scalar gradients follow.\n\n2D Signature:\n\ncompute_eddy_diffusivity!(model::AMDModel,\n    ∂u∂x, ∂u∂y,\n    ∂v∂x, ∂v∂y,\n    ∂b∂x, ∂b∂y\n)\n\n3D Signature:\n\ncompute_eddy_diffusivity!(model::AMDModel,\n    ∂u∂x, ∂u∂y, ∂u∂z,\n    ∂v∂x, ∂v∂y, ∂v∂z,\n    ∂w∂x, ∂w∂y, ∂w∂z,\n    ∂b∂x, ∂b∂y, ∂b∂z\n)\n\nReturns: The eddy diffusivity array model.eddy_diffusivity\n\n","title":"computeeddydiffusivity!"},{"category":"section","location":"api/les_models/#Subgrid-Stress","page":"LES Models","text":"","title":"Subgrid Stress"},{"category":"section","location":"api/les_models/#compute*sgs*stress","page":"LES Models","text":"Compute deviatoric SGS stress tensor τᵢⱼ = -2 νₑ S̄ᵢⱼ.\n\n2D Signature:\n\ncompute_sgs_stress(model, S11, S12, S22) -> (τ11, τ12, τ22)\n\n3D Signature:\n\ncompute_sgs_stress(model, S11, S12, S13, S22, S23, S33)\n    -> (τ11, τ12, τ13, τ22, τ23, τ33)\n\n","title":"computesgsstress"},{"category":"section","location":"api/les_models/#Accessors","page":"LES Models","text":"","title":"Accessors"},{"category":"section","location":"api/les_models/#get*eddy*viscosity","page":"LES Models","text":"Return the current eddy viscosity field (an Array on CPU(), a CuArray on GPU()).\n\nget_eddy_viscosity(model::EddyViscosityModel) -> A <: AbstractArray{T, N}","title":"geteddyviscosity"},{"category":"section","location":"api/les_models/#get*eddy*diffusivity","page":"LES Models","text":"Return the current eddy diffusivity field (AMD only).\n\nget_eddy_diffusivity(model::AMDModel) -> A <: AbstractArray{T, N}","title":"geteddydiffusivity"},{"category":"section","location":"api/les_models/#get*filter*width","page":"LES Models","text":"Return the filter width tuple.\n\nget_filter_width(model::EddyViscosityModel) -> NTuple{N, T}\n\n","title":"getfilterwidth"},{"category":"section","location":"api/les_models/#Statistics","page":"LES Models","text":"","title":"Statistics"},{"category":"section","location":"api/les_models/#mean*eddy*viscosity","page":"LES Models","text":"Compute domain-averaged eddy viscosity.\n\nmean_eddy_viscosity(model::EddyViscosityModel) -> T","title":"meaneddyviscosity"},{"category":"section","location":"api/les_models/#max*eddy*viscosity","page":"LES Models","text":"Return maximum eddy viscosity in the domain.\n\nmax_eddy_viscosity(model::EddyViscosityModel) -> T\n\n","title":"maxeddyviscosity"},{"category":"section","location":"api/les_models/#Dissipation-Rate","page":"LES Models","text":"","title":"Dissipation Rate"},{"category":"section","location":"api/les_models/#sgs_dissipation","page":"LES Models","text":"Compute the SGS dissipation rate field: εₛₛ = νₑ |S̄|²\n\nThere is no extra factor of 2 here. The exact dissipation is εₛₛ = -τᵢⱼ S̄ᵢⱼ = 2 νₑ S̄ᵢⱼS̄ᵢⱼ, and the |S̄| = √(2 S̄ᵢⱼS̄ᵢⱼ) convention used by compute_eddy_viscosity! already carries it, so εₛₛ = νₑ |S̄|².\n\nsgs_dissipation(model::EddyViscosityModel, strain_magnitude) -> A <: AbstractArray{T, N}\n\nThe natural argument is the strain magnitude the model just cached, model.strain_magnitude (Smagorinsky only — the AMD model does not cache one).","title":"sgs_dissipation"},{"category":"section","location":"api/les_models/#mean*sgs*dissipation","page":"LES Models","text":"Compute domain-averaged SGS dissipation rate.\n\nmean_sgs_dissipation(model::EddyViscosityModel, strain_magnitude) -> T\n\n","title":"meansgsdissipation"},{"category":"section","location":"api/les_models/#Configuration","page":"LES Models","text":"","title":"Configuration"},{"category":"section","location":"api/les_models/#set_constant!","page":"LES Models","text":"Update the model constant.\n\nset_constant!(model::SmagorinskyModel, C_s::Real)\nset_constant!(model::AMDModel, C::Real)","title":"set_constant!"},{"category":"section","location":"api/les_models/#reset!","page":"LES Models","text":"Reset eddy viscosity (and diffusivity for AMD) to zero.\n\nreset!(model::EddyViscosityModel)\nreset!(model::AMDModel)  # Also resets eddy_diffusivity\n\n","title":"reset!"},{"category":"section","location":"api/les_models/#Exports","page":"LES Models","text":"export SGSModel, EddyViscosityModel\nexport SmagorinskyModel, AMDModel\nexport compute_eddy_viscosity!, compute_eddy_diffusivity!\nexport compute_sgs_stress\nexport get_eddy_viscosity, get_eddy_diffusivity, get_filter_width\nexport mean_eddy_viscosity, max_eddy_viscosity\nexport reset!, set_constant!, set_filter_width!\nexport sgs_dissipation, mean_sgs_dissipation","title":"Exports"},{"category":"section","location":"api/les_models/#Mutating-a-model","page":"LES Models","text":"set_constant!(model, C) and set_filter_width!(model, Δ) validate their argument and keep the cached derived quantities (filter_width_sq, effective_delta) in step. Assigning model.filter_width directly is also honoured — the kernels re-derive Δ² and the geometric-mean Δ on every call — but it leaves those cached fields reading stale, so prefer the setter.\n\n","title":"Mutating a model"},{"category":"section","location":"api/les_models/#Index","page":"LES Models","text":"Pages = [\"les_models.md\"]","title":"Index"},{"category":"type","location":"api/les_models/#Tarang.SmagorinskyModel","page":"LES Models","text":"SmagorinskyModel{T, N, A, Arch}\n\nClassic Smagorinsky (1963) subgrid-scale model.\n\nMathematical Formulation\n\nThe eddy viscosity is:\n\nνₑ = (Cₛ Δ)² |S̄|\n\nwhere:\n\nCₛ is the Smagorinsky constant (typically 0.1-0.2)\nΔ is the filter width (grid spacing)\n|S̄| = √(2 S̄ᵢⱼ S̄ᵢⱼ) is the strain rate magnitude\n\nFields\n\nC_s::T: Smagorinsky constant\nfilter_width::NTuple{N, T}: Filter width in each direction (Δx, Δy, ...)\neddy_viscosity::A: Cached eddy viscosity field (Array or CuArray)\nstrain_magnitude::A: Cached |S̄| field\narchitecture::Arch: CPU() or GPU() architecture\n\nExample\n\n# Create model for 256³ domain with Δ = 2π/256\nmodel = SmagorinskyModel(\n    C_s = 0.17,\n    filter_width = (2π/256, 2π/256, 2π/256),\n    field_size = (256, 256, 256)\n)\n\n# Create GPU model\nmodel_gpu = SmagorinskyModel(\n    C_s = 0.17,\n    filter_width = (2π/256, 2π/256, 2π/256),\n    field_size = (256, 256, 256),\n    architecture = GPU()\n)\n\n# Compute eddy viscosity from velocity gradients\ncompute_eddy_viscosity!(model, ∂u∂x, ∂u∂y, ∂u∂z, ∂v∂x, ∂v∂y, ∂v∂z, ∂w∂x, ∂w∂y, ∂w∂z)\n\n# Access the result\nνₑ = get_eddy_viscosity(model)\n\n\n\n\n\n","title":"Tarang.SmagorinskyModel"},{"category":"type","location":"api/les_models/#Tarang.AMDModel","page":"LES Models","text":"AMDModel{T, N, A, Arch}\n\nAnisotropic Minimum Dissipation model (Rozema et al., 2015).\n\nMathematical Formulation\n\nThe eddy viscosity is:\n\nνₑ = max(0, νₑ†)\n\nwhere the predictor is:\n\nνₑ† = -C (Δₖ² ∂uᵢ/∂xₖ ∂uⱼ/∂xₖ Sᵢⱼ) / (∂uₘ/∂xₙ ∂uₘ/∂xₙ)\n\nKey features:\n\nUses anisotropic filter widths Δₖ in each direction\nAutomatically switches off in laminar/transitional regions\nProvides minimum dissipation required for subgrid energy transfer\nNo explicit filtering or test-filtering required\n\nFields\n\nC::T: Poincaré constant (model constant)\nfilter_width::NTuple{N, T}: Anisotropic filter widths (Δx, Δy, Δz)\neddy_viscosity::A: Cached eddy viscosity field (Array or CuArray)\neddy_diffusivity::A: Cached eddy diffusivity (for scalars)\narchitecture::Arch: CPU() or GPU() architecture\n\nModel Constant Recommendations\n\nDiscretization C\nSpectral methods 1/12 ≈ 0.0833\n4th-order finite difference 0.212\n2nd-order finite difference 0.3\n\nExample\n\n# Create AMD model for anisotropic grid\nmodel = AMDModel(\n    C = 1/12,  # Spectral method\n    filter_width = (2π/256, 2π/256, 2π/64),  # Anisotropic\n    field_size = (256, 256, 64)\n)\n\n# Create GPU AMD model\nmodel_gpu = AMDModel(\n    C = 1/12,\n    filter_width = (2π/256, 2π/256, 2π/64),\n    field_size = (256, 256, 64),\n    architecture = GPU()\n)\n\n# Compute eddy viscosity\ncompute_eddy_viscosity!(model, ∂u∂x, ∂u∂y, ∂u∂z, ∂v∂x, ∂v∂y, ∂v∂z, ∂w∂x, ∂w∂y, ∂w∂z)\n\nReferences\n\nRozema, W., Bae, H.J., Moin, P., Verstappen, R. (2015). \"Minimum-dissipation models for large-eddy simulation\", Physics of Fluids 27, 085107.\n\n\n\n\n\n","title":"Tarang.AMDModel"},{"category":"function","location":"api/les_models/#Tarang.compute_eddy_viscosity!","page":"LES Models","text":"compute_eddy_viscosity!(model::AMDModel, velocity_gradients...)\n\nCompute AMD eddy viscosity from velocity gradient components.\n\nGPU-aware: Uses broadcasting for GPU arrays, optimized SIMD loops for CPU.\n\n2D Case\n\ncompute_eddy_viscosity!(model, ∂u∂x, ∂u∂y, ∂v∂x, ∂v∂y)\n\n3D Case\n\ncompute_eddy_viscosity!(model, ∂u∂x, ∂u∂y, ∂u∂z, ∂v∂x, ∂v∂y, ∂v∂z, ∂w∂x, ∂w∂y, ∂w∂z)\n\nThe AMD formula uses anisotropic scaling:     νₑ† = -C (Δₖ² ∂uᵢ/∂xₖ ∂uⱼ/∂xₖ Sᵢⱼ) / (∂uₘ/∂xₙ ∂uₘ/∂xₙ)\n\n\n\n\n\n","title":"Tarang.compute_eddy_viscosity!"},{"category":"function","location":"api/les_models/#Tarang.compute_eddy_diffusivity!","page":"LES Models","text":"compute_eddy_diffusivity!(model::AMDModel, velocity_gradients..., scalar_gradients...)\n\nCompute eddy diffusivity for scalar transport using AMD model.\n\nGPU-aware: Uses broadcasting for GPU arrays, optimized SIMD loops for CPU.\n\nFor a scalar field b with gradient ∇b, the AMD eddy diffusivity (Abkar, Bae & Moin 2016, eq. 2.7) is the FULL double contraction over the scaled-gradient direction k AND all velocity components i:\n\nκₑ = max(0, κₑ†),   κₑ† = -C · [ Σₖ δₖ² (∂ₖ uᵢ)(∂ₖ b)(∂ᵢ b) ] / [ (∂ₗ b)(∂ₗ b) ]\n\ni.e. for each direction k form the inner sum Σᵢ (∂ₖ uᵢ)(∂ᵢ b) over ALL velocity components uᵢ, weight by δₖ²(∂ₖ b), and sum over k. The method therefore needs every velocity-gradient component ∂uᵢ/∂xₖ (2D: 4 of them; 3D: 9), passed in component-major order, followed by the scalar gradients ∂b/∂xₖ. (An earlier version summed only a single velocity component, contracting the scaled velocity gradient with the SAME scalar-gradient direction twice — that is NOT the AMD diffusivity and is fixed here.)\n\n\n\n\n\n","title":"Tarang.compute_eddy_diffusivity!"},{"category":"function","location":"api/les_models/#Tarang.compute_sgs_stress","page":"LES Models","text":"compute_sgs_stress(model::EddyViscosityModel, strain_components...)\n\nCompute the deviatoric subgrid stress tensor:\n\nτᵢⱼᵈ = -2 νₑ S̄ᵢⱼ\n\nGPU-aware: Uses broadcasting which works for both CPU and GPU arrays.\n\n2D Output\n\nReturns (τ11, τ12, τ22).\n\n3D Output\n\nReturns (τ11, τ12, τ13, τ22, τ23, τ33).\n\n\n\n\n\n","title":"Tarang.compute_sgs_stress"},{"category":"function","location":"api/les_models/#Tarang.get_eddy_viscosity","page":"LES Models","text":"get_eddy_viscosity(model::EddyViscosityModel)\n\nReturn the current eddy viscosity field.\n\n\n\n\n\n","title":"Tarang.get_eddy_viscosity"},{"category":"function","location":"api/les_models/#Tarang.get_eddy_diffusivity","page":"LES Models","text":"get_eddy_diffusivity(model::AMDModel)\n\nReturn the current eddy diffusivity field (AMD model only).\n\n\n\n\n\n","title":"Tarang.get_eddy_diffusivity"},{"category":"function","location":"api/les_models/#Tarang.get_filter_width","page":"LES Models","text":"get_filter_width(model::EddyViscosityModel)\n\nReturn the filter width(s).\n\n\n\n\n\n","title":"Tarang.get_filter_width"},{"category":"function","location":"api/les_models/#Tarang.mean_eddy_viscosity","page":"LES Models","text":"mean_eddy_viscosity(model::EddyViscosityModel; global_reduce=true)\n\nCompute the domain-averaged eddy viscosity.\n\nwarning: Collective under MPI\nWith global_reduce=true (the default) this is a collective call on MPI.COMM_WORLD: every rank must call it, or the ones that do will hang. In particular rank == 0 && @info mean_eddy_viscosity(model) deadlocks — compute on all ranks first, then log on one. Pass global_reduce=false for this rank's slab only, which is safe to call from a subset of ranks.\n\n\n\n\n\n","title":"Tarang.mean_eddy_viscosity"},{"category":"function","location":"api/les_models/#Tarang.max_eddy_viscosity","page":"LES Models","text":"max_eddy_viscosity(model::EddyViscosityModel; global_reduce=true)\n\nReturn the maximum eddy viscosity in the domain.\n\nwarning: Collective under MPI\nSee mean_eddy_viscosity — with global_reduce=true every rank must call this or the callers hang.\n\n\n\n\n\n","title":"Tarang.max_eddy_viscosity"},{"category":"function","location":"api/les_models/#Tarang.sgs_dissipation","page":"LES Models","text":"sgs_dissipation(model::EddyViscosityModel, strain_magnitude::AbstractArray)\n\nCompute the subgrid-scale dissipation rate:\n\nεₛₛ = νₑ |S̄|²\n\nwhere |S̄| = √(2 S̄ᵢⱼS̄ᵢⱼ) (the convention used by compute_eddy_viscosity!). The exact dissipation is εₛₛ = -τᵢⱼ S̄ᵢⱼ = 2 νₑ S̄ᵢⱼS̄ᵢⱼ = νₑ |S̄|² with that |S̄|; an extra factor of 2 here would double-count (the strain magnitude already carries it).\n\nGPU-aware: Uses broadcasting which works for both CPU and GPU arrays. Returns the dissipation field.\n\n\n\n\n\n","title":"Tarang.sgs_dissipation"},{"category":"function","location":"api/les_models/#Tarang.mean_sgs_dissipation","page":"LES Models","text":"mean_sgs_dissipation(model::EddyViscosityModel, strain_magnitude::AbstractArray)\n\nCompute domain-averaged SGS dissipation rate. GPU-aware: Uses broadcasting and sum() which work for both CPU and GPU arrays.\n\nwarning: Collective under MPI\nSee mean_eddy_viscosity — with global_reduce=true every rank must call this or the callers hang.\n\n\n\n\n\n","title":"Tarang.mean_sgs_dissipation"},{"category":"function","location":"api/les_models/#Tarang.set_constant!","page":"LES Models","text":"set_constant!(model::SmagorinskyModel, C_s::Real)\n\nUpdate the Smagorinsky constant.\n\n\n\n\n\nset_constant!(model::AMDModel, C::Real)\n\nUpdate the AMD Poincaré constant.\n\n\n\n\n\n","title":"Tarang.set_constant!"},{"category":"section","location":"api/solvers/#Solvers-API","page":"Solvers","text":"Solvers integrate PDEs in time or solve for steady states. Tarang.jl provides specialized solvers for different problem types.","title":"Solvers API"},{"category":"section","location":"api/solvers/#Solver-Types","page":"Solvers","text":"","title":"Solver Types"},{"category":"section","location":"api/solvers/#InitialValueSolver","page":"Solvers","text":"Time-stepping solver for Initial Value Problems (IVP).\n\nConstructor:\n\nInitialValueSolver(\n    problem::IVP,\n    timestepper::TimeStepper;\n    dt::Real=1e-3,\n    device::String=\"cpu\",\n    matsolver=:sparse\n)\n\nArguments:\n\nproblem: IVP problem definition\ntimestepper: Time integration scheme instance (RK222(), CNAB2(), …)\ndt: Initial timestep\nmatsolver: Backend used for the implicit per-mode solves (see Solver options)\n\nExamples:\n\n# IMEX Runge-Kutta\nsolver = InitialValueSolver(problem, RK222(); dt=0.001)\n\n# IMEX multistep\nsolver = InitialValueSolver(problem, CNAB2(); dt=0.01)\n\n# Higher-order Runge-Kutta\nsolver = InitialValueSolver(problem, RK443(); dt=0.001)\n\nThe timestepper is passed as an instance — RK222(), not RK222.\n\nProperties:\n\nsolver.problem            # IVP problem\nsolver.timestepper        # Time integration scheme\nsolver.dt                 # Current timestep\nsolver.sim_time           # Current simulation time\nsolver.iteration          # Current iteration number\nsolver.state              # Vector{ScalarField} — the solution fields\nsolver.rhs_plan           # Compiled RHS plan (see below)\nsolver.performance_stats  # total_time, total_steps, total_solves, avg_step_time\n\nMethods:","title":"InitialValueSolver"},{"category":"section","location":"api/solvers/#run!","page":"Solvers","text":"The recommended way to run a simulation. Drives the timestep loop, CFL-adaptive dt, output handlers, progress, and callbacks — so a typical script needs no manual step!/process!/close!:\n\n# Handlers created with the SOLVER auto-register; run! writes them every step\n# (at their own sim_dt/iter cadence) and closes them at the end.\nsnap = add_file_handler(\"output/snap\", solver; sim_dt=0.5)\nadd_task!(snap, u; name=\"u\")\n\nrun!(solver;\n     stop_time=10.0,\n     cfl=cfl,                       # adaptive dt = compute_timestep(cfl) each step\n     log_interval=100,              # built-in progress line every 100 steps\n     callbacks=Pair[\n         100 => (s -> @printf(\"t=%.3f dt=%.2e max|u_x|=%.3e\\n\",\n                              s.sim_time, s.dt, global_max(u.components[1]))),\n         0.5 => (s -> println(\"KE = \", total_kinetic_energy(u))),   # every 0.5 sim-time units\n     ])\n\nArguments:\n\nstop_time: stop when sim_time >= stop_time\nstop_iteration: stop after N iterations\nstop_wall_time: stop after N seconds of wall time\ncfl: a CFL controller — if given, dt = compute_timestep(cfl) each step\noutputs: extra output handlers to process each step (handlers created with the solver are already auto-registered; use this for ones created from a dist)\ncallbacks: vector of interval => function pairs; the function receives the solver\nlog_interval: print a built-in progress line every N iterations (0 to disable)\nprogress: print start/finish @info lines (default true)\n\nCallback (and output) intervals can be Int (every N iterations) or Float64 (every T sim-time units). Custom diagnostics — time, timestep, and field maxima via global_max/global_min/global_sum — go in a callback. Those three reducers take a ScalarField; for a VectorField pass a component (u.components[1]) or use an aggregate such as total_kinetic_energy(u).\n\nwarning: Annotate the callback vector with `Pair[...]` when mixing interval types\nAn Int interval means iterations, a Float64 interval means sim-time. In a plain […] literal Julia promotes the element type, so [100 => f, 0.5 => g] becomes a Vector{Pair{Float64,Function}} and the 100 silently turns into 100.0 — \"every 100 iterations\" quietly becomes \"every 100 time units\", and the callback never fires in a short run. Writing the literal as Pair[100 => f, 0.5 => g] keeps each interval's type intact.","title":"run!"},{"category":"section","location":"api/solvers/#step!","page":"Solvers","text":"Advance solution by one timestep (for custom loops):\n\nstep!(solver)           # Use solver.dt\nstep!(solver, dt)       # Use specified dt","title":"step!"},{"category":"section","location":"api/solvers/#proceed","page":"Solvers","text":"Check if simulation should continue — tests sim_time, iteration and wall_time against the solver's stop conditions:\n\nsolver.stop_sim_time = 10.0\n\nwhile proceed(solver)\n    step!(solver)\nend","title":"proceed"},{"category":"section","location":"api/solvers/#diagnose","page":"Solvers","text":"Print a formatted summary of solver state:\n\ndiagnose(solver)\n\nShows: timestepper, dt, sim time, iteration, architecture, MPI ranks, bases, state fields and DOF, equations, compiled-RHS status, boundary conditions, subproblem decomposition, stop conditions, and performance stats.","title":"diagnose"},{"category":"section","location":"api/solvers/#rhs-compilation","page":"Solvers","text":"During solver construction, Tarang.jl compiles each equation's explicit right-hand side into a type-specialized expression tree (the \"lazy\" RHS). This eliminates runtime type dispatch and per-timestep allocation.\n\nIf any term fails to translate, the whole solver — every equation — falls back to the interpreted evaluator.\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\nsolver.rhs_plan.is_compiled   # true  ⇒ fast path\n                              # false ⇒ interpreted fallback\ndiagnose(solver)              # reports the same, with the rest of the setup\n\nwarning: Do not ignore a fallback\nThe interpreted path is roughly 100× slower per RHS evaluation, and under MPI it is also wrong: on a distributed all-Fourier field its derivative evaluates to zero, so every term built on one silently vanishes. At 2 ranks the advection in ∂t(s) - nu*lap(s) = -u⋅∇(s) contributes nothing — only the diffusion survives, and nothing is raised. (Serial is correct, merely slow.) A fallback you did not intend is a bug in your equation, not a graceful degradation.Turn it into a hard error while developing:Tarang.require_lazy_rhs!()   # solver construction now errors instead of warning\n\nWhat compiles. Arithmetic (+ - * / ^), scalar functions (sin, exp, tanh, …), field and parameter references, derivatives (∂x(u), d(u,x)), and the vector/tensor operators that reduce to a scalar: lap(u), div(v), div(grad(u)).\n\nWhat does not, and why. curl (and anything built on it) has no scalar form and is not translated: the whole solver falls back, with a loud warning.\n\nTwo further cases should fall back, and do — but only when the derivative is spelled lap(·) or div(grad(·)). Written as a bare derivative (∂z(u), d(u,z), or the advection u⋅∇(u)) they are not declined, and the two behave very differently:\n\nCase As lap(·) / div(grad(·)) As a bare derivative (∂z(u), u⋅∇(u))\nDerivative along a distributed non-Fourier (Chebyshev) axis Declined → interpreted fallback, with a warning. Correct: the compiled path differentiates in coefficient space, where that axis is the decomposed one, while the interpreted path differentiates in grid space, where it is local. Compiles, then hard-errors on the first RHS evaluation (cannot differentiate along the non-Fourier axis … of a DISTRIBUTED field). Loud, not silent. Move the term to the implicit (L) side, or run serial.\nLegendre / non-Chebyshev Jacobi bases Declined → interpreted fallback, with a warning. Correct. Compiles and is silently wrong — it applies an unnormalized differentiation matrix to orthonormal coefficients. Measured on a 20-step Legendre run: the compiled answer disagrees with the interpreted one by 32%.\n\ndanger: Bare derivatives on a Legendre axis are silently wrong in an explicit RHS\nThis is a live bug, not a design choice: the guard that declines these axes is consulted only by the lap/div translators, so ∂z(u), d(u,z) and u⋅∇(u) slip past it. There is no warning and no fallback — the RHS is simply wrong. Until it is fixed, keep derivatives along a Legendre / non-Chebyshev Jacobi axis out of the explicit RHS (move them to the implicit L side), or write them as lap(·)/div(grad(·)), which do fall back correctly.\n\nnote: Behavior change\nlap(u*v) and div(grad(u)) in an explicit RHS used to raise an error inside the interpreted evaluator, which was caught and swallowed — silently zeroing that equation's entire right-hand side. Scripts written that way were integrating F ≡ 0. They now compile and produce correct results, so their output will differ from earlier versions.\n\n","title":"Compiled RHS"},{"category":"section","location":"api/solvers/#BoundaryValueSolver","page":"Solvers","text":"Steady-state solver. It handles both linear (LBVP) and nonlinear (NLBVP) boundary value problems — there is no separate nonlinear solver type; the problem type selects the algorithm (direct per-mode solve for an LBVP, per-mode Newton iteration with a symbolic Frechet Jacobian for an NLBVP).\n\nConstructor:\n\nBoundaryValueSolver(\n    problem::Union{LBVP, NLBVP};\n    device::String=\"cpu\",\n    matsolver=:sparse,\n    solver_type=nothing,\n    tolerance::Real=1e-10,\n    max_iterations::Int=100\n)\n\nArguments:\n\nproblem: LBVP or NLBVP problem definition\nmatsolver: matrix-solver backend for the per-mode tau systems (see Solver options)\nsolver_type: alias for matsolver; when it is nothing (the default), matsolver is used\ntolerance: Newton convergence tolerance (NLBVP only; ignored for an LBVP)\nmax_iterations: maximum Newton iterations (NLBVP only)\n\nMethods:","title":"BoundaryValueSolver"},{"category":"section","location":"api/solvers/#solve!","page":"Solvers","text":"Solve the boundary value problem. The solution is written in place into the problem's fields (in coefficient layout); solve! returns the solver.\n\nsolve!(solver)\n\nExample — 2D Poisson Δφ = -2 with φ = 0 on both z walls (exact solution φ = z(Lz - z); the run below reproduces it to 1.4e-16):\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb  = RealFourier(coords[\"x\"]; size=4,  bounds=(0.0, 2π))\nzb  = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))\ndom = Domain(dist, (xb, zb))\n\nphi  = ScalarField(dom, \"phi\")\ntau1 = ScalarField(dist, \"tau1\", (xb,), Float64)   # one tau variable per BC\ntau2 = ScalarField(dist, \"tau2\", (xb,), Float64)\nlb2  = derivative_basis(zb, 2)\n\nproblem = LBVP([phi, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"Δ(phi) + l1 + l2 = -2\")\nadd_bc!(problem, \"phi(z=0)   = 0\")     # BCs use add_bc!, never add_equation!\nadd_bc!(problem, \"phi(z=1.0) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\n\nensure_layout!(phi, :g)                # the solve writes coefficients\nphi_grid = get_grid_data(phi)\n\nThe bounded (Chebyshev) direction needs explicit tau variables lifted into the bulk equation — see Problems for the tau method.","title":"solve!"},{"category":"section","location":"api/solvers/#Nonlinear-problems-(NLBVP)","page":"Solvers","text":"An NLBVP is passed to the same BoundaryValueSolver. Put the nonlinear terms on the right-hand side; the solver linearizes them symbolically and runs a per-Fourier-mode Newton iteration, rebuilding the Jacobian at the current state each iteration. The current field values are the initial guess.\n\n# same domain/tau setup as above, plus a forcing field g\nproblem = NLBVP([u, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2), g=g)\nadd_equation!(problem, \"Δ(u) + l1 + l2 = u*u + g\")   # nonlinearity on the RHS\nadd_bc!(problem, \"u(z=0)   = 0\")\nadd_bc!(problem, \"u(z=1.0) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolver.tolerance = 1e-10          # Newton residual tolerance\nsolver.max_iterations = 100\n\nensure_layout!(u, :g); get_grid_data(u) .= 0.0       # initial guess\nsolve!(solver)\nensure_layout!(u, :g)\n\nWith g = -2 - u_exact² this manufactured problem has the exact solution u = z(Lz - z), which the Newton solve recovers to ~1.9e-12.\n\nsolve! returns the solver, not a convergence flag. If Newton does not reach tolerance within max_iterations the per-mode path warns and leaves the last iterate in the fields; check the residual yourself if you need a hard failure.\n\nwarning: One solver per problem object\nSolver construction merges the problem's add_bc! conditions into its equation list. Doing it twice appends them twice (1 equation → 3 → 5), so building a second solver from a Problem that already has one produces an over-determined system: the next solve! dies with DimensionMismatch (BVP) or the build itself with Number of equations (5) does not match number of variables (3) (EVP). To sweep a parameter, rebuild the problem from scratch — the fields persist, so the previous solution carries over as the next initial guess.\n\n","title":"Nonlinear problems (NLBVP)"},{"category":"section","location":"api/solvers/#EigenvalueSolver","page":"Solvers","text":"Solver for Eigenvalue Problems (EVP).\n\nConstructor:\n\nEigenvalueSolver(\n    problem::EVP;\n    nev::Int=10,\n    which::Union{String,Symbol}=:LM,\n    target::Union{Nothing,ComplexF64}=nothing,\n    matsolver=:sparse\n)\n\nArguments:\n\nproblem: EVP problem definition\nnev: Number of eigenvalues to return\nwhich: Which eigenvalues to keep. Symbol or String — :LM and \"LM\" are equivalent\ntarget: Order by proximity to this shift instead of by which. The default, nothing, means no shift (not a zero shift)\nmatsolver: matrix-solver backend (see Solver options)\n\nWhich options:\n\n:LM: Largest magnitude\n:SM: Smallest magnitude\n:LR: Largest real part\n:SR: Smallest real part\n:LI: Largest imaginary part\n:SI: Smallest imaginary part\n\nEigenvalue convention: the eigenvalue replaces the time derivative (dt(u) → σu), so keep the dt(·) term in the equation — it is what builds the mass matrix M. Returned eigenvalues are the growth rates σ of u ~ e^{σt}.\n\nMethods:","title":"EigenvalueSolver"},{"category":"section","location":"api/solvers/#solve!-2","page":"Solvers","text":"Compute eigenvalues and eigenvectors.\n\neigenvalues, eigenvectors = solve!(solver)\n\nReturns:\n\neigenvalues: Vector{ComplexF64}, ordered by which (or by distance to target)\neigenvectors: Matrix{ComplexF64} whose columns are the modes, in coefficient space. It is returned only when the problem has a single subproblem (no Fourier direction, serial); otherwise it comes back empty (0×0), because a per-mode eigenvector is defined per Fourier mode.\n\nExample — 1D diffusion eigenproblem σu = Δu with Dirichlet walls. The exact eigenvalues are σ_n = -(nπ/Lz)²; the run below returns -9.8696, -39.4784, -88.8264, …, matching to ~1e-12:\n\ncoords = CartesianCoordinates(\"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nzb     = ChebyshevT(coords[\"z\"]; size=32, bounds=(0.0, 1.0))\ndom    = Domain(dist, (zb,))\n\nu    = ScalarField(dom, \"u\")\ntau1 = ScalarField(dist, \"tau1\", (), Float64)\ntau2 = ScalarField(dist, \"tau2\", (), Float64)\nlb2  = derivative_basis(zb, 2)\n\nproblem = EVP([u, tau1, tau2]; eigenvalue=:σ)\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"dt(u) - Δ(u) - l1 - l2 = 0\")   # dt(u) → σu marks M\nadd_bc!(problem, \"u(z=0)   = 0\")\nadd_bc!(problem, \"u(z=1.0) = 0\")\n\nsolver = EigenvalueSolver(problem; nev=5, which=:LR)   # 5 fastest-growing modes\neigenvalues, eigenvectors = solve!(solver)\n\n# Most unstable mode\nmax_idx     = argmax(real.(eigenvalues))\ngrowth_rate = real(eigenvalues[max_idx])\nfrequency   = imag(eigenvalues[max_idx])\nprintln(\"Maximum growth rate: $growth_rate\")\nprintln(\"Frequency: $frequency\")\n\ncritical_mode = eigenvectors[:, max_idx]   # columns are modes\n\nTo search near a shift instead, pass target:\n\nsolver = EigenvalueSolver(problem; nev=3, target=-40.0 + 0.0im)\neigenvalues, _ = solve!(solver)   # -> -39.48, -9.87, -88.83  (ordered by |σ - target|)\n\n","title":"solve!"},{"category":"section","location":"api/solvers/#Solver-State","page":"Solvers","text":"The solution lives in the fields themselves; solver.state is the vector of ScalarFields the solver advances (a VectorField contributes one entry per component). There is no separate state-vector object to get or set — initialize and inspect the fields directly:\n\nsolver.state                       # Vector{ScalarField}: e.g. [s, u_x, u_y]\n\nf = solver.state[1]\nensure_layout!(f, :g)              # switch to grid layout\nget_grid_data(f)                   # the local grid array\n\nSet initial conditions on the fields before stepping (set!, fill_random!, or by writing into get_grid_data), and read them back the same way afterwards.\n\n","title":"Solver State"},{"category":"section","location":"api/solvers/#Time-Integration","page":"Solvers","text":"run! is the recommended driver. The patterns below are for custom loops.","title":"Time Integration"},{"category":"section","location":"api/solvers/#Time-Stepping-Loop","page":"Solvers","text":"solver = InitialValueSolver(problem, RK222(); dt=0.001)\n\nsolver.stop_sim_time = 10.0\n\nwhile proceed(solver)\n    step!(solver)\n\n    if solver.iteration % 100 == 0\n        println(\"t = $(solver.sim_time), iteration = $(solver.iteration)\")\n    end\nend","title":"Time Stepping Loop"},{"category":"section","location":"api/solvers/#Adaptive-Time-Stepping","page":"Solvers","text":"The CFL controller is constructed from the solver (not the problem), and velocities are added as VectorFields:\n\ncfl = CFL(solver; initial_dt=1e-3, cadence=1, safety=0.4,\n          max_change=1.5, min_change=0.5, max_dt=0.01)\nadd_velocity!(cfl, u)          # u::VectorField\n\n# Adaptive loop\nwhile solver.sim_time < t_end\n    solver.dt = compute_timestep(cfl)\n    step!(solver)\nend\n\nOr — the usual form — hand the controller to run! and let it do the same thing:\n\nrun!(solver; stop_time=t_end, cfl=cfl)\n\nSee Analysis for the full CFL kwarg list.","title":"Adaptive Time Stepping"},{"category":"section","location":"api/solvers/#Output-During-Integration","page":"Solvers","text":"Create the handler with the solver: it then registers itself, and run! processes it at its own cadence and closes it at the end.\n\noutput = add_file_handler(\"output/snap\", solver; sim_dt=0.1, max_writes=10)\nadd_task!(output, u; name=\"u\")        # pass field/operator OBJECTS, not strings\n\nrun!(solver; stop_time=t_end, cfl=cfl)\n\nA handler created from a dist instead of a solver does not auto-register — and its constructor needs a third positional vars namespace, which the solver form supplies for you. Pass such a handler to run! explicitly, or it never writes:\n\nh = add_file_handler(\"output/snap\", dist, Dict{String,Any}(); sim_dt=0.1)\nadd_task!(h, u; name=\"u\")\n\nrun!(solver; stop_time=t_end, outputs=[h])   # without outputs=, h is never processed\n\nSee I/O for the file layout and how to read the results back.\n\n","title":"Output During Integration"},{"category":"section","location":"api/solvers/#Solver-options","page":"Solvers","text":"","title":"Solver options"},{"category":"section","location":"api/solvers/#Matrix-solver-backend","page":"Solvers","text":"Every solver takes matsolver, which selects the backend used to factor the per-mode tau systems. Registered names:\n\nname backend\n:sparse, :lu, :direct sparse LU (default)\n:dense dense LU\n:banded banded LU\n:block, :blockdiagonal block-diagonal\n:spqr, :qr sparse rank-revealing QR\n\nsolver = BoundaryValueSolver(problem; matsolver=:dense)\n\nmatsolver=:iterative is accepted but warns and falls back to the sparse direct solver — there is no iterative or multigrid backend.","title":"Matrix solver backend"},{"category":"section","location":"api/solvers/#Convergence-criteria","page":"Solvers","text":"Only the nonlinear (NLBVP) path iterates:\n\nsolver.tolerance      = 1e-10   # Newton residual tolerance\nsolver.max_iterations = 100     # maximum Newton iterations","title":"Convergence criteria"},{"category":"section","location":"api/solvers/#Performance","page":"Solvers","text":"solver.performance_stats   # total_time, total_steps, total_solves, avg_step_time\ndiagnose(solver)           # prints the configuration + the same stats\n\n","title":"Performance"},{"category":"section","location":"api/solvers/#Complete-Example","page":"Solvers","text":"","title":"Complete Example"},{"category":"section","location":"api/solvers/#Time-Dependent-Simulation","page":"Solvers","text":"2D Rayleigh-Bénard convection: Fourier in x, Chebyshev in z, tau variables for the no-slip and fixed-temperature walls.\n\nusing Tarang\n\nLx, Lz = 4.0, 1.0\nNx, Nz = 64, 32\nRayleigh, Prandtl = 2e4, 1.0\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nzbasis = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\np = ScalarField(domain, \"p\")\nT = ScalarField(domain, \"T\")\nu = VectorField(domain, \"u\")\n\n# tau variables: one per boundary condition, carrying the Fourier basis\ntau_p  = ScalarField(dist, \"tau_p\",  (), Float64)          # pressure gauge\ntau_T1 = ScalarField(dist, \"tau_T1\", (xbasis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (xbasis,), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis,), Float64)\n\n# first-order reduction: grad_X = ∇X + ẑ·lift(τ)\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_T = grad(T) + ez * τ_lift(tau_T1)\n\nproblem = IVP([p, T, u, tau_p, tau_T1, tau_T2, tau_u1, tau_u2])\nadd_parameters!(problem, nu=Prandtl, buoy=Rayleigh*Prandtl, ez=ez,\n                grad_u=grad_u, grad_T=grad_T, τ_lift=τ_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) - buoy*T*ez + τ_lift(tau_u2) = -u⋅∇(u)\")\n\nadd_bc!(problem, \"T(z=0) = 1\")\nadd_bc!(problem, \"T(z=1) = 0\")\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")       # pressure gauge\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)\n\n# Initial condition: conduction profile + damped noise\nx, z = local_grids(dist, xbasis, zbasis)\nfill_random!(T, \"g\"; seed=42, distribution=\"normal\", scale=1e-3)\nget_grid_data(T) .*= z' .* (1.0 .- z')\nget_grid_data(T) .+= 1.0 .- z'\nensure_layout!(T, :c)\n\n# Adaptive timestep\ncfl = CFL(solver; initial_dt=1e-4, cadence=10, safety=0.4, max_dt=1e-3)\nadd_velocity!(cfl, u)\n\n# Output — created with the solver, so run! writes and closes it\noutput = add_file_handler(\"output/rbc\", solver; sim_dt=0.1)\nadd_task!(output, T; name=\"T\")\nadd_task!(output, u; name=\"u\")\n\nrun!(solver; stop_time=0.5, cfl=cfl, log_interval=500,\n     callbacks=Pair[\n         500 => (s -> println(\"t = $(s.sim_time), dt = $(s.dt), max|T| = $(global_max(T))\")),\n     ])\n\nThis takes ~21,800 steps (a few minutes on one core) and ends with max|T| = 1.0, max|u_z| ≈ 56.5, and the walls held to ~1e-14. Shrink Nx/Nz or stop_time to try it quickly.\n\nwarning: Choose `max_dt` conservatively\nWhile the fluid is still at rest the CFL estimate is unbounded, so the controller jumps straight to max_dt on its first update — max_dt is the timestep for the early transient. At max_dt=1e-2 this problem goes unstable and every field becomes NaN (silently — nothing throws). 1e-3 is stable; the CFL then pulls dt down to ~2e-5 once convection sets in.\n\nwarning: This example is serial-only\nUnder MPI the Chebyshev axis must come first, and a Chebyshev derivative cannot appear in the explicit RHS — which the -u⋅∇(u) and -u⋅∇(T) advection terms above require. Distributed runs on a Chebyshev-Fourier domain need every explicit derivative to be along a Fourier axis. See Parallelism.\n\n","title":"Time-Dependent Simulation"},{"category":"section","location":"api/solvers/#See-Also","page":"Solvers","text":"Problems: Problem definition\nTimesteppers: Time integration schemes\nAnalysis: CFL conditions and diagnostics\nTutorial: IVP: Complete example","title":"See Also"},{"category":"section","location":"pages/gql_approximation/#Generalized-Quasi-Linear-(GQL)-Approximation","page":"GQL Approximation","text":"Wavenumber-based scale separation for turbulence modeling and wave-mean flow interactions.\n\nReference: Marston, Chini & Tobias (2016). \"Generalized Quasilinear Approximation: Application to Zonal Jets\". Phys. Rev. Lett. 116, 214501.\n\n","title":"Generalized Quasi-Linear (GQL) Approximation"},{"category":"section","location":"pages/gql_approximation/#TL;DR-Quick-Summary","page":"GQL Approximation","text":"What: Splits fields into large-scale (|k| ≤ Λ) and small-scale (|k| > Λ) components in Fourier spaceWhy: Enables systematic approximations between full nonlinear (NL) and quasi-linear (QL) dynamicsKey insight: By varying Λ from 0 to k_max, you interpolate between QL (cheap) and NL (expensive)Which system to use:GQLDecomposition - Pure spectral decomposition (you handle FFTs)\nGQLWaveMeanSystem - Combined GQL + temporal filtering for complete wave-mean analysis\n\n","title":"TL;DR - Quick Summary"},{"category":"section","location":"pages/gql_approximation/#Quick-Start","page":"GQL Approximation","text":"","title":"Quick Start"},{"category":"section","location":"pages/gql_approximation/#Basic-GQL-Decomposition-(4-lines-of-code)","page":"GQL Approximation","text":"using Tarang\nusing FFTW\n\n# 1. Create GQL decomposition: 64×64 grid, domain 2π×2π, cutoff |k| ≤ 4\ngql = GQLDecomposition((64, 64), (2π, 2π); Λ=4.0)\n\n# 2. Decompose your spectral field\nf_hat = rfft(f)  # Your field in spectral space\nf_large, f_small = decompose!(gql, f_hat)\n\n# 3. Use in your simulation\n# f_large: large-scale modes (|k| ≤ Λ)\n# f_small: small-scale modes (|k| > Λ)","title":"Basic GQL Decomposition (4 lines of code)"},{"category":"section","location":"pages/gql_approximation/#Complete-Working-Example","page":"GQL Approximation","text":"using Tarang\nusing FFTW\n\n# Domain setup\nNx, Ny = 64, 64\nLx, Ly = 2π, 2π\nx = range(0, Lx, length=Nx+1)[1:end-1]\ny = range(0, Ly, length=Ny+1)[1:end-1]\n\n# Create test field: large-scale + small-scale\nf = zeros(Nx, Ny)\nfor j in 1:Ny, i in 1:Nx\n    # Large scale: k = 2\n    f[i,j] += sin(2*x[i]) * cos(2*y[j])\n    # Small scale: k = 10\n    f[i,j] += 0.3 * sin(10*x[i]) * cos(10*y[j])\nend\n\n# Create GQL decomposition with cutoff Λ = 5\n# This will separate k ≤ 5 (large) from k > 5 (small)\ngql = GQLDecomposition((Nx, Ny), (Lx, Ly); Λ=5.0)\n\n# Decompose\nf_hat = rfft(f)\nf_L_hat, f_S_hat = decompose!(gql, f_hat)\n\n# Transform back to physical space\nf_large = irfft(f_L_hat, Nx)  # Contains only k=2 mode\nf_small = irfft(f_S_hat, Nx)  # Contains only k=10 mode\n\n# Verify\nprintln(\"Large-scale max: \", maximum(abs.(f_large)))  # ≈ 1.0\nprintln(\"Small-scale max: \", maximum(abs.(f_small)))  # ≈ 0.3\nprintln(\"f ≈ f_L + f_S: \", maximum(abs.(f - f_large - f_small)))  # ≈ 0\n\n","title":"Complete Working Example"},{"category":"section","location":"pages/gql_approximation/#Understanding-the-GQL-Approximation","page":"GQL Approximation","text":"","title":"Understanding the GQL Approximation"},{"category":"section","location":"pages/gql_approximation/#Scale-Separation-in-Fourier-Space","page":"GQL Approximation","text":"The GQL approximation splits any field f into two parts based on wavenumber magnitude:\n\nf = f_L + f_S\n\nwhere:\n\nf_L = Large-scale (low wavenumber): modes with |k| ≤ Λ\nf_S = Small-scale (high wavenumber): modes with |k| > Λ\n\nWavenumber space (2D example):\n                    ky\n                    ↑\n                    │    Small-scale\n                    │      (|k| > Λ)\n            ────────┼────────\n           /        │        \\\n          /    ┌────┼────┐    \\\n         │     │    │    │     │\n    ─────┼─────┼────┼────┼─────┼───→ kx\n         │     │ Large   │     │\n          \\    │ scale  │    /\n           \\   └────────┘   /\n            ────────────────\n                    │\n\n    Inner circle: |k| ≤ Λ (Large-scale)\n    Outer region: |k| > Λ (Small-scale)","title":"Scale Separation in Fourier Space"},{"category":"section","location":"pages/gql_approximation/#The-GQL-Hierarchy","page":"GQL Approximation","text":"By varying the cutoff Λ, you get different approximations:\n\nΛ Value Name Nonlinear Terms Cost Accuracy\nΛ = 0 QL (Quasi-Linear) Only NL(fL, fL) Cheapest Lowest\n0 < Λ < k_max GQL NL(fL, fL) + NL(fS, fS) projected to L Intermediate Better\nΛ = k_max Full NL All terms Most expensive Exact","title":"The GQL Hierarchy"},{"category":"section","location":"pages/gql_approximation/#GQL-Equations","page":"GQL Approximation","text":"For a generic nonlinear PDE ∂f/∂t = NL(f, f) + L(f):\n\nLarge-scale equation:\n\n∂f_L/∂t = NL(f_L, f_L) + P_L[NL(f_S, f_S)] + L(f_L)\n                         ↑ eddy feedback\n\nSmall-scale equation:\n\n∂f_S/∂t = NL(f_L, f_S) + NL(f_S, f_L) + L(f_S)\n          ↑ linear in f_S (no f_S self-interaction!)\n\nKey simplification: The NL(fS, fS) term is dropped from the small-scale equation!\n\n","title":"GQL Equations"},{"category":"section","location":"pages/gql_approximation/#API-Reference","page":"GQL Approximation","text":"","title":"API Reference"},{"category":"section","location":"pages/gql_approximation/#GQLDecomposition","page":"GQL Approximation","text":"Pure wavenumber decomposition without temporal filtering.\n\n# Constructor\ngql = GQLDecomposition(field_size, domain_size; Λ, dtype=Float64)\n\n# Arguments:\n#   field_size  - Physical space size, e.g., (Nx, Ny) or (Nx, Ny, Nz)\n#   domain_size - Horizontal domain size, e.g., (Lx, Ly)\n#   Λ           - Cutoff wavenumber\n#   dtype       - Element type (default: Float64)","title":"GQLDecomposition"},{"category":"section","location":"pages/gql_approximation/#Methods","page":"GQL Approximation","text":"Function Description\ndecompose!(gql, f_hat) Split into (flarge, fsmall)\nproject_large!(gql, f_hat) Zero out k > Λ (in-place)\nproject_small!(gql, f_hat) Zero out k ≤ Λ (in-place)\nget_cutoff(gql) Get current Λ\nset_cutoff!(gql, Λ_new) Change Λ (rebuilds mask)\ncount_large_modes(gql) Number of large-scale modes\ncount_small_modes(gql) Number of small-scale modes","title":"Methods"},{"category":"section","location":"pages/gql_approximation/#Example:-In-place-Projection","page":"GQL Approximation","text":"# Instead of decompose!, you can project in-place\nf_hat = rfft(f)\n\n# Option 1: Get both components\nf_L, f_S = decompose!(gql, f_hat)\n\n# Option 2: Project in-place (modifies f_hat)\nf_hat_copy = copy(f_hat)\nproject_large!(gql, f_hat_copy)  # Now contains only modes with k ≤ Λ\n\n","title":"Example: In-place Projection"},{"category":"section","location":"pages/gql_approximation/#GQLWaveMeanSystem","page":"GQL Approximation","text":"Combined GQL decomposition with temporal filtering for wave-mean flow analysis.\n\n# Constructor\nsys = GQLWaveMeanSystem(field_size, domain_size; Λ, α, horizontal_dims=(1,2), dtype=Float64)\n\n# Arguments:\n#   field_size      - Physical space size\n#   domain_size     - Horizontal domain size\n#   Λ               - GQL wavenumber cutoff\n#   α               - Temporal filter parameter (1/averaging_time)\n#   horizontal_dims - Dimensions for horizontal averaging","title":"GQLWaveMeanSystem"},{"category":"section","location":"pages/gql_approximation/#Methods-2","page":"GQL Approximation","text":"Function Description\nadd_field!(sys, :u) Register field for decomposition\nadd_flux!(sys, :uw) Register flux product ⟨u'w'⟩\nupdate!(sys, fields_hat, fields_phys, dt) Update decomposition and filters\nget_large(sys, :u) Large-scale spectral component\nget_small(sys, :u) Small-scale spectral component\nget_mean(sys, :u) Time-filtered mean profile (1D)\nget_flux(sys, :uw) Filtered wave flux profile\nget_cutoff(sys) Get Λ\nset_cutoff!(sys, Λ_new) Change Λ\n\n","title":"Methods"},{"category":"section","location":"pages/gql_approximation/#Complete-GQL-Simulation-Example","page":"GQL Approximation","text":"","title":"Complete GQL Simulation Example"},{"category":"section","location":"pages/gql_approximation/#2D-Barotropic-Vorticity-(β-plane)","page":"GQL Approximation","text":"using Tarang\nusing FFTW\nusing LinearAlgebra\n\n# ============================================================================\n# GQL simulation of 2D turbulence with β-effect (zonal jet formation)\n# ============================================================================\n\n# Grid\nNx, Ny = 128, 128\nLx, Ly = 2π, 2π\ndx, dy = Lx/Nx, Ly/Ny\n\n# Wavenumbers\nkx = rfftfreq(Nx, 2π/dx)\nky = fftfreq(Ny, 2π/dy)\nKX = [kx[i] for i in 1:length(kx), j in 1:Ny]\nKY = [ky[j] for i in 1:length(kx), j in 1:Ny]\nK2 = KX.^2 .+ KY.^2\nK2[1,1] = 1  # Avoid division by zero\n\n# Physical parameters\nβ = 10.0     # β-effect (planetary vorticity gradient)\nν = 1e-4     # Viscosity\ndt = 0.001\nnsteps = 10000\n\n# GQL setup: cutoff at Λ = 4 (only large scales interact nonlinearly)\nΛ = 4.0\ngql = GQLDecomposition((Nx, Ny), (Lx, Ly); Λ=Λ)\n\nprintln(\"GQL cutoff Λ = \", Λ)\nprintln(\"Large-scale modes: \", count_large_modes(gql))\nprintln(\"Small-scale modes: \", count_small_modes(gql))\n\n# Initial condition: small random perturbation\nζ = 0.1 * randn(Nx, Ny)\nζ_hat = rfft(ζ)\n\n# Preallocate\nψ_hat = similar(ζ_hat)\nu_hat = similar(ζ_hat)\nv_hat = similar(ζ_hat)\nNL_hat = similar(ζ_hat)\n\n# Time stepping (RK4)\nfunction compute_rhs!(rhs, ζ_hat, gql, K2, KX, KY, β, ν)\n    # Stream function: ψ = -ζ/k²\n    @. ψ_hat = -ζ_hat / K2\n\n    # Velocity: u = -∂ψ/∂y, v = ∂ψ/∂x\n    @. u_hat = -im * KY * ψ_hat\n    @. v_hat =  im * KX * ψ_hat\n\n    # Transform to physical space\n    ζ = irfft(ζ_hat, Nx)\n    u = irfft(u_hat, Nx)\n    v = irfft(v_hat, Nx)\n\n    # ========================================\n    # GQL APPROXIMATION: Decompose velocity\n    # ========================================\n    u_hat_full = rfft(u)\n    v_hat_full = rfft(v)\n\n    # Large-scale velocity\n    u_L_hat, u_S_hat = decompose!(gql, u_hat_full)\n    u_L = irfft(copy(u_L_hat), Nx)\n\n    v_L_hat, v_S_hat = decompose!(gql, v_hat_full)\n    v_L = irfft(copy(v_L_hat), Nx)\n\n    # Small-scale velocity\n    u_S = irfft(copy(u_S_hat), Nx)\n    v_S = irfft(copy(v_S_hat), Nx)\n\n    # Vorticity decomposition\n    ζ_L_hat, ζ_S_hat = decompose!(gql, copy(ζ_hat))\n    ζ_L = irfft(copy(ζ_L_hat), Nx)\n    ζ_S = irfft(copy(ζ_S_hat), Nx)\n\n    # ========================================\n    # GQL Nonlinear terms\n    # ========================================\n    # Large-scale: NL(u_L, ζ_L) + project_L(NL(u_S, ζ_S))\n    NL_LL = u_L .* irfft(im * KX .* ζ_L_hat, Nx) .+\n            v_L .* irfft(im * KY .* ζ_L_hat, Nx)\n\n    NL_SS = u_S .* irfft(im * KX .* ζ_S_hat, Nx) .+\n            v_S .* irfft(im * KY .* ζ_S_hat, Nx)\n    NL_SS_hat = rfft(NL_SS)\n    project_large!(gql, NL_SS_hat)  # Keep only |k| ≤ Λ\n\n    # Small-scale: NL(u_L, ζ_S) + NL(u_S, ζ_L)  [NO NL(u_S, ζ_S)]\n    NL_LS = u_L .* irfft(im * KX .* ζ_S_hat, Nx) .+\n            v_L .* irfft(im * KY .* ζ_S_hat, Nx)\n    NL_SL = u_S .* irfft(im * KX .* ζ_L_hat, Nx) .+\n            v_S .* irfft(im * KY .* ζ_L_hat, Nx)\n\n    # Combine\n    NL_large = rfft(NL_LL) .+ NL_SS_hat\n    NL_small = rfft(NL_LS .+ NL_SL)\n\n    # Total advection (GQL)\n    @. NL_hat = NL_large + NL_small\n\n    # RHS: -u·∇ζ - βv + ν∇²ζ\n    @. rhs = -NL_hat - β * im * KX * ψ_hat - ν * K2 * ζ_hat\nend\n\n# Main time loop\nk1, k2, k3, k4 = similar(ζ_hat), similar(ζ_hat), similar(ζ_hat), similar(ζ_hat)\nζ_tmp = similar(ζ_hat)\n\nfor step in 1:nsteps\n    # RK4 stages\n    compute_rhs!(k1, ζ_hat, gql, K2, KX, KY, β, ν)\n    @. ζ_tmp = ζ_hat + 0.5*dt*k1\n    compute_rhs!(k2, ζ_tmp, gql, K2, KX, KY, β, ν)\n    @. ζ_tmp = ζ_hat + 0.5*dt*k2\n    compute_rhs!(k3, ζ_tmp, gql, K2, KX, KY, β, ν)\n    @. ζ_tmp = ζ_hat + dt*k3\n    compute_rhs!(k4, ζ_tmp, gql, K2, KX, KY, β, ν)\n\n    @. ζ_hat = ζ_hat + (dt/6) * (k1 + 2*k2 + 2*k3 + k4)\n\n    # Diagnostics\n    if step % 1000 == 0\n        ζ = irfft(ζ_hat, Nx)\n        enstrophy = sum(ζ.^2) * dx * dy / (Lx * Ly)\n\n        # Zonal mean (x-average)\n        u = irfft(-im * KY .* (-ζ_hat ./ K2), Nx)\n        u_zonal = vec(mean(u, dims=1))\n\n        println(\"Step $step: Enstrophy = $(round(enstrophy, digits=4)), max|u_zonal| = $(round(maximum(abs.(u_zonal)), digits=4))\")\n    end\nend\n\n","title":"2D Barotropic Vorticity (β-plane)"},{"category":"section","location":"pages/gql_approximation/#Combining-GQL-with-Wave-Mean-Decomposition","page":"GQL Approximation","text":"For problems with clear wave-mean separation (e.g., internal waves + zonal flow):\n\nusing Tarang\nusing FFTW\n\n# Domain\nNx, Ny, Nz = 64, 64, 32\nLx, Ly = 2π, 2π\ndt = 0.01\n\n# Combined system: GQL (Λ=4) + temporal filter (α=0.1)\nsys = GQLWaveMeanSystem((Nx, Ny, Nz), (Lx, Ly); Λ=4.0, α=0.1)\n\n# Register fields\nadd_field!(sys, :u)\nadd_field!(sys, :v)\nadd_field!(sys, :w)\nadd_field!(sys, :b)\n\n# Register fluxes for Reynolds stress\nadd_flux!(sys, :uw)  # ⟨u'w'⟩\nadd_flux!(sys, :vw)  # ⟨v'w'⟩\nadd_flux!(sys, :wb)  # ⟨w'b'⟩\n\n# Time loop\nfor step in 1:nsteps\n    # Your PDE solver advances u, v, w, b\n    # ...\n\n    # Update GQL + temporal filtering\n    update!(sys, Dict(:u => u, :v => v, :w => w, :b => b), dt)\n\n    # Access decomposed fields\n    u_L = get_large(sys, :u)      # Large-scale (|k| ≤ Λ) in spectral space\n    u_S = get_small(sys, :u)      # Small-scale (|k| > Λ) in spectral space\n    u_mean = get_mean(sys, :u)    # Time-filtered horizontal mean ū(z)\n\n    # Access filtered Reynolds stress\n    R_uw = get_flux(sys, :uw)     # ⟨u'w'⟩(z) profile\n    R_wb = get_flux(sys, :wb)     # ⟨w'b'⟩(z) profile\n\n    # Use for forcing in mean equations:\n    # ∂ū/∂t = ... - ∂⟨u'w'⟩/∂z\n    # ∂b̄/∂t = ... - ∂⟨w'b'⟩/∂z\nend\n\n","title":"Combining GQL with Wave-Mean Decomposition"},{"category":"section","location":"pages/gql_approximation/#Choosing-the-Cutoff-Λ","page":"GQL Approximation","text":"","title":"Choosing the Cutoff Λ"},{"category":"section","location":"pages/gql_approximation/#Guidelines","page":"GQL Approximation","text":"Application Suggested Λ Rationale\nZonal jets (β-plane) 2-6 Capture jet scale, parameterize eddies\nConvection 4-10 Large convective cells are \"mean\"\nShear flow 1-4 Mean shear + harmonics\nTesting QL validity 0 then increase Compare QL → GQL → NL","title":"Guidelines"},{"category":"section","location":"pages/gql_approximation/#Diagnostic:-Mode-Count","page":"GQL Approximation","text":"gql = GQLDecomposition((128, 128), (2π, 2π); Λ=4.0)\n\nn_large = count_large_modes(gql)\nn_small = count_small_modes(gql)\nn_total = n_large + n_small\n\nprintln(\"Large-scale modes: $n_large ($(round(100*n_large/n_total, digits=1))%)\")\nprintln(\"Small-scale modes: $n_small ($(round(100*n_small/n_total, digits=1))%)\")\n\n# Typical output for Λ=4:\n# Large-scale modes: 49 (0.6%)\n# Small-scale modes: 8143 (99.4%)","title":"Diagnostic: Mode Count"},{"category":"section","location":"pages/gql_approximation/#Sweeping-Λ-to-Test-Convergence","page":"GQL Approximation","text":"# Test GQL accuracy by varying Λ\nfor Λ in [0, 2, 4, 8, 16, Inf]\n    gql = GQLDecomposition((64, 64), (2π, 2π); Λ=Λ)\n    # Run simulation...\n    # Compare statistics (energy, enstrophy, mean profiles)\nend\n\n","title":"Sweeping Λ to Test Convergence"},{"category":"section","location":"pages/gql_approximation/#Performance-Considerations","page":"GQL Approximation","text":"","title":"Performance Considerations"},{"category":"section","location":"pages/gql_approximation/#Memory","page":"GQL Approximation","text":"System Arrays per Field Notes\nGQLDecomposition 2 complex flarge, fsmall work arrays\nGQLWaveMeanSystem 2 complex + 3 real Plus temporal filter storage","title":"Memory"},{"category":"section","location":"pages/gql_approximation/#Computational-Cost","page":"GQL Approximation","text":"Mask application: O(N) per field, very fast\nFFT/IFFT: O(N log N), dominates cost\nGQL vs NL: Similar cost per step, but GQL may allow larger Λ (fewer modes in expensive nonlinear terms)","title":"Computational Cost"},{"category":"section","location":"pages/gql_approximation/#Optimization-Tips","page":"GQL Approximation","text":"Reuse FFT plans: Pre-compute plan_rfft and plan_irfft\nIn-place operations: Use project_large! instead of decompose! when possible\nBatch updates: Update all fields before computing fluxes\n\n","title":"Optimization Tips"},{"category":"section","location":"pages/gql_approximation/#Theory:-Why-GQL-Works","page":"GQL Approximation","text":"","title":"Theory: Why GQL Works"},{"category":"section","location":"pages/gql_approximation/#The-Eddy-Mean-Decomposition","page":"GQL Approximation","text":"Traditional Reynolds decomposition: f = f̄ + f'\n\nGQL generalizes this to spectral space:\n\nf_L contains the \"mean\" (but can include some wave structure)\nf_S contains the \"eddies\" (high-k fluctuations)","title":"The Eddy-Mean Decomposition"},{"category":"section","location":"pages/gql_approximation/#Scale-Interactions","page":"GQL Approximation","text":"The nonlinear term NL(f, f) produces wavenumber triads (k₁, k₂, k₃) where k₁ + k₂ = k₃.\n\nGQL assumption: Small-scale self-interactions NL(f_S, f_S) that stay in the small scales can be neglected.\n\nThis is valid when:\n\nSmall scales are \"slaved\" to large scales\nEnergy cascade is local (not inverse cascade dominated)\nScale separation exists","title":"Scale Interactions"},{"category":"section","location":"pages/gql_approximation/#When-GQL-Fails","page":"GQL Approximation","text":"GQL may not capture:\n\nStrong inverse cascades (2D turbulence without β)\nIntermittency and extreme events\nSmall-scale instabilities\n\nAlways validate against full NL for your specific problem!\n\n","title":"When GQL Fails"},{"category":"section","location":"pages/gql_approximation/#References","page":"GQL Approximation","text":"Marston, Chini & Tobias (2016). \"Generalized Quasilinear Approximation: Application to Zonal Jets\". Phys. Rev. Lett. 116, 214501.\nTobias & Marston (2017). \"Three-dimensional rotating Couette flow via the generalised quasilinear approximation\". J. Fluid Mech. 810, 412-428.\nMarston, Chini & Tobias (2019). \"Generalized Quasilinear Approximation of the Interaction of Convection and Mean Flows\". Proc. R. Soc. A 474, 20180422.\nConstantinou & Parker (2018). \"Magnetic suppression of zonal flows on a beta plane\". Astrophys. J. 863, 46.\n\n","title":"References"},{"category":"section","location":"pages/gql_approximation/#See-Also","page":"GQL Approximation","text":"Temporal Filters - Time-domain filtering for wave-mean separation\nLES Models - Subgrid-scale modeling (alternative to GQL)\nStochastic Forcing - Adding stochastic terms to equations","title":"See Also"},{"category":"section","location":"pages/analysis/#Analysis","page":"Analysis","text":"Analysis tools for computing diagnostics, statistics, and derived quantities.","title":"Analysis"},{"category":"section","location":"pages/analysis/#CFL-Condition","page":"Analysis","text":"Compute stable timesteps based on flow velocity.\n\nusing Tarang\n\n# Create CFL calculator\ncfl = CFL(problem;\n    safety=0.5,      # Safety factor (0.3-0.5 typical)\n    max_change=1.5,  # Max dt increase per step\n    min_change=0.5   # Max dt decrease per step\n)\n\n# Register velocity field\nadd_velocity!(cfl, u)\n\n# Optional limits\ncfl.max_dt = 0.01\ncfl.min_dt = 1e-8\n\n# Compute timestep\ndt = compute_timestep(cfl)\n\nwarning: Advection only by default\nWith just add_velocity!, the returned dt accounts for advection only. Diffusion the timestepper integrates explicitly — notably an LES eddy viscosity νₑ, whose spatial variation rules out the implicit path — carries its own limit dt ≤ 1/(2 ν_max Σᵢ Δxᵢ⁻²) that nothing enforces here. Register it:add_diffusivity!(cfl, nu)                             # constant ν\nadd_diffusivity!(cfl, get_eddy_viscosity(les_model))  # LES νₑ (per-rank slab)Both limits then apply and the smaller step wins. See Diffusive Limit.","title":"CFL Condition"},{"category":"section","location":"pages/analysis/#Global-Reductions","page":"Analysis","text":"Compute global statistics across MPI processes.\n\nusing MPI\n\n# Create reducer\nreducer = GlobalArrayReducer(MPI.COMM_WORLD)\n\n# Maximum value\nglobal_max = reduce_scalar(reducer, local_max, MPI.MAX)\n\n# Sum\nglobal_sum = reduce_scalar(reducer, local_sum, MPI.SUM)\n\n# Mean (requires division by total elements)\nglobal_mean = global_sum / total_elements","title":"Global Reductions"},{"category":"section","location":"pages/analysis/#Flow-Statistics","page":"Analysis","text":"","title":"Flow Statistics"},{"category":"section","location":"pages/analysis/#Kinetic-Energy","page":"Analysis","text":"function compute_kinetic_energy(u, reducer)\n    local_energy = 0.0\n\n    for component in u.components\n        Tarang.ensure_layout!(component, :g)\n        local_energy += sum(get_grid_data(component).^2) / 2\n    end\n\n    return reduce_scalar(reducer, local_energy, MPI.SUM)\nend","title":"Kinetic Energy"},{"category":"section","location":"pages/analysis/#Enstrophy","page":"Analysis","text":"function compute_enstrophy(u, reducer)\n    # For 2D: ω = ∂v/∂x - ∂u/∂y\n    # Enstrophy = ∫ ω² dV\n\n    ux, uy = u.components[1], u.components[2]\n\n    # Compute vorticity (simplified)\n    # ... derivative calculation ...\n\n    return reduce_scalar(reducer, local_enstrophy, MPI.SUM)\nend","title":"Enstrophy"},{"category":"section","location":"pages/analysis/#Reynolds-Number","page":"Analysis","text":"function compute_reynolds_number(u, nu, L, reducer)\n    Tarang.ensure_layout!(u.components[1], :g)\n\n    # RMS velocity\n    local_u2 = sum(get_grid_data(u.components[1]).^2)\n    global_u2 = reduce_scalar(reducer, local_u2, MPI.SUM)\n    u_rms = sqrt(global_u2 / total_points)\n\n    return u_rms * L / nu\nend","title":"Reynolds Number"},{"category":"section","location":"pages/analysis/#Heat-Transfer","page":"Analysis","text":"","title":"Heat Transfer"},{"category":"section","location":"pages/analysis/#Nusselt-Number","page":"Analysis","text":"function compute_nusselt(T, w, L, kappa, reducer)\n    Tarang.ensure_layout!(T, :g)\n    Tarang.ensure_layout!(w, :g)\n\n    # Convective heat flux\n    local_flux = sum(get_grid_data(T) .* get_grid_data(w))\n    global_flux = reduce_scalar(reducer, local_flux, MPI.SUM)\n\n    # Normalize\n    flux_mean = global_flux / total_points\n\n    # Nusselt = 1 + convective/conductive\n    Nu = 1.0 + flux_mean * L / kappa\n\n    return Nu\nend","title":"Nusselt Number"},{"category":"section","location":"pages/analysis/#Spectral-Analysis","page":"Analysis","text":"","title":"Spectral Analysis"},{"category":"section","location":"pages/analysis/#Energy-Spectrum","page":"Analysis","text":"function compute_spectrum(field, kmax)\n    Tarang.ensure_layout!(field, :c)\n\n    # Initialize spectrum bins\n    E_k = zeros(kmax)\n\n    # Get wavenumbers\n    k = get_wavenumbers(field.bases[1])\n\n    # Bin energy by wavenumber\n    for (i, ki) in enumerate(k)\n        k_bin = round(Int, abs(ki))\n        if 1 <= k_bin <= kmax\n            E_k[k_bin] += abs2(get_coeff_data(field)[i])\n        end\n    end\n\n    return E_k\nend","title":"Energy Spectrum"},{"category":"section","location":"pages/analysis/#Shell-Averaged-3D-Spectrum","page":"Analysis","text":"function compute_3d_spectrum(u, kmax)\n    E_k = zeros(kmax)\n\n    for component in u.components\n        Tarang.ensure_layout!(component, :c)\n\n        kx = get_wavenumbers(component.bases[1])\n        ky = get_wavenumbers(component.bases[2])\n        kz = get_wavenumbers(component.bases[3])\n\n        for i in eachindex(kx), j in eachindex(ky), k in eachindex(kz)\n            k_mag = sqrt(kx[i]^2 + ky[j]^2 + kz[k]^2)\n            k_bin = round(Int, k_mag)\n\n            if 1 <= k_bin <= kmax\n                E_k[k_bin] += abs2(get_coeff_data(component)[i,j,k])\n            end\n        end\n    end\n\n    return E_k\nend","title":"Shell-Averaged 3D Spectrum"},{"category":"section","location":"pages/analysis/#Time-Series","page":"Analysis","text":"","title":"Time Series"},{"category":"section","location":"pages/analysis/#Recording-Diagnostics","page":"Analysis","text":"# Storage\ntimes = Float64[]\nenergies = Float64[]\nnusselts = Float64[]\n\n# During simulation\nwhile solver.sim_time < t_end\n    step!(solver, dt)\n\n    # Record\n    push!(times, solver.sim_time)\n    push!(energies, compute_kinetic_energy(u, reducer))\n    push!(nusselts, compute_nusselt(T, w, L, kappa, reducer))\nend","title":"Recording Diagnostics"},{"category":"section","location":"pages/analysis/#Saving-Time-Series","page":"Analysis","text":"if MPI.Comm_rank(MPI.COMM_WORLD) == 0\n    using JLD2\n    @save \"diagnostics.jld2\" times energies nusselts\nend","title":"Saving Time Series"},{"category":"section","location":"pages/analysis/#Spatial-Averages","page":"Analysis","text":"","title":"Spatial Averages"},{"category":"section","location":"pages/analysis/#Horizontal-Average","page":"Analysis","text":"function horizontal_average(field)\n    Tarang.ensure_layout!(field, :g)\n\n    # Average over x (first axis)\n    mean(get_grid_data(field), dims=1)\nend","title":"Horizontal Average"},{"category":"section","location":"pages/analysis/#Volume-Average","page":"Analysis","text":"function volume_average(field, reducer)\n    Tarang.ensure_layout!(field, :g)\n\n    local_sum = sum(get_grid_data(field))\n    global_sum = reduce_scalar(reducer, local_sum, MPI.SUM)\n\n    return global_sum / total_points\nend","title":"Volume Average"},{"category":"section","location":"pages/analysis/#See-Also","page":"Analysis","text":"Output: File output\nSolvers: Time integration\nAPI: Analysis: Complete reference","title":"See Also"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Tutorial:-2D-Rayleigh-Bénard-Convection","page":"2D Rayleigh-Benard","text":"This tutorial walks through the canonical Tarang example: 2D Rayleigh-Bénard convection between two horizontal plates. The code shown here mirrors examples/ivp/rayleigh_benard_2d.jl in the repository — so you can copy-paste either and they produce the same simulation.","title":"Tutorial: 2D Rayleigh-Bénard Convection"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Physical-problem","page":"2D Rayleigh-Benard","text":"Rayleigh-Bénard convection is driven by heating a fluid layer from below and cooling it from above. The setup:\n\nHorizontally periodic layer of fluid between two rigid plates\nBottom plate held at temperature T = 1 (hot)\nTop plate held at temperature T = 0 (cold)\nGravity acts downward\nNo-slip velocity on both plates\n\nAbove a critical Rayleigh number (Ra_c ≈ 1708), conduction becomes unstable and convection cells form.","title":"Physical problem"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Governing-equations-(thermal-diffusive-non-dimensionalization)","page":"2D Rayleigh-Benard","text":"We non-dimensionalize using the box height H and the thermal diffusion time τ_κ = H² / κ:\n\nlength scale: H\ntime scale: τ_κ = H² / κ\nvelocity scale: κ / H\npressure scale: ρ (κ/H)²\ntemperature scale: ΔT = T_bottom − T_top\n\nDefine T̃ = (T − T_top) / ΔT ∈ [0, 1]. The dimensionless equations become:\n\nbeginaligned\npartial_t mathbfu + mathbfu cdot nabla mathbfu\n  = -nabla p + mathrmPr nabla^2 mathbfu + mathrmRacdotmathrmPr T hatmathbfz \npartial_t T + mathbfu cdot nabla T = nabla^2 T \nnabla cdot mathbfu = 0\nendaligned\n\nDimensionless parameters:\n\nPr = ν/κ — Prandtl number (the viscous coefficient in the momentum equation)\nRa = g α ΔT H³ / (ν κ) — Rayleigh number (buoyancy amplitude is Ra·Pr)\n\nThe defining feature of the thermal-diffusive time scale is that the temperature equation has unit diffusivity — one time unit equals one thermal diffusion time, so the onset of convection and Nusselt scaling read off naturally.\n\nBoundary conditions:\n\nT(z=0) = 1, T(z=Lz) = 0 (fixed hot/cold)\nu(z=0) = u(z=Lz) = 0 (no-slip)\n\nThis problem runs in serial only — the explicit advection terms need a Chebyshev derivative, which Tarang cannot evaluate on a distributed field. See Running the simulation below.","title":"Governing equations (thermal-diffusive non-dimensionalization)"},{"category":"section","location":"tutorials/ivp_2d_rbc/#First-order-reformulation","page":"2D Rayleigh-Benard","text":"For better spectral conditioning — especially at high resolution — we rewrite the second-order operators in first-order form using auxiliary gradient variables:\n\nbeginaligned\nnabla_T = nabla T + hatmathbfz mathrmLift(tau_T_1) \nnabla_u = nabla mathbfu + hatmathbfz mathrmLift(tau_u_1)\nendaligned\n\nand replace ∇²T with ∇·∇_T and ∇²u with ∇·∇_u. The equations then become:\n\nbeginaligned\nmathrmtr(nabla_u) + tau_p = 0 \npartial_t T - nabla cdot nabla_T + mathrmLift(tau_T_2) = -mathbfu cdot nabla T \npartial_t mathbfu - mathrmPr nabla cdot nabla_u + nabla p - mathrmRacdotmathrmPr T hatmathbfz + mathrmLift(tau_u_2) = -mathbfu cdot nabla mathbfu\nendaligned\n\nwith five tau fields (τ_p, τ_{T_1}, τ_{T_2}, τ_{u_1}, τ_{u_2}) supplying the extra degrees of freedom needed to enforce the five algebraic constraints (2 T BCs, 2 u BCs, 1 pressure gauge). See the Tau method page for why the first-order formulation is recommended.","title":"First-order reformulation"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Complete-implementation","page":"2D Rayleigh-Benard","text":"","title":"Complete implementation"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Setup-and-parameters","page":"2D Rayleigh-Benard","text":"using Tarang\nusing Printf\n\n# ─── Parameters ───────────────────────────────────────────────\nLx, Lz      = 4.0, 1.0\nNx, Nz      = 256, 64\nRayleigh    = 2e6\nPrandtl     = 1.0\ndealias     = 3/2\nstop_time   = 25.0    # thermal diffusion times\nmax_dt      = 0.001   # small to resolve viscous/thermal transport\n\n# Coefficients in the diffusive-time formulation:\n#   ∂t(T) - ∇²T = -u⋅∇T              (unit diffusivity)\n#   ∂t(u) - Pr ∇²u + ∇p - Ra·Pr T ẑ = -u⋅∇u\nnu   = Prandtl                  # viscous coefficient (= Pr in diffusive units)\nbuoy = Rayleigh * Prandtl       # buoyancy forcing Ra·Pr\n\n@root_only begin\n    println(\"2D Rayleigh-Benard Convection (thermal-diffusive scaling)\")\n    @printf(\"  Ra=%.2e, Pr=%.1f\\n\", Rayleigh, Prandtl)\n    @printf(\"  Coefficients: Pr=%.4f, Ra·Pr=%.2e\\n\", Prandtl, buoy)\n    @printf(\"  Domain: %.1f × %.1f, Resolution: %d × %d\\n\", Lx, Lz, Nx, Nz)\n    @printf(\"  Stop time: %.1f thermal diffusion times\\n\\n\", stop_time)\nend","title":"Setup and parameters"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Bases-and-domain","page":"2D Rayleigh-Benard","text":"coords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\n\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=dealias)\nzbasis = ChebyshevT(coords[\"z\"];  size=Nz, bounds=(0.0, Lz), dealias=dealias)","title":"Bases and domain"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Fields","page":"2D Rayleigh-Benard","text":"domain = Domain(dist, (xbasis, zbasis))\n\np = ScalarField(domain, \"p\")\nT = ScalarField(domain, \"T\")        # Dimensionless temperature in [0, 1]\nu = VectorField(domain, \"u\")","title":"Fields"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Tau-fields","page":"2D Rayleigh-Benard","text":"Five tau fields for five algebraic constraints. Each \"drops the coupled direction\" — they live on (xbasis,) only, not on both axes, because the lift injects them at a single Chebyshev mode.\n\ntau_p  = ScalarField(dist, \"tau_p\",  (),         Float64)\ntau_T1 = ScalarField(dist, \"tau_T1\", (xbasis,),  Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (xbasis,),  Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis,), Float64)\n\ntau_p is 0-D (no bases) — a gauge-fixing scalar at the DC Fourier mode only. Valid-mode filtering drops it from non-DC subproblems automatically.\ntau_T1, tau_T2 are scalars on (xbasis,) — 1 DOF per Fourier mode.\ntau_u1, tau_u2 are vectors on (xbasis,) — 1 DOF per component per Fourier mode (= 2 DOFs per mode in 2D).","title":"Tau fields"},{"category":"section","location":"tutorials/ivp_2d_rbc/#First-order-substitutions","page":"2D Rayleigh-Benard","text":"ex, ez = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A) = lift(A, lift_basis, -1)\n\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_T = grad(T) + ez * τ_lift(tau_T1)\n\nThe lift_basis = derivative_basis(zbasis) is the idiomatic convention (see the Tau method page for the reason). The closure τ_lift(A) injects the tau field at the last Chebyshev coefficient (-1 = last mode, wraparound-indexed).\n\ngrad_u and grad_T are the augmented gradients — they carry the tau corrections that will enforce the bottom-wall BCs on the gradient itself.","title":"First-order substitutions"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Problem-and-equations","page":"2D Rayleigh-Benard","text":"problem = IVP([p, T, u, tau_p, tau_T1, tau_T2, tau_u1, tau_u2])\n\nadd_parameters!(problem,\n    nu=nu, buoy=buoy, ez=ez,\n    grad_u=grad_u, grad_T=grad_T, τ_lift=τ_lift)\n\n# Continuity (first-order form)\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\n\n# Temperature: ∂t(T) - ∇²T = -u⋅∇T\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\n\n# Momentum: ∂t(u) - Pr∇²u + ∇p - Ra·Pr T ẑ = -u⋅∇u\nadd_equation!(problem,\n    \"∂t(u) - nu*div(grad_u) + ∇(p) - buoy*T*ez + τ_lift(tau_u2) = -u⋅∇(u)\")\n\nKey substitutions:\n\ntrace(grad_u) replaces div(u) in the continuity equation and implicitly carries the τ_{u_1} lift.\ndiv(grad_T) replaces ∇²T; div(grad_u) replaces ∇²u.\nτ_lift(tau_T2) and τ_lift(tau_u2) are the tau corrections for the evolution-equation side (top-wall BCs).","title":"Problem and equations"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Boundary-conditions","page":"2D Rayleigh-Benard","text":"add_bc!(problem, \"T(z=0) = 1\")        # hot bottom\nadd_bc!(problem, \"T(z=$Lz) = 0\")      # cold top\nadd_bc!(problem, \"u(z=0) = 0\")        # no-slip bottom\nadd_bc!(problem, \"u(z=$Lz) = 0\")      # no-slip top\nadd_bc!(problem, \"integ(p) = 0\")      # pressure gauge\n\nwarning: BC strings are parsed by Tarang, not by Julia\nThe string handed to add_bc! is parsed by Tarang's own expression parser, which only knows the problem's variables and its add_parameters! names — it cannot see Julia globals. Writing \"T(z=Lz) = 0\" does not fail loudly: the parser warns Unknown variable: Lz, builds the equation with zero output DOFs, and the run then dies with a DimensionMismatch from the (now non-square) subproblem solve. Either interpolate the Julia value into the string (\"T(z=$Lz) = 0\", which is what is done above and expands to T(z=1.0)), or write a literal (\"T(z=1) = 0\").\n\nBoundary conditions must always go through add_bc! — declaring one with add_equation! compiles, but the constraint is silently not enforced.\n\nThe tau DOFs and the BC rows balance: per Fourier mode there are six tau unknowns (tau_T1, tau_T2, and two components each of tau_u1, tau_u2) against six BC rows (T and u at both walls, counting velocity components), plus the global gauge pair tau_p ↔ integ(p). That balance is what makes the filtered subproblem matrix square.","title":"Boundary conditions"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Solver","page":"2D Rayleigh-Benard","text":"solver = InitialValueSolver(problem, RK222(); dt=max_dt)\n\n@root_only diagnose(solver)\n\ndiagnose(solver) prints a tree-style summary of the solver — domain, bases, state fields, equations, BC count, lazy RHS plan status. Useful for debugging setup problems.","title":"Solver"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Output-handler","page":"2D Rayleigh-Benard","text":"snapshots = add_file_handler(\"snapshots\", solver; sim_dt=0.1, max_writes=50)\nadd_task!(snapshots, T;               name=\"temperature\")\nadd_task!(snapshots, u.components[1]; name=\"ux\")\nadd_task!(snapshots, u.components[2]; name=\"uz\")\n\nPass the solver — that form registers the handler with the solver's evaluator, so run! processes it every step with no further wiring. The other method, add_file_handler(path, dist, vars), does not auto-register; a handler built that way is never written unless you also pass it as run!(solver; outputs=[snapshots], …).\n\nThe written file is NetCDF-4 with groups, so read tasks back out of the vars group rather than from the file root:\n\nf = \"snapshots/snapshots_s1/snapshots_s1.nc\"\nTarang.group_variable_names(f, \"vars\")        # [\"temperature\", \"ux\", \"uz\"]\nTarang.group_ncread(f, \"vars\", \"temperature\") # (write, x, z)\nTarang.group_ncread(f, \"time\", \"sim_time\")    # (write,)","title":"Output handler"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Initial-conditions","page":"2D Rayleigh-Benard","text":"Start from the conduction profile T(z) = 1 − z/Lz with a small random perturbation to trigger convection:\n\nx, z = local_grids(dist, xbasis, zbasis)\nfill_random!(T, \"g\"; seed=42, distribution=\"normal\", scale=1e-3)\nget_grid_data(T) .*= z' .* (Lz .- z')        # damp noise at walls\nget_grid_data(T) .+= 1.0 .- z' ./ Lz          # add linear conduction profile\nensure_layout!(T, :c)                          # pre-compute coefficients for timestepper","title":"Initial conditions"},{"category":"section","location":"tutorials/ivp_2d_rbc/#CFL-adaptive-time-stepping","page":"2D Rayleigh-Benard","text":"cfl = CFL(solver; initial_dt=max_dt, cadence=10, safety=0.5,\n          threshold=0.05, max_change=1.5, min_change=0.5, max_dt=max_dt)\nadd_velocity!(cfl, u)\n\nCFL is constructed from the solver (not the problem), and add_velocity! takes a VectorField. Building the controller is not enough — you must hand it to run! with cfl=cfl (below), otherwise solver.dt stays pinned at its constructor value for the whole run.","title":"CFL-adaptive time stepping"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Diagnostic-callback","page":"2D Rayleigh-Benard","text":"ensure_layout!(T, :g)\nT_data = get_grid_data(T)\n@root_only @printf(\"Initial T: max=%.4f, min=%.4f, mean=%.4f\\n\",\n                    maximum(T_data), minimum(T_data), sum(T_data)/length(T_data))\n@root_only @printf(\"  z at max|T|: %.4f\\n\", z[argmax(T_data)[2]])\nensure_layout!(T, :c)","title":"Diagnostic callback"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Main-loop","page":"2D Rayleigh-Benard","text":"@root_only println(\"Starting main loop\")\n\nrun!(solver;\n     stop_time=stop_time,\n     cfl=cfl,\n     log_interval=100,\n     callbacks=[\n         on_interval(1) do s\n             if s.iteration <= 5 || s.iteration % 10 == 0\n                 ensure_layout!(T, :g)\n                 max_T = global_max(dist, abs.(get_grid_data(T)))\n                 ensure_layout!(u.components[2], :g)\n                 max_uz = global_max(dist, abs.(get_grid_data(u.components[2])))\n                 @root_only @printf(\"  iter=%d, t=%.4e, dt=%.4e, max|T|=%.4f, max|uz|=%.4e\\n\",\n                                     s.iteration, s.sim_time, s.dt, max_T, max_uz)\n             end\n         end\n     ])\n\n@root_only println(\"Done!\")","title":"Main loop"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Running-the-simulation","page":"2D Rayleigh-Benard","text":"julia --project=. examples/ivp/rayleigh_benard_2d.jl","title":"Running the simulation"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Why-this-problem-is-serial-only","page":"2D Rayleigh-Benard","text":"Do not launch it under mpiexec. Two independent limits of the distributed Chebyshev support block it, and both are loud rather than silently wrong:\n\nThe Chebyshev axis cannot be decomposed. PencilArrays splits the trailing dimensions, and a Chebyshev DCT needs the whole axis local on each rank. Domain(dist, (xbasis, zbasis)) — the Fourier-first order this tutorial uses — therefore errors at construction under nprocs > 1:\nMPI mixed Fourier-Chebyshev: the decomposed (trailing) axis/axes [2] are non-Fourier … Reorder your bases so the Chebyshev axis comes BEFORE the Fourier axes\nA Chebyshev derivative cannot appear on the explicit side. Reordering to (zbasis, xbasis) fixes the decomposition, and the solver still builds — but the advection terms -u⋅∇(T) and -u⋅∇(u) expand to a ∂z on the RHS, and the first step dies:\nLazy RHS: cannot differentiate along the non-Fourier axis 1 (ChebyshevT) of a DISTRIBUTED field — this rank owns only … of the … coefficients on that axis, so the global differentiation matrix does not apply to its local slab … Move the term to the implicit (L) side of the equation, or run in serial.\nReordering also forces the tau fields onto no bases (() instead of (xbasis,)), because per-Fourier-mode taus require the Fourier axis to come first.\n\nWhat does run distributed on a Chebyshev–Fourier domain is a problem whose explicit RHS differentiates only along Fourier axes (all Chebyshev derivatives on the implicit L side) — see test/test_mpi_cheb_fourier_ivp_nonlinear.jl. Fully periodic problems parallelize without restriction; see the 3D Turbulence tutorial.\n\nExpected early output:\n\n2D Rayleigh-Benard Convection (thermal-diffusive scaling)\n  Ra=2.00e+06, Pr=1.0\n  Coefficients: Pr=1.0000, Ra·Pr=2.00e+06\n  Domain: 4.0 × 1.0, Resolution: 256 × 64\n\nInitial T: max=1.0000, min=0.0000, mean=0.5000\n  z at max|T|: 0.0000\nStarting main loop\n  iter=1, t=1.0000e-03, dt=1.0000e-03, max|T|=1.0000, ...\n  iter=2, t=2.0000e-03, dt=1.0000e-03, max|T|=1.0000, ...\n  ...\n\nmax|T| should stay at ≈ 1.0 throughout (pinned by the bottom-wall BC). If you see it decay to zero or stick at 2 + √2 ≈ 3.414, that's a BC-enforcement bug — see the Tau method troubleshooting section.","title":"Why this problem is serial-only"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Physical-interpretation","page":"2D Rayleigh-Benard","text":"","title":"Physical interpretation"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Flow-regimes","page":"2D Rayleigh-Benard","text":"Ra Regime Characteristics\n< 1708 Conduction No flow, pure linear conduction profile\n10³–10⁴ Steady rolls Stationary convection cells\n10⁵–10⁶ Transitional Time-dependent, irregular rolls\n> 10⁶ Turbulent Chaotic small-scale structures","title":"Flow regimes"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Nusselt-number","page":"2D Rayleigh-Benard","text":"The Nusselt number measures the ratio of total heat flux to the conductive heat flux:\n\nmathrmNu = fraclangle u_z T rangle_xz + langle -partial_z T rangle_xzlangle -partial_z T_mathrmcond rangle_xz\n\nNu = 1 → pure conduction (no convection)\nNu > 1 → enhanced heat transfer from convection\nAsymptotically, Nu ~ Ra^(1/3) in the turbulent regime\n\nYou can compute it online via a callback that integrates over the full domain.","title":"Nusselt number"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Visualization","page":"2D Rayleigh-Benard","text":"Plots.jl is not a Tarang dependency — add it to your own environment first.\n\nusing Plots\n\nensure_layout!(T, :g)\nT_grid = Array(get_grid_data(T))       # (Nx, Nz)\nheatmap(T_grid', xlabel=\"x\", ylabel=\"z\", aspect_ratio=:equal,\n        title=\"Temperature (Ra = $(Rayleigh))\")\nsavefig(\"temperature.png\")","title":"Visualization"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Parameter-studies","page":"2D Rayleigh-Benard","text":"","title":"Parameter studies"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Exploring-Ra","page":"2D Rayleigh-Benard","text":"Ra_values = [1e4, 1e5, 1e6, 2e6]\n\nfor Ra in Ra_values\n    nu   = Prandtl\n    buoy = Ra * Prandtl\n    # ... rebuild problem with new `buoy` parameter and run\nend","title":"Exploring Ra"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Aspect-ratio","page":"2D Rayleigh-Benard","text":"aspect_ratios = [2.0, 4.0, 8.0]\n\nfor ar in aspect_ratios\n    Lx = ar * Lz\n    xbasis = RealFourier(coords[\"x\"]; size=Int(64*ar), bounds=(0.0, Lx), dealias=3/2)\n    # ... rebuild problem and run\nend","title":"Aspect ratio"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Performance-notes","page":"2D Rayleigh-Benard","text":"","title":"Performance notes"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Resolution","page":"2D Rayleigh-Benard","text":"For Ra = 2×10⁶ in 2D:\n\nMinimum: 128 × 32 (under-resolved but stable)\nRecommended: 256 × 64\nHigh-fidelity: 512 × 128\n\nScaling: roughly Nx, Nz × 1.5 for each Ra × 10.","title":"Resolution"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Timestep-control","page":"2D Rayleigh-Benard","text":"# For high Ra, reduce the safety factor to stay stable\ncfl = CFL(solver; initial_dt=max_dt, safety=0.3, cadence=10, max_dt=max_dt)\n\n# Smooth dt evolution to avoid thrashing the LHS cache\ncfl = CFL(solver; initial_dt=max_dt, max_change=1.2, min_change=0.5, max_dt=max_dt)\n\nadd_velocity!(cfl, u)\n\nThe LHS solver cache in step_subproblem_rk! (sp.LHS_solvers) is keyed by the stage coefficient a_ii, and every entry is marked dirty whenever dt changes — so if dt changes every step, the sparse LU is refactored every step, which is wasteful. Keeping max_change modest, and cadence above 1, lets CFL ride out several steps at a fixed dt between re-factorizations.","title":"Timestep control"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Troubleshooting","page":"2D Rayleigh-Benard","text":"","title":"Troubleshooting"},{"category":"section","location":"tutorials/ivp_2d_rbc/#maxT-decays-to-0","page":"2D Rayleigh-Benard","text":"Symptom: T(z=0) = 1 BC isn't being enforced; temperature drifts toward zero.\n\nMost common cause: a missing or mis-declared tau field. Check that you have exactly one tau DOF per BC (counting vector components and gauge constraints). See the Tau method → Common Pitfalls section.","title":"max|T| decays to 0"},{"category":"section","location":"tutorials/ivp_2d_rbc/#maxT-sticks-at-2-2-3.414","page":"2D Rayleigh-Benard","text":"Symptom: RK222 is scaling the BC by 1/γ = 1/(1 − 1/√2) = 2 + √2.\n\nCause: the apply_bc_override! DAE path in step_subproblem_rk! isn't firing for this BC. Make sure the BC equation is classified into sp.bc_rows (it should be, for any eq_size < Nz algebraic equation). If you see this with a custom equation type, file an issue.","title":"max|T| sticks at 2 + √2 ≈ 3.414"},{"category":"section","location":"tutorials/ivp_2d_rbc/#NaN-in-the-solution","page":"2D Rayleigh-Benard","text":"Causes and fixes:\n\nTimestep too large → reduce cfl.safety to 0.2–0.3 (CFL is mutable, so you can set the field directly)\nInsufficient resolution → bump Nz up\nMissing dealiasing → dealias is a padding factor, so 3/2 is the 3/2 rule and any value ≤ 1 switches dealiasing off. It only acts on Fourier axes; the factor passed to zbasis is ignored.","title":"NaN in the solution"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Simulation-not-converging-to-a-steady-state","page":"2D Rayleigh-Benard","text":"Run longer — thermal diffusion time is τ_κ = H² / κ, and steady state takes many diffusion times\nCheck that initial perturbation is large enough to grow above roundoff\nVerify Ra > 1708 (the critical Rayleigh number for onset)","title":"Simulation not converging to a steady state"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Memory-issues","page":"2D Rayleigh-Benard","text":"Reduce Nx, Nz\nSwitch to CNAB2 or SBDF2 (smaller LU cache than multi-stage RK443)\n\nMore MPI ranks will not help here — this problem is serial-only, see Running the simulation.","title":"Memory issues"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Complete-script","page":"2D Rayleigh-Benard","text":"The full example is at examples/ivp/rayleigh_benard_2d.jl in the repository. Copy-paste either from this tutorial or from that file — they're kept in sync.","title":"Complete script"},{"category":"section","location":"tutorials/ivp_2d_rbc/#Next-steps","page":"2D Rayleigh-Benard","text":"3D Turbulence — extend to 3D with two Fourier axes\nBoundary Conditions — more BC patterns, including time/space-dependent\nEigenvalue Problems — linear stability around the conduction profile\nTau method — the full story on how BCs are enforced","title":"Next steps"},{"category":"section","location":"tutorials/ivp_2d_rbc/#References","page":"2D Rayleigh-Benard","text":"Chandrasekhar, S. (1961). Hydrodynamic and Hydromagnetic Stability. Oxford.\nTritton, D. J. (1988). Physical Fluid Dynamics. Oxford.\nBurns, K. J., Vasil, G. M., Oishi, J. S., Lecoanet, D., & Brown, B. P. (2020). \"Dedalus: A flexible framework for numerical simulations with spectral methods.\" Physical Review Research, 2, 023068.","title":"References"},{"category":"section","location":"pages/les_models/#Large-Eddy-Simulation-(LES)-Models","page":"LES Models","text":"This page provides a comprehensive introduction to Large Eddy Simulation (LES) and the subgrid-scale (SGS) closure models available in Tarang.jl.\n\nnote: Scope: these models are array-level utilities\nThe SGS models are array-level utilities. They consume grid-space velocity-gradient arrays and produce an eddy-viscosity array (and, for AMD, an eddy-diffusivity array). They are not automatically coupled into an IVP, and no solver, RHS builder, timestepper or problem parser reads them: you evaluate the gradients, call the model, and apply the resulting stress yourself. The examples below therefore show the SGS update in isolation — the momentum and scalar updates around it are code you write. In particular, there is no timestepper keyword that takes an effective-viscosity array; a variable-viscosity term has to be assembled and applied explicitly.\n\n","title":"Large Eddy Simulation (LES) Models"},{"category":"section","location":"pages/les_models/#What-is-Large-Eddy-Simulation?","page":"LES Models","text":"","title":"What is Large Eddy Simulation?"},{"category":"section","location":"pages/les_models/#The-Turbulence-Challenge","page":"LES Models","text":"Turbulent flows contain a vast range of length scales, from the largest energy-containing eddies down to the smallest dissipative scales (Kolmogorov scales). In a Direct Numerical Simulation (DNS), we resolve all these scales, which requires:\n\nN sim Re^94\n\ngrid points in 3D, where Re is the Reynolds number. For atmospheric or oceanic flows with Re sim 10^9, this is computationally impossible.","title":"The Turbulence Challenge"},{"category":"section","location":"pages/les_models/#The-LES-Approach","page":"LES Models","text":"Large Eddy Simulation offers a practical alternative:\n\nResolve the large, energy-containing eddies directly\nModel the effect of small, unresolved eddies\n\n┌─────────────────────────────────────────────────────────────┐\n│                    Energy Spectrum E(k)                      │\n│                                                              │\n│    E(k)                                                      │\n│     │                                                        │\n│     │   ╭──╮                                                 │\n│     │  ╱    ╲          k^(-5/3)                              │\n│     │ ╱      ╲___                                            │\n│     │╱            ╲___                                       │\n│     │                  ╲___                                  │\n│     │                       ╲___                             │\n│     └──────────────────────────────────────────────► k       │\n│         │                    │                               │\n│         │◄── Resolved ──────►│◄── Modeled (SGS) ──►│        │\n│         │   (large eddies)   │   (small eddies)    │        │\n│                              │                               │\n│                          filter cutoff (Δ)                   │\n└─────────────────────────────────────────────────────────────┘\n\nThe filter width Delta (typically the grid spacing) separates resolved from unresolved scales.","title":"The LES Approach"},{"category":"section","location":"pages/les_models/#Filtering-the-Navier-Stokes-Equations","page":"LES Models","text":"We apply a spatial filter to decompose velocity into resolved (baru) and subgrid (u) parts:\n\nu = baru + u\n\nFiltering the incompressible Navier-Stokes equations gives:\n\nfracpartial baru_ipartial t + baru_j fracpartial baru_ipartial x_j = -frac1rhofracpartial barppartial x_i + nu nabla^2 baru_i - fracpartial tau_ijpartial x_j\n\nwhere the subgrid-scale (SGS) stress tensor appears:\n\ntau_ij = overlineu_i u_j - baru_i baru_j\n\nThis tensor represents the effect of unresolved turbulent motions on the resolved flow. Since we cannot compute tau_ij directly (it involves unresolved velocities), we must model it.\n\n","title":"Filtering the Navier-Stokes Equations"},{"category":"section","location":"pages/les_models/#The-Closure-Problem","page":"LES Models","text":"","title":"The Closure Problem"},{"category":"section","location":"pages/les_models/#Why-We-Need-Models","page":"LES Models","text":"The SGS stress tau_ij contains information about scales we don't resolve. This creates the closure problem: our filtered equations have more unknowns than equations.","title":"Why We Need Models"},{"category":"section","location":"pages/les_models/#The-Eddy-Viscosity-Hypothesis","page":"LES Models","text":"Most SGS models use the Boussinesq hypothesis, which assumes the SGS stress is proportional to the resolved strain rate:\n\ntau_ij - frac13tau_kkdelta_ij = -2 nu_e barS_ij\n\nwhere:\n\nnu_e\nis the eddy viscosity (to be modeled)\nbarS_ij\nis the resolved strain rate tensor:\n\nbarS_ij = frac12left(fracpartial baru_ipartial x_j + fracpartial baru_jpartial x_iright)\n\nThis transforms the filtered momentum equation into:\n\nfracpartial baru_ipartial t + baru_j fracpartial baru_ipartial x_j = -frac1rhofracpartial barp^*partial x_i + (nu + nu_e) nabla^2 baru_i\n\nwhere we've absorbed the isotropic part into a modified pressure barp^*.\n\nKey insight: The SGS model effectively adds a spatially-varying viscosity nu_e(xt) to the molecular viscosity nu.\n\n","title":"The Eddy Viscosity Hypothesis"},{"category":"section","location":"pages/les_models/#Energy-Cascade-and-Dissipation","page":"LES Models","text":"","title":"Energy Cascade and Dissipation"},{"category":"section","location":"pages/les_models/#Forward-Energy-Cascade","page":"LES Models","text":"In 3D turbulence, energy flows from large scales to small scales (forward cascade):\n\nLarge eddies ──► Medium eddies ──► Small eddies ──► Dissipation\n  (production)                                        (ε = 2νSᵢⱼSᵢⱼ)\n\nThe SGS model must drain energy from resolved scales at the correct rate to maintain physical behavior.","title":"Forward Energy Cascade"},{"category":"section","location":"pages/les_models/#SGS-Dissipation","page":"LES Models","text":"The rate at which energy is transferred from resolved to unresolved scales is:\n\nvarepsilon_sgs = -tau_ij barS_ij = 2nu_e barS_ijbarS_ij = nu_e barS^2\n\nwhere barS = sqrt2barS_ijbarS_ij is the strain rate magnitude.\n\nThere is no extra factor of 2 in the final expression: the sqrt2 in the definition of barS already carries it. This is what sgs_dissipation returns — writing 2nu_ebarS^2 would double-count the dissipation.\n\nA good SGS model ensures varepsilon_sgs matches the actual energy transfer rate.\n\n","title":"SGS Dissipation"},{"category":"section","location":"pages/les_models/#Available-Models-in-Tarang.jl","page":"LES Models","text":"Tarang.jl provides two eddy viscosity models:\n\nModel Year Key Feature Best For\nSmagorinsky 1963 Simple, robust Isotropic grids, fully turbulent flows\nAMD 2015 Minimum dissipation, anisotropic Transitional flows, stretched grids\n\n","title":"Available Models in Tarang.jl"},{"category":"section","location":"pages/les_models/#Smagorinsky-Model","page":"LES Models","text":"","title":"Smagorinsky Model"},{"category":"section","location":"pages/les_models/#Physical-Basis","page":"LES Models","text":"Joseph Smagorinsky (1963) proposed the first and most widely-used SGS model. It's based on dimensional analysis and mixing length theory.\n\nKey assumptions:\n\nThe subgrid scales are in equilibrium (production = dissipation)\nThe characteristic length scale is proportional to the filter width Delta\nThe characteristic velocity scale is Delta barS","title":"Physical Basis"},{"category":"section","location":"pages/les_models/#Mathematical-Formulation","page":"LES Models","text":"The eddy viscosity is computed as:\n\nnu_e = (C_s Delta)^2 barS\n\nwhere:\n\nC_s approx 017\nis the Smagorinsky constant\nDelta = (Delta_x Delta_y Delta_z)^13\nis the effective filter width\nbarS = sqrt2 barS_ij barS_ij\nis the strain rate magnitude\n\nExpanded form for 3D:\n\nbarS = sqrt2leftleft(fracpartial barupartial xright)^2 + left(fracpartial barvpartial yright)^2 + left(fracpartial barwpartial zright)^2right + left(fracpartial barupartial y + fracpartial barvpartial xright)^2 + left(fracpartial barupartial z + fracpartial barwpartial xright)^2 + left(fracpartial barvpartial z + fracpartial barwpartial yright)^2","title":"Mathematical Formulation"},{"category":"section","location":"pages/les_models/#Usage-in-Tarang.jl","page":"LES Models","text":"using Tarang\n\n# Grid parameters\nN = 128                     # Grid points per direction\nL = 2π                      # Domain size\nΔ = L / N                   # Grid spacing\n\n# Create the Smagorinsky model\nsgs_model = SmagorinskyModel(\n    C_s = 0.17,                    # Smagorinsky constant\n    filter_width = (Δ, Δ, Δ),      # Grid spacing in each direction\n    field_size = (N, N, N)         # Number of grid points\n)\n\n# Compute eddy viscosity from velocity gradients\n# You need all 9 components of the velocity gradient tensor\ncompute_eddy_viscosity!(sgs_model,\n    ∂u∂x, ∂u∂y, ∂u∂z,    # Gradients of u\n    ∂v∂x, ∂v∂y, ∂v∂z,    # Gradients of v\n    ∂w∂x, ∂w∂y, ∂w∂z     # Gradients of w\n)\n\n# Retrieve the computed eddy viscosity field\nνₑ = get_eddy_viscosity(sgs_model)","title":"Usage in Tarang.jl"},{"category":"section","location":"pages/les_models/#Choosing-the-Smagorinsky-Constant","page":"LES Models","text":"The constant C_s is not universal and depends on the flow:\n\nFlow Type Recommended C_s Notes\nIsotropic turbulence 0.17 - 0.20 Theoretical value from Lilly (1967)\nChannel flow 0.10 - 0.12 Reduced due to wall effects\nMixing layers 0.10 - 0.14 Transitional regions need lower values\nFree shear flows 0.10 - 0.12 Similar to channel flow\nAtmospheric boundary layer 0.10 - 0.15 Depends on stability\n\nRule of thumb: Start with C_s = 017 and reduce if you observe over-dissipation.","title":"Choosing the Smagorinsky Constant"},{"category":"section","location":"pages/les_models/#Limitations","page":"LES Models","text":"Over-dissipation: The model is always \"on\", even in laminar regions\nNo backscatter: Cannot represent energy transfer from small to large scales\nWall behavior: Needs damping functions near solid walls\nIsotropic assumption: Assumes same behavior in all directions\n\n","title":"Limitations"},{"category":"section","location":"pages/les_models/#Anisotropic-Minimum-Dissipation-(AMD)-Model","page":"LES Models","text":"","title":"Anisotropic Minimum Dissipation (AMD) Model"},{"category":"section","location":"pages/les_models/#Motivation","page":"LES Models","text":"The AMD model (Rozema et al., 2015) addresses key limitations of Smagorinsky:\n\nAutomatic switch-off: nu_e = 0 in laminar/transitional regions\nAnisotropic grids: Properly handles Delta_x neq Delta_y neq Delta_z\nMinimum dissipation: Adds only the dissipation needed for stability","title":"Motivation"},{"category":"section","location":"pages/les_models/#Mathematical-Formulation-2","page":"LES Models","text":"The AMD model computes eddy viscosity as:\n\nnu_e = maxleft(0 -C fracDelta_k^2 fracpartial baru_ipartial x_k fracpartial baru_jpartial x_k barS_ijfracpartial baru_mpartial x_n fracpartial baru_mpartial x_nright)\n\nwith summation over i, j and k. The filter width Delta_k is indexed by the derivative direction k, not by the velocity-component index — that is what makes the model anisotropy-aware, and it is the term most easily got wrong. Tying Delta to the component index instead (e.g. hatdelta_ij = Delta_i delta_ij) leaves only the diagonal strain contributing, and gives a different sign as well as a different magnitude.\n\nSimplified form:\n\nnu_e = maxleft(0 -C fractextnumeratortextdenominatorright)\n\nNumerator: Measures alignment between velocity gradients and strain rate\nDenominator: Total velocity gradient magnitude squared\nmax(0, ...): Ensures non-negative eddy viscosity","title":"Mathematical Formulation"},{"category":"section","location":"pages/les_models/#Physical-Interpretation","page":"LES Models","text":"The numerator can be negative (giving nu_e  0) when:\n\nVelocity gradients are aligned with the strain rate\nEnergy is being transferred to smaller scales\n\nThe numerator is positive (giving nu_e = 0) when:\n\nFlow is laminar or transitional\nNo SGS dissipation is needed","title":"Physical Interpretation"},{"category":"section","location":"pages/les_models/#Usage-in-Tarang.jl-2","page":"LES Models","text":"# Create the AMD model\nsgs_model = AMDModel(\n    C = 1/12,                      # Poincaré constant (for spectral methods)\n    filter_width = (Δx, Δy, Δz),   # Can be different in each direction\n    field_size = (Nx, Ny, Nz),\n    clip_negative = true           # Ensure νₑ ≥ 0 (recommended)\n)\n\n# Compute eddy viscosity (same interface as Smagorinsky)\ncompute_eddy_viscosity!(sgs_model,\n    ∂u∂x, ∂u∂y, ∂u∂z,\n    ∂v∂x, ∂v∂y, ∂v∂z,\n    ∂w∂x, ∂w∂y, ∂w∂z\n)\n\nνₑ = get_eddy_viscosity(sgs_model)","title":"Usage in Tarang.jl"},{"category":"section","location":"pages/les_models/#Choosing-the-AMD-Constant","page":"LES Models","text":"The constant C depends on the numerical discretization:\n\nDiscretization Recommended C Notes\nSpectral methods 1/12 ≈ 0.0833 Theoretical value\n4th-order finite difference 0.212 From Verstappen (2011)\n2nd-order finite difference 0.3 Higher due to numerical diffusion","title":"Choosing the AMD Constant"},{"category":"section","location":"pages/les_models/#Scalar-Transport-(Buoyancy)","page":"LES Models","text":"For buoyancy-driven flows, AMD also provides eddy diffusivity for scalar transport:\n\nkappa_e = maxleft(0 -C fracDelta_k^2 fracpartial baru_ipartial x_k fracpartial barthetapartial x_k fracpartial barthetapartial x_ifracpartial barthetapartial x_n fracpartial barthetapartial x_nright)\n\nThe inner sum runs over every velocity component u_i, so the method needs the complete velocity-gradient tensor (9 components in 3D, 4 in 2D) before the scalar gradients — not the gradient of a single velocity component:\n\n# Compute eddy diffusivity for buoyancy/temperature\ncompute_eddy_diffusivity!(sgs_model,\n    ∂u∂x, ∂u∂y, ∂u∂z,    # Gradients of u\n    ∂v∂x, ∂v∂y, ∂v∂z,    # Gradients of v\n    ∂w∂x, ∂w∂y, ∂w∂z,    # Gradients of w\n    ∂b∂x, ∂b∂y, ∂b∂z     # Buoyancy/scalar gradients\n)\n\nκₑ = get_eddy_diffusivity(sgs_model)\n\n","title":"Scalar Transport (Buoyancy)"},{"category":"section","location":"pages/les_models/#Choosing-Between-Models","page":"LES Models","text":"","title":"Choosing Between Models"},{"category":"section","location":"pages/les_models/#Decision-Flowchart","page":"LES Models","text":"Start\n  │\n  ▼\nIs your grid anisotropic (Δx ≠ Δy ≠ Δz)?\n  │\n  ├── Yes ──► Use AMD (handles anisotropy naturally)\n  │\n  └── No\n       │\n       ▼\nDoes your flow have laminar or transitional regions?\n       │\n       ├── Yes ──► Use AMD (automatically switches off)\n       │\n       └── No\n            │\n            ▼\nIs computational cost a primary concern?\n            │\n            ├── Yes ──► Use Smagorinsky (slightly cheaper)\n            │\n            └── No ──► Either works; AMD is generally more accurate","title":"Decision Flowchart"},{"category":"section","location":"pages/les_models/#Summary-Comparison","page":"LES Models","text":"Aspect Smagorinsky AMD\nComplexity Simple Moderate\nCost per timestep Low Slightly higher\nLaminar regions Over-dissipates Correctly gives νₑ = 0\nAnisotropic grids Needs modification Native support\nNear walls Needs damping Better behavior\nTuning required Often yes Usually not\nScalar transport Use Pr_t Built-in κₑ\n\n","title":"Summary Comparison"},{"category":"section","location":"pages/les_models/#Complete-Example:-LES-of-Decaying-Turbulence","page":"LES Models","text":"This example demonstrates the SGS side of an LES of decaying homogeneous isotropic turbulence: building the model, evaluating the velocity gradients, and refreshing νₑ each step. Advancing the momentum equation is not shown, because Tarang does not do it for you — see the scope note at the top of this page.\n\nusing Tarang\nusing Statistics\n\n# ============================================================\n# 1. Physical and Numerical Parameters\n# ============================================================\n\nN = 128                     # Grid points per direction\nL = 2π                      # Domain size [m]\nΔ = L / N                   # Grid spacing [m]\nν = 1e-4                    # Molecular (kinematic) viscosity [m²/s]\ndt = 0.001                  # Time step [s]\nnsteps = 1000               # Number of time steps\n\n# ============================================================\n# 2. Create the SGS Model\n# ============================================================\n\n# Option A: Smagorinsky (simple, robust)\nsgs = SmagorinskyModel(\n    C_s = 0.17,\n    filter_width = (Δ, Δ, Δ),\n    field_size = (N, N, N),\n    architecture = CPU()       # GPU() keeps νₑ on the device\n)\n\n# Option B: AMD (recommended for most applications)\n# sgs = AMDModel(\n#     C = 1/12,\n#     filter_width = (Δ, Δ, Δ),\n#     field_size = (N, N, N)\n# )\n\n# ============================================================\n# 3. Setup Computational Domain\n# ============================================================\n\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist = Distributor(coords; mesh=(1, 1, 1))  # Single processor\n\n# Fourier bases for periodic domain\nxbasis = RealFourier(coords[\"x\"]; size=N, bounds=(0.0, L))\nybasis = RealFourier(coords[\"y\"]; size=N, bounds=(0.0, L))\nzbasis = RealFourier(coords[\"z\"]; size=N, bounds=(0.0, L))\n\ndomain = Domain(dist, (xbasis, ybasis, zbasis))\n\n# Create velocity field\nu = VectorField(dist, coords, \"u\", (xbasis, ybasis, zbasis))\n\n# ============================================================\n# 4. Initialize with a Turbulent Velocity Field\n# ============================================================\n\n# A Taylor-Green vortex keeps this example self-contained; substitute your\n# own IC (e.g. random phases with a prescribed energy spectrum).\nx, y, z = local_grids(dist, xbasis, ybasis, zbasis)\nensure_layout!(u, :g)\nget_grid_data(u.components[1]) .=  sin.(x) .* cos.(y') .* cos.(reshape(z, 1, 1, :))\nget_grid_data(u.components[2]) .= .-cos.(x) .* sin.(y') .* cos.(reshape(z, 1, 1, :))\nget_grid_data(u.components[3]) .= 0.0\nensure_layout!(u, :c)\n\n# ============================================================\n# 5. Helper Function: Compute All Velocity Gradients\n# ============================================================\n\nfunction compute_velocity_gradients(u)\n    # grad(u) is a lazy Gradient — it is not itself iterable, so it cannot be\n    # destructured directly. evaluate() turns it into a TensorField whose\n    # components are already in the component-major order the models expect:\n    # (∂u∂x, ∂u∂y, ∂u∂z, ∂v∂x, ∂v∂y, ∂v∂z, ∂w∂x, ∂w∂y, ∂w∂z).\n    G = evaluate(grad(u))\n    return Tuple(get_grid_data(g) for g in G.components)\nend\n\n# ============================================================\n# 6. Per-Step SGS Update\n# ============================================================\n#\n# Nothing in the solver stack calls the SGS model for you. What follows is the\n# SGS half of a timestep; advancing ū is code you supply.\n\nfor step in 1:nsteps\n    # --- Step 1: Compute velocity gradients ---\n    grads = compute_velocity_gradients(u)\n\n    # --- Step 2: Update SGS eddy viscosity ---\n    compute_eddy_viscosity!(sgs, grads...)\n\n    # --- Step 3: Get effective viscosity ---\n    νₑ = get_eddy_viscosity(sgs)\n    ν_eff = ν .+ νₑ  # Total viscosity = molecular + SGS, one value per grid point\n\n    # --- Step 4: Advance the momentum equation (your code) ---\n    # The filtered Navier-Stokes with SGS model:\n    #   ∂ū/∂t + (ū·∇)ū = -∇p̄/ρ + ∇·((ν + νₑ)∇ū)\n    #\n    # ν_eff is a plain array and no timestepper keyword accepts it, so the\n    # variable-viscosity term must be assembled and applied explicitly.\n\n    # --- Step 5: Diagnostics ---\n    if step % 100 == 0\n        mean_νₑ = mean_eddy_viscosity(sgs)\n        max_νₑ = max_eddy_viscosity(sgs)\n\n        println(\"Step $step:\")\n        println(\"  Mean eddy viscosity: $(mean_νₑ)\")\n        println(\"  Max eddy viscosity:  $(max_νₑ)\")\n        println(\"  Ratio νₑ/ν (mean):   $(mean_νₑ/ν)\")\n    end\nend\n\n","title":"Complete Example: LES of Decaying Turbulence"},{"category":"section","location":"pages/les_models/#Complete-Example:-LES-of-Rayleigh-Bénard-Convection","page":"LES Models","text":"For buoyancy-driven turbulence we need both momentum and scalar closures. As above, only the SGS update is shown — the momentum and temperature updates are yours to write.\n\nusing Tarang\n\n# ============================================================\n# Physical Parameters\n# ============================================================\n\nNx, Nz = 256, 128           # Grid resolution\nLx, Lz = 4.0, 1.0           # Domain size\n\nRa = 1e8                    # Rayleigh number\nPr = 1.0                    # Prandtl number\nν = sqrt(Pr / Ra)           # Kinematic viscosity\nκ = ν / Pr                  # Thermal diffusivity\nnsteps = 1000               # Number of time steps\n\n# ============================================================\n# Domain and Fields\n# ============================================================\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx))  # periodic\nzbasis = ChebyshevT(coords[\"z\"];  size=Nz, bounds=(0.0, Lz))  # wall-bounded\ndomain = Domain(dist, (xbasis, zbasis))\n\nu = Field(dist; name=\"u\", bases=(xbasis, zbasis))   # horizontal velocity\nw = Field(dist; name=\"w\", bases=(xbasis, zbasis))   # vertical velocity\nT = Field(dist; name=\"T\", bases=(xbasis, zbasis))   # temperature\n\n# (Initialise u, w and T with your own IC and boundary conditions.)\n\n# ============================================================\n# Create AMD Model (recommended for RBC)\n# ============================================================\n\n# One filter width per axis. `grid_spacing` reports the *smallest* spacing on\n# each axis, which is the conservative choice on the Chebyshev-clustered z axis.\nΔx, Δz = grid_spacing(domain)\n\nsgs = AMDModel(\n    C = 1/12,\n    filter_width = (Δx, Δz),\n    field_size = (Nx, Nz)\n)\n\n# ============================================================\n# Per-Step SGS Update\n# ============================================================\n\nfor step in 1:nsteps\n    # Velocity gradients. grad() is lazy and not iterable; evaluate() realises\n    # it, and for a scalar field the result has one component per axis.\n    ∂u∂x, ∂u∂z = (get_grid_data(g) for g in evaluate(grad(u)).components)\n    ∂w∂x, ∂w∂z = (get_grid_data(g) for g in evaluate(grad(w)).components)\n\n    # Temperature gradients\n    ∂T∂x, ∂T∂z = (get_grid_data(g) for g in evaluate(grad(T)).components)\n\n    # --- Momentum closure ---\n    # 2D needs the complete 2×2 velocity-gradient tensor, component-major.\n    compute_eddy_viscosity!(sgs, ∂u∂x, ∂u∂z, ∂w∂x, ∂w∂z)\n    νₑ = get_eddy_viscosity(sgs)\n    ν_eff = ν .+ νₑ\n\n    # --- Scalar (temperature) closure ---\n    # The same velocity-gradient tensor first, then the scalar gradients.\n    compute_eddy_diffusivity!(sgs, ∂u∂x, ∂u∂z, ∂w∂x, ∂w∂z, ∂T∂x, ∂T∂z)\n    κₑ = get_eddy_diffusivity(sgs)\n    κ_eff = κ .+ κₑ\n\n    # ν_eff and κ_eff are plain arrays. Applying them to the momentum and\n    # temperature equations is your code — see the scope note at the top.\nend\n\n","title":"Complete Example: LES of Rayleigh-Bénard Convection"},{"category":"section","location":"pages/les_models/#Diagnostics-and-Analysis","page":"LES Models","text":"","title":"Diagnostics and Analysis"},{"category":"section","location":"pages/les_models/#SGS-Energy-Dissipation","page":"LES Models","text":"The SGS dissipation rate tells you how much energy is being drained by the model:\n\n# Get strain magnitude (computed during eddy viscosity calculation)\nS_mag = sgs.strain_magnitude  # Only for Smagorinsky\n\n# Or compute it yourself — note the broadcasting dots (sqrt is not vectorised)\nS_mag = sqrt.(2 .* (S11.^2 .+ S22.^2 .+ S33.^2 .+ 2 .* S12.^2 .+ 2 .* S13.^2 .+ 2 .* S23.^2))\n\n# SGS dissipation field: ε_sgs = νₑ |S̄|²  (no extra factor of 2 — |S̄| carries it)\nε_sgs = sgs_dissipation(sgs, S_mag)\n\n# Domain-averaged SGS dissipation\nε_sgs_mean = mean_sgs_dissipation(sgs, S_mag)","title":"SGS Energy Dissipation"},{"category":"section","location":"pages/les_models/#Monitoring-Model-Behavior","page":"LES Models","text":"# Check ratio of SGS to molecular viscosity\nνₑ_mean = mean_eddy_viscosity(sgs)\nprintln(\"νₑ/ν = $(νₑ_mean/ν)\")\n\n# If νₑ/ν >> 1: SGS model is dominant (typical for high Re)\n# If νₑ/ν << 1: Flow is nearly DNS-resolved\n# If νₑ/ν ~ 1: Well-resolved LES\n\n# For AMD: check how often νₑ = 0\nνₑ = get_eddy_viscosity(sgs)\nfraction_zero = sum(νₑ .== 0) / length(νₑ)\nprintln(\"Fraction with νₑ = 0: $(fraction_zero)\")\n# High fraction → flow is largely laminar/transitional\n\n","title":"Monitoring Model Behavior"},{"category":"section","location":"pages/les_models/#Tips-for-Successful-LES","page":"LES Models","text":"","title":"Tips for Successful LES"},{"category":"section","location":"pages/les_models/#Resolution-Requirements","page":"LES Models","text":"LES still requires adequate resolution:\n\n┌────────────────────────────────────────────────────────────┐\n│  Resolution Quality for LES                                 │\n├────────────────────────────────────────────────────────────┤\n│  80% of TKE resolved      →  Minimum acceptable LES        │\n│  90% of TKE resolved      →  Good quality LES              │\n│  95%+ of TKE resolved     →  Nearly DNS quality            │\n└────────────────────────────────────────────────────────────┘\n\nRule of thumb: Grid spacing should resolve the inertial range, typically Delta lesssim L_integral10.","title":"Resolution Requirements"},{"category":"section","location":"pages/les_models/#Common-Pitfalls","page":"LES Models","text":"Symptom Likely Cause Solution\nSimulation blows up νₑ too small Increase C_s or C\nFlow looks too smooth Over-dissipation Reduce C_s, or use AMD\nCheckerboard patterns Aliasing errors Enable dealiasing (2/3 rule)\nνₑ unrealistically large Poor resolution Refine grid\nAMD gives νₑ = 0 everywhere Flow is laminar This is correct!","title":"Common Pitfalls"},{"category":"section","location":"pages/les_models/#When-LES-May-Not-Be-Appropriate","page":"LES Models","text":"Very low Reynolds numbers: DNS may be feasible\nStrongly anisotropic turbulence: May need special treatment\nFlows with strong backscatter: Standard models don't capture this\nNear-wall regions: May need wall models or finer grids\n\n","title":"When LES May Not Be Appropriate"},{"category":"section","location":"pages/les_models/#2D-Flows","page":"LES Models","text":"Both models support 2D simulations:\n\n# 2D Smagorinsky\nsgs_2d = SmagorinskyModel(\n    C_s = 0.17,\n    filter_width = (Δx, Δy),\n    field_size = (Nx, Ny)\n)\n\n# 2D velocity gradients (4 components)\ncompute_eddy_viscosity!(sgs_2d, ∂u∂x, ∂u∂y, ∂v∂x, ∂v∂y)\n\nNote on 2D turbulence: 2D turbulence has an inverse energy cascade (energy flows to large scales), which is fundamentally different from 3D. Standard SGS models may not be appropriate for 2D flows.\n\n","title":"2D Flows"},{"category":"section","location":"pages/les_models/#Choosing-an-Architecture-(CPU-/-GPU)","page":"LES Models","text":"Both models take an architecture keyword, defaulting to CPU(). It decides where the model's internal buffers (eddy_viscosity, strain_magnitude, and for AMD eddy_diffusivity) are allocated: plain Arrays on CPU(), CuArrays on GPU().\n\n# CPU (the default)\nsgs_cpu = SmagorinskyModel(\n    C_s = 0.17,\n    filter_width = (Δ, Δ, Δ),\n    field_size = (N, N, N),\n    architecture = CPU()\n)\n\n# AMD takes the same keyword\namd_cpu = AMDModel(\n    C = 1/12,\n    filter_width = (Δx, Δy, Δz),\n    field_size = (Nx, Ny, Nz),\n    architecture = CPU()\n)\n\nGPU() requires CUDA.jl to be loaded first, and a CUDA device to be present — it throws otherwise:\n\nusing CUDA        # must be loaded before GPU() can be constructed\nusing Tarang\n\nsgs_gpu = SmagorinskyModel(\n    C_s = 0.17,\n    filter_width = (Δ, Δ, Δ),\n    field_size = (N, N, N),\n    architecture = GPU()\n)\n\ncompute_eddy_viscosity! and compute_eddy_diffusivity! dispatch on the model's architecture: broadcast kernels on the GPU, SIMD loops on the CPU. Gradient arrays that are not already on the model's architecture are copied there on every call, so a CPU array handed to a GPU() model works but pays a host-to-device transfer each step — keep the gradients on the same device as the model. get_eddy_viscosity returns an array of the model's own kind (Array on CPU(), CuArray on GPU()).\n\n","title":"Choosing an Architecture (CPU / GPU)"},{"category":"section","location":"pages/les_models/#API-Reference","page":"LES Models","text":"","title":"API Reference"},{"category":"section","location":"pages/les_models/#Constructors","page":"LES Models","text":"SmagorinskyModel(;\n    C_s = 0.17,                    # Smagorinsky constant\n    filter_width::NTuple{N, Real}, # (Δx, Δy) or (Δx, Δy, Δz)\n    field_size::NTuple{N, Int},    # (Nx, Ny) or (Nx, Ny, Nz)\n    dtype = Float64,               # Precision\n    architecture = CPU()           # CPU() or GPU()\n)\n\nAMDModel(;\n    C = 1/12,                      # Poincaré constant\n    filter_width::NTuple{N, Real}, # Can be anisotropic\n    field_size::NTuple{N, Int},\n    clip_negative = true,          # Ensure νₑ ≥ 0\n    dtype = Float64,               # Precision\n    architecture = CPU()           # CPU() or GPU()\n)","title":"Constructors"},{"category":"section","location":"pages/les_models/#Core-Functions","page":"LES Models","text":"Function Description\ncompute_eddy_viscosity!(model, grads...) Compute νₑ from velocity gradients\ncompute_eddy_diffusivity!(model, grads...) Compute κₑ for scalars (AMD only)\nget_eddy_viscosity(model) Return the νₑ field\nget_eddy_diffusivity(model) Return the κₑ field (AMD)","title":"Core Functions"},{"category":"section","location":"pages/les_models/#Analysis-Functions","page":"LES Models","text":"Function Description\nmean_eddy_viscosity(model) Domain-averaged νₑ\nmax_eddy_viscosity(model) Maximum νₑ\nsgs_dissipation(model, S_mag) SGS dissipation field\nmean_sgs_dissipation(model, S_mag) Domain-averaged dissipation","title":"Analysis Functions"},{"category":"section","location":"pages/les_models/#Utility-Functions","page":"LES Models","text":"Function Description\nset_constant!(model, C) Update model constant\nreset!(model) Reset νₑ (and κₑ) to zero\nget_filter_width(model) Return filter width tuple\ncompute_sgs_stress(model, S...) Compute the deviatoric SGS stress tensor τᵢⱼ = -2 νₑ S̄ᵢⱼ\n\n","title":"Utility Functions"},{"category":"section","location":"pages/les_models/#Troubleshooting","page":"LES Models","text":"Problem Possible Cause Solution\nSimulation becomes unstable νₑ too small Increase C_s or C; check CFL condition\nFlow appears over-damped νₑ too large Reduce C_s; consider AMD model\nSpurious oscillations Aliasing Enable 2/3 dealiasing rule\nAMD gives νₑ = 0 everywhere Flow is laminar Correct behavior; model switches off\nUnphysical behavior near walls Poor wall resolution Refine near-wall grid; use wall functions\n\n","title":"Troubleshooting"},{"category":"section","location":"pages/les_models/#References","page":"LES Models","text":"","title":"References"},{"category":"section","location":"pages/les_models/#Original-Papers","page":"LES Models","text":"Smagorinsky, J. (1963). \"General circulation experiments with the primitive equations: I. The basic experiment.\" Monthly Weather Review, 91(3), 99-164.\nRozema, W., Bae, H. J., Moin, P., & Verstappen, R. (2015). \"Minimum-dissipation models for large-eddy simulation.\" Physics of Fluids, 27(8), 085107. PDF\nAbkar, M., Bae, H. J., & Moin, P. (2016). \"Minimum-dissipation scalar transport model for large-eddy simulation of turbulent flows.\" Physical Review Fluids, 1(4), 041701. Link","title":"Original Papers"},{"category":"section","location":"pages/les_models/#Textbooks-and-Reviews","page":"LES Models","text":"Pope, S. B. (2000). Turbulent Flows. Cambridge University Press. (Chapter 13: Large-Eddy Simulation)\nSagaut, P. (2006). Large Eddy Simulation for Incompressible Flows. Springer.","title":"Textbooks and Reviews"},{"category":"section","location":"pages/les_models/#Related-Documentation","page":"LES Models","text":"dedaLES AMD Documentation\n\n","title":"Related Documentation"},{"category":"section","location":"pages/les_models/#See-Also","page":"LES Models","text":"Solvers - Time integration methods\nOperators - Gradient and differential operators\nStochastic Forcing - Forcing for turbulence\nAPI: LES Models - Complete API reference","title":"See Also"},{"category":"section","location":"tutorials/boundary_conditions/#Tutorial:-Boundary-Conditions","page":"Boundary Conditions","text":"This tutorial covers the different types of boundary conditions available in Tarang.jl and how to apply them effectively using the tau method with explicit tau fields.","title":"Tutorial: Boundary Conditions"},{"category":"section","location":"tutorials/boundary_conditions/#Overview","page":"Boundary Conditions","text":"Boundary conditions (BCs) are essential for well-posed PDE problems. Tarang.jl supports:\n\nDirichlet: Specify field value at boundary\nNeumann: Specify derivative at boundary\nRobin: Linear combination of value and derivative\nPeriodic: Automatic for Fourier bases","title":"Overview"},{"category":"section","location":"tutorials/boundary_conditions/#The-Tau-Method-Approach","page":"Boundary Conditions","text":"Tarang.jl uses an explicit tau-method approach for handling boundary conditions. Users must explicitly create tau fields and add them to equations using the lift() operator.","title":"The Tau Method Approach"},{"category":"section","location":"tutorials/boundary_conditions/#Why-Explicit-Tau-Fields?","page":"Boundary Conditions","text":"Clarity: The mathematical structure is visible in your code\nFlexibility: Full control over tau placement\nDebugging: Easy to inspect tau field values\nConsistency: Matches the mathematical formulation","title":"Why Explicit Tau Fields?"},{"category":"section","location":"tutorials/boundary_conditions/#Required-Steps","page":"Boundary Conditions","text":"For any problem with non-periodic boundary conditions:\n\nCreate tau fields — one scalar tau DOF per scalar constraint row (a BC on a VectorField contributes one row per component, so it needs a VectorField tau)\nAdd tau fields to the problem — include them in the field list\nAdd lift() terms to equations — place tau contributions at specific modes\nSpecify boundary conditions — add the algebraic constraint rows. Tau columns and BC rows couple through the assembled matrix, not through a name-based link between a BC and \"its\" tau field\n\nBecause there is no name-based pairing, the thing that keeps the system square is a count. Every variable you declare must be matched by an equation or a BC; Tarang checks this and rejects the problem otherwise. A tau field that you declare but never lift into an equation is an error, not a harmless extra.\n\nThe wording of that rejection depends on the problem type, because a BVP is allowed to carry extra equations (its BCs) while an IVP is not:\n\nproblem check message\nLBVP / NLBVP n_equations >= n_variables Number of equations (2) is less than number of variables (3)\nIVP / EVP n_equations == n_variables Number of equations (7) does not match number of variables (5)","title":"Required Steps"},{"category":"section","location":"tutorials/boundary_conditions/#Complete-Example:-Poisson-Equation","page":"Boundary Conditions","text":"Let's solve the Poisson equation nabla^2 u = -2 with Dirichlet BCs u = 0 on both z walls. This is a verified, runnable LBVP; the analytic solution is u = z(Lz - z) and the solver reproduces it to machine precision.\n\nThe tau method needs one tau variable per boundary condition. Each tau is lifted into the bulk equation with lift(tau, derivative_basis(z_basis, 2), -k) and registered as a parameter via add_parameters!. The lift order index -1 targets the last coefficient row and -2 the second-to-last; a 2nd-order problem therefore uses two taus at orders -1 and -2.\n\nusing Tarang\n\n# Create coordinates, distributor, and bases\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nx_basis = RealFourier(coords[\"x\"]; size=4,  bounds=(0.0, 2π))   # periodic (separable)\nz_basis = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))  # bounded  (coupled)\ndom = Domain(dist, (x_basis, z_basis))\n\n# Field to solve for\nu = ScalarField(dom, \"u\")\n\n# Step 1: Create tau fields (one per BC)\n# These live on the x-basis only (boundary is a line in 2D)\ntau1 = ScalarField(dist, \"tau1\", (x_basis,), Float64)  # For BC at z=0\ntau2 = ScalarField(dist, \"tau2\", (x_basis,), Float64)  # For BC at z=1\n\n# Step 2: Add ALL fields to problem (including tau fields)\nproblem = LBVP([u, tau1, tau2])\n\n# Step 3: Register the lifted tau terms as parameters\n#         (use the 2nd-derivative basis for a 2nd-order problem)\nlb2 = derivative_basis(z_basis, 2)\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\n\n# Step 4: Add equation referencing the lifted tau parameters\nadd_equation!(problem, \"Δ(u) + l1 + l2 = -2\")\n\n# Step 5: Add boundary conditions\nadd_bc!(problem, \"u(z=0)   = 0\")\nadd_bc!(problem, \"u(z=1.0) = 0\")\n\n# Solve\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nensure_layout!(u, :g)   # solve! writes coefficients; switch to grid space to read u\n\nThe solution matches z(1 - z) at every grid point to 1.4e-16, and both walls come out at exactly 0.0.\n\nnote: 1D pure-Chebyshev BVP\nThe example above keeps a Fourier x axis, but a pure single-axis Chebyshev BVP (no Fourier) also works — drop the x axis and define the tau variables on (). The solver builds one coupled tau subproblem over the Chebyshev spectrum.","title":"Complete Example: Poisson Equation"},{"category":"section","location":"tutorials/boundary_conditions/#Dirichlet-Boundary-Conditions","page":"Boundary Conditions","text":"Fix the value of a field at the boundary.","title":"Dirichlet Boundary Conditions"},{"category":"section","location":"tutorials/boundary_conditions/#Basic-Setup","page":"Boundary Conditions","text":"Continuing with the dist, x_basis, z_basis and dom from above:\n\nT = ScalarField(dom, \"T\")\n\n# Create tau fields for each Dirichlet BC\ntau_T1 = ScalarField(dist, \"tau_T1\", (x_basis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (x_basis,), Float64)\n\n# Add to problem\nproblem = LBVP([T, tau_T1, tau_T2])\n\n# Register the lifted tau terms (2nd-order problem -> 2nd-derivative basis)\nlb2 = derivative_basis(z_basis, 2)\nadd_parameters!(problem; l1=lift(tau_T1, lb2, -1), l2=lift(tau_T2, lb2, -2), source=0.0)\n\n# Add equation referencing the lifted tau parameters\nadd_equation!(problem, \"Δ(T) + l1 + l2 = source\")\n\n# Boundary conditions\nadd_bc!(problem, \"T(z=0) = 1\")   # hot wall\nadd_bc!(problem, \"T(z=1) = 0\")   # cold wall\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nensure_layout!(T, :g)\n\nWith a zero source this is the conduction profile T = 1 - z (recovered to 2.5e-16); the walls come out at exactly 1.0 and 0.0.","title":"Basic Setup"},{"category":"section","location":"tutorials/boundary_conditions/#No-Slip-Velocity-(Vector-Fields)","page":"Boundary Conditions","text":"For viscous flows at solid walls, use vector fields for compact notation.\n\nThe idiomatic form is a first-order reduction: rather than lifting two taus into a single second-order operator, introduce the gradient as a substitution and lift one tau into it. A second-order momentum equation then needs two velocity tau fields: one lifted into the gradient (tau_u1, giving grad_u = ∇u + ẑ·lift(τ)), one lifted into the equation (tau_u2). Both lifts use the first-derivative basis, derivative_basis(z_basis, 1) — see \"Which derivativebasis order?\" below. The divergence constraint gets its own gauge tau, `taup, paid for by aninteg(p)` condition.\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nx_basis = RealFourier(coords[\"x\"]; size=8,  bounds=(0.0, 2π))\nz_basis = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))\ndom = Domain(dist, (x_basis, z_basis))\n\np = ScalarField(dom, \"p\")\nu = VectorField(dom, \"u\")\n\n# Tau fields: scalar gauge for the pressure, one vector tau per lift site\ntau_p  = ScalarField(dist, \"tau_p\", (), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (x_basis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (x_basis,), Float64)\n\n# First-order reduction: grad_u = ∇u + ẑ·lift(tau_u1)\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(z_basis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u     = grad(u) + ez * τ_lift(tau_u1)\n\nproblem = IVP([p, u, tau_p, tau_u1, tau_u2])\nadd_parameters!(problem, nu=0.01, ez=ez, grad_u=grad_u, τ_lift=τ_lift)\n\n# Continuity with tau_p (removes the pressure degeneracy)\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\n\n# Momentum (single vector equation)\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) + τ_lift(tau_u2) = -u⋅∇(u)\")\n\n# No-slip boundary conditions (vector notation — all components at once)\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")   # pressure gauge, pays for tau_p\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\n# Start from a decaying shear layer\nux = u.components[1]\nx, z = local_grids(dist, x_basis, z_basis)\nensure_layout!(ux, :g)\nget_grid_data(ux) .= sin.(π .* z')\nensure_layout!(ux, :c)\n\nrun!(solver; stop_iteration=20, progress=false)\n\nAfter 20 steps the no-slip walls hold to 1.1e-16 and the interior shear layer has decayed slightly (max|u_x| = 0.985, from 1.0).","title":"No-Slip Velocity (Vector Fields)"},{"category":"section","location":"tutorials/boundary_conditions/#Neumann-Boundary-Conditions","page":"Boundary Conditions","text":"Specify the derivative (flux) at the boundary.","title":"Neumann Boundary Conditions"},{"category":"section","location":"tutorials/boundary_conditions/#Basic-Setup-2","page":"Boundary Conditions","text":"T = ScalarField(dom, \"T\")\ntau_T1 = ScalarField(dist, \"tau_T1\", (x_basis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (x_basis,), Float64)\n\nproblem = LBVP([T, tau_T1, tau_T2])\n\n# Register the lifted tau terms (2nd-order problem -> 2nd-derivative basis)\nlb2 = derivative_basis(z_basis, 2)\nadd_parameters!(problem; l1=lift(tau_T1, lb2, -1), l2=lift(tau_T2, lb2, -2), source=0.0)\n\nadd_equation!(problem, \"Δ(T) + l1 + l2 = source\")\n\n# Neumann: prescribed flux at z=0, Dirichlet at z=1\nadd_bc!(problem, \"∂z(T)(z=0) = 1\")\nadd_bc!(problem, \"T(z=1) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nensure_layout!(T, :g)\n\nThe exact solution is T = z - 1, and the solve returns it to 2.5e-16.\n\nPair each Neumann condition with something that pins the level of the solution. Two Neumann conditions on a Laplacian leave the solution defined only up to a constant; the tau solve will still return an answer, but it is not the one you meant.","title":"Basic Setup"},{"category":"section","location":"tutorials/boundary_conditions/#Per-Component-Conditions-(Stress-Free-Walls)","page":"Boundary Conditions","text":"A BC applies to a whole variable. add_bc!(problem, \"u(z=0) = 0\") constrains every component of the VectorField u, and there is no string syntax that reaches into a single component: \"u_x(z=0) = 0\" does not resolve to a component of u, so the four per-component strings are counted as four extra constraint rows and the problem fails validation (Number of equations (7) does not match number of variables (5)).\n\nTo impose different conditions on different components — a stress-free wall, where ∂u_x/∂z = 0 but u_z = 0 — declare the components as separate ScalarFields, each with its own tau pair:\n\nux = ScalarField(dom, \"ux\")\nuz = ScalarField(dom, \"uz\")\n\ntx1 = ScalarField(dist, \"tx1\", (x_basis,), Float64)\ntx2 = ScalarField(dist, \"tx2\", (x_basis,), Float64)\ntz1 = ScalarField(dist, \"tz1\", (x_basis,), Float64)\ntz2 = ScalarField(dist, \"tz2\", (x_basis,), Float64)\n\nproblem = LBVP([ux, uz, tx1, tx2, tz1, tz2])\nlb2 = derivative_basis(z_basis, 2)\nadd_parameters!(problem; lx1=lift(tx1, lb2, -1), lx2=lift(tx2, lb2, -2),\n                         lz1=lift(tz1, lb2, -1), lz2=lift(tz2, lb2, -2))\n\nadd_equation!(problem, \"Δ(ux) + lx1 + lx2 = -2\")\nadd_equation!(problem, \"Δ(uz) + lz1 + lz2 = 0\")\n\nadd_bc!(problem, \"ux(z=0) = 0\")       # no-slip at the bottom\nadd_bc!(problem, \"∂z(ux)(z=1) = 0\")   # stress-free at the top\nadd_bc!(problem, \"uz(z=0) = 0\")\nadd_bc!(problem, \"uz(z=1) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nensure_layout!(ux, :g)\n\nThe exact solution is ux = 2z - z² (zero value at the bottom, zero slope at the top), recovered to 1.7e-16.","title":"Per-Component Conditions (Stress-Free Walls)"},{"category":"section","location":"tutorials/boundary_conditions/#Robin-Boundary-Conditions","page":"Boundary Conditions","text":"Linear combination: alpha u + beta fracpartial upartial n = gamma\n\nParameters registered with add_parameters! may be used both as coefficients on the left-hand side of a BC and inside a constant right-hand side — the product h*T_amb is folded to a number when the BC row is built.\n\nT = ScalarField(dom, \"T\")\ntau_T1 = ScalarField(dist, \"tau_T1\", (x_basis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (x_basis,), Float64)\n\nproblem = LBVP([T, tau_T1, tau_T2])\n\n# Register the lifted tau terms and the scalar parameters\nlb2 = derivative_basis(z_basis, 2)\nadd_parameters!(problem; l1=lift(tau_T1, lb2, -1), l2=lift(tau_T2, lb2, -2),\n                         h=10.0, k=1.0, T_amb=25.0, source=0.0)\n\nadd_equation!(problem, \"Δ(T) + l1 + l2 = source\")\n\n# Convective heat transfer at top: h*T + k*dT/dz = h*T_ambient\nadd_bc!(problem, \"h*T(z=1) + k*∂z(T)(z=1) = h*T_amb\")\n\n# Dirichlet at bottom\nadd_bc!(problem, \"T(z=0) = 100\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nensure_layout!(T, :g)\n\nThe solve returns T = 100 - 68.18 z, and the Robin residual h·T(1) + k·T'(1) comes out at exactly 250.0 = h·T_amb.\n\nwarning: A name in a BC must be a registered parameter\nThe folding only reaches numbers and names passed to add_parameters!. A plain Julia global — Tbot = 3.0 in your script, never registered — is not in scope: the BC is enforced as zero. It is at least loud about it (Unknown variable: Tbot, then right-hand side … enforced as ZERO). See Constants and parameters in BC expressions for the full rules.","title":"Robin Boundary Conditions"},{"category":"section","location":"tutorials/boundary_conditions/#Periodic-Boundary-Conditions","page":"Boundary Conditions","text":"Periodic boundaries are automatically handled by Fourier bases - no tau fields needed!\n\n# Fourier basis implies periodicity\nperiodic_x = RealFourier(coords[\"x\"]; size=128, bounds=(0.0, 2π))\n\n# No boundary conditions or tau fields needed for x-direction\n# The Fourier representation automatically enforces u(x=0) = u(x=2π)\n\nwarning: Mixing Periodic and Non-Periodic\nWhen using Fourier (periodic) and Chebyshev (non-periodic) bases together, only create tau fields and boundary conditions for the non-periodic directions.","title":"Periodic Boundary Conditions"},{"category":"section","location":"tutorials/boundary_conditions/#The-lift()-Operator","page":"Boundary Conditions","text":"The lift(tau, derivative_basis(basis, order), -k) operator places a tau correction at a specific coefficient row of the bounded (Chebyshev/Jacobi) basis. You build the lifted terms once and register them as parameters, then reference those parameter names in the equation string:\n\nu    = ScalarField(dom, \"u\")\ntau1 = ScalarField(dist, \"tau1\", (x_basis,), Float64)\ntau2 = ScalarField(dist, \"tau2\", (x_basis,), Float64)\nproblem = LBVP([u, tau1, tau2])\n\nlb2 = derivative_basis(z_basis, 2)   # direct 2nd-order equation -> 2nd-derivative basis\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"Δ(u) + l1 + l2 = -2\")\n\nThe order index -1 lifts into the last coefficient row, -2 the second-to-last, and so on. The number of taus (and lift terms) must match the operator order in the bounded direction.","title":"The lift() Operator"},{"category":"section","location":"tutorials/boundary_conditions/#Which-derivative_basis-order?","page":"Boundary Conditions","text":"The lift basis order follows the order of the operator you are lifting into, and that depends on which formulation you wrote:\n\nFormulation Lift basis Tau layout\nDirect 2nd-order equation, e.g. Δ(u) + l1 + l2 = f derivative_basis(z_basis, 2) two taus, lifted at -1 and -2\nFirst-order reduction, grad_u = grad(u) + ez*lift(tau_u1, …, -1) then div(grad_u) derivative_basis(z_basis, 1) two taus, each lifted at -1 — one into the gradient, one into the equation\nDirect 4th-order equation (∇⁴) derivative_basis(z_basis, 4) four taus, lifted at -1 … -4\n\nThe first-order reduction is order 1, not 2 — this is the form used in the IVP examples below and in the Rayleigh-Bénard tutorial. Both formulations spend the same budget: two taus and two BCs per field per wall pair.\n\nnote: The basis argument is bookkeeping; the mode index is what assembles\nsubproblem_matrix(::Lift, …) builds the lift column from the subproblem's own Chebyshev basis and the mode index -k alone — the derivative_basis(…) object you pass is not consulted when the matrix is assembled. Passing the wrong order therefore does not raise an error, and the 2nd-order Poisson example above returns a bit-identical answer with derivative_basis(z_basis, 1). Get the order right anyway: it documents the formulation, and it is what the rest of the framework (and any future matrix builder) will read.","title":"Which derivative_basis order?"},{"category":"section","location":"tutorials/boundary_conditions/#Mode-Selection-Guidelines","page":"Boundary Conditions","text":"Operator Order Number of BCs Number of lift() terms derivative_basis order Lift indices\n1st (∂/∂z) 1 1 1 -1\n2nd (∂²/∂z²) 2 2 2 -1, -2\n4th (∇⁴) 4 4 4 -1, -2, -3, -4","title":"Mode Selection Guidelines"},{"category":"section","location":"tutorials/boundary_conditions/#Example:-Fourth-Order-Problem","page":"Boundary Conditions","text":"For a biharmonic equation (∇⁴u = f) with 4 boundary conditions:\n\nu = ScalarField(dom, \"u\")\n\n# Four tau fields needed\ntau_u1 = ScalarField(dist, \"tau_u1\", (x_basis,), Float64)\ntau_u2 = ScalarField(dist, \"tau_u2\", (x_basis,), Float64)\ntau_u3 = ScalarField(dist, \"tau_u3\", (x_basis,), Float64)\ntau_u4 = ScalarField(dist, \"tau_u4\", (x_basis,), Float64)\n\nproblem = LBVP([u, tau_u1, tau_u2, tau_u3, tau_u4])\n\n# Register the lifted tau terms (4th-order problem -> 4th-derivative basis)\nlb4 = derivative_basis(z_basis, 4)\nadd_parameters!(problem; l1=lift(tau_u1, lb4, -1), l2=lift(tau_u2, lb4, -2),\n                         l3=lift(tau_u3, lb4, -3), l4=lift(tau_u4, lb4, -4), f=1.0)\n\n# Biharmonic equation with all four lifted tau terms\nadd_equation!(problem, \"Δ(Δ(u)) + l1 + l2 + l3 + l4 = f\")\n\n# Clamped beam: u = 0 and du/dz = 0 at both ends\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"∂z(u)(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\nadd_bc!(problem, \"∂z(u)(z=1) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nensure_layout!(u, :g)\n\nThe clamped beam with a unit load has the exact solution u = z²(1-z)²/24, which the solve reproduces to 1.4e-18.","title":"Example: Fourth-Order Problem"},{"category":"section","location":"tutorials/boundary_conditions/#Complete-Examples","page":"Boundary Conditions","text":"","title":"Complete Examples"},{"category":"section","location":"tutorials/boundary_conditions/#Channel-Flow-(Poiseuille-Flow)","page":"Boundary Conditions","text":"A channel driven by a constant pressure gradient. The forcing enters the explicit right-hand side as a parameter times a unit vector, - dpdx*ex; the scalar coefficient is honoured (doubling dpdx doubles the start-up velocity).\n\nusing Tarang\n\nLx, Lz = 2π, 1.0\nNx, Nz = 16, 16\nnu   = 0.01\ndpdx = -1.0\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nx_basis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx))\nz_basis = ChebyshevT(coords[\"z\"];  size=Nz, bounds=(0.0, Lz))\ndom = Domain(dist, (x_basis, z_basis))\n\np = ScalarField(dom, \"p\")\nu = VectorField(dom, \"u\")\n\n# First-order tau fields\ntau_p  = ScalarField(dist, \"tau_p\", (), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (x_basis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (x_basis,), Float64)\n\n# First-order viscous operator\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(z_basis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u     = grad(u) + ez * τ_lift(tau_u1)\n\nproblem = IVP([u, p, tau_u1, tau_u2, tau_p])\nadd_parameters!(problem, nu=nu, dpdx=dpdx, ex=ex, grad_u=grad_u, τ_lift=τ_lift)\n\n# Continuity with tau_p (removes degeneracy)\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\n\n# Momentum (vector form) — dpdx is the driving pressure gradient\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) + τ_lift(tau_u2) = -u⋅∇(u) - dpdx*ex\")\n\n# No-slip at both walls (vector notation)\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")   # pressure gauge\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\nrun!(solver; stop_iteration=50, progress=false)\n\nStarting from rest, the flow is still in its linear start-up phase after 50 steps (t = 0.05): the measured peak is max u_x = 0.0500, matching |dpdx|·t = 0.05, and no-slip holds at both walls to 7e-18. The steady Poiseuille profile it is heading for is u_x = |dpdx|·z(Lz - z) / (2ν), which takes t ≫ Lz²/ν to establish.\n\nnote: Two things to know about this forcing\nA non-uniform body force cannot be written as parameter * unit_vector. Build a real VectorField on the domain, fill its components in grid space, and register it with add_parameters!(problem, F=F); then write … = -u⋅∇(u) + F.\nNaming the parameter dpdx trips a false-positive parser warning — Linear term on RHS: 'p' appears linearly on RHS — because the name contains the variable name p. It is harmless (the run above is correct); rename the parameter (e.g. fx) if you want a clean log.","title":"Channel Flow (Poiseuille Flow)"},{"category":"section","location":"tutorials/boundary_conditions/#Rayleigh-Bénard-Convection","page":"Boundary Conditions","text":"The full convection problem: momentum, continuity and temperature, with a first-order reduction on both u and T.\n\nusing Tarang\n\nLx, Lz = 4.0, 1.0\nNx, Nz = 32, 16\nRa, Pr = 2e4, 1.0\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nx_basis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nz_basis = ChebyshevT(coords[\"z\"];  size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndom = Domain(dist, (x_basis, z_basis))\n\np = ScalarField(dom, \"p\")\nT = ScalarField(dom, \"T\")\nu = VectorField(dom, \"u\")\n\ntau_p  = ScalarField(dist, \"tau_p\",  (), Float64)\ntau_T1 = ScalarField(dist, \"tau_T1\", (x_basis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (x_basis,), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (x_basis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (x_basis,), Float64)\n\n# First-order substitutions (lift basis order 1)\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(z_basis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_T = grad(T) + ez * τ_lift(tau_T1)\n\nproblem = IVP([p, T, u, tau_p, tau_T1, tau_T2, tau_u1, tau_u2])\nadd_parameters!(problem, nu=Pr, buoy=Ra*Pr, ez=ez,\n                grad_u=grad_u, grad_T=grad_T, τ_lift=τ_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) - buoy*T*ez + τ_lift(tau_u2) = -u⋅∇(u)\")\n\n# Boundary conditions (vector notation for velocity)\nadd_bc!(problem, \"u(z=0) = 0\")   # No-slip bottom\nadd_bc!(problem, \"u(z=1) = 0\")   # No-slip top\nadd_bc!(problem, \"T(z=0) = 1\")   # Hot bottom\nadd_bc!(problem, \"T(z=1) = 0\")   # Cold top\nadd_bc!(problem, \"integ(p) = 0\") # Pressure gauge\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)\n\n# Conduction profile + damped noise\nx, z = local_grids(dist, x_basis, z_basis)\nfill_random!(T, \"g\"; seed=42, distribution=\"normal\", scale=1e-3)\nget_grid_data(T) .*= z' .* (Lz .- z')\nget_grid_data(T) .+= 1.0 .- z' ./ Lz\nensure_layout!(T, :c)\n\nrun!(solver; stop_iteration=20, progress=false)\n\nAfter 20 steps the buoyancy is driving a weak vertical flow (max|u_z| = 7.6e-3), and the wall temperatures hold to machine precision: max|T(z=0) - 1| = 0.0, max|T(z=Lz)| = 1.6e-17.\n\nwarning: Run this example serially\nA (Fourier x, Chebyshev z) domain cannot be decomposed as written: Tarang stops at np ≥ 2 with \"the decomposed (trailing) axis/axes [2] are non-Fourier … a Chebyshev axis cannot be decomposed\", because the DCT needs the whole z axis local to each rank. Putting the Chebyshev axis first gets past that guard, but the 1-D boundary tau fields then hit the \"MPI parallelization is not supported for 1D problems\" guard. The LBVPs above (Poisson, Neumann, Robin, biharmonic) are unaffected — they are solved per Fourier mode.","title":"Rayleigh-Bénard Convection"},{"category":"section","location":"tutorials/boundary_conditions/#Time-and-Space-Dependent-Boundary-Conditions","page":"Boundary Conditions","text":"An IVP supports BCs whose value varies in time, space, or both. The BC expression string is re-evaluated at solver build time (for space-dependent) and at every time step / RK stage (for time-dependent), and the result is projected onto the appropriate Fourier mode for each per-subproblem solve.\n\nNothing extra is required — no add_coordinate_field! call, no set_time_variable! — the solver auto-registers coordinate arrays from the problem's bases and uses t as the default time variable.\n\nnote: Time-stepped problems only\nThis machinery is driven by the stepper. In an LBVP/NLBVP a space-dependent BC string is currently evaluated as zero — use a constant BC there.","title":"Time- and Space-Dependent Boundary Conditions"},{"category":"section","location":"tutorials/boundary_conditions/#Time-dependent-BC-(scalar)","page":"Boundary Conditions","text":"add_bc!(problem, \"T(z=0) = 1.0 + 0.1 * cos(2*pi*t)\")\n\nThe BC value is re-evaluated at every RK stage time t + c[i]·dt, so multi-stage methods retain full formal order of accuracy even for rapidly-varying BCs. Internally this goes through update_time_dependent_bcs!(bc_manager, stage_time) followed by _apply_bc_values_to_equations!, and the resulting ConstantOperator(value) is written into equation_data[eq_idx][\"F\"].\n\nMeasured on a 2D diffusion problem after 100 steps to t = 0.1, the enforced wall value tracks 1 + 0.1·cos(2πt) to 2.2e-16.","title":"Time-dependent BC (scalar)"},{"category":"section","location":"tutorials/boundary_conditions/#Space-dependent-BC-(1D-variation-in-2D-problem)","page":"Boundary Conditions","text":"add_bc!(problem, \"T(z=0) = 1.0 + 0.1 * sin(2*pi*x/4.0)\")\nadd_bc!(problem, \"T(z=1) = 0\")\n\nAt solver build, _apply_bc_values_to_equations!(solver, 0.0) evaluates \"1.0 + 0.1*sin(2*pi*x/4.0)\" against the auto-registered global x coordinate array, yielding an Nx-long grid-space array. This is wrapped in an ArrayOperator and stored in equation_data[eq_idx][\"F\"]. At each stepper call, gather_alg_F! runs _bc_array_projection on this array — taking an unnormalized FFTW.rfft and extracting each subproblem's own Fourier-mode coefficient.\n\nThe bottom-wall temperature is thus enforced at T(x, z=0) = 1 + 0.1·sin(2πx/Lx) in grid space — verified to 3.3e-15.","title":"Space-dependent BC (1D variation in 2D problem)"},{"category":"section","location":"tutorials/boundary_conditions/#Space-time-dependent-BC","page":"Boundary Conditions","text":"add_bc!(problem, \"T(z=0) = 1.0 + 0.1 * sin(2*pi*x/4.0) * cos(2*pi*t)\")\n\nCombines both paths. The spatial pattern is re-projected at each stage time, so the stepper always sees the correct instantaneous BC. This is what enables oscillating boundary temperature patterns, traveling thermal waves at the wall, etc. Measured after 100 steps to t = 0.1, the enforced wall value tracks 1 + 0.1·sin(2πx/Lx)·cos(2πt) to 2.0e-15.","title":"Space + time dependent BC"},{"category":"section","location":"tutorials/boundary_conditions/#3D-problems:-both-periodic-axes","page":"Boundary Conditions","text":"In a 3D problem with two periodic axes (x, y), a BC expression may vary along x, along y, or along both.\n\nA 1D expression in x alone is broadcast along y — \"uniform in y\":\n\n# 3D problem; this is constant in y by design\nadd_bc!(problem, \"T(z=0) = sin(2*pi*x/4.0)\")\n\nThe broadcast-and-FFT machinery places rfft(sin(2π x/Lx))[k_x] · Ny at the DC y mode and zero at non-DC y modes, matching the grid-space meaning exactly. Measured error 3.3e-16 on an 8×8×8 RealFourier × RealFourier × ChebyshevT problem.\n\nA genuinely two-axis expression works as well: it is evaluated as an (Nx, Ny) grid-space array, and each (kx, ky) subproblem extracts its own coefficient. Either of these is a valid single z=0 condition:\n\n# varies along y only\nadd_bc!(problem, \"T(z=0) = cos(2*pi*y/2.0)\")\n\n# varies along both axes (use ONE of these, not both)\n# add_bc!(problem, \"T(z=0) = sin(2*pi*x/4.0) * cos(2*pi*y/2.0)\")\n\nOn the same 8×8×8 problem the enforced wall matches the intended grid-space pattern to 3.3e-16 for the y-only form and 4.4e-16 for the x·y product.\n\nwarning: Bake the box size in as a literal, not as `Lx`/`Ly`\nNote the 2.0 and 4.0 above rather than Ly and Lx. This is the coordinate-expression rule from the table below, and it bites hardest here. \"T(z=0) = cos(2*pi*y/Ly)\" does not fail because of the y axis — it fails because Ly is a name, and a coordinate expression cannot see the problem's parameters. It is enforced as zero, and says so (Unknown variable: Ly, then right-hand side … enforced as ZERO). Registering Ly with add_parameters! does not help; interpolate the number into the string instead.","title":"3D problems: both periodic axes"},{"category":"section","location":"tutorials/boundary_conditions/#Constants-and-parameters-in-BC-expressions","page":"Boundary Conditions","text":"A BC right-hand side is read in one of two ways, and it matters which. A constant right-hand side is folded to a number — arithmetic over numeric literals and registered parameters is fine. An expression right-hand side (one that mentions a coordinate or t) goes through a different evaluator, and that one sees the coordinate arrays and t but not the problem's parameters:\n\nright-hand side how it is read works?\n\"T(z=0) = 1\" bare literal ✅\n\"T(z=0) = Tbot\", with add_parameters!(problem, Tbot=3.0) bare parameter name ✅\n\"T(z=0) = 10*25\", \"= h*T_amb\", \"= 1/Re\" compound constant, folded ✅\n\"T(z=0) = Tbot\", with Tbot an unregistered Julia global unknown name ❌ warns, enforced as zero\n\"T(z=0) = 1.0 + 0.1*sin(2*pi*x/4.0)\" coordinate/time expression, numeric literals ✅\n\"T(z=0) = 1.0 + amplitude*sin(2*pi*x/Lx)\" coordinate expression naming parameters ❌ warns, enforced as zero\n\nSo a constant BC may name parameters freely:\n\nadd_parameters!(problem, h=10.0, k=1.0, T_amb=25.0)\nadd_bc!(problem, \"h*T(z=1) + k*∂z(T)(z=1) = h*T_amb\")\n\nbut an expression BC may not — bake its constants in with Julia string interpolation instead:\n\nLx = 4.0\namplitude = 0.1\nadd_bc!(problem, \"T(z=0) = 1.0 + $amplitude * sin(2*pi*x/$Lx)\")\n\nAn unsupported right-hand side is enforced as zero, but it says so: you get Unknown variable: amplitude followed by Boundary condition right-hand side of type … is not supported and is being enforced as ZERO. Treat that warning as an error — the solve will otherwise \"succeed\" against the wrong boundary condition.","title":"Constants and parameters in BC expressions"},{"category":"section","location":"tutorials/boundary_conditions/#Common-pitfalls","page":"Boundary Conditions","text":"Missing coordinate field — for a BC to reference x, the problem must have a basis with element_label == \"x\". This is automatic if you build bases with coords[\"x\"], but if you have multiple coordinate systems the auto-registration may pick the wrong x. A \"space-dependent BCs detected but no coordinate fields registered\" warning at solver build means the auto-registration didn't find a matching basis.\nTime variable other than t — the parser and the runtime evaluator both hard-code t as the time symbol. Custom time variable names aren't fully supported; use t in your BC expressions.\nNon-constant custom function — the BC string evaluator whitelists sin, cos, tan, exp, log, sqrt, abs, sign, floor, ceil, round, rem, min, max, and hyperbolic variants. Arbitrary Julia functions in BC strings are not supported; if you need a custom function, build it into the value at simulation-script level (outside the string) and register via add_parameters!.\nBCs must be declared with add_bc! — passing a boundary condition to add_equation! does not register it with the BC manager. A constant BC happens to come out right; anything space- or time-dependent is then silently not enforced.","title":"Common pitfalls"},{"category":"section","location":"tutorials/boundary_conditions/#Validation","page":"Boundary Conditions","text":"","title":"Validation"},{"category":"section","location":"tutorials/boundary_conditions/#Checking-BC-Satisfaction","page":"Boundary Conditions","text":"# After solving, verify BCs are satisfied\nfunction check_dirichlet_bc(field, coord, location, expected_value; tol=1e-10)\n    ensure_layout!(field, :g)\n    data = get_grid_data(field)\n\n    # Get boundary values\n    if coord == \"z\" && location == :left\n        bc_values = data[:, 1]\n    elseif coord == \"z\" && location == :right\n        bc_values = data[:, end]\n    end\n\n    error = maximum(abs.(bc_values .- expected_value))\n    @assert error < tol \"BC error: $error\"\n\n    return error\nend\n\n# Usage\ncheck_dirichlet_bc(T, \"z\", :left, 1.0)\ncheck_dirichlet_bc(T, \"z\", :right, 0.0)\n\nRun against the Rayleigh-Bénard solver above, this returns 0.0 at the hot wall and 1.6e-17 at the cold wall.","title":"Checking BC Satisfaction"},{"category":"section","location":"tutorials/boundary_conditions/#Troubleshooting","page":"Boundary Conditions","text":"","title":"Troubleshooting"},{"category":"section","location":"tutorials/boundary_conditions/#Missing-or-Unused-Tau-Fields","page":"Boundary Conditions","text":"There is no automatic name-based pairing between a BC and a tau field. A missing lift column, an unused tau variable, or the wrong number of scalar tau DOFs surfaces instead as a counting or shape failure:\n\nmistake what you actually see (in the LBVPs above)\nBCs but no tau fields and no lift terms DimensionMismatch during matrix construction (the BC rows have no tau columns to land in, so the system is not square)\na tau field declared but never lifted ArgumentError: Problem validation failed: Number of equations (3) is less than number of variables (4)\na BC forgotten ArgumentError: Problem validation failed: Number of equations (2) is less than number of variables (3)\n\nIn an IVP the same two mistakes read Number of equations (n) does not match number of variables (m) instead — that is the message the per-component VectorField BC above trips.\n\nSolution: create the required tau fields, include every one in the problem variables, and reference every one through a lift term:\n\n# Wrong: no tau fields, no lift terms — the BCs have nothing to act through\n#     problem = LBVP([u])\n#     add_equation!(problem, \"Δ(u) = -2\")\n#     add_bc!(problem, \"u(z=0) = 0\")\n\n# Correct: create tau fields, register lifted tau parameters, add them to the equation\nu = ScalarField(dom, \"u\")\ntau_u1 = ScalarField(dist, \"tau_u1\", (x_basis,), Float64)\ntau_u2 = ScalarField(dist, \"tau_u2\", (x_basis,), Float64)\n\nproblem = LBVP([u, tau_u1, tau_u2])\nlb2 = derivative_basis(z_basis, 2)\nadd_parameters!(problem; l1=lift(tau_u1, lb2, -1), l2=lift(tau_u2, lb2, -2))\nadd_equation!(problem, \"Δ(u) + l1 + l2 = -2\")\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)","title":"Missing or Unused Tau Fields"},{"category":"section","location":"tutorials/boundary_conditions/#Over-Specified-System","page":"Boundary Conditions","text":"Problem: Too many boundary conditions cause singular matrices.\n\nSolution: Match the number of BCs (and tau fields) to the operator order in each direction.","title":"Over-Specified System"},{"category":"section","location":"tutorials/boundary_conditions/#Under-Specified-System","page":"Boundary Conditions","text":"Error (in an LBVP): ArgumentError: Problem validation failed: Number of equations (4) is less than number of variables (5)\n\nSolution: Every variable needs an equation. Bulk equations and BCs both count, so a tau field you declared but never lifted into an equation — or a BC you forgot — shows up here. Count them: n_equations + n_bcs == n_variables.","title":"Under-Specified System"},{"category":"section","location":"tutorials/boundary_conditions/#Tau-Field-Dimension-Mismatch","page":"Boundary Conditions","text":"Problem: Tau field has wrong dimensionality.\n\nSolution: Tau fields should live on the boundary, not the full domain:\n\n# For a 2D problem with x-basis (periodic) and z-basis (non-periodic):\n# Boundaries are at constant z, so tau fields live on x-basis only\ntau_u = ScalarField(dist, \"tau_u\", (x_basis,), Float64)  # Correct: 1D on x\n\n# NOT:\ntau_u = ScalarField(dist, \"tau_u\", (x_basis, z_basis), Float64)  # Wrong: 2D","title":"Tau Field Dimension Mismatch"},{"category":"section","location":"tutorials/boundary_conditions/#See-Also","page":"Boundary Conditions","text":"Tau Method (Advanced): Mathematical details\nProblems API: Problem definition\nBases API: Spectral basis selection\n2D Rayleigh-Bénard Tutorial: Complete example with BCs","title":"See Also"},{"category":"section","location":"tutorials/overview/#Tutorials-Overview","page":"Overview","text":"This section contains comprehensive tutorials that guide you through solving various types of PDEs with Tarang.jl. Each tutorial builds on concepts from previous ones, so we recommend following them in order if you're new to spectral methods or Tarang.jl.","title":"Tutorials Overview"},{"category":"section","location":"tutorials/overview/#Tutorial-Path","page":"Overview","text":"","title":"Tutorial Path"},{"category":"section","location":"tutorials/overview/#For-Beginners","page":"Overview","text":"If you're new to spectral methods or Tarang.jl, start here:\n\nFirst Steps - Basic workflow and simple diffusion problem\n2D Rayleigh-Bénard Convection - Complete fluid dynamics example\nBoundary Conditions - Master the boundary condition system","title":"For Beginners"},{"category":"section","location":"tutorials/overview/#Intermediate-Topics","page":"Overview","text":"Once comfortable with basics:\n\nFluid Dynamics Examples - Navier-Stokes, rotating convection, and turbulence examples\nSurface Dynamics - Fractional Laplacian, surface quasi-geostrophic dynamics\n3D Turbulent Flow - 3D problems with advanced parallelization\nAnalysis and Output - Data management and visualization\nEigenvalue Problems - Linear stability analysis","title":"Intermediate Topics"},{"category":"section","location":"tutorials/overview/#Advanced-Topics","page":"Overview","text":"For experienced users:\n\nCustom Operators - Define new differential operators\nOptimization Guide - Performance tuning","title":"Advanced Topics"},{"category":"section","location":"tutorials/overview/#Tutorial-List","page":"Overview","text":"","title":"Tutorial List"},{"category":"section","location":"tutorials/overview/#Initial-Value-Problems-(IVP)","page":"Overview","text":"Time-evolution problems where you integrate PDEs forward in time.\n\nTutorial Description Complexity Key Features\n2D Rayleigh-Bénard Thermal convection in 2D Intermediate Navier-Stokes, buoyancy, CFL\nFluid Dynamics Examples Navier-Stokes and turbulence examples Intermediate Shear flows, rotating convection, turbulence\nSurface Dynamics SQG and boundary-coupled dynamics Advanced Fractional Laplacian, surface dynamics, SQG inversion\n3D Taylor-Green Vortex 3D turbulence simulation Advanced 3D FFTs, energy spectra\nChannel Flow Turbulent channel flow Advanced Wall-bounded, statistics","title":"Initial Value Problems (IVP)"},{"category":"section","location":"tutorials/overview/#Boundary-Value-Problems-(BVP)","page":"Overview","text":"Steady-state problems with boundary conditions.\n\nTutorial Description Complexity Key Features\nSteady Convection Fixed temperature Rayleigh-Bénard Intermediate LBVP, sparse linear solve\nStokes Flow Low Reynolds number flow Beginner Simple BVP example","title":"Boundary Value Problems (BVP)"},{"category":"section","location":"tutorials/overview/#Eigenvalue-Problems-(EVP)","page":"Overview","text":"Linear stability analysis and normal modes.\n\nTutorial Description Complexity Key Features\nStability Analysis Eigenvalue problem setup Advanced EVP, eigensolvers","title":"Eigenvalue Problems (EVP)"},{"category":"section","location":"tutorials/overview/#Surface-and-Boundary-Dynamics","page":"Overview","text":"Problems with dynamics confined to surfaces or boundaries.\n\nTutorial Description Complexity Key Features\nSurface Dynamics SQG, QG, boundary advection-diffusion Advanced Fractional Laplacian, coupled systems","title":"Surface and Boundary Dynamics"},{"category":"section","location":"tutorials/overview/#Wave-Mean-Flow-Analysis","page":"Overview","text":"Tutorial Description Complexity Key Features\nRotating Shallow Water Inertia-gravity wave filtering Intermediate Temporal filters, wave-mean separation\nGQL Approximation Generalized Quasi-Linear methods Advanced Wavenumber cutoff, zonal jets","title":"Wave-Mean Flow Analysis"},{"category":"section","location":"tutorials/overview/#Special-Topics","page":"Overview","text":"Tutorial Description Complexity Key Features\nBoundary Conditions All BC types and usage Intermediate Dirichlet, Neumann, Robin\nAnalysis & Output Data management Intermediate NetCDF, analysis","title":"Special Topics"},{"category":"section","location":"tutorials/overview/#Problem-Types-Explained","page":"Overview","text":"","title":"Problem Types Explained"},{"category":"section","location":"tutorials/overview/#Initial-Value-Problems-(IVP)-2","page":"Overview","text":"When to use: Time-dependent PDEs where you know the initial state and want to evolve forward in time.\n\nExamples:\n\nFluid dynamics (Navier-Stokes)\nHeat diffusion\nWave propagation\nReaction-diffusion systems\n\nTypical structure (1D viscous Burgers, complete and runnable):\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=32, bounds=(0.0, 2π))\ndomain = Domain(dist, (xbasis,))\nu      = ScalarField(domain, \"u\")\n\nproblem = IVP([u])\nadd_parameters!(problem, nu=0.05)          # names used in equation strings must be parameters\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = -u*∂x(u)\")\nset!(u, x -> sin(x))                       # serial only — under MPI use `local_grids` (below)\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\nt_end = 0.02\nwhile solver.sim_time < t_end\n    step!(solver)\nend\n\nstep!(solver) uses solver.dt. step!(solver, dt) steps with dt and stores it: solver.dt is overwritten, so every later bare step!(solver) keeps using the new value. The run!(solver; stop_time=…, stop_iteration=…) driver is the usual alternative to writing the loop yourself, and is what you need if you want CFL control or file output (below).","title":"Initial Value Problems (IVP)"},{"category":"section","location":"tutorials/overview/#Boundary-Value-Problems-(BVP)-2","page":"Overview","text":"When to use: Steady-state problems where you solve for the spatial distribution given boundary conditions.\n\nTypes:\n\nLBVP: Linear boundary value problems\nNLBVP: Nonlinear boundary value problems (require iteration)\n\nExamples:\n\nSteady-state heat conduction\nPoisson equation\nSteady Stokes flow\n\nTypical structure (tau method: one tau variable per BC, lifted into the bulk equation and declared via add_parameters!; BCs use add_bc!):\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb = RealFourier(coords[\"x\"]; size=4,  bounds=(0.0, 2π))\nzb = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))\ndomain = Domain(dist, (xb, zb))\n\nT    = ScalarField(domain, \"T\")\ntau1 = ScalarField(dist, \"tau1\", (xb,), Float64)   # one tau per BC, carrying the Fourier basis\ntau2 = ScalarField(dist, \"tau2\", (xb,), Float64)\nlb2  = derivative_basis(zb, 2)\n\nproblem = LBVP([T, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"Δ(T) + l1 + l2 = -2\")\nadd_bc!(problem, \"T(z=0) = 0\")\nadd_bc!(problem, \"T(z=1) = 1\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)     # recovers T = 2z - z² to 1.7e-16\n\nThe BVP path supports both mixed Fourier+Chebyshev and pure single-axis Chebyshev domains; see the Problems API for a complete, runnable example.","title":"Boundary Value Problems (BVP)"},{"category":"section","location":"tutorials/overview/#Eigenvalue-Problems-(EVP)-2","page":"Overview","text":"When to use: Linear stability analysis, computing normal modes, or finding eigenvalues of differential operators.\n\nExamples:\n\nHydrodynamic stability\nNormal mode analysis\nResonance frequencies\n\nTypical structure (growth rates of the Dirichlet Laplacian on z ∈ [0, 1]):\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nzb     = ChebyshevT(coords[\"z\"]; size=32, bounds=(0.0, 1.0))\ndomain = Domain(dist, (zb,))\n\nu    = ScalarField(domain, \"u\")\ntau1 = ScalarField(dist, \"tau1\", (), Float64)\ntau2 = ScalarField(dist, \"tau2\", (), Float64)\nlb2  = derivative_basis(zb, 2)\n\n# tau variables + lift handle the bounded-direction BCs (tau method)\nproblem = EVP([u, tau1, tau2]; eigenvalue=:σ)\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\n# The eigenvalue REPLACES the time derivative: keep dt(u) to build the mass matrix.\n# Do NOT write `σ*u = ...` — that builds an empty M and returns no eigenvalues.\nadd_equation!(problem, \"dt(u) - Δ(u) - l1 - l2 = 0\")\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\n\nsolver = EigenvalueSolver(problem; nev=5, which=:SM)\neigenvalues, eigenvectors = solve!(solver)\n# eigenvalues ≈ [-9.8696, -39.478, -88.826, -157.91, -246.74] = -(nπ)², i.e. pure decay\n\nSee the Problems API for the full eigenvalue convention (L x = σ M x, with M assembled from dt(·) terms).","title":"Eigenvalue Problems (EVP)"},{"category":"section","location":"tutorials/overview/#Choosing-a-Tutorial","page":"Overview","text":"","title":"Choosing a Tutorial"},{"category":"section","location":"tutorials/overview/#By-Physics","page":"Overview","text":"Fluid Dynamics:\n\nStart: 2D Rayleigh-Bénard\nIntermediate: Fluid Dynamics Examples, Surface Dynamics\nAdvanced: 3D Turbulence, Channel Flow\n\nGeophysical Flows:\n\nStart: Surface Dynamics (SQG, QG)\nIntermediate: Rotating Shallow Water (wave-mean separation)\nAdvanced: GQL Approximation (zonal jets, turbulence closure)\n\nHeat Transfer:\n\nStart: First Steps (diffusion)\nAdvanced: 2D Rayleigh-Bénard (convection)\n\nStability Analysis:\n\nStart: Eigenvalue Problems","title":"By Physics"},{"category":"section","location":"tutorials/overview/#By-Technique","page":"Overview","text":"Want to learn:\n\nMPI parallelization → Running with MPI\nBoundary conditions → Boundary Conditions\nOutput and analysis → Analysis and Output\n3D problems → 3D Turbulence","title":"By Technique"},{"category":"section","location":"tutorials/overview/#By-Complexity","page":"Overview","text":"Beginner: Basic concepts, single field\n\nFirst Steps\n\nIntermediate: Multiple fields, coupled equations\n\n2D Rayleigh-Bénard\nFluid Dynamics Examples\nSurface Dynamics\nBoundary Conditions\nAnalysis and Output\n\nAdvanced: 3D problems, advanced analysis\n\n3D Turbulence\nEigenvalue Problems","title":"By Complexity"},{"category":"section","location":"tutorials/overview/#Common-Patterns","page":"Overview","text":"","title":"Common Patterns"},{"category":"section","location":"tutorials/overview/#Setting-Up-a-Simulation","page":"Overview","text":"Every tutorial follows this pattern. Here it is in full for 2D heat diffusion — the smallest complete simulation Tarang can run:\n\n# 1. MPI initialization (harmless in serial; needed for `mpiexec` runs)\nusing Tarang, MPI\nMPI.Initialized() || MPI.Init()\n\n# 2. Domain setup\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\nybasis = RealFourier(coords[\"y\"]; size=16, bounds=(0.0, 2π))\ndomain = Domain(dist, (xbasis, ybasis))\n\n# 3. Fields\nu = ScalarField(domain, \"u\")\n\n# 4. Problem\nproblem = IVP([u])\nadd_parameters!(problem, nu=0.1)\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = 0\")\n# add_bc!(problem, \"...\")   # only for bounded (Chebyshev/Jacobi) directions\n\n# 5. Initial condition — use `local_grids`, which returns THIS rank's slab\nx, y = local_grids(dist, xbasis, ybasis)\nensure_layout!(u, :g)\nget_grid_data(u) .= sin.(x) .* cos.(y')\nensure_layout!(u, :c)\n\n# 6. Solver and time loop\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\nt_end = 0.5\nwhile solver.sim_time < t_end\n    step!(solver)\nend\n# max|u| = 0.90483742, exactly exp(-2*nu*t_end)\n\nMPI.Init() defaults to finalize_atexit=true, so MPI.Finalize() runs on exit by itself — you do not need a cleanup step.\n\nwarning: `set!(field, ::Function)` is serial-only\nset!(u, (x, y) -> …) builds the global meshgrid, so under MPI it throws DimensionMismatch. local_grids(dist, bases...) gives each rank its own slice and works identically at np = 1, 2, 4, which is why the pattern above uses it.","title":"Setting Up a Simulation"},{"category":"section","location":"tutorials/overview/#Adding-Analysis","page":"Overview","text":"Common analysis tasks, on a solver whose state has a scalar T and a VectorField u:\n\n# Adaptive timestep: CFL takes the SOLVER, and velocities must be VectorFields\ncfl = CFL(solver; initial_dt=1e-3, cadence=10, safety=0.4, max_dt=0.01)\nadd_velocity!(cfl, u)\n\n# File output: base path FIRST, solver SECOND, then one task per quantity\nhandler = add_file_handler(\"outputs\", solver; sim_dt=0.005, max_writes=10)\nadd_task!(handler, u; name=\"u\")\nadd_task!(handler, T; name=\"T\")\n\n# `run!` applies the CFL controller and processes solver-registered handlers\nrun!(solver; stop_iteration=20, cfl=cfl)\n\n# Custom diagnostics\nfunction compute_diagnostics(u, T)\n    ke = total_kinetic_energy(u)    # Float64: domain integral of ½|u|²\n    temp_mean = integrate(T)        # Float64: quadrature-weighted integral\n    return (ke=ke, temp=temp_mean)\nend\n\nThree things that trip people up here:\n\nPass field or operator objects to add_task!, never strings. add_task!(h, \"u*u\") is accepted silently and then writes a scalar of zeros.\nThe last component of the base path is the handler name, and each write set gets its own directory: add_file_handler(\"outputs\", …) writes outputs/outputs_s1/outputs_s1.nc, and a new _sN set is started every max_writes writes (outputs/outputs_s2/…). Use Tarang.current_file(handler) to get the path in code. The variables live in NetCDF-4 groups (vars, time, grids), so a plain NetCDF.ncread(file, \"u\") will not find them — read them with Tarang.group_ncread(file, \"vars\", \"u\"). A VectorField task is stored with its component axis, e.g. vars/u has shape (write, component, x, y).\nadd_file_handler also has a (path, dist, vars) form. That one is not registered with the solver, so run! will not process it unless you also pass outputs=[handler].\n\nSee Analysis and Output for the full output API.","title":"Adding Analysis"},{"category":"section","location":"tutorials/overview/#Getting-Help","page":"Overview","text":"If you're stuck on a tutorial:\n\nCheck the complete example at the end of each tutorial\nLook at the source code in the examples/ directory\nSearch the API reference for function documentation\nAsk on GitHub Discussions for community help","title":"Getting Help"},{"category":"section","location":"tutorials/overview/#Contributing-Tutorials","page":"Overview","text":"We welcome tutorial contributions! See the Contributing Guide for details on:\n\nTutorial format and style\nAdding Jupyter notebooks\nIncluding visualizations\nTesting tutorial code","title":"Contributing Tutorials"},{"category":"section","location":"tutorials/overview/#Next-Steps","page":"Overview","text":"Ready to start? Pick a tutorial from the list above or continue with:\n\n2D Rayleigh-Bénard Convection for a complete fluid dynamics example\nBoundary Conditions to master BC specification\nRunning with MPI for parallel computing details","title":"Next Steps"},{"category":"section","location":"api/stochastic_forcing/#Stochastic-Forcing-API","page":"Stochastic Forcing","text":"","title":"Stochastic Forcing API"},{"category":"section","location":"api/stochastic_forcing/#Types","page":"Stochastic Forcing","text":"","title":"Types"},{"category":"section","location":"api/stochastic_forcing/#Abstract-Types","page":"Stochastic Forcing","text":"abstract type Forcing end\nabstract type StochasticForcingType <: Forcing end\nabstract type DeterministicForcingType <: Forcing end","title":"Abstract Types"},{"category":"section","location":"api/stochastic_forcing/#StochasticForcing","page":"Stochastic Forcing","text":"Stochastic forcing in Fourier space with white-noise temporal correlation.\n\nType signature:\n\nmutable struct StochasticForcing{\n    T<:AbstractFloat, N,\n    A<:AbstractArray{T, N},\n    CA<:AbstractArray{Complex{T}, N}\n} <: StochasticForcingType\n\nA and CA are the concrete real and complex array types on the chosen architecture, so the same struct holds Arrays on the CPU and CuArrays on the GPU.\n\nFields:\n\nField Type Description\nforcing_spectrum A √Q̂(k) - amplitude spectrum\nenergy_injection_rate T Target ε\ninjection_metric Symbol :direct or :vorticity_kinetic\nk_forcing T Central forcing wavenumber\ndk_forcing T Forcing bandwidth\ndt T Current timestep\ndomain_size NTuple{N, T} Domain extent (Lx, Ly, ...)\nfield_size NTuple{N, Int} Grid size (Nx, Ny, ...)\nwavenumbers NTuple{N, Vector{T}} Wavenumber arrays (kx, ky, ...), always on the CPU\ncached_forcing CA Cached F̂ (constant within timestep)\nprevsol Union{Nothing, CA} Previous solution for Stratonovich work\nrng AbstractRNG Random number generator\nrandom_phases A Pre-allocated random phase buffer\ndiagnostic_weights Union{Nothing,A} Cached backend diagnostic weights\ndiagnostic_global_shape NTuple{N,Int} Shape associated with cached weights\ndiagnostic_local_ranges NTuple{N,UnitRange{Int}} Local ranges associated with cached weights\ndiagnostic_metric Symbol Metric associated with cached weights\nlast_update_time T Time of last forcing update\nspectrum_type Symbol Type of forcing spectrum\nenforce_hermitian Bool Enforce F̂(-k) = F̂(k)* for real-valued fields\narchitecture AbstractArchitecture CPU() or GPU() backend\n\nProperty aliases (via getproperty, not stored fields):\n\nProperty Returns\nforcing.forcing_rate forcing.energy_injection_rate\nforcing.spectrum forcing.forcing_spectrum\nforcing.is_stochastic true\nforcing.is_gpu whether architecture is a GPU\n\nConstructor:\n\nStochasticForcing(;\n    field_size::NTuple{N, Int},                              # Required\n    domain_size::Union{Nothing, NTuple{N, Real}} = nothing,  # nothing => 2π in each direction\n    energy_injection_rate::Real = 1.0,\n    forcing_rate::Union{Nothing, Real} = nothing,            # alias for energy_injection_rate\n    injection_metric::Symbol = :direct,\n    k_forcing::Real = 4.0,\n    dk_forcing::Real = 1.0,\n    dt::Real = 0.01,\n    spectrum_type::Symbol = :ring,\n    rng::AbstractRNG = Random.MersenneTwister(),\n    dtype::Type{T} = Float64,\n    enforce_hermitian::Bool = true,\n    architecture::AbstractArchitecture = CPU()\n) where {T<:AbstractFloat, N}\n\nforcing_rate and energy_injection_rate set the same target ε. forcing_rate wins when provided. If a non-default energy_injection_rate is also supplied and the values disagree, a warning is emitted.\n\ninjection_metric = :direct makes ε the injection rate of the forced variable's direct quadratic invariant. Use :vorticity_kinetic when forcing 2-D vorticity and ε should be kinetic-energy injection; it weights each nonzero Fourier mode by 1/|k|².\n\nThe default rng is a fresh MersenneTwister per instance (not Random.GLOBAL_RNG), which avoids accidental RNG sharing between forcing instances. Concurrent mutation of the same forcing object is not supported. Under MPI with more than one rank the constructor broadcasts a seed from rank 0 on MPI.COMM_WORLD, so every rank draws the same phases and the forcing stays coherent across the decomposition.\n\nSet enforce_hermitian = false when the forced field is genuinely complex; with the default true, F̂(-k) = F̂(k)* is imposed so that the inverse transform of the forcing is real. Self-conjugate modes are projected as sqrt(2) * real(z); the factor preserves the variance that would otherwise be halved by discarding the imaginary part.\n\ndk_forcing must be positive for :ring, :band and :kolmogorov (an ArgumentError is thrown otherwise); :lowk ignores it. :kolmogorov also requires k_forcing > 0.\n\nSpectrum types:\n\nSymbol Description Unnormalized amplitude √Q̂(k)\n:ring Gaussian ring around k_f exp(-(|k| - kf)² / 4δf²)\n:band Sharp band |k| ∈ (kf - δf, kf + δf) 1 if ||k| - kf| < δf, else 0\n:lowk Low wavenumber forcing 1 if |k| < k_f, else 0\n:kolmogorov Large scales with a smooth cutoff at kf + δf √(|k|/kf) · exp(-(|k| - kf)² / 4δf²) if |k| < kf + δ_f, else 0\n\nIn every case the k = 0 mode is zero, and the full stored spectrum is rescaled so sum(Q̂ .* w) / (2M^2) == ε, where M = prod(field_size), w = 1 for :direct, and w = 1/|k|² for :vorticity_kinetic. A positive ε whose selected spectrum contains no representable nonzero mode raises ArgumentError. :isotropic aliases :ring, and :bandlimited aliases :band.","title":"StochasticForcing"},{"category":"section","location":"api/stochastic_forcing/#SeparableStochasticForcing","page":"Stochastic Forcing","text":"Use SeparableStochasticForcing for a Fourier–Chebyshev product domain. The random spectrum is defined only over the Fourier axes and is multiplied by a fixed Chebyshev profile:\n\nhat F(k_1ldotsk_dnt) = hatxi(k_1ldotsk_dt)p_n\n\nforcing = SeparableStochasticForcing(\n    fourier_size=(Nx,),\n    chebyshev_basis=zbasis,\n    chebyshev_profile=z -> z * (1 - z),\n    domain_size=(Lx,),\n    energy_injection_rate=0.1,\n    k_forcing=4.0,\n    dk_forcing=1.0,\n    dt=dt,\n    architecture=GPU(),\n)\nadd_stochastic_forcing!(problem, :b, forcing)\n\nchebyshev_profile can be either a function of the physical Chebyshev coordinate or a vector of Chebyshev coefficients whose length equals chebyshev_basis.meta.size. A function is sampled on the basis grid and transformed once during construction. In either form the profile is normalized to unit physical mean square with the basis quadrature weights. Non-finite, zero-norm, and mis-sized profiles are rejected.\n\nThe mixed normalization currently supports only injection_metric=:direct. Passing :vorticity_kinetic raises ArgumentError, because a kinetic-energy metric in a bounded direction requires model-specific inverse operators rather than the Fourier 1/|k|² weight.\n\nAll forcing arrays and outer-product views are persistent. With a supplied RNG, the Fourier phase draw is reproducible between CPU and GPU; the GPU path uses a persistent host staging array and copies into its persistent device phase array. Only Fourier axes may be shortened to real-FFT half spectra when the forcing is added to a field. The Chebyshev coefficient count must match exactly.\n\nFor a coupled Jacobi/Chebyshev GPU IVP, the default InitialValueSolver(...; matsolver=:auto) selects CuSparseLU. Its RHS, solution, and scratch device buffers are cached and reused across solves and same-size numeric refactors. An explicit CPU-only :sparse or :dense solver is rejected on that path. CPU and pure-Fourier IVPs retain the existing sparse default behavior.\n\nFor Fourier–Fourier domains, continue to use StochasticForcing; for Fourier–Chebyshev domains, use SeparableStochasticForcing.","title":"SeparableStochasticForcing"},{"category":"section","location":"api/stochastic_forcing/#DeterministicForcing","page":"Stochastic Forcing","text":"Deterministic (non-random) forcing.\n\nType signature:\n\nmutable struct DeterministicForcing{T<:AbstractFloat, N, A<:AbstractArray{T, N}} <: DeterministicForcingType\n\nFields:\n\nField Type Description\nforcing_function Function f(x, y, ..., t, params) → forcing array\nfield_size NTuple{N, Int} Grid size\ncached_forcing A Cached forcing values on the chosen architecture\nparameters Dict{Symbol, Any} Parameters for forcing function\narchitecture AbstractArchitecture CPU() or GPU() backend\n\nforcing.is_gpu is available as a property alias.\n\nConstructor:\n\nDeterministicForcing(\n    forcing_function::Function,\n    field_size::NTuple{N, Int};\n    parameters::AbstractDict{Symbol} = Dict{Symbol, Any}(),\n    dtype::Type{T} = Float64,\n    architecture::AbstractArchitecture = CPU()\n) where {T<:AbstractFloat, N}\n\nparameters may be any Dict keyed by Symbol (e.g. Dict(:A => 1.0, :k => 4.0)); it is copied into a Dict{Symbol, Any}.\n\nThe forcing function is called as forcing_function(grid..., t, parameters) and must return an array whose size is exactly field_size — a scalar-valued function raises ArgumentError: Deterministic forcing function must return an array, and a mis-shaped one raises ArgumentError: Deterministic forcing output size ... does not match field size .... Write it with broadcasting over the grid arrays:\n\nforcing = DeterministicForcing(\n    (x, y, t, p) -> p[:A] .* sin.(p[:k] .* x) .* cos.(y) .* cos(p[:omega] * t),\n    (16, 16);\n    parameters = Dict(:A => 1.0, :k => 4.0, :omega => 1.0),\n)\n\nx = reshape(range(0, 2π, length=17)[1:16], 16, 1)   # column vector\ny = reshape(range(0, 2π, length=17)[1:16], 1, 16)   # row vector\nF = generate_forcing!(forcing, (x, y), 0.0)         # 16×16 Matrix{Float64}\n\n","title":"DeterministicForcing"},{"category":"section","location":"api/stochastic_forcing/#Forcing-Generation","page":"Stochastic Forcing","text":"","title":"Forcing Generation"},{"category":"section","location":"api/stochastic_forcing/#generate_forcing!","page":"Stochastic Forcing","text":"Generate forcing realization. Returns cached value for substeps > 1.\n\nSignatures:\n\n# Stochastic forcing\ngenerate_forcing!(forcing::StochasticForcing, t::Real, substep::Int=1)\ngenerate_forcing!(forcing::StochasticForcing, t::Real)\ngenerate_forcing!(forcing::SeparableStochasticForcing, t::Real, substep::Int=1)\n\n# Deterministic forcing\ngenerate_forcing!(forcing::DeterministicForcing, grid, t::Real)\n\nKey behavior:\n\nsubstep == 1: Generates new random forcing\nsubstep > 1: Returns cached forcing (same as substep 1)\nTime check: Won't regenerate if t is exactly forcing.last_update_time\nThe k = 0 mode is always zeroed, and Hermitian symmetry is imposed when enforce_hermitian = true\nRequires forcing.dt > 0; otherwise an error is raised\n\nReturns: The cached forcing array forcing.cached_forcing","title":"generate_forcing!"},{"category":"section","location":"api/stochastic_forcing/#Automatic-registration-and-timestepper-compatibility","page":"Stochastic Forcing","text":"add_stochastic_forcing!(problem, variable, forcing) registers automatic RHS forcing. variable names a scalar field or a flattened vector/tensor component (for example :u_x). A container name such as :u is ambiguous and raises ArgumentError.\n\nOne forcing realization is reused across the stages of supported one-step RK/ETD methods; CNAB1 and SBDF1 are supported first-order methods. Tarang rejects CNAB2, MCNAB2, CNLF2, SBDF2, SBDF3, SBDF4, DiagonalIMEX_SBDF2, ETD_CNAB2, and ETD_SBDF2 before drawing forcing. Those schemes reuse or combine values across time levels, which colors white noise or gives it the wrong variance.","title":"Automatic registration and timestepper compatibility"},{"category":"section","location":"api/stochastic_forcing/#apply_forcing!","page":"Stochastic Forcing","text":"Add forcing to a field in spectral space.\n\nSignature:\n\napply_forcing!(\n    rhs::AbstractArray{Complex{T}, N},\n    forcing::StochasticForcing{T, N, A, CA},\n    t::Real,\n    substep::Int=1\n) where {T, N, A, CA}\n\nEquivalent to rhs .+= generate_forcing!(forcing, t, substep) when the shapes match exactly. When they do not, the forcing is first sliced to the region rhs covers — the leading half-spectrum for a real-FFT rhs, and this rank's local wavenumber block for a distributed PencilArray rhs. If no such slice exists, an ArgumentError is thrown.\n\n","title":"apply_forcing!"},{"category":"section","location":"api/stochastic_forcing/#Configuration","page":"Stochastic Forcing","text":"","title":"Configuration"},{"category":"section","location":"api/stochastic_forcing/#set_dt!","page":"Stochastic Forcing","text":"Update the timestep used for Stratonovich scaling.\n\nset_dt!(forcing::StochasticForcing{T, N, A, CA}, dt::Real) where {T, N, A, CA}\n\nCall this when dt changes (e.g., adaptive timestepping). If the value actually changes, the cache is zeroed and the next generate_forcing! call regenerates with the new √dt scaling.","title":"set_dt!"},{"category":"section","location":"api/stochastic_forcing/#reset_forcing!","page":"Stochastic Forcing","text":"Reset forcing cache. Forces regeneration on next generate_forcing! call.\n\nreset_forcing!(forcing::StochasticForcing{T, N, A, CA}) where {T, N, A, CA}\n\n","title":"reset_forcing!"},{"category":"section","location":"api/stochastic_forcing/#Work-Calculation","page":"Stochastic Forcing","text":"","title":"Work Calculation"},{"category":"section","location":"api/stochastic_forcing/#store_prevsol!","page":"Stochastic Forcing","text":"Store the current solution for Stratonovich work calculation.\n\nstore_prevsol!(forcing::StochasticForcing{T, N, A, CA}, sol::AbstractArray{Complex{T}, N})\n\nCall at the beginning of each timestep, before advancing.","title":"store_prevsol!"},{"category":"section","location":"api/stochastic_forcing/#work_stratonovich","page":"Stochastic Forcing","text":"Compute work done using Stratonovich interpretation.\n\nwork_stratonovich(forcing::StochasticForcing{T, N, A, CA},\n                  sol::AbstractArray{Complex{T}, N}) -> T\n\nFormula (with M = prod(field_size)):\n\nW = fracdtM^2textResum_k m_k w_k\n    fracq^n_k + q^n+1_k2 hatF_k^*\n\nUses midpoint evaluation and returns zero(T) unless store_prevsol! was called first. Here w_k is the selected injection-metric weight and m_k is the real-FFT multiplicity: 2 for an omitted conjugate partner, 1 for DC and an even-length Nyquist endpoint. For a full complex spectrum, every m_k = 1.","title":"work_stratonovich"},{"category":"section","location":"api/stochastic_forcing/#work_ito","page":"Stochastic Forcing","text":"Compute work done using Itô interpretation.\n\nwork_ito(forcing::StochasticForcing{T, N, A, CA},\n         sol_prev::AbstractArray{Complex{T}, N}) -> T\n\nFormula:\n\nW_textItô = fracdtM^2textResum_k m_k w_k q^n_k\nhatF_k^* + varepsilon dt\n\nUses the solution before the step (independent of the current forcing) plus the drift correction ε·dt, which makes ⟨WItô⟩ = ⟨WStratonovich⟩ = ε·dt.\n\nUnder MPI both work functions reduce over this rank's local slab and then Allreduce the partial sums, so every rank returns the same global value.\n\n","title":"work_ito"},{"category":"section","location":"api/stochastic_forcing/#Diagnostics","page":"Stochastic Forcing","text":"","title":"Diagnostics"},{"category":"section","location":"api/stochastic_forcing/#mean*energy*injection_rate","page":"Stochastic Forcing","text":"Return the target (mean) energy injection rate ε.\n\nmean_energy_injection_rate(forcing::StochasticForcing) -> T\nenergy_injection_rate(forcing::StochasticForcing) -> T      # same value","title":"meanenergyinjection_rate"},{"category":"section","location":"api/stochastic_forcing/#instantaneous_power","page":"Stochastic Forcing","text":"Compute the instantaneous power input.\n\ninstantaneous_power(forcing::StochasticForcing{T, N, A, CA},\n                    sol::AbstractArray{Complex{T}, N}) -> T\n\nFormula:\n\nP = frac1M^2textResum_k m_k w_k q_k hatF_k^*\n\nThis is the instantaneous correlation between solution and forcing, and it fluctuates. Its mean depends on which solution you pass: 0 for the solution before the forcing was applied, ε for the midpoint (ψⁿ + ψⁿ⁺¹)/2, and 2ε for the solution after. For the Stratonovich-consistent time-averaged power over a step, use work_stratonovich(forcing, sol) / forcing.dt instead.","title":"instantaneous_power"},{"category":"section","location":"api/stochastic_forcing/#forcing*enstrophy*injection_rate","page":"Stochastic Forcing","text":"Compute mean enstrophy injection rate (for 2D turbulence).\n\nforcing_enstrophy_injection_rate(forcing::StochasticForcing{T, N, A, CA}) -> T\n\nFormula:\n\neta = frac12M^2 sum_k hatQ(k)\n\nThis assumes the forcing is applied directly to 2-D vorticity. With injection_metric=:direct, η equals the configured ε. With :vorticity_kinetic, ε is kinetic-energy injection and η is generally of order k_forcing^2 * ε. For N != 2 the function warns and returns zero(T).","title":"forcingenstrophyinjection_rate"},{"category":"section","location":"api/stochastic_forcing/#get*forcing*spectrum","page":"Stochastic Forcing","text":"Return the forcing amplitude spectrum √Q̂(k), as the array type A of the chosen architecture.\n\nget_forcing_spectrum(forcing::StochasticForcing) -> A","title":"getforcingspectrum"},{"category":"section","location":"api/stochastic_forcing/#get*cached*forcing","page":"Stochastic Forcing","text":"Return the current cached forcing F̂(k), as the complex array type CA of the chosen architecture.\n\nget_cached_forcing(forcing::StochasticForcing) -> CA\nget_forcing_real(forcing::StochasticForcing)   -> real part of F̂(k)\n\n","title":"getcachedforcing"},{"category":"section","location":"api/stochastic_forcing/#Spectrum-Building-Utilities","page":"Stochastic Forcing","text":"These are exported so that you can inspect or build custom forcing spectra.","title":"Spectrum Building Utilities"},{"category":"section","location":"api/stochastic_forcing/#build_wavenumbers","page":"Stochastic Forcing","text":"Build wavenumber arrays for each dimension, in standard FFT ordering (0, dk, ..., k_nyq, -(k_nyq - dk), ..., -dk with dk = 2π/L).\n\nbuild_wavenumbers(\n    field_size::NTuple{N, Int},\n    domain_size::NTuple{N, Real},\n    dtype::Type{T}\n) -> NTuple{N, Vector{T}}\n\nFor example, build_wavenumbers((8,), (2π,), Float64)[1] is [0.0, 1.0, 2.0, 3.0, 4.0, -3.0, -2.0, -1.0].","title":"build_wavenumbers"},{"category":"section","location":"api/stochastic_forcing/#compute*forcing*spectrum","page":"Stochastic Forcing","text":"Compute the forcing amplitude spectrum √Q̂(k).\n\ncompute_forcing_spectrum(\n    wavenumbers::NTuple{N, Vector{T}},\n    k_f::Real,\n    dk_f::Real,\n    ε::Real,\n    domain_size::NTuple{N, Real},\n    spectrum_type::Symbol,\n    dtype::Type{T};\n    injection_metric::Symbol = :direct\n) -> Array{T, N}\n\nThe full spectrum is normalized so that sum(abs2(spectrum[k]) * w[k]) / (2 * prod(field_size)^2) == ε, with the metric weights defined above. compute_forcing_spectrum uses the supplied wavenumbers; the constructor obtains those wavenumbers from domain_size before calling it.\n\n","title":"computeforcingspectrum"},{"category":"section","location":"api/stochastic_forcing/#Worked-Example","page":"Stochastic Forcing","text":"A complete stochastically forced integration in Fourier space, with the work diagnostics.\n\nusing Tarang, Random\n\nforcing = StochasticForcing(\n    field_size = (32, 32),\n    domain_size = (2π, 2π),\n    energy_injection_rate = 0.1,\n    k_forcing = 6.0,\n    dk_forcing = 1.5,\n    dt = 1e-3,\n    spectrum_type = :ring,\n    rng = MersenneTwister(7),\n)\n\n# Euler-Maruyama in spectral space: ψⁿ⁺¹ = ψⁿ + F̂ dt\nfunction integrate!(forcing, nsteps)\n    ψ = zeros(ComplexF64, forcing.field_size...)\n    W = 0.0\n    for n in 1:nsteps\n        t = n * forcing.dt\n        store_prevsol!(forcing, ψ)              # ψⁿ, BEFORE advancing\n        F = generate_forcing!(forcing, t, 1)\n        ψ .+= F .* forcing.dt\n        W += work_stratonovich(forcing, ψ)      # work injected over this step\n    end\n    return ψ, W\nend\n\nψ, W = integrate!(forcing, 1000)\n\n@show mean_energy_injection_rate(forcing)       # 0.1, the target ε\n@show W / (1000 * forcing.dt)                   # the realized injection rate\n@show forcing_enstrophy_injection_rate(forcing)\n\nWith this seed the run prints\n\nmean_energy_injection_rate(forcing) = 0.1\nW / (1000 * forcing.dt) = 0.11453581797243557\nforcing_enstrophy_injection_rate(forcing) = 3.937500006838822\n\nThe realized rate scatters around the target ε: a single 1000-step realization is a random variable, not the ensemble mean, so expect it to land within a few tens of percent of ε and to tighten as the run gets longer.\n\nInside a multi-stage timestepper, pass the substep index so the realization is held fixed across the stages, and call set_dt! whenever the timestep changes:\n\nrhs = zeros(ComplexF64, 32, 32)\nfor substep in 1:3\n    apply_forcing!(rhs, forcing, 2.0, substep)   # substeps 2 and 3 reuse the cached F̂\nend\n\nset_dt!(forcing, 5e-4)                           # zeroes the cache; next draw rescales by 1/√dt\n\n","title":"Worked Example"},{"category":"section","location":"api/stochastic_forcing/#Exports","page":"Stochastic Forcing","text":"export Forcing, StochasticForcingType, DeterministicForcingType\nexport StochasticForcing, DeterministicForcing\nexport generate_forcing!, apply_forcing!\nexport reset_forcing!, set_dt!\nexport store_prevsol!, work_stratonovich, work_ito\nexport mean_energy_injection_rate, energy_injection_rate, instantaneous_power\nexport forcing_enstrophy_injection_rate\nexport get_forcing_spectrum, get_cached_forcing, get_forcing_real\nexport build_wavenumbers, compute_forcing_spectrum\n\n","title":"Exports"},{"category":"section","location":"api/stochastic_forcing/#Index","page":"Stochastic Forcing","text":"Pages = [\"stochastic_forcing.md\"]","title":"Index"},{"category":"type","location":"api/stochastic_forcing/#Tarang.StochasticForcing","page":"Stochastic Forcing","text":"StochasticForcing{T, N, A<:AbstractArray{T,N}, CA<:AbstractArray{Complex{T},N}}\n\nStochastic forcing in Fourier space with white-noise temporal correlation. Supports both CPU and GPU architectures.\n\nMathematical Properties\n\nThe forcing F̂(k,t) satisfies:\n\n⟨F̂(k,t)⟩ = 0                                          (zero mean)\n⟨F̂(k,t) F̂*(k',t')⟩ = Q̂(k) δ(k-k') δ(t-t')/(dt)     (white noise)\n\nImplementation\n\nAt each timestep, the forcing is computed as:     F̂(k) = √(Q̂(k)) · ξ(k) / √(dt)\n\nwhere ξ(k) is complex white noise with |ξ| = 1 and random phase.\n\nFields\n\nforcing_spectrum::A: √Q̂(k) - square root of power spectrum\nenergy_injection_rate::T: Target energy injection rate ε\ninjection_metric::Symbol: Quadratic invariant used to normalize ε\nk_forcing::T: Central forcing wavenumber k_f\ndk_forcing::T: Forcing bandwidth δ_f\ndt::T: Current timestep (for proper scaling)\ndomain_size::NTuple{N,T}: Domain size (Lx, Ly, ...)\nfield_size::NTuple{N,Int}: Grid size (Nx, Ny, ...)\nwavenumbers::NTuple{N,Vector{T}}: Wavenumber arrays (kx, ky, ...)\ncached_forcing::AbstractArray{Complex{T},N}: Cached forcing (constant within timestep)\nprevsol::Union{Nothing,AbstractArray{Complex{T},N}}: Previous solution (for Stratonovich work)\nrng::AbstractRNG: Random number generator (default: fresh MersenneTwister per instance for thread/parallel safety)\nrandom_phases::AbstractArray{T,N}: Pre-allocated random phase buffer (on target architecture)\ndiagnostic_weights: Cached backend weights for the active GPU diagnostic layout\nlast_update_time::T: Time of last forcing update\nspectrum_type::Symbol: Type of forcing spectrum\nenforce_hermitian::Bool: Enforce Hermitian symmetry for real-valued fields\narchitecture::AbstractArchitecture: CPU() or GPU() architecture\n\n\n\n\n\n","title":"Tarang.StochasticForcing"},{"category":"type","location":"api/stochastic_forcing/#Tarang.SeparableStochasticForcing","page":"Stochastic Forcing","text":"SeparableStochasticForcing\n\nStochastic forcing for a Fourier product domain with one trailing Chebyshev dimension. Randomness and spectral localization live only in the Fourier dimensions; chebyshev_profile supplies the fixed Chebyshev dependence.\n\n\n\n\n\n","title":"Tarang.SeparableStochasticForcing"},{"category":"type","location":"api/stochastic_forcing/#Tarang.DeterministicForcing","page":"Stochastic Forcing","text":"DeterministicForcing{T, N, A<:AbstractArray{T,N}}\n\nDeterministic (non-random) forcing.\n\nThe forcing function is called as forcing_function(grid..., t, parameters) and must return an array whose size is exactly field_size, so write it with broadcasting over the grid arrays.\n\nExample\n\n# Sinusoidal forcing on a 16×16 grid\nforcing = DeterministicForcing(\n    (x, y, t, p) -> p[:A] .* sin.(p[:k] .* x) .* cos.(y) .* cos(p[:omega] * t),\n    (16, 16);\n    parameters = Dict(:A => 1.0, :k => 4.0, :omega => 1.0)\n)\n\nx = reshape(range(0, 2π, length=17)[1:16], 16, 1)   # column vector\ny = reshape(range(0, 2π, length=17)[1:16], 1, 16)   # row vector\nF = generate_forcing!(forcing, (x, y), 0.0)         # 16×16 Matrix{Float64}\n\n\n\n\n\n","title":"Tarang.DeterministicForcing"},{"category":"function","location":"api/stochastic_forcing/#Tarang.generate_forcing!","page":"Stochastic Forcing","text":"generate_forcing!(forcing::StochasticForcing, t::Real, substep::Int=1)\n\nGenerate stochastic forcing at time t. Works on both CPU and GPU.\n\nKey Points\n\nForcing is regenerated only on substep 1 - ensures forcing stays constant within a timestep for IMEX and multi-stage methods.\nScaling: F̂(k) = √Q̂(k) · exp(i·φ) / √dt, where φ ∼ Uniform[0, 2π) (generated by _fill_random_phases!)\nZero mean: The k=0 mode is always set to zero.\nHermitian symmetry (optional): F̂(-k) = F̂(k)* so that ifft produces real fields.\nGPU support: Random phases are generated on the target architecture when possible, then combined with the spectrum using broadcasted operations.\n\nArguments\n\nforcing: StochasticForcing configuration\nt: Current simulation time\nsubstep: Current substep (1 for first substep)\n\nReturns\n\nThe cached forcing array (modified in-place).\n\n\n\n\n\ngenerate_forcing!(forcing::StochasticForcing, t::Real)\n\nGenerate forcing without substep tracking. Equivalent to substep=1.\n\n\n\n\n\ngenerate_forcing!(forcing::DeterministicForcing, grid, t::Real)\n\nEvaluate deterministic forcing at time t on the given grid.\n\n\n\n\n\n","title":"Tarang.generate_forcing!"},{"category":"function","location":"api/stochastic_forcing/#Tarang.apply_forcing!","page":"Stochastic Forcing","text":"apply_forcing!(rhs::AbstractArray, forcing::StochasticForcing, t::Real, substep::Int=1)\n\nAdd stochastic forcing to the RHS in spectral space. Works on both CPU and GPU.\n\nArguments\n\nrhs: Right-hand side array (modified in-place)\nforcing: StochasticForcing configuration\nt: Current simulation time\nsubstep: Current substep number\n\n\n\n\n\n","title":"Tarang.apply_forcing!"},{"category":"function","location":"api/stochastic_forcing/#Tarang.set_dt!","page":"Stochastic Forcing","text":"set_dt!(forcing::StochasticForcing, dt::Real)\n\nUpdate the timestep for Stratonovich scaling.\n\nCall this when dt changes (e.g., adaptive timestepping).\n\n\n\n\n\n","title":"Tarang.set_dt!"},{"category":"function","location":"api/stochastic_forcing/#Tarang.reset_forcing!","page":"Stochastic Forcing","text":"reset_forcing!(forcing::StochasticForcing)\n\nReset the forcing cache, causing regeneration on next call. Works on both CPU and GPU.\n\n\n\n\n\n","title":"Tarang.reset_forcing!"},{"category":"function","location":"api/stochastic_forcing/#Tarang.store_prevsol!","page":"Stochastic Forcing","text":"store_prevsol!(forcing::StochasticForcing, sol::AbstractArray)\n\nStore the current solution for Stratonovich work calculation. Works on both CPU and GPU arrays.\n\nCall this at the beginning of each timestep, before advancing.\n\n\n\n\n\n","title":"Tarang.store_prevsol!"},{"category":"function","location":"api/stochastic_forcing/#Tarang.work_stratonovich","page":"Stochastic Forcing","text":"work_stratonovich(forcing::StochasticForcing, sol::AbstractArray)\n\nCompute work done by forcing using Stratonovich interpretation. Works on both CPU and GPU arrays.\n\nFormula\n\nW = Re⟨(ψⁿ + ψⁿ⁺¹)/2 · ΔF̂*⟩\n\nwhere ΔF̂ = √Q̂ · ξ · √dt is the forcing increment over dt.\n\nThis correctly accounts for the correlation between forcing and response.\n\nArguments\n\nforcing: StochasticForcing with prevsol stored\nsol: Current solution ψⁿ⁺¹\n\nReturns\n\nWork done during this timestep (scalar, units of energy).\n\n\n\n\n\n","title":"Tarang.work_stratonovich"},{"category":"function","location":"api/stochastic_forcing/#Tarang.work_ito","page":"Stochastic Forcing","text":"work_ito(forcing::StochasticForcing, sol::AbstractArray)\n\nCompute work done by forcing using Itô interpretation. Works on both CPU and GPU arrays.\n\nFormula\n\nW_Itô = Re⟨ψⁿ · ΔF̂*⟩ + ε · dt\n\nwhere ΔF̂ = √Q̂ · ξ · √dt is the forcing increment.\n\nThe drift correction ε · dt accounts for the Itô-Stratonovich conversion. In Itô calculus, ψⁿ is independent of Fⁿ⁺¹, so ⟨ψⁿ · ΔF̂⟩ = 0. The drift ensures ⟨WItô⟩ = ⟨WStratonovich⟩ = ε · dt.\n\n\n\n\n\n","title":"Tarang.work_ito"},{"category":"function","location":"api/stochastic_forcing/#Tarang.mean_energy_injection_rate","page":"Stochastic Forcing","text":"mean_energy_injection_rate(forcing::StochasticForcing)\n\nReturn the target (mean) energy injection rate ε.\n\nThis is the ensemble average of work done per unit time.\n\n\n\n\n\n","title":"Tarang.mean_energy_injection_rate"},{"category":"function","location":"api/stochastic_forcing/#Tarang.instantaneous_power","page":"Stochastic Forcing","text":"instantaneous_power(forcing::StochasticForcing, sol::AbstractArray)\n\nCompute instantaneous power input from the Parseval-normalized spectral pairing. Works on both CPU and GPU arrays.\n\nThis is the correlation between the solution and forcing at a given instant. For white noise forcing, the expected value depends on which solution is passed:\n\n⟨P⟩ = 0 if sol is the solution BEFORE the forcing was applied (independent)\n⟨P⟩ = ε if sol is the MIDPOINT (ψⁿ + ψⁿ⁺¹)/2\n⟨P⟩ = 2ε if sol is the solution AFTER forcing (includes full response)\n\nNote: For the Stratonovich-consistent time-averaged power over the timestep, use work_stratonovich(forcing, sol) / forcing.dt instead.\n\nReturns\n\nInstantaneous power (energy per unit time).\n\n\n\n\n\n","title":"Tarang.instantaneous_power"},{"category":"function","location":"api/stochastic_forcing/#Tarang.forcing_enstrophy_injection_rate","page":"Stochastic Forcing","text":"forcing_enstrophy_injection_rate(forcing::StochasticForcing)\n\nCompute mean enstrophy injection rate (for 2D turbulence).\n\nThis assumes the stochastic forcing is applied directly to vorticity. With M = prod(field_size) and Tarang's unnormalized FFT convention,\n\nη = ∑_k Q̂(k) / (2M²)\n\nFor injection_metric=:direct, this equals the configured energy injection rate. For :vorticity_kinetic, it is generally of order k_forcing² times the configured kinetic-energy injection rate.\n\n\n\n\n\n","title":"Tarang.forcing_enstrophy_injection_rate"},{"category":"function","location":"api/stochastic_forcing/#Tarang.get_forcing_spectrum","page":"Stochastic Forcing","text":"get_forcing_spectrum(forcing::StochasticForcing)\n\nReturn the forcing amplitude spectrum √Q̂(k).\n\n\n\n\n\n","title":"Tarang.get_forcing_spectrum"},{"category":"function","location":"api/stochastic_forcing/#Tarang.get_cached_forcing","page":"Stochastic Forcing","text":"get_cached_forcing(forcing::StochasticForcing)\n\nReturn the current cached forcing F̂(k).\n\n\n\n\n\nget_cached_forcing(state::TimestepperState)\n\nGet the cached forcing array. Returns nothing if no forcing is configured.\n\n\n\n\n\n","title":"Tarang.get_cached_forcing"},{"category":"function","location":"api/stochastic_forcing/#Tarang.build_wavenumbers","page":"Stochastic Forcing","text":"build_wavenumbers(field_size, domain_size, dtype)\n\nBuild wavenumber arrays for each dimension.\n\n\n\n\n\n","title":"Tarang.build_wavenumbers"},{"category":"function","location":"api/stochastic_forcing/#Tarang.compute_forcing_spectrum","page":"Stochastic Forcing","text":"compute_forcing_spectrum(wavenumbers, k_f, dk_f, ε, domain_size, spectrum_type, dtype;\n                         injection_metric=:direct)\n\nCompute the forcing amplitude spectrum √Q̂(k). With M physical grid points, the full spectrum is normalized so sum(Q̂(k) * w(k)) / (2M²) == ε, where w=1 for :direct and w=1/|k|² for :vorticity_kinetic. A positive ε whose selected band contains no representable nonzero mode raises ArgumentError.\n\n\n\n\n\n","title":"Tarang.compute_forcing_spectrum"},{"category":"section","location":"pages/bases/#Spectral-Bases","page":"Bases","text":"Spectral bases define how functions are represented in each coordinate direction.","title":"Spectral Bases"},{"category":"section","location":"pages/bases/#Basis-Selection-Guide","page":"Bases","text":"Boundary Type Recommended Basis Grid Points\nPeriodic RealFourier Uniform\nNon-periodic (walls) ChebyshevT Gauss-Lobatto (endpoints included)\nNon-periodic (alternative) ChebyshevU / Legendre Gauss (endpoints excluded)","title":"Basis Selection Guide"},{"category":"section","location":"pages/bases/#Fourier-Bases","page":"Bases","text":"","title":"Fourier Bases"},{"category":"section","location":"pages/bases/#RealFourier","page":"Bases","text":"For periodic directions with real-valued data.\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\")\nx = coords[\"x\"]\n\n# Basic usage (dealias defaults to 3/2)\nbasis = RealFourier(x; size=128, bounds=(0.0, 2π))\n\n# Explicit dealiasing factor\nbasis = RealFourier(x; size=128, bounds=(0.0, 2π), dealias=3/2)\n\nParameters:\n\nsize: Number of modes (should be even)\nbounds: Domain boundaries (min, max)\ndealias: Padding factor, 3/2 by default (see the Dealiasing section below)\n\nProperties live under basis.meta; the basis object itself has no size/bounds fields (basis.size throws a FieldError):\n\nbasis.meta.size           # 128         number of modes\nbasis.meta.bounds         # (0.0, 2π)   (min, max)\nbasis.meta.dealias        # 1.5         padding factor\nbasis.meta.element_label  # \"x\"         coordinate name\nbasis.meta.dtype          # Float64\n\nwavenumbers(basis)        # physical wavenumbers k = 2πn/L","title":"RealFourier"},{"category":"section","location":"pages/bases/#ComplexFourier","page":"Bases","text":"For complex-valued data or full complex FFT.\n\nbasis = ComplexFourier(x; size=128, bounds=(0.0, 2π))","title":"ComplexFourier"},{"category":"section","location":"pages/bases/#Chebyshev-Bases","page":"Bases","text":"","title":"Chebyshev Bases"},{"category":"section","location":"pages/bases/#ChebyshevT","page":"Bases","text":"Chebyshev polynomials of the first kind. Best for bounded domains with walls.\n\ncoords = CartesianCoordinates(\"z\")\nz = coords[\"z\"]\n\nbasis = ChebyshevT(z; size=64, bounds=(0.0, 1.0))\n\nGrid Points: Chebyshev-Gauss-Lobatto points, clustered near boundaries and including both endpoints (so wall boundary conditions sit exactly on a grid point).\n\n# Native grid in [-1, 1], ascending:\n# ξ_k = -cos(πk / (N-1)),  k = 0, 1, ..., N-1\n\n# Mapped to [a, b]:\n# z_k = (a+b)/2 + (b-a)/2 * ξ_k","title":"ChebyshevT"},{"category":"section","location":"pages/bases/#ChebyshevU","page":"Bases","text":"Chebyshev polynomials of the second kind. Grid excludes endpoints (ξ_k = -cos(π(k+1/2)/N)).\n\nbasis = ChebyshevU(z; size=64, bounds=(0.0, 1.0))","title":"ChebyshevU"},{"category":"section","location":"pages/bases/#Legendre-Basis","page":"Bases","text":"Alternative to Chebyshev for bounded domains. Grid points are the Gauss-Legendre nodes (roots of P_N), which also exclude the endpoints.\n\nbasis = Legendre(z; size=64, bounds=(0.0, 1.0))\n\nWhen to use:\n\nSome problems with specific weight functions\nAlternative numerical properties\nGenerally similar to ChebyshevT\n\nNote that a Legendre derivative inside an explicit (RHS) term is not compiled by the lazy RHS — see Solvers. Keep non-Fourier derivatives on the implicit side.","title":"Legendre Basis"},{"category":"section","location":"pages/bases/#Resolution-Guidelines","page":"Bases","text":"","title":"Resolution Guidelines"},{"category":"section","location":"pages/bases/#Fourier-Resolution","page":"Bases","text":"# Rule of thumb: resolve the smallest scales\nL = 2π                      # Domain length\nν, ε = 1e-3, 0.1            # Viscosity, dissipation rate\nη = (ν^3 / ε)^(1/4)         # Kolmogorov scale (turbulence)\nN = ceil(Int, L / η)        # Required modes (minimum)\n\nFlow Type Typical Resolution\nLaminar 64-128\nTransitional 128-256\nTurbulent 256-1024+","title":"Fourier Resolution"},{"category":"section","location":"pages/bases/#Chebyshev-Resolution","page":"Bases","text":"Feature Typical Modes\nSmooth profiles 32-64\nBoundary layers 64-128\nSteep gradients 128-256","title":"Chebyshev Resolution"},{"category":"section","location":"pages/bases/#Dealiasing","page":"Bases","text":"Prevents aliasing errors in nonlinear terms. dealias is a padding factor, not a cutoff fraction: values > 1 dealias, values <= 1 switch dealiasing off.","title":"Dealiasing"},{"category":"section","location":"pages/bases/#3/2-Rule","page":"Bases","text":"The default for RealFourier and ComplexFourier:\n\nbasis = RealFourier(x; size=128, dealias=3/2)\n\nNonlinear products are evaluated on a grid padded to ceil(3/2 * 128) = 192 points and truncated back to the 128-mode representation. Under MPI the equivalent 2/3-rule truncation is applied instead: modes with |k| > N / (2 * dealias) are zeroed.","title":"3/2 Rule"},{"category":"section","location":"pages/bases/#2/1-Rule","page":"Bases","text":"For highly nonlinear problems (padded grid of 256 points at size=128):\n\nbasis = RealFourier(x; size=128, dealias=2.0)","title":"2/1 Rule"},{"category":"section","location":"pages/bases/#No-Dealiasing","page":"Bases","text":"For linear problems only:\n\nbasis = RealFourier(x; size=128, dealias=1.0)","title":"No Dealiasing"},{"category":"section","location":"pages/bases/#Cutoffs-at-size-32","page":"Bases","text":"Retained modes per axis, from min(floor(N / (2*dealias)), (N-1) ÷ 3):\n\ndealias Cutoff Effect\n3/2 10 3/2 rule (default)\n2.0 8 Stronger truncation\n1.0 – Dealiasing off\n2/3 – Dealiasing off — 2/3 is a factor below 1, not the 2/3 rule\n\nChebyshev/Legendre axes ignore dealias (no cutoff is applied on a non-Fourier axis); passing it there is harmless but does nothing.","title":"Cutoffs at size = 32"},{"category":"section","location":"pages/bases/#Multi-Dimensional-Domains","page":"Bases","text":"Combine bases for each direction:\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords; dtype=Float64, device=CPU())\n\n# Periodic x, bounded z\nx_basis = RealFourier(coords[\"x\"]; size=256, bounds=(0.0, 4.0))\nz_basis = ChebyshevT(coords[\"z\"]; size=64, bounds=(0.0, 1.0))\n\n# Create domain\ndomain = Domain(dist, (x_basis, z_basis))\n\nUnder MPI, two rules apply (see Parallelism):\n\nA Chebyshev/Legendre axis must come first, because the trailing axes are the ones that get decomposed and a non-Fourier axis cannot be split across ranks. Ordering it last raises a clear error.\nA 2-D domain takes a 1-D process mesh (mesh=(nprocs,)); a 3-D domain takes a 2-D mesh. Omitting mesh= lets the distributor pick a valid one. A mesh whose size does not match the process count is rejected (mesh=(2,2) with one process throws ArgumentError: Mesh size 4 does not match number of processes 1).","title":"Multi-Dimensional Domains"},{"category":"section","location":"pages/bases/#Grid-and-Spectral-Space","page":"Bases","text":"","title":"Grid and Spectral Space"},{"category":"section","location":"pages/bases/#Transforms","page":"Bases","text":"# A new field starts in grid layout\nfield = ScalarField(dist, \"T\", (x_basis, z_basis), Float64)\n\n# Transform to grid space (no-op if already there)\nensure_layout!(field, :g)\n\n# Access grid data\ndata = get_grid_data(field)\n\n# Transform to spectral space\nensure_layout!(field, :c)\ncoeffs = get_coeff_data(field)\n\nFor a RealFourier(size=32) × ChebyshevT(size=16) domain the grid data is 32 × 16 and the coefficient data is 17 × 16 (the real FFT halves the first axis). The grid is not padded by dealias — padding happens inside the nonlinear-product evaluation only.","title":"Transforms"},{"category":"section","location":"pages/bases/#Grid-Points","page":"Bases","text":"local_grids returns each rank's local slice of the per-axis grids, so the same code is correct in serial and under MPI:\n\nx_grid, z_grid = local_grids(dist, x_basis, z_basis)\n\nensure_layout!(field, :g)\nget_grid_data(field) .= sin.(2π .* x_grid ./ 4.0) .* z_grid'\nensure_layout!(field, :c)\n\ncreate_meshgrid builds full-rank meshgrid arrays (one array per coordinate, each of the global grid shape), which is convenient in serial post-processing:\n\nmesh = create_meshgrid(domain; on_device=false)   # Dict(\"x\" => …, \"z\" => …)\nX, Z = mesh[\"x\"], mesh[\"z\"]                       # each 256 × 64 here\n\nBecause these are global arrays, use local_grids (not create_meshgrid) whenever the result is broadcast into field data under MPI.","title":"Grid Points"},{"category":"section","location":"pages/bases/#Performance-Tips","page":"Bases","text":"Power-of-2 sizes: Use N = 2^m for fastest FFTs\nDealiasing cost: the padded nonlinear transform costs ~(3/2)^d more work in d dealiased directions\nChebyshev efficiency: Sparse differentiation matrices\nMemory: RealFourier stores N/2+1 complex coefficients where ComplexFourier stores N","title":"Performance Tips"},{"category":"section","location":"pages/bases/#See-Also","page":"Bases","text":"Coordinates: Coordinate systems\nDomains: Creating full domains\nAPI: Bases: Complete reference","title":"See Also"},{"category":"section","location":"pages/operators/#Operators","page":"Operators","text":"Operators perform mathematical operations on fields, including differentiation and vector calculus.","title":"Operators"},{"category":"section","location":"pages/operators/#Differential-Operators","page":"Operators","text":"","title":"Differential Operators"},{"category":"section","location":"pages/operators/#First-Derivatives","page":"Operators","text":"∂x, ∂y, ∂z are equation-string syntax: the parser reads ∂<name> as a derivative along the coordinate named <name>. They are not Julia functions — a bare ∂x(field) in Julia code raises UndefVarError.\n\n# Syntax in equations: ∂<coordinate name>\nadd_equation!(problem, \"∂t(T) = -ux*∂x(T) - uz*∂z(T)\")\n\nIn Julia code, use the exported d constructor, which takes a Coordinate:\n\nd(T, coords[\"x\"])      # ∂T/∂x\nd(T, coords[\"x\"], 2)   # ∂²T/∂x²","title":"First Derivatives"},{"category":"section","location":"pages/operators/#Second-Derivatives","page":"Operators","text":"# Composed derivatives (equation-string syntax)\nadd_equation!(problem, \"∂t(T) - kappa*(∂x(∂x(T)) + ∂z(∂z(T))) = 0\")\n\n# ∂x(∂x(f))   ∂²/∂x²\n# ∂z(∂z(f))   ∂²/∂z²\n# ∂x(∂z(f))   ∂²/∂x∂z","title":"Second Derivatives"},{"category":"section","location":"pages/operators/#Laplacian","page":"Operators","text":"Δ (alias lap, ∇²) works both in equation strings and in Julia code.\n\n# Δ(f) = ∇²f\nΔ(field)\n\n# Example: Diffusion equation\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = 0\")","title":"Laplacian"},{"category":"section","location":"pages/operators/#Fractional-Laplacian","page":"Operators","text":"The fractional Laplacian (-Δ)^α generalizes the Laplacian to non-integer powers:\n\n# fraclap(f, α) = (-Δ)^α f  — Julia code\nfraclap(field, 0.5)    # Square root: (-Δ)^(1/2)\nfraclap(field, -0.5)   # Inverse square root: (-Δ)^(-1/2)\nfraclap(field, 1.0)    # Standard Laplacian: (-Δ)^1 = -Δ\nfraclap(field, 2.0)    # Biharmonic: (-Δ)^2 = Δ²\n\n# Convenience functions (Julia code only — see the note below)\nsqrtlap(field)         # Same as fraclap(f, 0.5)\ninvsqrtlap(field)      # Same as fraclap(f, -0.5)\n\nIn spectral space: Multiplication by |k|^(2α) where k is the wavenumber.\n\nApplications:\n\nSQG dynamics: ψ = (-Δ)^(-1/2) θ for streamfunction from buoyancy\nFractional diffusion: ∂θ/∂t = -κ(-Δ)^α θ for anomalous diffusion\nHyperviscosity: (-Δ)^n for numerical dissipation at small scales\n\nInside an equation string, write fraclap(f, α) (or the Unicode alias Δᵅ(f, α)). Put the term on the implicit (LHS) side: the compiled RHS declines a fractional Laplacian and falls back to the slower interpreted path if it appears on the right.\n\n# Buoyancy with fractional dissipation, treated implicitly\nadd_equation!(problem, \"∂t(θ) + κ*fraclap(θ, 0.5) = -u⋅∇(θ)\")\n\nwarning: `sqrtlap` does not parse inside an equation string\nsqrtlap(f) in an add_equation! string hits a parser bug, and the whole term is silently dropped from the equation (you get a one-time warning and an UnknownOperator placeholder). Write fraclap(f, 0.5) instead. invsqrtlap(f) and fraclap(f, -0.5) both parse correctly.","title":"Fractional Laplacian"},{"category":"section","location":"pages/operators/#Hyperviscosity-(Higher-Order-Laplacian)","page":"Operators","text":"For turbulence simulations, hyperviscosity provides selective dissipation at small scales while preserving large-scale dynamics:\n\n# General form (Julia code): hyperlap(f, n) = (-Δ)^n = |k|^(2n) in Fourier space\nhyperlap(field, 2)   # Biharmonic: (-Δ)² = |k|⁴\nhyperlap(field, 4)   # 8th-order: (-Δ)⁴ = |k|⁸\nhyperlap(field, 8)   # 16th-order: (-Δ)⁸ = |k|¹⁶\n\n# Unicode shortcuts — these are the forms to use inside equation strings\nΔ²(field)   # Biharmonic (4th-order derivative)\nΔ⁴(field)   # 8th-order derivative\nΔ⁶(field)   # 12th-order derivative\nΔ⁸(field)   # 16th-order derivative\n\nIn spectral space: Multiplication by |k|^(2n) - very efficient for Fourier bases.\n\nUsage in equations:\n\n# 2D turbulence with biharmonic hyperviscosity\nadd_equation!(problem, \"∂t(ω) + ν₄*Δ²(ω) = -u⋅∇(ω)\")\n\n# Incompressible turbulence with 8th-order hyperviscosity\nadd_equation!(problem, \"∂t(u) + ∇(p) + ν₈*Δ⁴(u) = -u⋅∇(u)\")\nadd_equation!(problem, \"div(u) = 0\")\n\nwarning: `hyperlap(f, n)` does not parse inside an equation string\nhyperlap requires an Integer order, but the equation parser turns every numeric literal into a Float64. hyperlap(u, 4) inside an add_equation! string therefore fails to build and the hyperviscosity term is silently dropped from the equation. Use Δ²/Δ⁴/Δ⁶/Δ⁸ in equation strings, and reserve hyperlap(f, n) for direct Julia calls.\n\nWhy use hyperviscosity?\n\nOrder Operator Spectral Use Case\n2 Δ -k² Standard viscosity\n4 Δ² k⁴ Mild scale separation\n8 Δ⁴ k⁸ Strong scale separation\n16 Δ⁸ k¹⁶ Extreme Reynolds numbers\n\nHigher orders concentrate dissipation at the smallest resolved scales, extending the inertial range.\n\nTyping Unicode:\n\nSymbol Type\nΔ² \\Delta + Tab, \\^2 + Tab\nΔ⁴ \\Delta + Tab, \\^4 + Tab\nΔ⁶ \\Delta + Tab, \\^6 + Tab\nΔ⁸ \\Delta + Tab, \\^8 + Tab","title":"Hyperviscosity (Higher-Order Laplacian)"},{"category":"section","location":"pages/operators/#Tau-Method-Operators","page":"Operators","text":"These operators support the first-order formulation (tau method) for problems with Chebyshev bases, where explicit tau lifting is required for correct boundary condition enforcement.","title":"Tau Method Operators"},{"category":"section","location":"pages/operators/#trace(expr)","page":"Operators","text":"Algebraic trace reduction: converts a first-order gradient variable (a rank-2 tensor or matrix expression) into its trace, i.e., the sum of diagonal components. Used in the first-order formulation to replace div(u) = 0 with the trace of the velocity gradient:\n\n# In the first-order formulation, use trace instead of div on the gradient variable\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")","title":"trace(expr)"},{"category":"section","location":"pages/operators/#derivative_basis(basis,-order)","page":"Operators","text":"Returns the order-th derivative basis associated with basis. For Chebyshev polynomials, differentiating once maps ChebyshevT to ChebyshevU:\n\n# Get the first-derivative basis for the vertical Chebyshev basis\nlift_basis = derivative_basis(zbasis, 1)\n\nThis basis is then passed to lift to construct the tau lifting operator.","title":"derivative_basis(basis, order)"},{"category":"section","location":"pages/operators/#lift(operand,-basis,-n)","page":"Operators","text":"The tau method lifting operator. Embeds operand (a tau variable) into the full spectral space using basis, selecting the n-th mode from the end (n = -1 selects the last mode):\n\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A) = lift(A, lift_basis, -1)\n\n# Apply lifting to tau variables in first-order gradient definitions\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_b = grad(b) + ez * τ_lift(tau_b1)\n\n# Tau variables also appear in the equations themselves\nadd_equation!(problem, \"∂t(b) - kappa*div(grad_b) + τ_lift(tau_b2) = -u⋅∇(b)\")","title":"lift(operand, basis, n)"},{"category":"section","location":"pages/operators/#Vector-Calculus","page":"Operators","text":"","title":"Vector Calculus"},{"category":"section","location":"pages/operators/#Gradient","page":"Operators","text":"Converts scalar to vector field.\n\n# grad(p) = ∇p\nadd_equation!(problem, \"∂t(u) + grad(p) = 0\")\n\n# Components:\n# ∂p/∂x, ∂p/∂y, ∂p/∂z","title":"Gradient"},{"category":"section","location":"pages/operators/#Divergence","page":"Operators","text":"Converts vector to scalar field.\n\n# div(u) = ∇·u\nadd_equation!(problem, \"div(u) = 0\")\n\n# Expands to:\n# ∂u_x/∂x + ∂u_y/∂y + ∂u_z/∂z = 0\n\nnote: `div` vs `divergence` in Julia code\ndiv is a parser alias, valid inside equation strings. In Julia code the name resolves to Base.div (integer division), so div(u) on a field is a MethodError. Call divergence(u) instead:divu = divergence(u)   # Divergence operator","title":"Divergence"},{"category":"section","location":"pages/operators/#Curl","page":"Operators","text":"curl(u) takes a VectorField (not a Gradient or other operator node). In 3D it returns a vector; in 2D it evaluates to the scalar vorticity ω = ∂v/∂x - ∂u/∂y.\n\n# curl(u) = ∇×u\nomega = curl(u)","title":"Curl"},{"category":"section","location":"pages/operators/#Perpendicular-Gradient","page":"Operators","text":"For 2D flows, the perpendicular gradient creates a divergence-free velocity from a streamfunction. Unlike the operators above, perp_grad is eager: it takes a 2-D ScalarField and returns a VectorField computed immediately, rather than a symbolic operator node.\n\n# perp_grad(ψ) = ∇⊥ψ = (-∂ψ/∂y, ∂ψ/∂x)\nu = perp_grad(psi)\n\n# This gives: u_x = -∂ψ/∂y, u_y = ∂ψ/∂x\n# Automatically satisfies: ∇·u = 0\n\nApplications:\n\n2D incompressible flow: u = ∇⊥ψ from streamfunction\nSQG velocity: u = ∇⊥((-Δ)^(-1/2) θ)\nQG geostrophic flow: u = ∇⊥ψ","title":"Perpendicular Gradient"},{"category":"section","location":"pages/operators/#Time-Derivatives","page":"Operators","text":"# ∂t(field) for IVP equations\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\")\n\n# Only valid in Initial Value Problems","title":"Time Derivatives"},{"category":"section","location":"pages/operators/#Unicode-Operators","page":"Operators","text":"Tarang supports Unicode mathematical notation for cleaner, more readable code:\n\nUnicode ASCII Equivalent Description\n∇ grad Gradient\nΔ lap Laplacian\n∇² lap Laplacian (alternative)\n∂t dt Time derivative\n⋅ dot Dot product\n× cross Cross product\n∇⊥ perp_grad Perpendicular gradient\nΔᵅ fraclap Fractional Laplacian","title":"Unicode Operators"},{"category":"section","location":"pages/operators/#In-Equations","page":"Operators","text":"# Unicode syntax\nadd_equation!(problem, \"∂t(u) + ∇(p) - nu*Δ(u) = -u⋅∇(u)\")","title":"In Equations"},{"category":"section","location":"pages/operators/#In-Code","page":"Operators","text":"# ASCII\npressure_grad = grad(pressure)   # ScalarField -> Gradient\nvorticity     = curl(u)          # VectorField -> Curl\n\n# Unicode (equivalent)\npressure_grad = ∇(pressure)\nvorticity     = curl(u)\n\n# Mixed - use what's clearest\nvelocity    = ∇⊥(ψ)   # Perpendicular gradient (2D ScalarField -> VectorField)\ndissipation = Δ(T)    # Laplacian\n\ncurl needs a VectorField; passing it an operator node such as grad(p) throws.","title":"In Code"},{"category":"section","location":"pages/operators/#Typing-Unicode-in-Julia","page":"Operators","text":"In Julia REPL or editors with Julia support:\n\nSymbol Type\n∇ \\nabla + Tab\nΔ \\Delta + Tab\n∂ \\partial + Tab\n⋅ \\cdot + Tab\n× \\times + Tab\n⊥ \\perp + Tab\nα \\alpha + Tab\n² ⁴ ⁶ ⁸ \\^2 + Tab, \\^4 + Tab, …","title":"Typing Unicode in Julia"},{"category":"section","location":"pages/operators/#Using-Operators-in-Equations","page":"Operators","text":"","title":"Using Operators in Equations"},{"category":"section","location":"pages/operators/#Symbolic-Syntax","page":"Operators","text":"# Equations are strings parsed symbolically\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -ux*∂x(T)\")\n\n# Supports:\n# - Addition/subtraction: +, -\n# - Multiplication: *\n# - Division: /\n# - Parentheses: (, )\n# - Function calls: sin, cos, exp, etc.","title":"Symbolic Syntax"},{"category":"section","location":"pages/operators/#Common-Patterns","page":"Operators","text":"# Advection (ux, uz are ScalarFields)\n\"ux*∂x(f) + uz*∂z(f)\"\n\n# Diffusion\n\"nu*Δ(u)\"\n\n# Pressure gradient\n\"∂x(p)\"  # or \"∇(p)\" for vector\n\n# Navier-Stokes viscous term\n\"nu*(∂x(∂x(u)) + ∂z(∂z(u)))\"\n# or simply:\n\"nu*Δ(u)\"","title":"Common Patterns"},{"category":"section","location":"pages/operators/#Advection-Operator:-u(f)","page":"Operators","text":"The u⋅∇(f) notation is automatically expanded component-wise by the parser:\n\nu⋅∇(f)  →  u₁∂₁f + u₂∂₂f + ... + uₙ∂ₙf\n\nThis works for both scalar fields f and vector fields. No manual expansion is needed:\n\n# Scalar advection — automatically expands to Σᵢ uᵢ ∂ᵢf\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -u⋅∇(T)\")\n\n# Vorticity advection\nadd_equation!(problem, \"∂t(ω) + ν₄*Δ²(ω) = -u⋅∇(ω)\")","title":"Advection Operator: u⋅∇(f)"},{"category":"section","location":"pages/operators/#VectorField-Differentiation","page":"Operators","text":"Applying Differentiate to a VectorField operates component-wise — each component is differentiated with respect to the given coordinate independently:\n\n# Differentiate(VectorField, coord) works component-wise\n# e.g. ∂z(u) differentiates each component of u with respect to z\nadd_bc!(problem, \"∂z(u)(z=0) = 0\")   # stress-free BC for a vector field\nadd_bc!(problem, \"∂z(u)(z=1) = 0\")\n\nThis means you can apply ∂z, ∂x, etc. directly to vector fields in equations and boundary conditions without manually indexing components. Note that boundary conditions must be declared with add_bc!, not add_equation! — see Boundary Conditions.","title":"VectorField Differentiation"},{"category":"section","location":"pages/operators/#Implementation-Details","page":"Operators","text":"","title":"Implementation Details"},{"category":"section","location":"pages/operators/#Fourier-Differentiation","page":"Operators","text":"For Fourier bases, differentiation is multiplication by wavenumber:\n\nfracpartialpartial x sum_k hatf_k e^ikx = sum_k (ik) hatf_k e^ikx","title":"Fourier Differentiation"},{"category":"section","location":"pages/operators/#Chebyshev-Differentiation","page":"Operators","text":"Uses recurrence relations:\n\nfracdT_ndx = n U_n-1(x)\n\nImplemented as sparse matrix operations.","title":"Chebyshev Differentiation"},{"category":"section","location":"pages/operators/#Computational-Cost","page":"Operators","text":"Operation Cost\nFourier derivative O(N)\nChebyshev derivative O(N²) sparse\nLaplacian Same as 2× first derivative","title":"Computational Cost"},{"category":"section","location":"pages/operators/#Custom-Operators","page":"Operators","text":"","title":"Custom Operators"},{"category":"section","location":"pages/operators/#Helper-Functions","page":"Operators","text":"Build operator trees in Julia with d(field, coord, order) — the coordinates come from the field's distributor, so the same helper works in any dimension. (Remember that ∂x/∂z are equation-string syntax, not Julia functions.)\n\nfunction advection(u::VectorField, field)\n    # u·∇f\n    cs = u.dist.coordsys\n    result = u.components[1] * d(field, cs[1], 1)\n    for i in 2:length(u.components)\n        result = result + u.components[i] * d(field, cs[i], 1)\n    end\n    return result\nend\n\nThe helper returns a deferred expression tree, not a field. Give it to the problem with add_parameters! and refer to it by name in the equation — the same mechanism the tau method uses for grad_u:\n\nadd_parameters!(problem, kappa=0.05, adv=advection(u, T))\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -adv\")\n\nThis produces bit-identical results to writing -u⋅∇(T) directly, which is what you would normally do — the parser performs exactly this expansion for you.","title":"Helper Functions"},{"category":"section","location":"pages/operators/#Using-Built-in-Operators-in-Equations","page":"Operators","text":"Operators the equation parser recognises out of the box:\n\n# Differential:  ∂t (or dt), ∂x/∂y/∂z, d(f, x, n), grad (∇), div, curl,\n#                lap (Δ, ∇²), trace, lift\n# Fractional:    fraclap(f, α) (Δᵅ), invsqrtlap, Δ², Δ⁴, Δ⁶, Δ⁸\n# Reductions:    integ(f) / integrate(f)\n# Interpolation: f(z=0)      # boundary/point evaluation\n# Products:      a⋅b (dot), a×b (cross), component(u, i)\n# Elementwise:   sin, cos, tan, exp, log, sqrt, abs, tanh\n\n# Use operators directly\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) = -ux*∂x(T) - uz*∂z(T)\")\n\naverage(f, x), integrate(f, x) and interpolate(f, x, pos) take a Coordinate argument, and coordinate names are not bound to Coordinate objects in the default namespace. Register the coordinate first if you need them:\n\nadd_parameters!(problem, x=coords[\"x\"])\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) + average(T, x) = 0\")","title":"Using Built-in Operators in Equations"},{"category":"section","location":"pages/operators/#Performance-Tips","page":"Operators","text":"Minimize transforms: Group operations in same space\nUse Laplacian: Δ(f) is optimized vs ∂x(∂x(f)) + ∂z(∂z(f))\nSpectral derivatives: Free in Fourier, cheap in Chebyshev\nNonlinear terms: Require transform to grid space","title":"Performance Tips"},{"category":"section","location":"pages/operators/#See-Also","page":"Operators","text":"Fields: What operators act on\nProblems: Using operators in PDEs\nSurface Dynamics: SQG, QG, and boundary advection-diffusion\nAPI: Operators: Complete reference","title":"See Also"},{"category":"section","location":"pages/problems/#Problems","page":"Problems","text":"Problems define the PDE system to be solved, including equations and boundary conditions.","title":"Problems"},{"category":"section","location":"pages/problems/#Problem-Types","page":"Problems","text":"","title":"Problem Types"},{"category":"section","location":"pages/problems/#IVP-Initial-Value-Problem","page":"Problems","text":"Time-dependent PDEs with initial conditions. Every unknown — including any tau variables — is listed in the IVP([...]) constructor, and the number of equations (evolution equations plus boundary conditions) must equal the number of variables.\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nbx = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π), dealias=3/2)\nby = RealFourier(coords[\"y\"]; size=16, bounds=(0.0, 2π), dealias=3/2)\ndom = Domain(dist, (bx, by))\n\ns = ScalarField(dom, \"s\")\nu = VectorField(dom, \"u\")\n\nproblem = IVP([s, u])\nadd_parameters!(problem, nu=0.05)\n\n# Linear terms on the LHS (treated implicitly), nonlinear terms on the RHS\nadd_equation!(problem, \"∂t(s) - nu*Δ(s) = -u⋅∇(s)\")\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\nA fully periodic (all-Fourier) problem like this needs no boundary conditions and no tau variables. A bounded Chebyshev/Jacobi direction needs both — see Boundary Conditions below.","title":"IVP - Initial Value Problem"},{"category":"section","location":"pages/problems/#LBVP-Linear-Boundary-Value-Problem","page":"Problems","text":"Steady-state linear PDEs. Boundary conditions in a bounded (Chebyshev/Jacobi) direction use the tau method: declare one tau variable per boundary condition, lift each into the bulk equation via lift(tau, derivative_basis(basis, 2), -k) (registered with add_parameters!), and declare the boundary conditions with add_bc! (not add_equation!).\n\n# 2D Poisson  Δu = -2,  u = 0 on both z walls  (solution u = z(Lz - z))\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb = RealFourier(coords[\"x\"]; size=4,  bounds=(0.0, 2π))   # periodic (separable)\nzb = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))  # bounded  (coupled)\ndom = Domain(dist, (xb, zb))\n\nu    = ScalarField(dom, \"u\")\ntau1 = ScalarField(dist, \"tau1\", (xb,), Float64)           # one tau per BC\ntau2 = ScalarField(dist, \"tau2\", (xb,), Float64)\nlb2  = derivative_basis(zb, 2)\n\nproblem = LBVP([u, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"Δ(u) + l1 + l2 = -2\")\nadd_bc!(problem, \"u(z=0)   = 0\")\nadd_bc!(problem, \"u(z=1.0) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nensure_layout!(u, :g)            # scatter writes coefficients; switch to grid\n\nnote: 1D pure-Chebyshev BVP\nA pure single-axis Chebyshev BVP (no Fourier direction) works too: drop the x axis and define the tau variables on (). The solver builds one coupled tau subproblem over the Chebyshev spectrum.","title":"LBVP - Linear Boundary Value Problem"},{"category":"section","location":"pages/problems/#NLBVP-Nonlinear-Boundary-Value-Problem","page":"Problems","text":"Steady-state nonlinear PDEs. Same tau-method boundary handling as the LBVP (tau variables + lift + add_bc!). Put the nonlinear terms on the right-hand side; the solver linearizes them with a symbolic Frechet derivative and runs a per-Fourier-mode Newton iteration.\n\n# Manufactured nonlinearity  Δu = u² + g,  with g = -2 - u_exact²  so u_exact = z(Lz - z)\n# domain / fields / taus as in the LBVP example above (u, tau1, tau2, lb2)\ng = ScalarField(dom, \"g\"); ensure_layout!(g, :g)\nzg = create_meshgrid(dom; on_device=false)[\"z\"]\nget_grid_data(g) .= -2 .- (zg .* (1.0 .- zg)).^2\n\nproblem = NLBVP([u, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2), g=g)\nadd_equation!(problem, \"Δ(u) + l1 + l2 = u*u + g\")   # nonlinearity on the RHS\nadd_bc!(problem, \"u(z=0)   = 0\")\nadd_bc!(problem, \"u(z=1.0) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolver.tolerance = 1e-10\nensure_layout!(u, :g); get_grid_data(u) .= 0.0       # initial guess\nsolve!(solver)\nensure_layout!(u, :g)","title":"NLBVP - Nonlinear Boundary Value Problem"},{"category":"section","location":"pages/problems/#EVP-Eigenvalue-Problem","page":"Problems","text":"Linear stability and eigenvalue analysis. Tarang solves the generalized problem L x = σ M x, where the mass matrix M is assembled from the time-derivative terms dt(·): the eigenvalue replaces the time derivative (dt(u) → σ u). Keep the dt(u) term in the equation — do not multiply the eigenvalue symbol into it (σ*u = … builds an empty M and returns no eigenvalues). Boundary conditions use the same tau method (tau vars + lift + add_bc!).\n\n# 1D diffusion eigenproblem  σu = Δu, Dirichlet;  eigenvalues σ_n = -(nπ/Lz)²\ncoords = CartesianCoordinates(\"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nzb     = ChebyshevT(coords[\"z\"]; size=32, bounds=(0.0, 1.0))\ndom    = Domain(dist, (zb,))\n\nu    = ScalarField(dom, \"u\")\ntau1 = ScalarField(dist, \"tau1\", (), Float64)\ntau2 = ScalarField(dist, \"tau2\", (), Float64)\nlb2  = derivative_basis(zb, 2)\n\nevp = EVP([u, tau1, tau2]; eigenvalue=:σ)\nadd_parameters!(evp; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(evp, \"dt(u) - Δ(u) - l1 - l2 = 0\")   # dt(u) → σu marks M\nadd_bc!(evp, \"u(z=0)   = 0\")\nadd_bc!(evp, \"u(z=1.0) = 0\")\n\nsolver = EigenvalueSolver(evp; nev=5, which=:SM)   # 5 smallest-magnitude\neigenvalues, eigenvectors = solve!(solver)\n# |eigenvalues| ≈ (nπ)² = 9.87, 39.48, 88.83, ...\n\nEigenvalueSolver accepts only nev, which (∈ :LM :SM :LR :SR :LI :SI), target, and matsolver.","title":"EVP - Eigenvalue Problem"},{"category":"section","location":"pages/problems/#Adding-Equations","page":"Problems","text":"","title":"Adding Equations"},{"category":"section","location":"pages/problems/#Equation-Syntax","page":"Problems","text":"An equation is a \"LHS = RHS\" string. Everything linear goes on the LHS, where it is stepped implicitly; the nonlinear terms go on the RHS, where they are stepped explicitly.\n\n# scalar advection-diffusion: diffusion implicit, advection explicit\nadd_equation!(problem, \"∂t(s) - nu*Δ(s) = -u⋅∇(s)\")\n\n# any number of linear terms may share the LHS\nadd_equation!(problem, \"∂t(s) - nu*Δ(s) + ∂x(s) = -u⋅∇(s)\")","title":"Equation Syntax"},{"category":"section","location":"pages/problems/#Equation-Sizing","page":"Problems","text":"The solver automatically determines each equation's row count in the system matrix from the expression's output type (scalar, vector, tensor, etc.). Equations can be added in any order — no specific ordering is required.\n\n# q, ψ: ScalarFields;  u: VectorField;  tau_ψ = ScalarField(dist, \"tau_ψ\", (), Float64)\nproblem = IVP([q, ψ, u, tau_ψ])\nadd_parameters!(problem, nu=1e-6)\n\n# Any order is fine:\nadd_equation!(problem, \"Δ(ψ) + tau_ψ - q = 0\")           # scalar → D rows\nadd_equation!(problem, \"u - skew(grad(ψ)) = 0\")           # vector → 2D rows\nadd_equation!(problem, \"∂t(q) + nu*Δ⁴(q) = -u⋅∇(q)\")     # scalar → D rows\nadd_bc!(problem, \"integ(ψ) = 0\")                           # constraint → 1 row","title":"Equation Sizing"},{"category":"section","location":"pages/problems/#Supported-Operations","page":"Problems","text":"Derivatives: ∂x, ∂y, ∂z, ∂t (or dt), Δ (or lap), ∇, div, curl, Δ⁴ (hyperdiffusion), and the advection shorthand u⋅∇(f)\nTensor/algebraic: trace, skew, grad, lift, integ\nArithmetic: +, -, *, /, ^\nFunctions: sin, cos, tan, exp, log, sqrt, abs, tanh\nParameters: any name registered with add_parameters! (i.e. present in problem.namespace) — a bare Julia global is not visible to an equation string","title":"Supported Operations"},{"category":"section","location":"pages/problems/#Parameters","page":"Problems","text":"Equation strings resolve names against problem.namespace. They cannot see plain Julia globals, so every constant, field or operator an equation refers to must be registered first — with add_parameters! (preferred) or by writing into the namespace directly.\n\nadd_parameters!(problem; nu=0.01, Ra=1e6, Pr=1.0)   # preferred\nproblem.namespace[\"nu\"] = 0.01                       # equivalent, direct dict access\n\nadd_equation!(problem, \"∂t(u) - nu*Δ(u) = 0\")\n\nKeep parameter-times-field terms on the LHS: a linear term on the RHS (= Ra*Pr*w) breaks the IMEX splitting, and add_equation! warns about it.","title":"Parameters"},{"category":"section","location":"pages/problems/#First-Order-Formulation","page":"Problems","text":"For problems involving Chebyshev bases, Tarang supports a first-order reduction (tau method) that replaces Δ(f) with div(grad_f) where grad_f includes a tau-lifting term. This ensures correct boundary condition enforcement.\n\n# Derivative basis and lift closure\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zbasis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\n\n# First-order gradient substitutions\ngrad_u = grad(u) + ez * τ_lift(tau_u1)\ngrad_b = grad(b) + ez * τ_lift(tau_b1)\n\n# The substitutions are Julia objects, so they must be registered before the equation\n# strings can name them\nadd_parameters!(problem, kappa=0.1, grad_u=grad_u, grad_b=grad_b, τ_lift=τ_lift)\n\n# Equations use div(grad_f) instead of Δ(f)\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(b) - kappa*div(grad_b) + τ_lift(tau_b2) = -u⋅∇(b)\")\n\nEach τ_lift(tau_*) term carries one tau variable, and each tau variable pays for one boundary condition. The Rayleigh-Bénard pattern below is a complete system in this form.\n\nThe advection operator u⋅∇(f) is automatically expanded component-wise to Σᵢ uᵢ ∂ᵢf, so it works for both scalar and vector fields without manual expansion.","title":"First-Order Formulation"},{"category":"section","location":"pages/problems/#Boundary-Conditions","page":"Problems","text":"Boundary conditions are declared with add_bc!, not add_equation!. The syntax field(coord=value) is auto-detected and converted to the appropriate condition.\n\nTwo rules govern every BC:\n\nEach boundary condition needs its own tau variable. A BC adds one row to the system, and the solver requires as many equations (PDEs + BCs) as variables. A wall problem with two conditions per field carries two tau variables per field, lifted into the bulk equation (see the tau method); without them, solver construction throws Problem validation failed: Number of equations ... does not match number of variables.\nThe value must be a literal or a registered parameter. BC strings resolve names against problem.namespace, so a plain Julia global warns Unknown variable and is silently enforced as 0. Use a literal, or register the name with add_parameters!.\n\nDeclaring a BC with add_equation! also registers it as an equation row, but it never reaches the BC manager, so a space- or time-dependent value is never refreshed and is enforced as zero. Always use add_bc!.","title":"Boundary Conditions"},{"category":"section","location":"pages/problems/#Dirichlet-(Value)","page":"Problems","text":"# field = value at location\nadd_bc!(problem, \"T(z=0) = 1\")   # T = 1 at z = 0\nadd_bc!(problem, \"T(z=1) = 0\")   # T = 0 at z = 1","title":"Dirichlet (Value)"},{"category":"section","location":"pages/problems/#Neumann-(Derivative)","page":"Problems","text":"# ∂field/∂z = value at location\nadd_bc!(problem, \"∂z(T)(z=0) = 1\")   # ∂T/∂z = 1 at z = 0","title":"Neumann (Derivative)"},{"category":"section","location":"pages/problems/#Robin-(Mixed)","page":"Problems","text":"# α*T + β*∂T/∂n = γ\nadd_bc!(problem, \"1.0*T(z=0) + 1.0*∂z(T)(z=0) = 0\")\n\n# The same condition as a structured object: robin_bc(field, coord, position, α, β, γ)\nadd_bc!(problem, robin_bc(\"T\", \"z\", 0.0, 1.0, 1.0, 0.0))","title":"Robin (Mixed)"},{"category":"section","location":"pages/problems/#No-Slip-and-Stress-Free-Walls","page":"Problems","text":"add_bc!(problem, \"u(z=0) = 0\")       # no-slip:     u = 0 at a solid wall\nadd_bc!(problem, \"∂z(u)(z=1) = 0\")   # stress-free: ∂u/∂z = 0 at a free surface","title":"No-Slip and Stress-Free Walls"},{"category":"section","location":"pages/problems/#Named-Helpers","page":"Problems","text":"The common physical conditions have wrappers that build the same BCs:\n\nno_slip!(problem, \"u\", \"z\", 0.0)            # u = 0        at z = 0\nfree_slip!(problem, \"u\", \"z\", 1.0)          # ∂u/∂z = 0    at z = 1\nfixed_value!(problem, \"T\", \"z\", 0.0, 1.0)   # T = 1        at z = 0\ninsulating!(problem, \"T\", \"z\", 1.0)         # ∂T/∂z = 0    at z = 1","title":"Named Helpers"},{"category":"section","location":"pages/problems/#Gauge-Constraints","page":"Problems","text":"A pressure-like field defined only up to a constant needs a gauge condition, which is also declared with add_bc! and also consumes a tau variable:\n\nadd_bc!(problem, \"integ(p) = 0\")","title":"Gauge Constraints"},{"category":"section","location":"pages/problems/#Problem-Validation","page":"Problems","text":"validate_problem returns true for a well-posed system and otherwise throws an ArgumentError listing every problem it found (missing variables or equations, a mismatched equation count, invalid boundary conditions). It runs automatically inside every solver constructor, so a badly-posed problem fails at solver construction — you rarely need to call it yourself.\n\nvalidate_problem(problem)   # true, or throws ArgumentError\n\nThe equation count check is exact for IVPs and EVPs (#equations + #BCs == #variables) and a lower bound for BVPs (#equations + #BCs >= #variables). Boundary conditions are merged into the equation list when the solver is built, which is why every BC must be paired with a tau variable.","title":"Problem Validation"},{"category":"section","location":"pages/problems/#Common-Problem-Patterns","page":"Problems","text":"All three wall-bounded patterns below share one skeleton: a periodic Fourier direction x, a bounded Chebyshev direction z, one tau variable per boundary condition, and BCs declared with add_bc!.","title":"Common Problem Patterns"},{"category":"section","location":"pages/problems/#Heat-Equation","page":"Problems","text":"Two Dirichlet walls, so two tau variables lifted into the diffusion term. Lifting into a second-order operator (Δ) uses derivative_basis(zb, 2) at lift orders -1 and -2.\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb = RealFourier(coords[\"x\"]; size=8,  bounds=(0.0, 2π))\nzb = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))\ndom = Domain(dist, (xb, zb))\n\nT    = ScalarField(dom, \"T\")\ntau1 = ScalarField(dist, \"tau1\", (xb,), Float64)   # one tau per BC\ntau2 = ScalarField(dist, \"tau2\", (xb,), Float64)\nlb2  = derivative_basis(zb, 2)\n\nproblem = IVP([T, tau1, tau2])\nadd_parameters!(problem; kappa=0.01, l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"∂t(T) - kappa*Δ(T) + l1 + l2 = 0\")\nadd_bc!(problem, \"T(z=0) = 1\")   # hot bottom\nadd_bc!(problem, \"T(z=1) = 0\")   # cold top\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\nset!(T, 0.0)\nrun!(solver; stop_iteration=10, progress=false)","title":"Heat Equation"},{"category":"section","location":"pages/problems/#Incompressible-Navier-Stokes","page":"Problems","text":"Velocity is a single VectorField (not per-component scalars), and the equations use the first-order tau form: div(grad_u) in place of Δ(u), with tau_u1 inside grad_u and tau_u2 lifted into the momentum equation. tau_p is the gauge unknown that pays for integ(p) = 0.\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 4.0), dealias=3/2)\nzb = ChebyshevT(coords[\"z\"];  size=12, bounds=(0.0, 1.0), dealias=3/2)\ndom = Domain(dist, (xb, zb))\n\np = ScalarField(dom, \"p\")\nu = VectorField(dom, \"u\")\ntau_p  = ScalarField(dist, \"tau_p\", (), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xb,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xb,), Float64)\n\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(zb, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u     = grad(u) + ez * τ_lift(tau_u1)\n\nproblem = IVP([p, u, tau_p, tau_u1, tau_u2])\nadd_parameters!(problem, nu=0.01, grad_u=grad_u, τ_lift=τ_lift)\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")                                 # continuity\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) + τ_lift(tau_u2) = -u⋅∇(u)\")  # momentum\nadd_bc!(problem, \"u(z=0) = 0\")     # no-slip walls\nadd_bc!(problem, \"u(z=1) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")   # pressure gauge\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)","title":"Incompressible Navier-Stokes"},{"category":"section","location":"pages/problems/#Rayleigh-Bénard","page":"Problems","text":"Navier-Stokes plus a buoyant temperature field: two more tau variables (tau_T1, tau_T2) for the two temperature walls.\n\nRayleigh, Prandtl = 2e4, 1.0\n\n# coords / dist / xb / zb / dom / p / u / tau_p / tau_u1 / tau_u2 / ez / τ_lift / grad_u\n# exactly as in the Navier-Stokes pattern above\nT      = ScalarField(dom, \"T\")\ntau_T1 = ScalarField(dist, \"tau_T1\", (xb,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (xb,), Float64)\ngrad_T = grad(T) + ez * τ_lift(tau_T1)\n\nproblem = IVP([p, T, u, tau_p, tau_T1, tau_T2, tau_u1, tau_u2])\nadd_parameters!(problem, nu=Prandtl, buoy=Rayleigh*Prandtl, ez=ez,\n                grad_u=grad_u, grad_T=grad_T, τ_lift=τ_lift)\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(T) - div(grad_T) + τ_lift(tau_T2) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) - buoy*T*ez + τ_lift(tau_u2) = -u⋅∇(u)\")\nadd_bc!(problem, \"T(z=0) = 1\")   # hot bottom\nadd_bc!(problem, \"T(z=1) = 0\")   # cold top\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)\n\nwarning: Chebyshev advection is serial-only\nThe -u⋅∇(u) / -u⋅∇(T) terms differentiate along the Chebyshev axis on the explicit side, which a distributed run cannot do — each rank owns only part of that axis. Both patterns run in serial; under MPI the first step raises an error. See Parallelism.","title":"Rayleigh-Bénard"},{"category":"section","location":"pages/problems/#Poisson-Equation-(BVP)","page":"Problems","text":"A steady BVP needs the tau method. The 2D form below (Δu = -2, u = 0 on both z walls) is shown; a pure 1D Chebyshev Poisson works the same way — drop the x axis and put the tau variables on ().\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb = RealFourier(coords[\"x\"]; size=4,  bounds=(0.0, 2π))\nzb = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))\ndom = Domain(dist, (xb, zb))\n\nu    = ScalarField(dom, \"u\")\ntau1 = ScalarField(dist, \"tau1\", (xb,), Float64)\ntau2 = ScalarField(dist, \"tau2\", (xb,), Float64)\nlb2  = derivative_basis(zb, 2)\n\nproblem = LBVP([u, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nadd_equation!(problem, \"Δ(u) + l1 + l2 = -2\")\nadd_bc!(problem, \"u(z=0)   = 0\")\nadd_bc!(problem, \"u(z=1.0) = 0\")\n\nsolver = BoundaryValueSolver(problem); solve!(solver); ensure_layout!(u, :g)","title":"Poisson Equation (BVP)"},{"category":"section","location":"pages/problems/#See-Also","page":"Problems","text":"Solvers: Solving problems\nBoundary Conditions Tutorial: Detailed BC guide\nAPI: Problems: Complete reference","title":"See Also"},{"category":"section","location":"getting_started/installation/#Installation","page":"Installation","text":"This guide walks you through installing Tarang.jl and its dependencies.","title":"Installation"},{"category":"section","location":"getting_started/installation/#System-Requirements","page":"Installation","text":"","title":"System Requirements"},{"category":"section","location":"getting_started/installation/#Julia","page":"Installation","text":"Tarang.jl requires Julia 1.6 or later. We recommend using the latest stable release of Julia.\n\nDownload Julia:\n\nVisit julialang.org/downloads\nFor Linux/macOS: Use juliaup for easy version management\nFor Windows: Download the installer from the Julia website\n\nVerify installation:\n\njulia --version","title":"Julia"},{"category":"section","location":"getting_started/installation/#MPI-Library","page":"Installation","text":"Tarang.jl requires an MPI implementation for parallel computing.","title":"MPI Library"},{"category":"section","location":"getting_started/installation/#Linux-(Ubuntu/Debian)","page":"Installation","text":"sudo apt-get install openmpi-bin libopenmpi-dev","title":"Linux (Ubuntu/Debian)"},{"category":"section","location":"getting_started/installation/#macOS","page":"Installation","text":"brew install open-mpi","title":"macOS"},{"category":"section","location":"getting_started/installation/#Windows","page":"Installation","text":"Download and install Microsoft MPI or use WSL with Linux MPI.\n\nVerify MPI installation:\n\nmpiexec --version","title":"Windows"},{"category":"section","location":"getting_started/installation/#Optional:-HPC-Clusters","page":"Installation","text":"On HPC systems, load the appropriate MPI module:\n\nmodule load openmpi/4.1.0  # or your system's MPI module\n\nwarning: MPI Compatibility\nEnsure that MPI.jl is built against the same MPI library you plan to use. See the MPI.jl documentation for details on configuring MPI.","title":"Optional: HPC Clusters"},{"category":"section","location":"getting_started/installation/#Installing-Tarang.jl","page":"Installation","text":"","title":"Installing Tarang.jl"},{"category":"section","location":"getting_started/installation/#From-GitHub-(Recommended)","page":"Installation","text":"The package is currently hosted on GitHub and can be installed directly:\n\nusing Pkg\nPkg.add(url=\"https://github.com/subhk/Tarang.jl\")","title":"From GitHub (Recommended)"},{"category":"section","location":"getting_started/installation/#Development-Installation","page":"Installation","text":"For development or to get the latest features:\n\ngit clone https://github.com/subhk/Tarang.jl.git\ncd Tarang.jl\njulia --project=. -e 'using Pkg; Pkg.instantiate()'\n\nThen in Julia:\n\nusing Pkg\nPkg.develop(path=\"/path/to/Tarang.jl\")","title":"Development Installation"},{"category":"section","location":"getting_started/installation/#Installing-Dependencies","page":"Installation","text":"Tarang.jl will automatically install most Julia dependencies. Key packages include:\n\nMPI.jl: MPI bindings for Julia\nPencilArrays.jl: Distributed array library for spectral methods\nPencilFFTs.jl: Parallel FFT transforms\nFFTW.jl: Fast Fourier Transform library\nNetCDF.jl: NetCDF file I/O\nLinearAlgebra: Standard library (included with Julia)\nSparseArrays: Standard library (included with Julia)","title":"Installing Dependencies"},{"category":"section","location":"getting_started/installation/#Configuring-MPI.jl","page":"Installation","text":"If you need to use a system-provided MPI:\n\nusing Pkg\nENV[\"JULIA_MPI_BINARY\"] = \"system\"\nPkg.build(\"MPI\"; verbose=true)\n\nVerify MPI configuration:\n\nusing MPI\nMPI.versioninfo()","title":"Configuring MPI.jl"},{"category":"section","location":"getting_started/installation/#Verification","page":"Installation","text":"Test your installation:\n\nusing Tarang\nusing MPI\n\nprintln(\"Tarang.jl version: \", Tarang.__version__)\nprintln(\"MPI available: \", MPI.Initialized() || MPI.Init())\n\nRun the test suite:\n\nusing Pkg\nPkg.test(\"Tarang\")","title":"Verification"},{"category":"section","location":"getting_started/installation/#Quick-Test-Run","page":"Installation","text":"Create a test file test_tarang.jl:\n\nusing Tarang, MPI\n\nMPI.Init()\n\nrank = MPI.Comm_rank(MPI.COMM_WORLD)\nsize = MPI.Comm_size(MPI.COMM_WORLD)\n\nif rank == 0\n    println(\"Running Tarang on $size MPI processes\")\nend\n\n# Create simple 2D domain\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist = Distributor(coords, mesh=(2, 2))\n\nx = RealFourier(coords[\"x\"]; size=64, bounds=(0.0, 2π))\nz = ChebyshevT(coords[\"z\"]; size=32, bounds=(0.0, 1.0))\n\ndomain = Domain(dist, (x, z))\n\nif rank == 0\n    println(\"Successfully created domain!\")\nend\n\nMPI.Finalize()\n\nRun with MPI:\n\nmpiexec -n 4 julia test_tarang.jl","title":"Quick Test Run"},{"category":"section","location":"getting_started/installation/#Troubleshooting","page":"Installation","text":"","title":"Troubleshooting"},{"category":"section","location":"getting_started/installation/#MPI-Issues","page":"Installation","text":"Problem: ERROR: MPI has not been built\n\nSolution: Build MPI.jl manually:\n\nusing Pkg\nPkg.build(\"MPI\")\n\nProblem: MPI version mismatch\n\nSolution: Rebuild MPI.jl against system MPI:\n\nENV[\"JULIA_MPI_BINARY\"] = \"system\"\nPkg.build(\"MPI\"; verbose=true)","title":"MPI Issues"},{"category":"section","location":"getting_started/installation/#Performance-Issues","page":"Installation","text":"Problem: Warning about OMP_NUM_THREADS\n\nSolution: Set the environment variable:\n\nexport OMP_NUM_THREADS=1\nmpiexec -n 4 julia your_script.jl","title":"Performance Issues"},{"category":"section","location":"getting_started/installation/#FFTW-Issues","page":"Installation","text":"Problem: FFTW planning errors\n\nSolution: Use a different FFTW planning rigor in your configuration:\n\nENV[\"FFTW_PLANNING_RIGOR\"] = \"FFTW_ESTIMATE\"","title":"FFTW Issues"},{"category":"section","location":"getting_started/installation/#NetCDF-Issues","page":"Installation","text":"Problem: NetCDF library conflicts\n\nSolution: Rebuild NetCDF.jl:\n\nusing Pkg\nPkg.build(\"NetCDF\")","title":"NetCDF Issues"},{"category":"section","location":"getting_started/installation/#Next-Steps","page":"Installation","text":"Now that Tarang.jl is installed, continue to:\n\nFirst Steps: Create your first simulation\nRunning with MPI: Learn about parallel execution\nTutorials: Detailed examples and guides","title":"Next Steps"},{"category":"section","location":"getting_started/installation/#System-Specific-Notes","page":"Installation","text":"","title":"System-Specific Notes"},{"category":"section","location":"getting_started/installation/#macOS-Apple-Silicon-(M1/M2)","page":"Installation","text":"Julia and all dependencies work natively on Apple Silicon. Use the ARM64 Julia build for best performance.","title":"macOS Apple Silicon (M1/M2)"},{"category":"section","location":"getting_started/installation/#HPC-Clusters","page":"Installation","text":"Load Julia and your system MPI before using Tarang. NetCDF is provided by the bundled NetCDF_jll artifact, so no system NetCDF/HDF5 module is required:\n\nmodule load julia/1.9\nmodule load openmpi/4.1\n\nConfigure MPI.jl to use the system MPI as shown above.","title":"HPC Clusters"},{"category":"section","location":"getting_started/installation/#Containers","page":"Installation","text":"Tarang.jl works well in containers. Example Dockerfile:\n\nFROM julia:1.9\n\nRUN apt-get update && apt-get install -y \\\n    openmpi-bin \\\n    libopenmpi-dev \\\n    && rm -rf /var/lib/apt/lists/*\n\nRUN julia -e 'using Pkg; \\\n    Pkg.add(url=\"https://github.com/subhk/Tarang.jl\"); \\\n    Pkg.precompile()'\n\nWORKDIR /work","title":"Containers"},{"category":"section","location":"examples/heat_transfer/#Heat-Transfer-Examples","page":"Heat Transfer Examples","text":"Collection of heat transfer simulations with Tarang.jl.\n\nA bounded (Chebyshev) direction carries boundary conditions with the tau method: one tau variable per boundary condition, lifted into the bulk equation and declared with add_bc!. Boundary conditions are never declared with add_equation! — a constant BC happens to come out right that way, but anything space- or time-dependent is silently ignored.","title":"Heat Transfer Examples"},{"category":"section","location":"examples/heat_transfer/#Pure-Diffusion","page":"Heat Transfer Examples","text":"","title":"Pure Diffusion"},{"category":"section","location":"examples/heat_transfer/#1D-Heat-Equation","page":"Heat Transfer Examples","text":"A hot wall at x=0, a cold wall at x=1. The steady state is the linear conduction profile T = 1 - x.\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nbasis  = ChebyshevT(coords[\"x\"]; size=32, bounds=(0.0, 1.0))\ndomain = Domain(dist, (basis,))\n\nT    = ScalarField(domain, \"T\")\ntau1 = ScalarField(dist, \"tau1\", (), Float64)   # one tau per boundary condition\ntau2 = ScalarField(dist, \"tau2\", (), Float64)\n\n# first-order reduction: grad_T = ∇T + x̂·lift(τ₁)\nex, = unit_vector_fields(coords, dist)\nlift_basis  = derivative_basis(basis, 1)\ntau_lift(A) = lift(A, lift_basis, -1)\ngrad_T      = grad(T) + ex * tau_lift(tau1)\n\nproblem = IVP([T, tau1, tau2])\nadd_parameters!(problem; kappa=0.5, grad_T=grad_T, tau_lift=tau_lift)\nadd_equation!(problem, \"∂t(T) - kappa*div(grad_T) + tau_lift(tau2) = 0\")\n\nadd_bc!(problem, \"T(x=0) = 1\")   # hot\nadd_bc!(problem, \"T(x=1) = 0\")   # cold\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\n# Initial condition: step function\nensure_layout!(T, :g)\nx, = local_grids(dist, basis)\nget_grid_data(T) .= Float64.(x .< 0.5)\nensure_layout!(T, :c)\n\nrun!(solver; stop_iteration=2000, progress=false)   # to t = 2.0, effectively steady\nensure_layout!(T, :g)\n\nThe field relaxes from the step to the linear profile. Note that the equation string refers to fields by their field name (the \"T\", \"tau1\", \"tau2\" you passed to the constructor) — if the Julia variable and the field name disagree, the parser warns Unknown variable and silently substitutes zero.\n\nwarning: 1-D pure-Chebyshev IVP: BCs are enforced to O(1/N), not to machine precision\nWith no periodic direction, the tau variables must live on (), and the time-stepping path enforces the Dirichlet values only to first order in N. Measured max|T - (1-x)| at steady state: 3.3e-2 (N=16), 1.6e-2 (N=32), 7.9e-3 (N=64) — i.e. 1/(2N-2). The same steady problem solved as an LBVP (the Laplace Equation section below) hits 2.5e-16, and adding a periodic direction (next section) also gives machine precision. Prefer either of those if you need the boundary values to be exact.","title":"1D Heat Equation"},{"category":"section","location":"examples/heat_transfer/#2D-Diffusion","page":"Heat Transfer Examples","text":"One periodic direction (x, Fourier) and one bounded direction (z, Chebyshev). The tau variables carry the Fourier basis, giving one tau per x-mode — this enforces the boundary conditions to machine precision (measured max|T(z=0) - 1| < 1e-15).\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\nzbasis = ChebyshevT(coords[\"z\"]; size=16, bounds=(0.0, 1.0))\ndomain = Domain(dist, (xbasis, zbasis))\n\nT    = ScalarField(domain, \"T\")\ntau1 = ScalarField(dist, \"tau1\", (xbasis,), Float64)   # one tau per x-mode\ntau2 = ScalarField(dist, \"tau2\", (xbasis,), Float64)\n\nex, ez      = unit_vector_fields(coords, dist)\nlift_basis  = derivative_basis(zbasis, 1)\ntau_lift(A) = lift(A, lift_basis, -1)\ngrad_T      = grad(T) + ez * tau_lift(tau1)\n\nproblem = IVP([T, tau1, tau2])\nadd_parameters!(problem; kappa=0.1, grad_T=grad_T, tau_lift=tau_lift)\nadd_equation!(problem, \"∂t(T) - kappa*div(grad_T) + tau_lift(tau2) = 0\")\n\nadd_bc!(problem, \"T(z=0) = 1\")   # hot bottom\nadd_bc!(problem, \"T(z=1) = 0\")   # cold top\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\nensure_layout!(T, :g)\nx, z = local_grids(dist, xbasis, zbasis)\nget_grid_data(T) .= (1 .- z') .+ 0.1 .* sin.(x) .* sin.(π .* z')\nensure_layout!(T, :c)\n\nrun!(solver; stop_iteration=100, progress=false)\n\nnote: Only one bounded direction per domain\nTarang couples a single non-periodic axis. A domain with two Chebyshev directions (a box with Dirichlet walls on all four sides) does not build — the tau subproblem is not square and the solve fails with a DimensionMismatch. Keep at most one Chebyshev axis; make the others Fourier.","title":"2D Diffusion"},{"category":"section","location":"examples/heat_transfer/#Convection-Diffusion","page":"Heat Transfer Examples","text":"","title":"Convection-Diffusion"},{"category":"section","location":"examples/heat_transfer/#Advection-by-Known-Velocity","page":"Heat Transfer Examples","text":"A constant advecting velocity is just a parameter, and U*∂x(T) is linear, so it goes on the implicit (left) side with the diffusion term.\n\nproblem = IVP([T, tau1, tau2])\nadd_parameters!(problem; kappa=0.01, U=1.0, grad_T=grad_T, tau_lift=tau_lift)\nadd_equation!(problem, \"∂t(T) + U*∂x(T) - kappa*div(grad_T) + tau_lift(tau2) = 0\")\n\nadd_bc!(problem, \"T(z=0) = 0\")\nadd_bc!(problem, \"T(z=1) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\nensure_layout!(T, :g)\nget_grid_data(T) .= sin.(x) .* sin.(π .* z')\nensure_layout!(T, :c)\n\nrun!(solver; stop_iteration=100, progress=false)\n\n(T, tau1, tau2, grad_T, tau_lift, x, z are the ones built in the 2D diffusion example above.)","title":"Advection by Known Velocity"},{"category":"section","location":"examples/heat_transfer/#Natural-Convection","page":"Heat Transfer Examples","text":"Buoyancy-driven flow with temperature coupling — the Rayleigh-Bénard system. Velocity, pressure and temperature are solved together; the nonlinear advection terms sit on the right-hand (explicit) side, everything linear on the left.\n\nusing Tarang\n\nLx, Lz = 4.0, 1.0\nNx, Nz = 16, 12\nRayleigh, Prandtl = 2e4, 1.0\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nzbasis = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\np = ScalarField(domain, \"p\")\nT = ScalarField(domain, \"T\")\nu = VectorField(domain, \"u\")\n\ntau_p  = ScalarField(dist, \"tau_p\",  (), Float64)          # pressure gauge\ntau_T1 = ScalarField(dist, \"tau_T1\", (xbasis,), Float64)\ntau_T2 = ScalarField(dist, \"tau_T2\", (xbasis,), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (xbasis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (xbasis,), Float64)\n\nex, ez      = unit_vector_fields(coords, dist)\nlift_basis  = derivative_basis(zbasis, 1)\ntau_lift(A) = lift(A, lift_basis, -1)\ngrad_u = grad(u) + ez * tau_lift(tau_u1)\ngrad_T = grad(T) + ez * tau_lift(tau_T1)\n\nproblem = IVP([p, T, u, tau_p, tau_T1, tau_T2, tau_u1, tau_u2])\nadd_parameters!(problem; nu=Prandtl, buoy=Rayleigh*Prandtl, ez=ez,\n                grad_u=grad_u, grad_T=grad_T, tau_lift=tau_lift)\n\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\nadd_equation!(problem, \"∂t(T) - div(grad_T) + tau_lift(tau_T2) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*div(grad_u) + ∇(p) - buoy*T*ez + tau_lift(tau_u2) = -u⋅∇(u)\")\n\nadd_bc!(problem, \"T(z=0) = 1\")\nadd_bc!(problem, \"T(z=1) = 0\")\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=1) = 0\")\nadd_bc!(problem, \"integ(p) = 0\")      # pressure gauge\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-4)\n\n# Conduction profile + damped noise\nx, z = local_grids(dist, xbasis, zbasis)\nfill_random!(T, \"g\"; seed=42, distribution=\"normal\", scale=1e-3)\nget_grid_data(T) .*= z' .* (1.0 .- z')\nget_grid_data(T) .+= 1.0 .- z'\nensure_layout!(T, :c)\n\nrun!(solver; stop_iteration=20, progress=false)\n\nSee the Rayleigh-Bénard tutorial for the full-resolution version with output.\n\nwarning: Not available under MPI\nThis example is serial-only, and it fails twice over. First, MPI decomposes the trailing axes, and a Chebyshev axis cannot be decomposed (its DCT needs the whole axis on each rank), so the (x_fourier, z_chebyshev) order above errors at Domain construction: \"Reorder your bases so the Chebyshev axis comes BEFORE the Fourier axes.\" Second, even after reordering to (z_chebyshev, x_fourier), the -u⋅∇(T) / -u⋅∇(u) advection terms expand to a ∂z derivative on the Chebyshev axis in the explicit right-hand side; no rank owns the whole Chebyshev axis, so the solver builds and then dies on the first step: \"cannot differentiate along the non-Fourier axis of a DISTRIBUTED field ... Move the term to the implicit (L) side, or run in serial.\" What does run distributed is a Chebyshev-first domain whose explicit right-hand side differentiates only along Fourier axes.","title":"Natural Convection"},{"category":"section","location":"examples/heat_transfer/#Boundary-Conditions","page":"Heat Transfer Examples","text":"These slot into the 2D problem above, in place of its two Dirichlet conditions. They must be declared with add_bc!; the tau variables are what make them enforceable.","title":"Boundary Conditions"},{"category":"section","location":"examples/heat_transfer/#Convective-(Robin)-BC","page":"Heat Transfer Examples","text":"Heat transfer to an environment at T_amb: h*T + k*∂T/∂n = h*T_amb.\n\nadd_bc!(problem, \"T(z=0) = 1\")\nadd_bc!(problem, \"10*T(z=1) + 1*∂z(T)(z=1) = 0\")   # h=10, k=1, T_amb=0\n\nThe steady solution is T = 1 - (10/11)·z, giving T(z=1) = 1/11. Relaxed from T = 0 with kappa=0.5, dt=1e-3 to t = 10: T(z=1) = 0.09090909 and max|T - (1 - 10z/11)| = 3.8e-15. The Robin condition is enforced to machine precision once the transient has decayed — at t = 3 the residual is still 2.9e-6, which is the slowest mode, not a tau error.\n\nCoefficients may be literals (as above) or names registered with add_parameters!. A BC string cannot see plain Julia globals — an unregistered name warns Unknown variable and is silently treated as zero.","title":"Convective (Robin) BC"},{"category":"section","location":"examples/heat_transfer/#Insulated-(Neumann)-BC","page":"Heat Transfer Examples","text":"Zero heat flux.\n\nadd_bc!(problem, \"T(z=0) = 1\")\nadd_bc!(problem, \"∂z(T)(z=1) = 0\")   # ∂T/∂z = 0 at the top\n\nWith a hot bottom and an insulated top the steady state is uniform, T = 1 everywhere. The bottom value is held to machine precision (max|T(z=0) - 1| < 1e-15) while the interior relaxes at the expected rate: started from T = 0 with kappa=0.5, max|T - 1| = 3.14e-2 at t = 3, against the slowest decaying mode (4/π)·exp(-kappa·(π/2)²·t) = 3.14e-2.","title":"Insulated (Neumann) BC"},{"category":"section","location":"examples/heat_transfer/#Time-Varying-BC","page":"Heat Transfer Examples","text":"A BC value may depend on t (and on the spatial coordinates). It is re-evaluated every step — no callback needed.\n\nadd_bc!(problem, \"T(z=0) = sin(2*pi*t)\")\nadd_bc!(problem, \"T(z=1) = 0\")\n\nMeasured at t = 0.1: T(z=0) = 0.587785, against sin(2π·0.1) = 0.587785.","title":"Time-Varying BC"},{"category":"section","location":"examples/heat_transfer/#Steady-State-Problems","page":"Heat Transfer Examples","text":"","title":"Steady-State Problems"},{"category":"section","location":"examples/heat_transfer/#Laplace-Equation","page":"Heat Transfer Examples","text":"Steady heat conduction. This is a linear boundary value problem (LBVP): boundary conditions are enforced with the tau method (one tau variable per BC, lifted into the bulk equation and declared with add_bc!). The bounded direction here is z (Chebyshev); the periodic x direction is separable.\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb = RealFourier(coords[\"x\"]; size=4,  bounds=(0.0, 1.0))   # periodic (separable)\nzb = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))   # bounded  (coupled)\ndom = Domain(dist, (xb, zb))\n\nT    = ScalarField(dom, \"T\")\ntau1 = ScalarField(dist, \"tau1\", (xb,), Float64)            # one tau per z-BC\ntau2 = ScalarField(dist, \"tau2\", (xb,), Float64)\nlb2  = derivative_basis(zb, 2)\n\nproblem = LBVP([T, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\nTarang.add_equation!(problem, \"Δ(T) + l1 + l2 = 0\")\n\n# Boundary conditions on the bounded (z) direction, via the tau method\nTarang.add_bc!(problem, \"T(z=0)   = 1\")\nTarang.add_bc!(problem, \"T(z=1.0) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nTarang.ensure_layout!(T, :g)            # scatter writes coefficients; switch to grid\n\nRecovers the exact conduction profile: max|T - (1-z)| = 2.5e-16.\n\nnote: 1D pure-Chebyshev BVP\nThe example keeps a periodic x direction, but a pure single-axis Chebyshev BVP (no Fourier) also works — drop the x axis and put the tau variables on (). The solver builds one coupled tau subproblem over the Chebyshev spectrum, and it is machine-precision accurate (max|T - (1-x)| = 2.5e-16).","title":"Laplace Equation"},{"category":"section","location":"examples/heat_transfer/#Poisson-Equation","page":"Heat Transfer Examples","text":"With heat source. Like the Laplace example this is a steady LBVP, so the wall boundary conditions use the tau method (tau variables + lift + add_bc!). The source must sit on the right-hand side.\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb = RealFourier(coords[\"x\"]; size=4,  bounds=(0.0, 1.0))   # periodic (separable)\nzb = ChebyshevT(coords[\"z\"];  size=16, bounds=(0.0, 1.0))   # bounded  (coupled)\ndom = Domain(dist, (xb, zb))\n\nT    = ScalarField(dom, \"T\")\ntau1 = ScalarField(dist, \"tau1\", (xb,), Float64)            # one tau per z-BC\ntau2 = ScalarField(dist, \"tau2\", (xb,), Float64)\nlb2  = derivative_basis(zb, 2)\n\n# Heat source (e.g., volumetric heating)\nq  = ScalarField(dom, \"q\")\nTarang.ensure_layout!(q, :g)\nzg = create_meshgrid(dom; on_device=false)[\"z\"]\nget_grid_data(q) .= sin.(π .* zg)                           # source distribution\n\nproblem = LBVP([T, tau1, tau2])\nadd_parameters!(problem; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2), q=q)\nTarang.add_equation!(problem, \"Δ(T) + l1 + l2 = -q\")\nTarang.add_bc!(problem, \"T(z=0)   = 0\")\nTarang.add_bc!(problem, \"T(z=1.0) = 0\")\n\nsolver = BoundaryValueSolver(problem)\nsolve!(solver)\nTarang.ensure_layout!(T, :g)\n\nRecovers the exact solution T = sin(πz)/π² to 4.7e-16.\n\nThe build prints a warning that a \"boundary condition right-hand side\" of type NegateOperator{ScalarField} is \"being enforced as ZERO\". For the -q equation source this is a false alarm — the source is applied and the boundary conditions are enforced. Checked by giving the same problem a nonzero wall value (T(z=0) = 1, exact solution sin(πz)/π² + 1 - z): the solve reproduces it to 5.6e-16, holds T(z=0) = 1.0000000000, and differs from the source-free 1 - z by 1/π², so the source is demonstrably there. The warning is real and worth heeding when it names a genuine boundary right-hand side.","title":"Poisson Equation"},{"category":"section","location":"examples/heat_transfer/#Heat-Transfer-Analysis","page":"Heat Transfer Examples","text":"","title":"Heat Transfer Analysis"},{"category":"section","location":"examples/heat_transfer/#Nusselt-Number","page":"Heat Transfer Examples","text":"The wall heat flux needs ∂T/∂z as data. Build the derivative operator with d(field, coordinate) and evaluate it into a field with evaluate(op, :g).\n\nfunction compute_nusselt(T, coord, dist, xbasis, zbasis; kappa=1.0, H=1.0, Delta_T=1.0)\n    # Vertical temperature gradient as a grid-space field\n    dT_dz = get_grid_data(evaluate(d(T, coord), :g))\n\n    # Wall heat flux: horizontally averaged ∂T/∂z at the bottom wall\n    _, z  = local_grids(dist, xbasis, zbasis)\n    wall  = argmin(Array(z))\n    q_wall = -kappa * sum(Array(dT_dz)[:, wall]) / size(dT_dz, 1)\n\n    # Conductive reference\n    q_cond = kappa * Delta_T / H\n\n    return q_wall / q_cond\nend\n\n# Check against the pure conduction state T = 1 - z, for which Nu must be exactly 1\nensure_layout!(T, :g)\nget_grid_data(T) .= 1 .- z'\nensure_layout!(T, :c)\n\nNu = compute_nusselt(T, coords[\"z\"], dist, xbasis, zbasis)\n\nThis returns Nu = 1.0000000000, as it must.","title":"Nusselt Number"},{"category":"section","location":"examples/heat_transfer/#Thermal-Boundary-Layer","page":"Heat Transfer Examples","text":"The x-averaged profile is a plain reduction over the grid data. Chebyshev grid points are not ordered monotonically in general, so sort by z first.\n\nfunction boundary_layer_thickness(T, dist, xbasis, zbasis; frac=0.01)\n    ensure_layout!(T, :g)\n    data = Array(get_grid_data(T))\n    _, z = local_grids(dist, xbasis, zbasis)\n\n    profile = vec(sum(data, dims=1) ./ size(data, 1))   # x-averaged T(z)\n    perm    = sortperm(Array(z))\n    zs, ps  = Array(z)[perm], profile[perm]\n\n    # δ_T: first height where the profile has decayed to `frac` of its wall value\n    idx = findfirst(<(frac * ps[1]), ps)\n    return idx === nothing ? nothing : zs[idx]\nend\n\nFor T = exp(-z/0.1) on a 24-point Chebyshev grid this gives δ_T = 0.466, against the analytic -0.1·log(0.01) = 0.461.","title":"Thermal Boundary Layer"},{"category":"section","location":"examples/heat_transfer/#Multi-Physics","page":"Heat Transfer Examples","text":"Tarang solves one domain at a time. There is no multi-domain or interface coupling, so conjugate heat transfer (a solid and a fluid region exchanging flux across a shared boundary) cannot be expressed: two Domains cannot be coupled by a boundary condition. Likewise there is no enthalpy / phase-change formulation. Both would have to be built on top of the framework — for example as a single domain with spatially varying properties — and neither is provided.","title":"Multi-Physics"},{"category":"section","location":"examples/heat_transfer/#Tips","page":"Heat Transfer Examples","text":"","title":"Tips"},{"category":"section","location":"examples/heat_transfer/#Numerical-Stability","page":"Heat Transfer Examples","text":"Put diffusion on the implicit (left) side — that is what lets the IMEX steppers take a timestep unconstrained by the diffusive limit.\nAdvection by a known velocity is linear (U*∂x(T)), so it goes implicit too.\nNonlinear advection (-u⋅∇(T)) must be explicit, and the timestep is then CFL-limited. Drive it with the CFL controller: cfl = CFL(solver; ...), add_velocity!(cfl, u), run!(solver; cfl=cfl).\nAt high Peclet number the only remedies are resolution and dealiasing (dealias=3/2 on the Fourier bases, which is the default). Tarang has no upwinding — it is a spectral code.","title":"Numerical Stability"},{"category":"section","location":"examples/heat_transfer/#Convergence","page":"Heat Transfer Examples","text":"Exponential convergence for smooth solutions.\nCheck spectral coefficient decay.\nThere is no local mesh refinement. A Chebyshev grid already clusters points at the walls, so a boundary layer is resolved by raising the Chebyshev size, not by refining a region.","title":"Convergence"},{"category":"section","location":"examples/heat_transfer/#See-Also","page":"Heat Transfer Examples","text":"Rayleigh-Bénard Tutorial\nFluid Dynamics Examples\nExample Gallery","title":"See Also"},{"category":"section","location":"tutorials/eigenvalue_problems/#Tutorial:-Eigenvalue-Problems","page":"Eigenvalue Problems","text":"This tutorial demonstrates solving eigenvalue problems (EVP) with Tarang.jl for linear stability analysis.","title":"Tutorial: Eigenvalue Problems"},{"category":"section","location":"tutorials/eigenvalue_problems/#Overview","page":"Eigenvalue Problems","text":"Eigenvalue problems arise in linear stability analysis where we seek modes of the form:\n\nmathbfu(xzt) = hatmathbfu(z) e^i k x + sigma t\n\nwhere sigma is the eigenvalue (growth rate + frequency) and hatmathbfu is the eigenfunction.","title":"Overview"},{"category":"section","location":"tutorials/eigenvalue_problems/#Eigenvalue-Convention","page":"Eigenvalue Problems","text":"Tarang solves the generalized problem Lmathbfx = sigma Mmathbfx, where the mass matrix M is built from the time-derivative terms dt(·). The eigenvalue sigma replaces the time derivative (partial_t to sigma), which matches the normal-mode ansatz mathbfusim e^sigma t. So you write the equation with dt(u) kept in place — not by multiplying the eigenvalue symbol into the equation. (sigma*u = … produces an empty M and returns no eigenvalues.)\n\ndt(·) must wrap a variable, not an expression: the mass matrix contributed by dt(X) is the identity on X. A problem in which the eigenvalue multiplies an operator — the classic Orr–Sommerfeld form c(D^2-k^2)hatpsi = dots — must first be reduced to that shape by introducing an auxiliary variable (see Orr-Sommerfeld Equation below).\n\nBounded directions use the tau method, identical to the BVP: add one tau variable per boundary condition, lift it into the bulk equation with lift(tau, derivative_basis(basis, 2), -k), and declare BCs with add_bc!.","title":"Eigenvalue Convention"},{"category":"section","location":"tutorials/eigenvalue_problems/#Coefficients-on-the-implicit-side","page":"Eigenvalue Problems","text":"Two rules that the solver does not enforce for you, and that silently change the physics if you break them:\n\nFold constant products into a single parameter. A coefficient built from two parameters — Ra*Pr*T_hat, Pr*k2*u_hat — is assembled with a coefficient of 1, not Ra*Pr. Register the product itself: add_parameters!(evp; RaPr=Ra*Pr) and write RaPr*T_hat. (A single parameter, a literal, or a right-nested Pr*(k2*u_hat) are all handled correctly.)\nA spatially varying coefficient field must be real. A field coefficient U(z)*q on the implicit side is supported when U varies along a single Chebyshev/Jacobi axis, but only if U is declared Float64. A ComplexF64 coefficient field is dropped (coefficient 1). Declare base-flow profiles as real fields — the eigenvalues and eigenvectors are complex regardless of the field element type.","title":"Coefficients on the implicit side"},{"category":"section","location":"tutorials/eigenvalue_problems/#Basic-EVP-Setup","page":"Eigenvalue Problems","text":"A complete example — the 1D diffusion eigenproblem sigma u = Delta u with Dirichlet walls. Its spectrum is the Dirichlet Laplacian, sigma_n = -(npiL_z)^2.\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nz_basis = ChebyshevT(coords[\"z\"]; size=64, bounds=(0.0, 1.0))\ndom    = Domain(dist, (z_basis,))\n\n# Eigenmode field + one tau per boundary condition\nu_hat = ScalarField(dom, \"u_hat\")\ntau1  = ScalarField(dist, \"tau1\", (), Float64)\ntau2  = ScalarField(dist, \"tau2\", (), Float64)\nlb2   = derivative_basis(z_basis, 2)\n\nevp = Tarang.EVP([u_hat, tau1, tau2]; eigenvalue=:σ)\nadd_parameters!(evp; l1=lift(tau1, lb2, -1), l2=lift(tau2, lb2, -2))\n\n# σ u = Δu  ⇒  keep dt(u) (the solver maps dt → σ), move linear terms to the LHS\nTarang.add_equation!(evp, \"dt(u_hat) - Δ(u_hat) - l1 - l2 = 0\")\nTarang.add_bc!(evp, \"u_hat(z=0)   = 0\")\nTarang.add_bc!(evp, \"u_hat(z=1.0) = 0\")\n\nsolver = Tarang.EigenvalueSolver(evp; nev=5, which=:SM)   # 5 smallest |σ|\neigenvalues, eigenvectors = Tarang.solve!(solver)\n\nfor (i, σ) in enumerate(eigenvalues)\n    println(\"Mode $i: σ = $(real(σ)) + $(imag(σ))im\")   # |σ_n| ≈ (nπ)²\nend\n\nprints sigma_n = -98696 -39478 -88826 -15791 -24674 — that is -(npi)^2, matched to about ten significant digits.","title":"Basic EVP Setup"},{"category":"section","location":"tutorials/eigenvalue_problems/#What-solve!-returns","page":"Eigenvalue Problems","text":"eigenvalues     # Vector{ComplexF64},  length nev, ordered by `which`\neigenvectors    # Matrix{ComplexF64},  column i = mode i\n\neigenvectors[:, i] is the i-th mode as a single stacked coefficient vector in the subproblem's variable ordering (all state fields, then the taus) — not a dictionary keyed by field name. Extracting Eigenmodes below shows how to unpack it back into fields.\n\nEigenvectors are only returned when the problem has a single subproblem, which is the case here (no Fourier axis, serial run). When the problem decomposes into several per-Fourier-mode subproblems, or runs distributed, eigenvectors comes back as an empty 0×0 matrix and only the eigenvalues are meaningful.","title":"What solve! returns"},{"category":"section","location":"tutorials/eigenvalue_problems/#Rayleigh-Bénard-Stability","page":"Eigenvalue Problems","text":"Classical linear stability problem for thermal convection.","title":"Rayleigh-Bénard Stability"},{"category":"section","location":"tutorials/eigenvalue_problems/#Governing-Equations","page":"Eigenvalue Problems","text":"Linearized Boussinesq equations for perturbations propto e^ikx+sigma t:\n\nbeginaligned\nsigma hatu = -i k hatp + textPr nabla^2 hatu \nsigma hatw = -D hatp + textPr nabla^2 hatw + textRa cdot textPr cdot hatT \ni k hatu + D hatw = 0 \nsigma hatT = hatw + nabla^2 hatT\nendaligned\n\nwhere D = ddz and nabla^2 = D^2 - k^2.","title":"Governing Equations"},{"category":"section","location":"tutorials/eigenvalue_problems/#Implementation","page":"Eigenvalue Problems","text":"The horizontal wavenumber k is a parameter here, so the domain is the single bounded direction z. Each second-order equation carries two tau variables; the continuity equation carries a scalar gauge tau, fixed by integ(p_hat) = 0.\n\nBecause parameter values are baked into the equations when add_equation! parses them, a parameter sweep must rebuild the problem — so wrap the whole setup in a function.\n\nusing Tarang\n\nfunction rbc_evp(Ra, k; Pr=1.0, Nz=24, nev=4, which=:LR)\n    coords = CartesianCoordinates(\"z\")\n    dist   = Distributor(coords; dtype=Float64, device=CPU())\n    zb     = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(0.0, 1.0))\n\n    # complex amplitudes û(z), ŵ(z), p̂(z), T̂(z)\n    u = ScalarField(dist, \"u_hat\", (zb,), ComplexF64)\n    w = ScalarField(dist, \"w_hat\", (zb,), ComplexF64)\n    p = ScalarField(dist, \"p_hat\", (zb,), ComplexF64)\n    T = ScalarField(dist, \"T_hat\", (zb,), ComplexF64)\n    # two taus per second-order equation + one gauge tau for the pressure\n    tau = [ScalarField(dist, \"tau$i\", (), ComplexF64) for i in 1:7]\n\n    lb  = derivative_basis(zb, 2)\n    evp = Tarang.EVP([u, w, p, T, tau...]; eigenvalue=:σ)\n\n    # every implicit coefficient is ONE parameter: Ra*Pr and Pr*k² are folded here\n    add_parameters!(evp; Pr=Pr, RaPr=Ra*Pr, Prk2=Pr*k^2, ik=1im*k, k2=k^2,\n                    lu1=lift(tau[1], lb, -1), lu2=lift(tau[2], lb, -2),\n                    lw1=lift(tau[3], lb, -1), lw2=lift(tau[4], lb, -2),\n                    lT1=lift(tau[5], lb, -1), lT2=lift(tau[6], lb, -2),\n                    taup=tau[7])\n\n    # Momentum: keep dt(·) — the eigenvalue σ replaces ∂t and builds the mass matrix M.\n    Tarang.add_equation!(evp,\n        \"dt(u_hat) + ik*p_hat - Pr*∂z(∂z(u_hat)) + Prk2*u_hat + lu1 + lu2 = 0\")\n    Tarang.add_equation!(evp,\n        \"dt(w_hat) + ∂z(p_hat) - Pr*∂z(∂z(w_hat)) + Prk2*w_hat - RaPr*T_hat + lw1 + lw2 = 0\")\n    # Continuity: algebraic constraint — no dt term, so its M rows are zero\n    Tarang.add_equation!(evp, \"ik*u_hat + ∂z(w_hat) + taup = 0\")\n    # Temperature\n    Tarang.add_equation!(evp,\n        \"dt(T_hat) - w_hat - ∂z(∂z(T_hat)) + k2*T_hat + lT1 + lT2 = 0\")\n\n    # No-slip, fixed temperature, and a pressure gauge\n    for f in (\"u_hat\", \"w_hat\", \"T_hat\"), z in (0, 1)\n        Tarang.add_bc!(evp, \"$f(z=$z) = 0\")\n    end\n    Tarang.add_bc!(evp, \"integ(p_hat) = 0\")\n\n    return Tarang.solve!(Tarang.EigenvalueSolver(evp; nev=nev, which=which))\nend\n\ngrowth(Ra, k; kwargs...) = maximum(real.(first(rbc_evp(Ra, k; kwargs...))))\n\nfor Ra in (1000.0, 1707.76, 2500.0)\n    println(\"Ra = $Ra, k = 3.117:  max Re σ = \", round(growth(Ra, 3.117), digits=6))\nend\n\nRa = 1000.0, k = 3.117:  max Re σ = -6.002789\nRa = 1707.76, k = 3.117:  max Re σ = -1.4e-5\nRa = 2500.0, k = 3.117:  max Re σ = 5.514215\n\nAt the textbook critical point (textRa_c = 170776, k_c = 3117) the leading growth rate is zero to five digits, with 24 Chebyshev modes.","title":"Implementation"},{"category":"section","location":"tutorials/eigenvalue_problems/#Orr-Sommerfeld-Equation","page":"Eigenvalue Problems","text":"Stability of parallel shear flows.","title":"Orr-Sommerfeld Equation"},{"category":"section","location":"tutorials/eigenvalue_problems/#Equation","page":"Eigenvalue Problems","text":"left (U - c)(D^2 - k^2) - U + fracitextRe cdot k(D^2 - k^2)^2 right hatpsi = 0\n\nwhere c = sigma(-ik) is the complex wave speed. As written, the eigenvalue multiplies the operator (D^2-k^2), which Tarang cannot assemble (the mass matrix from dt(X) is the identity on X). Introduce the auxiliary variable hat q = (D^2-k^2)hatpsi and the problem becomes a standard generalized eigenproblem in (hatpsi hat q):\n\nbeginaligned\nsigma hat q = -ikleft(U hat q - U hatpsiright) + frac1textRe(D^2-k^2)hat q \nhat q = (D^2 - k^2)hatpsi\nendaligned\n\nThe second equation has no dt term, so it contributes zero rows to M — exactly like the continuity constraint above.","title":"Equation"},{"category":"section","location":"tutorials/eigenvalue_problems/#Implementation-2","page":"Eigenvalue Problems","text":"Plane Poiseuille flow, U = 1 - z^2 on zin-11, so U = -2. The base-flow profile is a real field (see the coefficient rules above); the four no-slip conditions hatpsi = hatpsi = 0 at both walls give four taus.\n\nusing Tarang\n\nfunction orr_sommerfeld(Re, k; Nz=64, nev=4)\n    coords = CartesianCoordinates(\"z\")\n    dist   = Distributor(coords; dtype=Float64, device=CPU())\n    zb     = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(-1.0, 1.0))\n\n    psi = ScalarField(dist, \"psi\", (zb,), Float64)   # ψ̂\n    q   = ScalarField(dist, \"q\",   (zb,), Float64)   # q̂ = (D² − k²)ψ̂\n    U   = ScalarField(dist, \"U\",   (zb,), Float64)   # base flow: REAL field\n    set!(U, z -> 1.0 - z^2)\n    ensure_layout!(U, :c)\n\n    tau = [ScalarField(dist, \"t$i\", (), Float64) for i in 1:4]\n    lb  = derivative_basis(zb, 2)\n    evp = Tarang.EVP([psi, q, tau...]; eigenvalue=:σ)\n    add_parameters!(evp; U=U, ik=1im*k, k2=k^2, invRe=1/Re, invRe_k2=k^2/Re,\n                    ikUpp=1im*k*(-2.0),                 # U'' = -2 for U = 1 − z²\n                    l1=lift(tau[1], lb, -1), l2=lift(tau[2], lb, -2),\n                    l3=lift(tau[3], lb, -1), l4=lift(tau[4], lb, -2))\n\n    Tarang.add_equation!(evp,\n        \"dt(q) + ik*(U*q) - ikUpp*psi - invRe*∂z(∂z(q)) + invRe_k2*q + l1 + l2 = 0\")\n    Tarang.add_equation!(evp, \"q - ∂z(∂z(psi)) + k2*psi + l3 + l4 = 0\")\n\n    Tarang.add_bc!(evp, \"psi(z=-1) = 0\")\n    Tarang.add_bc!(evp, \"psi(z=1) = 0\")\n    Tarang.add_bc!(evp, \"∂z(psi)(z=-1) = 0\")\n    Tarang.add_bc!(evp, \"∂z(psi)(z=1) = 0\")\n\n    λ, _ = Tarang.solve!(Tarang.EigenvalueSolver(evp; nev=nev, which=:LR))\n    return λ\nend\n\nk = 1.02056\nσ = orr_sommerfeld(5772.22, k)[1]     # least-stable mode at the critical point\nc = 1im * σ / k                       # wave speed:  σ = -i k c\nprintln(\"σ = \", round(σ, digits=6))\nprintln(\"c = \", round(c, digits=6))\n\nσ = -0.0 - 0.26943im\nc = 0.264002 - 0.0im\n\nwhich is the textbook neutral point of plane Poiseuille flow: textRe_c = 577222, k_c = 102056, c_r = 0264, c_i = 0. The growth rate is zero only to roundoff — unrounded, textResigma approx -3times10^-9, whose trailing digits shift from run to run, which is why the printout above is rounded. Lowering the Reynolds number damps the mode and raising it destabilises — the same call at Re = 4000 gives c_i = -00045, and at Re = 10000 gives c_i = +00032.","title":"Implementation"},{"category":"section","location":"tutorials/eigenvalue_problems/#Parameter-Studies","page":"Eigenvalue Problems","text":"Parameter values are substituted into the equations at add_equation! time, so a sweep rebuilds the problem for each value — that is what the rbc_evp / growth functions above are for. (Assigning to evp.parameters[...] after the equations are parsed changes nothing; and a Problem can only be handed to one solver, because building a solver merges the boundary conditions into its equation list.)","title":"Parameter Studies"},{"category":"section","location":"tutorials/eigenvalue_problems/#Scanning-Rayleigh-Number","page":"Eigenvalue Problems","text":"Ra_values   = range(1000.0, 2500.0; length=6)\ngrowth_rates = [growth(Ra, 3.117) for Ra in Ra_values]\n\n# critical Ra: linear interpolation of the zero crossing\ni = findfirst(>(0), growth_rates)\nRa_lo, Ra_hi = Ra_values[i-1], Ra_values[i]\ng_lo,  g_hi  = growth_rates[i-1], growth_rates[i]\nRa_crit = Ra_lo - g_lo * (Ra_hi - Ra_lo) / (g_hi - g_lo)\n\nprintln(\"growth rates: \", round.(growth_rates, digits=4))\nprintln(\"Critical Rayleigh number: \", round(Ra_crit, digits=1))\n\ngrowth rates: [-6.0028, -3.2885, -0.8321, 1.4287, 3.5347, 5.5142]\nCritical Rayleigh number: 1710.4\n\n(1710.4 rather than 1707.8 because the zero crossing is interpolated linearly from a coarse six-point scan; the bisection below converges to the exact value.)","title":"Scanning Rayleigh Number"},{"category":"section","location":"tutorials/eigenvalue_problems/#Scanning-Wavenumber","page":"Eigenvalue Problems","text":"k_values = range(2.0, 4.5; length=6)\ngr = [growth(2500.0, k) for k in k_values]\n\nprintln(\"growth rates: \", round.(gr, digits=4))\nprintln(\"Most unstable wavenumber at Ra = 2500: \", k_values[argmax(gr)])\n\ngrowth rates: [1.4701, 3.9323, 5.3431, 5.6553, 4.9042, 3.159]\nMost unstable wavenumber at Ra = 2500: 3.5","title":"Scanning Wavenumber"},{"category":"section","location":"tutorials/eigenvalue_problems/#Neutral-Curves","page":"Eigenvalue Problems","text":"Computing the stability boundary in parameter space — bisect on Ra at fixed k:\n\nfunction neutral_Ra(k; Ra_lo=500.0, Ra_hi=5000.0, tol=1.0)\n    while Ra_hi - Ra_lo > tol\n        Ra_mid = (Ra_lo + Ra_hi) / 2\n        growth(Ra_mid, k) > 0 ? (Ra_hi = Ra_mid) : (Ra_lo = Ra_mid)\n    end\n    return (Ra_lo + Ra_hi) / 2\nend\n\nfor k in (2.5, 3.117, 4.0)\n    println(\"k = $k   neutral Ra ≈ \", round(neutral_Ra(k), digits=1))\nend\n\nk = 2.5   neutral Ra ≈ 1822.5\nk = 3.117   neutral Ra ≈ 1707.7\nk = 4.0   neutral Ra ≈ 1879.1\n\nThe curve has its minimum at k approx 3117 with textRa approx 1708 — the critical point of Rayleigh-Bénard convection between rigid plates.","title":"Neutral Curves"},{"category":"section","location":"tutorials/eigenvalue_problems/#Extracting-Eigenmodes","page":"Eigenvalue Problems","text":"eigenvectors[:, i] is a stacked coefficient vector, not per-field arrays. Scatter it back into the problem's fields to get a grid-space profile — the fields are overwritten in place, so this reuses the solver from the Basic EVP Setup:\n\nsp   = solver.subproblems[1]              # single subproblem: no Fourier axis\nmode = eigenvectors[:, 1]                 # column 1 = the mode you want\nTarang.scatter_inputs(sp, mode, [u_hat, tau1, tau2])   # same order as EVP(...)\n\nensure_layout!(u_hat, :g)\nz       = local_grids(dist, z_basis)[1]\nprofile = Array(get_grid_data(u_hat))\nprofile ./= profile[argmax(abs.(profile))]            # normalise: peak = +1\n\nreference = sin.(π .* z)\nreference ./= reference[argmax(abs.(reference))]\nprintln(\"max |mode − sin(πz)| = \",\n        round(maximum(abs, profile .- reference), sigdigits=2))\n\nmax |mode − sin(πz)| = 1.9e-12\n\nprofile and z are plain vectors, ready for whichever plotting package you use. Note that u_hat here is a Float64 field, so only the real part of the mode is kept; declare the fields ComplexF64 (as in rbc_evp) when the eigenfunction is genuinely complex.","title":"Extracting Eigenmodes"},{"category":"section","location":"tutorials/eigenvalue_problems/#Solver-Options","page":"Eigenvalue Problems","text":"","title":"Solver Options"},{"category":"section","location":"tutorials/eigenvalue_problems/#Which-Eigenvalues","page":"Eigenvalue Problems","text":"nev, which and target can be given to the constructor, or overridden per call on an existing solver — the latter avoids rebuilding the matrices, and is the only way to re-select on a problem you have already solved (a Problem cannot be handed to a second solver):\n\nTarang.solve!(solver; nev=3, which=:LM)   # largest magnitude\nTarang.solve!(solver; nev=3, which=:LR)   # largest real part (most unstable)\nTarang.solve!(solver; nev=3, which=:SM)   # smallest magnitude\nTarang.solve!(solver; nev=3, which=:SR)   # smallest real part","title":"Which Eigenvalues"},{"category":"section","location":"tutorials/eigenvalue_problems/#Shift-invert-near-a-target","page":"Eigenvalue Problems","text":"# order eigenvalues by proximity to a target (useful for interior spectra)\nλ, _ = Tarang.solve!(solver; nev=3, target = -90.0 + 0.0im)\nprintln(round.(real.(λ), digits=4))     # [-88.8264, -39.4784, -157.9137]\n\nEigenvalueSolver accepts nev, which (:LM/:SM/:LR/:SR/:LI/:SI), target, and matsolver. It solves eigen(L, M) per Fourier mode on the square tau matrices and discards spurious eigenvalues from the singular mass matrix.","title":"Shift-invert near a target"},{"category":"section","location":"tutorials/eigenvalue_problems/#See-Also","page":"Eigenvalue Problems","text":"Problems API: EVP problem definition\nSolvers API: Eigenvalue solver details\nRayleigh-Bénard Tutorial: Time-dependent version","title":"See Also"},{"category":"section","location":"api/io/#I/O-API","page":"I/O","text":"File input/output for simulation data.","title":"I/O API"},{"category":"section","location":"api/io/#NetCDF-Output","page":"I/O","text":"","title":"NetCDF Output"},{"category":"section","location":"api/io/#NetCDFFileHandler","page":"I/O","text":"","title":"NetCDFFileHandler"},{"category":"section","location":"api/io/#add*file*handler","page":"I/O","text":"There are two ways to create a handler, and they behave differently.\n\nSolver form (recommended). The handler is registered with the solver, so run! processes it every step and closes it at the end. It also takes its time metadata (sim_time, iteration, dt) from the solver, so the sim_dt/iter cadence works and every record is stamped with the simulation time:\n\nhandler = add_file_handler(\"snapshots\", solver; sim_dt=0.002, max_writes=100)\nadd_task!(handler, T; name=\"T\")\nrun!(solver; stop_iteration=6, progress=false)   # writes at sim_time 0, 0.002, 0.004, 0.006\n\nDistributor form. Builds a handler that knows nothing about a solver: it does not auto-register (you must pass it in outputs=), and with no solver attached its sim_time/iteration are always 0, so a sim_dt or iter cadence never advances and only the first write lands. Pass solver= if you drive it with run!:\n\nhandler = add_netcdf_handler(\n    \"snapshots\",               # Output path/name\n    dist,                      # Distributor\n    Dict(\"T\" => T, \"u\" => u);  # Dict of fields (namespace for string-expression tasks)\n    solver=solver,             # so sim_time/iteration are real, not 0\n    sim_dt=0.002,\n    max_writes=100             # writes per file before rolling over to the next set\n)\nadd_task!(handler, T; name=\"T\")\nrun!(solver; stop_iteration=6, outputs=[handler], progress=false)   # outputs= is required\n\nArguments:\n\nbase_path: base path for output files (a trailing .nc is stripped)\ndist: distributor\nvars: dictionary mapping names to fields\nsolver: optional solver, used for time metadata\nsim_dt / iter / wall_dt: write cadence (simulation time, iterations, wall seconds)\nmax_writes: maximum writes per file before starting a new file set\nparallel: \"gather\" or \"virtual\" — see the Parallel I/O section below\nmode: \"overwrite\" (default) or \"append\"\n\nReturns: NetCDFFileHandler\n\n","title":"addfilehandler"},{"category":"section","location":"api/io/#add*netcdf*handler","page":"I/O","text":"Same as the distributor form of add_file_handler above.\n\n","title":"addnetcdfhandler"},{"category":"section","location":"api/io/#add_task!","page":"I/O","text":"Add a field output task.\n\n","title":"add_task!"},{"category":"section","location":"api/io/#add_task","page":"I/O","text":"Add a field output task (alternative syntax).\n\nAlways pass the field or operator object, never a string expression: a string task (add_task!(handler, \"T*T\")) is accepted silently but writes a scalar of zeros instead of the field. Build the operator instead (add_task!(handler, T*T; name=\"T2\")).\n\nadd_task!(handler, field; name=\"field_name\")\n\nWith postprocessing — postprocess receives the task's data array (this rank's slab) and its return value is what gets written, so the written shape follows from it:\n\nusing Statistics\n\nadd_task!(handler, field;\n    name=\"field_xmean\",\n    postprocess = data -> dropdims(mean(data, dims=1), dims=1)\n)\n\n","title":"add_task"},{"category":"section","location":"api/io/#add*profile*task!","page":"I/O","text":"Add a task that reduces the field to a 1D profile: the mean over every dimension except dim. The keyword is dim (singular) and it names the dimension to keep — either an axis index or a coordinate symbol.\n\n# z-profile: mean over x, keep z\nadd_profile_task!(handler, field; dim=:z, name=\"field_profile_z\")\n\n# same thing by axis index, on an (x, z) domain\nadd_profile_task!(handler, field; dim=2, name=\"field_profile_z\")\n\nTo average over several dimensions instead, use add_mean_task! with dims=.\n\n","title":"addprofiletask!"},{"category":"section","location":"api/io/#add*mean*task!","page":"I/O","text":"Add a task that computes mean values. With dims it averages over those dimensions; without dims it writes the single whole-domain mean (MPI-aware — combined across ranks).\n\nadd_mean_task!(handler, field; name=\"field_mean\")                 # scalar, whole domain\nadd_mean_task!(handler, field; dims=(:x, :z), name=\"field_mean_xz\")\n\n","title":"addmeantask!"},{"category":"section","location":"api/io/#add*slice*task!","page":"I/O","text":"Add a task that extracts a slice, given the axis (dim) and the index along it (idx):\n\nadd_slice_task!(handler, field; dim=1, idx=8, name=\"field_slice\")\n\nwarning: `slices=` is currently broken\nThe slices=Dict(...) form advertised in the docstring above throws at write time (MethodError: Cannot convert an object of type Int64 to an object of type Colon). Use the dim/idx form.\n\n","title":"addslicetask!"},{"category":"section","location":"api/io/#add*rms*task!","page":"I/O","text":"Add a task that computes RMS (root-mean-square) values — whole-domain by default, or per-remaining-axis with dims.\n\nadd_rms_task!(handler, field; name=\"field_rms\")\n\n","title":"addrmstask!"},{"category":"section","location":"api/io/#add*variance*task!","page":"I/O","text":"Add a task that computes variance (same dims convention as add_rms_task!).\n\nadd_variance_task!(handler, field; name=\"field_variance\")\n\n","title":"addvariancetask!"},{"category":"section","location":"api/io/#add*extrema*task!","page":"I/O","text":"Add a task that tracks minimum and maximum values. It writes two variables, <name>_min and <name>_max (whole-domain, MPI-aware):\n\nadd_extrema_task!(handler, field; name=\"T\")   # writes vars/T_min and vars/T_max\n\n","title":"addextrematask!"},{"category":"section","location":"api/io/#process!","page":"I/O","text":"Write pending data to file (respecting the handler's sim_dt/iter/wall_dt cadence). This is the low-level call.\n\nRecommended: create the handler with the solver and let run! drive output — the handler auto-registers, and run! calls process! every step and close! at the end, so you never write a manual loop or forget a process!:\n\nhandler = add_file_handler(\"output/snap\", solver; sim_dt=0.5)\nadd_task!(handler, u; name=\"u\")\nrun!(solver; stop_time=20.0, cfl=cfl)   # auto: dt, process!(handler), close!\n\nCall process!/close! directly only when you write your own step loop:\n\nprocess!(handler;\n    iteration=solver.iteration,\n    wall_time=time() - wall_start,\n    sim_time=solver.sim_time,\n    timestep=solver.dt\n)\n\nIt returns true if the write happened and false if the schedule said \"not yet\".\n\n","title":"process!"},{"category":"section","location":"api/io/#Handler-Management","page":"I/O","text":"","title":"Handler Management"},{"category":"section","location":"api/io/#check_schedule","page":"I/O","text":"Check if the handler should write based on schedule.\n\n","title":"check_schedule"},{"category":"section","location":"api/io/#create*current*file!","page":"I/O","text":"Create a new output file.\n\n","title":"createcurrentfile!"},{"category":"section","location":"api/io/#current_path","page":"I/O","text":"Get the directory of the current file set (<name>_s<set>), not a file:\n\ncurrent_path(handler)   # \"snapshots/snapshots_s1\"\n\n","title":"current_path"},{"category":"section","location":"api/io/#current_file","page":"I/O","text":"Get the file the handler is writing into, inside that directory:\n\ncurrent_file(handler)   # serial: \"snapshots/snapshots_s1/snapshots_s1.nc\"\n                        # np=2:   \"snapshots/snapshots_s1/snapshots_s1_p0.nc\" (per rank)\n\n","title":"current_file"},{"category":"section","location":"api/io/#get*output*files","page":"I/O","text":"List the files this handler has created (all sets).\n\n","title":"getoutputfiles"},{"category":"section","location":"api/io/#get*handler*info","page":"I/O","text":"A Dict describing the handler state, with keys base_path, name, set_num, file_writes, total_writes, max_writes, num_tasks, task_names, parallel, precision, mpi_rank, mpi_size.\n\n","title":"gethandlerinfo"},{"category":"section","location":"api/io/#close!","page":"I/O","text":"Close the handler and finalize output.\n\nclose!(handler)\n\n","title":"close!"},{"category":"section","location":"api/io/#reset!","page":"I/O","text":"Reset the handler state.\n\n","title":"reset!"},{"category":"section","location":"api/io/#File-Merging","page":"I/O","text":"Under MPI each rank writes its own slab into its own file (see Parallel I/O below). Merging is the post-processing step that stitches those per-rank files back into one file holding the global field. It is a serial post-processing step: run it after the simulation, from the handler's output directory (the one that contains <name>_s<set>/). In a serial run there is nothing to merge — merging reports \"No processor files found\" and returns false.","title":"File Merging"},{"category":"section","location":"api/io/#NetCDFMerger","page":"I/O","text":"","title":"NetCDFMerger"},{"category":"section","location":"api/io/#merge*netcdf*files","page":"I/O","text":"Merge one file set written by a parallel run:\n\ncd(\"snapshots\")   # the directory containing snapshots_s1/\nmerge_netcdf_files(\"snapshots\"; set_number=1, output_name=\"snapshots_merged.nc\")\n\nThe per-rank slabs are reconstructed with the start/count/global_shape attributes each rank stored, so vars/T in the merged file has the full global shape ((writes, Nx, Nz)).\n\nWhole-domain reduction tasks (add_mean_task! and friends without dims) store no such attributes, because their value is already global on every rank. The merger has nothing to reconstruct for them, and says so loudly — expect, for each one,\n\n┌ Warning: No global_shape metadata found for 'T_mean'. Using single-processor shape (3, 1)\n│ — the merged file may contain only a fraction of the full domain.\n      Error placing data from snapshots_s1_p1.nc: BoundsError(...)\n\nThis is noise, not failure: the merger keeps rank 0's value, which is the correct global one, and the merge still returns true.\n\n","title":"mergenetcdffiles"},{"category":"section","location":"api/io/#merge_files!","page":"I/O","text":"The lower-level call: build a NetCDFMerger yourself and run it.\n\n","title":"merge_files!"},{"category":"section","location":"api/io/#batch*merge*netcdf","page":"I/O","text":"Merge several handlers in one go; returns a Dict of handler name to success flag. Like merge_netcdf_files, it looks for <name>_s<set>/ in the current directory — so every handler you name must have its set directories there. A handler created with a bare base_path roots its sets in a directory of its own (snapshots/snapshots_s1/), and no two such handlers ever share a parent; give them a common parent instead:\n\n# at setup, so both handlers write into out/\nh1 = add_file_handler(\"out/snapshots\", solver; sim_dt=0.002)\nh2 = add_file_handler(\"out/analysis\",  solver; sim_dt=0.002)\n# → out/snapshots_s1/snapshots_s1_p*.nc, out/analysis_s1/analysis_s1_p*.nc\n\n# after the run\ncd(\"out\")\nfind_mergeable_handlers(\".\")                    # Dict(\"snapshots\" => [1], \"analysis\" => [1])\nbatch_merge_netcdf([\"snapshots\", \"analysis\"])   # Dict(\"snapshots\" => true, \"analysis\" => true)\n\nCalled from the wrong directory it finds nothing, warns No processor files found for merging, and returns false for every handler.\n\n","title":"batchmergenetcdf"},{"category":"section","location":"api/io/#find*mergeable*handlers","page":"I/O","text":"Scan a directory for handler file sets, returning a Dict of handler name to the set numbers found:\n\nfind_mergeable_handlers(\"snapshots\")   # Dict(\"snapshots\" => [1])\n\n","title":"findmergeablehandlers"},{"category":"section","location":"api/io/#cleanup*source*files!","page":"I/O","text":"Remove source files after merging.\n\n","title":"cleanupsourcefiles!"},{"category":"section","location":"api/io/#Equation-Parsing","page":"I/O","text":"Internal functions for parsing equation strings.","title":"Equation Parsing"},{"category":"section","location":"api/io/#split_equation","page":"I/O","text":"","title":"split_equation"},{"category":"section","location":"api/io/#split_call","page":"I/O","text":"","title":"split_call"},{"category":"section","location":"api/io/#lambdify_functions","page":"I/O","text":"","title":"lambdify_functions"},{"category":"section","location":"api/io/#Reading-NetCDF","page":"I/O","text":"","title":"Reading NetCDF"},{"category":"section","location":"api/io/#Using-NetCDF.jl","page":"I/O","text":"Output files are NetCDF-4 with groups, so the task data is not at the file root: a plain NetCDF.ncread(file, \"T\") fails with \"does not have a variable named T\", and ncinfo lists no variables. Read through the group helpers instead:\n\nusing Tarang, NetCDF\n\nfile = \"snapshots/snapshots_s1/snapshots_s1.nc\"\n\nTarang.group_variable_names(file, \"vars\")     # [\"T\", \"T_mean\", \"T_min\", \"T_max\", ...]\nT = Tarang.group_ncread(file, \"vars\", \"T\")    # size (writes, Nx, Nz)\n\ntimes = Tarang.group_ncread(file, \"time\", \"sim_time\")   # size (writes,)\nx     = Tarang.group_ncread(file, \"grids\", \"x\")         # size (Nx,)\n\nNetCDF.ncgetatt(file, \"global\", \"title\")      # \"Tarang.jl simulation output\"","title":"Using NetCDF.jl"},{"category":"section","location":"api/io/#File-Structure","page":"I/O","text":"","title":"File Structure"},{"category":"section","location":"api/io/#NetCDF-Layout","page":"I/O","text":"Three groups: vars (the tasks), time (per-write metadata), grids (coordinates). The write index is the leading dimension of every task variable.\n\nsnapshots/snapshots_s1/snapshots_s1.nc\n├── vars\n│   ├── T          (sim_time, x, z)\n│   ├── T_profile_z (sim_time, T_profile_z_dim1)\n│   └── T_min, T_max, ...   (sim_time, 1)\n├── time\n│   ├── sim_time    [N_writes]   (unlimited dimension)\n│   ├── wall_time   [N_writes]\n│   ├── timestep    [N_writes]\n│   ├── iteration   [N_writes]\n│   └── write_number[N_writes]\n├── grids\n│   ├── x [Nx]\n│   ├── z [Nz]\n│   └── <task>_dim1 [...]    for reduced/derived tasks\n└── global attributes\n\nEach task variable also carries layout (\"g\"/\"c\"), grid_space, and — for a distributed run — the start/count/global_shape/local_shape of that rank's slab, which is what merge_netcdf_files uses to reconstruct the global field.","title":"NetCDF Layout"},{"category":"section","location":"api/io/#Global-Attributes","page":"I/O","text":"Written automatically:\n\nAttribute Value\ntitle \"Tarang.jl simulation output\"\nhandler_name Handler name from path\nsoftware \"Tarang\"\nsoftware_repository Repository URL\ntarang_version Package version\ninstitution, source, history, Conventions Provenance / CF-1.8\nset_number, writes Current file set and its write count\nmpi_size, processor_rank Only when running on more than one rank\n\nRead them with NetCDF.ncgetatt(file, \"global\", name).","title":"Global Attributes"},{"category":"section","location":"api/io/#Checkpointing","page":"I/O","text":"Tarang has no built-in checkpoint type. Write a small helper over solver.state — the integrator's live fields, each a ScalarField with a .name (a vector variable u appears as its components u_x, u_z, …). Use solver.state, not the problem-variable handles: those are separate objects and writing to them does not restore the integrator.","title":"Checkpointing"},{"category":"section","location":"api/io/#Saving-State","page":"I/O","text":"Grid space is real-valued and exact, so it round-trips losslessly:\n\nusing NetCDF\n\nfunction save_checkpoint(solver, path)\n    isfile(path) && rm(path)\n    for f in solver.state\n        ensure_layout!(f, :g)\n        g = get_grid_data(f)\n        dimspec = collect(Iterators.flatten(\n            (\"$(f.name)_d$i\" => s for (i, s) in enumerate(size(g)))))\n        nccreate(path, f.name, dimspec...; t=NC_DOUBLE)\n        ncwrite(g, path, f.name)\n    end\n    ncputatt(path, \"Global\", Dict(\"sim_time\" => solver.sim_time,\n                                  \"iteration\" => solver.iteration, \"dt\" => solver.dt))\n    return path\nend","title":"Saving State"},{"category":"section","location":"api/io/#Loading-State","page":"I/O","text":"function load_checkpoint!(solver, path)\n    for f in solver.state\n        ensure_layout!(f, :g)               # make the grid buffer current\n        get_grid_data(f) .= ncread(path, f.name)\n    end\n    solver.sim_time  = ncgetatt(path, \"Global\", \"sim_time\")\n    solver.iteration = Int(ncgetatt(path, \"Global\", \"iteration\"))\n    solver.dt        = ncgetatt(path, \"Global\", \"dt\")\n    return solver\nend\n\nRestarting then reproduces the uninterrupted trajectory to the bit:\n\nsave_checkpoint(solver, \"chk.nc\")\n# … later, with the same problem/solver rebuilt …\nload_checkpoint!(solver, \"chk.nc\")\nrun!(solver; stop_iteration=solver.iteration + 20, progress=false)\n\nget_grid_data is the rank-local slab, so under MPI either put the rank in path (one checkpoint file per rank, restarted on the same decomposition) or gather to rank 0 with gather_array(f.dist, get_grid_data(f)) before writing.","title":"Loading State"},{"category":"section","location":"api/io/#Parallel-I/O","page":"I/O","text":"There is no gather-to-rank-0 write path. On more than one rank every rank writes its own file, containing its own slab:\n\nsnapshots/snapshots_s1/snapshots_s1_p0.nc    # rank 0's slab, e.g. (writes, 16, 8)\nsnapshots/snapshots_s1/snapshots_s1_p1.nc    # rank 1's slab\n\nReassemble them afterwards with merge_netcdf_files (see File Merging). Whole-domain reductions (add_mean_task!, add_rms_task!, add_variance_task!, add_extrema_task! without dims) are MPI-aware: every rank's file already holds the global value.\n\nThe parallel keyword (\"gather\", the default, or \"virtual\") only chooses the serial file name: with parallel=\"gather\" on one rank you get a single snapshots_s1.nc; otherwise the file is named ..._p<rank>.nc. It does not move data between ranks.","title":"Parallel I/O"},{"category":"section","location":"api/io/#File-Management","page":"I/O","text":"","title":"File Management"},{"category":"section","location":"api/io/#File-Naming","page":"I/O","text":"Each file set lives in its own directory, and sets are numbered sequentially — a new set starts once max_writes writes have landed in the current one:\n\nsnapshots/snapshots_s1/snapshots_s1.nc   # first set\nsnapshots/snapshots_s2/snapshots_s2.nc   # after max_writes reached\nsnapshots/snapshots_s3/snapshots_s3.nc   # etc.\n\nWith mode=\"overwrite\" (the default) the handler deletes pre-existing sets of the same name when it is constructed.","title":"File Naming"},{"category":"section","location":"api/io/#Performance-Tips","page":"I/O","text":"Batch writes: use a sim_dt/iter cadence rather than writing every iteration\nCap file size: max_writes rolls over to a new set instead of one huge file\nWrite what you need: reduction tasks (profiles, means, extrema) are far cheaper to store than full fields\nMerge offline: reassemble per-rank files after the run, not during it","title":"Performance Tips"},{"category":"section","location":"api/io/#See-Also","page":"I/O","text":"Analysis Tutorial: Complete examples\nParallelism: MPI considerations","title":"See Also"},{"category":"type","location":"api/io/#Tarang.NetCDFFileHandler","page":"I/O","text":"NetCDF File Handler matching Tarang H5FileHandler structure\n\nFollows Tarang pattern:\n\nbasepath/handlernames1/handlernames1p0.nc for processor files\nbasepath/handlername_s1.nc for gathered files\n/scales/ group with time coordinates\n/tasks/ group with field data\n\n\n\n\n\n","title":"Tarang.NetCDFFileHandler"},{"category":"function","location":"api/io/#Tarang.add_file_handler","page":"I/O","text":"Tarang-style helper to create a NetCDF file handler. Matches evaluator.addfilehandler(...) usage in Tarang.\n\n\n\n\n\nAdd file handler for output\n\n\n\n\n\nAdd file handler with automatic format detection\n\nSupported formats:\n\n:netcdf or :nc - NetCDF format\n:auto - Auto-detect from file extension (defaults to NetCDF)\n\n\n\n\n\n","title":"Tarang.add_file_handler"},{"category":"function","location":"api/io/#Tarang.add_netcdf_handler","page":"I/O","text":"Convenience function to create NetCDF file handler (matching Tarang API) Usage: handler = addfilehandler(\"snapshots\", dist, vars, simdt=0.25, maxwrites=50)\n\n\n\n\n\n","title":"Tarang.add_netcdf_handler"},{"category":"function","location":"api/io/#Tarang.add_task!","page":"I/O","text":"Add task to handler (matching Tarang API)\n\nExample usage (matching Tarang style):\n\nsnapshots = evaluator.addfilehandler('snapshots', simdt=0.25, maxwrites=50) snapshots.addtask(b, name='buoyancy') snapshots.addtask(-d3.div(d3.skew(u)), name='vorticity')\n\n\n\n\n\nAdd field or operator to file handler\n\n\n\n\n\nAdd field or operator to file handler with explicit name\n\n\n\n\n\nAdd a field/operator task to the dictionary handler.\n\n\n\n\n\nAdd a field/array to be written by this file handler.\n\n\n\n\n\n","title":"Tarang.add_task!"},{"category":"function","location":"api/io/#Tarang.add_task","page":"I/O","text":"Alias without bang for Tarang-style task addition.\n\n\n\n\n\n","title":"Tarang.add_task"},{"category":"function","location":"api/io/#Tarang.add_profile_task!","page":"I/O","text":"Add a task that computes a 1D profile (mean over all but one dimension).\n\nArguments\n\nhandler: NetCDFFileHandler instance\nfield: Field to compute profile of\ndim: The dimension to keep (profile along this dimension)\nname: Optional name for the output variable\n\nExample\n\nadd_profile_task!(handler, u, dim=:z, name=\"u_profile_z\")\n\n\n\n\n\n","title":"Tarang.add_profile_task!"},{"category":"function","location":"api/io/#Tarang.add_mean_task!","page":"I/O","text":"Add a task that computes the mean over specified dimensions.\n\nArguments\n\nhandler: NetCDFFileHandler instance\nfield: Field to compute mean of\ndims: Dimensions to average over (e.g., (:x, :y) or (1, 2))\nname: Optional name for the output variable\n\nExample\n\nadd_mean_task!(handler, u, dims=(:x, :y), name=\"u_mean_z\")\n\n\n\n\n\n","title":"Tarang.add_mean_task!"},{"category":"function","location":"api/io/#Tarang.add_slice_task!","page":"I/O","text":"Add a task that extracts a slice of a field.\n\nArguments\n\nhandler: NetCDFFileHandler instance\nfield: Field to slice\nslices: Dictionary or named tuple specifying slice positions         e.g., Dict(:z => 0.5) or (z=0.5,) for midplane slice\nname: Optional name for the output variable\n\nExample\n\nadd_slice_task!(handler, u, slices=Dict(:z => 0.0), name=\"u_bottom\")\nadd_slice_task!(handler, T, slices=(x=0.5, y=0.5), name=\"T_centerline\")\n\n\n\n\n\n","title":"Tarang.add_slice_task!"},{"category":"function","location":"api/io/#Tarang.add_rms_task!","page":"I/O","text":"Add a task that computes the RMS (root mean square) over specified dimensions.\n\n\n\n\n\n","title":"Tarang.add_rms_task!"},{"category":"function","location":"api/io/#Tarang.add_variance_task!","page":"I/O","text":"Add a task that computes the variance over specified dimensions.\n\n\n\n\n\n","title":"Tarang.add_variance_task!"},{"category":"function","location":"api/io/#Tarang.add_extrema_task!","page":"I/O","text":"Add a task that computes min/max over the domain.\n\n\n\n\n\n","title":"Tarang.add_extrema_task!"},{"category":"function","location":"api/io/#Tarang.process!","page":"I/O","text":"Process handler: write all tasks to NetCDF (matching Tarang process method)\n\n\n\n\n\nprocess!(handler::VirtualFileHandler, solver, wall_time, sim_time, iteration)\n\nWrite per-rank data files (LOCAL slabs only — no gather) and rank-0 manifest. Each dataset name is stored with name_start (global offsets of the slab) and the manifest stores name_gshape (global shape) for reconstruction.\n\n\n\n\n\n","title":"Tarang.process!"},{"category":"function","location":"api/io/#Tarang.check_schedule","page":"I/O","text":"Check if handler should process based on schedule (matching Tarang logic)\n\nAlways writes initial conditions (iteration=0) when any scheduling mode is configured. For subsequent iterations, uses the configured cadence.\n\n\n\n\n\n","title":"Tarang.check_schedule"},{"category":"function","location":"api/io/#Tarang.create_current_file!","page":"I/O","text":"Create NetCDF file with Tarang-style structure\n\n\n\n\n\n","title":"Tarang.create_current_file!"},{"category":"function","location":"api/io/#Tarang.current_path","page":"I/O","text":"Get current set path following Tarang naming: handlernames1/\n\n\n\n\n\n","title":"Tarang.current_path"},{"category":"function","location":"api/io/#Tarang.current_file","page":"I/O","text":"Get current file path following Tarang naming: handlernames1/handlernames1_p0.nc\n\n\n\n\n\n","title":"Tarang.current_file"},{"category":"function","location":"api/io/#Tarang.get_output_files","page":"I/O","text":"Get list of all output files created by this handler.\n\n\n\n\n\n","title":"Tarang.get_output_files"},{"category":"function","location":"api/io/#Tarang.get_handler_info","page":"I/O","text":"Get metadata about the handler's output.\n\n\n\n\n\n","title":"Tarang.get_handler_info"},{"category":"function","location":"api/io/#Tarang.close!","page":"I/O","text":"Close handler and finalize all files.\n\nWrites final metadata attributes to the current NetCDF file. Also clears the internal variable-creation cache.\n\nNote: NetCDF.jl's filename-based API (nccreate, ncwrite, ncread, ncputatt) opens and closes the underlying file descriptor on each call, so there is no persistent file handle to release here. This method exists to flush final metadata and reset handler state.\n\n\n\n\n\n","title":"Tarang.close!"},{"category":"function","location":"api/io/#Tarang.reset!","page":"I/O","text":"Reset handler for a new simulation run.\n\n\n\n\n\nreset!(filter::ExponentialMean)\n\nReset the filter state to zero.\n\n\n\n\n\nreset!(filter::ButterworthFilter)\n\nReset the filter state to zero.\n\n\n\n\n\nreset!(filter::LagrangianFilter)\n\nReset the Lagrangian filter state.\n\n\n\n\n\nreset!(model::EddyViscosityModel)\n\nReset the eddy viscosity field to zero. GPU-aware: fill!() works for both CPU and GPU arrays.\n\n\n\n\n\n","title":"Tarang.reset!"},{"category":"type","location":"api/io/#Tarang.NetCDFMerger","page":"I/O","text":"NetCDF File Merger - handles merging of per-processor files\n\n\n\n\n\n","title":"Tarang.NetCDFMerger"},{"category":"function","location":"api/io/#Tarang.merge_netcdf_files","page":"I/O","text":"merge_netcdf_files(base_name; kwargs...)\n\nMerge per-processor NetCDF files into a single merged file.\n\nArguments\n\nbase_name::String: Base name of the handler (e.g., \"snapshots\", \"analysis\")\nset_number::Int=1: Set number to merge (default: 1)\noutput_name::String=\"\": Output filename (default: auto-generated)\nmerge_mode::MergeMode=RECONSTRUCT: How to combine processor data\ncleanup::Bool=false: Delete source files after successful merge\nverbose::Bool=true: Print progress information\n\nExamples\n\n# Basic merge\nmerge_netcdf_files(\"snapshots\")\n\n# Advanced options  \nmerge_netcdf_files(\"analysis\", \n                   set_number=2,\n                   output_name=\"analysis_complete.nc\", \n                   cleanup=true,\n                   merge_mode=SIMPLE_CONCAT)\n\n\n\n\n\n","title":"Tarang.merge_netcdf_files"},{"category":"function","location":"api/io/#Tarang.merge_files!","page":"I/O","text":"Main merge function - orchestrates the entire merging process\n\n\n\n\n\n","title":"Tarang.merge_files!"},{"category":"function","location":"api/io/#Tarang.batch_merge_netcdf","page":"I/O","text":"batch_merge_netcdf(handlers; kwargs...)\n\nMerge multiple handlers in batch mode.\n\nExamples\n\n# Merge multiple handlers\nbatch_merge_netcdf([\"snapshots\", \"analysis\", \"checkpoints\"])\n\n# With cleanup\nbatch_merge_netcdf([\"snapshots\", \"analysis\"], cleanup=true)\n\n\n\n\n\n","title":"Tarang.batch_merge_netcdf"},{"category":"function","location":"api/io/#Tarang.find_mergeable_handlers","page":"I/O","text":"find_mergeable_handlers(directory=\".\")\n\nFind all handlers with processor files ready for merging.\n\nReturns a dictionary mapping handler names to available set numbers.\n\n\n\n\n\n","title":"Tarang.find_mergeable_handlers"},{"category":"function","location":"api/io/#Tarang.cleanup_source_files!","page":"I/O","text":"Clean up source processor files after successful merge\n\n\n\n\n\n","title":"Tarang.cleanup_source_files!"},{"category":"function","location":"api/io/#Tarang.split_equation","page":"I/O","text":"split_equation(equation::AbstractString)\n\nSplit an equation string into left- and right-hand sides, tracking bracket depth so that keyword arguments (e.g. f(x=1)) and array indices do not trigger false splits.\n\nTracks parentheses (), square brackets [], and curly braces {}.\n\nThrows SymbolicParsingError if there is not exactly one top-level equals sign.\n\n\n\n\n\n","title":"Tarang.split_equation"},{"category":"function","location":"api/io/#Tarang.split_call","page":"I/O","text":"split_call(call::AbstractString)\n\nSplit a function-style string f(x y) into a head f and a tuple of argument names. Returns (call ()) if the string does not have call syntax.\n\nHandles nested parentheses correctly, e.g., f(g(x y) z) splits into (f (g(x y) z)).\n\n\n\n\n\n","title":"Tarang.split_call"},{"category":"function","location":"api/io/#Tarang.lambdify_functions","page":"I/O","text":"lambdify_functions(call::AbstractString, result::AbstractString)\n\nConvert a math-style definition f(x y)/x*y into a Julia anonymous function encoded as a string (xy) - x*y. Returns the original result for non-call statements to preserve standard semantics.\n\n\n\n\n\n","title":"Tarang.lambdify_functions"},{"category":"section","location":"pages/custom_operators/#Custom-Operators","page":"Custom Operators","text":"Creating custom differential and integral operators.","title":"Custom Operators"},{"category":"section","location":"pages/custom_operators/#Overview","page":"Custom Operators","text":"Beyond built-in operators (grad, div, curl, lap), you can define custom operators for specialized physics. There is no plugin API to implement — a custom operator is just a Julia function that composes built-in operators into an expression. Such an expression is lazy: nothing is computed until you either\n\npass it to the solver as part of an equation, or\ncall evaluate(expr, :g) to get a field back.\n\nFor diagnostics you can also skip the operator layer entirely and work directly on grid data with get_grid_data.\n\nAll examples below share this setup:\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π), dealias=3/2)\nzbasis = RealFourier(coords[\"z\"]; size=16, bounds=(0.0, 2π), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\nT = ScalarField(domain, \"T\")\nu = VectorField(domain, \"u\")\n\nx, z = local_grids(dist, xbasis, zbasis)\n\nensure_layout!(T, :g)\nget_grid_data(T) .= sin.(x) .* cos.(z')      # T = sin(x) cos(z)\nensure_layout!(T, :c)\n\nensure_layout!(u, :g)\nget_grid_data(u.components[1]) .= ones(length(x)) .* sin.(z)'   # u = (sin z, 0): a shear flow\nget_grid_data(u.components[2]) .= 0\nensure_layout!(u, :c)","title":"Overview"},{"category":"section","location":"pages/custom_operators/#Helper-Functions","page":"Custom Operators","text":"","title":"Helper Functions"},{"category":"section","location":"pages/custom_operators/#Simple-Custom-Operators","page":"Custom Operators","text":"An operator is a function returning an operator expression. Build the expression from the built-ins and let evaluate do the transforms:\n\n# Advection operator: u·∇f\nadvect(u, f) = u ⋅ grad(f)\n\nadv    = advect(u, T)          # DotProduct — nothing computed yet\nresult = evaluate(adv, :g)     # ScalarField, in grid layout\n\nmaximum(abs, get_grid_data(result))   # 0.5, matching sin(z)·cos(x)cos(z)\n\nNote that u ⋅ grad(f) is dot(u, grad(f)): ⋅ and × between two vector-valued operands build DotProduct / CrossProduct nodes, which evaluate understands.","title":"Simple Custom Operators"},{"category":"section","location":"pages/custom_operators/#What-evaluate-accepts","page":"Custom Operators","text":"evaluate(expr, layout) handles operator nodes (Differentiate, Gradient, Divergence, Curl, Laplacian, Integrate, …) and ⋅ / × products of vector fields:\n\nevaluate(d(T, coords[\"x\"]), :g)   # ScalarField\nevaluate(lap(T), :g)              # ScalarField\nevaluate(grad(u), :g)             # TensorField\n\nPlain arithmetic around an operator (2.0 * lap(T), a + b) builds a deferred Multiply / Add node instead. Those nodes are meant for equations; evaluate does not compute them and throws a MethodError. Scale the evaluated field instead:\n\nlapT = evaluate(lap(T), :g)\nget_grid_data(lapT) .*= 2.0","title":"What evaluate accepts"},{"category":"section","location":"pages/custom_operators/#Using-in-Equations","page":"Custom Operators","text":"A custom operator becomes usable inside an equation string once you register it as a parameter. Equation strings resolve names against the problem's variables and its registered parameters only — they cannot see Julia globals — so add_parameters! is what makes the name visible:\n\nhyperdiff(f) = lap(lap(f))        # custom operator: ∇⁴\n\nproblem = IVP([T])\nadd_parameters!(problem, nu4=0.01, hyperdiff=hyperdiff)\nadd_equation!(problem, \"∂t(T) + nu4*hyperdiff(T) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\nrun!(solver; stop_iteration=100, progress=false)\n\n# T = sin(x)cos(z) is a ∇⁴ eigenfunction with eigenvalue (kx²+kz²)² = 4\nensure_layout!(T, :g)\nmaximum(abs, get_grid_data(T))    # 0.99600798934 ≈ exp(-nu4 * 4 * 0.1)\n\nThe same trick works for operator values, not just functions: add_parameters!(problem, gradT=grad(T)) registers the expression under the name gradT.\n\nwarning: An operator used in an equation must return an operator expression\nThe parser calls your function with the parsed operands and splices the result into the equation tree. If the function computes eagerly and returns a raw array — anything built with get_grid_data — the term is silently dropped: no error, no warning, and the equation integrates as if the term were absent. Keep equation-side operators symbolic (lap, grad, d, lift, …). Grid-space kernels are for diagnostics and post-processing only.","title":"Using in Equations"},{"category":"section","location":"pages/custom_operators/#Using-Operators-in-Equations","page":"Custom Operators","text":"The equation parser recognizes all built-in operators. Use them directly:\n\n# Differential:  grad (∇), div, curl, lap (Δ, laplacian), d (differentiate), dt (∂t)\n#                hyperlap, fraclap, sqrtlap, invsqrtlap, hilbert\n# Reductions:    integrate (integ), average (avg, ave), interpolate\n# Tensor:        trace, skew, transpose_components (trans), outer, component (comp)\n# Basis:         convert, lift, grid, coeff, copy\n# Ufuncs:        sin, cos, tan, tanh, exp, log, sqrt, abs\n\nT2 = ScalarField(domain, \"T2\")\nu2 = VectorField(domain, \"u2\")\n\nproblem2 = IVP([T2, u2])\nadd_parameters!(problem2, kappa=0.05, nu=0.05)\nadd_equation!(problem2, \"∂t(T2) - kappa*Δ(T2) = -u2⋅∇(T2)\")\nadd_equation!(problem2, \"∂t(u2) - nu*lap(u2) = -u2⋅∇(u2)\")\n\nsolver2 = InitialValueSolver(problem2, RK222(); dt=1e-3)\nsolver2.rhs_plan.is_compiled     # true — the RHS was compiled, not interpreted\n\n-u⋅∇(u) (vector advection) is likewise valid inside an equation. It has no direct evaluate form: evaluate(u ⋅ grad(u), :g) throws, because grad(u) is a tensor and DotProduct requires two vector fields. Compute it componentwise if you need it outside a solve (see Convective Derivative below).","title":"Using Operators in Equations"},{"category":"section","location":"pages/custom_operators/#Spectral-Differentiation","page":"Custom Operators","text":"","title":"Spectral Differentiation"},{"category":"section","location":"pages/custom_operators/#Fourier-Derivative","page":"Custom Operators","text":"Coefficient arrays store the first axis in RFFT layout (N÷2+1 complex values, non-negative wavenumbers) and every trailing axis in FFT layout (signed wavenumbers). Use the matching wavenumber vector:\n\nfunction fourier_dx!(f, basis)\n    ensure_layout!(f, :c)\n    k = Tarang.wavenumbers_rfft(basis)    # first axis: [0, k₀, 2k₀, …, (N/2)k₀]\n    get_coeff_data(f) .*= im .* k         # multiply by ik\n    return f\nend\n\ng = ScalarField(domain, \"g\")\nensure_layout!(g, :g)\nget_grid_data(g) .= sin.(x) .* cos.(z')\nensure_layout!(g, :c)\n\nfourier_dx!(g, xbasis)\nensure_layout!(g, :g)\nmaximum(abs, get_grid_data(g) .- cos.(x) .* cos.(z'))   # 2.0e-15\n\nFor a trailing axis the layout is the standard FFT ordering, so the wavenumbers are wavenumbers_fft and they broadcast along that axis:\n\nfunction fourier_dz!(f, basis)\n    ensure_layout!(f, :c)\n    k = Tarang.wavenumbers_fft(basis)     # [0, …, N/2-1, -N/2, …, -1] · k₀\n    get_coeff_data(f) .*= im .* k'\n    return f\nend\n\n(wavenumbers(basis) returns the native RealFourier cos/sin ordering of length N, which does not match the coefficient array. Tarang.wavenumbers_rfft / Tarang.wavenumbers_fft are the ones that do.)","title":"Fourier Derivative"},{"category":"section","location":"pages/custom_operators/#Chebyshev-Derivative","page":"Custom Operators","text":"There is no hand-rolled path worth writing here: d(f, coord) already applies the correct recurrence, in the correct basis, for Chebyshev and every other Jacobi basis.\n\nzcoords = CartesianCoordinates(\"x\", \"z\")\nzdist   = Distributor(zcoords; dtype=Float64, device=CPU())\nxb = RealFourier(zcoords[\"x\"]; size=8,  bounds=(0.0, 2π))\nzb = ChebyshevT(zcoords[\"z\"];  size=16, bounds=(0.0, 1.0))\ncdomain = Domain(zdist, (xb, zb))\n\nq = ScalarField(cdomain, \"q\")\nxq, zq = local_grids(zdist, xb, zb)\nensure_layout!(q, :g)\nget_grid_data(q) .= ones(length(xq)) .* (zq .^ 3)'\nensure_layout!(q, :c)\n\ndq = evaluate(d(q, zcoords[\"z\"]), :g)\nmaximum(abs, get_grid_data(dq) .- (3 .* zq .^ 2)')     # 1.9e-14\n\nIf you need the underlying coefficient-space matrix — to assemble your own operator, say — it is differentiation_matrix(basis, order) (a sparse N×N matrix, cached on the basis):\n\nD = differentiation_matrix(zb, 1)      # 16×16 SparseMatrixCSC","title":"Chebyshev Derivative"},{"category":"section","location":"pages/custom_operators/#Integral-Operators","page":"Custom Operators","text":"","title":"Integral Operators"},{"category":"section","location":"pages/custom_operators/#Spatial-Integration","page":"Custom Operators","text":"Integration is a built-in operator, and it carries the right quadrature weights for every basis (uniform on Fourier, Chebyshev–Gauss on Chebyshev), so do not roll your own:\n\nintegrate(T)                                     # Float64: integral over the whole domain\nTx = evaluate(integrate(T, coords[\"x\"]), :g)     # ScalarField on z: integral along x only\nTz = evaluate(average(T, coords[\"z\"]), :g)       # ScalarField on x: mean along z\n\nintegrate(q)                                     # ∫∫ z³ dz dx over [0,2π]×[0,1] = 1.5707963268\n\nintegrate(field) is also the MPI-safe form: it reduces across ranks for you.","title":"Spatial Integration"},{"category":"section","location":"pages/custom_operators/#Vector-Calculus","page":"Custom Operators","text":"","title":"Vector Calculus"},{"category":"section","location":"pages/custom_operators/#Strain-Rate-Tensor","page":"Custom Operators","text":"function strain_rate(u)\n    cs = u.coordsys\n    S  = TensorField(u.dist, cs, \"S\", u.bases, u.dtype)\n\n    for i in 1:cs.dim, j in 1:cs.dim   # S_ij = ½(∂u_i/∂x_j + ∂u_j/∂x_i)\n        dij = evaluate(d(u.components[i], cs.coords[j]), :g)\n        dji = evaluate(d(u.components[j], cs.coords[i]), :g)\n        Sij = S.components[i, j]\n        ensure_layout!(Sij, :g)\n        get_grid_data(Sij) .= 0.5 .* (get_grid_data(dij) .+ get_grid_data(dji))\n    end\n    return S\nend\n\nS = strain_rate(u)\nmaximum(abs, get_grid_data(S.components[1,2]))   # 0.5, matching ½cos(z) for u = (sin z, 0)\n\nTensorField components live in a dim × dim matrix — S.components[i,j] — and are ScalarFields like any other. There is no S[i,j] = … setindex and no symmetric=true option.\n\nThe velocity gradient itself is a single call, with the convention (∇u)_ij = ∂u_j/∂x_i:\n\ngu   = evaluate(grad(u), :g)                     # TensorField\ngu_T = evaluate(transpose_components(gu), :g)    # its transpose\n\ntranspose_components needs a concrete TensorField, so evaluate grad(u) first.","title":"Strain Rate Tensor"},{"category":"section","location":"pages/custom_operators/#Vorticity-(2D)","page":"Custom Operators","text":"In two dimensions curl returns a ScalarField:\n\nomega = evaluate(curl(u), :g)                    # ω = ∂x u_z − ∂z u_x\nmaximum(abs, get_grid_data(omega))               # 1.0, matching −cos(z)\n\nwhich is the same thing as writing it out by hand:\n\nomega2 = evaluate(d(u.components[2], coords[\"x\"]) - d(u.components[1], coords[\"z\"]), :g)","title":"Vorticity (2D)"},{"category":"section","location":"pages/custom_operators/#Helicity-(3D)","page":"Custom Operators","text":"coords3 = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist3   = Distributor(coords3; dtype=Float64, device=CPU())\nbx = RealFourier(coords3[\"x\"]; size=8, bounds=(0.0, 2π))\nby = RealFourier(coords3[\"y\"]; size=8, bounds=(0.0, 2π))\nbz = RealFourier(coords3[\"z\"]; size=8, bounds=(0.0, 2π))\ndomain3 = Domain(dist3, (bx, by, bz))\n\nv = VectorField(domain3, \"v\")\nx3, y3, z3 = local_grids(dist3, bx, by, bz)\nensure_layout!(v, :g)\nvx, vy, vz = (get_grid_data(c) for c in v.components)\nfor k in eachindex(z3), j in eachindex(y3), i in eachindex(x3)\n    vx[i,j,k] = sin(z3[k]) + cos(y3[j])          # ABC flow with A=B=C=1: ∇×v = v\n    vy[i,j,k] = sin(x3[i]) + cos(z3[k])\n    vz[i,j,k] = sin(y3[j]) + cos(x3[i])\nend\nensure_layout!(v, :c)\n\nH = evaluate(v ⋅ curl(v), :g)      # H = u·ω, a ScalarField\nintegrate(H)                       # 744.15 — total helicity (= ∫|v|² for this flow)","title":"Helicity (3D)"},{"category":"section","location":"pages/custom_operators/#Nonlinear-Operators","page":"Custom Operators","text":"","title":"Nonlinear Operators"},{"category":"section","location":"pages/custom_operators/#Convective-Derivative","page":"Custom Operators","text":"u ⋅ grad(f) is the one-liner (see above). When you want the loop — to reuse buffers, or to get at the individual terms — write it in grid space:\n\nfunction convective_derivative(u, f)\n    ensure_layout!(f, :g)\n    result = zeros(size(get_grid_data(f)))\n    cs = u.coordsys\n\n    for (i, comp) in enumerate(u.components)\n        ensure_layout!(comp, :g)\n        df = evaluate(d(f, cs.coords[i]), :g)\n        result .+= get_grid_data(comp) .* get_grid_data(df)\n    end\n    return result\nend\n\nconvective_derivative(u, T)   # matches evaluate(u ⋅ grad(T), :g) to roundoff (2.8e-16)\n\ncs.coords[i] is the Coordinate for axis i; d(f, coord) differentiates along it.","title":"Convective Derivative"},{"category":"section","location":"pages/custom_operators/#Nonlinear-Term-(u·u)","page":"Custom Operators","text":"Inside an equation, write it directly: add_equation!(problem, \"∂t(u) - nu*lap(u) = -u⋅∇(u)\"). Outside a solve, apply convective_derivative to each velocity component:\n\nfunction nonlinear_advection(u)\n    result = VectorField(u.dist, u.coordsys, \"adv\", u.bases, u.dtype)\n\n    for (j, uj) in enumerate(u.components)\n        rj = result.components[j]\n        ensure_layout!(rj, :g)\n        get_grid_data(rj) .= convective_derivative(u, uj)\n    end\n    return result\nend","title":"Nonlinear Term (u·∇u)"},{"category":"section","location":"pages/custom_operators/#Physics-Specific-Operators","page":"Custom Operators","text":"","title":"Physics-Specific Operators"},{"category":"section","location":"pages/custom_operators/#Coriolis-Force","page":"Custom Operators","text":"For rotation about the z-axis, -2Ω ẑ × u = (2Ω u_y, -2Ω u_x, 0) has no z-component and needs no derivatives, so build it straight in grid space:\n\nfunction coriolis(u, Omega)\n    cs = u.coordsys\n    f  = VectorField(u.dist, cs, \"coriolis\", u.bases, u.dtype)\n    ux, uy = u.components[1], u.components[2]\n    ensure_layout!(ux, :g); ensure_layout!(uy, :g)\n    for comp in f.components\n        ensure_layout!(comp, :g)\n    end\n\n    get_grid_data(f.components[1]) .=  2 .* Omega .* get_grid_data(uy)   # +2Ω v\n    get_grid_data(f.components[2]) .= -2 .* Omega .* get_grid_data(ux)   # -2Ω u\n    get_grid_data(f.components[3]) .= 0\n    return f\nend\n\nfc = coriolis(v, 0.5)      # VectorField on the same bases as v\n\nNote that the ez from unit_vector_fields is a basis-less VectorField, so cross(ez, u) raises ArgumentError: Cannot compute cross product of VectorFields with different bases. Unit vectors are for equation strings (buoy*T*ez), not for evaluate.","title":"Coriolis Force"},{"category":"section","location":"pages/custom_operators/#Lorentz-Force-(MHD)","page":"Custom Operators","text":"cross (×) works directly on two vector fields that share a domain:\n\nJ = VectorField(domain3, \"J\")\nB = VectorField(domain3, \"B\")\nensure_layout!(J, :g); ensure_layout!(B, :g)\nget_grid_data(J.components[1]) .= 1.0        # J = x̂\nget_grid_data(B.components[2]) .= 1.0        # B = ŷ\nensure_layout!(J, :c); ensure_layout!(B, :c)\n\nF = evaluate(J × B, :g)                      # x̂ × ŷ = ẑ\nget_grid_data(F.components[3])[1,1,1]        # 1.0","title":"Lorentz Force (MHD)"},{"category":"section","location":"pages/custom_operators/#Tips","page":"Custom Operators","text":"","title":"Tips"},{"category":"section","location":"pages/custom_operators/#Performance","page":"Custom Operators","text":"Keep operations in same space (grid or spectral)\nMinimize transforms\nPre-compute constant factors","title":"Performance"},{"category":"section","location":"pages/custom_operators/#Validation","page":"Custom Operators","text":"Test against analytical solutions\nCheck symmetries\nVerify conservation properties","title":"Validation"},{"category":"section","location":"pages/custom_operators/#See-Also","page":"Custom Operators","text":"Operators: Built-in operators\nFields: Field types\nAPI: Operators: Reference","title":"See Also"},{"category":"section","location":"examples/kernel_operation/#KernelOperation:-Portable-CPU/GPU-Kernels","page":"KernelOperation: Portable CPU/GPU Kernels","text":"Use KernelOperation when you want a single KernelAbstractions kernel to run on both CPU and GPU backends through Tarang's architecture abstraction.\n\nImport the kernel macros by name rather than with using KernelAbstractions: KernelAbstractions also exports CPU, so a bare using Tarang, KernelAbstractions makes CPU() ambiguous and device = CPU() fails with an UndefVarError.\n\nusing Tarang\nusing KernelAbstractions: @kernel, @index, @Const\n\n# Define a simple kernel (here: y .= α * x)\n@kernel function scale_kernel!(y, α, @Const(x))\n    i = @index(Global)\n    @inbounds y[i] = α * x[i]\nend\n\n# Wrap the kernel; the do-block computes the default ndrange from the call arguments\nscale_op = KernelOperation(scale_kernel!) do y, α, x\n    length(y)  # can return a tuple for multi-dimensional arrays\nend\n\n# Example data\ndevice = CPU()  # or GPU(), which requires CUDA.jl to be loaded\nx = ones(device, Float64, 1024)\ny = zeros(device, Float64, 1024)\n\n# Launch via the operation (auto-selects backend, workgroup, etc.)\nscale_op(device, y, 2.0, x)   # y == fill(2.0, 1024)\n\nThe same wrapper works on multi-dimensional arrays as long as the kernel indexes them that way and the do-block returns the matching ndrange tuple:\n\n@kernel function scale2d!(y, α, @Const(x))\n    i, j = @index(Global, NTuple)\n    @inbounds y[i, j] = α * x[i, j]\nend\n\nop2 = KernelOperation(scale2d!) do y, α, x\n    size(y)\nend\n\nX = ones(device, Float64, 8, 4)\nY = zeros(device, Float64, 8, 4)\nop2(device, Y, 2.5, X)        # Y == fill(2.5, 8, 4)\n\nKey points:\n\nBackend selection is driven by device: CPU(), or GPU() once CUDA.jl is loaded (GPU() carries the CUDA device, so GPU(device_id=1) picks the second one). Without CUDA.jl loaded, GPU() throws a clear error telling you to load it.\nThe workgroup size is chosen for you by workgroup_size(device, ndrange) (workgroup_size(CPU(), 1024) == 64) inside launch!.\nPass ndrange to override the do-block default for a single call: scale_op(device, y, 3.0, x; ndrange=512) touches only y[1:512].\nOn the GPU, call synchronize(device) when you need to wait for the launch to finish before reading the result.\n\nIf you do not want the wrapper, launch! takes the raw kernel and an explicit ndrange, and accepts either an architecture or an array (whose architecture it infers):\n\nlaunch!(device, scale_kernel!, y, 4.0, x; ndrange=length(y))\nlaunch!(y, scale_kernel!, y, 5.0, x; ndrange=length(y))","title":"KernelOperation: Portable CPU/GPU Kernels"},{"category":"section","location":"notebooks/channel_flow/#Notebook:-Channel-Flow","page":"Notebook: Channel Flow","text":"This notebook demonstrates pressure-driven channel flow simulation.","title":"Notebook: Channel Flow"},{"category":"section","location":"notebooks/channel_flow/#Overview","page":"Notebook: Channel Flow","text":"Channel flow (plane Poiseuille flow) is a fundamental benchmark for viscous flow simulations. Fluid flows between two parallel plates driven by a pressure gradient.\n\nThe walls are resolved with a Chebyshev basis, the streamwise direction with a Fourier basis. Because the momentum equation carries the advection term -u⋅∇(u) on the explicit side, and that term contains a Chebyshev derivative, this example is serial only — a non-Fourier derivative in the explicit right-hand side is not supported under MPI (the solver raises a clear error on the first step). See Running with MPI.","title":"Overview"},{"category":"section","location":"notebooks/channel_flow/#Setup","page":"Notebook: Channel Flow","text":"using Tarang\nusing Statistics   # for mean()","title":"Setup"},{"category":"section","location":"notebooks/channel_flow/#Parameters","page":"Notebook: Channel Flow","text":"Re   = 100.0            # Reynolds number (centreline velocity, half-height)\nLx   = 4π               # Streamwise period\nLz   = 2.0              # Channel height\nnu   = 1.0 / Re         # Kinematic viscosity\ndpdx = -8 * nu / Lz^2   # Driving pressure gradient (chosen so that u_max = 1)","title":"Parameters"},{"category":"section","location":"notebooks/channel_flow/#Domain","page":"Notebook: Channel Flow","text":"The Fourier axis comes first, the Chebyshev axis last.\n\nNx, Nz = 32, 24\n\ncoords  = CartesianCoordinates(\"x\", \"z\")\ndist    = Distributor(coords; dtype=Float64, device=CPU())\nx_basis = RealFourier(coords[\"x\"]; size=Nx, bounds=(0.0, Lx), dealias=3/2)\nz_basis = ChebyshevT(coords[\"z\"]; size=Nz, bounds=(0.0, Lz), dealias=3/2)\ndomain  = Domain(dist, (x_basis, z_basis))","title":"Domain"},{"category":"section","location":"notebooks/channel_flow/#Fields","page":"Notebook: Channel Flow","text":"The velocity is a VectorField; the pressure is a ScalarField. The tau fields carry the boundary-condition unknowns: one tau per streamwise mode (hence the (x_basis,) basis tuple), plus a scalar gauge unknown tau_p for the pressure.\n\np = ScalarField(domain, \"p\")\nu = VectorField(domain, \"u\")\n\ntau_p  = ScalarField(dist, \"tau_p\", (), Float64)\ntau_u1 = VectorField(dist, coords, \"tau_u1\", (x_basis,), Float64)\ntau_u2 = VectorField(dist, coords, \"tau_u2\", (x_basis,), Float64)","title":"Fields"},{"category":"section","location":"notebooks/channel_flow/#Problem-Definition","page":"Notebook: Channel Flow","text":"The wall-normal derivative is reduced to first order with a lift term, which is what makes the tau method work: grad_u = ∇u + ẑ·lift(τ₁).\n\nex, ez     = unit_vector_fields(coords, dist)\nlift_basis = derivative_basis(z_basis, 1)\nτ_lift(A)  = lift(A, lift_basis, -1)\ngrad_u     = grad(u) + ez * τ_lift(tau_u1)\n\nproblem = IVP([p, u, tau_p, tau_u1, tau_u2])\nadd_parameters!(problem, nu=nu, dpdx=dpdx, ex=ex, ez=ez,\n                grad_u=grad_u, τ_lift=τ_lift)\n\n# Continuity (with pressure gauge)\nadd_equation!(problem, \"trace(grad_u) + tau_p = 0\")\n\n# Momentum, driven by the imposed pressure gradient\nadd_equation!(problem,\n    \"∂t(u) - nu*div(grad_u) + ∇(p) + τ_lift(tau_u2) = -u⋅∇(u) - dpdx*ex\")\n\nNames used inside an equation string must be registered with add_parameters! — a string expression cannot see plain Julia globals (an unregistered name is silently substituted with 0).","title":"Problem Definition"},{"category":"section","location":"notebooks/channel_flow/#Boundary-Conditions","page":"Notebook: Channel Flow","text":"Boundary conditions are declared with add_bc!, never with add_equation!: only add_bc! registers the condition with the boundary-condition manager. $Lz is interpolated by Julia before parsing, so the string the parser sees contains a numeric literal.\n\n# No-slip at both walls\nadd_bc!(problem, \"u(z=0) = 0\")\nadd_bc!(problem, \"u(z=$Lz) = 0\")\n\n# Pressure gauge (the pressure is only defined up to a constant)\nadd_bc!(problem, \"integ(p) = 0\")","title":"Boundary Conditions"},{"category":"section","location":"notebooks/channel_flow/#Analytical-Solution","page":"Notebook: Channel Flow","text":"For laminar flow, the exact solution is parabolic:\n\npoiseuille(z) = -dpdx / (2 * nu) * z * (Lz - z)\n\nu_max = -dpdx * Lz^2 / (8 * nu)\nprintln(\"Expected u_max = $u_max\")     # 1.0","title":"Analytical Solution"},{"category":"section","location":"notebooks/channel_flow/#Initial-Conditions","page":"Notebook: Channel Flow","text":"local_grids returns this rank's grid vectors for each axis — use it instead of a global meshgrid.\n\nxg, zg = local_grids(dist, x_basis, z_basis)\nux = u.components[1]\n\nfill_random!(ux, \"g\"; seed=42, distribution=\"normal\", scale=1e-3)\nget_grid_data(ux) .*= zg' .* (Lz .- zg')   # damp the perturbation at the walls\nget_grid_data(ux) .+= poiseuille.(zg')     # laminar base flow\nensure_layout!(u, :c)","title":"Initial Conditions"},{"category":"section","location":"notebooks/channel_flow/#Solver","page":"Notebook: Channel Flow","text":"CFL is constructed from the solver, and velocities are added as VectorFields. The controller is then handed to run!, which applies it every cadence iterations.\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\ncfl = CFL(solver; initial_dt=1e-3, cadence=10, safety=0.4, max_dt=0.01)\nadd_velocity!(cfl, u)","title":"Solver"},{"category":"section","location":"notebooks/channel_flow/#Simulation","page":"Notebook: Channel Flow","text":"Callbacks are (interval, function) tuples; the function receives the solver.\n\nreport = s -> begin\n    ensure_layout!(u, :g)\n    uc = mean(get_grid_data(ux)[:, Nz ÷ 2])\n    println(\"t = $(round(s.sim_time, digits=4)), u_center = $(round(uc, digits=6))\")\nend\n\nrun!(solver; stop_iteration=50, cfl=cfl, callbacks=[(25, report)], progress=false)\n\nOutput of the run above:\n\nt = 0.169, u_center = 0.995211\nt = 0.419, u_center = 0.995268","title":"Simulation"},{"category":"section","location":"notebooks/channel_flow/#Results","page":"Notebook: Channel Flow","text":"","title":"Results"},{"category":"section","location":"notebooks/channel_flow/#Velocity-Profile","page":"Notebook: Channel Flow","text":"ensure_layout!(u, :g)\n\n# Average over x\nu_profile    = mean(get_grid_data(ux), dims=1)[:]\nu_analytical = poiseuille.(zg)\n\nu_profile and zg are ordinary vectors, ready for the plotting package of your choice.","title":"Velocity Profile"},{"category":"section","location":"notebooks/channel_flow/#Error-Analysis","page":"Notebook: Channel Flow","text":"error = maximum(abs.(u_profile .- u_analytical))\nprintln(\"Maximum error: $error\")     # 0.000124…\n\nThe residual is the wall-damped perturbation decaying back to the laminar state. Starting from the exact Poiseuille profile with no perturbation, the same run reproduces it to 5.6e-16, and the no-slip conditions hold to 1e-16 — the discrete steady state is the analytical one.","title":"Error Analysis"},{"category":"section","location":"notebooks/channel_flow/#Turbulent-Channel-(Higher-Re)","page":"Notebook: Channel Flow","text":"Turbulent flow needs a much larger Reynolds number (Re = 5000, nu = 1/Re), and with it a finer grid (Nx = 256, Nz = 128), a longer integration, and statistical averaging over the homogeneous x direction and over time. That is a production-scale run, not a notebook demo.","title":"Turbulent Channel (Higher Re)"},{"category":"section","location":"notebooks/channel_flow/#Exercises","page":"Notebook: Channel Flow","text":"Vary Reynolds number: Compare Re = 100, 500, 1000\nConvergence study: How does error scale with Nz?\nAdd turbulence: Re = 5000 with statistics\nCouette flow: Moving top wall instead of pressure gradient","title":"Exercises"},{"category":"section","location":"notebooks/channel_flow/#References","page":"Notebook: Channel Flow","text":"Pope, S.B. (2000). Turbulent Flows\nKim, J., Moin, P., Moser, R. (1987). Turbulence statistics in channel flow","title":"References"},{"category":"section","location":"getting_started/running_with_mpi/#running-with-mpi","page":"Running with MPI","text":"Tarang.jl is designed for efficient parallel computing using MPI (Message Passing Interface). This guide covers everything you need to know about running Tarang.jl simulations in parallel.","title":"Running with MPI"},{"category":"section","location":"getting_started/running_with_mpi/#Basic-MPI-Execution","page":"Running with MPI","text":"","title":"Basic MPI Execution"},{"category":"section","location":"getting_started/running_with_mpi/#Running-a-Script","page":"Running with MPI","text":"Execute a Tarang.jl script with MPI using mpiexec:\n\nmpiexec -n 4 julia --project=. your_script.jl\n\nThe -n 4 flag specifies 4 MPI processes. The process count must match your process mesh configuration in the script.","title":"Running a Script"},{"category":"section","location":"getting_started/running_with_mpi/#Process-Mesh-Configuration","page":"Running with MPI","text":"The process mesh determines how MPI processes are arranged. The one rule that governs every choice below:\n\nwarning: The mesh must have exactly one fewer dimension than the domain\nA parallel FFT needs at least one array axis to stay local on each rank. PencilArrays decomposes the trailing length(mesh) axes, so a mesh whose rank equals the domain dimension decomposes every axis and leaves nothing for the transform. A 2-D domain takes a 1-D mesh; a 3-D domain takes a 2-D mesh.\n\n# 2D domain, 4 processes: slab decomposition (one axis split, one axis local)\ndist = Distributor(coords; mesh=(4,))\n\n# 3D domain, 4 processes: pencil decomposition (two axes split, one axis local)\ndist = Distributor(coords; mesh=(2, 2))\n\n# Best default: omit `mesh` entirely and let Tarang pick\ndist = Distributor(coords)     # 2D -> (nprocs,) ;  3D -> a 2D pencil mesh\n\nA mesh containing a unit factor — (4, 1) or (1, 4) on a 2-D domain — is silently normalized to the equivalent slab mesh (4,), so those forms work too. A genuine 2-D mesh on a 2-D domain does not:\n\ndist = Distributor(coords; mesh=(2, 2))   # 2D domain, 4 ranks -> HARD ERROR\n# ERROR: PencilFFT plan creation failed with 4 MPI processes.\n\ntip: Matching Process Count\nprod(mesh) must equal the number of MPI processes:mesh=(4,)     # 2D domain, requires: mpiexec -n 4\nmesh=(16,)    # 2D domain, requires: mpiexec -n 16\nmesh=(4, 2)   # 3D domain, requires: mpiexec -n 8\nmesh=(4, 4)   # 3D domain, requires: mpiexec -n 16","title":"Process Mesh Configuration"},{"category":"section","location":"getting_started/running_with_mpi/#Process-Mesh-Strategies","page":"Running with MPI","text":"","title":"Process Mesh Strategies"},{"category":"section","location":"getting_started/running_with_mpi/#2D-Problems","page":"Running with MPI","text":"A 2-D domain is decomposed as a slab: the last axis is split across all ranks, the first axis stays local. There is only one shape available, so the only decision is the process count:\n\nmesh=(8,)     # 8 processes, last axis split 8 ways\nmesh=(16,)    # 16 processes\n\nEach rank owns N_last / nprocs planes of the decomposed axis, so pick a process count that divides the decomposed axis size evenly. Uneven splits work — PencilArrays spreads the remainder so that some ranks get one extra plane (a 10-point axis over 4 ranks gives local sizes 2, 3, 2, 3) — but they cost you load balance.","title":"2D Problems"},{"category":"section","location":"getting_started/running_with_mpi/#3D-Problems","page":"Running with MPI","text":"For 3D problems, Tarang.jl uses pencil decomposition: two axes are distributed while the third remains contiguous (local) on each rank, which is what makes the parallel FFT efficient.\n\n# Square pencil mesh (for isotropic domains)\nmesh=(8, 8)     # 64 processes\n\n# Anisotropic mesh\nmesh=(16, 4)    # 64 processes\n\nRule of thumb: match the mesh aspect ratio to the sizes of the two decomposed (trailing) axes, so each rank's block is roughly cubic.","title":"3D Problems"},{"category":"section","location":"getting_started/running_with_mpi/#Which-Basis-Layouts-Work-Under-MPI","page":"Running with MPI","text":"","title":"Which Basis Layouts Work Under MPI"},{"category":"section","location":"getting_started/running_with_mpi/#Pure-Fourier-—-any-dimension-2D","page":"Running with MPI","text":"coords = CartesianCoordinates(\"x\", \"y\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxb = RealFourier(coords[\"x\"]; size=32, bounds=(0.0, 2π))\nyb = RealFourier(coords[\"y\"]; size=32, bounds=(0.0, 2π))\ndom = Domain(dist, (xb, yb))          # decomposed on y","title":"Pure Fourier — any dimension ≥ 2D"},{"category":"section","location":"getting_started/running_with_mpi/#Mixed-Chebyshev–Fourier-—-supported,-but-Chebyshev-must-come-FIRST","page":"Running with MPI","text":"PencilArrays decomposes the trailing axes, and a Chebyshev axis cannot be decomposed: its DCT is a local FFTW r2r that needs the entire axis on every rank. So under MPI the Chebyshev axis must be listed before the Fourier axes — the reverse of the conventional (x Fourier, z Chebyshev) channel layout used in serial.\n\ncoords = CartesianCoordinates(\"z\", \"x\")            # note the coordinate order too\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nzb = ChebyshevT(coords[\"z\"];  size=12, bounds=(0.0, 1.0))\nxb = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\ndomain = Domain(dist, (zb, xb))                    # Chebyshev FIRST\n\nGetting this wrong is a loud error, not a silent wrong answer:\n\nMPI mixed Fourier-Chebyshev: the decomposed (trailing) axis/axes [2] are non-Fourier (e.g. Chebyshev), but PencilArrays decomposes the LAST 1 dimension(s) and a Chebyshev axis cannot be decomposed ... Reorder your bases so the Chebyshev axis comes BEFORE the Fourier axes\n\nBecause the Chebyshev axis must stay local, a 2-D Chebyshev–Fourier domain has exactly one decomposable axis and therefore only ever supports a 1-D mesh.\n\nwarning: `ChannelDomain` / `ChannelDomain3D` are serial-only\nThe ChannelDomain helpers build (x Fourier, z Chebyshev) — Chebyshev last — and so hit the error above under MPI. For a distributed channel, build the Domain yourself with the Chebyshev axis first.\n\nA complete distributed Chebyshev–Fourier diffusion problem (this runs identically at -n 1, -n 2 and -n 4):\n\nb    = ScalarField(domain, \"b\")\ntau1 = ScalarField(dist, \"tau1\", (), Float64)   # under MPI, tau fields carry NO bases\ntau2 = ScalarField(dist, \"tau2\", (), Float64)\n\nez, ex   = unit_vector_fields(coords, dist)     # coords are (\"z\",\"x\") => ez is first\nτ_lift(A) = lift(A, derivative_basis(zb, 1), -1)\ngrad_b    = grad(b) + ez * τ_lift(tau1)\n\nproblem = IVP([b, tau1, tau2])\nadd_parameters!(problem, kappa=0.1, ez=ez, grad_b=grad_b, τ_lift=τ_lift)\nadd_equation!(problem, \"∂t(b) - kappa*div(grad_b) + τ_lift(tau2) = 0\")\nadd_bc!(problem, \"b(z=0) = 0\")\nadd_bc!(problem, \"b(z=1) = 0\")\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\nzg, xg = local_grids(dist, zb, xb)              # per-rank LOCAL grids\nensure_layout!(b, :g)\nget_grid_data(b) .= sin.(π .* zg) .* (1 .+ 0.5 .* cos.(2 .* xg'))\nensure_layout!(b, :c)\n\nrun!(solver; stop_iteration=20, progress=false)\n\nPer-Fourier-mode tau fields (ScalarField(dist, \"tau\", (xbasis,), Float64)) only work with the Fourier axis first, which MPI forbids — under MPI use bases-free () tau fields and accept boundary enforcement at ~1e-6 instead of machine precision.","title":"Mixed Chebyshev–Fourier — supported, but Chebyshev must come FIRST"},{"category":"section","location":"getting_started/running_with_mpi/#What-Is-Not-Supported-Under-MPI","page":"Running with MPI","text":"These all raise clear errors; none of them silently produce wrong answers.\n\nNot supported Message / behaviour Do this instead\n1D domains MPI parallelization is not supported for 1D problems. 1D FFT requires global data access. Run 1D problems serially.\nPure-Chebyshev domains MPI parallelization is not supported for pure Chebyshev domains. Needs at least one Fourier axis to decompose; run serially.\nChebyshev axis not first The \"Reorder your bases\" error above. Put the Chebyshev axis first.\nMesh rank == domain rank PencilFFT plan creation failed with N MPI processes. Use a mesh with one fewer dimension than the domain.\nset!(field, ::Function) DimensionMismatch (it builds the global meshgrid and writes it into the local slab). Use local_grids(dist, bases...) and broadcast into get_grid_data(field).\nGrid-space set_scales! / change_scales! Grid-space resampling of a distributed field ... is not supported under MPI Resample in coefficient space, or set the scales before distributing.\nChebyshev derivative on the explicit (RHS) side First step errors: Lazy RHS: cannot differentiate along the non-Fourier axis 1 (ChebyshevT) of a DISTRIBUTED field Move the term to the implicit (L) side. RHS derivatives along Fourier axes are fine.\n\nThe last one is worth spelling out: on a Chebyshev–Fourier domain, -u⋅∇(u) and -u⋅∇(T) expand to include ∂z, so the classic 2D Rayleigh–Bénard / channel setup with advection runs in serial only. A nonlinearity whose derivatives are all along Fourier axes — e.g. -b*∂x(b) — runs distributed and matches serial to roundoff.","title":"What Is Not Supported Under MPI"},{"category":"section","location":"getting_started/running_with_mpi/#Setting-Initial-Conditions-in-Parallel","page":"Running with MPI","text":"set!(field, ::Function) is serial-only. The distributed-safe idiom uses the rank's local grid vectors:\n\nxg, yg = local_grids(dist, xb, yb)\nensure_layout!(u, :g)\nget_grid_data(u) .= sin.(xg) .* cos.(yg')     # correct at any process count\nensure_layout!(u, :c)\n\nset!(field, ::Number) and fill_random!(field, \"g\"; seed=..., scale=...) are safe distributed.","title":"Setting Initial Conditions in Parallel"},{"category":"section","location":"getting_started/running_with_mpi/#Environment-Variables","page":"Running with MPI","text":"","title":"Environment Variables"},{"category":"section","location":"getting_started/running_with_mpi/#Thread-Control","page":"Running with MPI","text":"Under MPI, one rank per core is the intended model: when MPI.Comm_size > 1 Tarang forces FFTW to a single thread, regardless of JULIA_NUM_THREADS. Keep other libraries (BLAS, OpenMP) from oversubscribing the same cores:\n\nexport OMP_NUM_THREADS=1\nmpiexec -n 8 julia --project=. script.jl\n\nTo override Tarang's FFTW thread count deliberately — e.g. a few fat ranks on a many-core node — set TARANG_FFTW_THREADS:\n\nexport TARANG_FFTW_THREADS=4     # 4 FFTW threads per rank\nmpiexec -n 2 julia --project=. script.jl","title":"Thread Control"},{"category":"section","location":"getting_started/running_with_mpi/#Other-Useful-Variables","page":"Running with MPI","text":"# Tarang logging, read at `using Tarang` time\nexport TARANG_LOG_LEVEL=DEBUG      # TRACE, DEBUG, INFO, NOTICE, WARN, ERROR\nexport TARANG_LOG_FILE=tarang.log  # optional log file","title":"Other Useful Variables"},{"category":"section","location":"getting_started/running_with_mpi/#HPC-Cluster-Execution","page":"Running with MPI","text":"","title":"HPC Cluster Execution"},{"category":"section","location":"getting_started/running_with_mpi/#SLURM","page":"Running with MPI","text":"Example SLURM submission script:\n\n#!/bin/bash\n#SBATCH --job-name=tarang_sim\n#SBATCH --nodes=4\n#SBATCH --ntasks-per-node=32\n#SBATCH --time=24:00:00\n#SBATCH --partition=compute\n\n# Load modules\nmodule load julia/1.9\nmodule load openmpi/4.1\n\n# Set environment\nexport OMP_NUM_THREADS=1\nexport JULIA_NUM_THREADS=1\n\n# Calculate total tasks\nNTASKS=$((SLURM_NNODES * SLURM_NTASKS_PER_NODE))\n\n# Run simulation\nsrun -n $NTASKS julia --project=. simulation.jl\n\nSubmit with:\n\nsbatch submit_tarang.sh","title":"SLURM"},{"category":"section","location":"getting_started/running_with_mpi/#PBS/Torque","page":"Running with MPI","text":"Example PBS script:\n\n#!/bin/bash\n#PBS -N tarang_sim\n#PBS -l nodes=4:ppn=32\n#PBS -l walltime=24:00:00\n#PBS -q batch\n\ncd $PBS_O_WORKDIR\n\nmodule load julia/1.9\nmodule load openmpi/4.1\n\nexport OMP_NUM_THREADS=1\n\nmpiexec julia --project=. simulation.jl\n\nSubmit with:\n\nqsub submit_tarang.pbs","title":"PBS/Torque"},{"category":"section","location":"getting_started/running_with_mpi/#Load-Balancing","page":"Running with MPI","text":"","title":"Load Balancing"},{"category":"section","location":"getting_started/running_with_mpi/#Automatic-Load-Balancing","page":"Running with MPI","text":"Tarang.jl automatically distributes work across MPI processes using PencilArrays. For balanced performance:\n\nDivide the decomposed axes evenly: nprocs should divide the size of the trailing axis (2D) or of both trailing axes (3D); otherwise some ranks carry an extra plane.\nUse power-of-2 process counts when possible (4, 8, 16, 32, ...) — spectral sizes usually are.\nConsider memory: each process needs enough RAM for its subdomain.","title":"Automatic Load Balancing"},{"category":"section","location":"getting_started/running_with_mpi/#Checking-Load-Distribution","page":"Running with MPI","text":"Add diagnostics to your script. Tarang does not re-export MPI, so import it yourself:\n\nusing Tarang, MPI\n\nrank  = MPI.Comm_rank(MPI.COMM_WORLD)\nnproc = MPI.Comm_size(MPI.COMM_WORLD)\n\nlocal_size = size(parent(get_grid_data(T)))   # this rank's slab\n\nprintln(\"Rank $rank/$nproc: local grid size = $local_size\")\n\nNote parent(...): get_grid_data returns a PencilArray under MPI, and parent gets the raw local storage. PencilArrays.range_local(get_grid_data(T)) gives this rank's global index range.","title":"Checking Load Distribution"},{"category":"section","location":"getting_started/running_with_mpi/#Communication-Patterns","page":"Running with MPI","text":"","title":"Communication Patterns"},{"category":"section","location":"getting_started/running_with_mpi/#PencilArrays-Transposes","page":"Running with MPI","text":"Tarang.jl uses PencilArrays for efficient data distribution. Key operations:\n\nFFTs: May require transposes between different pencil orientations\nDerivatives: Computed in spectral space (minimal communication)\nNonlinear terms: Evaluated in grid space (may require transforms)","title":"PencilArrays Transposes"},{"category":"section","location":"getting_started/running_with_mpi/#Reductions-Are-Already-Collective","page":"Running with MPI","text":"A reduction over a whole PencilArray (sum, maximum, mapreduce, ...) performs its own Allreduce: it is a collective, so every rank must reach it, and its result is already global. Wrapping it in another MPI.Allreduce double-reduces — an nprocs× error that is silent on x86 (on Apple Silicon the PencilArray reduction throws instead: \"User-defined reduction operators are currently not supported on non-Intel architectures\"). Reduce the local storage and do one explicit collective, or use Tarang's helpers:\n\ncomm = MPI.COMM_WORLD\ngd   = get_grid_data(u)\n\n# Correct: reduce the local slab, then one collective\nlmax = maximum(abs, parent(gd));  gmax = MPI.Allreduce(lmax, MPI.MAX, comm)\n\n# Correct: Tarang's helpers already do exactly that\nglobal_max(u)     # Float64\nglobal_sum(u)     # Float64\nintegrate(u)      # Float64, quadrature-weighted\n\n# WRONG: sum(gd) is itself an Allreduce -> nprocs× too large\nMPI.Allreduce(sum(gd), MPI.SUM, comm)\n\nThe same rule applies to global_max / global_sum / integrate: they are collective, so call them on every rank and only guard the println with if rank == 0, never the call itself.","title":"Reductions Are Already Collective"},{"category":"section","location":"getting_started/running_with_mpi/#Minimizing-Communication","page":"Running with MPI","text":"To reduce communication overhead:\n\nGroup operations: Batch multiple operations before synchronizing\nKeep derivatives spectral: they are local in coefficient space\nOptimize process mesh: align with the dominant communication direction","title":"Minimizing Communication"},{"category":"section","location":"getting_started/running_with_mpi/#Performance-Monitoring","page":"Running with MPI","text":"","title":"Performance Monitoring"},{"category":"section","location":"getting_started/running_with_mpi/#MPI-Profiling","page":"Running with MPI","text":"Use MPI profiling tools to identify bottlenecks:\n\n# With mpiP\nmpiexec -n 8 julia --project=. script.jl\n\n# With Intel MPI\nexport I_MPI_STATS=20\nmpiexec -n 8 julia --project=. script.jl","title":"MPI Profiling"},{"category":"section","location":"getting_started/running_with_mpi/#Tarang.jl-Built-in-Profiling","page":"Running with MPI","text":"Enable performance logging:\n\nusing Tarang\n\n# Setup logging with MPI awareness\nTarang.setup_tarang_logging(\n    level=\"INFO\",\n    filename=\"performance.log\",\n    mpi_aware=true,\n    console=true\n)","title":"Tarang.jl Built-in Profiling"},{"category":"section","location":"getting_started/running_with_mpi/#Timing-Critical-Sections","page":"Running with MPI","text":"Add timing to your simulation:\n\nusing Printf, MPI\n\nt_start = time()\n\n# ... simulation code ...\n\nt_end = time()\nelapsed = t_end - t_start\n\nif MPI.Comm_rank(MPI.COMM_WORLD) == 0\n    @printf \"Total time: %.2f seconds\\n\" elapsed\nend","title":"Timing Critical Sections"},{"category":"section","location":"getting_started/running_with_mpi/#Debugging-MPI-Applications","page":"Running with MPI","text":"","title":"Debugging MPI Applications"},{"category":"section","location":"getting_started/running_with_mpi/#Running-in-Serial","page":"Running with MPI","text":"Test your code without MPI first:\n\njulia --project=. script.jl  # No mpiexec\n\nThe same script runs serially and in parallel — just omit mesh= and let the Distributor size itself from the communicator:\n\ndist = Distributor(coords)   # mesh = (1,) on 1 process, (nprocs,) under mpiexec","title":"Running in Serial"},{"category":"section","location":"getting_started/running_with_mpi/#MPI-Debugging-Tools","page":"Running with MPI","text":"Use parallel debuggers:\n\n# With TotalView\nmpiexec -tv -n 4 julia script.jl\n\n# With DDT (ARM Forge)\nddt mpiexec -n 4 julia script.jl\n\n# With gdb (attach to rank 0)\nmpiexec -n 4 xterm -e gdb julia script.jl","title":"MPI Debugging Tools"},{"category":"section","location":"getting_started/running_with_mpi/#Rank-Specific-Output","page":"Running with MPI","text":"Debug specific MPI ranks. Reduce the rank's local slab (parent(...)) — a reduction over the whole PencilArray is collective and must be called by every rank:\n\nusing Tarang, MPI\n\nrank = MPI.Comm_rank(MPI.COMM_WORLD)\nlmax = maximum(abs, parent(get_grid_data(u)))   # this rank's slab only\n\nif rank == 0\n    println(\"Debug: Rank 0 max|u| = \", lmax)\nend\n\n# Or print from every rank\nprintln(\"Rank $rank: max|u| = $lmax\")","title":"Rank-Specific Output"},{"category":"section","location":"getting_started/running_with_mpi/#Common-Issues-and-Solutions","page":"Running with MPI","text":"","title":"Common Issues and Solutions"},{"category":"section","location":"getting_started/running_with_mpi/#Deadlocks","page":"Running with MPI","text":"Symptom: Program hangs without error\n\nCauses:\n\nMismatched collective operations\nRanks waiting for different communications\nIncorrect synchronization\n\nSolution: Ensure all ranks participate in collective operations:\n\n# All ranks must call collective operations\nMPI.Barrier(MPI.COMM_WORLD)  # All ranks must call this","title":"Deadlocks"},{"category":"section","location":"getting_started/running_with_mpi/#Load-Imbalance","page":"Running with MPI","text":"Symptom: Some ranks finish much faster than others\n\nCause: the decomposed axis does not divide evenly by the process count, so some ranks carry an extra plane of the trailing axis.\n\nSolution: choose a process count that divides the decomposed axis size:\n\n# 2D domain, trailing axis size 100\nmesh=(8,)    # 100 = 12×8 + 4  -> four ranks carry 13 planes, four carry 12\nmesh=(4,)    # 100 = 25×4      -> balanced","title":"Load Imbalance"},{"category":"section","location":"getting_started/running_with_mpi/#Memory-Issues","page":"Running with MPI","text":"Symptom: Out of memory errors on some ranks\n\nSolution: Reduce resolution or increase process count:\n\n# Reduce resolution\nx_basis = RealFourier(coords[\"x\"]; size=512, bounds=(0.0, 2π))  # was 1024\n\n# Or use more processes to distribute memory (2D domain -> 1D mesh)\nmesh=(16,)   # was (4,)","title":"Memory Issues"},{"category":"section","location":"getting_started/running_with_mpi/#Wrong-Number-of-Processes","page":"Running with MPI","text":"Symptom:\n\nERROR: ArgumentError: Mesh size 9 does not match number of processes 4\n\nSolution: Match mpiexec -n to prod(mesh):\n\nmesh=(4, 2)  # 3D domain, requires mpiexec -n 8","title":"Wrong Number of Processes"},{"category":"section","location":"getting_started/running_with_mpi/#PencilFFT-Plan-Creation-Failed","page":"Running with MPI","text":"Symptom:\n\nERROR: PencilFFT plan creation failed with 4 MPI processes.\n\nCause: the mesh has as many dimensions as the domain, so no axis is left local for the FFT.\n\nSolution: drop a mesh dimension — (2, 2) → (4,) on a 2-D domain — or omit mesh=.","title":"PencilFFT Plan Creation Failed"},{"category":"section","location":"getting_started/running_with_mpi/#Performance-Tips","page":"Running with MPI","text":"","title":"Performance Tips"},{"category":"section","location":"getting_started/running_with_mpi/#Optimal-Process-Count","page":"Running with MPI","text":"Divide the decomposed axis evenly: nprocs | N_trailing (2D), or both trailing axes (3D)\nProfile with different counts: Try 4, 8, 16, 32 processes\nCheck scaling: Plot speedup vs. process count\nConsider communication: More processes = more transpose traffic per FFT","title":"Optimal Process Count"},{"category":"section","location":"getting_started/running_with_mpi/#Node-Level-Optimization","page":"Running with MPI","text":"On multi-socket nodes:\n\n# Bind processes to cores\nmpiexec -n 16 --bind-to core julia script.jl\n\n# Use one process per socket\nmpiexec -n 2 --map-by socket julia script.jl","title":"Node-Level Optimization"},{"category":"section","location":"getting_started/running_with_mpi/#Network-Optimization","page":"Running with MPI","text":"For InfiniBand or high-speed networks:\n\n# Enable UCX (if available)\nexport OMPI_MCA_pml=ucx\nexport OMPI_MCA_osc=ucx\n\nmpiexec -n 32 julia script.jl","title":"Network Optimization"},{"category":"section","location":"getting_started/running_with_mpi/#Benchmarking","page":"Running with MPI","text":"","title":"Benchmarking"},{"category":"section","location":"getting_started/running_with_mpi/#Weak-Scaling","page":"Running with MPI","text":"Keep the number of points per process constant while the total grows. With a slab mesh only the trailing axis is split, so grow both axes and let the split absorb the extra ranks:\n\n# 4 processes:  local slab 256×32   = 8192 points\nmesh=(4,);  x_size=256;  z_size=128\n\n# 16 processes: local slab 512×16   = 8192 points\nmesh=(16,); x_size=512;  z_size=256\n\n# 64 processes: local slab 1024×8   = 8192 points\nmesh=(64,); x_size=1024; z_size=512\n\nIdeal weak scaling: time remains constant.","title":"Weak Scaling"},{"category":"section","location":"getting_started/running_with_mpi/#Strong-Scaling","page":"Running with MPI","text":"Keep total problem size constant, increase processes:\n\n# All use: x_size=1024, z_size=512  (2D domain -> 1D mesh)\nmesh=(4,)    # 4 processes,  local 1024×128\nmesh=(8,)    # 8 processes,  local 1024×64\nmesh=(16,)   # 16 processes, local 1024×32\n\nIdeal strong scaling: time decreases linearly with processes.","title":"Strong Scaling"},{"category":"section","location":"getting_started/running_with_mpi/#Next-Steps","page":"Running with MPI","text":"First Steps: Basic Tarang.jl workflow\nTutorials: Example simulations with MPI\nConfiguration: Advanced MPI configuration options\nParallelism Guide: Deep dive into parallel algorithms","title":"Next Steps"},{"category":"section","location":"pages/parallelism/#Parallelism","page":"Parallelism","text":"Tarang.jl uses MPI for distributed-memory parallelism.","title":"Parallelism"},{"category":"section","location":"pages/parallelism/#MPI-Basics","page":"Parallelism","text":"","title":"MPI Basics"},{"category":"section","location":"pages/parallelism/#Initialization","page":"Parallelism","text":"using MPI\nusing Tarang\n\n# Always initialize MPI first\nMPI.Init()\n\n# Get process info\nrank = MPI.Comm_rank(MPI.COMM_WORLD)\nnprocs = MPI.Comm_size(MPI.COMM_WORLD)\n\n# Your simulation code here\n# ...\n\n# Always finalize MPI\nMPI.Finalize()\n\nDistributor calls MPI.Init() for you if you have not called it, so the explicit MPI.Init() is only required when you touch MPI before building the distributor.","title":"Initialization"},{"category":"section","location":"pages/parallelism/#Running-with-MPI","page":"Parallelism","text":"# Run with 4 processes\nmpiexec -n 4 julia --project simulation.jl\n\n# With thread control\nexport OMP_NUM_THREADS=1\nmpiexec -n 4 julia --project simulation.jl","title":"Running with MPI"},{"category":"section","location":"pages/parallelism/#Process-Mesh","page":"Parallelism","text":"The distributor organizes MPI processes into a mesh. The mesh has one fewer dimension than the domain: one axis must stay local on every rank for the FFT to be planned. A mesh with as many dimensions as the domain decomposes every axis, leaves no local axis, and PencilFFTs fails to plan.\n\n# 2D domain -> 1D mesh\ncoords = CartesianCoordinates(\"x\", \"y\")\ndist = Distributor(coords; mesh=(4,))          # 4 processes\n\n# 3D domain -> 2D mesh\ncoords = CartesianCoordinates(\"x\", \"y\", \"z\")\ndist = Distributor(coords; mesh=(2, 2))        # 4 processes\n\nIn practice, omit mesh=. The auto-generated mesh already follows the rule: a 2D domain gets (nprocs,), a 3D domain gets a balanced 2D mesh ((2,2) at 4 processes, (2,4) at 8).\n\ndist = Distributor(coords; dtype=Float64, device=CPU())   # mesh chosen for you\ndist.mesh    # (4,) for a 2D domain on 4 ranks; (2, 2) for a 3D domain on 4 ranks","title":"Process Mesh"},{"category":"section","location":"pages/parallelism/#Mesh-Guidelines","page":"Parallelism","text":"Processes 2D domain (1D mesh) 3D domain (2D mesh)\n4 (4,) (2, 2)\n8 (8,) (2, 4)\n16 (16,) (4, 4)\n64 (64,) (8, 8)\n\nRules:\n\nProduct of mesh = number of processes\nlength(mesh) == ndims(domain) - 1 — one axis stays local for the FFT\nA square mesh on a 2D domain (e.g. mesh=(2,2) with 4 ranks) is a hard error: PencilFFT plan creation failed with 4 MPI processes\nFor 3D, keep the mesh close to square to balance transpose volume","title":"Mesh Guidelines"},{"category":"section","location":"pages/parallelism/#Domain-Decomposition","page":"Parallelism","text":"","title":"Domain Decomposition"},{"category":"section","location":"pages/parallelism/#Pencil-Decomposition","page":"Parallelism","text":"Tarang uses PencilArrays/PencilFFTs. The trailing axes are decomposed; the leading axis stays local. A 2D domain is therefore cut into slabs:\n\n2D domain (32×32) decomposed across 4 processes:\n\nFull Domain:        Decomposed (mesh = (4,)):\n┌─────────────┐     ┌──────┬──────┬──────┬──────┐\n│             │     │      │      │      │      │\n│    32×32    │     │  P0  │  P1  │  P2  │  P3  │\n│             │     │ 32×8 │ 32×8 │ 32×8 │ 32×8 │\n└─────────────┘     └──────┴──────┴──────┴──────┘\n                       y is split, x stays local\n\nA 3D domain gets a 2D mesh: the last two axes are split into pencils, the first stays local.","title":"Pencil Decomposition"},{"category":"section","location":"pages/parallelism/#Local-vs-Global","page":"Parallelism","text":"# Local data on this process\nensure_layout!(u, :g)\nlocal_data = get_grid_data(u)     # a PencilArray when nprocs > 1\nlocal_size = size(local_data)     # e.g. (32, 8) at np=4 on a 32×32 grid\n\n# Global size — bases expose their metadata under `.meta`\nglobal_size = (bx.meta.size, by.meta.size)     # (32, 32)\n\n# This rank's global index range on an axis (a UnitRange, not a start index)\nlocal_range = Tarang.local_indices(dist, 2, by.meta.size)   # rank 2 of 4 -> 17:24\n\n# Same information straight from PencilArrays\nusing PencilArrays\nPencilArrays.range_local(local_data)    # rank 2 of 4 -> (1:32, 17:24)\nparent(local_data)                      # the raw local storage array\n\nGrid coordinates come from local_grids, which returns this rank's slice of each axis, so the same code is correct serially and in parallel:\n\nxg, yg = local_grids(dist, bx, by)\nensure_layout!(u, :g)\nget_grid_data(u) .= sin.(xg) .* cos.(yg')\nensure_layout!(u, :c)\n\nset!(field, ::Function) is serial only — it builds the global meshgrid and broadcasts it into the local slab, which throws DimensionMismatch under MPI. Use the local_grids form above. set!(field, ::Number) and fill_random! are safe in parallel.","title":"Local vs Global"},{"category":"section","location":"pages/parallelism/#Restrictions-under-MPI","page":"Parallelism","text":"Distributed runs are more constrained than serial ones:\n\nA Chebyshev axis must come first. PencilArrays decomposes the trailing axes, and a Chebyshev axis cannot be split (its DCT needs the whole axis on each rank). Put the Chebyshev basis before the Fourier bases — and order the coordinates the same way.\ncoords = CartesianCoordinates(\"z\", \"x\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nzb     = ChebyshevT(coords[\"z\"]; size=12, bounds=(0.0, 1.0))\nxb     = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2π))\ndomain = Domain(dist, (zb, xb))       # Chebyshev FIRST\nChebyshev last raises a clear error rather than silently producing a wrong answer.\nNo Chebyshev derivative on the explicit (RHS) side. A term that differentiates along the non-Fourier axis — including -u⋅∇(u) advection on a Chebyshev–Fourier domain — errors on the first step, because a rank owns only part of that axis' coefficients. Move such terms to the implicit (L) side, or run serially. RHS derivatives along Fourier axes (e.g. -b*∂x(b)) are fine.\nPer-Fourier-mode tau fields do not work, since they require the Fourier axis first. Use tau fields with no bases (ScalarField(dist, \"tau1\", (), Float64)).","title":"Restrictions under MPI"},{"category":"section","location":"pages/parallelism/#Communication-Patterns","page":"Parallelism","text":"","title":"Communication Patterns"},{"category":"section","location":"pages/parallelism/#Automatic-Communication","page":"Parallelism","text":"Communication happens automatically during:\n\nLayout transforms: Grid ↔ Spectral\nGlobal reductions: global_sum, global_max, global_min, global_mean, integrate\nFile I/O: per-rank writes, plus the manifest/merge step","title":"Automatic Communication"},{"category":"section","location":"pages/parallelism/#Manual-Communication","page":"Parallelism","text":"Use the exported helpers — they already perform the collective correctly:\n\nensure_layout!(u, :g)\ngmax = global_max(u)     # same value on every rank\ngmin = global_min(u)\ngsum = global_sum(u)\ngmean = global_mean(u)\ntotal = integrate(u)     # quadrature-weighted domain integral\n\nIf you must reduce by hand, reduce the local storage with parent, then do exactly one Allreduce:\n\ngd = get_grid_data(u)   # PencilArray under MPI\n\nlocal_max = maximum(abs, parent(gd))\nglobal_max_val = MPI.Allreduce(local_max, MPI.MAX, MPI.COMM_WORLD)\n\nlocal_sum = sum(parent(gd))\nglobal_sum_val = MPI.Allreduce(local_sum, MPI.SUM, MPI.COMM_WORLD)\n\nwarning: Never Allreduce a whole-PencilArray reduction\nsum/maximum/minimum applied to a PencilArray are already collective — they Allreduce internally and return the global value. Wrapping them in another MPI.Allreduce double-reduces (a sum comes out nprocs× too large). On Apple Silicon, sum(gd) on a PencilArray does not even run: \"User-defined reduction operators are currently not supported on non-Intel architectures\". Always reduce parent(gd), or use the helpers above.","title":"Manual Communication"},{"category":"section","location":"pages/parallelism/#Output-Strategies","page":"Parallelism","text":"","title":"Output Strategies"},{"category":"section","location":"pages/parallelism/#NetCDF-File-Handler","page":"Parallelism","text":"Under MPI, add_file_handler writes one file per rank, each holding that rank's local slab — there is no gather to rank 0. Passing the handler a solver auto-registers it, so run! processes it every step:\n\nh = add_file_handler(\"snapshots\", solver; iter=5, max_writes=10)\nadd_task!(h, u; name=\"u\")\nrun!(solver; stop_iteration=10, progress=false)\n\nAt 2 ranks this writes snapshots/snapshots_s1/snapshots_s1_p0.nc and snapshots/snapshots_s1/snapshots_s1_p1.nc. The data lives in NetCDF-4 groups, so read it with the group helpers (on a 32×32 grid at np=2, rank 0's file holds (nwrites, 32, 16) — its slab, not the global array):\n\nf = \"snapshots/snapshots_s1/snapshots_s1_p0.nc\"\nTarang.group_variable_names(f, \"vars\")   # [\"u\"]\nTarang.group_ncread(f, \"vars\", \"u\")      # (nwrites, 32, 16) at np=2\n\nPros: scalable, no rank-0 memory limit Cons: post-processing needed to stitch the ranks together","title":"NetCDF File Handler"},{"category":"section","location":"pages/parallelism/#Virtual-Mode","page":"Parallelism","text":"VirtualFileHandler writes per-rank files plus a rank-0 manifest recording each slab's global offsets, which merge_virtual! uses to reconstruct the global array offline. It is driven manually (it is not accepted by run!'s outputs=):\n\nvfh = VirtualFileHandler(outdir, \"virt\"; comm=MPI.COMM_WORLD, cadence=5)\nadd_task!(vfh, u; name=\"u\")\n\nwall0 = time()\nsolver.stop_iteration = 10\nwhile proceed(solver)\n    step!(solver)\n    Tarang.process!(vfh, solver, time() - wall0, solver.sim_time, solver.iteration)\nend\n\n# Post-processing (rank 0): stitch the per-rank slabs back into the global array\nif rank == 0\n    merged = merge_virtual!(vfh; set_num=1)   # -> virt_s1_merged.nc, u has the global shape\nend\n\nPros: scalable parallel I/O, reconstruction metadata included Cons: an extra merge step","title":"Virtual Mode"},{"category":"section","location":"pages/parallelism/#Performance-Optimization","page":"Parallelism","text":"","title":"Performance Optimization"},{"category":"section","location":"pages/parallelism/#Load-Balance","page":"Parallelism","text":"Ensure even distribution:\n\n# Good: Global size divisible by mesh\nN = 256  # 256 / 4 = 64 per process\nmesh = (4,)\n\n# Bad: Uneven distribution\nN = 250  # Runs, but some ranks get more points than others\n\nUneven splits are legal — a 30-point axis across 4 ranks is spread 7/8/7/8 — but the fullest rank sets the pace of every transpose.","title":"Load Balance"},{"category":"section","location":"pages/parallelism/#Communication-Minimization","page":"Parallelism","text":"# Minimize layout transforms\nensure_layout!(u, :g)\n# Do all grid-space operations\n# Then transform once\nensure_layout!(u, :c)\n\nEach :g↔:c transform on a distributed field is a global transpose, so batch grid-space work rather than round-tripping the layout inside a loop.","title":"Communication Minimization"},{"category":"section","location":"pages/parallelism/#FFT-Plans","page":"Parallelism","text":"Tarang builds its FFT plans once, when the domain and solver are constructed, and reuses them for the whole run. There is no user-facing planning-rigor or FFTW-wisdom knob — the planning cost is a fixed part of startup, not something you tune per run.","title":"FFT Plans"},{"category":"section","location":"pages/parallelism/#Troubleshooting","page":"Parallelism","text":"","title":"Troubleshooting"},{"category":"section","location":"pages/parallelism/#Common-Issues","page":"Parallelism","text":"Deadlock: All processes must call MPI functions together\n\n# Bad - only rank 0 calls\nif rank == 0\n    MPI.Barrier(MPI.COMM_WORLD)\nend\n\n# Good - all ranks call\nMPI.Barrier(MPI.COMM_WORLD)\nif rank == 0\n    # Only rank 0 does work\nend\n\nThe same trap applies to the collective reductions above: global_max(u) and friends must be called on every rank, never inside an if rank == 0 block.\n\nMemory errors: Each process memory limited\n\n# Estimate memory per process\nmemory = total_points * bytes_per_point / nprocs","title":"Common Issues"},{"category":"section","location":"pages/parallelism/#Debugging","page":"Parallelism","text":"# Print from specific rank\nif rank == 0\n    println(\"Debug info...\")\nend\n\n# Print from all ranks (with ordering)\nfor r in 0:nprocs-1\n    if rank == r\n        println(\"Rank $r: \", size(get_grid_data(u)))\n    end\n    MPI.Barrier(MPI.COMM_WORLD)\nend","title":"Debugging"},{"category":"section","location":"pages/parallelism/#See-Also","page":"Parallelism","text":"Coordinates: Distributor setup\nDomains: Domain decomposition\nRunning with MPI: Getting started","title":"See Also"},{"category":"section","location":"tutorials/analysis_and_output/#Tutorial:-Analysis-and-Output","page":"Analysis & Output","text":"This tutorial covers saving simulation data and computing diagnostics in Tarang.jl.","title":"Tutorial: Analysis and Output"},{"category":"section","location":"tutorials/analysis_and_output/#Setup","page":"Analysis & Output","text":"Every block on this page runs against the same small periodic problem: a scalar temperature T advected by a velocity u on a 16×16 doubly-Fourier domain.\n\nusing Tarang\n\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2pi), dealias=3/2)\nzbasis = RealFourier(coords[\"z\"]; size=16, bounds=(0.0, 2pi), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\nT = ScalarField(domain, \"T\")\nu = VectorField(domain, \"u\")\n\nproblem = IVP([T, u])\nadd_parameters!(problem, kappa=0.05, nu=0.05)\nadd_equation!(problem, \"∂t(T) - kappa*lap(T) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*lap(u) = 0\")\n\n# Initial condition: local_grids is correct both in serial and under MPI\nxg, zg = local_grids(dist, xbasis, zbasis)\nensure_layout!(T, :g)\nget_grid_data(T) .= sin.(xg) .* cos.(zg')\nensure_layout!(T, :c)\n\nux, uz = u.components\nensure_layout!(ux, :g); get_grid_data(ux) .= 0.5;             ensure_layout!(ux, :c)\nensure_layout!(uz, :g); get_grid_data(uz) .= 0.2 .* sin.(xg); ensure_layout!(uz, :c)\n\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)","title":"Setup"},{"category":"section","location":"tutorials/analysis_and_output/#File-Output","page":"Analysis & Output","text":"Output goes through a NetCDF file handler. add_file_handler has two methods, and they do not behave the same way in run!.","title":"File Output"},{"category":"section","location":"tutorials/analysis_and_output/#Handler-bound-to-the-solver","page":"Analysis & Output","text":"Pass the solver: the handler auto-registers with it, and run! processes it at its own cadence — no manual process! anywhere. This is the recommended form.\n\nhandler = add_file_handler(\"output/snapshots\", solver;\n                           parallel=\"gather\",   # parallel I/O mode\n                           sim_dt=0.002,        # write every 0.002 time units\n                           max_writes=100)      # writes per file set\n\nadd_task!(handler, T; name=\"temperature\")   # pass the FIELD OBJECT\nadd_task!(handler, u; name=\"velocity\")      # a VectorField writes all components\n\nrun!(solver; stop_iteration=10, progress=false)\n\nThis writes output/snapshots_s1/snapshots_s1.nc: the directory part of the base path is the output root, the file name part names the file set. When max_writes is exceeded the handler rolls over to output/snapshots_s2/…. (A base path with no directory part, \"snapshots\", uses the name itself as the root, i.e. snapshots/snapshots_s1/… — passing a directory is clearer.)\n\nTasks must be field or operator objects. A string expression (add_task!(handler, \"u*u\")) is accepted silently and then writes zeros — build the operator instead: add_task!(handler, u ⋅ u; name=\"u2\").\n\nOn a CUDA run the same API stages each unique field/layout to a CPU array once per write, because NetCDF cannot write a CuArray directly. This is a bulk, synchronizing device-to-host copy; it does not use scalar GPU indexing and it does not move the solver field itself off the GPU. Grid- and coefficient-layout tasks are both supported. A completed timestep's algebraic constraints are not re-solved by the writer, so producing a snapshot is observational and leaves the live solver state unchanged.","title":"Handler bound to the solver"},{"category":"section","location":"tutorials/analysis_and_output/#Handler-bound-to-the-distributor","page":"Analysis & Output","text":"The dist method takes a vars Dict and does not register itself with a solver. If you use it, hand it to run! via outputs= (or call process! yourself):\n\nhandler_dist = add_file_handler(\"output/snapshots_dist\", dist, Dict(\"T\" => T);\n                                parallel=\"virtual\", iter=5, max_writes=100)\nadd_task!(handler_dist, T; name=\"temperature\")\n\nrun!(solver; stop_iteration=20, outputs=[handler_dist], progress=false)\n\nForgetting outputs= is a silent no-op: the run completes and no file appears.","title":"Handler bound to the distributor"},{"category":"section","location":"tutorials/analysis_and_output/#Output-Modes","page":"Analysis & Output","text":"h_gather  = add_file_handler(\"output/snap_gather\",  solver; parallel=\"gather\")\nh_virtual = add_file_handler(\"output/snap_virtual\", solver; parallel=\"virtual\")\n\nOn the CPU NetCDF path, parallel only decides the file layout in the serial case: \"gather\" with one process writes a single file, output/snap_gather_s1/snap_gather_s1.nc. With more than one process — in either mode — every rank writes its own file, …_s1/…_s1_p0.nc, …_s1_p1.nc, …, each holding that rank's slab: on two ranks of a 16×16 grid, a field task is (nwrites, 16, 8) in each of the two files. The reduction tasks (add_mean_task! and friends) are the exception — they reduce globally, so every rank writes the same global result.\n\nTo reassemble the per-rank files into one dataset, use scripts/merge_netcdf.jl.","title":"Output Modes"},{"category":"section","location":"tutorials/analysis_and_output/#Output-Frequency","page":"Analysis & Output","text":"The handler owns its schedule; you do not gate it from the loop. The scheduling keywords are sim_dt (simulation time), iter (iterations) and wall_dt (wall-clock seconds); a write happens whenever one of them comes due.\n\nh1 = add_file_handler(\"output/snaps_simdt\", solver; sim_dt=0.002)  # every 0.002 sim-time\nh2 = add_file_handler(\"output/timeseries\",  solver; iter=5)        # every 5 iterations\nh3 = add_file_handler(\"output/restarts\",    solver; wall_dt=60.0)  # every 60 wall seconds\n\nIf you drive the loop yourself instead of using run!, call process! each step and let the handler decide. Set a stop criterion on the solver first: a fresh solver has stop_iteration = typemax(Int) and stop_sim_time = Inf, so proceed is true forever and the loop below never exits. (run! sets these for you from its keywords.)\n\nsolver.stop_iteration = 5      # or solver.stop_sim_time = 0.01\n\nwhile proceed(solver)\n    step!(solver)\n    process!(handler)   # no-op unless the schedule says \"write\"\nend","title":"Output Frequency"},{"category":"section","location":"tutorials/analysis_and_output/#Reading-the-Output-Back","page":"Analysis & Output","text":"The files use NetCDF-4 groups, so a bare ncread(file, \"temperature\") does not find the variable. The groups are vars, time and grids:\n\nf = \"output/snapshots_s1/snapshots_s1.nc\"\n\nTarang.group_variable_names(f, \"vars\")     # [\"temperature\", \"velocity\", ...]\ngroup_ncread(f, \"vars\", \"temperature\")     # (nwrites, 16, 16)\ngroup_ncread(f, \"vars\", \"velocity\")        # (nwrites, 2, 16, 16) — component axis first\ngroup_ncread(f, \"time\", \"sim_time\")        # (nwrites,)\ngroup_ncread(f, \"grids\", \"x\")              # (16,)","title":"Reading the Output Back"},{"category":"section","location":"tutorials/analysis_and_output/#Analysis-Tasks","page":"Analysis & Output","text":"","title":"Analysis Tasks"},{"category":"section","location":"tutorials/analysis_and_output/#Computing-Means","page":"Analysis & Output","text":"dims are the dimensions averaged over:\n\n# Average over x, leaving a profile in z\nadd_mean_task!(handler, T; dims=1, name=\"T_mean_x\")\n\n# Average over z, leaving a profile in x\nadd_mean_task!(handler, T; dims=2, name=\"T_mean_z\")\n\n# Full spatial average (scalar)\nadd_mean_task!(handler, T; dims=(1,2), name=\"T_avg\")","title":"Computing Means"},{"category":"section","location":"tutorials/analysis_and_output/#Extracting-Slices-and-Profiles","page":"Analysis & Output","text":"# Slice at a specific index along a dimension\nadd_slice_task!(handler, T; dim=2, idx=1,  name=\"T_bottom\")\nadd_slice_task!(handler, T; dim=2, idx=16, name=\"T_top\")\n\n# Profile: mean over every dimension EXCEPT `dim` (singular — the axis KEPT)\nadd_profile_task!(handler, T; dim=2, name=\"T_profile_z\")","title":"Extracting Slices and Profiles"},{"category":"section","location":"tutorials/analysis_and_output/#Custom-Analysis","page":"Analysis & Output","text":"postprocess is any function of the task's data array; its return value is written in place of the field:\n\nhalf_sumsq(data) = 0.5 * sum(abs2, data)\n\nadd_task!(handler, T; name=\"T_half_sumsq\", postprocess=half_sumsq)\n\nwarning: postprocess sees the rank-local slab\nUnder MPI a custom postprocess receives only this rank's data, so a reduction written that way is per-rank, not global. The built-in add_mean_task!, add_profile_task!, add_rms_task!, add_variance_task! and add_extrema_task! are MPI-aware and reduce globally.","title":"Custom Analysis"},{"category":"section","location":"tutorials/analysis_and_output/#Global-Diagnostics","page":"Analysis & Output","text":"","title":"Global Diagnostics"},{"category":"section","location":"tutorials/analysis_and_output/#CFL-Condition","page":"Analysis & Output","text":"CFL is constructed from the solver (not the problem), and velocities are registered as VectorFields. Every knob is a keyword with a default: initial_dt=0.01, cadence=1, safety=0.4, threshold=0.1, max_change=2.0, min_change=0.5, max_dt=Inf.\n\ncfl = CFL(solver; initial_dt=1e-3, cadence=5, safety=0.5,\n          max_change=1.5, min_change=0.5, max_dt=0.01)\n\nadd_velocity!(cfl, u)          # VectorField only\n\n# Hand it to run!: the loop sets solver.dt from the CFL condition each step\nrun!(solver; stop_iteration=30, cfl=cfl, progress=false)\n\ncompute_timestep(cfl) returns the proposed dt if you drive the loop yourself (solver.dt = compute_timestep(cfl); step!(solver)). It recomputes only every cadence-th call, and commits a change only when it exceeds threshold (relative), so the implicit LHS factorization is not rebuilt on every tiny drift.","title":"CFL Condition"},{"category":"section","location":"tutorials/analysis_and_output/#Flow-Statistics","page":"Analysis & Output","text":"MPI reductions have one trap: a reduction over a whole PencilArray (what get_grid_data returns when nprocs > 1) is already collective. Reduce the rank-local storage with parent(...) and do exactly one Allreduce:\n\nusing MPI\n\nreducer = GlobalArrayReducer(dist.comm)\n\nensure_layout!(T, :g)\nTd = parent(get_grid_data(T))                            # rank-local storage\n\nT_max = reduce_scalar(reducer, maximum(abs, Td), MPI.MAX)\nT_l2  = sqrt(reduce_scalar(reducer, sum(abs2, Td), MPI.SUM))\n\nThe bundled helpers already do exactly that, and are usually what you want:\n\nglobal_max(T)        # Float64, identical on every rank\nglobal_sum(T)\nintegrate(T)         # quadrature-weighted domain integral\n\nfunction total_energy(field)\n    ensure_layout!(field, :g)\n    return 0.5 * global_sum(field.dist, abs2.(parent(get_grid_data(field))))\nend","title":"Flow Statistics"},{"category":"section","location":"tutorials/analysis_and_output/#Reynolds-Number","page":"Analysis & Output","text":"reynolds_number(u, ν, L) is built in. It is based on the maximum velocity magnitude, Re = max|u| · L / ν, and reduces across ranks:\n\nRe = reynolds_number(u, 0.05, 2pi)","title":"Reynolds Number"},{"category":"section","location":"tutorials/analysis_and_output/#Nusselt-Number","page":"Analysis & Output","text":"For thermal convection there is no built-in, so reduce explicitly:\n\nfunction compute_nusselt(T::ScalarField, uz::ScalarField, L, kappa, dT)\n    ensure_layout!(T, :g)\n    ensure_layout!(uz, :g)\n    Td  = parent(get_grid_data(T))\n    uzd = parent(get_grid_data(uz))\n\n    # Convective heat flux ⟨T·u_z⟩, averaged over the global grid\n    r    = GlobalArrayReducer(T.dist.comm)\n    npts = reduce_scalar(r, Float64(length(Td)), MPI.SUM)\n    flux = reduce_scalar(r, sum(Td .* uzd), MPI.SUM) / npts\n\n    # Nusselt = total flux / conductive flux, with dT the temperature difference\n    return 1.0 + flux * L / (kappa * dT)\nend\n\nNu = compute_nusselt(T, u.components[2], 2pi, 0.05, 1.0)","title":"Nusselt Number"},{"category":"section","location":"tutorials/analysis_and_output/#Energy-Spectra","page":"Analysis & Output","text":"Use the built-in power_spectrum — no need to hand-roll wavenumber binning. It is the radially-binned shell spectrum of a ScalarField whose axes are all Fourier; a Chebyshev axis is not supported and throws Unsupported field dimensions: 2 with 1 Fourier bases. It returns a NamedTuple (k, power, bin_counts, bin_edges), where k holds the bin centres in physical wavenumber.\n\nps = power_spectrum(T)                  # T::ScalarField, every axis Fourier\nps.k                                    # bin centres: [0.5, 1.5, 2.5, ...]\nps.power                                # binned power\nps.bin_counts                           # modes per bin\n\nThere is no power_spectrum(::VectorField) method — for a velocity field use the vector spectra, which return the same NamedTuple:\n\nes = energy_spectrum(u)                 # → (k, power, bin_counts, bin_edges)\nen = enstrophy_spectrum(u)              # vorticity spectrum, same shape\n\nWatch the return types of the related diagnostics: kinetic_energy returns the field ½ρ|u|², not a number.\n\nke_field = kinetic_energy(u)            # ScalarField\nke_total = total_kinetic_energy(u)      # Float64\n\nSee the Analysis API for options (max_wavenumber, radial_average, binning).","title":"Energy Spectra"},{"category":"section","location":"tutorials/analysis_and_output/#Time-Series","page":"Analysis & Output","text":"","title":"Time Series"},{"category":"section","location":"tutorials/analysis_and_output/#Storing-Scalar-Time-Series","page":"Analysis & Output","text":"using DelimitedFiles\n\n# Initialize storage\ntimes = Float64[]\nenergies = Float64[]\nnusselts = Float64[]\n\n# Record diagnostics via a run! callback (Int interval = every N iterations;\n# use a Float interval for every-Δt of sim-time).\n# total_energy / compute_nusselt are the helpers defined under Global Diagnostics.\nrun!(solver; stop_iteration=40, progress=false,\n     callbacks=[2 => function (s)\n         push!(times, s.sim_time)\n         push!(energies, total_energy(T))\n         push!(nusselts, compute_nusselt(T, u.components[2], 2pi, 0.05, 1.0))\n     end])\n\n# Save to file (plain vectors → any format; here a CSV via the stdlib)\nif dist.rank == 0\n    writedlm(\"diagnostics.csv\", [times energies nusselts], ',')\nend","title":"Storing Scalar Time Series"},{"category":"section","location":"tutorials/analysis_and_output/#Visualization-Integration","page":"Analysis & Output","text":"Tarang depends on no plotting package, so it cannot draw a figure for you. It does export plot_1d, plot_2d, plot_vector_field and plot_streamlines, but these render nothing: plot_2d(T) logs the data ranges and hands back a PlotData struct (x, y, data, labels) for you to feed to a real backend.\n\nFor an actual plot, take the grid data — a plain array — and give it to whatever package you already use. Convert with Array first: under MPI get_grid_data returns this rank's PencilArray slab, and a plot of it is a plot of one slab only.\n\nensure_layout!(T, :g)\ndata = Array(get_grid_data(T))    # dense (16, 16) Matrix{Float64}, ready to plot\n\ndata' is then the usual (x horizontal, z vertical) orientation for a heatmap.","title":"Visualization Integration"},{"category":"section","location":"tutorials/analysis_and_output/#Checkpointing","page":"Analysis & Output","text":"Tarang has no built-in checkpoint type — write a small helper over the evolving state fields (solver.state, each a ScalarField with a .name) and plain NetCDF. Grid space is exact and real-valued, so it round-trips losslessly.","title":"Checkpointing"},{"category":"section","location":"tutorials/analysis_and_output/#Saving-State","page":"Analysis & Output","text":"using NetCDF\n\nfunction save_checkpoint(solver, path)\n    isfile(path) && rm(path)\n    for f in solver.state\n        ensure_layout!(f, :g)\n        g = get_grid_data(f)\n        dimspec = collect(Iterators.flatten(\n            (\"$(f.name)_d$i\" => s for (i, s) in enumerate(size(g)))))\n        nccreate(path, f.name, dimspec...; t=NC_DOUBLE)   # NC_DOUBLE: keep Float64\n        ncwrite(g, path, f.name)\n    end\n    ncputatt(path, \"Global\", Dict(\"sim_time\" => solver.sim_time,\n                                  \"iteration\" => solver.iteration, \"dt\" => solver.dt))\n    return path\nend","title":"Saving State"},{"category":"section","location":"tutorials/analysis_and_output/#Loading-State","page":"Analysis & Output","text":"function load_checkpoint!(solver, path)\n    for f in solver.state\n        ensure_layout!(f, :g)                 # ensure the grid buffer exists / layout is :g\n        get_grid_data(f) .= ncread(path, f.name)\n    end\n    solver.sim_time  = ncgetatt(path, \"Global\", \"sim_time\")\n    solver.iteration = Int(ncgetatt(path, \"Global\", \"iteration\"))\n    solver.dt        = ncgetatt(path, \"Global\", \"dt\")\n    return solver\nend\n\nUse solver.state (the integrator's live fields), not the problem-variable handles. For a vector variable the state holds its components, named u_x, u_z, … Restart then continues from run! as usual, and reproduces the uninterrupted trajectory to the bit:\n\nsave_checkpoint(solver, \"chk.nc\")\n# … later, in a fresh session with the same problem/solver built …\nload_checkpoint!(solver, \"chk.nc\")\nrun!(solver; stop_iteration=solver.iteration + 20, cfl=cfl, progress=false)\n\nwarning: The state and your field handles are the same object only until the first step\nOn a freshly built solver solver.state[1] === T. After the first step they are distinct objects holding equal data: the stepper rebinds solver.state to its own history buffer and then copies from the state back into the problem variables. The sync is one-way. So once a run has started, T is a read-only view for diagnostics — writing into it (to perturb, reset, or restore a field) is silently discarded on the next step. Write to solver.state instead, which is what the load_checkpoint! above does.\n\nnote: MPI\nget_grid_data is the rank-local slab, so this helper is per-rank/serial. Under MPI either write one file per rank (include the rank in path), or gather to rank 0 with gather_array(f.dist, get_grid_data(f)) before writing and scatter on load.","title":"Loading State"},{"category":"section","location":"tutorials/analysis_and_output/#Complete-Example","page":"Analysis & Output","text":"using Tarang\nusing MPI\nusing Printf\n\nMPI.Init()\nrank = MPI.Comm_rank(MPI.COMM_WORLD)\n\n# Setup\ncoords = CartesianCoordinates(\"x\", \"z\")\ndist   = Distributor(coords; dtype=Float64, device=CPU())\nxbasis = RealFourier(coords[\"x\"]; size=16, bounds=(0.0, 2pi), dealias=3/2)\nzbasis = RealFourier(coords[\"z\"]; size=16, bounds=(0.0, 2pi), dealias=3/2)\ndomain = Domain(dist, (xbasis, zbasis))\n\nT = ScalarField(domain, \"T\")\nu = VectorField(domain, \"u\")\n\nproblem = IVP([T, u])\nadd_parameters!(problem, kappa=0.05, nu=0.05)\nadd_equation!(problem, \"∂t(T) - kappa*lap(T) = -u⋅∇(T)\")\nadd_equation!(problem, \"∂t(u) - nu*lap(u) = 0\")\n\nxg, zg = local_grids(dist, xbasis, zbasis)\nensure_layout!(T, :g); get_grid_data(T) .= sin.(xg) .* cos.(zg'); ensure_layout!(T, :c)\nux, uz = u.components\nensure_layout!(ux, :g); get_grid_data(ux) .= 0.5;             ensure_layout!(ux, :c)\nensure_layout!(uz, :g); get_grid_data(uz) .= 0.2 .* sin.(xg); ensure_layout!(uz, :c)\n\n# Create solver\nsolver = InitialValueSolver(problem, RK222(); dt=1e-3)\n\n# CFL — takes the SOLVER, not the problem\ncfl = CFL(solver; initial_dt=1e-3, safety=0.5, max_dt=0.01)\nadd_velocity!(cfl, u)\n\n# Output handler — pass the SOLVER so the handler auto-registers and `run!`\n# processes it at its `sim_dt` cadence (no manual `process!` in the loop). No\n# `vars` Dict needed: T is a problem variable, already in the solver namespace.\nhandler = add_file_handler(\"output/snapshots\", solver; parallel=\"gather\", sim_dt=0.1, max_writes=100)\nadd_task!(handler, T; name=\"temperature\")            # field snapshot\nadd_mean_task!(handler, T; dims=1, name=\"T_mean\")    # x-averaged profile\n\n# Diagnostics storage (time series accumulated in a callback)\ntimes, max_T = Float64[], Float64[]\n\n# run! drives the whole loop: CFL-adaptive dt, auto-writes the registered handler,\n# runs callbacks, and closes the handler at the end. A Float callback interval\n# (0.1) fires every 0.1 sim-time units; an Int interval fires every N iterations.\n# global_max is MPI-reduced, so the value is identical on every rank.\nrun!(solver; stop_time=1.0, cfl=cfl, progress=false,\n     callbacks=[0.1 => function (s)\n         push!(times, s.sim_time)\n         push!(max_T, global_max(T))\n         rank == 0 && @printf(\"t = %.3f, max|T| = %.6e\\n\", s.sim_time, max_T[end])\n     end])\n\nMPI.Finalize()","title":"Complete Example"},{"category":"section","location":"tutorials/analysis_and_output/#See-Also","page":"Analysis & Output","text":"Analysis API: Full API reference\nI/O API: NetCDF output documentation\nParallelism: Parallel I/O configuration","title":"See Also"}]
}
