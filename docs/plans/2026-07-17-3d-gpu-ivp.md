# 3D GPU IVP Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Verify and fix single-GPU 3D IVPs for fully periodic and Fourier--Fourier--Chebyshev domains with registered stochastic forcing and bounded warmed device allocations.

**Architecture:** Add small CPU/GPU oracle tests before changing production code. Keep fully periodic states on the field-native RK path and mixed states on per-mode `CuSparseLU` subproblems; remove only dimension-specific failures found by those tests.

**Tech Stack:** Julia 1.10--1.12, Tarang fields and RK222, CUDA/CUFFT/CUSOLVER, KernelAbstractions, Julia `Test`.

### Task 1: Fully periodic 3D GPU IVP

**Files:**
- Modify: `test/test_gpu_transform_correctness.jl`
- Modify production files only if the failing test identifies a 3D defect

**Step 1: Write the failing test**

Add an `8³` `RealFourier × RealFourier × RealFourier` scalar diffusion problem.
Run the same analytic initial condition on CPU and GPU for several RK222 steps.
Assert CPU/GPU coefficient agreement, GPU field/workspace residency, finite
output, and no global CPU matrices on the GPU path. Register a small 3D
`StochasticForcing` and assert its cache is a `CuArray`, is fixed within a
timestep, changes across steps, and matches the real-FFT coefficient shape.

**Step 2: Run RED**

Run:

```bash
TARANG_USE_MPI=0 TARANG_RUN_GPU_TESTS=true julia --project=. test/test_gpu_transform_correctness.jl
```

Expected on CUDA CI: the first unsupported 3D dispatch, residency, correctness,
or allocation assertion fails. CPU-only hosts skip cleanly.

**Step 3: Implement the minimal fix**

Remove only the exposed 2D assumption. Reuse existing architecture-aware field,
transform, forcing, and RK buffers. Do not introduce a separate 3D timestepper.

**Step 4: Run GREEN and commit**

Run the focused GPU file and relevant CPU forcing/RK tests. Commit as
`fix: support fully periodic 3D GPU IVPs`.

### Task 2: Forced Fourier--Fourier--Chebyshev GPU IVP

**Files:**
- Modify: `test/test_gpu_transform_correctness.jl`
- Modify: `test/test_separable_stochastic_forcing.jl`
- Modify production files identified by the failing test

**Step 1: Write the failing CPU and conditional CUDA tests**

Build an `8 × 8 × 10` scalar diffusion/tau problem with bases
`(RealFourier, RealFourier, ChebyshevT)` and profile `z -> z*(1-z)`. Register a
two-Fourier-dimensional `SeparableStochasticForcing`. Run seeded CPU and GPU
problems for several RK222 steps.

Assert:

- GPU construction selects `CuSparseLU`;
- CPU/GPU coefficients agree;
- forcing and all full-field/subproblem/sparse-solve buffers are device arrays;
- forcing is stage-constant and timestep-varying; and
- CUDA scalar indexing remains disabled.

**Step 2: Run RED**

Run the focused CPU and GPU files. Expected: a concrete 3D mixed dispatch,
shape, solve, or residency assertion fails on CUDA.

**Step 3: Implement the minimal dimension-generic fix**

Generalize only the failing shape/view, gather/scatter, transform, or solver
operation. Preserve persistent buffers and the Fourier-only random realization
used by `SeparableStochasticForcing`.

**Step 4: Run GREEN and commit**

Commit as `fix: support mixed-basis 3D GPU IVPs`.

### Task 3: Warmed 3D allocation regression

**Files:**
- Modify: `test/test_gpu_transform_correctness.jl`
- Modify allocation-owning production files only after a failing assertion

**Step 1: Add the failing allocation assertions**

Warm both 3D GPU solvers, synchronize CUDA, sample `CUDA.alloc_stats.alloc_bytes`,
advance one more step, and assert zero new device bytes. If a CUDA library has an
unavoidable internal allocation, measure two resolutions and enforce a fixed,
resolution-independent ceiling instead.

**Step 2: Run RED, implement persistent buffers, and run GREEN**

Move any exposed full-field or per-subproblem temporary into the appropriate
solver, timestepper, transform, or forcing cache. Re-run both 3D tests and the
existing 2D allocation tests.

**Step 3: Commit**

Commit as `perf: bound 3D GPU IVP allocations`.

### Task 4: Documentation and final verification

**Files:**
- Modify: `docs/src/pages/stochastic_forcing.md`
- Modify: `docs/src/api/stochastic_forcing.md` only if API behavior changes

**Step 1: Document supported 3D layouts**

State that single-GPU IVPs support Fourier--Fourier--Fourier through
`StochasticForcing` and Fourier--Fourier--Chebyshev through
`SeparableStochasticForcing`, including solver selection and allocation behavior.

**Step 2: Run final verification**

```bash
julia --project=. test/test_separable_stochastic_forcing.jl
julia --project=. test/test_stochastic_forcing.jl
julia --project=. test/test_solvers.jl
julia --project=. test/test_subproblem_rk.jl
julia --project=. test/test_gpu_field_rk_allocations.jl
TARANG_USE_MPI=0 TARANG_RUN_GPU_TESTS=true julia --project=. test/test_gpu_transform_correctness.jl
git diff --check
```

CUDA assertions must pass on a CUDA host or skip cleanly locally. Commit the
documentation, push the branch, and update PR #58 with the new 3D scope and CI
status.
